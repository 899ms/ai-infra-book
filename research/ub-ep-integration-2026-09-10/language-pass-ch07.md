# 第 7 章语言通读：逐句修改对照

共 134 处，按正文顺序分三批写入。左列为修改前，右列为修改后。

| 修改前 | 修改后 |
|---|---|
| 先在超节点内合并各副本的梯度，再跨节点交换，与第 5 章先在局部累加部分和、以减少外部搬运的做法遵循同一原则。局部合并本身也要花时间，只有它省下的远端传输和等待更多，这种安排才划算。 | 先在超节点内合并各副本的梯度，再跨节点交换，与第 5 章先在局部累加部分和、减少外部搬运的做法是同一个道理。局部合并本身也要花时间，只有省下的远端传输和等待多于这些时间，这样安排才划算。 |
| 全章围绕两台服务器、每台四个通信进程展开 | 全章围绕两台服务器、每台四张卡展开 |
| 我们还会比较低并发推理中的小消息，说明同一网络在不同负载下应当优先优化什么。 | 还会比较低并发推理中的小数据量传输，说明同一个网络在不同负载下应当优先优化什么。 |
| KV-Direct 研究由可编程网卡直接处理键值存储请求，1Pipe 研究用网络提供全局操作顺序，简化分布式程序的协作，Unified Bus（UB）提供统一的设备互联。笔者在参与这些研究与设计时，也一直在思考三个问题：怎样减少数据移动，怎样在等待一次操作时执行其他工作，怎样在简化编程的同时避免系统停滞。 | KV-Direct 让可编程网卡直接处理键值存储请求，1Pipe 用网络提供全局的操作顺序来简化分布式程序的协作，Unified Bus（UB）提供统一的设备互联。笔者参与这些研究和设计时，一直在想三个问题：怎样减少数据搬运，怎样在等待一次操作时做别的工作，怎样在简化编程的同时不让系统停摆。 |
| 为比较这些通信方式，本章使用一个由八个通信进程组成的通信组，每个进程对应一张加速卡。集合通信由一组进程共同执行数据收集、分发或归约等操作，通常将这些进程称为参与者，用 rank 编号区分。参与者 0–3 位于服务器 A，4–7 位于服务器 B。加速器间的本地互联带宽为 200 GB/s；每台服务器有两张 25 GB/s 网卡（network interface card，NIC），两张网卡共用的发送接口和接收接口各有 40 GB/s 带宽。交换网络可以承载两台服务器之间的双向流量。大块集合通信每轮的启动时间为 2 μs。 | 为了比较这些通信方式，本章用一个八张卡的通信组，每张卡上运行一个通信进程。集合通信由一组进程共同完成收集、分发或归约，本章把这些进程称为参与者，用编号（rank）区分。参与者 0 到 3 在服务器 A，4 到 7 在服务器 B。服务器内卡之间的本地互联带宽为 200 GB/s；每台服务器有两张 25 GB/s 的网卡（network interface card，NIC），两张网卡共用的发送接口和接收接口各有 40 GB/s 带宽。交换网络能承载两台服务器之间的双向流量。大块集合通信每轮的启动时间为 2 μs。 |
| 本章沿用第 6.1.4 节的并行方式一览表，不再逐项定义并行方式。优先把频繁的 TP 归约留在较快互联内，把 DP 的可分层梯度同步或 PP 的阶段交接放到外部，是应当首先计算的一类候选方案；若本地容量不足、CP 需要更大范围的上下文，或 EP 路由跨越多个节点，就必须扩大通信范围。是否采用这种放置，要由实际流量和关键路径决定。 | 本章沿用第 6.1.4 节的并行方式一览表，不再逐一定义。把频繁的张量并行归约留在较快的互联内，把数据并行的梯度同步或流水线并行的阶段交接放到服务器之间，是应当首先计算的一类候选方案；若本地容量不够，上下文并行需要更大范围的上下文，或者专家并行的路由跨越多个节点，通信范围就必须扩大。是否这样放置，要由实际流量和关键路径决定。 |
| 只知道网络是 InfiniBand 还是 RoCE，也推不出有效带宽、消息延迟、超售比、在途窗口和故障恢复时间。后文用明确给定的网络参数计算，实际部署时再换成对应系统的测量值。 | 只知道网络是 InfiniBand 还是 RoCE，推不出有效带宽、传输延迟、超售比、在途窗口和故障恢复时间。后文用明确给定的网络参数计算，实际部署时换成对应系统的测量值。 |
| 下联总带宽为 400 GB/s、上联总带宽为 200 GB/s 时，**超售比**即下联总带宽与上联总带宽的比值，此处为 2:1。这些总带宽为 200 GB/s 的上行链路，构成了离开本叶交换机的流量必须经过的割集。 | 叶交换机的下联总带宽为 400 GB/s、上联总带宽为 200 GB/s 时，两者之比 2:1 称为**超售比**。这些合计 200 GB/s 的上联链路，就是离开这台叶交换机的流量必须经过的割集。 |
| 一次本地访存和一次远程消息，在程序中表达相似的数据依赖，却经过不同的提交与完成路径。 | 一次本地访存和一次远程传输，在程序里表达的是相似的数据依赖，走的却是不同的提交和完成路径。 |
| 回顾第六章，**AllReduce** | 回顾第 6 章，**AllReduce** |
| 数据并行通过归约汇合各参与者的梯度。张量并行（TP）则把一层的计算分给多个参与者，后续算子经常需要等局部结果汇合后才能开始，因此层内通信会直接推迟后续计算。流水线并行（PP）则按阶段切开模型，把跨服务器交接集中到阶段边界。两种方式下，数据跨越服务器边界的次数不同。 | 数据并行通过归约汇合各参与者的梯度。张量并行把一层的计算分给多个参与者，后面的算子往往要等局部结果汇合后才能开始，所以层内通信会直接推迟后续计算。流水线并行按阶段切开模型，把跨服务器的交接集中在阶段边界。两种方式下，数据跨越服务器边界的次数不同。 |
| 消息远大于这个值时，传输时间占主导，提高带宽更有效；消息远小于这个值时，减少请求次数更有效。 | 数据量远大于这个值时，传输时间占主导，提高带宽更有效；远小于这个值时，减少传输次数更有效。 |
| 消息模型给出了每次交接的代价；对 PP 还要说明多次交接怎样与计算穿插。PP 将连续若干层放在同一阶段 | 上面的式子给出了每次交接的代价；对流水线并行还要看多次交接怎样与计算穿插。流水线并行把连续若干层放在同一阶段 |
| 若每次分派单独传送，dispatch 为 $nkfd$，加上返回并合并专家结果（combine）为 $2nkfd$。把热门专家放在发送者一侧，降低的是 $f$；改变激活的数据类型或编码方式，改变的是 $d$。 | 若每次分派单独传送，派发的传输量为 $nkfd$，加上返回并合并（combine）共 $2nkfd$。把热门专家放到发送方所在的服务器，降低的是 $f$；改变激活的数据类型或编码，改变的是 $d$。 |
| 将源加速器到目标加速器的载荷记为 $V_{ij}$。一行的和是对应发送端的发送量，一列的和是对应接收端的接收量；将跨越某个割集的元素相加，得到该割集的传输量。把这些量分别除以相应带宽，再取最大值，就是第 7.1 节资源模型在不规则交换上的应用。 | 把从卡 $i$ 发往卡 $j$ 的载荷记为 $V_{ij}$。同一行相加是发送方的发送量，同一列相加是接收方的接收量；把跨越某个割集的元素相加，就是这个割集的传输量。把这些量各自除以对应的带宽，再取最大值，就是第 7.1 节的资源模型在不规则交换上的应用。 |
| 这个矩阵还说明了 dispatch 与 combine 的热点是怎样对应的。若按 token—专家逐份发送、返回相同大小向量，且没有目标端合并，返回矩阵就是发送矩阵的转置：dispatch 的热点接收端会成为 combine 的热点发送端。两次通信的总字节数相同，也不代表两段耗时相同：返回数据必须等专家算完才就绪，方向可用带宽、数据格式与归约位置也可能不同。若 dispatch 使用 FP8、combine 使用 BF16，返回载荷约为分发载荷的两倍，量化尺度等元数据另行计算。第 9.4 节将把这些流量与计算就绪时间放到同一个算例中。 | 这个矩阵还说明了派发与合并的热点是怎样对应的。若按 token 到专家逐份发送、返回同样大小的向量，且接收端不做合并，返回矩阵就是发送矩阵的转置：派发时的热点接收方，就是合并时的热点发送方。两次通信的总字节数相同，不代表两段耗时相同：返回的数据要等专家算完才就绪，两个方向的可用带宽、数据格式和归约位置也可能不同。若派发用 FP8、合并用 BF16，返回的载荷约为派发的两倍，量化尺度等元数据另计。第 9.4 节会把这些流量与计算就绪时间放进同一个算例。 |
| 专家放置因此有两项互相联系的目标：让更多分派留在本地，让远端分派分散到有余量的端点。复制热门专家用额外容量减少远端访问；将发往同一目标加速器上多个专家的输入合并，可以先传一次激活，再在目标端展开分派。这两种方法都减少了相应路径上的传输量，从而缩短通信时间。 | 专家放置因此有两项互相联系的目标：让更多分派留在本地，让远端分派分散到有余量的接收方。复制热门专家是用额外的容量换少走远端；发往同一张卡上多个专家的输入可以只传一次激活，到了那张卡再分给各个专家。两种办法都减少了相应路径上的传输量。 |
| 以前述两张 25 GB/s NIC 的配置为例。如果只使用一张，连续环十四轮共需约 14.1 ms；将每次传送的数据拆成两个不重叠的区间，分别交给两张 NIC，吞吐升至共享出口允许的 40 GB/s，时间降至约 8.8 ms。第三张 NIC 加入同一个 40 GB/s 接口，无法再缩短这段传输时间。共享接口带宽耗尽后，增加网卡便不再提高吞吐率。 | 以前面两张 25 GB/s 网卡的配置为例。只用一张时，连续环十四轮约需 14.1 ms；把每次要传的数据拆成两段，分别交给两张网卡，吞吐升到共享出口允许的 40 GB/s，时间降到约 8.8 ms。第三张网卡接在同一个 40 GB/s 接口上，不能再缩短这段传输。共享接口的带宽用尽之后，再加网卡也不提高吞吐。 |
| 另一种情况是与某张 GPU 直接连接的 NIC 忙碌，相邻 GPU 的 NIC 空闲。借用相邻出口要先把数据送到相邻 GPU，因此又引入一段本地传输。FuseLink 利用这种路径，通过机内 GPU 互联和网络缓冲重映射使用空闲 NIC。 | 另一种情况是某张 GPU 直连的网卡正忙，相邻 GPU 的网卡却空闲。借用相邻的出口要先把数据送到相邻 GPU，多了一段本地传输。FuseLink 就是走这条路：通过机内 GPU 互联和网络缓冲区的重新映射来使用空闲网卡。 |
| 下面比较直接发送和经相邻 GPU 中继两种方式。直接连接的 NIC 带宽为 40 GB/s，两张可借用 NIC 的带宽也各为 40 GB/s； | 下面比较直接发送和经相邻 GPU 中继两种方式。直连网卡的带宽为 40 GB/s，两张可借用的网卡也各为 40 GB/s； |
| $B_D\le40$ 时直接连接的 NIC 已能用满下游带宽 | $B_D\le40$ 时直连网卡已能用满下游带宽 |
| 无论是归约分层、专家放置还是多 NIC 中继，分析步骤相同： | 无论是分层归约、专家放置还是多网卡中继，分析步骤相同： |
| **单边读写**让发起者访问预先授权的远端内存。例如卡 0 可以向卡 4 的接收区域写入分片，由对端 NIC 完成数据移动。**双边消息**需要发送方与接收方配合。接收方预先提交接收请求并提供缓冲区，通信系统将到达的消息与相应请求匹配。已知目标位置的大块分片适合直接读写；来自多个发送者的通知则适合放进消息队列，由队列区分各条通知并交给相应的数据使用方（消费者）。 | **单边读写**让发起方直接访问预先授权的远端内存，例如卡 0 把分片写进卡 4 的接收区域，数据搬运由对端网卡完成。**双边消息**要发送方和接收方配合：接收方预先提交接收请求并提供缓冲区，通信系统把到达的消息与相应的请求配对。已知目标位置的大块分片适合直接读写；来自多个发送方的通知则适合放进消息队列，由队列区分各条通知并交给相应的数据使用方，下文称为消费者。 |
| 把写入和通知合并为一次提交，还能减少重复发起。消费者收到并确认通知之后开始归约，两项操作的正确先后关系将在第 7.4 节展开。 | 把写入和通知合并成一次提交，还能少发起一次。消费者收到通知后开始归约；写入与通知的先后关系在第 7.4 节展开。 |
| 写入和通知完成了数据交接。如果还希望接收方按指定方式处理数据，就可以使用**远程过程调用**（remote procedure call，RPC）。它要求对端执行指定函数。一次调用包括参数传送、远端执行和结果返回。 | 写入和通知完成了数据交接。若还要接收方按指定方式处理数据，就用**远程过程调用**（remote procedure call，RPC）：请求对端执行指定的函数，一次调用包括传参数、远端执行和返回结果。 |
| 上一小节从接收方看到的操作出发，区分了读写、消息与 RPC。现在沿相反方向看发送端：谁最先知道数据已经就绪，谁又负责提交请求？卡 0 的分片在 GPU 上产生。若 CPU 负责提交通信，GPU 先报告就绪，CPU 构造请求并提交 NIC，完成后再将状态交还 GPU。这条控制路径包含多次交接，即使载荷从未经过 CPU 内存，交接仍要消耗时间。 | 上一小节从接收方的角度区分了读写、消息与 RPC。现在换到发送方：谁最先知道数据已经就绪，谁负责提交请求？卡 0 的分片在 GPU 上产生。若由 CPU 提交通信，GPU 先报告就绪，CPU 构造请求交给网卡，完成后再把状态交还 GPU。这条控制路径要交接多次，即使载荷从不经过 CPU 内存，交接本身也要花时间。 |
| NVIDIA 的 **GPUDirect RDMA** 让 NIC 直接访问已经注册和授权的 GPU 内存，省去载荷经主机内存中转的步骤。采用 CPU 发起通信的实现时，请求提交和完成通知处理仍由 CPU 负责。由加速器发起通信则进一步让 GPU 构造或触发请求，并读取完成状态，使 GPU 在生成数据后即可直接发起发送。NVLink 是 NVIDIA 的加速器互联，UB 是 Unified Bus 统一互联；URMA 是 UB 提供的统一远程内存访问接口。 | NVIDIA 的 **GPUDirect RDMA** 让网卡直接访问已注册、已授权的 GPU 内存，载荷不必经主机内存中转。由 CPU 发起通信时，提交请求和处理完成通知仍由 CPU 负责；由加速器发起通信，则让 GPU 自己构造或触发请求并读取完成状态，数据一算完就能发出。NVLink 是 NVIDIA 的加速器互联，UB 是 Unified Bus 统一互联，URMA 是 UB 提供的统一远程内存访问接口。 |
| 数据绕过主机内存之后，仍可进一步改变通信的发起者。图 7-14 由 GPU 自己发起对端内存访问，因此还省去了每次操作都由 CPU 提交的交接。 | 数据绕过主机内存之后，还可以进一步改变由谁发起。图 7-14 中 GPU 自己发起对端内存访问，省去了每次都由 CPU 提交的交接。 |
| 这种变化可以用第 7.2 节的消息模型解释。一条 8 KiB 消息在 25 GB/s 链路上的数据传输时间约为 0.33 μs。若请求启动时间从 5 μs 缩短到 2 μs，总时间便从约 5.3 μs 降至 2.3 μs，节省超过一半。64 MiB 的载荷本身需要约 2.7 ms，同样省下的 3 μs 只占很小部分。因此，小消息应优先减少提交请求的开销，大消息应优先提高数据传输速度。 | 这个变化可以用第 7.2 节的传输时间公式解释。8 KiB 数据在 25 GB/s 链路上的传输时间约为 0.33 μs；若启动时间从 5 μs 缩到 2 μs，总时间就从约 5.3 μs 降到 2.3 μs，省了一半多。64 MiB 的载荷本身要传约 2.7 ms，同样省下的 3 μs 就微不足道。因此，小数据量的传输应优先减少提交开销，大数据量的传输应优先提高带宽。 |
| 由主机统一组织通信，可以为不同程序提供通用的任务提交和完成处理机制。模型执行明确了数据的生产者、消费者和就绪条件后，可以把频繁的发起与完成处理放到设备侧。系统预先建立地址映射和访问权限，随后由设备连续发起计算与通信，省去反复经主机中转的控制操作。收益直接表现为上例中启动时间的缩短。 | 由主机统一组织通信，好处是能为各种程序提供通用的提交和完成处理机制。模型执行的数据生产者、消费者和就绪条件都很明确，就可以把频繁的发起和完成处理放到设备一侧：系统预先建立地址映射和访问权限，之后由设备连续发起计算与通信，不必每次都经主机中转。收益就是上例中启动时间的缩短。 |
| 职责这样划分后，设备上的通信线程也会限制吞吐。要分析这种限制，先要分清一次请求等待多久、等待期间能否发起其他请求，再计算单位时间内能处理多少项请求。 | 职责这样划分之后，设备上发起通信的线程本身也会限制吞吐。要分析这种限制，先要分清一次请求要等多久、等待期间能不能发起别的请求，再算单位时间内能处理多少项请求。 |
| 考察八个独立的远端读取，硬件能同时处理八项请求，每次等待 2 μs，忽略相对于等待时间很短的提交开销。逐次“发出—等待—使用”需要约 16 μs；若八项同时发出并等待结果，结果可以在约 2 μs 后一起到达。若第一个读取返回第二个地址，第二个又返回第三个地址，就必须等上一次读取返回后才能发起下一次读取。这个例子说明，能够同时发起多少次读取，取决于有多少个地址已经确定且互不依赖的请求。 | 考察八个互不依赖的远端读取，硬件能同时处理八项，每项等 2 μs，提交开销相对很短可以忽略。逐个“发出、等待、使用”要约 16 μs；八项一起发出，结果约 2 μs 后一起到达。若第一次读取返回的是第二次的地址，第二次又返回第三次的地址，就只能等上一次返回后再发下一次。能同时发起多少次读取，取决于有多少个地址已知且互不依赖的请求。 |
| OpenURMA 用另一种口径描述这种分离：在一个本地接口上，$L$ 个本地应用端点访问 $P$ 台远端主机，核心端点与传输上下文按 $O(L+P)$ 增长；若逐应用关系独占可靠连接，则该部分按 $O(LP)$ 增长。这个结论针对可以共享的核心硬件状态，不表示所有软件映射、权限、未完成请求与缓存都只需加法空间。上式中的 $LPr$ 是本教学方案显式计入的逐关系绑定；本小节末尾用实现中的记录尺寸重新计算一次。 | OpenURMA 论文用另一种口径描述这种分离：一个本地接口上有 $L$ 个应用端点、访问 $P$ 台远端主机时，网卡上的核心端点与传输上下文按 $O(L+P)$ 增长；若每条应用关系独占一条可靠连接，这部分按 $O(LP)$ 增长。这个结论只针对可以共享的核心硬件状态，不是说所有软件映射、权限、未完成请求和缓存都只需要加法空间。上式中的 $LPr$ 是本教学方案显式计入的逐关系绑定；本小节末尾用实现中的记录尺寸重新算一次。 |
| 这两个式子直接给出了共享所节省的空间：$LPt$ 变成 $Pt$，应用关系的 $LPr$ 仍然存在。Jetty 与传输路径的分离采用了这种设计。Jetty 是应用提交请求和获取完成通知的接口，底层传输路径负责可靠传输。同一套可靠传输状态因而能服务多条应用关系。 | 两个式子直接给出了共享省下的空间：$LPt$ 变成 $Pt$，应用关系的 $LPr$ 仍在。UB 把 Jetty 与传输通道分开，采用的就是这种设计：Jetty 是应用提交请求、获取完成通知的接口，传输通道负责可靠传输，同一份传输状态可以服务多条应用关系。 |
| 图 7-28 中多条关系复用同一份传输状态，节省了存储，也可能争用该通道的发送窗口、调度机会与恢复资源。分层并不要求所有事务进入一条全局严格有序的队列；应用事务的执行序、完成序与报文到达顺序仍需分别规定。若实现共享 FIFO 或顺序等待，丢包与长请求就可能产生队头阻塞；允许独立事务继续推进可以缩小这种影响，但不会消除共享带宽和窗口的竞争。一条通信关系的大量未完成请求占用共享队列，其他关系的短请求就要等待。若为不同业务建立 $K$ 类独立传输组，总状态变成 | 图 7-28 中多条关系复用同一份传输状态，省了存储，也可能争用这条通道的发送窗口、调度机会和恢复资源。分层不要求所有事务排进一条全局有序的队列，应用事务的执行顺序、完成顺序与报文到达顺序仍要分别规定。若实现里用共享的先进先出队列或顺序等待，丢包和长请求就会造成队头阻塞；让独立的事务继续推进可以缩小影响，但消不掉对共享带宽和窗口的竞争。一条通信关系的大量未完成请求占着共享队列，其他关系的短请求就得等。若为不同业务建立 $K$ 组独立的传输状态，总状态变成 |
| 完整状态放在主存，频繁使用的部分缓存在 NIC 片上。 | 完整状态放在主存，频繁使用的部分缓存在网卡片上。 |
| 比较增加端点数和增加目标数对固定状态与传输状态的影响。 最后改用实现中的记录尺寸 | 比较增加端点数和增加目标数对固定状态与传输状态的影响。最后改用实现中的记录尺寸 |
| 下游来不及处理时，通过限制上游提交速度来防止请求继续积压的机制，称为**背压（backpressure）**。这里，请求槽位用满后，端点暂停或减慢提交，直到有槽位释放。它通过限制未完成请求数，控制上游提交速度，防止未完成工作无限增长。若缓冲区的释放需要等待远端程序用完数据，背压就会跨设备传播；若许多工作共享传输状态，传播范围还会进一步扩大。 | 下游来不及处理时，限制上游提交速度、不让请求继续积压的机制称为**背压**（backpressure）。这里请求槽位用满后，端点就暂停或放慢提交，直到有槽位释放；它靠限制未完成请求数来控制上游，防止未完成的工作无限增长。若缓冲区要等远端程序用完数据才能释放，背压就会跨设备传播；许多工作共享传输状态时，传播范围还会更大。 |
| 至此，端点已经能限制提交、处理完成通知并安全释放空间；当多个端点共享网络时，同样的到达与处理速度之差，会表现为交换机中的队列。 | 到这里，端点已经能限制提交、处理完成通知并安全地释放空间；多个端点共享网络时，同样的到达速度与处理速度之差，会变成交换机里的队列。 |
| CASSINI 利用训练的周期通信，通过安排作业的部署位置和通信时刻，让通信高峰错开，并监测漂移、重新调整。[^cassini] 本书用两个 CPU 上的分布式数据并行（DDP）训练作业做过一次性错峰的对照实验：把其中一项作业推迟 50 ms 后，两者的相位在运行中继续漂移，三轮测量中整组作业的完成时间反而增加约 2%–3%。[^phase] 周期调度需要持续维护相位；拥塞控制则根据实时反馈，调整偏离预期的发送速率。 | CASSINI 利用训练通信的周期性，通过安排作业的放置和通信时刻错开高峰，并监测漂移、重新调整。[^cassini] 本书用两个在 CPU 上运行的分布式数据并行（DDP）训练作业做过一次性错峰的对照：把其中一项推迟 50 ms 后，两者的相位在运行中继续漂移，三轮测量中整组作业的完成时间反而多了约 2% 到 3%。[^phase] 周期调度要持续维护相位；拥塞控制则根据实时反馈调整偏离预期的发送速率。 |
| **端到端拥塞控制**把瓶颈信息送回发送者，降低发送速率。 | **端到端拥塞控制**把瓶颈信息送回发送方，降低发送速率。 |
| **显式拥塞通知**（ECN）通过交换机标记把队列信息带回发送者，**DCQCN** 是利用拥塞通知调节 RDMA 发送速率的控制算法；时延反馈通过往返时间观察排队，UB 的 **C-AQM** 是端点与交换网络协同进行主动队列管理的机制，通过反馈控制发送。理解这些机制，先看它们缩短了哪段反馈路径、发送者收到信息后改变了什么。 | **显式拥塞通知**（ECN）由交换机在报文上打标记，把队列信息带回发送方；**DCQCN** 是利用这种通知调节 RDMA 发送速率的算法；基于时延的方法从往返时间的变化观察排队；UB 的 **C-AQM** 让端点与交换网络协同做主动队列管理。理解这些机制，先看它们缩短了哪一段反馈路径、发送方收到信息后改变了什么。 |
| 将不同连接分散到不同路径，可以分散原本集中在热点链路上的流量；将同一条消息中的报文分到多条路径，可以同时利用多条链路的带宽。后一种做法也把不同路径的时延差带到了接收端。 | 把不同的连接分到不同路径，能分散热点链路上的流量；把同一次传输的报文分到多条路径，能同时用上多条链路的带宽。后一种做法也把不同路径的时延差带到了接收方。 |
| 两条路径的传播时延都为 1 μs 时，整条消息约在 5.1 μs 交付； | 两条路径的传播时延都为 1 μs 时，整个向量约在 5.1 μs 交付； |
| 消息的总字节数相同，增加路径却比单路径更慢，因为节省的串行发送时间小于新增的路径时延。 | 总字节数相同，多用一条路径却比单路径更慢，因为省下的串行发送时间小于新增的路径时延。 |
| 消息交付要等缺口补齐，而不只取决于其余报文多早到达。 | 交付要等缺口补齐，不取决于其余报文多早到达。 |
| 图 7-41 中的水平线说明，快路径发完并不等于整条消息可以交给应用使用。 | 图 7-41 中的横线说明，快路径发完并不等于整份数据可以交给应用。 |
| 再让序号 0 丢失，在其原发送结束 20 μs 后重传，整条消息约在 23.0 μs 才能交付。 | 再让序号 0 丢失，在它原来发送结束的 20 μs 后重传，整份数据约在 23.0 μs 才能交付。 |
| 重传还包含一个独立问题：何时认定缺口需要恢复。等待太久，真正丢包后的停顿增长；仅因慢路径上的报文暂未到达就重发，又会增加重复流量。路径时延分布决定检测需要容忍多大乱序，接收端记录的状态决定能缓存多少在缺失报文之后到达的数据。 | 重传还有一个独立的问题：什么时候认定缺口需要恢复。等太久，真丢包后的停顿就长；慢路径上的报文只是还没到就重发，又会增加重复流量。路径时延的分布决定检测要容忍多大的乱序，接收方记录的状态决定能缓存多少在缺失报文之后到达的数据。 |
| 在归约系统中，这种循环可以跨越多个层次：请求占满接收缓冲，完成响应等待发送队列，发送队列又等待远端释放请求所占的空间。画出“持有前一资源、请求后一资源”的边，就得到资源依赖图。环上的每项资源都被持有时，所有释放条件同时受阻。 | 在归约系统里，这种循环可以跨越多个层次：请求占满接收缓冲，完成响应等待发送队列，发送队列又等待远端释放请求所占的空间。把“持有前一项资源、申请后一项资源”画成边，就得到资源依赖图。环上的每项资源都被持有时，所有释放条件同时被卡住。 |
| 设接收方八个数据槽已满，发送者等待释放； | 设接收方八个数据槽已满，发送方等待释放； |
| 内存访问还可能需要进行地址转换、读取页表。 | 内存访问还可能要做地址转换、读页表。 |
| 最后还要确定通信在什么时候开始。某张卡进入集合调用很早，另一张卡的梯度尚未生成，前者会在调用内部等待后者。这段等待计在通信调用的耗时里，根源却是前面的计算或数据准备。 | 最后还要确定通信什么时候开始。一张卡很早就进入集合通信调用，另一张卡的梯度还没算出来，前者就在调用里等后者。这段等待记在通信调用的耗时里，根源却在前面的计算或数据准备。 |
| 考虑四个需要进行本地归约的参与者 | 考虑四个要做本地归约的参与者 |
| 本书使用集合通信库 Gloo 的四进程 CPU 测量也呈现同样关系： | 本书用集合通信库 Gloo 在 CPU 上做的四进程测量也是同样的关系： |
| ![小消息启动开销与大消息传输开销](ch07/figure-7-21-message.svg) | ![启动开销与传输开销随数据量的变化](ch07/figure-7-21-message.svg) |
| 橙色将每轮启动减至 2 μs；小消息主要受启动影响，大消息主要受传输影响。 | 橙色将每轮启动减至 2 μs；数据量小时主要受启动影响，数据量大时主要受传输影响。 |
| 沿图 7-55 的横轴从左向右看，小消息区的总时间接近水平，大消息区则随载荷增大而上升。 | 沿图 7-55 的横轴从左向右看，数据量小的一段总时间接近水平，数据量大的一段随载荷增大而上升。 |
| 这也解释了第 7.2 节讨论的 PP 方案： | 这也解释了第 7.2 节讨论的流水线并行方案： |
| 频繁发送的短消息需要降低单次启动开销； | 频繁的小数据量传输需要降低每次的启动开销； |
| 可以用一次小消息实验区分“带宽更高”和“交接更快”。设某串行通信步骤固定启动时间为 5 μs，需要传输 1 MB，有效带宽为 100 GB/s；不含排队时共需 15 μs。带宽翻倍后为 10 μs，只有 1.5 倍加速。若消息缩至 10 KB，同样改动只把 5.1 μs 缩至 5.05 μs。减小消息之后，启动、通知和同步比继续增加带宽更值得关注。 | 可以用一个小数据量的例子区分“带宽更高”和“交接更快”。设某个串行的通信步骤启动时间固定为 5 μs，要传 1 MB，有效带宽 100 GB/s，不算排队共需 15 μs。带宽翻倍后为 10 μs，只有 1.5 倍加速。若数据量缩到 10 KB，同样的改动只把 5.1 μs 缩到 5.05 μs。数据量小了之后，启动、通知和同步比继续加带宽更值得关注。 |
| MoE 的路由、分发与合并会提出对网络操作的要求；网络能否高效完成这些操作，又会影响专家粒度与跨节点部署。若模型计算因专用硬件变快，通信时间本身未变，它在关键路径上的耗时占比却会增加。 | MoE 的路由、派发与合并对网络操作提出要求；网络能否高效完成这些操作，又反过来影响专家的粒度和跨节点部署。若模型计算因专用硬件变快，通信时间本身没变，它在关键路径上的占比却会上升。 |
| 继续分析同一个 V4.1 会话，设请求被迁移到没有其全局 KV 的实例。 | 继续分析同一个 DeepSeek V4.1 会话，设请求被迁移到一个没有它的全局 KV 的实例。 |
| 若请求依次经历排队、缓存查询、传输和局部 SWA 重建，恢复时间就是四段之和；其中传输缩短多少，总时间便缩短多少。第九章将把这条恢复路径与留在原实例等待、从输入重新计算放在一起比较，决定请求应去哪里执行。 | 若请求依次经历排队、缓存查询、传输和局部滑动窗口注意力（SWA）状态的重建，恢复时间就是四段之和；传输缩短多少，总时间就缩短多少。第 9 章会把这条恢复路径与留在原实例等待、从输入重新计算放在一起比较，决定请求该去哪里执行。 |
| **固定模型与训练工作。** 采用 Qwen3-32B 的 64 层、5120 隐藏维形状，参数量近似为 $P=32\times10^9$。1024 张 80 GB 卡按 TP8×DP128 组织，每个八卡 TP 组都位于一个超节点内。每次更新固定处理 $2^{20}$ 个 token，每个 DP 副本处理一个 8192-token 微批次；不改变全局批量、精度或优化器更新的语义。训练状态按 16 bytes／参数计，每卡 64 GB，另假设 8 GB 激活与工作区足够，暂不采用 ZeRO。 | **固定模型与训练工作。** 采用 Qwen3-32B 的形状：64 层、隐藏维 5120，参数量取 $P=32\times10^9$。1024 张 80 GB 卡按张量并行 8、数据并行 128 组织，记作 TP8×DP128，每个八卡张量并行组都在一个超节点内。每次更新固定处理 $2^{20}$ 个 token，每个数据并行副本处理一个 8192-token 的微批次；全局批量、精度和优化器更新的含义都不变。训练状态按每参数 16 bytes 计，每卡 64 GB，另假设 8 GB 的激活与工作区够用，暂不用 ZeRO。 |
| 梯度按 BF16 传输，每个 TP 分片为 $G=2P/8=8$ GB。设超节点大小为 $S$，则超节点数 $H=1024/S$，每个 TP 坐标在节点内有 $q=S/8$ 个 DP 成员。图 7-56 展示了 64 卡超节点中的分组：一行是一个 TP8 模型副本，同一列属于同一个梯度同步组。 | 梯度按 BF16 传输，每张卡持有的分片为 $G=2P/8=8$ GB。设超节点有 $S$ 张卡，则超节点数 $H=1024/S$，每个超节点内同一张量并行坐标上有 $q=S/8$ 个数据并行成员。图 7-56 画出 64 卡超节点内的分组：同一行的八张卡是一个模型副本，同一列的卡属于同一个梯度同步组。 |
| **跨超节点的传输量。** 本地 $q$ 个 DP 成员先执行 ReduceScatter，每卡留下 $G/q$ 的梯度分片；持有对应分片的 $H$ 个节点再执行 AllReduce；最后本地 AllGather。按环形模型，每卡跨节点发送 $2(H-1)G/(Hq)$，乘以节点内的 $8q$ 张卡，得到每超节点每方向的发送量 | **跨超节点的传输量。** 节点内的 $q$ 个数据并行成员先做 ReduceScatter，每张卡留下 $G/q$ 的梯度分片；持有同一分片的 $H$ 个超节点之间再做 AllReduce；最后节点内做 AllGather。按环形算法，每张卡跨节点发送 $2(H-1)G/(Hq)$，乘以节点内的 $8q$ 张卡，得到每个超节点每个方向的发送量 |
| 节点内有效带宽取每卡 200 GB/s、每轮 2 μs；跨节点 NIC 为每卡每方向 25 GB/s、每轮 5 μs。 | 节点内有效带宽取每卡 200 GB/s、每轮 2 μs；跨节点网卡为每卡每方向 25 GB/s、每轮 5 μs。 |
| 式中依次计本地归约／收集、跨节点启动，以及 NIC 与共享出口中较慢的约束。完整实现还可能受交换网络割集和本地同时通信影响；本例假设网络可以提供所列带宽，各阶段不重叠。 | 式中依次是本地归约与收集、跨节点的启动，以及网卡与共享出口两者中较慢的一项。真实系统还可能受交换网络割集和本地并发通信的影响；本例假设网络能提供所列带宽，各阶段不重叠。 |
| 忽略 attention 的二次项。每层前向与反向合计按四次 TP AllReduce 计 | 忽略注意力的二次项。每层前向与反向合计按四次张量并行 AllReduce 计 |
| 这是计算、TP 通信、梯度同步和更新串行的教学估算；真实系统应沿梯度就绪时间线扣除已重叠的部分，不能把本例当作 GPU 测量。作为对照，再计算一个不分层的连续 DP 环：每个 TP 坐标只有一条跨节点的环边，不把这条边分摊到其他 NIC。下表在两种算法中取较快者。 | 这是把计算、张量并行通信、梯度同步和更新串起来的教学估算；真实系统应沿梯度就绪的时间线扣掉已经重叠的部分，不能把本例当作实测。作为对照，再算一个不分层的连续数据并行环：每个张量并行坐标只有一条跨节点的环边，不把这条边分摊到其他网卡。下表在两种算法中取较快者。 |
| 这与第 7.2 节的本地归约成本判断相同。 | 这和第 7.2 节关于本地归约是否合算的判断相同。 |
| 因为计算与 TP 已占据大部分时间。 | 因为计算与张量并行通信已经占了大部分时间。 |
| **并行度的重新比较。** 更大的高速域可以容纳更大的 TP，但不必把它填满。对 128 卡超节点，在出口扩展条件下枚举 TP8／16／32／64，DP 分别为 128／64／32／16，并保持全局 token 数不变。每个 DP 副本处理的 8192-token 序列数量随之增加，局部矩阵行数和 TP 消息都要重算；假设这些形状均可达到本例有效算力，步时间分别约为 2.321、2.504、2.930、3.812 s。此时 TP8 仍较好：更大的 TP 增加了每副本激活的交换量，节省的局部梯度同步不足以抵消它。TP 超过八个 KV 头时还需支持 KV 复制等布局，本例假定内核支持且余量足够。 | **并行度的重新比较。** 更大的高速域可以容纳更大的张量并行组，但不必把它填满。对 128 卡超节点，在出口扩展的条件下枚举张量并行 8、16、32、64，对应的数据并行为 128、64、32、16，全局 token 数不变。每个数据并行副本处理的 8192-token 序列数随之增加，局部矩阵的行数和张量并行的传输量都要重算；假设这些形状都能达到本例的有效算力，步时间分别约为 2.321、2.504、2.930、3.812 s。此时 TP8 仍然最好：张量并行组越大，每个副本要交换的激活越多，省下的本地梯度同步抵不过它。张量并行超过八个 KV 头时还要支持复制 KV 等布局，本例假定内核支持且余量足够。 |
| 这次筛选说明，1024 卡可以一起训练一个模型，而不是将模型的每一层切到 1024 卡。若改为推理，多个较小实例还可以独立处理请求和隔离故障，第 6.7 节已经展示这种比较。真实训练若局部矩阵效率、内存或重叠能力改变，TP／CP／PP 的组合可能更好；应像第 6.7.3 节一样扩展候选并重算，而不是把本例的 TP8 当作通用答案。 | 这次筛选说明，1024 张卡可以一起训练一个模型，但不是把模型的每一层都切到 1024 张卡上。若改为推理，多个较小的实例还能各自处理请求、隔离故障，第 6.7 节已经比较过。真实训练中局部矩阵效率、内存或重叠能力不同时，张量、上下文、流水线并行的组合可能更好；应像第 6.7.3 节那样扩展候选重算，而不是把本例的 TP8 当作通用答案。 |
| **故障恢复的开销。** 同步训练中，一张卡失败可能使整个作业停止；DP 副本也不能像独立推理实例一样任意丢掉，因为这会改变本次更新的样本与梯度。除非系统提供保持训练语义的局部恢复或弹性方案，否则需恢复一致检查点。 | **故障恢复的开销。** 同步训练中，一张卡故障可能让整个作业停下；数据并行副本也不能像独立的推理实例那样随手丢掉，因为那会改变这次更新用到的样本和梯度。除非系统提供不改变训练含义的局部恢复或弹性方案，否则要回退到一致的检查点。 |
| 大梯度、小消息和故障恢复分别突出了带宽、启动与依赖等待。 | 大梯度、小数据量传输和故障恢复分别突出了带宽、启动开销和依赖等待。 |
| GPU 到 NIC 的连接、共享接口和交换拓扑决定可用带宽； | GPU 到网卡的连接、共享接口和交换拓扑决定可用带宽； |
| 高速域扩大后，可以把更多归约留在本地，也可以重新比较 TP 与 DP 的组合……一个作业使用全部加速器，不要求每种通信都覆盖全部加速器。 | ……重新比较张量并行与数据并行的组合……一个作业用上全部的卡，不等于每种通信都要覆盖全部的卡。 |
| 一块数据从 GPU 内存出发，经加速器接口到达 NIC，再经过交换网络，最后写入接收端内存。 | 一块数据从 GPU 内存出发，经加速器接口到达网卡，再经过交换网络，最后写入接收方的内存。 |
| 例如，两张 NIC 合计能发送 50 GB/s，共享接口只能通过 40 GB/s， | 例如，两张网卡合计能发送 50 GB/s，共享接口只能通过 40 GB/s， |
| 全部发给一张 25 GB/s NIC，接收至少需约 1.34 ms；平均分给四张独立的同速 NIC，每张接收 8 MiB，接收时间降至约 0.34 ms。若这四张 NIC 共用一个 40 GB/s 入口， | 全部发给一张 25 GB/s 的网卡，接收至少需约 1.34 ms；平均分给四张独立的同速网卡，每张接收 8 MiB，接收时间降至约 0.34 ms。若这四张网卡共用一个 40 GB/s 入口， |
| 再将每服务器 NIC 数从一张增为两张、三张 | 再将每台服务器的网卡数从一张增为两张、三张 |

## 第二遍通读（2026-09-11）

共 51 处。

| 修改前 | 修改后 |
|---|---|
| 上一章把矩阵、层和专家分给不同加速器，并根据通信代价选择实例规模。现在，模型和总卡数都不变，只把部分加速器移到另一个超节点：原先的切分还合适吗？经过节点内高速互联的部分和，可能要改走 InfiniBand 或 RoCE；同一个同步操作因此占用不同的带宽、队列和故障域。需要重新计算的是分块的放置与执行代价，而不是重新学习一套并行概念。 | 上一章把矩阵、层和专家分给不同的卡，并按通信代价选择实例规模。现在模型和总卡数都不变，只把一部分卡移到另一个超节点：原来的切分还合适吗？原本走节点内高速互联的部分和，可能要改走 InfiniBand 或 RoCE；同一次同步因此占用不同的带宽、队列和故障域。要重新算的是切分的放置和执行代价，不是重新学一套并行概念。 |
| 这些状态共需约 4.5 TB，必须分散到更多加速器上存储和处理。 | 这些状态共需约 4.5 TB，必须分散到更多的卡上存储和处理。 |
| 推理虽然不需要保存梯度和优化器状态，大模型的权重仍可能需要多台服务器才能容纳。 | 推理不需要保存梯度和优化器状态，但大模型的权重仍可能要多台服务器才放得下。 |
| 混合专家模型（MoE）由路由器为每个 token 选择部分前馈网络，即专家。每个 token 只激活部分专家，减少的是这一次前向的计算量；未被激活的专家，其权重仍然要有地方存放。 | 混合专家模型（MoE）的路由器为每个 token 只选一部分前馈网络（专家）来执行，减少的是这一次前向的计算量；没被选中的专家，权重仍然要有地方放。 |
| 算法、放置、精度和接收端是否去重决定要传多少字节 | 算法、放置、精度和接收方是否去重决定要传多少字节 |
| 分别画出服务器内 TP／EP、服务器间 PP 的分工和跨服务器 TP 的分工， | 分别画出服务器内张量并行或专家并行、服务器间流水线并行的分工，以及跨服务器张量并行的分工， |
| 确定了加速器位置和梯度大小，接下来要计算数据经过路径上的每一段需要多久。 | 确定了卡的位置和梯度大小，接下来要计算数据经过路径上的每一段需要多久。 |
| 只有跨叶交换机的通信才占用上行带宽。 | 只有跨叶交换机的通信才占用上联带宽。 |
| 先采用两服务器算例中的环形归约：每台服务器向另一台发送 336 MiB。共享出口为 40 GB/s，原加速器规模下的计算时间为 20 ms。增加加速器时，计算均匀分摊，跨服务器传输量保持不变。 | 沿用两台服务器算例中的环形归约：每台服务器向另一台发送 336 MiB。共享出口为 40 GB/s，原来的卡数下计算时间为 20 ms。增加卡数时计算均匀分摊，跨服务器的传输量不变。 |
| 令加速器数相对原规模的倍数为 $x$，计算时间变为 $20/x$ ms。串行安排需要 $20/x+8.8$ ms；计算与通信完全重叠时，总时间等于两者中的较大值。加速器增加一倍，两个结果分别从约 28.8、20.0 ms 降至 18.8、10.0 ms。 | 令卡数相对原来的倍数为 $x$，计算时间变为 $20/x$ ms。串行安排需要 $20/x+8.8$ ms；计算与通信完全重叠时，总时间等于两者中的较大值。卡数翻倍，两个结果分别从约 28.8、20.0 ms 降至 18.8、10.0 ms。 |
| *图 7-2：固定跨服务器需求下的加速器扩展。每方向发送 336 MiB，共享出口为 40 GB/s；计算时间从 20 ms 开始，随加速器数增加而按反比缩短。 | *图 7-2：跨服务器传输量固定时增加卡数的效果。每方向发送 336 MiB，共享出口为 40 GB/s；计算时间从 20 ms 开始，随卡数增加按反比缩短。 |
| 越过这一点后，计算时间已经短于传输时间，继续增加加速器无法降低完全重叠安排的时间。 | 越过这一点后，计算时间已经短于传输时间，继续加卡也不能缩短完全重叠安排的时间。 |
| 这里固定总任务、增加加速器的过程称为**强扩展**。**弱扩展**则随加速器数增加而增加任务量。例如，若每个方向的传输量也增加一倍而出口保持不变，通信时间就增加一倍。扩展曲线的形状，取决于计算量和跨服务器传输量各自如何随加速器数变化。 | 这种总任务固定、增加卡数的做法称为**强扩展**；**弱扩展**则随卡数增加而增加任务量。例如，若每个方向的传输量也翻倍而出口不变，通信时间就翻倍。扩展曲线的形状，取决于计算量和跨服务器传输量各自怎样随卡数变化。 |
| 并求计算耗时与通信耗时相等时的加速器倍数。再令流量随加速器倍数线性增长，求完全重叠安排下使完成时间最短的加速器倍数。 | 并求计算耗时与通信耗时相等时的卡数倍数。再令流量随卡数倍数线性增长，求完全重叠安排下使完成时间最短的卡数倍数。 |
| 上面的下界说明了物理链路的限制。程序还面临另一种复杂性：同一份数据在机内和跨服务器传输时，往往要使用不同的访问接口。这与总线和网络长期承担的不同角色有关。紧耦合互联把设备连接到处理器与内存系统，适合频繁、细粒度的协作；集群网络连接大量独立节点，并处理节点或链路故障。模型跨越服务器之后，同一项计算就同时使用这两类机制。 | 上面的下界说明了物理链路的限制。程序还面临另一个麻烦：同一份数据在机内传输和跨服务器传输时，往往要用不同的访问接口。这和总线与网络长期以来的分工有关：紧耦合的互联把设备接到处理器和内存系统上，适合频繁、细粒度的协作；集群网络连接大量独立的节点，还要处理节点和链路故障。模型跨越服务器之后，同一项计算就同时用到这两类机制。 |
| 每个参与者计算局部梯度，再将对应元素相加。最终，每个参与者都需要完整的归约结果。我们选择 Qwen3-8B 第一层门控（gate）投影的梯度；门控投影是 SwiGLU 前馈网络中产生门控信号的那一支，其梯度的形状为 $[12288,4096]$，以每元素 4 字节的 FP32 浮点格式存放，每个参与者的输入为 | 每个参与者算出局部梯度，再把对应元素相加，最后每个参与者都要拿到完整的归约结果。算例选 Qwen3-8B 第一层门控（gate）投影的梯度：门控投影是 SwiGLU 前馈网络里产生门控信号的那一支，梯度形状为 $[12288,4096]$，按每元素 4 字节的 FP32 存放，每个参与者的输入为 |
| ReduceScatter 每轮向下一参与者发送一份，并把收到的部分加入局部结果； | ReduceScatter 每轮向下一个参与者发送一份，并把收到的一份加进本地结果； |
| 每一组在两个方向合计发送 96 MiB，四组共发送 384 MiB。 | 每一对在两个方向合计发送 96 MiB，四对共发送 384 MiB。 |
| *图 7-5：本地先把八份贡献中的本机四份归约成分片，再由两台服务器交换对应分片，最后在本地收集完整结果。 | *图 7-5：每台服务器先把本机四份贡献归约成分片，再和另一台服务器交换对应的分片，最后在本机收集完整结果。 |
| 这个式子将两种关系区分开来：一轮的时间由最慢的传输决定，各轮的时间依次相加。这里先计算数据传输和每轮启动的时间。 | 这个式子分开了两种关系：一轮之内由最慢的传输决定，各轮之间依次相加。这里只算数据传输和每轮启动的时间。 |
| ### 7.2.2 TP 与 PP 的通信需求 | ### 7.2.2 张量并行与流水线并行的通信需求 |
| 因此，TP 与 PP 会产生不同的等待：TP 需要频繁汇合局部结果，PP 在流水线启动和结束时有部分加速器空闲。 | 因此两种并行产生不同的等待：张量并行要频繁汇合局部结果，流水线并行在启动和结束时有一部分卡空闲。 |
| TP 和 PP 的通信由模型切分方式确定；MoE 还多了一个变数：每个 token 选择的专家可能不同，发送目标也随之改变。专家并行（EP）把专家放在不同加速器，token 按路由前往相应专家。取 1024 个 token，每个选择八个专家，共有 8192 次分派。每份激活为 8 KiB，一半分派跨服务器，向远端专家分发输入（dispatch）就有 4096 份，共 32 MiB；返回相同大小的结果，再传 32 MiB。 | 张量并行和流水线并行的通信由切分方式确定；MoE 还多一个变数：每个 token 选的专家可能不同，发送目标也随之改变。专家并行把专家放在不同的卡上，token 按路由去相应的专家。取 1024 个 token，每个选八个专家，共 8192 次分派。每份激活 8 KiB，一半分派跨服务器，向远端专家派发（dispatch）输入就有 4096 份，共 32 MiB；返回同样大小的结果，再传 32 MiB。 |
| 这 32 MiB 怎样分配到接收端，同样影响传输时间。 | 这 32 MiB 怎样分配到接收方，同样影响传输时间。 |
| *图 7-14：GPU 通过 NVLink 访问对端 GPU 内存，由发起加速器执行已授权的访问。* | *图 7-14：GPU 通过 NVLink 访问对端 GPU 内存，由发起的 GPU 执行已授权的访问。* |
| URPC 是统一互联上的远程过程调用接口，它以函数调用的形式提供远端访问，按引用传递使被调用者能够只取实际需要的数据。 | URPC 是统一互联上的远程过程调用接口，以函数调用的形式提供远端访问；按引用传递让被调用方只取实际需要的数据。 |
| 前一小节用复用减少远端读取次数。对于仍然需要通过网络的请求，接下来要看它们能否连续利用链路。下面分析本章算例中服务器的 40 GB/s 出口需要多少个并发请求。**请求槽位**是保存一项未完成请求的地址、长度和状态等信息的记录空间。提交时分配，完成状态被处理后释放；释放前不能将同一记录用于新请求。 | 上一小节用复用减少远端读取的次数。对于仍要经过网络的请求，接下来要看它们能否让链路一直有数据可传。下面分析本章算例中服务器的 40 GB/s 出口需要多少个并发请求。**请求槽位**是保存一项未完成请求的地址、长度和状态的记录：提交时分配，完成状态处理完后释放，释放前不能给新请求用。 |
| 发送者需要知道源缓冲何时可以改写，接收者需要知道目的缓冲何时可以读取，下一次写入操作则需要确定这块目的缓冲何时可以覆盖。三者对应不同的事件。 | 发送方要知道源缓冲何时可以改写，接收方要知道目的缓冲何时可以读取，下一次写入还要知道这块目的缓冲何时可以覆盖。三者对应不同的事件。 |
| 源缓冲能否复用，要看所用接口的完成语义是否保证传输不再读取它；接收方开始使用数据，需要保证写入已对它可见，并按约定发布和观察就绪通知；目的缓冲能否覆盖，还要等接收方不再使用其中的数据。网络传输完成本身不能代替消费完成通知。 | 源缓冲能否复用，要看接口的完成语义是否保证传输不再读它；接收方要开始用数据，先要保证写入对它已经可见，并按约定发布和查看就绪通知；目的缓冲能否覆盖，还要等接收方用完其中的数据。网络传输完成本身不能代替“用完了”的通知。 |
| 源端可以按发送完成条件复用自己的源缓冲，目的缓冲则要到 12 μs 才能覆盖。 | 源端在发送完成后就可以复用源缓冲，目的缓冲则要到 12 μs 才能覆盖。 |
| 前一节把槽位占用时间作为已知条件，这一节沿这些事件确定缓冲区何时可以释放。 | 上一节把槽位占用时间当作已知条件，这一节沿这些事件确定缓冲区何时可以释放。 |
| 删除额外依赖，把恢复造成的停顿限制在需要这份数据的链上。对于等待 C 的下游任务，这种隔离比全组只提前 10 μs 更重要。 | 去掉多余的依赖，就把恢复造成的停顿限制在真正需要这份数据的那条链上。对等着 C 的下游任务来说，这种隔离比全组提前 10 μs 重要得多。 |
| 让一个发起者的请求停在等待前序完成的位置，另外四个发起者提交的 8 个不要求顺序的请求仍在 78 个周期（约 242 ns）内全部发出，停住的请求没有拖住其他发起者。 | 让一个发起方的请求停在等待前序完成的位置，另外四个发起方提交的 8 个不要求顺序的请求仍在 78 个周期（约 242 ns）内全部发出，停住的请求没有拖住其他发起方。 |
| 发送者在部分发送后退出，系统要确定哪些节点已收到；接收者永久退出，系统要决定哪些工作继续执行。 | 发送方发到一半退出，系统要确定哪些节点已经收到；接收方永久退出，系统要决定哪些工作继续执行。 |
| 目标端排序把等待移到接收侧，冲突检查则负责判断哪些提前读取的结果仍然有效。 | 在接收方排序是把等待挪到了接收一侧，冲突检查则负责判断哪些提前读到的结果仍然有效。 |
| **请求槽位**是一块保存请求地址、长度和状态的记录空间。软件处理完成通知后，释放这个槽位，新请求才能使用它。 | 第 7.3.4 节定义的**请求槽位**要等软件处理完完成通知才释放，新请求才能使用它。 |
| 让两项训练共享一条 50 GB/s 上行链路。两项作业都能从各自的服务器以 40 GB/s 发送数据，两个高峰相遇时，需求便超过了共享链路的传输能力。 | 让两项训练共享一条 50 GB/s 的上联链路。两项作业都能从各自的服务器以 40 GB/s 发送数据，两个高峰撞在一起时，需求就超过了共享链路的传输能力。 |
| *图 7-38：将到达与服务速率的差额随时间累积，得到 600、150、0 MB 三种峰值。此算例使用无限缓冲计算需求积压。* | *图 7-38：把到达速率与出口速率的差额随时间累积，得到 600、150、0 MB 三种峰值。此算例按无限缓冲计算积压。* |
| 因此，还需要在队列开始增长时，及时让发送端减速。 | 因此，还需要在队列开始增长时，及时让发送方减速。 |
| **链路流控**让相邻接收端在缓冲不足时暂停上游，防止接收端缓冲区溢出。**端到端拥塞控制**把瓶颈信息送回发送方，降低发送速率。前者迅速阻止局部溢出，后者让源端的发送速率适应整条路径的传输能力。 | **链路流控**让相邻的接收方在缓冲不足时暂停上游，防止接收缓冲区溢出。**端到端拥塞控制**把瓶颈信息送回发送方，降低发送速率。前者迅速阻止局部溢出，后者让源头的发送速率适应整条路径的传输能力。 |
| 虚线表示拥塞反馈返回发送端。 | 虚线表示拥塞反馈返回发送方。 |
| 快路径上提前到达的后续报文需要等待前面的缺失报文补齐，接收端最多需要缓存 3 KiB 乱序数据。 | 快路径上提前到达的后续报文要等前面缺失的报文补齐，接收方最多要缓存 3 KiB 乱序数据。 |
| *图 7-43：序号 0 丢失后按算例等待时间重传，其他七个报文保留在接收端。 | *图 7-43：序号 0 丢失后按算例等待时间重传，其他七个报文保留在接收方。 |
| 此时其他七个报文均已到达，接收端缓存了 7 KiB 载荷 | 此时其他七个报文都已到达，接收方缓存了 7 KiB 载荷 |
| *图 7-46：原安排中，前三参与者等待第四个在 2 ms 就绪，再交换 0.4 ms。 | *图 7-46：原安排中，前三个参与者等第四个在 2 ms 就绪，再交换 0.4 ms。 |
| 平均约为 0.52 ms，按由小到大排序的第 99 项取 p99，得到 2.4 ms。 | 平均约为 0.52 ms；第 99 百分位（p99）取由小到大排序的第 99 项，得到 2.4 ms。 |
| 先保持通信图不变，比较带宽和启动开销的影响； | 先保持通信模式不变，比较带宽和启动开销的影响； |
| ![超节点内部的 TP 与 DP 坐标](ch07/figure-7-supernode-groups.svg) | ![超节点内部的张量并行与数据并行坐标](ch07/figure-7-supernode-groups.svg) |
| *图 7-56：64 卡超节点由八个 TP8 组组成。同行八卡处理同一微批次的不同模型分片；同列八卡先在节点内汇合对应梯度，再与其他超节点的同类分片交换。一个训练作业使用全部卡，不等于全部卡组成一个 TP 组。* | *图 7-56：64 卡超节点由八个张量并行组组成。同一行的八张卡处理同一微批次的不同模型分片；同一列的八张卡先在节点内汇合对应的梯度，再与其他超节点交换同一分片。一个训练作业用上全部的卡，不等于全部的卡组成一个张量并行组。* |
| 它们分别提供具体的连接与通信机制。选择部署方案时，先用本章的三个模型比较各部署方案的通信路径。 | 它们分别提供具体的连接与通信机制。选择部署方案时，先用本章的三个模型比较各方案的通信路径。 |
| 两服务器算例通过本地归约，把跨服务器发送量从 672 MiB 降至 384 MiB | 两台服务器的算例通过本地归约，把跨服务器发送量从 672 MiB 降至 384 MiB |


## 全书通读（2026-09-11，按最终文本与通读前快照逐行对照）

共 14 行有改动；英文术语按“首次出现保留中文注释、之后用英文”的原则已回退，下表是回退之后的净改动。省略号表示只截取了改动附近的文字。

| 修改前 | 修改后 |
|---|---|
| **例题 7.1：增加加速器的收益何时受跨服务器通信限制？** 沿用两台服务器算例中的环形归约：每台服务器向… | **例题 7.1：增加卡数的收益何时受跨服务器通信限制？** 沿用两台服务器算例中的环形归约：每台服务器向… |
| …量减半，以及出口带宽翻倍这两种改动，分别画出串行与完全重叠两种安排下，完成时间随加速器倍数变化的曲线，并求计算耗时与通信耗时相等时的卡数倍数。再令流量随卡数倍数线性增… | …量减半，以及出口带宽翻倍这两种改动，分别画出串行与完全重叠两种安排下，完成时间随卡数倍数变化的曲线，并求计算耗时与通信耗时相等时的卡数倍数。再令流量随卡数倍数线性增… |
| …操作顺序。统一接口的收益也在这三个层次上体现：减少搬运，缩短发起路径，简化协作。6.5.5 节已经从超节点规模的角度讨论了 UB 的两项设计选择：把控制器接在片… | …操作顺序。统一接口的收益也在这三个层次上体现：减少搬运，缩短发起路径，简化协作。第 6.5.5 节已经从超节点规模的角度讨论了 UB 的两项设计选择：把控制器接在片… |
| …-8B 的隐藏宽度为 4096，一个 BF16 隐藏向量是 8 KiB。8192-token prefill 的同类张量为 64 MiB，单请求 decode 则为 8 KiB。取一条 25 … | …-8B 的隐藏宽度为 4096，一个 BF16 隐藏向量是 8 KiB。8192 个 token 的 prefill 同类张量为 64 MiB，单请求 decode 则为 8 KiB。取一条 25 … |
| …k$、每份激活占 $d$ bytes、跨边界比例为 $f$。若每次分派单独传送，派发的传输量为 $nkfd$，加上返回并合并（combine）共 $2nkfd$。把热门专家放到发送方所在的服务器，降低的是… | …k$、每份激活占 $d$ bytes、跨边界比例为 $f$。若每次分派单独传送，dispatch 的传输量为 $nkfd$，加上返回并合并专家结果（combine）共 $2nkfd$。把热门专家放到发送方所在的服务器，降低的是… |
| 这个矩阵还说明了派发与合并的热点是怎样对应的。若按 token 到专家逐份发送、返回同样大小的向量，且接收端不做合并，返回矩阵就是发送矩阵的转置：派发时的热点接收方，就是合并时的热点发送方。两次通信的总字节数相同，不代表两段耗时相同：返回的数据要等专家算完才就绪，两个方向的可用带宽、数据格式和归约位置也可能不同。若派发用 FP8、合并用 BF16，返回的载荷约为派发的两倍，量化尺度等元数据另计。第 9.4 节会把这些流量与计算就绪时间放进同一个… | 这个矩阵还说明了 dispatch 与 combine 的热点是怎样对应的。若按 token 到专家逐份发送、返回同样大小的向量，且接收方不做合并，返回矩阵就是发送矩阵的转置：dispatch 的热点接收方，就是 combine 的热点发送方。两次通信的总字节数相同，不代表两段耗时相同：返回的数据要等专家算完才就绪，两个方向的可用带宽、数据格式和归约位置也可能不同。若 dispatch 用 FP8、combine 用 BF16，返回的载荷约为 dispatch 的两倍，量化尺度等元数据另计。第 9.4 节会把这些流量与计算就绪时间放进同一个… |
| 专家分派说明了多入口并行的收益和共享入口的限制。同样的分析也适用于发送端：增加网卡，只有在它提供了额外可用带宽时才会缩短传输。以前面两张 25 GB/s… | 专家分派说明了多入口并行的收益和共享入口的限制。同样的分析也适用于发送方：增加网卡，只有在它提供了额外可用带宽时才会缩短传输。以前面两张 25 GB/s… |
| …过剩余带宽为 60 GB/s 的本地互联。两路在下游汇合，最后还受外部网络和接收端可用带宽限制。箭头表示传输方向。* | …过剩余带宽为 60 GB/s 的本地互联。两路在下游汇合，最后还受外部网络和接收方可用带宽限制。箭头表示传输方向。* |
| 例如，发送在 5 μs 完成，消费者在 8 μs 开始读取、12 μs 结束。源端在发送完成后就可以复用源缓冲，目的缓冲则要到 12 μs 才能覆盖。如果下一次写… | 例如，发送在 5 μs 完成，消费者在 8 μs 开始读取、12 μs 结束。发送方在发送完成后就可以复用源缓冲，目的缓冲则要到 12 μs 才能覆盖。如果下一次写… |
| 图 7-27 中两条占用区间结束于不同事件。把“必须等哪一事件发生”画成箭头，就得到描述正确交接的**依赖图**。依赖图用有向边表达这些先… | 图 7-27 中两条占用区间结束于不同事件。把“必须等哪个事件发生”画成箭头，就得到描述正确交接的**依赖图**。依赖图用有向边表达这些先… |
| …约 155 ns；不要求顺序的请求不承担这笔开销。更重要的是隔离效果：把一个发起者的请求停在等待前序操作的位置，另外四个发起者提交的 8 个不要求顺序的请求仍然在 78 个周期（约 242 ns）内全部发出，被停住的请求没有拖累其他发起者。作为对照，RoCE 可靠连接中同一个队列对里的请求必须严格按序执行，一个请求等… | …约 155 ns；不要求顺序的请求不承担这笔开销。更重要的是隔离效果：把一个发起方的请求停在等待前序操作的位置，另外四个发起方提交的 8 个不要求顺序的请求仍然在 78 个周期（约 242 ns）内全部发出，被停住的请求没有拖累其他发起方。作为对照，RoCE 可靠连接中同一个队列对里的请求必须严格按序执行，一个请求等… |
| …内。每次更新固定处理 $2^{20}$ 个 token，每个数据并行副本处理一个 8192-token 的微批次；全局批量、精度和优化器更新的含义都不变。训练状态按每参数 … | …内。每次更新固定处理 $2^{20}$ 个 token，每个数据并行副本处理一个含 8192 个 token 的微批次；全局批量、精度和优化器更新的含义都不变。训练状态按每参数 … |
| …意力的二次项。每层前向与反向合计按四次张量并行 AllReduce 计，8192-token 的 BF16 隐藏张量为 80 MiB，64 层合计约 0.195 … | …意力的二次项。每层前向与反向合计按四次张量并行 AllReduce 计，8192 个 token 的 BF16 隐藏张量为 80 MiB，64 层合计约 0.195 … |
| …数据并行为 128、64、32、16，全局 token 数不变。每个数据并行副本处理的 8192-token 序列数随之增加，局部矩阵的行数和张量并行的传输量都要重算；假设这些形状都能达到本例的有效算力，… | …数据并行为 128、64、32、16，全局 token 数不变。每个数据并行副本要处理的序列数随之增加，每条序列仍是 8192 个 token，局部矩阵的行数和张量并行的传输量都要重算；假设这些形状都能达到本例的有效算力，… |

## 翻译腔审查（2026-09-11）

作者确认术语原则并统一“笔者”“N 个 token”“Queqiao”之后，再做一遍只针对翻译腔的审查（含图注里汉字数字改阿拉伯数字）。共 16 行有改动，按最终文本与审查前快照逐行对照。

| 修改前 | 修改后 |
|---|---|
| 这个式子给出传输时间的下界：无论怎样调度，链路每秒最多只能传送其带宽所允许的字节数。全双工链路的两个方向分别计算；若多个端口共享内部接口，再对该接口计算合计流量。… | 这个式子给出传输时间的下界：无论怎样调度，链路每秒传的字节数都不会超过它的带宽。全双工链路的两个方向分别计算；若多个端口共享内部接口，再对该接口计算合计流量。… |
| 分层归约先通过较快的本地互联完成部分归约，再将结果通过较慢的网络发送，以额外的本地通信减少跨服务器传输。本地归约增加的时间少于它节省的远端传输时间时，分层归约才更快。 | 分层归约先在较快的本地互联上完成部分归约，再把结果经较慢的网络发出，用多一些本地通信换少一些跨服务器传输。只有本地归约多花的时间少于它省下的远端传输时间，分层归约才更快。 |
| …。借用相邻的出口要先把数据送到相邻 GPU，多了一段本地传输。FuseLink 就是走这条路：通过机内 GPU 互联和网络缓冲区的重新映射来使用空闲网卡。[^fuselink] | …。借用相邻的出口要先把数据送到相邻 GPU，多了一段本地传输。FuseLink 走的就是这条路：借助机内的 GPU 互联，把网络缓冲区重新映射到空闲的网卡上。[^fuselink] |
| 本书一组跨主机 RPC 测量中，请求大小从约 1.33 MiB 降至 1.00 Mi… | 本书配套的一组跨主机 RPC 测量中，请求大小从约 1.33 MiB 降至 1.00 Mi… |
| *图 7-18：每次使用都访问远端，会重复传送同一份数据；下半图先搬回本地，再进行多次复用。两种路径处理同一份不变快照。* | *图 7-18：每次使用都访问远端，会重复传送同一份数据；下半图先搬回本地，再多次复用。两种路径处理同一份不变快照。* |
| 上一小节用复用减少远端读取的次数。对于仍要经过网络的请求，接下来要看它们能否让链路一直有数据可传。下面分析本章算例中服… | 上一小节用复用减少远端读取的次数。仍要经过网络的请求，接下来要看它们能否让链路一直有数据可传。下面分析本章算例中服… |
| **设计案例：按需指定顺序的硬件开销。** 作者的 OpenURMA 实现把 UB 的四种顺序服务模式和三种执行标签放在同一条… | **设计案例：按需指定顺序的硬件开销。** 笔者的 OpenURMA 实现把 UB 的四种顺序服务模式和三种执行标签放在同一条… |
| …接收方排序是把等待挪到了接收一侧，冲突检查则负责判断哪些提前读到的结果仍然有效。为简化编程而提供顺序保证，需要保存这些状态并执行相应检查。 | …接收方排序是把等待挪到了接收一侧，冲突检查则负责判断哪些提前读到的结果仍然有效。要靠顺序保证来简化编程，就得保存这些状态并做相应的检查。 |
| CASSINI 利用训练通信的周期性，通过安排作业的放置和通信时刻错开高峰，并监测漂移、重新调整。[^cassini] 本书用两个在 CPU 上运行的分布式数据并行（DDP）训练作业做过一次性错峰的对照：… | CASSINI 利用训练通信的周期性，安排作业的放置和通信时刻来错开高峰，并监测漂移、重新调整。[^cassini] 本书配套实验用两个在 CPU 上运行的分布式数据并行（DDP）训练作业做过一次性错峰的对照：… |
| …反馈生效时刻决定队列满载状态持续多久，降速幅度决定下降段有多陡。各种拥塞控制机制都是通过这两个量来改变队列的。**显式拥塞通知**（ECN）由交换机在报文上打标记，把队列信息带回发送方；*… | …反馈生效时刻决定队列满载状态持续多久，降速幅度决定下降段有多陡。各种拥塞控制机制改变队列，靠的都是这两个量。**显式拥塞通知**（ECN）由交换机在报文上打标记，把队列信息带回发送方；*… |
| …。把“持有前一项资源、申请后一项资源”画成边，就得到资源依赖图。环上的每项资源都被持有时，所有释放条件同时被卡住。 | …。把“持有前一项资源、申请后一项资源”画成边，就得到资源依赖图。环上的每项资源都有人持有时，所有释放条件就一起卡住了。 |
| 虚拟通道通过逻辑上分开的队列实现资源划分。把请求与响应分入不同通道，再规定资源申请方向，可以建立无环的依赖结构。这里真正… | 虚拟通道用逻辑上分开的队列来划分资源。把请求与响应分入不同通道，再规定资源申请方向，可以建立无环的依赖结构。这里真正… |
| 前面沿一份数据追踪了传输、等待、完成通知和空间释放。要判断这些改进能节省多少训练时间，还必须结合产生和使用这份数据的计算过程来分析。到这里，我们已经减少了归约需要传输的数据量，配置了足够的并发请求，并让请求记录… | 前面沿一份数据追踪了传输、等待、完成通知和空间释放。要判断这些改进能省下多少训练时间，还得把产生和使用这份数据的计算过程一起算进来。到这里，我们已经减少了归约需要传输的数据量，配置了足够的并发请求，并让请求记录… |
| …步分析发现，这一差异与前向计算期间的主机操作有关。[^megascale] 本书用集合通信库 Gloo 在 CPU 上做的四进程测量也是同样的关系：4 KiB 输入下，当一个参与者晚到约 25.1 ms 时，全组完成… | …步分析发现，这一差异与前向计算期间的主机操作有关。[^megascale] 本书配套实验用集合通信库 Gloo 在 CPU 上做的四进程测量也显示同样的关系：4 KiB 输入下，当一个参与者晚到约 25.1 ms 时，全组完成… |
| …动。绿色更新要等计算和通信都结束后才能开始。这三个模型也给出了清楚的改进顺序。先通过分层归约减少跨服务器传输，再通过并发提交和处理请求，充分利用链路带宽；达到带宽上限之后，寻找可以提早开始的通信；通信时间被计算时间完全覆盖后，优化此时决定完成时间的计算或更新操作。 | …动。绿色更新要等计算和通信都结束后才能开始。这三个模型也给出了清楚的改进顺序。先用分层归约减少跨服务器传输，再靠并发提交和处理请求用满链路带宽；到了带宽上限，就找可以提早开始的通信；通信时间被计算完全盖住之后，再优化此时决定完成时间的计算或更新。 |
| 继续分析同一个 DeepSeek V4.1 会话，设请求被迁移到一个没有它的全局 KV 的实例。先计算全局历史这一段载荷的传输时间：上下文为 128K，有效带宽为 25 GB… | 继续分析同一个 DeepSeek V4.1 会话，设这条请求要迁移到一个没有它的全局 KV 的实例上。先计算全局历史这一段载荷的传输时间：上下文为 128K，有效带宽为 25 GB… |
