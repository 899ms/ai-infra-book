# 第 4 章《加速器架构》翻译腔清扫（2026-09-11）

章节文件：`manuscripts/04-加速器架构.md`。本章此前已经过两轮语言通读（见 `research/ub-ep-integration-2026-09-10/language-pass-ch04.md`），本次只清扫残留的翻译腔。共改动 **14 处**，均为句式理顺，未改动任何技术内容、数字、公式、代码块、图片路径或章节结构。

| 修改前 | 修改后 |
|---|---|
| …对本例，$I(M)$ 最终趋近 2,048 FLOPs/byte。因此，继续增大 batch **所带来的**收益会逐渐减小… | …对本例，$I(M)$ 最终趋近 2,048 FLOPs/byte。因此，继续增大 batch 的收益会逐渐减小… |
| SM 内**由线程协作使用的软件管理缓冲**称为共享内存（shared memory）。 | SM 内**由线程协作使用、由软件管理的缓冲**称为共享内存（shared memory）。 |
| …以原始矩阵运算速度为基准，**将**加速比从两倍提高到四倍，**与把**其余工作从 40 μs **降至** 25 μs，哪种改动节省的时间更多？ | …以原始矩阵运算速度为基准，**把**加速比从两倍提高到四倍，**和把**其余工作从 40 μs **降到** 25 μs，哪种改动节省的时间更多？ |
| 面积之外，功耗是另一项预算；芯片越大、卡越多，功耗**越常**成为决定性的约束。 | 面积之外，功耗是另一项预算；芯片越大、卡越多，功耗**越容易**成为决定性的约束。 |
| **能耗取决于被充放电的电容**，电容又随距离增长… | **能耗取决于要充放电的电容**，电容又随距离增长… |
| 每对需要四次乘法、两次加减，**共 15,360 FLOPs/token**。 | 每对需要四次乘法、两次加减，**每个 token 共 15,360 FLOPs**。 |
| **按行连续存储的矩阵的** $s_c=b$，行步长由完整行宽决定。 | **矩阵按行连续存储时** $s_c=b$，行步长由完整行宽决定。 |
| …需要多少在途事务？** **同时**把在途事务数提高到至少 7,000 个，才能维持 1,792 GB/s… | …需要多少在途事务？** 把在途事务数提高到至少 7,000 个，才能维持 1,792 GB/s… |
| 扩展增加了资源，也**增加了**生成数据**的**单元**与**使用数据**的**单元之间的距离。 | 扩展增加了资源，也**拉长了**生成数据**和**使用数据的单元之间的距离。 |
| …通用寄存器便可用于其他线程状态。**这是增加专用存储对工作驻留的直接影响。** | …通用寄存器便可用于其他线程状态。**这就是新增专用存储给工作驻留带来的直接变化。** |
| **CNN 中受益的**卷积窗口展开，在 Q、K、V 投影上没有工作可做… | **在 CNN 中发挥作用的**卷积窗口展开，在 Q、K、V 投影上没有工作可做… |
| Qwen3-235B-A22B **在本书的 4-bit 存储方案中，包括**尺度与保留高精度的张量，常驻权重约 123.142 GB。 | Qwen3-235B-A22B **采用本书的 4-bit 存储方案，连同**尺度与保留高精度的张量，常驻权重约 123.142 GB。 |
| 沿用该项目历史执行记录中的一组张量工作，共 15,134,641,792 次操作。 | 工作负载取自该项目的历史执行记录：一组张量操作，共计 15,134,641,792 次。 |
| 机器不变，仅改变矩阵尺寸，Mac **保持能耗优势所允许的**功率就从约 155 W 降到约 10.7 W。 | 机器不变，仅改变矩阵尺寸，Mac **要保持能耗优势，允许的**功率就从约 155 W 降到约 10.7 W。 |
