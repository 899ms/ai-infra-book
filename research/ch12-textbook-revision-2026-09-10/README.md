# 第十二章教材体例重写

本次按用户要求重写全章，修订前正文、构建器与校验器保存于 `before.tar.gz`。

## 教学结构

- 以资源服务、执行依赖、实际供给三层建立性能模型，在图片、音频、分工、传输与部署中反复使用。
- 图片从 12.8 秒串行预算推导加速、压缩和分块重叠；语音解释三个最大值各自代表的等待。
- 截图主例统一为 30 轮，0.8 MB 输入、0.3 秒终端工作、2 秒原服务模型、0.2 秒 RTT、1 秒上传。部署章节从前十轮完成后继续，比较剩余二十轮。
- 删除阶段切分的独立 10 MB 小例、重复的唤醒与图编译回本数字，统一采用迁移的摊销模型。
- 分工以消去共同工作、比较编码与表示差额展开；TP 解释频繁同步怎样沿依赖累积，PP 解释单请求等待与多请求吞吐。
- Queqiao 采用解释、预测、匹配对照、机制轨迹、下一步选择的案例结构。
- 部署分解云端与附近的 6 秒差额，增加倒数关系导致的带宽波动惩罚、期限余量及保留进度的恢复比较。
- 八道练习改成分步小问，计算、条件变化和机制解释各有独立目标；更新配套数值答案。

## 语言与证据

删除正文段末自我辩护式适用范围声明，模型条件在定义与例题开头交代。脚注保留来源、实验边界与统计信息。理论模型给出明确判断，实验解释与记录的支持范围对应。

保留六节、20 小节、七图与原有科学来源。图 12-7 纵轴改为剩余二十轮时间，其计算数据保持一致；图注同步梳理。SVG、PNG、PDF 和 HTML 重新生成。

## 检查

校验器新增主例连贯性、连接复用、恢复回本轮数、云近时间差、带宽变化与恢复结果的检查。检查结构、公式、来源及输出哈希、链接和图内标签；浏览器检查桌面与手机尺寸。最新结果见 `manuscripts/ch12/validation.json`、`math-validation.json`、`browser-validation.json`。
