# 预登记：恢复时从2个worker改为1或4个

固定复用原1061/1062/1063三seed的17/41步检查点及各自64步不中断基线；只新增恢复后缀，不重跑基线或保存前缀。恢复worker取1、4，prefetch_factor仍2，顺序DataLoader、确定性文本读取，模型、优化器及训练条件不变。

共12个恢复进程，420次后续真实更新。每次必须实际观察到指定数量worker读取记录；保存的未消费记录从消费游标重新读取。每步输入字节/位置、loss、梯度范数和最终模型、Adam、两份RNG、packing残留、消费游标必须与原基线精确一致。派发游标可能随worker数不同，不作为逻辑状态相等要求，完整保留并报告。

源文件、原checkpoint与基线固定SHA；正式运行源码SHA写入environment。每条件只执行一次，不用启动/吞吐计时做worker数量的性能排名，不添加随机增强或把本轮外推为随机多worker训练恢复。若失败，先记录并分析原因，不改变数值精确门槛。
