--- before/02-模型架构.md
+++ manuscripts/02-模型架构.md
@@ -2,11 +2,11 @@
 
 第一章从存储容量、运算量和数据读取量三个方面分析了一次模型执行，并借助硬件的关键数字估算时间下界。这样的估算可以迅速排除一些方案，却还留下一个问题：所代入的计算量和数据量从哪里来？“一个 8B 模型”只说明参数数量的大致范围，没有说明每个 token 使用哪些参数、上下文保存在什么地方，也没有说明不同工作之间的先后关系。
 
-本章从一个 token 的数值表示开始，逐步分析它经过网络各层后，如何得到下一个 token 的分数，再计算这一过程所需的存储容量 $M$、运算量 $F$ 和读写量 $R$。计算图用节点表示运算，用边表示运算之间传递的数据。先认识图上的对象，才能知道资源预算中的每个数字来自哪里。
-
-这张计算图也提供了优化实现所需的信息。哪些权重在不同请求间保持不变，哪些状态会被后续生成反复使用，哪些结果只供紧接着的运算使用，都可以从模型结构中找到。后面的存储、编译和调度设计将利用这些数据在用途上的区别，决定哪些数据可以共享、哪些需要长期保存、哪些只需暂存。
-
-我们使用 Qwen3-8B 作为贯穿例子。这是一个约含 81.9 亿参数的语言模型，通过 36 层网络处理文本。先完整地理解这个模型，再改变上下文的保存方式、每个 token 使用的参数集合和层间连接，比较 Qwen3.6-35B-A3B（下文简称 Qwen3.6）、DeepSeek V4-Flash 与 Kimi K3 的资源需求。
+本章从一个 token 的数值表示开始，逐步分析它经过网络各层后，如何得到下一个 token 的分数，再计算这一过程所需的存储容量 $M$、运算量 $F$ 和读写量 $R$。计算图用节点表示运算，用边表示运算之间传递的数据。先认识图中的运算和数据，才能知道资源预算中的每个数字来自哪里。
+
+这张计算图也提供了优化实现所需的信息。哪些权重在不同请求间保持不变，哪些状态会被后续生成反复使用，哪些结果只供紧接着的运算使用，都可以从模型结构中找到。后面的存储、编译和调度设计将利用这些数据用途的差异，决定哪些数据可以共享、哪些需要长期保存、哪些只需暂存。
+
+我们使用 Qwen3-8B 作为贯穿例子。这是一个约含 81.9 亿参数的语言模型，通过 36 层网络处理文本。先把这个模型的执行过程弄清楚，再改变上下文的保存方式、每个 token 使用的参数集合和层间连接，比较 Qwen3.6-35B-A3B（下文简称 Qwen3.6）、DeepSeek V4-Flash 与 Kimi K3 的资源需求。
 
 统计运算量时，一次乘法加一次加法计为 2 FLOPs。存储量采用 GB 和 GiB：$1\ \mathrm{GB}=10^9\ \mathrm{bytes}$，$1\ \mathrm{GiB}=2^{30}\ \mathrm{bytes}$。BF16 是每个数占 16 位的浮点表示，每个元素占 2 字节，因此将张量的元素数乘以 2，就得到它的存储大小。
 
@@ -36,11 +36,11 @@
 
 > **练习 2-1〔延伸〕：RNN 与 Transformer 的依赖如何限制并行计算**
 >
-> 对于含四个已知输入位置的序列，分别画出三层 RNN 和三层因果 Transformer 的计算依赖图。再增加一个待生成的输出位置，指出计算该位置之前必须等待哪个节点完成。设每层每位置的计算耗时为一个单位、并行资源充足，分别求完成已知输入的最长依赖链长度；解释增加计算单元可以缩短哪些等待、不能缩短哪些等待。
+> 对于含四个已知输入位置的序列，分别画出三层 RNN 和三层因果 Transformer 的计算依赖图。再增加一个待生成的输出位置，指出计算该位置之前必须等待哪个节点完成。设每层每位置的计算耗时为一个单位、并行资源充足，分别求处理全部已知输入时的最长依赖链长度；解释增加计算单元可以缩短哪些等待、不能缩短哪些等待。
 
 ### 2.1.2 模型配置与张量结构
 
-先把一个计算节点展开。设本次对 $m$ 个输入 token 执行投影，每个 token 用一个包含 $k$ 个特征的向量表示。把这些向量逐行排成输入矩阵 $X$，它就有 $m$ 行、$k$ 列，输出宽度为 $n$：
+先把一个计算节点展开。设本次对 $m$ 个输入 token 执行投影，每个 token 用一个包含 $k$ 个特征的向量表示。把这些向量逐行排成输入矩阵 $X$，它就有 $m$ 行、$k$ 列，设输出宽度为 $n$，则有：
 
 $$
 X\in\mathbb R^{m\times k},\qquad W\in\mathbb R^{k\times n},\qquad Y=XW\in\mathbb R^{m\times n}.
@@ -60,7 +60,7 @@
 
 一次前向执行从 token ID（词元在词表中的整数编号）开始。词嵌入为每个 ID 查出一个 4096 维向量，向量依次经过 36 层注意力与 FFN，最后经过归一化和词表投影产生 logits，即尚未转换为概率的候选 token 分数。归一化按向量的统计量调整各特征的尺度，使逐层计算的数值保持在适当范围内。词嵌入按 ID 选择行，一个 token 取出一条向量，整个词表则作为常驻数据供不同 token 查询。词表输出头则将隐藏向量与词表矩阵相乘；只生成下一个 token 时，通常只需要最后一个输入位置的 logits。
 
-设同时处理 $B$ 条等长请求，每条本次输入 $P$ 个已知 token。投影和 FFN 可以把它们的输入表示为 $X\in\mathbb{R}^{BP\times4096}$，每一行是一个 token 在当前层的表示，共有 $m=BP$ 行。已有上下文中从缓存读取的键和值所对应的位置，不计入这里的 $m$。每条请求只补入一个 token 时，$m=B$。第一种情形容易形成行数较大的矩阵，第二种在小 batch 下接近矩阵与向量的乘法。权重相同，输入形状却明显不同。
+设同时处理 $B$ 条等长请求，每条本次输入 $P$ 个已知 token。投影和 FFN 可以把它们的输入表示为 $X\in\mathbb{R}^{BP\times4096}$，每一行是一个 token 在当前层的表示，共有 $m=BP$ 行。从缓存读取键和值的已有上下文位置，不计入这里的 $m$。每条请求只补入一个 token 时，$m=B$。第一种情形容易形成行数较大的矩阵，第二种在小 batch 下接近矩阵与向量的乘法。权重相同，输入形状却明显不同。
 
 下面比较两种输入形状：一条请求一次处理 8192 个输入位置，以及一条已有 8192 个上下文位置的请求再处理一个新位置。用 $B$ 表示请求数，$S$ 表示调用前已有的上下文长度，$P$ 表示本次输入长度，$d$ 表示隐藏维度。前者取 $B=1,S=0,P=8192$，后者取 $B=1,S=8192,P=1$。
 
@@ -207,7 +207,7 @@
 
 > **练习 2-2〔核心〕：输入行数相同，注意力计算量为何不同**
 >
-> 使用表 2-1，取 $B=4,S=4096,P=1024$，分别计算投影与 FFN、有效因果注意力，以及最后一个位置的词表输出层所需的 FLOPs。保持 $BP=4096$，改为 $B=1,S=4096,P=4096$，先预测哪些项相同，再计算差异。最后比较两种输入处理方式：完整处理 8K 输入，或复用前 6144 个位置的 KV、只处理新增的 2048 个位置。求后一种方式节省的矩阵运算比例。
+> 使用表 2-1，取 $B=4,S=4096,P=1024$，分别计算投影与 FFN、有效因果注意力，以及最后一个位置的词表输出层所需的 FLOPs。保持 $BP=4096$，改为 $B=1,S=4096,P=4096$，先预测哪些项相同，再计算差异。最后比较两种输入处理方式：完整处理 8K 输入，或复用前 6144 个位置的 KV、只处理新增的 2048 个位置。求后一种方式相对于完整处理输入，节省了多大比例的矩阵运算量。
 
 ## 2.2 自回归生成的资源消耗计算
 
@@ -219,7 +219,7 @@
 
 如果每生成一个 token 都把整个前缀重新送入模型，旧位置的投影与 FFN 会重复执行。因果模型中，旧位置不能看到后来加入的 token；在权重、位置与相关计算条件不变时，旧位置产生的 $K$、$V$ 可以保留。后续查询读取这些上下文 $K$、$V$，并补入当前位置的新 $K$、$V$，就避免了旧位置的大量重算。
 
-缓存把“重新计算旧位置的表示”变成“保存并访问旧位置的状态”。未来位置会产生自己的 $Q$，用它对旧位置的 $K$ 评分，再以分数汇总 $V$，所以长期保存的是 $K$ 和 $V$。旧查询已经完成了自己的任务，下一步使用新的查询。第八章将进一步讨论如何在不同请求之间共享这些状态。
+缓存把“重新计算旧位置的表示”变成“保存并访问旧位置的状态”。未来位置会产生自己的 $Q$，用它对旧位置的 $K$ 评分，再根据评分得到的权重汇总 $V$，所以长期保存的是 $K$ 和 $V$。旧查询已经完成了自己的任务，下一步使用新的查询。第八章将进一步讨论如何在不同请求之间共享这些状态。
 
 Qwen3-8B 的 BF16 KV 每个上下文 token 占：
 
@@ -243,7 +243,7 @@
 
 对于 $[m,d]\times[d,f]$，每一行都使用同一个权重矩阵。将权重块读入片上存储后，可以依次用于多行输入，让一次读取的数据参与更多乘加运算。处理行数 $m$ 增加，计算量随之增加，整份矩阵的容量保持不变。第五章将进一步解释片上容量与分块怎样决定一份权重能够复用多少次。
 
-临时数据占用多少内存，取决于哪些张量同时存在。前一层释放缓冲后，后一层可以复用同一块空间；算子融合还可以让部分中间值只保存在寄存器或片上存储中。因此，计算读写量时要数清每次访问，计算内存峰值时则要找出同时存活的张量。第五章将结合具体程序分析。
+临时数据占用多少内存，取决于哪些张量同时存在。前一层释放缓冲后，后一层可以复用同一块空间；算子融合还可以让部分中间值只保存在寄存器或片上存储中。因此，计算读写量时要数清每次访问，计算内存峰值时则要找出同时仍需保存的张量。第五章将结合具体程序分析。
 
 存储量取决于某一时刻需要保留哪些数据，访问量则取决于执行过程中读写了多少次数据。例如，一步 decode 若将整段旧上下文读一遍，逻辑读取量为 $c_{\mathrm{KV}}H$；连续执行多步，就要逐步累计。确定这些访问实际经过哪一级存储后，才能将相应的 $R$ 除以该接口的带宽。
 
@@ -297,9 +297,9 @@
 
 模型继续生成时，需要利用前面已经读过的内容。先想象两种记录办法：一种逐页留下记录，要查哪一页就取哪一页；另一种只保留一张定长摘要，每读到新内容，就改写这张摘要。前一种记录随历史增长，后一种大小固定，但不能保证找回每个位置的全部细节。
 
-对应到模型，保存逐位置 KV 的机制留下各位置的键和值，有限状态递推则把历史贡献汇入固定大小的状态。这里的“记录”和“摘要”实际都是供计算使用的数值向量或矩阵，并非自然语言笔记。模型处理已有上下文后保存、供后续计算复用的这些表示，称为上下文状态。上下文长度按 token 位置计数，状态容量按实际保存的字节数计量。
-
-比较这些机制时，先看历史留下了什么，再看新一步怎样取得所需信息：每个位置可以保存得更窄，也可以只直接访问近期位置、将多个位置汇总为较少条目，或先筛选再读取。另一类机制持续更新固定大小的状态，查询从中取得结果。这些安排分别改变表示宽度、条目数量和访问方式；省下的存储与读取，要同新增的选择、压缩和更新工作一起计算。
+对应到模型，保存逐位置 KV 的机制留下各位置的键和值，有限状态递推则把历史贡献汇入固定大小的状态。逐位置记录与递推摘要都以数值向量或矩阵保存。模型处理已有上下文后保存、供后续计算复用的这些表示，称为上下文状态。上下文长度按 token 位置计数，状态容量按实际保存的字节数计量。
+
+比较这些机制时，先看历史留下了什么，再看新一步怎样取得所需信息：可以用更少的维度表示每个位置，也可以只直接访问近期位置、将多个位置汇总为较少条目，或先筛选再读取。另一类机制持续更新固定大小的状态，查询从中取得结果。这些安排分别改变表示宽度、条目数量和访问方式；省下的存储与读取，要同新增的选择、压缩和更新工作一起计算。
 
 ### 2.3.1 多头注意力与 KV 共享
 
@@ -351,11 +351,11 @@
 
 > **练习 2-3〔延伸〕：输出变长后，KV 容量与累计读取量如何增长**
 >
-> 取 Qwen3-8B、$B=1,S=0,P=8192$，分别返回 $G=513$ 和 $G=1025$ 个 token。求后续 decode 次数、最终 KV 大小和累计旧上下文读取量。解释输出数量接近翻倍时，新增状态、总状态与累计读取为什么有不同的比例。再将 KV 位宽减半，指出这三项中哪些随之减半。
+> 取 Qwen3-8B、$B=1,S=0,P=8192$，分别返回 $G=513$ 和 $G=1025$ 个 token。求后续 decode 次数、最终 KV 大小和累计旧上下文读取量。解释输出数量接近翻倍时，新增 KV 容量、最终 KV 总容量与累计读取量为何按不同的比例增长。再将 KV 位宽减半，指出这三项中哪些随之减半。
 
 ### 2.3.3 局部窗口、上下文压缩与稀疏索引
 
-假设模型已经读过一段很长的文本，现在要处理一个新位置。它可以直接读取最近一段位置，也可以读取由多个旧位置汇成的条目，还可以先找出与当前查询相关的条目，再读取它们的主状态。这三种动作分别引出局部窗口、上下文压缩与稀疏选择。它们可以组合使用，但要分别计量：窗口限制近期范围，压缩减少长期条目，索引选择当前要读哪些条目。筛选本身也要读取索引表示并计算分数，不能只统计最后选中的主状态。
+假设模型已经读过一段很长的文本，现在要处理一个新位置。它可以直接读取最近一段位置，也可以读取由多个旧位置汇成的条目，还可以先找出与当前查询相关的条目，再读取它们的主注意力状态。这三种动作分别引出局部窗口、上下文压缩与稀疏选择。它们可以组合使用，但要分别计量：窗口限制近期范围，压缩减少长期条目，索引选择当前要读哪些条目。筛选本身也要读取索引表示并计算分数，不能只统计最后选中的主注意力状态。
 
 先把这里的三个对象分开。**局部滑动窗口注意力（Sliding-Window Attention，SWA）**直接读取最近的一段位置；窗口长 128 时，较早位置退出这一局部窗口。**全局主 KV**是供主注意力读取的长期表示，可能已将多个位置合并；“全局”说明它覆盖较长历史，不表示每次都读取全部条目。**索引 K**是用于检索的另一份较小表示：索引器先用它给条目打分，再让主注意力读取选中的主 KV。索引分数用于选位置，主注意力另用自己的查询计算最终汇总权重。
 
@@ -363,17 +363,17 @@
 
 DeepSeek V4-Flash 有 43 个主干层、4096 隐藏维度、64 个注意力头，每头维度 512。$Q$ 先从 4096 维投影到 1024 维，再得到 $64\times 512$ 维查询；共享 KV 从 4096 维投影到 512 维；输出采用分 8 组的低秩投影。各投影的输入、输出维度分别确定矩阵形状。
 
-DeepSeek V4-Flash 使用两种压缩注意力：**压缩稀疏注意力（Compressed Sparse Attention，CSA）**先压缩上下文，再通过索引选取相关条目；**高压缩注意力（Heavily Compressed Attention，HCA）**把更多位置汇成较少的粗粒度条目，供查询访问。两者都用于减少长上下文的存储与访问量，区别在于压缩程度和是否进一步筛选条目。其主干包含 2 个纯窗口层、21 个 CSA 层和 20 个 HCA 层，窗口长度为 128。CSA 按压缩比 4 保存上下文，并维护索引；HCA 按压缩比 128 保存更粗的上下文。用 BF16 参考表示，CSA 每个完成的压缩条目含 512 维主状态，另有 128 维索引条目。已有 $S$ 个位置时，已生成的条目数按 $\lfloor S/4\rfloor$ 计算；尚未完成的压缩块还需独立缓冲。
+DeepSeek V4-Flash 使用两种压缩注意力：**压缩稀疏注意力（Compressed Sparse Attention，CSA）**先压缩上下文，再通过索引选取相关条目；**高压缩注意力（Heavily Compressed Attention，HCA）**把更多位置汇成较少的粗粒度条目，供查询访问。两者都用于减少长上下文的存储与访问量，区别在于压缩程度和是否进一步筛选条目。其主干包含 2 个纯窗口层、21 个 CSA 层和 20 个 HCA 层，窗口长度为 128。CSA 按压缩比 4 保存上下文，并维护索引；HCA 按压缩比 128 保存更粗的上下文。用 BF16 参考表示，CSA 每个完成的压缩条目含 512 维主注意力状态，另有 128 维索引条目。已有 $S$ 个位置时，已生成的条目数按 $\lfloor S/4\rfloor$ 计算；尚未完成的压缩块还需独立缓冲。
 
 压缩比描述输出条目数，不一定等于一个条目的全部输入范围。V4 的 CSA 每四个新位置生成一条记录，但采用相邻块重叠：除首块的边界处理外，一个条目通过两组不同投影汇总相邻两块、共八个位置的信息；每个位置还使用学习得到的逐通道权重。HCA 每 128 个位置生成一条记录，不采用这种相邻块重叠，也不使用 Top-K 索引，而是读取所有已完成的粗粒度条目。V4.1 的 CSA2 才取消了 V4 CSA 的重叠压缩。
 
-一次 CSA 查询的主注意力最多读取窗口加 512 个压缩条目，但索引打分仍扫描已保存的压缩索引条目。在 $S=8192$ 时，一个 CSA 层保留 2048 个压缩条目：主注意力状态约 2 MiB、索引约 0.5 MiB，窗口约 0.125 MiB；主注意力选择的压缩条目最多约 0.5 MiB，索引扫描却仍需处理那 2048 个索引条目。HCA 则有 64 个已生成的条目，主注意力状态约 0.0625 MiB，并访问窗口状态与这组粗粒度压缩条目。
-
-将 43 层相加，8192-token 上下文下的窗口、压缩上下文和索引约为 59.125 MiB；另有约 11.641 MiB 的 FP32（每元素 4 字节浮点格式）压缩器缓冲，合计约 70.8 MiB。最后查询的主注意力与索引数据量约 27.6 MiB。[状态分解](../calculations/results/state-deepseek-v4-flash-n8192-b1-native.md)分别列出了这些数据，因此能看出选中的条目数、保存的状态大小和每步读取量分别代表什么。
-
-![上下文压缩与索引选择的先后关系。上方只示意八个位置产生两条记录的数量关系，省略 V4 CSA 的重叠投影与加权汇总，下方展示一次查询先扫描索引再读取主状态的过程。](ch02/figure-2-5-sparse.svg)
-
-*图 2-13　上下文压缩与索引选择的先后关系。上方只示意八个位置产生两条记录的数量关系，省略 V4 CSA 的重叠投影与加权汇总，下方展示一次查询先扫描索引再读取主状态的过程。*
+一次 CSA 查询的主注意力最多读取窗口加 512 个压缩条目，但索引打分仍扫描已保存的压缩索引条目。在 $S=8192$ 时，一个 CSA 层保留 2048 个压缩条目：主注意力状态约 2 MiB、索引约 0.5 MiB，窗口约 0.125 MiB；主注意力选中的压缩条目合计最多约 0.5 MiB，索引扫描却仍需处理那 2048 个索引条目。HCA 则有 64 个已生成的条目，主注意力状态约 0.0625 MiB，并访问窗口状态与这组粗粒度压缩条目。
+
+将 43 层相加，8192-token 上下文下的窗口、压缩上下文和索引约为 59.125 MiB；另有约 11.641 MiB 的 FP32（每元素 4 字节浮点格式）压缩器缓冲，合计约 70.8 MiB。最后一次查询的主注意力与索引读取量合计约为 27.6 MiB。[状态分解](../calculations/results/state-deepseek-v4-flash-n8192-b1-native.md)分别列出了这些数据，因此能看出选中的条目数、保存的状态大小和每步读取量分别代表什么。
+
+![上下文压缩与索引选择的先后关系。上方只示意八个位置产生两条记录的数量关系，省略 V4 CSA 的重叠投影与加权汇总，下方展示一次查询先扫描索引再读取主注意力状态的过程。](ch02/figure-2-5-sparse.svg)
+
+*图 2-13　上下文压缩与索引选择的先后关系。上方只示意八个位置产生两条记录的数量关系，省略 V4 CSA 的重叠投影与加权汇总，下方展示一次查询先扫描索引再读取主注意力状态的过程。*
 
 ![DeepSeek V4-Flash 在 8K 上下文下的四项状态。窗口、压缩上下文和索引保存已处理的上下文信息；压缩缓冲保存尚在汇总或更新中的数据，单独计入容量。](ch02/figure-2-sparse-capacity.svg)
 
@@ -387,7 +387,7 @@
 
 > **练习 2-4〔延伸〕：KV 头共享与上下文压缩如何改变存储和访问量**
 >
-> 先把 Qwen3-8B 的 KV 头数从 8 改为 4，保持 32 个查询头不变，分别计算 KV 容量、KV 投影参数量，以及查询与上下文之间的运算量如何变化。再对 DeepSeek V4-Flash 的 CSA 层，把上下文从 8192 增至 16384，按压缩比 4、每次最多选择 512 个压缩条目，计算保存条目数、索引扫描条目数和选中条目上限。解释三者为何不按同一比例增长。
+> 先把 Qwen3-8B 的 KV 头数从 8 改为 4，保持 32 个查询头不变，分别计算 KV 容量、KV 投影参数量，以及查询与上下文交互所需运算量的变化。再对 DeepSeek V4-Flash 的 CSA 层，把上下文从 8192 增至 16384，按压缩比 4、每次最多选择 512 个压缩条目，计算保存条目数、索引扫描条目数和选中条目上限。解释三者为何不按同一比例增长。
 
 ### 2.3.4 线性注意力与有限状态递推
 
@@ -409,7 +409,7 @@
 
 Kimi K3 每个 KDA 层有 96 个头，每头递推矩阵为 $128\times128$。按 FP32 保存，单层为 6 MiB，69 层为 414 MiB。一次 decode 读取旧矩阵、融合新信息并写回，理想情况下，各读写一次的总量就是 828 MiB。这个量不随上下文长度增长：短上下文时它是一笔显著的固定开销，长上下文时则避免了扫描量随位置数持续增加。
 
-prefill 与 decode 的实现方式也不同。decode 使用单步递推；一段已知输入可以通过块式算法并行计算，再在块之间传递状态。块内中间量和块间状态会占用临时空间，因此仅凭最终递推状态的大小，还无法预测 prefill 期间的内存占用峰值。第五章按缓冲寿命分析这一问题。[^source-15]
+prefill 与 decode 的实现方式也不同。decode 使用单步递推；一段已知输入可以通过块式算法并行计算，再在块之间传递状态。块内中间量和块间状态会占用临时空间，因此仅凭最终递推状态的大小，还无法预测 prefill 期间的内存占用峰值。第五章根据缓冲区从分配到释放的过程分析这一问题。[^source-15]
 
 ### 2.3.5 混合注意力的状态构成
 
@@ -417,7 +417,7 @@
 
 Kimi K3 共 93 层，其中 69 个 KDA 层、24 个 MLA 层。8192-token 上下文、紧凑 BF16 MLA、FP32 递推状态及给定 BF16 短卷积槽位下，三项分别为 216 MiB、414 MiB 与 19.4 MiB，合计约 649.4 MiB。若使用参考展开缓存，MLA 一项就变为 11.25 GiB，整体随之改变。[^source-16]
 
-Qwen3.6-35B-A3B 同样需要分别计算线性注意力层和完整注意力层的状态占用。其 30 个线性注意力层加 10 个完整注意力层，使每请求每个上下文 token 的全局 KV 为 20 KiB，另有固定 FP32 递推状态 60 MiB，以及保存短卷积所需近期输入的 BF16 缓冲区 1.875 MiB。短卷积先混合邻近位置的信息，缓冲区让下一步更新可以复用这些输入。8192-token 上下文时，三项合计 221.9 MiB。这些状态来自注意力分支；接下来的混合专家（Mixture of Experts，MoE）分支准备多个前馈子网络，由路由器为每个 token 选择其中一部分执行，决定同一主干中实际使用哪些权重，两类机制共同组成完整模型。
+Qwen3.6-35B-A3B 同样需要分别计算线性注意力层和完整注意力层的状态占用。其 30 个线性注意力层加 10 个完整注意力层，使每请求每个上下文 token 的全局 KV 为 20 KiB，另有60 MiB 的固定 FP32 递推状态，以及保存短卷积所需近期输入的 BF16 缓冲区 1.875 MiB。短卷积先混合邻近位置的信息，缓冲区让下一步更新可以复用这些输入。8192-token 上下文时，三项合计 221.9 MiB。这些状态来自注意力分支；接下来的混合专家（Mixture of Experts，MoE）分支准备多个前馈子网络，由路由器为每个 token 选择其中一部分执行，决定同一主干中实际使用哪些权重，两类机制共同组成完整模型。
 
 ![Qwen3.6 的两类层。配置决定每层采用线性或完整注意力，随后都执行路由专家和共享专家；上方两个分支分别表示线性注意力和完整注意力，模型分别有 30 层与 10 层。](ch02/figure-2-6-hybrid.svg)
 
@@ -439,13 +439,13 @@
 M_{\mathrm{state}}(H)=M_{\mathrm{fixed}}+cH,\qquad H_* = \frac{M_{\mathrm{fixed}}}{c}.
 $$
 
-$H_*$ 表示两部分占用相等的上下文长度。Qwen3.6 的固定部分为 $61.875$ MiB，每位置增加 20 KiB，因此 $H_*=3168$；Kimi K3 的紧凑表示每位置增加 27 KiB，固定部分约为 433.4 MiB，交点约在 16.4K 个上下文位置。低于交点时，缩短上下文只能节省较小部分；远高于交点时，减少每位置宽度对容量更有效。DeepSeek V4-Flash 则按完成的压缩块增加条目，曲线呈阶梯增长。
+$H_*$ 表示两部分占用相等的上下文长度。Qwen3.6 的固定部分为 $61.875$ MiB，每位置增加 20 KiB，因此 $H_*=3168$；Kimi K3 的紧凑表示每位置增加 27 KiB，固定部分约为 433.4 MiB，交点约在 16.4K 个上下文位置。低于交点时，缩短上下文只能节省较小一部分空间；远高于交点时，减小每个位置的表示宽度能更有效地节省容量。DeepSeek V4-Flash 则按完成的压缩块增加条目，曲线呈阶梯增长。
 
 这些状态支持不同的信息访问方式：全局 KV 允许查询直接选择旧位置，递推矩阵通过更新保留上下文关系，压缩索引则在较少条目中寻找相关内容。2.6.3 将结合具体检索任务比较资源需求。[^source-17]
 
 > **练习 2-5〔核心〕：上下文多长时，KV 容量会超过固定状态？**
 >
-> 对 Qwen3.6，取固定状态 61.875 MiB、每个上下文位置 20 KiB；对 Kimi K3 的紧凑表示，递推状态占 414 MiB，卷积状态占 20,348,928 字节，每个上下文位置另占 27 KiB。分别求固定项与增长项相等的上下文长度，以及 32K、128K 上下文的总状态。若只有 256 MiB 状态预算，两个模型各自最多能保存多少个上下文位置？说明容量预算小于固定状态大小时，为什么缩短上下文仍无法容纳模型状态。
+> 对 Qwen3.6，取固定状态 61.875 MiB、每个上下文位置 20 KiB；对 Kimi K3 的紧凑表示，递推状态占 414 MiB，卷积状态占 20,348,928 字节，每个上下文位置另占 27 KiB。分别求随上下文增长的状态与固定状态容量相等时的上下文长度，以及 32K、128K 上下文的状态总容量。若只有 256 MiB 状态预算，两个模型各自最多能保存多少个上下文位置？说明容量预算小于固定状态大小时，为什么缩短上下文仍无法容纳模型状态。
 
 ### 2.3.6 每个 token 存多少，每次 decode 读多少
 
@@ -467,7 +467,7 @@
 
 Kimi K3 的两种 MLA 表示展示了缓存路径对容量的影响：表中的紧凑路径每个位置增加 27,648 B，本书采用的 HF 参考实现将 K/V 展开，每个位置增加 1,474,560 B。[跨模型缓存计算](../calculations/results/kv-comparison-n8192-b1.md)列出了两种路径的精确字节数。
 
-混合注意力还要加固定状态。在本章 FP32 递推、BF16 短卷积的条件下，Qwen3.5 的递推矩阵与卷积槽合计 184.219 MiB，Qwen3.6 为 61.875 MiB，Kimi K3 为 433.406 MiB。这些固定状态与表中的历史缓存共同构成当前容量；一次 decode 还要读写递推矩阵并更新卷积槽。
+混合注意力还要加固定状态。在本章 FP32 递推、BF16 短卷积的条件下，Qwen3.5 的递推矩阵与卷积槽合计 184.219 MiB，Qwen3.6 为 61.875 MiB，Kimi K3 为 433.406 MiB。这些固定状态与表中的上下文缓存共同决定当前的内存占用；一次 decode 还要读写递推矩阵并更新卷积槽。
 
 理解 V4.1 时，可以先把“生成全局表示”和“查询全局表示”分开。它把 40 层文本主干按顺序分成前 20 层**因果编码器**和后 20 层**解码器**，称为因果编码器—解码器（Causal Encoder-Decoder，CED）。两部分属于同一个自回归模型，编码器中的每个位置只访问当前位置及其之前的信息。
 
@@ -497,13 +497,13 @@
 21\frac{584+68}{4}+20\frac{584}{128}=3514.25\ \mathrm{B/token}.
 $$
 
-两者的全局 KV 平均增量相差约 3.95 倍，对应官方图中取整后的 3,514 与 890 B/token。全局历史之外，V4.1 的局部窗口每层最多保存 128 条记录，每条 FP8+scale 记录占 528 B，40 层合计 2.578 MiB。全局历史随上下文增长，局部窗口则在填满后保持固定容量。
+两者的全局 KV 平均增量相差约 3.95 倍，对应官方图中取整后的 3,514 与 890 B/token。除全局上下文缓存外，V4.1 的局部窗口每层最多保存 128 条记录，每条 FP8+scale 记录占 528 B，40 层合计 2.578 MiB。全局历史随上下文增长，局部窗口则在填满后保持固定容量。
 
 沿生成过程逐步观察，还能看出全局缓存的增长节奏。对 V4.1，全局缓存从偶数长度增长到下一个奇数长度只增加 356 B；到下一个偶数长度时，三个 2:1 压缩器各完成一条记录，增加 1,424 B。窗口填满后，后续写入会覆盖旧槽位，容量不再增长，但仍然产生写入：40 层 SWA 每步合计写入 $40\times528=21{,}120$ B，压缩器更新另计。
 
 共享同一份全局缓存的各层仍需读取数据，部分层还会重新索引；8K 场景下，两代 Flash 的注意力 KV（含局部 SWA）与索引逻辑读取量约为 12.56 和 11.38 MiB，跨层共享主要减少了重复保存的空间。
 
-缓存复用与位置选择还可以分别安排。**压缩稀疏注意力第二版（Compressed Sparse Attention 2，CSA2）**把层分成三种模式。**Full** 层完整执行“生成全局 KV、索引、选取和注意力”的流程。**Reindex** 层共享 KV，用自己的索引查询重新选取 Top-K；**Reuse** 层连同所选位置一起复用。V4.1 的 38 个全局注意力层中，有 4 个 Full、4 个 Reindex 和 30 个 Reuse；另外两个层仅使用 SWA。解码器的首个 Full 层扫描全局，并选出最多 $2048\times8=16{,}384$ 个候选位置；技术报告第 2.3.2 节将这个集合称为**候选池（candidate pool）**，其中的候选是可供注意力检索的位置。后续 Reindex 层只在池内各自选取最多 512 个条目。[^v41-candidate]后续层的搜索量因此有了上限，初始全局扫描仍随上下文增长。
+缓存复用与位置选择还可以分别安排。**压缩稀疏注意力第二版（Compressed Sparse Attention 2，CSA2）**把层分成三种模式。**Full** 层完整执行“生成全局 KV、索引、选取和注意力”的流程。**Reindex** 层共享 KV，用自己的索引查询重新选取 Top-K；**Reuse** 层连同所选位置一起复用。V4.1 的 38 个全局注意力层中，有 4 个 Full、4 个 Reindex 和 30 个 Reuse；另外两个层仅使用 SWA。解码器的首个 Full 层扫描全局，并选出最多 $2048\times8=16{,}384$ 个候选位置；技术报告第 2.3.2 节将这个集合称为**候选池（candidate pool）**，其中的候选是可供注意力检索的位置。后续 Reindex 层只在池内各自选取最多 512 个条目。[^v41-candidate] 后续层的搜索量因此有了上限，初始全局扫描仍随上下文增长。
 
 例如，某次查询面对 131,072 个全局位置，将它们按每块 8 个分成 16,384 块。首个解码器 Full 层先为所有位置评分，以块内最高分作为块分数，再选出 2,048 块，形成 16,384 个候选位置。它自己仍从全局分数选取 Top-512；后续 Reindex 层在候选内用新的索引查询重算分数，各自选出 512 个位置。候选池限定后续层的搜索范围，各层的 Top-512 决定本层最终读取哪些条目。
 
@@ -572,25 +572,25 @@
 
 > **练习 2-6〔延伸〕：专家数量与分派方式如何影响计算和权重读取**
 >
-> 对 Qwen3.6 的一层，取 $B=64$、256 个路由专家、每 token 选 8 个。分别考虑均匀分派和集中分派到同一组 8 个专家的情况，写出各专家接收的 token 数 $t_e$，再计算 FLOPs、被访问的专家数量与 BF16 权重读取量。再将专家总数减半、中间维度加倍、每个 token 选中的专家数减半，判断专家总参数量与实际执行的专家矩阵运算量是否变化。最后说明如何将共享专家，以及进入和离开潜空间的投影，计入参数量与运算量。
+> 对 Qwen3.6 的一层，取 $B=64$、256 个路由专家、每 token 选 8 个。分别考虑均匀分派和集中分派到同一组 8 个专家的情况，写出各专家接收的 token 数 $t_e$，再计算 FLOPs、被访问的专家数量与 BF16 权重读取量。再将专家总数减半、中间维度加倍、每个 token 选中的专家数减半，判断专家总参数量与实际执行的专家矩阵运算量是否变化。最后说明如何在上述计算中加入共享专家的参数量与运算量；对于采用潜空间专家的模型，再说明如何计入进入和离开潜空间的投影。
 
 ## 2.5 网络形状与计算组织
 
-上下文状态与专家选择改变了主要数据集合，网络的深度、宽度和连接方式则进一步决定这些工作使用多大的矩阵、按什么顺序执行。参数量相近的模型也可以有不同的计算图，甚至相近的矩阵 FLOPs 也可能伴随不同的缓冲寿命和串行步骤。
+上下文状态与专家选择改变了主要数据集合，网络的深度、宽度和连接方式则进一步决定执行这些工作时使用多大的矩阵、按什么顺序执行。参数量相近的模型也可以有不同的计算图，甚至相近的矩阵 FLOPs 也可能伴随不同的缓冲区占用时间和串行步骤。
 
 ### 2.5.1 深度、宽度与专家粒度
 
-先忽略词表和小向量参数。若 FFN 宽度与隐藏维度保持固定比例，稠密主干的参数量大致正比于 $Ld^2$。把隐藏维度减半、层数增至四倍，可以保持这部分参数量相近，但逐层依赖链也增至原来的四倍长。每个矩阵更小，但必须经过更多层；每层还要保存相应的上下文状态。层数与宽度由此在相近参数预算下改变了计算粒度和依赖深度。
+先忽略词表和小向量参数。若 FFN 宽度与隐藏维度保持固定比例，稠密主干的参数量大致正比于 $Ld^2$。把隐藏维度减半、层数增至四倍，可以保持这部分参数量相近，但逐层依赖链的长度也增至原来的四倍。每个矩阵更小，但必须经过更多层；每层还要保存相应的上下文状态。层数与宽度由此在相近参数预算下改变了计算粒度和依赖深度。
 
 专家结构也可以作类似调整，同时保持参数量和运算量不变。单层路由专家的总参数为 $3Edf$，单 token 专家运算量为 $6k_{\mathrm{top}}df$。将专家数 $E$ 减半、中间维度 $f$ 加倍、选中数 $k_{\mathrm{top}}$ 减半，这两个量都不变。Qwen3-235B-A22B 的 128 个宽度 1536 的专家、每 token 选 8 个，可以与 64 个宽度 3072 的专家、每 token 选 4 个作这一对照。路由输出维度与每专家接收行数仍会改变，从而改变分组矩阵乘法的形状。
 
 在 16-token 算例中，三种专家粒度的专家矩阵运算量保持相同，基线与 64 专家变体均约为 454 GFLOPs。专家数减半、单专家宽度加倍时，相同数量的权重被分成更少的大矩阵；专家数加倍、宽度减半时，则成为更多的小矩阵。路由器要为每个专家计算路由分数，因此其参数也随专家数变化。[专家粒度计算](../calculations/results/qwen235-granularity-baseline.md)将路由与专家矩阵分别累计，说明专家参数总量相同时，矩阵形状为何仍会不同。
 
-形状也决定多卡分工的最小单位。128 个专家可分成八组，每组 16 个；四个 KV 头按完整头分工时只能形成四份，八卡执行就需要复制部分 KV 或调整分组。第五章从矩阵分块分析执行效率，第六章将进一步把这些矩阵和状态分配到设备。
+矩阵和状态的形状也决定多卡分工的最小单位。128 个专家可分成八组，每组 16 个；四个 KV 头按完整头分工时只能形成四份，八卡执行就需要复制部分 KV 或调整分组。第五章从矩阵分块分析执行效率，第六章将进一步把这些矩阵和状态分配到设备。
 
 ### 2.5.2 残差连接与跨层状态
 
-除了层数和宽度，还要看各层怎样连接。残差连接把子层输入直接加到变换结果上，使原始信息和梯度具有跨层直通路径。普通残差计算 $x_{\ell+1}=x_\ell+f_\ell(x_\ell)$，其中 $f_\ell$ 是第 $\ell$ 个子层的变换，因此子层计算期间要保留输入 $x_\ell$，等变换结果产生后才能相加。若后续多个层还会引用同一表示，它的寿命就要延长到最后一次使用。连接方式由此直接决定哪些张量需要同时保存。
+除了层数和宽度，还要看各层怎样连接。残差连接把子层输入直接加到变换结果上，使原始信息和梯度具有跨层直通路径。普通残差计算 $x_{\ell+1}=x_\ell+f_\ell(x_\ell)$，其中 $f_\ell$ 是第 $\ell$ 个子层的变换，因此子层计算期间要保留输入 $x_\ell$，等变换结果产生后才能相加。若后续多个层还会引用同一表示，就必须将它保存到最后一次使用结束。连接方式由此直接决定哪些张量需要同时保存。
 
 普通残差把不同深度的信息不断相加到同一条通路中。为了让层间信息有更多组合方式，同时保持深层网络中的信号传播稳定，DeepSeek V4-Flash 使用 **mHC（Manifold-Constrained Hyper-Connections，流形约束超连接）**。它把一条残差通路扩展成多条，并约束通路之间的混合系数；这里的“流形约束”具体表现为将残差混合矩阵的元素限制为非负、各行与各列的和接近 1。[^architecture-motivation]
 
@@ -600,7 +600,7 @@
 
 Kimi K3 希望当前层能够按需要选取不同深度的信息，而不只是接收逐层相加后的一个混合结果。它使用 **Attention Residuals（AttnRes，注意力残差连接）**，对较浅层的表示计算权重，再加权求和，形成当前层的输入。这种注意力沿网络深度选择信息，前面的序列注意力则在不同 token 位置之间选择信息。为减少保存和读取所有层输出的开销，块式实现把若干层组织成一块，保留供后续块选择的表示。[^architecture-motivation]
 
-后面的层还要使用这些块表示，所以它们需要在本次前向中继续保存；前向结束后即可释放。上下文 KV 则要保留到后续生成步骤。两者的寿命不同：AttnRes 增加一次前向中的临时存储，KV 随请求跨多次调用保留。[^source-19]
+后面的层还要使用这些块表示，所以它们需要在本次前向中继续保存；前向结束后即可释放。上下文 KV 则要保留到后续生成步骤。两者需要保存的时间范围不同：AttnRes 增加一次前向中的临时存储，KV 随请求跨多次调用保留。[^source-19]
 
 ![普通残差与四路 mHC 的连接范围。普通残差保留一条子层输入旁路；mHC 汇合多路状态供子层计算，再混合输出。两种图中的子层输入均为 4096 维。](ch02/figure-2-8-residual.svg)
 
@@ -650,7 +650,7 @@
 
 表中列出四个模型的文本主干。参数规模的后缀 B、T 分别表示十亿和万亿。Kimi K3 将不同的前馈结构用于不同层：首层是中间维度为 $33792$ 的稠密 FFN，后续层则使用中间维度为 $3072$ 的路由专家。稠密层让每个 token 经过同一组宽矩阵，MoE 层则让每个 token 使用多组较窄的专家矩阵。
 
-先观察三个关系。Qwen3.6 的总参数比 Qwen3-8B 多，主干宽度却只有一半：它把大量参数放进可选择的专家，每个 token 只调用其中八个。Qwen3-8B 与 DeepSeek V4-Flash 的主干同为 4096 维，DeepSeek V4-Flash 通过每层 256 个专家形成更大的参数集合。Kimi K3 又将专家计算移到 3584 维潜空间，使主干宽度与专家输入宽度分开。这些差异解释了为什么“保存多大的模型”和“一个 token 做多少工作”需要分别计算。表 2-1、2-2、2-4、2-5 将逐个展开这四个模型，DeepSeek-V3 的表 2-3 用作 MLA 的历史参照。
+先观察三个关系。Qwen3.6 的总参数比 Qwen3-8B 多，主干宽度却只有一半：它把大量参数放进可选择的专家，每个 token 只调用其中八个。Qwen3-8B 与 DeepSeek V4-Flash 的主干同为 4096 维，DeepSeek V4-Flash 通过每层 256 个专家形成更大的参数集合。Kimi K3 又将专家计算移到 3584 维潜空间，使专家输入不必与主干采用相同的宽度。这些差异解释了为什么“保存多大的模型”和“一个 token 做多少工作”需要分别计算。表 2-1、2-2、2-4、2-5 将逐个展开这四个模型，DeepSeek-V3 的表 2-3 用作 MLA 的历史参照。
 
 ![四模型的主干层数，使用同一线性尺度。层数决定同类工作沿网络深度重复多少次。](ch02/figure-2-architecture.svg)
 
@@ -703,7 +703,7 @@
 
 四个模型的逐矩阵展开见本章末的[模型矩阵查阅表](#model-matrix-tables)。表 2-2 至表 2-5 列出每个模块的输入输出尺寸、权重形状、单次运算量与层数。下面先用这些分项的合计解释资源差异。
 
-将矩阵尺寸相乘并按层数累计，DeepSeek V4-Flash 主干约有 2843 亿个逻辑参数，统一 BF16 表示约为 568.7 GB。发布的模型检查点（保存参数的文件集合）则采用混合存储格式，主干数据量约为 156.0 GB。前者回答结构包含多少参数，后者回答这些参数在文件中占多少字节；格式转换把二者连接起来。
+将矩阵尺寸相乘并按层数累计，DeepSeek V4-Flash 主干约有 2843 亿个逻辑参数，统一 BF16 表示约为 568.7 GB。发布的模型检查点（保存参数的文件集合）则采用混合存储格式，主干数据量约为 156.0 GB。前者按统一精度反映参数规模，后者反映这些参数在实际文件中占多少字节；格式转换把二者连接起来。
 
 Kimi K3 文本主干约有 2.78 万亿个参数，绝大多数位于 92 个 MoE 层的路由专家中。每个 token 在每层只选择 16 个专家，因此完整模型的存储容量与单 token 的计算量取决于不同的因素：前者由全部专家决定，后者由选中的专家及共同执行的注意力、共享专家和投影决定。[^source-7]
 
@@ -720,7 +720,7 @@
 | DeepSeek V4-Flash | 1.059 | 5.085 | 1.082 | 277.025 | 0.081 | 284.332 |
 | Kimi K3（紧凑 MLA） | 2.349 | 36.180 | 12.882 | 2722.741 | 5.333 | 2779.484 |
 
-其他包含 router、潜空间进出投影、归一化、卷积和连接等参数；注意力投影包括 DeepSeek V4-Flash 的压缩与索引投影。Qwen3-8B 的主要参数集中在每个 token 都执行的稠密 FFN，三个 MoE 模型则将大部分参数放进路由专家。Qwen3.6 的路由专家占约 93%，DeepSeek V4-Flash 与 Kimi K3 约为 97%—98%。因此，它们的总容量主要由专家集合决定，单 token 的专家运算量则取决于每层选中的专家。
+其他包含 router、潜空间进出投影、归一化、卷积和连接等参数；注意力投影包括 DeepSeek V4-Flash 的压缩与索引投影。Qwen3-8B 的主要参数集中在处理每个 token 时都要执行的稠密 FFN，三个 MoE 模型则将大部分参数放进路由专家。Qwen3.6 的路由专家占约 93%，DeepSeek V4-Flash 与 Kimi K3 约为 97%—98%。因此，它们的总容量主要由专家集合决定，单 token 的专家运算量则取决于每层选中的专家。
 
 **表 2-C　相同输入条件下的计算与状态**
 
@@ -739,7 +739,7 @@
 
 **量化编码与元数据共同决定权重容量。** 统一按 BF16 计算时，每个参数占 2 字节，权重大小可以直接反映参数量的差异。实际发布的权重文件（checkpoint，即模型参数的保存快照）使用各自的存储格式，DeepSeek V4-Flash 主干约为 156.02 GB，Kimi K3 文本约为 1559.97 GB。文件大小由低位宽矩阵、采用较高精度的参数以及格式元数据共同决定。模型加载后，还需要为格式转换和计算分配空间。第五章将分析存储格式与运行时实现如何影响显存占用。
 
-表 2-B 的参数组成解释了权重容量，表 2-C 则展示这些参数怎样参与一次调用。二者结合起来，可以区分“模型能容纳多少参数”与“处理一个 token 要使用多少资源”。[^comparison-data]
+表 2-B 的参数组成解释了权重容量，表 2-C 则展示这些参数怎样参与一次调用。二者结合起来，可以区分“保存整个模型需要多少空间”与“处理一个 token 要使用多少资源”。[^comparison-data]
 
 <!-- MODEL-COMPARISON:END -->
 
@@ -788,9 +788,9 @@
 
 上下文变为 16K 时，每条请求占 2.25 GiB，同一空间只能放两条；变为 4K 时，则能放九条。整数跳变来自剩余空间必须足以容纳一条请求的完整状态，而不是只够存放其中一部分。
 
-保留多少历史由此成为一个同时影响应用和系统的决定。应用多保留一段材料，模型就能继续参考它；服务需要为每条请求分配更多状态空间。前面的 GQA、MLA 和递推表示进一步改变了保存这些信息的方式。模型结构、上下文组织与并发容量可以放在同一份预算里比较，任务检查则确定哪些方案能够提供所需的回答质量。
-
-再看 DeepSeek-R1-Distill-Llama-70B，其参数量约为 705.5 亿。其 BF16 权重为 141.107 GB，每条 8192-token BF16 KV 为 2.5 GiB。在本例采用的分组量化方案中，让嵌入、输出头和归一化等部分继续使用较高精度，并计入 scale 与打包尾部后，8-bit 方案约为 73.726 GB，4-bit 方案约为 39.500 GB。
+保留多少历史由此成为一个同时影响应用和系统的决定。应用多保留一段材料，模型就能继续参考它；服务需要为每条请求分配更多状态空间。前面的 GQA、MLA 和递推表示进一步改变了保存这些信息的方式。模型结构、上下文组织与并发容量可以放在同一份预算里比较，再通过实际任务测试，确定哪些方案能够达到所需的回答质量。
+
+再看 DeepSeek-R1-Distill-Llama-70B，其参数量约为 705.5 亿。其 BF16 权重为 141.107 GB，每条 8192-token 请求的 BF16 KV 为 2.5 GiB。在本例采用的分组量化方案中，让嵌入、输出头和归一化等部分继续使用较高精度，并计入缩放因子（scale）及打包时尾部填充的空间后，8-bit 方案约为 73.726 GB，4-bit 方案约为 39.500 GB。
 
 4-bit 方案的 39.500 GB 包括低位宽矩阵、仍采用高精度的参数，以及 scale 与打包开销，比将全部参数一律按半字节计算更大。显存容量为 80 GB（十进制）、固定预留 2 GiB 时，8-bit 方案可放 1 条 8K 请求，4-bit 方案可放 14 条。上下文从 8K 增至 32K 时，每请求 KV 增至四倍，4-bit 方案可同时容纳的请求数降到 3 条。压缩权重腾出的空间可以保存更多请求的上下文，但每条上下文越长，能同时容纳的请求就越少。[^source-23]
 
@@ -816,9 +816,9 @@
 N_{\max}=\frac{C-M_0-BK}{b_w}.
 $$
 
-先确定服务所需的状态大小，计算留给权重的空间。取 24 GB 容量、2 GiB 工作区、4 条各 8192 位置的请求，每请求 KV 沿用 Qwen3-8B 的 1.125 GiB。BF16 每参数 2 字节，剩余空间最多容纳约 85.1 亿参数。[^core-calculation]图中先扣除服务状态，再划出权重预算。
-
-改变模型结构时，权重预算与状态预算会一起变化。增加层数或 KV 头数会增大每请求的 $K$，留给权重的空间随之减少；跨设备部署则扩大总容量，同时增加通信。Llama 3 报告中的 405B BF16 推理使用两台、共 16 张 H100，其所需容量超过一台服务器所能提供的显存。[^codesign]模型规模、状态结构与设备数量因此需要共同选择。
+先确定服务所需的状态大小，计算留给权重的空间。取 24 GB 容量、2 GiB 工作区、4 条各 8192 位置的请求，每请求 KV 沿用 Qwen3-8B 的 1.125 GiB。BF16 每参数 2 字节，剩余空间最多容纳约 85.1 亿参数。[^core-calculation] 图中先扣除服务状态，再划出权重预算。
+
+改变模型结构时，权重预算与状态预算会一起变化。增加层数或 KV 头数会增大每请求的 $K$，留给权重的空间随之减少；跨设备部署则扩大总容量，同时增加通信。Llama 3 报告中的 405B BF16 推理使用两台、共 16 张 H100，其所需容量超过一台服务器所能提供的显存。[^codesign] 模型规模、状态结构与设备数量因此需要共同选择。
 
 ![图 2-34 从设备反推权重预算。24 GB 中先为 4 条 8K 请求的 KV 和 2 GiB 工作区预留空间，余量给出 BF16 参数上界；图中容量均为十进制 GB。](ch02/figure-2-reverse-budget.svg)
 
@@ -843,7 +843,7 @@
 
 从一次调用扩展到完整请求之后，输入和输出的作用更加清楚。增加 $P$，主要增加一次输入处理的工作量，以及后续解码开始时的上下文长度；增加 $G$，则反复执行投影、专家与输出头，并多次读取上下文。长输入、短输出与短输入、长输出由此对各类资源提出不同的需求，第三章将分析这两类请求持续到达时的系统负载。
 
-> **练习 2-8〔延伸〕：前缀复用与输入改写如何改变模型调用量**
+> **练习 2-8〔延伸〕：前缀复用与输入改写如何改变模型计算量**
 >
 > 一轮输入总长 1443 个 token，其中前 1392 个命中缓存，返回 128 个 token。求 $S,P,G,n_d$ 的值、请求结束时的上下文长度，以及本轮新增 KV 的大小。比较完全重算与复用缓存时，投影行数、有效因果位置对数分别减少多少。若在第 501 个输入位置修改了内容，重新求最长可复用前缀与新输入长度。
 
@@ -855,13 +855,13 @@
 
 *图 2-35　生成四个输出的调用顺序。prefill 处理 128 个输入并产生首输出，随后三次 decode 各将前一输出送回模型；最终保留 131 个位置。*
 
-![同一个输入 128 个 token、输出四个 token 的请求的矩阵运算量。模型执行范围与缓存路径见本节题设；这是逐调用累计的计算量。](ch02/figure-2-request-compute.svg)
-
-*图 2-36　同一个输入 128 个 token、输出四个 token 的请求的矩阵运算量。模型执行范围与缓存路径见本节题设；这是逐调用累计的计算量。*
+![输入 128 个 token、输出四个 token 时，各模型处理同一请求所需的矩阵运算量。模型执行范围与缓存路径见本节题设；这是逐调用累计的计算量。](ch02/figure-2-request-compute.svg)
+
+*图 2-36　输入 128 个 token、输出四个 token 时，各模型处理同一请求所需的矩阵运算量。模型执行范围与缓存路径见本节题设；这是逐调用累计的计算量。*
 
 > **练习 2-9〔延伸〕：根据容量、计算需求与任务质量选择模型**
 >
-> 使用四模型资源表，为每个模型分别列出完整权重、8K 状态、单步 decode 与 8K prefill 的需求。设单卡显存为 80 GB，预留 2 GiB 工作区，并采用 BF16 权重。先判断哪些模型能完整放入单卡；再把问题改为多卡部署，说明还需从后续章节获得哪些时间与通信参数。将短请求的 $G$ 改为 1，检验调用次数。最后为一个短语检索任务写出质量标准，说明同 token 数比较与同文本比较分别回答什么问题。
+> 使用四模型资源表，为每个模型分别列出完整权重、8K 状态、单步 decode 与 8K prefill 的需求。设单卡显存为 80 GB，预留 2 GiB 工作区，并采用 BF16 权重。先判断哪些模型能完整放入单卡；再把问题改为多卡部署，说明还需从后续章节获得哪些时间与通信参数。将输出 token 数 $G$ 改为 1，重新计算 prefill 与 decode 的调用次数。最后为一个短语检索任务写出质量标准，说明同 token 数比较与同文本比较分别回答什么问题。
 
 本章从计算图出发，逐步算出了完整请求的资源需求。注意力决定如何保存和访问上下文，MoE 决定每次选择哪些权重，网络深度与连接方式决定计算顺序和临时存储。下一章将加入请求长度分布、到达时间、工具调用、训练与 RL，分析这些计算如何随时间累积成系统负载。
 
@@ -1040,7 +1040,7 @@
 
 累计时，用各行公式乘最后一列的层数；路由专家再对实际接收行数 $t_e$ 求和。末端归一化执行一次，层内归一化、激活、位置处理和状态更新沿相应分支执行。
 
-这张表解释了为什么“每步只选少量上下文”不等于总工作固定：主注意力使用 $A$，索引扫描使用 $I$，压缩投影则对新输入持续执行。这三类工作量的增长规律不同。[^source-21]
+这张表解释了为什么“每步只选少量上下文”不等于总工作量固定：主注意力使用 $A$，索引扫描使用 $I$，压缩投影则对新输入持续执行。这三类工作量的增长规律不同。[^source-21]
 
 **表 2-5　Kimi K3：KDA、紧凑 MLA 与潜空间专家**
 
