--- before/manuscripts/00-前言.md
+++ after/manuscripts/00-前言.md
@@ -4,9 +4,11 @@
 
 大多数软件工程师不需要亲自开发操作系统、编译器和芯片，但仍然需要学习操作系统、编译原理和计算机体系结构。这些知识帮助我们理解程序所依赖的抽象，以及抽象背后的实现。申请一块内存、读取一个文件、调用一个函数，看起来只是简单的操作，却各有资源与时间代价。理解这些代价，才能解释程序为什么慢，以及怎样改进。基于模型开发应用，同样需要这样的基础。选择多大的模型、保留多长的上下文、允许多少条请求同时执行、把任务放在本地还是云端，都会改变系统需要完成的工作。
 
-这本书也延续了我在计算机系统与网络研究中的一些思考。2019 年加入华为后，我参与了 MindSpore 的自动算子生成项目 AKG，担任动态形状团队的架构师和技术负责人。AKG 的工作是把模型中的计算转换为适合昇腾神经网络处理器执行的程序。数学表达式确定了要算什么，但数据怎样分块、放在哪一级存储、计算与搬运怎样配合，仍然需要编译器作出安排。[^preface-akg]
+## 从一个算子到万卡互联
 
-刚加入团队时，我负责过 softmax 算子，它将一组数值转换为归一化的权重。为了把它与前面的计算融合，我到处寻找合适的算法，后来找到 NVIDIA 在 2018 年发表的一项在线 softmax 工作，才有了继续实现融合的办法。[^preface-softmax] 一个公式可以很短，落实到芯片上，却需要认真安排中间结果的保存与读取。如果每个算子都把结果写回片外内存，再由下一个算子读进来，就会产生额外的数据搬移；要让数据尽量留在片内继续处理，又必须同时考虑算法、执行顺序和有限的存储空间。
+回头看，从模型应用走向基础设施，也是在重新梳理我自己走过的路。2019 年加入华为后，我参与了 MindSpore 的自动算子生成项目 AKG，担任动态形状团队的架构师和技术负责人。AKG 的工作是把模型中的计算转换为适合昇腾神经网络处理器执行的程序。数学表达式确定了要算什么，但数据怎样分块、放在哪一级存储、计算与搬运怎样配合，仍然需要编译器作出安排。[^preface-akg]
+
+我还记得刚加入团队时，自己负责过 softmax 算子，它将一组数值转换为归一化的权重。为了把它与前面的计算融合，我到处寻找合适的算法，后来找到 NVIDIA 在 2018 年发表的一项在线 softmax 工作，才有了继续实现融合的办法。[^preface-softmax] 当时我甚至还不了解注意力机制，却已经在为其中一个算子的执行方式寻找办法。这件小事后来一直提醒我：一个公式可以很短，落实到芯片上，却需要认真安排中间结果的保存与读取。如果每个算子都把结果写回片外内存，再由下一个算子读进来，就会产生额外的数据搬移；要让数据尽量留在片内继续处理，又必须同时考虑算法、执行顺序和有限的存储空间。
 
 AKG 让我从算子和编译器这一侧接触到模型执行的具体代价。到了 2020 年，我加入 Unified Bus（UB）项目，关注的问题从芯片内部延伸到了设备之间：怎样让成千上万个处理器高效地共同完成一项计算？
 
@@ -14,13 +16,17 @@
 
 我们作出这个判断，有另一组依据。2019 年，谭焜博士把 AI 算力需求的发展趋势画成散点图，看到需求的增长远快于单芯片能力的提升，于是推动了面向上万张卡的高性能互联研究。2020 年我加入项目，GPT-3 论文的发表又为这个方向提供了新的论据。[^preface-taste] 如果模型训练需要的计算量继续这样增长，就必须让更多设备协同工作；互联架构的研究和实现需要时间，等需求完全显现以后再开始，很可能已经来不及。
 
-这段争论让我记住，系统设计需要同时看清眼前的负载和正在变化的条件。八张卡能够完成当时熟悉的任务，是一种实际经验；更多计算会推动更大规模协作，是对未来需求的判断。要说服别人，就需要把这个判断所依赖的趋势、资源和代价讲清楚。后来的需求变化让大规模互联的重要性更加明确，也让我们面对更具体的问题：设备多了以后，怎样分配状态，怎样减少交接，怎样避免大量计算资源一起等待？
+今天回头看，万卡训练已经很容易被理解。但我想把当年的质疑写进这本书，因为站在当时，答案远没有今天看起来这样清楚。系统设计需要同时看清眼前的负载和正在变化的条件。八张卡能够完成当时熟悉的任务，是一种实际经验；更多计算会推动更大规模协作，是对未来需求的判断。要说服别人，就需要把这个判断所依赖的趋势、资源和代价讲清楚。后来的需求变化让大规模互联的重要性更加明确，也让我们面对更具体的问题：设备多了以后，怎样分配状态，怎样减少交接，怎样避免大量计算资源一起等待？
 
 UB 希望把数据中心作为一台计算机来设计，让处理器、内存和存储能够更直接地协作，也让程序更方便地使用分布在不同位置的资源。[^preface-ub] 真正设计时，需要逐项决定应用与硬件之间的分工：应用需要传递一条消息，还是访问一块数据？数据传完以后，谁负责通知使用它的程序？哪些操作必须保持先后顺序，哪些可以独立推进？每一种划分都能简化某些工作，也会把责任和代价转移到别处。
 
 2022 年 12 月，我在北京大学的一次课程报告中讨论这些变化，后来将报告整理为《计算机网络的新黄金时代》系列文章。[^preface-network] 数据中心里的设备越来越多地共同完成一项大规模计算，通信直接参与计算过程，下一步能否开始取决于其他设备的数据何时到达。应用需求与硬件能力一起变化，网络接口、软件组织和设备分工也需要重新考虑。
 
 从 AKG 到 UB，工作跨越了不同的尺度，我反复遇到的却是相通的问题：计算需要的数据在哪里，现有接口增加了哪些工作，掌握更多应用信息以后，能否换一种执行方式？后来写《Unified Bus 背后的思考》时，我尝试把早期设计中的这些问题和选择记录下来。今天产品化的 UB 已经经历了后续团队的大量工作，具体实现也可能与早期方案不同。本书会结合亲历、公开规范与具体计算，解释设计的依据和适用条件。
+
+## 我希望这本书讲清楚什么
+
+写这本书时，我一直在想，怎样把这些经历中值得反复使用的思考方法讲给读者。具体方案背后，究竟有哪些问题是我们可以一起算清楚的？
 
 模型应用的发展，使这些问题有了新的研究对象。开发者既用代码规定流程，也通过选择模型、组织上下文和提供工具来表达任务。同一个模型可以完成多种应用任务，底层则围绕共同的模型计算组织执行。不同应用由此可以共用模型的执行机制，为系统设计提供了新的优化机会。
 
@@ -34,13 +40,54 @@
 
 估算的作用，是帮助我们选择下一步最值得做的工作。公式使用了哪些假设，哪些操作尚未计入，哪些阶段实际上无法重叠，都需要在实验中检查。测量与预测不一致时，这个差异往往正好指出了尚未理解的地方。书中的练习会邀请读者先作预测，再用小程序、实验记录或设备测量检验，最后改变一个条件，看看原先的选择是否仍然合适。
 
-全书分三个阶段展开。首先理解模型与负载，弄清需要完成什么工作；随后进入芯片与系统，研究这些工作如何在设备上执行、如何在设备之间协作；最后组织推理与训练系统，并把模型服务放回完整的任务环境与部署条件中。第一章给出了十二章的结构图、逐章导读和阅读所需的基础。随着阅读推进，同一个问题会不断加入新的约束：最初只考虑权重容量，后来还要计算上下文状态；最初比较单次执行，后来还要面对并发、交接和故障恢复。每补上一项重要条件，我们就有机会修正此前的判断。
+## 全书结构
 
-我希望这本书能帮助应用开发者理解自己的选择会带来什么资源需求，也帮助系统、网络和芯片工程师理解，上层任务为什么需要这些能力。读者可以从熟悉的问题入手，再沿着计算与数据的关系走向相邻层次。书中采用具体的模型、设备和实现作为例子，是为了让推导得到可以核查的具体数字。配套的计算脚本、实验记录和扩展资料，则方便读者更换输入，继续追问。
+十二章按照“理解工作需求—认识执行资源—组织完整系统”的顺序展开，如图 0-1 所示。第一部分是模型与负载（第 1—3 章），建立分析方法，说明计算量、数据量和任务依赖从哪里来。第二部分是芯片与系统（第 4—7 章），从单设备执行走向多设备协作，说明资源怎样承担这些工作。第三部分是推理与训练系统（第 8—12 章），研究如何组织请求、模型状态和训练过程，并将这些服务延伸到任务运行环境与端边云部署。
 
-我希望读者养成提出问题、定量估算并用证据检验判断的习惯。遇到新的系统时，先问它要完成什么，再追踪计算、数据和等待，最后解释为什么选择这一种实现。
+![十二章的阅读顺序分为模型与负载、芯片与系统、推理与训练系统三个阶段。](ch01/figure-1-book-roadmap.svg)
 
-当你能够面对一个新模型、一款新设备或一种新的部署要求，画出执行过程，算出几个关键数字，指出最需要验证的假设，并根据观察修改自己的选择时，这本书就达到了它的目的。你的结论可以与书中不同；能够说明差异来自哪里，本身就是理解系统的表现。
+*图 0-1　全书结构与阅读顺序。前一部分为后一部分提供分析依据；进入服务与部署之后，还要根据任务质量、完成时间和成本，重新检查模型与资源选择。*
+
+下面是每章要回答的主要问题。你可以先在这里找到自己关心的问题，再看看前面的章节为它提供了哪些基础。
+
+| 章节 | 主要问题 |
+| --- | --- |
+| 第 1 章 初识 AI Infra | 怎样认识系统全景，并用几个关键数字估算一次模型执行？ |
+| 第 2 章 模型架构 | 模型的计算、参数和上下文状态怎样形成资源需求？ |
+| 第 3 章 推理与训练负载 | 请求到达、多轮调用、多模态和训练过程怎样改变需求与等待？ |
+| 第 4 章 加速器架构 | 芯片的计算、存储和数据通路怎样配合，适合什么负载？ |
+| 第 5 章 算子与运行时 | 怎样组织算子与执行过程，减少重复读写、提交和等待？ |
+| 第 6 章 超节点 | 模型怎样在多台设备间分工，协作规模应当多大？ |
+| 第 7 章 数据中心网络 | 数据怎样跨设备传输，交接规则、拥塞和故障怎样影响计算？ |
+| 第 8 章 推理优化 | 怎样安排批处理、请求和缓存，提高给定设备组合的服务效率？ |
+| 第 9 章 分布式推理 | 计算与状态应当放在哪里，怎样组织分工、共享和扩缩容？ |
+| 第 10 章 训练系统 | 怎样安排训练状态、通信与恢复，在期限内取得有效训练进展？ |
+| 第 11 章 资源调度与运行环境 | 怎样把模型服务、工具环境和共享资源组织成完整的任务系统？ |
+| 第 12 章 端边云协同 | 怎样结合实际传输与交互要求，选择任务在端、边、云的部署位置？ |
+
+随着阅读推进，同一个问题会不断加入新的条件：最初只考虑权重容量，后来还要计算上下文状态；最初比较单次执行，后来还要面对并发、交接和故障恢复。我希望你能跟着这些变化，回头检查自己在前面作出的判断。这也是我把模型、芯片、网络和服务系统放在一本书里讲的原因：它们共同决定了一项任务怎样完成。
+
+## 如何阅读本书
+
+如果你希望从头建立系统认识，我建议先读第 1—3 章，学会估算一次执行，弄清模型与负载提出了哪些需求，再顺着后面两部分逐步展开。不同背景的读者，也可以在这个共同基础上选择自己的重点。
+
+- **模型与应用开发者**可以重点阅读第 8、9 章的推理服务，以及第 11、12 章的任务环境和部署。遇到容量、算子或通信问题时，再回到第 4—7 章追踪原因。
+- **系统与网络工程师**可以重点阅读第 5—7 章的执行与设备协作，再通过第 9、10 章检查这些机制如何影响分布式推理和训练。
+- **芯片与体系结构工程师**可以重点阅读第 4—7 章，并结合后续章节的任务算例，检查自己熟悉的硬件指标怎样转化为实际的服务能力。
+
+我尤其建议你在看答案之前，先自己估一下。拿一张纸，写下数据量、处理能力和必须等待的步骤，通常就能发现一些值得追问的地方。如果结果与书中不同，先检查双方是否使用了相同的条件；如果条件相同，就继续寻找漏算或重复计算的工作。这种来回检查，往往比直接记住一个结论更有收获。
+
+书中的练习分为核心与延伸，核心练习帮助你完成本章的主要推导，延伸练习可按兴趣和项目需要选择。配套计算脚本、实验记录和扩展资料保存在本书仓库中；需要实际运行的项目会说明环境和设备条件。没有相应设备时，也可以先分析已有记录，再改变输入重新计算。
+
+读这本书时，你也可以带着自己的模型、机器和业务问题。把书里的输入替换掉，看看结论是否改变，再决定下一步值得测什么。我希望这些例子能够成为你分析自己问题的起点。
+
+## 前置知识
+
+本书面向具有编程经验、希望理解模型执行与系统设计的读者。数学上，需要理解向量、矩阵乘法和基本代数，能够进行单位换算；理解平均值、概率和导数的基本含义，有助于阅读负载与训练章节。系统方面，需要了解进程、内存、文件与网络通信的基本用途。模型结构、并行方式和专用硬件机制会随问题逐步介绍。动手部分主要使用 Python，基础练习可以从手算、小脚本和已有实验记录开始；使用特定设备的实验会另行说明条件。
+
+不必等到熟悉所有层次才开始阅读。我做 AKG 时，也是从一个具体算子开始，逐渐理解编译器和硬件怎样配合；做 UB 时，又需要重新学习更大规模的设备协作。遇到陌生的概念，可以先抓住它要解决的问题，再回来看实现细节。
+
+我希望你读完后，面对一个新模型、一款新设备或一种新的部署要求，能够画出执行过程，算出几个关键数字，指出最需要验证的假设，并根据观察修改自己的选择。你的结论可以与书中不同；能够说明差异来自哪里，本身就是理解系统的表现。
 
 [^preface-network]: 李博杰，《计算机网络的新黄金时代（一）：数据中心网络》，根据 2022 年 12 月 12 日在北京大学的课程报告整理。[原文存档](../references/files/documents/network-golden-1.md)。
 [^preface-ub]: 李博杰，《Unified Bus 背后的思考》，2025 年 9 月 28 日。[原文存档](../references/files/documents/ub-reflection.md)。
--- before/manuscripts/01-初识 AI Infra.md
+++ after/manuscripts/01-初识 AI Infra.md
@@ -1,35 +1,8 @@
 # 第 1 章 初识 AI Infra
 
-前言介绍了本书的写作缘起。从本章开始，我们沿着模型的执行过程，认识支撑它的系统。**AI Infra** 是支撑 AI 训练和推理的基础设施，包括计算设备、存储与互联，以及组织这些资源的软件。训练通过样本调整模型参数；推理使用训练得到的参数处理输入、产生输出。
-
-本章先说明应用怎样通过代码、模型与上下文共同表达任务，再跟随一次请求认识系统，用容量、算力和带宽回答具体的设计问题：数据能否放下，回答要等多久，增加设备或改变数据表示能带来多少收益。最后通过真实架构的形成过程，理解需求如何推动系统设计。
-
-**从模型负载到执行资源，再到完整系统。** 十二章按照“理解工作需求—认识执行资源—组织完整系统”的顺序展开，如图 1-1 所示。第一部分是模型与负载（第 1—3 章），建立分析方法，说明计算量、数据量和任务依赖从哪里来。第二部分是芯片与系统（第 4—7 章），从单设备执行走向多设备协作，说明资源怎样承担这些工作。第三部分是推理与训练系统（第 8—12 章），研究如何组织请求、模型状态和训练过程，并将这些服务延伸到任务运行环境与端边云部署。
-
-![十二章的阅读顺序分为模型与负载、芯片与系统、推理与训练系统三个阶段。](ch01/figure-1-book-roadmap.svg)
-
-*图 1-1　全书结构与阅读顺序。前一部分为后一部分提供分析依据；进入服务与部署之后，还要根据任务质量、完成时间和成本，重新检查模型与资源选择。*
-
-**各章要解决的系统设计问题。** 下表列出每章要回答的主要问题，便于读者理解具体技术与全书各部分的联系。
-
-| 章节 | 主要问题 |
-| --- | --- |
-| 第 1 章 初识 AI Infra | 怎样认识系统全景，并用几个关键数字估算一次模型执行？ |
-| 第 2 章 模型架构 | 模型的计算、参数和上下文状态怎样形成资源需求？ |
-| 第 3 章 推理与训练负载 | 请求到达、多轮调用、多模态和训练过程怎样改变需求与等待？ |
-| 第 4 章 加速器架构 | 芯片的计算、存储和数据通路怎样配合，适合什么负载？ |
-| 第 5 章 算子与运行时 | 怎样组织算子与执行过程，减少重复读写、提交和等待？ |
-| 第 6 章 超节点 | 模型怎样在多台设备间分工，协作规模应当多大？ |
-| 第 7 章 数据中心网络 | 数据怎样跨设备传输，交接规则、拥塞和故障怎样影响计算？ |
-| 第 8 章 推理优化 | 怎样安排批处理、请求和缓存，提高给定设备组合的服务效率？ |
-| 第 9 章 分布式推理 | 计算与状态应当放在哪里，怎样组织分工、共享和扩缩容？ |
-| 第 10 章 训练系统 | 怎样安排训练状态、通信与恢复，在期限内取得有效训练进展？ |
-| 第 11 章 资源调度与运行环境 | 怎样把模型服务、工具环境和共享资源组织成完整的任务系统？ |
-| 第 12 章 端边云协同 | 怎样结合实际传输与交互要求，选择任务在端、边、云的部署位置？ |
-
-**阅读所需的基础。** 本书面向具有编程经验、希望理解模型执行与系统设计的读者。数学上，需要理解向量、矩阵乘法和基本代数，能够进行单位换算；理解平均值、概率和导数的基本含义，有助于阅读负载与训练章节。系统方面，需要了解进程、内存、文件与网络通信的基本用途。模型结构、并行方式和专用硬件机制会随问题逐步介绍。动手部分主要使用 Python，基础练习可以从手算、小脚本和已有实验记录开始；使用特定设备的实验会另行说明条件。
-
-首次阅读建议先完成前面三章，建立共同的模型与负载基础，再按顺序进入后续两部分。带着实际项目阅读时，可以先查看相关章节的问题和小结，遇到不熟悉的计算、状态或通信机制，再回到前面的推导。下面从应用如何表达任务开始。
+前言介绍了本书的写作缘起、章节安排和阅读方法。从本章开始，我们沿着模型的执行过程，认识支撑它的系统。**AI Infra** 是支撑 AI 训练和推理的基础设施，包括计算设备、存储与互联，以及组织这些资源的软件。训练通过样本调整模型参数；推理使用训练得到的参数处理输入、产生输出。
+
+本章先说明应用怎样通过代码、模型与上下文共同表达任务，再沿着一次请求的处理过程认识系统，用容量、算力和带宽回答具体的设计问题：数据能否放下，回答要等多久，增加设备或改变数据表示能带来多少收益。最后通过真实架构的形成过程，理解需求如何推动系统设计。
 
 ## 1.1 AI Infra 为什么重要
 
@@ -39,17 +12,17 @@
 
 如果要让这个服务检查代码，可以把代码、测试要求和可以调用的工具一同提供给模型，由模型提出修改方案或发起工具调用；应用程序执行测试，把结果加入下一次调用的上下文，再由模型继续判断。工具是供应用调用的程序功能，例如读取文件或运行测试。模型负责选择下一步做什么，宿主程序负责检查权限、执行操作和保存结果。完成一次任务可能需要多次模型调用和工具执行。
 
-**抽象**是把复杂实现封装在明确的接口之后，让开发者用较少的概念组织程序。**可编程性**是开发者通过某种接口改变系统行为的能力。传统应用主要用程序代码表达行为，编译器将代码转换为可执行指令，操作系统管理程序运行所需的资源。模型驱动的应用增加了另一个重要入口：通过选择模型、组织上下文和提供工具来表达任务。一部分原来需要逐条编写的行为规则，现在由模型根据上下文决定；代码继续组织调用流程，工具程序继续通过操作系统访问文件、网络和设备。[^abstraction]
-
-图 1-2 对照了这两种组织方式。右侧的 Agent 负责组织上下文、调用模型，并执行模型选择的工具。右侧的**模型接口**是应用向模型提交输入、取得输出的调用方式。接口背后的运行系统计算模型输出，并保存后续步骤需要复用的上下文状态；加速器是适合并行执行大量模型运算的处理器，内存保存参数与运行状态，互联在设备之间传递数据。开发者从上层改变任务，底层要把这些变化落实为具体运算与数据访问。
+**抽象**是把复杂实现封装在明确的接口之后，让开发者用较少的概念组织程序。**可编程性**是开发者通过某种接口改变系统行为的能力。传统应用主要用程序代码表达行为，编译器将代码转换为可执行指令，操作系统管理程序运行所需的资源。模型驱动的应用增加了另一个重要入口：通过选择模型、组织上下文和提供工具来表达任务。一部分原来需要逐条编写规则来规定的行为，现在由模型根据上下文决定；代码继续组织调用流程，工具程序继续通过操作系统访问文件、网络和设备。[^abstraction]
+
+图 1-1 对照了这两种组织方式。右侧的 Agent 负责组织上下文、调用模型，并执行模型选择的工具。其中，**模型接口**规定应用如何提交输入、取得输出。接口背后的运行系统计算模型输出，并保存后续步骤需要复用的上下文状态；加速器是适合并行执行大量模型运算的处理器，内存保存参数与运行状态，互联在设备之间传递数据。开发者从上层改变任务，底层要把这些变化落实为具体运算与数据访问。
 
 ![应用行为的表达方式与执行路径。左侧通过代码组织程序，右侧通过上下文调用模型；实线表示向下的执行依赖，虚线表示工具执行仍需操作系统。](ch01/figure-1-programmability.svg)
 
-*图 1-2　应用行为的表达方式与执行路径。左侧通过代码组织程序，右侧通过上下文调用模型；实线表示向下的执行依赖，虚线表示工具执行仍需传统操作系统。加速器、内存与互联分别承担模型计算、数据保存和设备间传输的职责。*
-
-在 AI 系统中，同一个模型服务于不同应用时，会使用同一组参数完成各请求的计算，系统可以把多条请求放到一起，共同使用读入的参数，提高设备利用率；这称为批处理，每批共同执行的请求数称为批次大小（batch size）。与此同时，长文档、长回答和多轮工具调用会改变计算量、状态占用和等待时间。同一个模型、同一套设备，处理不同任务时，性能可能相差很大。
-
-从模型设计来看，压缩保存的状态、改变注意力机制或只使用部分模型参数，可以改变硬件需要完成的工作；从硬件条件来看，存储容量、数据传输速度和设备连接方式，又会影响哪些模型结构和执行方法更合适。理解这种双向关系，才能同时讨论回答质量、响应时间和成本，并解释为什么某个应用的最佳方案在另一个应用中需要重新选择。
+*图 1-1　应用行为的表达方式与执行路径。左侧通过代码组织程序，右侧通过上下文调用模型；实线表示向下的执行依赖，虚线表示工具执行仍需传统操作系统。加速器、内存与互联分别承担模型计算、数据保存和设备间传输的职责。*
+
+在 AI 系统中，同一个模型为不同应用处理请求时，使用的是同一组参数。系统可以将多条请求合并计算，让它们共用一次读入的参数，提高设备利用率。这种方式称为批处理，每批共同执行的请求数称为批次大小（batch size）。与此同时，长文档、长回答和多轮工具调用会改变计算量、状态占用和等待时间。同一个模型、同一套设备，处理不同任务时，性能可能相差很大。
+
+从模型设计来看，压缩保存的状态、改变注意力机制或只使用部分模型参数，可以改变硬件需要完成的工作；从硬件条件来看，存储容量、数据传输速度和设备连接方式，又会影响哪些模型结构和执行方法更合适。理解这种双向关系，才能同时讨论回答质量、响应时间和成本，并解释为什么适合一个应用的方案，用于另一个应用时还需重新评估。
 
 **编程抽象从操作系统到模型上下文的迁移是计算机系统领域数十年来最重要的变化之一。** 计算机系统领域的两大顶级会议 SOSP（操作系统原理研讨会）、OSDI（操作系统设计与实现）中都包括“OS”（操作系统）。传统操作系统需要支撑多种多样的应用，编译器和硬件也要为事先未知的程序提供通用能力。各层通过接口分工，在简化编程、隔离执行和提高性能之间作出设计取舍。例如，独立算子通过内存交换结果，便于组合成不同程序，也增加了中间数据的读写；主机统一提交设备任务，便于控制执行，也增加了反复交接的时间。
 
@@ -57,11 +30,11 @@
 
 ### 1.1.2 从任务到设备的六层分工
 
-先看 AI 系统怎样处理一次请求。应用提出回答质量和响应时间要求，模型按照自身结构对输入执行一组运算，服务系统安排这些运算在设备上执行。设备保存的数据、完成的计算和交换的结果，共同构成 AI Infra 的工作。图 1-3 按这一过程划分六个层次；后面的估算将逐层追踪同一个请求。
+先看 AI 系统怎样处理一次请求。应用提出回答质量和响应时间要求，模型按照自身结构对输入执行一组运算，服务系统安排这些运算在设备上执行。设备保存的数据、完成的计算和交换的结果，共同构成 AI Infra 的工作。图 1-2 按这一过程划分六个层次；后面的估算将逐层追踪同一个请求。
 
 ![从应用到设备的六层分工。沿箭头向下，任务逐步落实为运算、程序和物理资源；调度、观测等跨层能力由正文说明。](ch01/figure-1-1-panorama.svg)
 
-*图 1-3　从应用到设备的六层分工。沿箭头向下，任务逐步落实为运算、程序和物理资源；调度、观测等跨层能力由正文说明。*
+*图 1-2　从应用到设备的六层分工。沿箭头向下，任务逐步落实为运算、程序和物理资源；调度、观测等跨层能力由正文说明。*
 
 最上面是**应用与任务**。对话、代码生成、语音交互，以及能够调用工具推进任务的智能体（Agent），都在这一层提出具体要求：要完成什么任务，多快返回结果，成本上限是多少。训练也有自己的任务目标，例如在给定时间内完成一次模型更新或一轮预训练。
 
@@ -77,7 +50,7 @@
 
 六个层次说明了主要分工，但有些能力贯穿多个层次。资源调度、运行环境、观测和计费需要结合多个层次的信息来组织；工艺、封装、供电和散热则决定设备能够怎样制造和部署。网络也并非只在最下方发挥作用：需要跨设备协作的模型、算子和服务，都要经过具体互联。
 
-沿这六层向下看，任务逐步变成具体的计算与数据访问；向上看，设备容量、算力与带宽决定系统能运行多大的模型、同时处理多少请求，以及多快作出响应。海思首席科学家廖恒博士提出的“十八层宝塔”，用来描述从应用、模型与软件系统，到芯片、制造工艺和底层物理的多个层次，强调这些层次之间的联系。本书借鉴这一思路，从各层之间的联系出发，并按教学需要将讨论内容归为图 1-3 的六层。[^panorama]
+沿这六层向下看，任务逐步变成具体的计算与数据访问；向上看，设备容量、算力与带宽决定系统能运行多大的模型、同时处理多少请求，以及多快作出响应。海思首席科学家廖恒博士提出的“十八层宝塔”，用来描述从应用、模型与软件系统，到芯片、制造工艺和底层物理的多个层次，强调这些层次之间的联系。本书借鉴这一思路，从各层之间的联系出发，并按教学需要将讨论内容归为图 1-2 的六层。[^panorama]
 
 这些层次之间的接口，也是开展联合优化的切入点。模型改变状态表示，会改变存储和通信需求；编译器将相邻算子融合，会改变中间结果的保存位置；运行时让设备直接交接，会改变主机参与的次数。后续章节将从这些具体变化出发，计算省下的读写、提交和等待，再比较完整请求的耗时，判断哪一种组织方式更合适。
 
@@ -87,11 +60,11 @@
 
 ### 1.1.3 一次生成请求的处理过程
 
-服务实例是能够独立完成模型请求的一组执行资源，可以由一个或多个设备组成。每次请求交由其中一个实例处理。图 1-4 展示了请求分配与实例内部执行的分工：服务路由器是选择请求执行位置的软件，选定实例后，由实例内部安排设备执行模型。先分清谁负责执行，才能知道一份权重应放在哪里、一次中间结果应传给谁。
+服务实例是能够独立完成模型请求的一组执行资源，可以由一个或多个设备组成。每次请求交由其中一个实例处理。图 1-3 展示了请求分配与实例内部执行的分工：服务路由器是选择请求执行位置的软件，选定实例后，由实例内部安排设备执行模型。先分清谁负责执行，才能知道一份权重应放在哪里、一份中间结果应传给谁。
 
 ![一次请求先由路由器选择模型实例，再由实例内部的调度器安排执行。灰色外框圈出同一个实例，实线表示请求和工作提交的方向。](ch01/figure-1-2-request.svg)
 
-*图 1-4　一次请求先由路由器选择模型实例，再由实例内部的调度器安排执行。灰色外框圈出同一个实例，实线表示请求和工作提交的方向。*
+*图 1-3　一次请求先由路由器选择模型实例，再由实例内部的调度器安排执行。灰色外框圈出同一个实例，实线表示请求和工作提交的方向。*
 
 上下文（context）是模型当前可使用的输入信息，包括用户输入、已有对话、检索结果和此前生成的内容。应用将这些信息组织成请求，交给模型服务入口。入口核验访问权限、检查配额和请求内容后，路由器依据目标模型、实例负载等条件选择副本。这里的副本指一个能够独立完成请求的模型实例，可以运行在一张卡上，也可以分布在多张卡上。路由选定实例之后，实例内部再安排这些设备协作。
 
@@ -103,13 +76,13 @@
 
 ![权重加载一次，生成时逐步读取。蓝框表示显存中持续保存的同一份权重，三个绿框表示先后发生的计算；箭头表示读取或结果依赖，步骤间距不代表耗时。](ch01/figure-1-weight-lifetime.svg)
 
-*图 1-5　权重加载一次，生成时逐步读取。蓝框表示显存中持续保存的同一份权重，三个绿框表示先后发生的计算；箭头表示读取或结果依赖，步骤间距不代表耗时。*
-
-这些软件功能最终要在机房中的设备上运行，图 1-6 展示了它们对应的硬件。数据中心包含入口与 CPU 服务、共享存储，以及由多个超节点构成的加速器资源池。超节点是通过高带宽互联紧密协作的一组设备，可以跨多个服务器或计算托盘组织。
+*图 1-4　权重加载一次，生成时逐步读取。蓝框表示显存中持续保存的同一份权重，三个绿框表示先后发生的计算；箭头表示读取或结果依赖，步骤间距不代表耗时。*
+
+这些软件功能最终要在机房中的设备上运行，图 1-5 展示了它们对应的硬件。数据中心包含入口与 CPU 服务、共享存储，以及由多个超节点构成的加速器资源池。超节点是一组通过高带宽互联紧密协作的设备，可以分布在多个服务器或计算托盘中。
 
 ![物理连接的两层视图。上方用数据中心网络连接服务与超节点，下方放大一个超节点，显示主机、网卡、GPU 与显存；线条展示数据路径，设备数量用于示意。](ch01/figure-1-3-datacenter.svg)
 
-*图 1-6　物理连接的两层视图。上方用数据中心网络连接服务与超节点，下方放大一个超节点，显示主机、网卡、GPU 与显存；线条展示数据路径，设备数量用于示意。*
+*图 1-5　物理连接的两层视图。上方用数据中心网络连接服务与超节点，下方放大一个超节点，显示主机、网卡、GPU 与显存；线条展示数据路径，设备数量用于示意。*
 
 在一个服务器或计算托盘内，CPU 连接主存，负责主机程序和执行控制；GPU 连接各自的显存，执行模型计算、保存运行所需的数据。HBM（高带宽内存）是加速器常用的一类显存，通过宽接口持续提供数据。CPU 与 GPU 可以通过 PCIe（高速外设互联）连接，GPU 之间也可以通过独立的高速互联直接交换数据。网卡把本机连接到网络。在支持直接内存访问的系统中，数据可以由传输硬件直接写入指定内存区域，CPU 负责发起和管理传输；第 7 章会进一步解释这种交接。
 
@@ -119,7 +92,7 @@
 
 > **练习 1-1〔延伸〕：追踪一次请求的数据**
 >
-> 沿图 1-4 至图 1-6，补画 KV 状态随输出增加的过程，再分别标出模型文件加载、一步权重读取、KV 追加和卡间结果传递。若回答从 100 个 token 增至 1000 个，哪些数据只加载一次，哪些访问会增加？若把一个实例复制成两个实例，权重容量与请求路由又怎样改变？
+> 沿图 1-3 至图 1-5，补画 KV 状态随输出增加的过程，再分别标出模型文件加载、一步权重读取、KV 追加和卡间结果传递。若回答从 100 个 token 增至 1000 个，哪些数据只加载一次，哪些访问会增加？若把一个实例复制成两个实例，权重容量与请求路由又怎样改变？
 
 一次模型执行既受单卡能力影响，也受设备连接方式影响。单卡的算力和存储带宽决定本地计算与读写的耗时，多卡协作则增加数据交换和等待。下面先估算单卡执行，再将同样的方法用于后续章节的超节点和网络分析。
 
@@ -168,7 +141,7 @@
 
 ![Jeff Dean 2009 年演讲中的三种操作延迟。横轴为对数刻度，每相邻数量级相差十倍；先识别操作，再比较它们在串行等待中的代价。](ch01/figure-1-4-numbers.svg)
 
-*图 1-7　Jeff Dean 2009 年演讲中的三种操作延迟。横轴为对数刻度，每相邻数量级相差十倍；先识别操作，再比较它们在串行等待中的代价。*
+*图 1-6　Jeff Dean 2009 年演讲中的三种操作延迟。横轴为对数刻度，每相邻数量级相差十倍；先识别操作，再比较它们在串行等待中的代价。*
 
 从这张历史资源表中学到的方法，可以直接用于模型执行：先确定各项工作使用什么资源，再比较它们所需的时间。接下来用加速器的容量、算力和带宽完成同样的估算。
 
@@ -223,11 +196,11 @@
 
 还可以改变权重的存储精度。**量化（quantization）**用较少的位数表示数值，例如用 8 位整数近似保存原来的 16 位浮点权重，并用缩放因子将整数还原为相应范围内的近似值。每个被量化的权重由 2 字节减为 1 字节，因而能显著降低内存占用。
 
-本书采用的分组量化方案每 128 个权重共享一个缩放因子，同时让部分参数继续使用 BF16。按真实模型配置计算，8 比特方案的全部权重及量化附加数据约占 73.73 GB，可以放入一张 H100 SXM，留下约 6.27 GB。图 1-8 将两种办法放在相同的容量刻度上：BF16 权重分到两张卡，或者量化后放入一张卡。
+本书采用的分组量化方案每 128 个权重共享一个缩放因子，同时让部分参数继续使用 BF16。按真实模型配置计算，8 比特方案的全部权重及量化附加数据约占 73.73 GB，可以放入一张 H100 SXM，留下约 6.27 GB。图 1-7 将两种办法放在相同的容量刻度上：BF16 权重分到两张卡，或者量化后放入一张卡。
 
 ![DeepSeek-R1-Distill-Llama-70B 的 BF16 权重与 8 比特量化权重在 H100 SXM 上的容量比较](ch01/figure-1-capacity-path.svg)
 
-*图 1-8　DeepSeek-R1-Distill-Llama-70B：BF16 权重共 141.11 GB，两卡均分后每卡约 70.55 GB；分组 8 比特量化后共 73.73 GB。各条使用相同尺度，虚线表示一张 H100 SXM 的名义 80 GB 容量。柱长只计权重及相应量化附加数据。*
+*图 1-7　DeepSeek-R1-Distill-Llama-70B：BF16 权重共 141.11 GB，两卡均分后每卡约 70.55 GB；分组 8 比特量化后共 73.73 GB。各条使用相同尺度，虚线表示一张 H100 SXM 的名义 80 GB 容量。柱长只计权重及相应量化附加数据。*
 
 **KV 缓存如何增加单请求的显存需求。** 生成文本时，显存还要保存上下文 token 的键和值，称为 **KV 缓存（key–value cache）**，以便后续计算直接复用；算子执行还需要临时工作区。因此，容量检查应写成
 
@@ -263,17 +236,17 @@
 
 ![本例按每参数一字节，将主要权重读取量近似取为 70 GB。权重驻留显存，计算单元每步沿同一接口读取一遍；用读取量除以接口带宽，得到 20.90 ms 的读取下界。](ch01/figure-1-read-path.svg)
 
-*图 1-9　本例按每参数一字节，将主要权重读取量近似取为 70 GB。权重驻留显存，计算单元每步沿同一接口读取一遍；用读取量除以接口带宽，得到 20.90 ms 的读取下界。*
+*图 1-8　本例按每参数一字节，将主要权重读取量近似取为 70 GB。权重驻留显存，计算单元每步沿同一接口读取一遍；用读取量除以接口带宽，得到 20.90 ms 的读取下界。*
 
 同一份权重会在相邻生成步骤间反复使用。对单个请求，下一步的输入由当前输出确定，后续步骤要沿这条依赖逐次推进。权重虽然已经加载到设备，显存到计算单元的读取仍在每步发生。
 
-计算量也可以先做一个近似。对于一次矩阵与向量相乘，一个权重通常参与一次乘法，并累加到结果中；按乘法与加法分别计数，大约对应两次浮点运算。因此，本例的主要矩阵运算量近似为：
+计算量也可以先做一个近似。对于一次矩阵与向量相乘，每个权重通常参与一次乘法，所得乘积再累加到结果中；按乘法与加法分别计数，大约对应两次浮点运算。因此，本例的主要矩阵运算量近似为：
 
 $$
 \begin{aligned}\text{每步矩阵运算量}&\approx2\times\text{参数数}\\&\approx2\times70\times10^9\ \mathrm{FLOPs}\\&=140\ \mathrm{GFLOPs}.\end{aligned}
 $$
 
-$2N$ 的来源是在一个投影中，每个权重都对一个 token 的特征向量进行一次乘加运算。参数矩阵中的权重越多，这部分工作越大；一批中本次参与计算的 token 增加时，同一权重矩阵处理更多 token 的特征向量，运算量随之增加。第二章将展开真实模型，在这部分线性工作之外加入随上下文长度变化的注意力交互。
+$2N$ 的来源是在一个投影中，每个权重与一个 token 特征向量中的对应分量相乘，再将乘积累加到输出中。参数矩阵中的权重越多，这部分工作越大；一批中本次参与计算的 token 增加时，同一权重矩阵处理更多 token 的特征向量，运算量随之增加。第二章将展开真实模型，在这部分线性工作之外加入随上下文长度变化的注意力交互。
 
 把在一次执行中共同处理的请求组成一个批次，批内请求数记为 $B$，也称 batch size。将批内请求数增至 $B$，并让这些请求共同使用一次读入的权重，就得到这个教学模型的三个量：
 
@@ -311,15 +284,15 @@
 
 ![保持运算量与读取量不变，分别把算力或带宽翻倍。蓝条是权重读取下界，橙条是矩阵计算下界；较长的读取项决定这组条件下的优化方向。](ch01/figure-1-5-budget.svg)
 
-*图 1-10　保持运算量与读取量不变，分别把算力或带宽翻倍。蓝条是权重读取下界，橙条是矩阵计算下界；较长的读取项决定这组条件下的优化方向。*
+*图 1-9　保持运算量与读取量不变，分别把算力或带宽翻倍。蓝条是权重读取下界，橙条是矩阵计算下界；较长的读取项决定这组条件下的优化方向。*
 
 接着改变请求组织。假设同时处理 8 个请求，并让它们共享一次权重读取，整批的权重读取仍是 70 GB，矩阵运算量则约为单请求的 8 倍。这时矩阵计算的时间下界约为 1.13 ms，读取下界仍为 20.90 ms。若这一批产生 8 个输出，每个输出分摊的权重读取时间约为 2.61 ms。
 
-整批约 20.90 ms 才完成这些权重的读取，八个请求各得到一个新输出；将它除以八，得到的是每个输出分摊的服务时间。吞吐因而从单请求模型的约 47.9 token/s 提高到约 383 token/s，而每个请求仍经历整批执行。**批内复用增加同一时间内生成的输出数，单请求延迟则取决于它经历的执行与等待。**
+整批约 20.90 ms 才完成这些权重的读取，八个请求各得到一个新输出；将它除以八，得到的是每个输出分摊的服务时间。吞吐因而从单请求模型的约 47.9 token/s 提高到约 383 token/s，而每个请求仍须等待整批执行完成。**批内复用增加同一时间内生成的输出数，单请求延迟则取决于它经历的执行与等待。**
 
 ![一批八个请求共享一次权重读取，各产生一个输出。整批读取仍需约 20.90 ms，除以八得到每输出分摊的服务时间；每个请求经历整批执行。](ch01/figure-1-batch-reuse.svg)
 
-*图 1-11　一批八个请求共享一次权重读取，各产生一个输出。整批读取仍需约 20.90 ms，除以八得到每输出分摊的服务时间；每个请求经历整批执行。*
+*图 1-10　一批八个请求共享一次权重读取，各产生一个输出。整批读取仍需约 20.90 ms，除以八得到每输出分摊的服务时间；每个请求经历整批执行。*
 
 batch 增加到一定程度后，计算时间会赶上权重读取时间。令 $2BN/\Pi=b_WN/\beta$，得到转折点
 
@@ -331,15 +304,15 @@
 
 ![批内请求增加时，矩阵运算量随请求数成正比增长，权重读取保持每批 70 GB。约 148 个请求处两项下界相等，随后计算耗时决定整体速度。](ch01/figure-1-batch-transition.svg)
 
-*图 1-12　批内请求增加时，矩阵运算量按 $2BN$ 增长，权重读取保持每批 70 GB。约 148 个请求处两项下界相等，随后计算耗时决定整体速度。*
+*图 1-11　批内请求增加时，矩阵运算量按 $2BN$ 增长，权重读取保持每批 70 GB。约 148 个请求处两项下界相等，随后计算耗时决定整体速度。*
 
 把每批输出数 $B$ 除以上图中的时间下界，便得到这组题设对应的吞吐上界。转折前，共享读取被更多输出分摊；转折后，计算时间与输出数一起增长，曲线逐渐趋于平坦。
 
 ![同一批量模型的理想输出吞吐。每批输出数除以时间下界得到曲线；竖虚线与前图对应同一个约 148 请求的转折点。](ch01/figure-1-batch-throughput.svg)
 
-*图 1-13　同一批量模型的理想输出吞吐。每批输出数除以时间下界得到曲线；竖虚线与前图对应同一个约 148 请求的转折点。*
-
-同一转折也可以用算术强度 $I=F/R_W=2B/b_W$ 描述，即每读取一字节数据所完成的运算次数。当 $I$ 小于设备的算力与带宽之比 $\Pi/\beta$ 时，读取较慢；超过这个比值后，计算较慢。这就是第四章 Roofline 模型的出发点。[^roofline]
+*图 1-12　同一批量模型的理想输出吞吐。每批输出数除以时间下界得到曲线；竖虚线与前图对应同一个约 148 请求的转折点。*
+
+同一转折也可以用算术强度 $I=F/R_W=2B/b_W$ 描述，即每读取一字节数据所完成的运算次数。当 $I$ 小于设备的算力与带宽之比 $\Pi/\beta$ 时，读取所需时间更长；超过这个比值后，计算所需时间更长。这就是第四章 Roofline 模型的出发点。[^roofline]
 
 > **练习 1-3〔核心〕：带宽与批量如何影响单步生成时限**
 >
@@ -347,19 +320,19 @@
 
 ### 1.3.3 用实测检验估算
 
-前面的模型预测了两个趋势：batch 较小时，吞吐随 batch 增加；整批时间则受到一次权重读取的限制。真实程序中，每条新增请求还会增加上下文访问与计算。图 1-14、1-15 给出 Qwen3-8B 在 RTX PRO 6000 Blackwell 上的测量：使用 BF16 权重。vLLM 是加州大学伯克利分校等机构的研究者于 2023 年提出的开源推理服务系统，最初重点解决 KV 缓存浪费显存、限制批量的问题；它组织模型请求并执行推理。本次采用 0.23 版本的 eager 模式，即按程序运行过程逐项提交设备工作。每请求输入 2048 个 token、生成 256 个 token，每档 batch 测量三次。[^measurement] 吞吐表示单位时间内产生的输出数；TPOT（time per output token）在这里表示客户端观察到的平均输出间隔。下面先看整批输出速度，再看单个请求的生成节奏。
+前面的模型预测了两个趋势：batch 较小时，吞吐随 batch 增加；整批时间则受到一次权重读取的限制。真实程序中，每条新增请求还会增加上下文访问与计算。图 1-13、1-15 给出 Qwen3-8B 在 RTX PRO 6000 Blackwell 上使用 BF16 权重的测量结果。vLLM 是加州大学伯克利分校等机构的研究者于 2023 年提出的开源推理服务系统，最初重点解决 KV 缓存浪费显存、限制批量的问题；它组织模型请求并执行推理。本次采用 0.23 版本的 eager 模式，即按程序运行过程逐项提交设备工作。每请求输入 2048 个 token、生成 256 个 token，每档 batch 测量三次。[^measurement] 吞吐表示单位时间内产生的输出数；TPOT（time per output token）在这里表示客户端观察到的平均输出间隔。下面先看整批输出速度，再看单个请求的生成节奏。
 
 ![Qwen3-8B 实测的整批输出吞吐。四档请求数等距排列，纵轴从零开始；模型、精度、输入输出长度和计时范围保持一致。](ch01/figure-1-measured-throughput.svg)
 
-*图 1-14　Qwen3-8B 实测的整批输出吞吐。四档请求数等距排列，纵轴从零开始；模型、精度、输入输出长度和计时范围保持一致。*
+*图 1-13　Qwen3-8B 实测的整批输出吞吐。四档请求数等距排列，纵轴从零开始；模型、精度、输入输出长度和计时范围保持一致。*
 
 ![同一组实测中的每请求输出间隔。吞吐增加的同时，单个请求的平均间隔也在增大；两张图分别说明设备的输出速度和用户的等待时间。](ch01/figure-1-measured-tpot.svg)
 
-*图 1-15　同一组实测中的每请求输出间隔。吞吐增加的同时，单个请求的平均间隔也在增大；两张图分别说明设备的输出速度和用户的等待时间。*
+*图 1-14　同一组实测中的每请求输出间隔。吞吐增加的同时，单个请求的平均间隔也在增大；两张图分别说明设备的输出速度和用户的等待时间。*
 
 从 1 个请求增到 64 个请求，整批吞吐约提高 31 倍，单请求输出间隔则从约 26.5 ms 增至 40.5 ms。两幅图合起来描述批量组织的取舍：设备在同一时间内完成更多请求的工作，每个请求完成一步生成所需的时间也更长。
 
-图 1-14 的吞吐按整批输出数除以处理这批请求的总时间计算，其中包括输入处理时间。TPOT 则先对每个请求计算首末输出事件之间的时间，再除以剩余的 255 个输出间隔，最后求所有请求的平均值。前者描述整个实例的输出速度，后者描述单个用户经历的生成节奏。第三章将进一步分析请求到达、排队和少量慢请求对这些指标的影响。
+图 1-13 的吞吐按整批输出数除以处理这批请求的总时间计算，其中包括输入处理时间。TPOT 则先对每个请求计算首末输出事件之间的时间，再除以剩余的 255 个输出间隔，最后求所有请求的平均值。前者描述整个实例的输出速度，后者描述单个用户经历的生成节奏。第三章将进一步分析请求到达、排队和少量慢请求对这些指标的影响。
 
 这一趋势可以从权重与上下文状态的区别来解释。多个请求共同使用模型权重，但各自保存上下文状态。并发增加时，一次矩阵运算会处理更多请求当前输入的 token 表示，权重得到更多复用；与此同时，注意力计算和上下文访问也随请求数增加。
 
@@ -373,7 +346,7 @@
 
 > **练习 1-4〔核心〕：用实测吞吐与输出间隔检验批处理模型**
 >
-> 根据图 1-14、1-15 标出的四档并发请求测量值，计算相对单请求的吞吐加速比和 TPOT 增长比例。假设增加并发后仍只受共享权重读取限制，计算与上下文访问都不增加耗时，吞吐率应怎样变化？对照实际结果，提出两个可区分的原因，并设计一次只改变上下文长度的测量来判断它们。指出测量前需要保持不变的模型、输出长度与计时起止点。
+> 根据图 1-13、1-15 标出的四档并发请求测量值，计算相对单请求的吞吐加速比和 TPOT 增长比例。假设增加并发后仍只受共享权重读取限制，计算与上下文访问都不增加耗时，吞吐率应怎样变化？对照实际结果，提出两个可区分的原因，并设计一次只改变上下文长度的测量来判断它们。指出测量前需要保持不变的模型、输出长度与计时起止点。
 
 回看这次估算，我们实际上一直在回答五个问题：**搬什么、搬多少、搬几次、经过哪里、谁在等待。** 模型结构决定需要哪些权重，位宽和参数量决定存储大小，批量复用改变了读取次数，显存接口提供了带宽，执行依赖决定了等待。这五个问题把模型结构、数据路径和执行顺序连成一份可以逐项计算的预算。
 
@@ -383,7 +356,7 @@
 
 ### 1.4.1 TPU
 
-TPU 是 Google 为神经网络计算设计的张量处理器，其第一代产品记为 TPU v1。介绍这代产品的论文回顾了一次需求判断。Google 很早就讨论过在数据中心使用 GPU、可在部署后配置硬件逻辑的 FPGA，或专用芯片，但在一些早期应用中，既有数据中心的空闲资源已经足以满足需求。2013 年，一项语音搜索使用量预测改变了讨论：如果每天使用三分钟语音搜索，满足这项计算需求可能需要让数据中心规模翻倍。Google 因而推进专用推理芯片设计，TPU v1 于 2015 年进入数据中心部署，架构论文于 2017 年发表。[^tpu]
+TPU 是 Google 为神经网络计算设计的张量处理器，其第一代产品记为 TPU v1。介绍这代产品的论文回顾了一次需求判断。Google 很早就讨论过在数据中心使用 GPU、可在部署后配置硬件逻辑的 FPGA，或专用芯片，但在一些早期应用中，既有数据中心的空闲资源已经足以满足需求。2013 年，一项语音搜索使用量预测改变了讨论：如果每位用户每天使用三分钟语音搜索，满足这项计算需求可能需要让数据中心规模翻倍。Google 因而推进专用推理芯片设计，TPU v1 于 2015 年进入数据中心部署，架构论文于 2017 年发表。[^tpu]
 
 把每天的请求量记为 $Q$、每请求需要的计算量记为 $F_1$，每日总需求就是 $QF_1$。请求量增加，现有设备的空闲能力最终会被用尽，此时可以增加通用服务器，也可以开发专用处理器。后者有前期开发成本，却可能降低每请求的设备时间和能耗；服务规模越大，累计节省的成本就越容易抵偿前期投入。
 
@@ -391,7 +364,7 @@
 
 ![专用处理器围绕反复出现的矩阵运算组织计算阵列和输入输出缓冲。缓冲是临时保存待计算或已算完数据的存储区域；三个方框及其箭头展示数据搬运方向。](ch01/figure-1-design-tpu.svg)
 
-*图 1-16　专用处理器围绕反复出现的矩阵运算组织计算阵列和输入输出缓冲。缓冲是临时保存待计算或已算完数据的存储区域；三个方框及其箭头展示数据搬运方向。*
+*图 1-15　专用处理器围绕反复出现的矩阵运算组织计算阵列和输入输出缓冲。缓冲是临时保存待计算或已算完数据的存储区域；三个方框及其箭头展示数据搬运方向。*
 
 第 4 章将介绍 TPU 的矩阵阵列、缓冲和数据通路。本例先说明一个关系：应用的使用量达到一定规模后，专用处理器就可能比通用处理器更合算。
 
@@ -413,7 +386,7 @@
 
 ![可编程网卡在数据进入主机前完成指定的包处理。图中实线跟踪数据；网卡承担的处理减少主机 CPU 的辅助工作。](ch01/figure-1-design-smartnic.svg)
 
-*图 1-17　可编程网卡在数据进入主机前完成指定的包处理。图中实线跟踪数据；网卡承担的处理减少主机 CPU 的辅助工作。*
+*图 1-16　可编程网卡在数据进入主机前完成指定的包处理。图中实线跟踪数据；网卡承担的处理减少主机 CPU 的辅助工作。*
 
 将包处理移到网卡后，部分操作可以在数据到达时直接完成，CPU 因而少做一些工作。如果网卡还需要主机内存中的数据，就要通过 PCIe 读取；此时，返回延迟和可同时发出的请求数决定了读取速度。第 7 章将用 KV-Direct 分析这种访问；它是让可编程网卡直接处理键值存储请求的系统。键值存储按唯一的键查找、读取或更新对应的值。
 
@@ -429,7 +402,7 @@
 
 ![统一互联连接不同设备的计算与存储资源。模型分工确定要交换什么，互联负责把数据送到后续使用它的设备。](ch01/figure-1-design-ub.svg)
 
-*图 1-18　统一互联连接不同设备的计算与存储资源。模型分工确定要交换什么，互联负责把数据送到后续使用它的设备。*
+*图 1-17　统一互联连接不同设备的计算与存储资源。模型分工确定要交换什么，互联负责把数据送到后续使用它的设备。*
 
 从语音推理到云网络，再到大模型的多设备执行，这些设计分别着重提高计算效率、减少主机 CPU 占用和改善跨设备访问。需求改变了需要优先解决的瓶颈，也改变了硬件、软件与互联之间的分工。
 
@@ -439,11 +412,11 @@
 
 ## 谬误与陷阱
 
-**误区：峰值算力翻倍，执行速度就会翻倍。** 先检查计算是否限制当前执行。例题 1-1 中，权重读取的时间下界远大于矩阵计算的时间下界，提高矩阵峰值算力几乎不改变执行时间的下界；若优化的是完整任务的一部分，还要用 Amdahl 定律检查收益上限。
+**误区：峰值算力翻倍，执行速度就会翻倍。** 先检查当前执行是否受计算能力限制。例题 1-1 中，权重读取的时间下界远大于矩阵计算的时间下界，提高矩阵峰值算力几乎不改变执行时间的下界；若优化的是完整任务的一部分，还要用 Amdahl 定律检查收益上限。
 
 **误区：显存放得下权重，就能支持目标并发数。** 权重只是常驻数据的一部分。上下文状态、工作区和运行时预留共同消耗容量，多设备还必须逐卡核算。
 
-**误区：吞吐率提高，每个用户的等待时间就会缩短。** 批内复用减少每个输出分摊的读取开销，但每个请求仍经历整批执行和自己的排队。吞吐与响应时间应同时报告。
+**误区：吞吐率提高，每个用户的等待时间就会缩短。** 批内复用减少每个输出分摊的读取开销，但每个请求仍须等待整批执行完成和自己的排队。吞吐与响应时间应同时报告。
 
 ## 本章数字速查
 
@@ -463,11 +436,11 @@
 
 这组数字解释了两阶段的差别：prefill 在一次调用中处理大量输入，矩阵工作量较大；单请求 decode 每次只处理一个位置，却仍要读取大量权重和上下文。提高计算吞吐、提高带宽和增加批内复用，分别作用于不同的时间项。
 
-实际响应表现可参见图 1-14、1-15：那里使用 RTX PRO 6000 Blackwell、vLLM 0.23 和明确的请求长度，显示吞吐与客户端输出间隔。GPU 规格表提供硬件性能数据，模型表用于估算工作量，实测图则展示系统实际运行的表现。
+实际响应表现可参见图 1-13、1-15：那里使用 RTX PRO 6000 Blackwell、vLLM 0.23 和明确的请求长度，显示吞吐与客户端输出间隔。GPU 规格表提供硬件性能数据，模型表用于估算工作量，实测图则展示系统实际运行的表现。
 
 [^panorama]: “十八层宝塔”的提出者为海思首席科学家廖恒博士。全栈讨论的背景资料见[《Unified Bus 背后的思考》本地原件](../references/files/documents/ub-reflection.md)及[系统抽象边界取材记录](../research/system-abstraction-boundary/report.md)。六层图为本书自绘的教学分组。
 
-[^dean]: Jeff Dean，LADIS 2009 演讲，[本地 PDF](../references/outline-checks/2026-09-07/scaling-history/jeff-dean-ladis2009.pdf)，查阅“Numbers Everyone Should Know”与随后纸笔估算的幻灯片。正文 20 次寻道的例子是本书教学假设。
+[^dean]: Jeff Dean，LADIS 2009 演讲，[本地 PDF](../references/outline-checks/2026-09-07/scaling-history/jeff-dean-ladis2009.pdf)，“Numbers Everyone Should Know”及纸笔估算部分。寻道算例设连续执行 20 次随机读取。
 
 [^h100]: NVIDIA，[H100 架构白皮书](../references/files/specs/nvidia-h100.pdf)与[规格页面快照](../references/files/specs/nvidia-h100-spec.md)。本章采用 SXM 形态与 BF16 稠密矩阵运算规格；原始输入版本和校验值见[配图来源清单](ch01/sources.json)。
 
@@ -475,7 +448,7 @@
 
 [^roofline]: Williams、Waterman、Patterson，[Roofline 论文](../references/files/papers/roofline.pdf)。
 
-[^measurement]: [练习 1-4 记录说明](../experiments/ch01/01-04/README.md)、[结构化分析结果](../experiments/ch01/01-04/results/analysis.json)及[后续补测](../experiments/ch01/01-04/FOLLOWUP.md)。正文和图 1-14、图 1-15 采用最初记录的短输入组。
+[^measurement]: [练习 1-4 记录说明](../experiments/ch01/01-04/README.md)、[结构化分析结果](../experiments/ch01/01-04/results/analysis.json)及[后续补测](../experiments/ch01/01-04/FOLLOWUP.md)。正文和图 1-13、图 1-14 采用最初记录的短输入组。
 
 [^tpu]: Jouppi 等，[TPU v1 论文](../references/files/papers/tpu-v1.pdf)，第 2 节关于起源、架构与实现的说明。三分钟语音搜索是论文记载的历史需求预测。
 
@@ -487,7 +460,7 @@
 
 [^request]: 请求图是通用职责示意；调度与状态管理可参照 [vLLM Scheduler 文档](https://docs.vllm.ai/en/v0.26.0/api/vllm/v1/core/sched/scheduler/)。路由策略、tokenizer 位置以及 prefill／decode 是否分离属于部署选择。
 
-[^datacenter]: 具体产品例子见 [NVIDIA GB200 NVL72](https://www.nvidia.com/en-us/data-center/gb200-nvl72/)、[硬件指南](https://docs.nvidia.com/dgx/dgxgb200-user-guide/hardware.html)与[网络指南](https://docs.nvidia.com/dgx/dgxgb200-user-guide/networking.html)。图 1-6 使用一般化连接。
+[^datacenter]: 具体产品例子见 [NVIDIA GB200 NVL72](https://www.nvidia.com/en-us/data-center/gb200-nvl72/)、[硬件指南](https://docs.nvidia.com/dgx/dgxgb200-user-guide/hardware.html)与[网络指南](https://docs.nvidia.com/dgx/dgxgb200-user-guide/networking.html)。图 1-5 使用一般化连接。
 
 [^real70]: 参数与 BF16 字节数来自 [DeepSeek-R1-Distill-Llama-70B 公开权重索引](../calculations/sources/deepseek-r1-distill-llama-70b/model.safetensors.index.json)及[固定版本配置](../calculations/configs/models/deepseek-r1-distill-llama-70b/config.json)。分组量化的权重总量采用[逐项计算记录](../calculations/results/dense-quant-deepseek-r1-distill-llama-70b-tp1-pp8-80gb-8192.json)中的全模型汇总，每组 128 个参数、缩放因子占 2 字节。RTX 4090 的 24 GB 名义规格见[官方页面存档](../calculations/sources/hardware/nvidia-rtx4090-page.txt)。本节用规格标称 GB 作统一容量预算。
 
--- before/manuscripts/03-推理与训练负载.md
+++ after/manuscripts/03-推理与训练负载.md
@@ -710,7 +710,7 @@
 
 [^agent-calc]: [开启 thinking 的真实轨迹复算](../calculations/results/agent-thinking-on.md)。
 
-[^context]: [作者上下文材料及取材范围](../case-studies/author-context-and-design.md)，连接作者 AI Agent 书第二章与实验记录。
+[^context]: [上下文组织与 Agent 案例](../case-studies/author-context-and-design.md)，连接作者 AI Agent 书第二章与实验记录。
 
 [^reasoning]: [练习 3-3：1,024 预算](../experiments/ch03/03-03/README.md)与[4,096 预算](../experiments/ch03/03-03/wide-budget/README.md)；理论背景见[Test-Time Compute](../references/files/papers/test-time-compute.pdf)。
 
@@ -774,7 +774,7 @@
 
 [^lifecycle]: [根据 C4 拟合结果估算生命周期成本](../calculations/results/real-c4-lifecycle-512-128.md)。
 
-[^history]: [训练投入与 Scaling Law 历史笔记](../case-studies/scaling-history.md)及[锁定公开字段的复算](../calculations/results/training-history-published.md)，保留各模型原报告与阶段范围。
+[^history]: [训练投入与 Scaling Law 历史笔记](../case-studies/scaling-history.md)及[锁定公开字段的复算](../calculations/results/training-history-published.md)。
 
 [^source-1]: [多模态计算资料](../case-studies/generative-multimodal-models.md)。
 
--- before/manuscripts/04-加速器架构.md
+++ after/manuscripts/04-加速器架构.md
@@ -336,7 +336,7 @@
 
 > **例 4-2：把接口带宽翻倍，读取速度也会翻倍吗？**
 >
-> **比较在途事务数与接口带宽对 KV 读取的限制。** 读取一条请求的 8,192 位置 KV，共 1.125 GiB。接口带宽为 1 TB/s，每事务 128 bytes，完成延迟为 500 ns。比较 128 和 4,096 个在途事务，再把接口升级到 2 TB/s。
+> **比较在途事务数与接口带宽对 KV 读取的限制。** 读取一条请求的 8,192 个上下文位置所对应的 KV，共 1.125 GiB。接口带宽为 1 TB/s，每事务 128 bytes，完成延迟为 500 ns。比较 128 和 4,096 个在途事务，再把接口升级到 2 TB/s。
 >
 > **由在途字节数和访问延迟求有效读取带宽。** 维持 1 TB/s 需要 $\lceil10^{12}\times500\times10^{-9}/128\rceil=3907$ 个事务。128 个事务只能提供约 32.8 GB/s，读取时间至少为 36.9 ms；4,096 个事务足以维持 1 TB/s，时间降为约 1.21 ms。
 >
@@ -664,7 +664,7 @@
 
 输入流水给出了同样的例子：四块共需 256 tick 输入传输、512 tick 计算，而首块在 192 才就绪，所以最早完成时间为 704 tick。三个槽可以按前面的时间表完成计算，两个槽则因第三块晚到又增加 64 tick。计算量和传输量决定各步骤耗时，数据依赖与缓冲容量决定各步骤何时开始。
 
-矩阵单元的运算速率还与输入和累加精度有关。RTX 4090 的 Tensor Core 执行稠密矩阵运算时的峰值，FP16（每元素 2 字节的半精度浮点格式）输入与 FP16 累加约为 330 TFLOP/s，FP32 累加约为 165 TFLOP/s；本章 BF16／FP32 投影采用 165.2 TFLOP/s。[^hardware] 对 8.59 GFLOPs，两个速率分别对应约 26 和 52 μs。后面算出的读取时间约为 37 μs，速率选择会直接改变计算与读取的大小关系。因此，速率应从执行路径确定：先确定矩阵单元实际处理的数据格式，再用该格式对应的运算速率计算时间。
+矩阵单元的运算速率还与输入和累加精度有关。RTX 4090 的 Tensor Core 执行稠密矩阵运算时的峰值，FP16（每元素 2 字节的半精度浮点格式）输入与 FP16 累加约为 330 TFLOP/s，FP32 累加约为 165 TFLOP/s；本章 BF16／FP32 投影采用 165.2 TFLOP/s。[^hardware] 对 8.59 GFLOPs，两个速率分别对应约 26 和 52 μs。后面算出的读取时间约为 37 μs，速率选择会直接改变计算与读取的大小关系。同一矩阵单元采用不同累加精度时，计算速率不同，计算与读取之间的瓶颈也随之改变。
 
 由此可以把建模分成三步：先确定实际执行的操作，再把占用同一硬件单元的耗时相加，最后按数据依赖安排执行顺序。接下来先对最初的 Q 投影应用前两步，观察 batch 怎样改变对算力和带宽的需求比例。
 
--- before/manuscripts/05-算子与运行时.md
+++ after/manuscripts/05-算子与运行时.md
@@ -236,7 +236,7 @@
 
 **例 5-4：激活算子融合如何减少中间张量读写？** 分开执行时，先计算 $T=\operatorname{SiLU}(G)$，读取 G、写出 T，共 48 MiB；再计算 $Z=T\odot U$，读取 T、U，写出 Z，共 72 MiB。总读写量为 120 MiB。将两步融合，每次读取 G、U 的一片，在局部完成 SiLU 和乘法，写出 Z，只需 72 MiB。
 
-省下的 48 MiB 正是 T 的一次写回和一次读取。沿用上一节的带宽模型，取两种程序的有效带宽相同，时间比为 $72/120=0.6$，加速约 1.7 倍。实际激活链对照从约 14.2 μs 降到 7.7 μs，约快 1.8 倍。理论计算与实测结果都说明了同一个收益来源：省去完整 T 的写回和重读。[^exp2]
+省下的 48 MiB 正是 T 的一次写回和一次读取。沿用上一节的带宽模型，取两种程序的有效带宽相同，时间比为 $72/120=0.6$，加速约 1.7 倍。实际激活链对照从约 14.2 μs 降到 7.7 μs，加速比约为 1.8。理论计算与实测结果都说明了同一个收益来源：省去完整 T 的写回和重读。[^exp2]
 
 接下来将 Z 按固定尺度量化，使每个元素只占 1 byte。独立转换需读取 24 MiB 的 Z，写出 12 MiB 量化结果，增加 36 MiB。三步完全分开的总量为 156 MiB；融合前两步为 108 MiB；三步全部融合为 $24+24+12=60$ MiB。图 5-9 将必需的输入输出读写画在每条柱形的左侧，再依次加上两个中间量的读写。
 
@@ -254,7 +254,7 @@
 
 缓冲还决定两个阶段能否重叠执行。设搬运器把块 0 放入槽 A，计算单元随后读取 A。在 A 被读取期间，搬运器将块 1 放入槽 B；计算转向 B 后，A 便可装入块 2。这样，两个槽交替使用，使计算单元读取当前块时，搬运器可以写入下一块。
 
-注意，槽 A 的“数据已经搬完”与“数据已经用完”是两个时刻。搬运结束后，计算才开始读取；只有最后一次读取结束，槽 A 才能覆盖成下一个块。取每块搬运 2 μs、计算 3 μs，搬运器和计算单元独立工作。先看三个时段的槽位状态，再把它们放回完整时间线。
+注意，槽 A 的“数据已经搬完”与“数据已经用完”是两个时刻。搬运结束后，计算才开始读取；只有最后一次读取结束，才能将下一块数据写入槽 A，覆盖原有内容。取每块搬运 2 μs、计算 3 μs，搬运器和计算单元独立工作。先看三个时段的槽位状态，再把它们放回完整时间线。
 
 ![图 5-10 两个缓冲槽的交替使用](ch05/figure-5-buffer-slots.svg)
 
@@ -348,7 +348,7 @@
 
 **数学后端（`SDPBackend.MATH`）**按注意力公式组合矩阵乘、Softmax 等通用运算，保存完整的分数和概率中间矩阵。**FlashAttention 后端（`SDPBackend.FLASH_ATTENTION`）**采用前面介绍的分块与在线归约方法，避免保存完整的中间矩阵。实验分别指定这两个后端，比较它们完成同一次注意力计算的时间与新增显存。
 
-实验在 RTX PRO 6000 Blackwell 上进行，输入输出为 BF16，一次处理一个序列、一个注意力头，头维度为 128。序列长为 8192 token，其中 token 是模型处理的一个离散输入位置；采用因果注意力，即每个位置只使用自身及此前的位置。预热后，使用 CUDA Graph 将一组 GPU 操作预先记录并重复执行，以减少逐次由主机提交的干扰。按每次注意力计算统计，保存完整中间矩阵的数学实现约需 2.6 ms，采用分块与在线归约的 FlashAttention 实现约需 80 μs，后者约快 33 倍。[^exp3]
+实验在 RTX PRO 6000 Blackwell 上进行，输入输出为 BF16，一次处理一个序列、一个注意力头，头维度为 128。序列长为 8192 token，其中 token 是模型处理的一个离散输入位置；采用因果注意力，即每个位置只使用自身及此前的位置。预热后，使用 CUDA Graph 将一组 GPU 操作预先记录并重复执行，以减少逐次由主机提交的干扰。按每次注意力计算统计，保存完整中间矩阵的数学实现约需 2.6 ms，采用分块与在线归约的 FlashAttention 实现约需 80 μs，后者的加速比约为 33。[^exp3]
 
 再比较一次普通调用新增的显存峰值。输入 Q、K、V 预先驻留显存，计量范围为输出与临时工作区：数学实现约为 848 MiB，FlashAttention 实现约为 22 MiB。
 
@@ -595,7 +595,7 @@
 
 仍以投影后接激活为例，取 512 个 token 的特征向量，每 64 个 token 的向量组成一个数据块，共八块。设矩阵与向量资源独立、缓冲充足，每块投影约需 32.2 μs，激活约需 39.3 μs，主机启动每次 5 μs。等全部投影完成再开始激活，共需 $2\times5+8\times(32.2+39.3)\approx582$ μs。
 
-现在将计算分为八个投影任务和八个激活任务，每个任务增加 0.5 μs 调度时间、0.2 μs 完成通知时间。每块投影变为 32.9 μs，激活变为 40.0 μs。一次主机启动后，先完成第一块投影，随后投影与较慢的激活重叠，完成时间为 $5+32.9+8\times40.0\approx358$ μs，约快 1.6 倍。
+现在将计算分为八个投影任务和八个激活任务，每个任务增加 0.5 μs 调度时间、0.2 μs 完成通知时间。每块投影变为 32.9 μs，激活变为 40.0 μs。一次主机启动后，先完成第一块投影，随后投影与较慢的激活重叠，完成时间为 $5+32.9+8\times40.0\approx358$ μs，加速比约为 1.6。
 
 ![图 5-27 从等待全部投影到逐块开始激活](ch05/figure-5-15-persistent.svg)
 
@@ -621,7 +621,7 @@
 S_{\mathrm{request}}=\frac{1}{(1-f)+f/s}.
 $$
 
-若热点占 20%，速度提高到原来的两倍后，总时间变为 $0.8+0.2/2=0.9$，约快 1.11 倍。继续把热点加快到十倍，总时间为 0.82；即使热点耗时降到零，其他阶段仍需原总时间的 0.8，整体加速上限为 1.25 倍。
+若热点占 20%，速度提高到原来的两倍后，总时间变为 $0.8+0.2/2=0.9$，加速比约为 1.11。继续把热点加快到十倍，总时间为 0.82；即使热点耗时降到零，其他阶段仍需原总时间的 0.8，整体加速上限为 1.25 倍。
 
 这个关系就是 Amdahl 定律。它解释了优化收益逐渐缩小的原因：热点耗时越短，其他阶段在总时间中所占的比例就越高。因此，应先估算各部分最多能为请求节省多少时间，再决定优先优化哪里。
 
@@ -756,11 +756,11 @@
 
 [^exp3]: [实验 5-3](../experiments/ch05/05-03/README.md)。RTX 热缓存单头对照：2.64 ms／79.5 μs；新增分配峰值约 848／22.2 MiB，包括各后端分配的临时量。
 
-[^akg]: Zhao 等，2021，[AKG: Automatic Kernel Generation for Neural Processing Units using Polyhedral Transformations](../references/files/papers/akg-pldi21.pdf)。本章采用其编译架构、分块融合和存储管理机制，以 FFN 循环演示。
+[^akg]: Zhao 等，2021，[AKG: Automatic Kernel Generation for Neural Processing Units using Polyhedral Transformations](../references/files/papers/akg-pldi21.pdf)。
 
 [^korch]: [Korch 的编排案例](../case-studies/kernel-orchestration-and-quantization.md)：论文 V100／FP32 子图，三 kernel 为 38.7、24.2、28.2 μs，四 kernel 为 7.6、18.4、7.5、35.7 μs。
 
-[^schedule]: [TVM](../references/files/papers/tvm.pdf)、[TensorIR](../references/files/papers/tensorir.pdf)。正文采用 Halide／TVM 风格的调度概念；具体接口、变换方向和循环示例见配套材料。
+[^schedule]: [TVM](../references/files/papers/tvm.pdf)、[TensorIR](../references/files/papers/tensorir.pdf)。
 
 [^author]: [作者《MSRA 读博五年（一）》](../references/author-materials/2026-09-09/phd-summary.md)与[取材记录](../case-studies/author-context-and-design.md)。
 
--- before/manuscripts/06-超节点.md
+++ after/manuscripts/06-超节点.md
@@ -782,13 +782,13 @@
 
 [^dense]: 单卡完整 BF16 权重为 16,381,470,720 bytes，8193 位置 KV 为 1,208,107,008 bytes；TP=8 每卡分别为 2,048,223,232 与 151,013,376 bytes。表中 GB 为十进制，GiB／MiB 为二进制，合计先用精确值求和再取近似；2 GiB 包含激活与其余工作区预留。详见[Qwen3-8B 逐卡放置](../calculations/results/placement-qwen8-tp8-pp1-dp1.md)及其 JSON，读取固定官方配置与权重索引；状态保存范围为 history+tokens 个位置。
 
-[^models]: [具体模型的并行推算](../case-studies/model-parallelism.md)，[Qwen3-235B-A22B 配置](../references/outline-checks/2026-09-07/scaling-history/qwen3-235b-config.json)，[DeepSeek V4 报告](../references/files/papers/deepseek-v4.pdf)，[Kimi K3 报告](../references/files/papers/kimi-k3.pdf)表 1 与 §2.3。模型配置与版本分别保留，DeepSeek-V3 的 671B 作为历史对照。
+[^models]: [具体模型的并行推算](../case-studies/model-parallelism.md)，[Qwen3-235B-A22B 配置](../references/outline-checks/2026-09-07/scaling-history/qwen3-235b-config.json)，[DeepSeek V4 报告](../references/files/papers/deepseek-v4.pdf)，[Kimi K3 报告](../references/files/papers/kimi-k3.pdf)表 1 与 §2.3。DeepSeek-V3 的 671B 为历史对照模型。
 
 [^tp-experiment]: 八个正式配置通过 FP64 参考检查，配对 TP/SP 输出逐位相同；完整形状与验收见[实验 6-2：JAX TP/SP 实际 FFN 路径](../experiments/ch06/06-02/jax-tp-sp/README.md)。实验在 CPU 逻辑设备上执行。
 
 [^variants]: [MeshSlice 的矩阵形状与流水推算](../case-studies/mesh-shape-and-slicing.md)、[Shift Parallelism 的状态与额外驻留](../case-studies/parallel-switching-and-state.md)。MeshSlice 的部分重叠为预测，Arctic 的 SP 定义及动态切换边界按固定实现说明。
 
-[^pipeline]: [有限槽位与背压计算](../calculations/results/pipeline-finite-1-slots.md)、[基础 TP／PP 通信路径](../calculations/results/dense-comm-qwen8-tp8-pp1-t1.md)。声明的执行时序、缓冲安排和线上传输格式决定所计范围。
+[^pipeline]: [有限槽位与背压计算](../calculations/results/pipeline-finite-1-slots.md)、[基础 TP／PP 通信路径](../calculations/results/dense-comm-qwen8-tp8-pp1-t1.md)。
 
 [^ownership]: [Qwen235 所有权连接练习](../research/2026-infra-survey/parallel-moe-ownership/NOTES.md)及[独立核对结果](../research/2026-infra-survey/parallel-moe-ownership/results.json)。本例采用给定的 TP／EP 教学布局。
 
@@ -808,7 +808,7 @@
 
 [^opentallas]: [OpenTallas 的设计修正](../case-studies/opentallas.md)，案例 D；项目与作者关系、旧版本与教学输入在该文件披露。
 
-[^numa]: [PCIe 中转与 NUMA 放置](../case-studies/pcie-staging-and-numa.md)、[发送端附近缓冲的计算](../calculations/results/staging-local-grouped.md)。TCCL 采用作者材料与源码。
+[^numa]: [PCIe 中转与 NUMA 放置](../case-studies/pcie-staging-and-numa.md)、[发送端附近缓冲的计算](../calculations/results/staging-local-grouped.md)。
 
 [^physical]: [端口数量与互联介质分析](../research/draft11-outline-audit/report.md#radix)。端口与功率算例采用题设输入。
 
@@ -828,7 +828,7 @@
 
 [^cost]: [四请求无故障条件](../calculations/results/supernode-qwen3-8b-n4-d250-healthy.md)、[单请求 80 ms](../calculations/results/supernode-qwen3-8b-n1-d80-healthy.md)、[四请求长恢复](../calculations/results/supernode-qwen3-8b-n4-d250-long.md)、[32B 容量对照](../calculations/results/supernode-qwen3-32b-n4-d600-healthy.md)。服务时长、恢复、定价及静态轮转均为教学假设。
 
-[^continuous]: 贯穿模型与逐步计算见[模型说明](ch06/continuous-example.md)、[计算脚本](ch06/continuity_model.py)和[完整结果](ch06/continuity-model.json)。Qwen3-8B 形状取自已锁定配置。1 TB/s、100 TFLOP/s、0.20 ms 串行项及通信参数为教学输入；矩阵算子的时间取内存访问与计算时间的较大值，通信按层串行，上下文 KV 每步增长。归一化、逐元素与模型输入输出的处理集中为固定服务项。原 62／100／160 ms 的独立调度情景保留在[配套说明](ch06/continuous-example.md)，正文使用逐层推导得到的执行时间。
+[^continuous]: 贯穿模型与逐步计算见[模型说明](ch06/continuous-example.md)、[计算脚本](ch06/continuity_model.py)和[完整结果](ch06/continuity-model.json)。Qwen3-8B 形状取自已锁定配置。1 TB/s、100 TFLOP/s、0.20 ms 串行项及通信参数为教学输入；矩阵算子的时间取内存访问与计算时间的较大值，通信按层串行，上下文 KV 每步增长。归一化、逐元素与模型输入输出的处理集中为固定服务项。
 
 [^v41-case]: [DeepSeek V4.1 官方技术报告](../calculations/sources/deepseek-v4.1-flash/DeepSeek_V41_Tech_Report.pdf)，第 1、2、3 节与第 6 节；[跨章会话的固定条件与复算](../calculations/results/v41-throughline.json)。
 
--- before/manuscripts/07-数据中心网络.md
+++ after/manuscripts/07-数据中心网络.md
@@ -727,11 +727,11 @@
 >
 > 复算例题 7.6，画出连续环、分层归约、在途请求数不足、提前通信与出口退化的整步时间线。求分层通信恰好被 20 ms 计算覆盖时的数据最晚就绪时刻。再换成 36 层、每层两次 8 KiB 归约，分别考虑将带宽提高到原来的三倍，以及将每次启动开销降至 2 μs，解释两类负载的改进顺序为何不同。
 
-[^capacity]: 模型规模与状态大小的计算方法见[模型资源计算](../case-studies/model-resource-accounting.md)及[第七章扩写资料](../outlines/extensions/07-数据中心网络.md#detail-7.1)。16 bytes／参数是本章声明的混合精度 Adam 教学配置；万亿参数推理例子按每参数 0.5 byte 计算权重容量。
+[^capacity]: 模型规模与状态大小的计算方法见[模型资源计算](../case-studies/model-resource-accounting.md)及[第七章扩写资料](../outlines/extensions/07-数据中心网络.md#detail-7.1)。混合精度 Adam 算例按每参数 16 bytes 保存训练状态；万亿参数推理例子按每参数 0.5 byte 计算权重容量。
 
 [^ub]: 李博杰，[《Unified Bus 背后的思考》](../references/files/documents/ub-reflection.md)，2025-09-28。笔者在文中讨论早期设计的动机与不同方案；协议版本与产品实现见[UB 规范核对记录](../references/UB-ASCEND-NOTES.md)。
 
-[^gradient]: [两级梯度归约计算](../calculations/results/gradient-fp32-hierarchical-nic2.md)、[连续平坦环](../calculations/results/gradient-fp32-flat-contiguous-nic2.md)、[交错平坦环](../calculations/results/gradient-fp32-flat-interleaved-nic2.md)。固定模型形状，带宽与轮间屏障为教学输入。计算程序检查每个参与者的输入是否恰好参与一次求和；浮点加法顺序变化可能改变舍入结果。正文时间按本地 200 GB/s、每方向共享出口 40 GB/s、两 NIC 各 25 GB/s、每轮启动 2 μs 计算；各轮有屏障，未计归约、传播与其他任务干扰。
+[^gradient]: [两级梯度归约计算](../calculations/results/gradient-fp32-hierarchical-nic2.md)、[连续平坦环](../calculations/results/gradient-fp32-flat-contiguous-nic2.md)、[交错平坦环](../calculations/results/gradient-fp32-flat-interleaved-nic2.md)。固定模型形状，带宽与轮间屏障为教学输入。各轮以屏障同步，所列时间为无争用条件下的载荷传输与启动时间之和：本地 200 GB/s、每方向共享出口 40 GB/s、两 NIC 各 25 GB/s、每轮启动 2 μs。
 
 [^parallel]: [模型并行案例](../case-studies/model-parallelism.md)、[第六章正文](06-超节点.md)。本章 EP 算例使用 1024 个 token、每 token 八次分派、每份 8 KiB、跨边界比例二分之一，路由为教学构造。
 
@@ -761,7 +761,7 @@
 
 [^feedback]: [有限缓冲与反馈延迟](../calculations/results/feedback-queue-overflow.md)。反馈时刻设为 20 μs，反馈后的发送速率按题设调整。
 
-[^packets]: [两路径均延迟](../calculations/results/packet-reorder-balanced.md)、[路径偏斜](../calculations/results/packet-reorder-skewed.md)、[指定丢包恢复](../calculations/results/packet-reorder-loss.md)。数据只含载荷，不含协议头、ACK 或编码。报文采用固定载荷与轮转分配，恢复时刻由题设给定；8 KiB 后缀重发用于计算额外载荷。
+[^packets]: [两路径均延迟](../calculations/results/packet-reorder-balanced.md)、[路径偏斜](../calculations/results/packet-reorder-skewed.md)、[指定丢包恢复](../calculations/results/packet-reorder-loss.md)。字节数按应用载荷统计。报文采用固定载荷与轮转分配，恢复时刻由题设给定；8 KiB 后缀重发用于计算额外载荷。
 
 [^megascale]: Jiang 等，*MegaScale: Scaling Large Language Model Training to More Than 10,000 GPUs*，NSDI 2024；[正式论文](../references/proceedings/NSDI/2024/selected/nsdi24-jiang-ziheng.pdf)、[集合通信诊断](../case-studies/collective-paths-and-diagnosis.md)。
 
--- before/manuscripts/08-单实例推理.md
+++ after/manuscripts/08-单实例推理.md
@@ -15,7 +15,7 @@
 | 公共前缀长度 | 0 | 6144 token |
 | 完成时限 | 3 s | 3 s |
 
-首先计算实例能够同时保存多少条请求的 KV 缓存，再看增加批量能为每个输出 token 减少多少权重读取。随后引入分页、前缀共享、压缩、卸载和推测解码，分别分析它们节省了什么、增加了什么。章末将这些分析用于同一个问题：16 条长请求同时到达时，应选择哪种配置？各节还会结合已有实验解释相关机制，设备和运行条件见图注及资料说明。
+首先计算实例能够同时保存多少条请求的 KV 缓存，再看增加批量能为每个输出 token 减少多少权重读取。随后引入分页、前缀共享、压缩、卸载和推测解码，分别分析它们节省了什么、增加了什么。章末将这些分析用于同一个问题：16 条长请求同时到达时，应选择哪种配置？
 
 ## 8.1 推理请求的执行过程与资源需求
 
@@ -690,7 +690,7 @@
 
 [^schedule-survey]: [Orca 原文](../references/files/papers/orca.pdf)与[分块计算和版本说明](../case-studies/chunking-and-state-transfer.md)。
 
-[^schedule]: [固定批次](../calculations/results/iteration-batching-fixed.md)、[连续批处理](../calculations/results/iteration-batching-continuous.md)、[分块策略](../calculations/results/iteration-batching-chunked.md)，显式教学成本，最终输出已生成但尚未写入 KV 的状态及容量计算范围见各文件。
+[^schedule]: [固定批次](../calculations/results/iteration-batching-fixed.md)、[连续批处理](../calculations/results/iteration-batching-continuous.md)、[分块策略](../calculations/results/iteration-batching-chunked.md)。三个算例均采用给定的执行成本；最后一个输出 token 在请求结束时尚未写入 KV。
 
 [^chunk]: [逐块计算和封存测量](../calculations/results/chunk-history-book.md)。11 次请求、每次 16 块，首末块时间中位数 25.30、35.07 ms；CUDA event 区间包括完整模型路径和可能的主机间隙，不含外部 logits／采样。各块按原始顺序执行，输入位置与内容逐块变化。
 
@@ -728,7 +728,7 @@
 
 [^spec-measure]: [DFlash 同执行器对照](../experiments/ch08/08-05/README.md)及[逐请求阶段观测](../experiments/ch08/08-05/kernel-trace/README.md)。实验在低并发下，按固定顺序执行给定短题集。并发 1 通过短题的普通、K7、K15 时间中位数分别为 90.02、30.58、29.43 ms；配对输出共 32 对。
 
-[^service]: [服务与能量原始记录](../experiments/ch08/08-09/README.md)。同一设备、八道题、七条件各三个 16 请求窗口；同题跨条件输出一致，42 个错误来自同一道题。吞吐表为各窗口指标中位数。每窗口 16 请求的 nearest-rank p95 即最大值。NVML 与 RAPL 组件计数窗口略宽于请求窗口，其他服务仍在运行；完整窗口能量保留在原始记录。
+[^service]: [服务与能量原始记录](../experiments/ch08/08-09/README.md)。同一设备、八道题、七条件各三个 16 请求窗口；同题跨条件输出一致，42 个错误来自同一道题。吞吐表为各窗口指标中位数。每窗口 16 请求的 nearest-rank p95 即最大值。NVML 与 RAPL 组件计数窗口略宽于请求窗口，其他服务仍在运行。
 
 [^task]: [生成实验与 Agent 运行记录](../experiments/ch08/08-06/README.md)。Agent 记录采用非流式调用；阶段时间图采用题设参数。
 
--- before/manuscripts/09-分布式推理.md
+++ after/manuscripts/09-分布式推理.md
@@ -629,21 +629,21 @@
 
 [^capacity]: [DeepSeek V4-Flash 与 Kimi K2 配置记录](../experiments/ch09/09-05/README.md)。表中并发上限取自部署配置。
 
-[^af]: [跨机执行与框架演进](../case-studies/framework-evolution.md)及[AF 扩写依据](../outlines/extensions/09-分布式推理.md#detail-9.3.4)，保存 MegaScale-Infer 与实验性 AFD 的版本条件。
+[^af]: [跨机执行与框架演进](../case-studies/framework-evolution.md)及[AF 扩写依据](../outlines/extensions/09-分布式推理.md#detail-9.3.4)。
 
 [^moe]: [MoE Serving Tax、CRAFT 与启动研究笔记](../case-studies/moe-and-startup.md)。
 
 [^replica]: [专家副本回本计算](../calculations/results/replica-payback-book.md)。算例固定路由、有效传输速率与逐卡额外可用空间，按串行复制计算准备时间。 准备为10.60464608 ms，每批节省0.402784256 ms；正文显示值经舍入，27批边界按原值求解。
 
-[^expert-exp]: [专家执行与后端已有记录](../experiments/ch09/09-06/README.md)。
-
-[^cache]: [缓存层次、路径与路由研究](../case-studies/cache-tiers-and-routing.md)。本文按已查阅实现区分实例私有 host 与配置共享的存储层。
+[^expert-exp]: [专家执行与后端实验记录](../experiments/ch09/09-06/README.md)。
+
+[^cache]: [缓存层次、路径与路由研究](../case-studies/cache-tiers-and-routing.md)。
 
 [^shared]: [共享 CPU KV 池容量对照](../experiments/ch09/09-07/shared-kv/README.md)与[真实上下文保留后的 Agent 回放](../experiments/ch09/09-07/history-preserved/README.md)。容量对照采用受控查找任务；真实 Agent 回放中出现了提前结束及任务质量下降。
 
-[^v4]: [DeepSeek V4 技术报告](../references/files/papers/deepseek-v4.pdf)的状态持久化与生成服务内容，以及[本章的阅读笔记](../outlines/extensions/09-分布式推理.md#detail-9.6.3)。模型状态形状和恢复机制依固定版本解释。
-
-[^restart]: [重启页读取与实际复用核算](../calculations/results/cache-restart-book.md)，读取量为 144 MiB，有效复用量为 141.75 MiB。 原始有效复用为141.75 MiB，正文约142 MiB；1024／1008位置与64／63页保留精确整数。
+[^v4]: [DeepSeek V4 技术报告](../references/files/papers/deepseek-v4.pdf)的状态持久化与生成服务内容，以及[本章的阅读笔记](../outlines/extensions/09-分布式推理.md#detail-9.6.3)。
+
+[^restart]: [重启页读取与实际复用核算](../calculations/results/cache-restart-book.md)，读取量为 144 MiB，有效复用量为 141.75 MiB。读取 64 页、共 1024 个位置，其中 63 页、共 1008 个位置得到复用。
 
 [^fault]: [截断页的预取策略对照](../experiments/ch09/09-08/prefetch-policy/README.md)，实验观察窗口为 60 秒。
 
@@ -661,7 +661,7 @@
 
 [^migration]: [串行迁移的时间计算](../calculations/results/reconfiguration-declared-serial.md)，按载荷、设定带宽和串行准备时间计算迁移耗时。
 
-[^cost-model]: 例 9.7 的容量、共享通道、启动时间、排空目标和成本，以及 SLO 通过比例、9.4.3 的重叠时间均为教材假设，用于推导条件变化。单位成本用完整小时成本除以（3600×有效请求率）。迁移对比沿用原载荷，另给5／25 GB/s带宽与9秒串行准备，区别于原结果的100秒声明网络时间。
+[^cost-model]: 例 9.7 的容量、共享通道、启动时间、排空目标和成本，以及 SLO 通过比例、9.4.3 的重叠时间均为教材假设，用于推导条件变化。单位成本用完整小时成本除以（3600×有效请求率）。迁移算例分别采用 5／25 GB/s 带宽，串行准备时间设为 9 秒。
 
 [^v41-case]: [DeepSeek V4.1 官方技术报告](../calculations/sources/deepseek-v4.1-flash/DeepSeek_V41_Tech_Report.pdf)，第 1、2、3 节与第 6 节；[跨章会话的固定条件与复算](../calculations/results/v41-throughline.json)。
 
--- before/manuscripts/11-资源调度与运行环境.md
+++ after/manuscripts/11-资源调度与运行环境.md
@@ -628,7 +628,7 @@
 
 [^platform]: [调度、模型路由与云端环境研究](../case-studies/platform-routing.md)，包含 ASI、RLBoost、DistRS、SpecBox。ASI 研究覆盖六个月、155,410 张 GPU，其分配比例度量设备归属。计费公式采用题设价格。
 
-[^e2b]: [E2B 固定架构](../references/outline-checks/2026-09-07/platform-routing/e2b-architecture.md)、[暂停与快照接口行为核对](../experiments/ch11/11-02/README.md)。实现采用 Firecracker；保存文件／内存与重建连接分别说明。
+[^e2b]: [E2B 固定架构](../references/outline-checks/2026-09-07/platform-routing/e2b-architecture.md)、[暂停与快照接口行为核对](../experiments/ch11/11-02/README.md)。E2B 使用 Firecracker。
 
 [^ub]: [UB 操作系统参考设计](../references/files/specs/UB-Software-Reference-Design-for-OS-2.0-zh.pdf)中的设备虚拟化与交付路径，作为 QEMU／VFIO 的实现例证；这里描述支持设备直通的虚拟机配置。
 
@@ -648,7 +648,7 @@
 
 [^routing]: [routing-cost 固定结果](../calculations/results/routing-cost-book.md)、[交点结果](../calculations/results/routing-cost-crossover.md)及[完整教学推导](../case-studies/routing-cost-and-completion.md)。计算采用条件独立的质量概率，成功任务数按给定概率计算。
 
-[^purchase]: 原计算精确时间为 25.317 ms，其中 decode 15.918 ms、其余 9.399 ms；正文按用途取约一位小数。此处将逐层固定开销和周期全量重建设为教学输入，详见[持续 Agent 比较基线](../experiments/ch13/13-06/single-agent-serving/COMPARISON-RESULTS.md)、[优化审计](../experiments/ch13/13-06/single-agent-serving/OPTIMIZATION-AUDIT.md)、[时间归因与敏感性](../experiments/ch13/13-06/single-agent-serving/AUDIT-RESULTS.md)。
+[^purchase]: 计算结果为 25.317 ms，其中 decode 为 15.918 ms，其余阶段共 9.399 ms。此处将逐层固定开销和周期全量重建设为教学输入，详见[持续 Agent 比较基线](../experiments/ch13/13-06/single-agent-serving/COMPARISON-RESULTS.md)、[优化审计](../experiments/ch13/13-06/single-agent-serving/OPTIMIZATION-AUDIT.md)、[时间归因与敏感性](../experiments/ch13/13-06/single-agent-serving/AUDIT-RESULTS.md)。
 
 [^records]: [任务记录与证据范围](../case-studies/evaluation-and-agent-records.md)、[文件队列故障注入](../experiments/ch11/11-09/collector-faults/README.md)、[任务退出状态记录](../experiments/ch11/11-09/exit-status/README.md)。这些记录分别用于说明结果持久化、故障诊断和成本核算。
 
--- before/manuscripts/12-端边云协同.md
+++ after/manuscripts/12-端边云协同.md
@@ -97,7 +97,7 @@
 p_i=\max(r_i,p_{i-1}+\tau).
 $$
 
-这里取最大值，是因为播放器既要等待“本块到达”，也要等待“上一块播完”。当第一项更大时，差额就是新增停顿。把初始缓冲从 40 ms 增至 45 ms，第三块的计划播放时刻也推迟到 123 ms，恰好吸收晚到的 5 ms；代价是首段同样晚开始 5 ms。增加初始缓冲会延长首次播放前的等待，但也能吸收更大的到达时间波动，使后续播放更连续。
+这里取最大值，是因为播放器既要等待“本块到达”，也要等待“上一块播完”。当第一项更大时，差额就是新增停顿。把初始缓冲从 40 ms 增至 45 ms，第三块的计划播放时刻也推迟到 123 ms，恰好避免这 5 ms 的晚到造成播放中断；代价是首段同样晚开始 5 ms。增加初始缓冲会延长首次播放前的等待，但也能容忍更大的到达时间波动，使后续播放更连续。
 
 长期传输速率不足会产生另一种停顿。为便于计算，取 16 kHz、单声道、16-bit PCM，播放所需的数据速率为 256 kbit/s，20 ms 对应 640 bytes。若网络长期只能以 130 kbit/s 传输音频，缓冲每秒净减少 126 kbit。初始缓冲保存的 60 ms 音频包含 15,360 bits，约 $15360/(256000-130000)\approx0.12$ 秒便会耗尽。
 
@@ -107,7 +107,7 @@
 
 *图 12-7：缓冲量等于初始数据量加累计接收量，再减累计播放量。PCM 播放需要 256 kbit/s，接收只有 130 kbit/s，两条直线的下降速率均为 126 kbit/s。初始缓冲分别包含 60 ms 和 120 ms 音频，约在 0.12 秒和 0.24 秒耗尽；图画到各自第一次耗尽为止。*
 
-增大缓冲只会按比例延后耗尽；提高传输速率或降低音频码率，才能让收到的数据足够播放。短时抖动可以靠缓冲吸收，长期传输速率不足则需要增加带宽或减少数据量。
+增大缓冲只会按比例延后耗尽；提高传输速率或降低音频码率，才能让收到的数据足够播放。缓冲可以减轻短时延迟波动对播放的影响，长期传输速率不足则需要增加带宽或减少数据量。
 
 ### 12.1.3 Computer Use：通过界面操作完成任务
 
@@ -173,7 +173,7 @@
 
 *图 12-10：端侧编码的执行路径。预处理和视觉编码移到终端，跨网络的对象变为最终投影与三组 DeepStack 的完整 BF16 特征，共 8.192 MB；远端接收后仍按相同方式接入语言模型。相同上行的特征发送时间为 10.24 秒，本地编码、序列化和转换时间另计。图中数值采用下文的 Qwen3-VL-4B 固定配置。*
 
-**例：上传视觉特征为何可能比上传原图更慢？** 沿用 0.8 MB 截图与 6.4 Mbit/s 上行。视觉编码采用 Qwen3-VL-4B 的固定配置：预处理后 $640\times640$，图像块合并后得到 400 个视觉位置，即用来表示图片内容的 400 个特征向量；这里的“位置”不是文本中的词元，也不是原图的单个像素。最终投影的每个特征向量有 2560 个分量，另有三组同宽 DeepStack 特征。DeepStack 将视觉编码器不同深度的特征送入语言模型不同层，因而要一起传送这些分支输出。BF16 是每个数占 2 字节的浮点格式。每个位置共需保存 $4\times2560=10240$ 个 BF16 数，完整输出为
+**例：上传视觉特征为何可能比上传原图更慢？** 沿用 0.8 MB 截图与 6.4 Mbit/s 上行。视觉编码采用 Qwen3-VL-4B 的固定配置：预处理后 $640\times640$，图像块合并后得到 400 个视觉位置，即用来表示图片内容的 400 个特征向量；这里的“位置”不是文本中的 token，也不是原图的单个像素。最终投影的每个特征向量有 2560 个分量，另有三组同宽 DeepStack 特征。DeepStack 将视觉编码器不同深度的特征送入语言模型不同层，因而要一起传送这些分支输出。BF16 是每个数占 2 字节的浮点格式。每个位置共需保存 $4\times2560=10240$ 个 BF16 数，完整输出为
 
 $$
 S_E=400\times10240\times2=8\,192\,000\ \mathrm{bytes}
@@ -196,13 +196,13 @@
 
 链路变快后，发送特征比发送图片多花的时间会减少。在 25 GB/s 的链路上，图片与特征的发送时间相差约 0.30 ms。于是端侧编码要获益，只需在编码和排队上净省超过这 0.30 ms；在原有的慢速上行链路上，则要省出超过 9 秒。所以，决定编码器放在哪里时，慢链路上首先要比较传输量，快链路上则更需要比较编码和排队时间。
 
-以上比较假定每张截图都重新编码。对多轮任务，还要检查图片是否重复出现，因为缓存可以进一步减少编码次数。相同图片再次出现时，缓存命中省掉一次编码；界面内容改变后则重新编码。在一组使用相同尺寸图片的 CPU 实验中，再次输入同一张图片时可以命中缓存，修改图片内容后则需要重新编码。[^ec-test] 计算多轮任务的编码总时间时，可以用单次编码时间乘以缓存未命中的次数。这样就能把前面轮次留下的缓存也计入比较。
+多轮任务中，重复出现的图片可以复用编码缓存，进一步减少编码次数。相同图片再次出现时，缓存命中省掉一次编码；界面内容改变后则重新编码。在一组使用相同尺寸图片的 CPU 实验中，再次输入同一张图片时可以命中缓存，修改图片内容后则需要重新编码。[^ec-test] 计算多轮任务的编码总时间时，可以用单次编码时间乘以缓存未命中的次数。这样就能把前面轮次留下的缓存也计入比较。
 
 视觉特征只能省去视觉编码；如果希望复用语言模型已经算出的结果，就要保存注意力的上下文键和值（KV）。编码缓存（encoder cache，EC）保存视觉编码器的输出；KV 则保存语言模型处理前缀时逐层产生的注意力状态。该配置每个语言位置需要 144 KiB 逻辑 KV，400 个视觉位置约为 56.3 MiB。图片、约 7.8 MiB 的视觉特征、约 56.3 MiB 的视觉位置 KV，分别对应不同的重算起点。缓存越接近模型的最终输出，再次使用时省下的计算越多，需要传送的数据也可能越大。下一节就比较传送一次状态要花多久，以及后续使用这些状态能省多少时间。
 
 ![三种保存对象对应三个恢复计算的起点](ch12/figure-12-cache-restart.svg)
 
-*图 12-12：三种保存对象对应三个恢复计算的起点。EC 省去视觉编码；匹配模型权重、前缀与位置的 KV 进一步省去已处理前缀的语言计算。容量对应正文固定视觉配置。*
+*图 12-12：三种保存对象对应三个恢复计算的起点。EC 省去视觉编码；匹配模型权重、前缀与位置的 KV 进一步省去已处理前缀的语言模型计算。容量对应正文固定视觉配置。*
 
 ### 12.2.3 会话迁移的准备、复用与恢复
 
@@ -276,7 +276,7 @@
 
 ### 12.3.2 窗口、反馈与有效吞吐
 
-复用连接省去了建链等待，连续发送数据还需要足够的发送窗口。连接建立以后，发送方也必须限制在途数据。拥塞窗口限制已经发送但尚未确认的数据量；接收流量控制根据接收端的缓冲空间，限制发送方还能发送多少数据。确认消息（ACK）告知发送方哪些数据已收到。ACK 到达后，发送方可以继续发送；接收端从缓冲区读取数据后，也会通知发送方新增的可用空间。达到发送上限时，即使链路空闲，发送方仍要等待这些通知。
+复用连接省去了建链等待，连续发送数据还需要足够的发送窗口。连接建立以后，发送方也必须限制在途数据。拥塞窗口限制已经发送但尚未确认的数据量；接收端流量控制根据接收端的缓冲空间，限制发送方还能发送多少数据。确认消息（ACK）告知发送方哪些数据已收到。ACK 到达后，发送方可以继续发送；接收端从缓冲区读取数据后，也会通知发送方新增的可用空间。达到发送上限时，即使链路空闲，发送方仍要等待这些通知。
 
 在途量是已经发出、尚未得到确认的数据字节数。若允许在途量为 $W$，往返时间为 $R$，每个往返周期最多传送约一个窗口的数据，因此
 
@@ -302,7 +302,7 @@
 
 NewReno 是根据丢包与确认调整拥塞窗口的算法，CUBIC 在拥塞避免阶段按时间的立方函数调节窗口；慢启动是连接初期随确认快速增加窗口的阶段。在相同的数据填充方式与 NewReno 配置下，将立即 ACK 改为每两包确认或最多等待 10 ms，实际传输的数据从约 39.6 MB 降至 38.2 MB，完整请求却从约 14.52 秒增至 14.56 秒，晚约 43 ms。[^ack] 这组对照揭示了反馈的双重作用：少发确认减少传输时间，确认晚到延长窗口等待。最终能否更快完成，要比较省下的传输时间与增加的等待时间。
 
-ACK 决定发送方何时能够继续使用窗口发送数据，拥塞控制则决定窗口本身如何变化。比较拥塞控制算法时，应先找出请求经历了哪个阶段。相同路径的大图比较中，NewReno 与 CUBIC 完整响应均约为 14.5 秒；窗口轨迹显示 CUBIC 一直在慢启动。[^ack] 本次请求在进入立方拥塞避免阶段之前就已结束，因此观察到的是启动阶段的行为。
+ACK 决定发送方何时能够继续使用窗口发送数据，拥塞控制则决定窗口本身如何变化。相同路径的大图比较中，NewReno 与 CUBIC 完整响应均约为 14.5 秒；窗口轨迹显示 CUBIC 一直在慢启动。[^ack] 本次请求在进入立方拥塞避免阶段之前就已结束，因此观察到的是启动阶段的行为。
 
 ### 12.3.3 多流传输与优先级
 
@@ -401,7 +401,7 @@
 
 保持数据与恢复轨迹不变，将端到端 ACK 数减半，能释放约 $15400\times134\ \mathrm{\mu s}\approx2.1$ 秒空口。上一节已经说明，反馈延迟又会增加窗口停等。因此，需要重新计算各包的发送与确认时刻：减少 ACK 腾出了空口时间，但等待确认又可能推迟后续发送。两者共同决定完整请求何时结束。
 
-TACK 是减少无线确认开销的一种反馈机制。它将日常累计确认与需要即时反馈的事件分开：前者通过合并来减少交换次数，后者及时返回，使发送方尽快继续发送或重传丢失的数据。[^wireless-survey] 这种分工同时减少确认报文的发送开销和等待反馈的时间。
+TACK 是减少无线确认开销的一种反馈机制。它将常规的累积确认与需要即时反馈的事件分开：前者通过合并来减少交换次数，后者及时返回，使发送方尽快继续发送或重传丢失的数据。[^wireless-survey] 这种分工同时减少确认报文的发送开销和等待反馈的时间。
 
 ### 12.4.2 业务时限、过期数据与取消
 
@@ -485,7 +485,7 @@
 
 云端每轮比附近工作站少算 0.7 秒，二十轮省 14 秒，准备再省 2 秒；但上传每轮多 0.92 秒，传播多 0.18 秒，二十轮多等 22 秒。合计下来，云端反而慢 6 秒。把时间差拆开，便能看到更快的模型怎样因每轮通信而失去优势。
 
-图 12-26 至图 12-28 将各项差额放回完整任务。云端的绿色计算段更短，蓝色上传段却更长；应比较整条横条的终点，而不是只看模型计算那一段。
+图 12-26 至图 12-28 将各项差额放回完整任务。云端的绿色计算段更短，蓝色上传段却更长；两段与其余工作依次累加，决定横条终点所示的任务完成时间。
 
 ![不同执行设备的时间组成](ch12/figure-12-14-budgets.svg)
 
@@ -559,7 +559,7 @@
 
 ### 12.5.4 资源条件变化后的系统调整
 
-将前文的硬件与执行方式比较用于完整任务。设一次固定轨迹中，模型执行合计 8 秒，工具与网络等不可重叠部分合计 2 秒。即使模型整体加速 10 倍，任务也只能从 10 秒降至 2.8 秒，约快 3.57 倍；[^core-calculation]模型耗时趋近于零时，下界仍是 2 秒。
+将前文的硬件与执行方式比较用于完整任务。设一次固定轨迹中，模型执行合计 8 秒，工具与网络等不可重叠部分合计 2 秒。即使模型整体加速 10 倍，任务也只能从 10 秒降至 2.8 秒，加速比约为 3.57；[^core-calculation]模型耗时趋近于零时，下界仍是 2 秒。
 
 要继续缩短剩余两秒，可以改变系统组织。把执行移到本地可减少网络往返，保留环境可减少创建等待；相应增加的本地设备与驻留空间，则进入新的成本预算。前面的部署算例已经给出了比较方法：先求满足质量与期限的方案，再比较每个完成任务的总成本。
 
@@ -651,9 +651,9 @@
 
 (d) 对十轮任务，设第八轮已执行完时断连，恢复 2 秒。分别考虑两种恢复情况：已保留前七轮的进度，只需重做第八轮；前八轮的进度全部丢失，需要全部重做。计算两种情况下完成剩余工作所需的时间，以及整项任务的总耗时。这次断连发生在远程服务路径，端侧继续正常执行；恢复成本包含在基础费率中。恢复期间维持连接并保存服务状态，初始化只做一次；成本按实际执行轮次及发送字节计收，按原上传单价重新判断最低成本的可行方案。
 
-[^raw]: [RAW 图片精修案例与条件预算](../case-studies/raw-retouching.md)。RAW／DNG 与信息保留由归档的华为专业模式及 Adobe 材料支持；本章 30 MB／5 MB／0.3 秒为题设值。
-
-[^stream]: 分三块的两种计算分别为 12.8 s 与 12.36 s，正文以约 0.4 s 差额解释重叠收益；额外预览另增编码与传输。来源：[整图屏障](../calculations/results/image-stream-whole-image-without-preview.md)、[独立分块](../calculations/results/image-stream-independent-blocks-without-preview.md)及[额外预览](../calculations/results/image-stream-independent-blocks-with-preview.md)。各自采用对应的依赖与编码条件。
+[^raw]: [RAW 图片精修案例与条件预算](../case-studies/raw-retouching.md)。算例设原图 30 MB、成片 5 MB、图像处理耗时 0.3 秒。
+
+[^stream]: 分三块的两种计算分别为 12.8 s 与 12.36 s，正文以约 0.4 s 差额解释重叠收益；额外预览另增编码与传输。来源：[整图屏障](../calculations/results/image-stream-whole-image-without-preview.md)、[独立分块](../calculations/results/image-stream-independent-blocks-without-preview.md)及[额外预览](../calculations/results/image-stream-independent-blocks-with-preview.md)。
 
 [^audio]: [音频块时序计算](../calculations/results/audio-timing-base.md)，输入帧与输出音频块一一对应是该模型的简化；该简化用于说明逐块处理与按序播放的依赖。
 
@@ -671,7 +671,7 @@
 
 [^closed]: 精确分包为上传 25,685 包、下载 4,281 包；共 29,966 个数据包和同数 ACK，线上 39,554,832 bytes，完整响应 14.36339008 s。正文用约三万包、39.6 MB 和 14.4 s 说明量级。来源：[完整图片参考闭环](../calculations/results/closed-loop-book-30mb-5mb.md)。包长、立即 ACK、恢复与数值量化是明确输入，该结果用于解释逐包反馈如何增加请求时间。
 
-[^ack]: 同一填充条件下，立即 ACK 为 39,555,120 bytes／14.52103724 s，聚合 ACK 为 38,177,328 bytes／14.564459796 s，差约 1.38 MB 和 43.4 ms。14.36339008 s 属于另一无填充分支；本节单因素比较使用这里具有相同填充条件的两组结果。来源：[立即 ACK 的 NewReno 对照](../calculations/results/controller-loop-book-newreno.md)、[聚合 ACK 对照](../calculations/results/ack-policy-book-newreno.md)及[CUBIC 控制阶段记录](../calculations/results/controller-loop-book-cubic_hystart.md)。完整逐包日志保留在相应 JSON。
+[^ack]: 同一填充条件下，立即 ACK 为 39,555,120 bytes／14.52103724 s，聚合 ACK 为 38,177,328 bytes／14.564459796 s，差约 1.38 MB 和 43.4 ms。来源：[立即 ACK 的 NewReno 对照](../calculations/results/controller-loop-book-newreno.md)、[聚合 ACK 对照](../calculations/results/ack-policy-book-newreno.md)及[CUBIC 控制阶段记录](../calculations/results/controller-loop-book-cubic_hystart.md)。
 
 [^media]: [FIFO](../calculations/results/shared-media-schedule-fifo.md)与[音频优先](../calculations/results/shared-media-schedule-priority.md)；固定发送记录的[整体有序](../calculations/results/shared-media-hol-connection.md)与[逐流交付](../calculations/results/shared-media-hol-per_stream.md)。分别隔离调度和交付依赖。
 
@@ -689,7 +689,7 @@
 
 [^queqiao]: [固定归档的路径记录](../references/author-context/queqiao-168ff4b/PATH-CHARACTER-DC-20260826.md)与[设计记录](../references/author-context/queqiao-168ff4b/DESIGN-DC-PROFILE.md)，来自提交 `168ff4b`。文件、调优设置和计时起止点均采用原记录中的定义。
 
-[^queqiao-calc]: 新建连接的直接／Queqiao p50 为 1185.3／301.6 ms，调优保持连接为 240.9／236.5 ms；两个中位数之比分别约 3.93／1.019。文档另给逐轮配对比中位数 3.96／1.03，统计量不同。正文将后一组写成均约 0.24 s。来源：[Queqiao 同条件统计](../calculations/results/queqiao-records-conditions.md)。
+[^queqiao-calc]: 新建连接的直接／Queqiao p50 为 1185.3／301.6 ms，调优保持连接为 240.9／236.5 ms；两个中位数之比分别约 3.93／1.019。逐轮计算耗时比再取中位数，结果分别为 3.96／1.03。来源：[Queqiao 同条件统计](../calculations/results/queqiao-records-conditions.md)。
 
 [^physical]: 8 次完整接收测试中，收到的 PCM 与播放回调读取的 PCM 均逐位一致，播放回调累计有 360–1440 ms 无法取得足够音频样本；实验使用静音输出设备，记录包含接收与播放回调事件。另有 3 次请求在首个播放回调之前取消，接收字节、连接关闭事件和远端 EOF 记录了取消过程。完整四轮数据见[配套证据说明](ch12/evidence-notes.md)。来源：[显式物理接口绑定的音频实验](../experiments/ch12/12-07/audio-physical/README.md)，固定提交 `496ca627`，基线含 socket 绑定适配。
 
