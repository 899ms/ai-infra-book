# Application write size across image,ASR and Computer Use

Preparation only. Run after receive-windows completes and is verified,with no concurrent own benchmark container. Use the same actual source/result pairs and measured service-time replays. Keep TCP_NODELAY=1/proto6 explicit and verified,and receive settings at defaults on both endpoints. QUIC initial connection/stream receive credit stays1MiB. No new model processing,progressive RAW processing,streaming ASR or browser task is introduced.

Compare whole body,16KiB and64KiB application writes. All source bytes are available before upload; the server begins its recorded service only after the full original upload. The result becomes available after that service wait and is then written in the same configured size. For ASR/action JSON,the result is smaller than16KiB and consequently remains one write in all modes. The larger image changes write sizes on both upload and download; stage timestamps and separate chunk ledgers distinguish the directions. This factor is application write/backpressure granularity,not HTTP chunked transfer encoding,TLS record size,QUIC packet size or a source-generation release policy. TTS's separate294-release/64KiB/whole availability experiment covers a different explicitly documented chunking boundary.

HTTP1.1 retains Content-Length and writes each piece with writer.drain; HTTP3 enqueues each piece,transmits and yields the event loop. Whole-body mode uses exactly the same helper with one piece. Retain each application offset/length,enqueue and post-drain/yield timestamps. No artificial per-piece delay is inserted. Both write overhead and backpressure are part of the measured policy. Qlogs and complete byte validation must not be used to infer one application write equals one wire packet.

Three trials use position-balanced whole/16k/64k,16k/64k/whole,64k/whole/16k orders. Nine fresh Docker subruns per workload,netem80ms RTT/20Mbit/s/0.1% random loss,2CPU/2GiB,network-none/NET_ADMIN. Each protocol gets one warmup and one fresh reused connection carrying two formal image/ASR requests or the full8-round Computer trace.54 image+54 ASR+162 Computer=270 exchanges (216 formal54 warmups). Common120s request deadline and20s handshake. Preserve all samples,failures,exact chunks/byte identities,source service waits,usable result validation,TLS/ALPN/reuse/socket/flow-control settings and container cleanup. The completed result must not infer acoustic onset or safe action retries from these transfers.
