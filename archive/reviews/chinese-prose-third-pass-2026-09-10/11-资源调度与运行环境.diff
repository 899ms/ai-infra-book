--- before/11-资源调度与运行环境.md
+++ manuscripts/11-资源调度与运行环境.md
@@ -2,7 +2,7 @@
 
 代码修复服务收到用户提交的问题后，先让模型生成修改，再让工具改文件、执行测试，把测试结果交回模型，继续下一轮，直到得到通过测试的补丁。这项需要交付最终结果的工作称为**任务**；一次模型请求或一次工具执行只是其中的一个步骤。
 
-**控制器**保存任务记录并决定下一步调用什么，**执行器**实际运行工具，**运行环境**承载工具所需的文件、进程和内存。模型调用使用推理服务，工具执行使用 CPU，环境中的进程和缓存占用内存。沿着每次调用、返回和等待，可以分别计算这些资源在什么时候被使用、使用多久。
+**控制器**保存任务记录并决定下一步调用什么，**执行器**实际运行工具，**运行环境**承载工具所需的文件、进程和内存。模型调用使用推理服务，工具执行使用 CPU，环境中的进程和缓存占用内存。沿着每次调用、返回和等待的过程，可以分别计算这些资源在什么时候被使用、使用多久。
 
 前两章讨论了推理和训练服务的构建。本章研究怎样把模型服务、工具环境和共享资源组织成一个任务运行系统。我们从一个平台设计题开始，逐步讨论何时创建和释放环境、如何安排任务、如何选择模型，以及如何从失败中恢复。
 
@@ -62,7 +62,7 @@
 \text{资源需求率}=\text{任务到达率}\times\text{每任务的平均资源需求}.
 $$
 
-每项任务累计使用 3 秒 CPU 时间，所以每秒到达的任务合计需要 30 秒 CPU 时间，平均有 30 个 CPU 核在执行工具；每项任务的累计内存占用为 60 GiB·秒，所以平台的环境平均占用 600 GiB 内存；每项有三次 9 秒模型调用，所以平均有 $10\times3\times9=270$ 次模型调用正在处理。
+每项任务累计使用 3 秒 CPU 时间，所以每秒到达的任务合计需要 30 秒 CPU 时间，平均有 30 个 CPU 核在执行工具；每项任务的累计内存占用为 60 GiB·秒，所以平台的环境平均占用 600 GiB 内存；每项有三次 9 秒模型调用，所以平均有 $10\times3\times9=270$ 次模型调用正在执行。
 
 **例 11-1：代码修复平台为何先受环境内存容量限制？** 将需求除以章首给定的容量，CPU 占用为 $30/40=75\%$，模型调用的并发容量使用率为 $270/300=90\%$，内存占用为 $600/640\approx94\%$。内存只剩 40 GiB，可以再容纳 20 个环境；CPU 还剩 10 核。此时增加 CPU 核数，仍然无法解决环境内存不足的问题。
 
@@ -91,7 +91,7 @@
 
 ### 11.1.3 任务完成条件与必须保留的状态
 
-减少等待期间的内存占用，意味着要释放环境中的一部分状态。释放之前，必须先弄清丢失这些状态会让哪些工作重做。在图 11-1 的三轮任务中，前两轮结束时，已经花了 20 秒，其中工具累计使用的 CPU 时间只有 2 秒。如果丢失工作目录后必须从头执行，就要重新花费这 20 秒，而不仅是重做 2 秒工具计算。能保留哪些状态，决定了故障后哪些阶段需要重来。
+减少等待期间的内存占用，意味着要释放环境中的一部分状态。释放之前，必须先弄清丢失这些状态会让哪些工作重做。在图 11-1 的三轮任务中，前两轮结束时，已经过去了 20 秒，其中工具累计使用的 CPU 时间只有 2 秒。如果丢失工作目录后必须从头执行，就要重新花费这 20 秒，而不仅是重做 2 秒工具计算。能保留哪些状态，决定了故障后哪些阶段需要重来。
 
 | 状态 | 需要记录的信息 | 丢失后的恢复方式 |
 | --- | --- | --- |
@@ -135,13 +135,13 @@
 
 E2B 是面向工具执行的环境平台，Firecracker 是亚马逊云服务（Amazon Web Services，AWS）为无服务器计算开发、于 2018 年开源的轻量虚拟机监控器；其 2020 年 NSDI 论文讨论了如何在大量短时任务之间兼顾硬件虚拟化隔离、启动速度和内存开销。[^firecracker-history] E2B 使用 Firecracker microVM 运行工具环境，模板可由容器镜像构建。镜像提供文件与依赖，microVM 提供运行时的虚拟机内核和设备边界。创建时，平台从模板建立环境；运行时，工具通过虚拟机内核读写文件和启动进程。容器镜像决定环境中有哪些文件和依赖，microVM 则决定这些程序运行时如何与宿主系统隔离。[^e2b]
 
-模型根据上下文选择动作，执行器在已经配置好权限与资源的环境中运行工具。两条路径在同一个任务控制器处汇合：它知道模型调用何时开始、工具何时使用环境，以及哪些文件需要跨轮保存。这些信息让平台能够分别安排模型状态与工具状态的生命周期。模型计算期间可以准备下一轮环境，工具完成后可以保存文件并释放进程；后文将据此计算容量与时间开销，选择环境的创建、保留和恢复方式。
+模型根据上下文选择动作，执行器在已经配置好权限与资源的环境中运行工具。两条路径在同一个任务控制器处汇合：它知道模型调用何时开始、工具何时使用环境，以及哪些文件需要跨轮保存。这些信息让平台能够分别安排模型状态与工具状态的保存和释放。模型计算期间可以准备下一轮环境，工具完成后可以保存文件并释放进程；后文将据此计算容量与时间开销，选择环境的创建、保留和恢复方式。
 
 ### 11.2.2 资源分配与环境创建
 
 确定隔离方式以后，平台还需为环境分配资源并加载程序所需的数据。工具平台通过 CPU 配额、内存上限以及文件和设备访问权限来分配资源。给一个等待模型的环境增加 CPU 配额，并不会改变其文件和内存状态；销毁这个环境，则会释放内存，同时要求下一次执行前重建这些内容。CUDA 是 NVIDIA GPU 的编程与运行平台；验证 CUDA 程序还需向设备池申请 GPU。设备直通让虚拟机直接操作分配给它的物理设备，可由虚拟机程序 QEMU 配合 Linux 的 VFIO 设备访问接口完成。[^ub]
 
-创建环境需要取得模板、建立私有状态并准备首次工作所需的数据。最简单的方式是完整复制模板；另一种方式是共享只读内容，仅复制需要写入的页面；还可以按访问需求加载数据。创建时少加载一些数据，可以缩短启动时间，但程序首次访问这些数据时仍需等待加载。
+创建环境需要取得模板、建立私有状态并准备首次工作所需的数据。最简单的方式是完整复制模板；另一种方式是共享只读内容，仅复制需要写入的页面；还可以按访问需求加载数据。创建环境时少加载一些数据，可以缩短启动时间，但程序首次访问这些数据时仍需等待加载。
 
 ![模板保存共享的文件与依赖，私有内容保存环境修改；程序运行时还会占用进程内存和缓冲区](ch11/figure-11-template-runtime.svg)
 
@@ -207,14 +207,14 @@
 
 这项安排依赖准备能在工具执行前完成。若下一个工具的类型要从流式输出中预测，那么调用发出后还要等多久，就取决于准备提前了多少，以及预测是否正确。SpecBox 用流式输出中透露的工具类型和工具切换记录提前准备相应环境，下面按一次调用分析它的收益。[^platform]
 
-设一次准备需要 $P$ 秒，提前量为 $L$ 秒，预测选对环境的概率为 $h$。假设正确预测不会争用资源，错误预测可以立即取消，错误时再按需准备，发出调用后等待环境就绪的平均时间为
+设一次准备需要 $P$ 秒，提前量为 $L$ 秒，正确预测所需环境的概率为 $h$。假设正确预测不会争用资源，错误预测可以立即取消，错误时再按需准备，发出调用后等待环境就绪的平均时间为
 
 $$
 E[W]=h\max(P-L,0)+(1-h)P
 =P-h\min(P,L).
 $$
 
-正确预测时，准备的前 $\min(P,L)$ 秒与模型调用重叠，工具调用仍需等待剩余的准备工作完成；猜错时，实际需要的环境从头准备。两条路径按命中概率加权，就得到上式。当准备工作已经能在模型返回前完成时，再提早开始准备，只会延长环境准备好之后的空闲时间。
+正确预测时，准备的前 $\min(P,L)$ 秒与模型调用重叠，工具调用仍需等待剩余的准备工作完成；猜错时，实际需要的环境从头准备。两条路径按命中概率加权，就得到上式。当准备工作已经能在模型返回前完成时，再提早开始准备，只会延长环境就绪后等待调用的时间。
 
 取 $P=2$ 秒、$h=0.75$。不提前准备时，每次调用等 2 秒。提前 1 秒时，四次调用平均有三次只等 1 秒，一次因猜错仍等 2 秒，平均等待为 1.25 秒。提前 2 秒时，正确分支不再等待，平均降至 0.5 秒。提前 3 秒仍是 0.5 秒，却让正确分支多保留 1 秒就绪状态。对 2 GiB 环境，每项预测因此增加 $0.75\times2\times1=1.5$ GiB·秒的平均空闲内存占用。
 
@@ -240,7 +240,7 @@
 
 由此可以比较三种减少等待的办法。缓存模板可以省去重复获取数据的开销；保留已经启动的环境，可以在调用到达后立即执行，但需要持续占用内存；每轮重新建立环境，则可以利用模型调用的时间完成准备。章首任务已经知道下一轮所需的运行时，因此在模型调用期间准备下一轮环境；多种工具交替出现时，还需在计算中计入预测命中率。
 
-本地工具重放展示了过早准备的后果：从提前 50 ms 准备改为在模型等待一开始就准备后，累计工具调用时间仍约为 0.26–0.28 秒，按采样结果计算的累计内存占用却从约 0.019 增到 0.47 GiB·秒，约增至 24 倍。增加的时间主要落在环境已经就绪、调用尚未到达的区间。只要能赶在工具调用到达前准备好环境，就没有必要更早创建。环境提前就绪后多等待的时间，都会增加内存占用。[^prewarm]
+本地工具重放展示了过早准备的后果：从提前 50 ms 准备改为在模型等待一开始就准备后，累计工具调用时间仍约为 0.26–0.28 秒，按采样结果计算的累计内存占用却从约 0.019 增到 0.47 GiB·秒，约增至 24 倍。增加的内存占用主要发生在环境已经就绪、调用尚未到达的区间。只要能赶在工具调用到达前准备好环境，就没有必要更早创建。环境提前就绪后多等待的时间，都会增加内存占用。[^prewarm]
 
 ## 11.3 共享资源池的分配与调度
 
@@ -274,9 +274,9 @@
 
 ### 11.3.2 队列、优先级与抢占代价
 
-上一节解决了资源分散在不同节点的问题。即使节点配置合适，请求同时到达也可能让资源来不及周转。章首任务均匀到达，每秒需要执行 30 次工具调用，40 个 CPU 核足以处理。当调用集中到达时，即使总工作量相同，排队时间也会不同。
-
-先只看一个执行进程（worker），它执行每项工具工作都需要 1 秒。十项工作在第 0、1、……、9 秒分别到达时，每项到达即可执行，排队时间都是零。若十项都在第 0 秒到达，执行仍需 10 秒，但它们的排队时间依次为 0、1、……、9 秒，平均为 4.5 秒。总工作量决定执行进程要工作多久，到达顺序决定每个任务先等多久。
+上一节解决了资源分散在不同节点的问题。即使节点配置合适，请求同时到达也可能让资源来不及周转。章首任务均匀到达，每秒需要执行 30 次工具调用，40 个 CPU 核足以处理。当调用集中到达时，即使总工作量相同，排队时间也可能不同。
+
+先只看一个执行进程（worker），它执行每项工具工作都需要 1 秒。十项工作在第 0、1、……、9 秒分别到达时，每项到达即可执行，排队时间都是零。若十项都在第 0 秒到达，执行仍需 10 秒，但它们的排队时间依次为 0、1、……、9 秒，平均为 4.5 秒。总工作量决定执行进程要工作多久，到达时刻和执行顺序决定每个任务先等多久。
 
 图 11-23 用执行块前方的灰色条形表示等待时间。同样十秒的执行工作，既可以没有排队，也可以产生大量等待。因此，从平均资源需求推到实际响应时间，还必须知道请求是否集中到达。
 
@@ -342,7 +342,7 @@
 
 临时实例提前被收回时，损失的不仅是尚未利用的容量，还有已经生成的内容。保存生成前缀可以把已完成工作带到新实例。新实例读入“原输入＋保存输出”，执行预填充（prefill，处理完整前缀）建立注意力上下文键值缓存（KV），然后继续生成。PolyRL 的一个批量分支按组内最短前缀对齐：两条响应已收到 4,000 和 1,000 token 时，各采用 1,000 token，共复用 2,000 token，较长那条的 3,000 token 要重新生成。将前缀长度对齐后，可以统一安排后续生成，但较长回答超出共同前缀的部分就需要重新生成。
 
-在一次 Qwen3-8B 对照中，保留前缀省去了 256 个重复采样 token，完整时间却约为 12.8 秒，而从头重做约为 11.6 秒。节省的 token 对应生成阶段，完整路径还包含前缀重建与重新调度。这组对照中，前缀重建与重新调度的额外时间超过了省下的生成时间。[^recovery]
+在一次 Qwen3-8B 对照中，保留前缀省去了 256 个重复采样 token，请求总耗时却约为 12.8 秒，而从头重做约为 11.6 秒。节省的 token 对应生成阶段，完整路径还包含前缀重建与重新调度。这组对照中，前缀重建与重新调度的额外时间超过了省下的生成时间。[^recovery]
 
 还可以从成本判断增加临时实例是否划算。按 RLBoost 使用的历史价格，训练实例每小时约 84 美元，六个额外实例各约 5.3 美元，总费率约为每小时 116 美元。全部实例全程计费时，处理同一批训练任务的吞吐量需达到原来的约 $116/84\approx1.38$ 倍，单位工作成本才下降。吞吐量只达到原来的 1.2 倍，完成时间缩短，但成本约增至原来的 1.15 倍；达到原来的 1.6 倍，成本则约降至 0.86 倍。[^rollout]
 
@@ -396,7 +396,7 @@
 C_{100}=\frac{10000\times2+(100+200)\times10}{10^6}=0.023.
 $$
 
-思考 token 数降为原来的十分之一，总成本却只减少约 28%。原来成本中的 0.020 用于输入，0.002 用于可见输出，这两项保持不变；下降的是从 0.010 变为 0.001 的思考成本。因而，思考占原成本的比例决定了压缩它能省多少。[^platform]
+思考 token 数降为原来的十分之一，总成本却只减少约 28%。原有成本中的 0.020 用于输入，0.002 用于可见输出，这两项保持不变；下降的是从 0.010 变为 0.001 的思考成本。因而，思考占原成本的比例决定了压缩它能省多少。[^platform]
 
 图 11-29 将节省发生的位置单独标出。这个比较假定两种设置都能完成任务；如果缩短思考后需要重试，省下的橙色部分就要与重试新增的整次调用成本一起比较。
 
@@ -437,9 +437,9 @@
 
 缓存只有在请求到达保存了相应前缀的后端时才能复用。因此，选择后端既影响命中率，也影响排队时间。为了使用已有的前缀缓存，有时值得多等一会儿。设运行同一模型的后端 A 排队 200 ms，命中后处理前缀需 50 ms；后端 B 排队 20 ms，未命中处理需 500 ms；共同网络时间为 50 ms，后续生成相同。进入生成前，A 用 300 ms，B 用 570 ms，A 领先 270 ms。缓存命中省下了 450 ms 的处理时间，但多付出了 180 ms 的排队时间，净收益恰好为 270 ms。当 A 的排队时间增至 470 ms 时，两者在生成开始前花费的时间就相同了。
 
-章首每秒 30 次模型调用还受到并发容量的约束。每次调用需 9 秒时，平均有 270 次调用正在处理；缩短到 6 秒后，降为 180 次。服务最多同时处理 300 次调用，因此可用于处理其他请求的并发容量增加了 90 次。若每次调用的输入或输出增多，token 额度可能先于请求数额度用完。选择模型服务时，需要分别比较请求排队时间、并发调用数和 token 处理能力，找出限制任务完成速度的因素。
-
-**例 11-7：缓存命中与成功率何时能抵消更高的服务单价？** 向两个假想服务各提交 1,000 项单次调用任务。每次输入 20,000 token，其中公共前缀 19,000、新输入 1,000。没有额外缓存创建、存储、工具和预热成本，质量成功概率与缓存命中独立。价格与成功率均为教学输入。[^routing]
+章首每秒 30 次模型调用还受到并发容量的约束。每次调用需 9 秒时，平均有 270 次调用正在执行；缩短到 6 秒后，降为 180 次。服务最多同时处理 300 次调用，因此可用于处理其他请求的并发容量增加了 90 次。若每次调用的输入或输出增多，token 额度可能先于请求数额度用完。选择模型服务时，需要分别比较请求排队时间、并发调用数和 token 处理能力，找出限制任务完成速度的因素。
+
+**例 11-7：缓存命中与成功率何时能抵消更高的服务单价？** 向两个假想服务各提交 1,000 项单次调用任务。每次输入 20,000 token，其中公共前缀 19,000、新输入 1,000。没有额外缓存创建、存储、工具和预热成本，任务通过验收的概率与缓存是否命中相互独立。价格与成功率均为教学输入。[^routing]
 
 | 参数 | A | B |
 | --- | ---: | ---: |
@@ -464,9 +464,9 @@
 
 *图 11-31：B 的成功任务成本随命中率提高而下降，在约 84.9% 处等于 A。两者的总成本先分别除以各自成功完成的任务数；本图只比较成本，下一图单独加入期限。*
 
-![只有 B 命中的 4 s 路径满足 6 s 期限；再乘 98% 质量概率，按时成功比例为 0.98h](ch11/figure-11-routing-deadline.svg)
-
-*图 11-32：只有 B 命中的 4 s 路径满足 6 s 期限；再乘 98% 质量概率，按时成功比例为 0.98h。达到 90% 目标要求 h 至少约 91.8%。*
+![只有 B 命中的 4 s 路径满足 6 s 期限；再乘 98% 的验收通过率，按时成功比例为 0.98h](ch11/figure-11-routing-deadline.svg)
+
+*图 11-32：只有 B 命中的 4 s 路径满足 6 s 期限；再乘 98% 的验收通过率，按时成功比例为 0.98h。达到 90% 目标要求 h 至少约 91.8%。*
 
 本例每次前缀都是 19,000 token，所以请求命中率 $h$ 也等于前缀内部的 token 命中比例；相对于完整 20,000-token 输入，缓存读取占比为 $0.95h$。成本交点约为 84.9%，业务门槛约为 91.8%。从低命中率向右移动，B 先变得便宜，随后才满足按时完成目标。把服务目标放到成本曲线上，就能直接读出最终可选区域。
 
@@ -494,7 +494,7 @@
 
 原先每个输出 token 所需时间中，约 63% 用于 decode，其余 37% 保持不变，因而两倍 decode 加速只能将总时间缩短约 31%。即使把 decode 时间降到零，其余处理仍需要 9.4 ms。若要突破这一限制，需进一步优化输入处理和上下文重建；同一组设备在单位时间内生成更多 token，固定租价才能由更多产出分摊。
 
-章首的代码修复平台每秒接收 10 项任务，每项调用模型三次，因此每秒调用模型 30 次。每次价格增加 0.002，平台每秒便多支出 0.06；如果这次换服务让任务从 30 秒降到 21 秒，这笔额外成本就使任务能够在 24 秒期限内完成。章末将把这笔新增模型费与节省的环境占用放在同一张方案表里。
+章首的代码修复平台每秒接收 10 项任务，每项调用模型三次，因此每秒调用模型 30 次。每次价格增加 0.002，平台每秒便多支出 0.06；如果更换服务让任务耗时从 30 秒降到 21 秒，这笔额外成本就使任务能够在 24 秒期限内完成。章末将把这笔新增模型费与节省的环境占用放在同一张方案表里。
 
 ## 11.5 完整任务的恢复、成本计算与扩容
 
@@ -523,7 +523,7 @@
 
 把一次失败尝试从分子删除，会漏算它消耗的模型和工具资源。比如十项任务共花 1 个成本单位，五项成功，平台每完成一项实际支付 0.2。若只统计五项成功尝试花费的 0.5 个成本单位，就会得到 0.1，恰好漏掉另一半资源。总支出除以完成数，使成功与失败共同决定单位产出成本。
 
-有限重试树可以在部署前用于初步估算。对路径 $\pi$，令路径概率为 $p_\pi$，各步骤成本之和为 $c_\pi$，则 $E[C]=\sum_\pi p_\pi c_\pi$。成功概率是所有最终成功的执行路径概率之和。每个节点的概率都应根据此前经历的执行结果计算；如果两类失败的后续成功率不同，应拆成不同节点。
+有限重试树可以在部署前用于初步估算。对路径 $\pi$，令路径概率为 $p_\pi$，各步骤成本之和为 $c_\pi$，则 $E[C]=\sum_\pi p_\pi c_\pi$。成功概率是所有最终成功路径的概率之和。每个节点的概率都应根据此前经历的执行结果计算；如果两类失败的后续成功率不同，应拆成不同节点。
 
 **例 11-8：局部修复对成功率与超时比例的影响。** 另取一个首次尝试需 10 秒的小例子，比较以下有限恢复策略。首次尝试花费 0.010、耗时 10 秒；80% 直接成功，12% 进入局部修复，8% 直接升级。局部修复新增 0.006 和 4 秒，条件成功率 60%，其余升级。升级新增 0.030 和 8 秒，条件成功率 98%。20 秒期限只用于评价，不主动终止任务。[^retry]
 
@@ -568,7 +568,7 @@
 
 ### 11.5.3 CPU、GPU、内存与服务额度的扩容选择
 
-下面根据章首提出的 24 秒完成期限，选择模型服务与环境管理方案。此前已经得到三项结果：普通服务每次调用 9 秒，三轮需要 30 秒；快速服务每次 6 秒，三轮需要 21 秒；每轮重建环境把每项环境占用压到 18 GiB·秒，准备工作额外使用 0.3 秒 CPU 时间。
+下面根据章首提出的 24 秒完成期限，选择模型服务与环境管理方案。此前已经得到三项结果：普通服务每次调用 9 秒，三轮需要 30 秒；快速服务每次 6 秒，三轮需要 21 秒；每轮重建环境把每项任务的累计环境内存占用降到 18 GiB·秒，准备工作额外使用 0.3 秒 CPU 时间。
 
 **例 11-9：如何选择模型服务与环境管理方式，才能按期完成且成本最低？** 沿用章首条件：每 0.1 秒到达一项任务，每项执行三轮；工具平台提供 40 核和 640 GiB 内存，模型服务最多同时处理 300 次调用。两种模型处理同一任务集时，最终通过测试的概率均为 95%，且任务能否通过测试与到达时刻无关。普通服务每次 0.010 成本单位，快速服务每次 0.012，均已包含输入、生成与缓存成本。CPU 按每 CPU·秒 0.0001 计费，环境内存按每 GiB·秒 0.0001 计费，均按实际使用量计算。文件持久化成本已包含在每轮工具执行中；平台共有的固定支出在四方案中相同。[^design]
 
@@ -589,7 +589,7 @@
 | 快速服务＋始终驻留 | 21 | 3 | 42 | 420 | 0.0405 | 95% |
 | 快速服务＋每轮重建环境 | 21 | 3.3 | 18 | 180 | 0.0381 | 95% |
 
-以最后一行为例，三次模型调用花费 $3\times0.012=0.036$，CPU 成本为 $3.3\times0.0001=0.00033$，内存成本为 $18\times0.0001=0.0018$，合计约 0.0381。每秒进入 10 项任务，工具平均使用 33 个 CPU 核和 180 GiB 内存，模型服务平均同时处理 180 次调用，均在现有容量以内。
+以最后一行为例，三次模型调用花费 $3\times0.012=0.036$，CPU 成本为 $3.3\times0.0001=0.00033$，内存成本为 $18\times0.0001=0.0018$，合计约 0.0381。每秒进入 10 项任务，工具执行与环境准备平均使用 33 个 CPU 核和 180 GiB 内存，模型服务平均同时执行 180 次调用，均在现有容量以内。
 
 表中的平均需求满足容量要求，接下来还要确认各阶段能否按时安排。均匀到达使我们可以直接写出一个满足容量限制的时间表。快速服务下，以每项任务的到达时刻为起点，分别在第 4–6、11–13、18–20 秒准备环境，在第 6–7、13–14、20–21 秒运行工具；准备工作累计使用 0.1 秒 CPU 时间，均匀分布在两秒内。任务每 0.1 秒到达一项，稳定运行后，每一轮的工具执行各占 10 个核，每一轮的准备工作各占一个核；活跃环境最多约 90 个，共 180 GiB。三个模型调用阶段各平均有 60 次调用正在处理，合计 180 次并发调用。按这个时间表执行，既满足各阶段的先后依赖，也没有超过平台容量。
 
@@ -597,7 +597,7 @@
 
 第 11.5.2 节还说明，增加重试会改变按时完成的比例。这里正常任务在第 21 秒完成，剩余期限只有 3 秒；若最终失败后再加一次 4 秒局部修复，结果将到第 25 秒才出现。因此，本题在三轮后结束任务；未通过测试的任务直接返回失败结果。若业务把期限延长到 25 秒，局部修复才有机会增加按时成功数，届时再将条件成功概率和额外资源放入恢复树。
 
-以上选择建立在模型每轮调用 6 秒的条件下。模型继续加速时，环境能释放的空闲时间也会缩短，原来的环境策略就需要重新比较。保留环境还是重新建立环境，可以根据两种方案成本相等时的等待长度来判断。设两次工具执行之间需要等待模型 $G$ 秒。保持 2 GiB 环境，驻留成本为 $2G\times0.0001$；销毁后在下一轮模型调用末尾重新准备，内存成本为 $2\times2\times0.0001$，准备所需的 CPU 成本为 $0.1\times0.0001$，共 0.00041。两者在 $G=2.05$ 秒时相等。等待 6 秒时应重建；等待缩短到 2 秒时，保留环境开始更省，准备也占满全部可重叠窗口。模型继续加速，会改变最合适的环境策略。
+以上选择建立在模型每轮调用 6 秒的条件下。模型继续加速时，可以释放环境内存的空闲时段也会缩短，原来的环境策略就需要重新比较。保留环境还是重新建立环境，可以根据两种方案成本相等时的等待长度来判断。设两次工具执行之间需要等待模型 $G$ 秒。保持 2 GiB 环境，驻留成本为 $2G\times0.0001$；销毁后在下一轮模型调用末尾重新准备，内存成本为 $2\times2\times0.0001$，准备所需的 CPU 成本为 $0.1\times0.0001$，共 0.00041。两者在 $G=2.05$ 秒时相等。等待 6 秒时应重建；等待缩短到 2 秒时，保留环境开始更省，准备也占满全部可重叠窗口。模型继续加速，会改变最合适的环境策略。
 
 还可以用同一个公式概括局部加速对总时间的影响。令原时间中可加速部分占比为 $f$，将这部分加速 $s$ 倍，剩余部分保持原时长，则
 
@@ -613,20 +613,22 @@
 
 以下练习沿用 11-1 至 11-10 的资料编号。核心题 11-1、11-2 和 11-10 构成从需求到设计的完整练习；实验原始记录与选做运行入口见[本章配套](ch11/README.md)。
 
-1. **实验 11-1〔核心〕：从多轮任务推算模型并发、CPU 与环境内存。** 平台每秒接收 8 项任务，其中一半执行两轮，另一半执行四轮。每轮模型调用耗时 6 秒，工具执行消耗 1 CPU·秒。求模型调用率、平均并发模型调用数与平均使用的 CPU 核数。若每个环境在任务全程占用 2 GiB，求平台的平均环境内存占用。再假设环境只在每轮 2 秒的准备阶段和 1 秒的工具执行阶段保留，重新计算平均内存占用，以及每秒需要创建的环境数量。
-2. **实验 11-2〔核心〕：环境重建与快照恢复的耗时和驻留比较。** 一个环境中有可重新下载的依赖、未保存的编辑缓冲、已持久化的补丁和已提交的外部写入。逐项说明发生故障后能从哪里恢复，以及哪些内容无法直接恢复。设完整重建环境需要 5 秒，从快照恢复需要 2 秒。保存快照需要 1 秒，可以与耗时 9 秒的模型调用重叠；画出两种方案从开始恢复到下一次工具执行完成的时间线，再计算各自需要保留环境的时长。
+1. **实验 11-1〔核心〕：从多轮任务推算模型并发、CPU 与环境内存。** 平台每秒接收 8 项任务，其中一半执行两轮，另一半执行四轮。每轮模型调用耗时 6 秒，工具占用一个 CPU 核执行 1 秒。求模型调用率、平均并发模型调用数与平均使用的 CPU 核数。若每个环境在任务全程占用 2 GiB，求平台的平均环境内存占用。再假设环境只在每轮 2 秒的准备阶段和 1 秒的工具执行阶段保留，重新计算平均内存占用，以及每秒需要创建的环境数量。
+2. **实验 11-2〔核心〕：重建环境与恢复快照分别需要多久、占用多久内存？** 一个环境中有可重新下载的依赖、未保存的编辑缓冲、已持久化的补丁和已提交的外部写入。逐项说明发生故障后能从哪里恢复，以及哪些内容无法直接恢复。
+
+   再比较两种能够恢复下一轮所需状态的方案：完整重建环境需要 5 秒，从快照恢复需要 2 秒，保存快照需要 1 秒。以上一轮工具结束为起点，下一次模型调用立即开始并耗时 9 秒，随后工具执行 1 秒。保存快照可与模型调用同时开始；在不推迟下一次工具执行的前提下，尽量晚地开始重建或恢复。画出两种方案的时间线，再计算从该起点到下一次工具完成期间，各自累计需要保留环境多久；保存快照期间也计入驻留时间。
 3. **实验 11-3：提前准备环境能省多少等待，又占用多少内存？** 创建环境需要 2 秒，对下一次调用所需环境的预测准确率为 0.6。调用到达后取消错误预测所对应的环境；每个提前准备的环境从准备开始就占用 2 GiB。分别在调用到达前 1、2、4 秒开始准备，计算调用到达后的期望等待时间，以及错误准备造成的期望累计内存占用（以 GiB·秒计）。预测错误时，在调用到达后重新创建所需环境。若正确率提高到 0.9，哪些收益变大，哪些浪费减少？
-4. **实验 11-4：不同作业完成目标下，迁移何时优于等待。** 等待资源释放需 12 秒，本地执行需 20 秒；迁移时间为未知量 $m$，被迁移任务额外推迟 $m+2$ 秒。分别以新作业最早完成、两个作业新增等待之和最小为目标，求迁移优于等待的条件。说明为何两个目标可以作出不同选择。
-5. **实验 11-5：启动耗时如何影响短期实例的有效产出。** 每个新实例传输与加载共需 8 秒，之后以每秒 100 个有效 token 的速率工作。三个实例分别存活 10、20、40 秒，求总产出及有效工作时间占比。若准备时间降到 4 秒，比较三个实例各自的产出改善，并解释可用时间较短的实例为何更敏感。
-6. **实验 11-6：验证进程数量与长尾样本如何决定整批完成时间。** 三条样本在 0、6、12 秒到达，验证分别需 10、2、8 秒。先求只有一个执行进程时，按到达顺序处理各条样本的开始和完成时刻；再求使用两个执行进程时，整批最早何时完成。把最后一条样本的验证时间改为 80 秒，解释增加执行进程所能节省的时间为什么会变化。
+4. **实验 11-4：不同作业完成目标下，迁移何时优于等待。** 新作业所需的资源正被已有作业占用，等待其释放需要 12 秒。新作业获得资源后，在本地执行需要 20 秒。也可以将已有作业迁至其他节点，在迁移完成后释放本地资源；迁移耗时为未知量 $m$，使已有作业的完成时间比不迁移时推迟 $m+2$ 秒。先以新作业最早完成为目标，求迁移优于等待的条件；再以新作业的资源等待时间与已有作业的完成延迟之和最小为目标，重新求解。说明为何两个目标可以作出不同选择。
+5. **实验 11-5：启动耗时如何影响短期实例的有效产出。** 每个新实例传输与加载共需 8 秒，之后以每秒 100 个有效 token 的速率工作。从启动开始到回收为止，三个实例的总可用时长分别为 10、20、40 秒，求总产出及有效工作时间占比。若准备时间降到 4 秒，比较三个实例各自的产出改善，并解释可用时间较短的实例为何更敏感。
+6. **实验 11-6：验证进程数量与长尾样本如何决定整批完成时间。** 三条样本在 0、6、12 秒到达，验证分别需 10、2、8 秒。先求只有一个执行进程时，按到达顺序处理各条样本的开始和完成时刻；再求使用两个执行进程时，整批最早何时完成。把最后一条样本的验证时间改为 80 秒，判断增加执行进程能否缩短整批完成时间，并解释哪条样本决定了这一结果。
 7. **实验 11-7：缓存命中与结果正确性相关时，如何计算成本与按时成功率？** 沿用例 11-7 的 B 服务成本，改为命中时成功率 99%、未命中时 90%。推导平均每个成功任务的成本随命中率变化的关系，以及在 6 秒内通过测试的任务比例。求至少让 90% 的任务按时通过测试所需的最低命中率，并与原独立概率模型比较。
-8. **实验 11-8：自建与 API 服务的成功任务成本交点。** 自建服务的固定支出为 1,000 个成本单位，每处理一项任务另需 0.002，任务成功率为 90%；API 服务每项任务收费 0.012，成功率为 98%。两者均有足够能力满足期限。假设两种服务处理相同数量的任务，求它们的平均成功任务成本相等时的任务量。再将自建处理上限设为每月 80,000 项，讨论交点怎样影响购买决定。
+8. **实验 11-8：自建与 API 服务的成功任务成本交点。** 自建服务的固定支出为 1,000 个成本单位，每处理一项任务另需 0.002，任务成功率为 90%；API 服务每项任务收费 0.012，成功率为 98%。按一个月作为成本比较期，固定支出全部计入该月。两者均有足够能力满足期限，并处理相同数量的任务，求它们的平均成功任务成本相等时的任务量。再将自建处理上限设为每月 80,000 项，讨论交点怎样影响购买决定。
 9. **实验 11-9：任务期限与提前停止如何改变重试成本和成功率。** 沿用例 11-8，将期限改为 14、18、22 秒，分别求按时通过测试的比例。再设计一种在剩余时间不足以完成下一节点时停止的策略，重新计算期望成本，并解释它与仅改变评价期限的差别。
 10. **实验 11-10〔核心〕：到达率提高后如何扩容并选择环境驻留策略。** 沿用例 11-9，将到达率增加到每秒 15 项、快速服务调用时间降到 2 秒，每项任务仍执行三轮，每轮工具执行仍需 1 秒。CPU、内存、模型调用的并发容量和单价保持原值。比较始终驻留与每轮重建环境，决定应增加哪项资源、至少增加多少，并说明环境策略是否需要改变。最后将到达方式改为每秒有 15 项任务同时到达，画出工具执行的时间线，并标出 CPU 并发占用的峰值。
 
 [^environment]: 本书 [environment-resources 固定计算](../calculations/results/environment-resources-book.md)及[工具子进程记录](../experiments/ch11/11-01/process-resources/README.md)。工具实验包含九组、36 个进程，累计内存占用由采样 RSS 对时间积分得到。
 
-[^lifecycle]: [environment-lifecycle 固定预算](../calculations/results/environment-lifecycle-default.md)区分共享／私有容量、串行字节下界和局部预热实测。本章使用固定容量预算比较四种内容加载方式。
+[^lifecycle]: [environment-lifecycle 固定预算](../calculations/results/environment-lifecycle-default.md)区分共享／私有容量、串行传输的耗时下界和局部预热实测。本章使用固定容量预算比较四种内容加载方式。
 
 [^platform]: [调度、模型路由与云端环境研究](../case-studies/platform-routing.md)，包含 ASI、RLBoost、DistRS、SpecBox。ASI 研究覆盖六个月、155,410 张 GPU，其分配比例度量设备归属。计费公式采用题设价格。
 
@@ -646,9 +648,9 @@
 
 [^validation]: [超时与资源释放实验](../experiments/ch11/11-05/README.md)、[双批次调度记录](../experiments/ch11/11-05/batch-scheduling/README.md)。实验使用受控 CPU 进程。
 
-[^thinking]: [思考预算与质量记录](../experiments/ch11/11-08/README.md)、[宽预算检查](../experiments/ch11/11-08/wide-budget/README.md)。记录分别检查生成是否自然结束，以及输入状态是否满足要求。
-
-[^routing]: [routing-cost 固定结果](../calculations/results/routing-cost-book.md)、[交点结果](../calculations/results/routing-cost-crossover.md)及[完整教学推导](../case-studies/routing-cost-and-completion.md)。计算采用条件独立的质量概率，成功任务数按给定概率计算。
+[^thinking]: [思考预算与质量记录](../experiments/ch11/11-08/README.md)、[增加思考预算后的检查](../experiments/ch11/11-08/wide-budget/README.md)。记录分别检查生成是否自然结束，以及输入状态是否满足要求。
+
+[^routing]: [routing-cost 固定结果](../calculations/results/routing-cost-book.md)、[交点结果](../calculations/results/routing-cost-crossover.md)及[完整教学推导](../case-studies/routing-cost-and-completion.md)。计算采用与缓存命中相互独立的验收通过概率，成功任务数按给定概率计算。
 
 [^purchase]: 计算结果为 25.317 ms，其中 decode 为 15.918 ms，其余阶段共 9.399 ms。此处将逐层固定开销和周期全量重建设为教学输入，详见[持续 Agent 比较基线](../experiments/ch13/13-06/single-agent-serving/COMPARISON-RESULTS.md)、[优化审计](../experiments/ch13/13-06/single-agent-serving/OPTIMIZATION-AUDIT.md)、[时间归因与敏感性](../experiments/ch13/13-06/single-agent-serving/AUDIT-RESULTS.md)。
 
@@ -656,7 +658,7 @@
 
 [^retry]: 精确结果：成功概率 0.99744，通过测试且按时完成概率 0.9504，每项期望成本 0.01456，累计 CPU 时间 3.376 秒，驻留 23.008 GiB·秒。计算结果分别列出资源用量和成本；各节点的给定成本已经是总价，无需再按资源用量重复计费。见[retry-paths 有限条件图](../calculations/results/retry-paths-book.md)。各节点概率与成本按题设给定，重试沿有限执行树展开；超过评价期限的任务仍继续执行到终点。
 
-[^design]: 贯穿平台与例 11-9 是确定性教学设计，输入、逐阶段时间表、成本、成本相等的条件与复算见[贯穿设计数据](ch11/platform-design.json)及[验证程序](ch11/verify.py)。40 核、640 GiB 内存、最多 300 次并发模型调用、两种服务时间／价格和 95% 质量概率为题设。完整环境准备含文件取得与初始化共 2 秒、0.1 CPU·秒，结果保存与工作状态清理包含在工具的 1 秒内；原模板容量算例单独说明内容加载。
+[^design]: 贯穿平台与例 11-9 是确定性教学设计，输入、逐阶段时间表、成本、成本相等的条件与复算见[贯穿设计数据](ch11/platform-design.json)及[验证程序](ch11/verify.py)。40 核、640 GiB 内存、最多 300 次并发模型调用、两种服务时间／价格和 95% 验收通过率为题设。完整环境准备含文件取得与初始化共 2 秒、0.1 CPU·秒，结果保存与工作状态清理包含在工具的 1 秒内；原模板容量算例单独说明内容加载。
 
 [^firecracker-history]: Agache 等，AWS，[Firecracker: Lightweight Virtualization for Serverless Applications](../references/text/firecracker.txt)，NSDI 2020。
 
@@ -668,7 +670,7 @@
 
 章首平台平均使用 30 个 CPU 核，环境却占用 600 GiB 内存。原因是三轮任务中有 27 秒在等待模型，而工具环境始终占用内存。在模型调用期间准备环境，把每项任务的累计内存占用从 60 降到 18 GiB·秒，准备工作多用 0.3 秒 CPU 时间，就能释放大量内存。
 
-这项改进利用了完整任务的执行关系。环境管理器确定模型调用期间可用于准备的时间后，可以把创建提前，让进程仅在工具准备和执行期间存在；持久化文件保存各轮之间需要保留的状态。模型服务和环境平台共同安排准备时机，使原来贯穿任务全程的资源占用变成按阶段分配。
+这项改进利用了完整任务的执行关系。环境管理器确定模型调用期间可用于准备的时间后，可以提前创建环境，让进程仅在工具准备和执行期间存在；持久化文件保存各轮之间需要保留的状态。模型服务和环境平台共同安排准备时机，使原来贯穿任务全程的资源占用变成按阶段分配。
 
 释放内存以后，任务仍需 30 秒，模型调用成为满足 24 秒期限的关键。将调用从 9 秒降到 6 秒，把三轮任务从 30 秒降到 21 秒，也将平均并发模型调用数从 270 次降到 180 次。结合每轮重建环境，现有 40 核、640 GiB 内存和最多 300 次并发模型调用的容量即可支持给定任务流。成本比较随后在满足质量和期限的方案之间完成。
 
