# 第 3 章语言通读：逐句修改对照

本文件记录 2026-09-11 全书通读在本章留下的净改动。

## 全书通读（2026-09-11，按最终文本与通读前快照逐行对照）

共 47 行有改动；英文术语按“首次出现保留中文注释、之后用英文”的原则已回退，下表是回退之后的净改动。省略号表示只截取了改动附近的文字。

| 修改前 | 修改后 |
|---|---|
| …，每个请求输入 4,608 个 token、输出 1,152 个 token。第二章已经教我们按输入和输出长度计算模型工作量。现在把平均工作量乘以每秒请求数，是否… | …，每个请求输入 4,608 个 token、输出 1,152 个 token。第 2 章已经教我们按输入和输出长度计算模型工作量。现在把平均工作量乘以每秒请求数，是否… |
| …。智能体（Agent）是通过模型决定下一步操作、调用工具并根据结果继续工作的程序，它在模型与工具之间往返，GPU 暂停生成时，上下文状态仍需保留，供后续调用使用。训练中的序列长度、有效标签和更新次数，也会影响运算量和状态… | …。智能体（Agent）是通过模型决定下一步操作、调用工具并根据结果继续工作的程序。它在模型与工具之间往返，等待工具结果时模型暂停生成，上下文状态却仍要保留，供后续调用使用。训练中的序列长度、有效标签和更新次数，也会影响运算量和状态… |
| 第二章给出了单次调用的容量、运算量和读取量。本章再加入三个问题：工作何时到达，哪些步… | 第 2 章给出了单次调用的容量、运算量和读取量。本章再加入三个问题：工作何时到达，哪些步… |
| …$j$ 次开始时，已有上下文为 $S+P+j$。因此，每次调用的资源需求可以用第二章的方法算出，调用之间的顺序则由生成依赖确定。[^model] | …$j$ 次开始时，已有上下文为 $S+P+j$。因此，每次调用的资源需求可以用第 2 章的方法算出，调用之间的顺序则由生成依赖确定。[^model] |
| …多乘加运算；低并发 decode 则可能为很少几个 token 读取整组权重。这是“prefill 通常更依赖算力、decode 通常更依赖内存带宽”的来源。 | …多乘加运算；低并发 decode 则可能为很少几个 token 读取整组权重。这就是“prefill 通常受算力限制、decode 通常受内存带宽限制”这种说法的由来。 |
| TTFT 是 time to first token，即从请求到达到首个 token 返回的时间。首响应衡量多久开始输出，完整请求延迟衡量多久生成完，平均 token 间隔… | TTFT（time to first token）是从请求到达到首个 token 返回的时间，本书也称首响应时间。首响应衡量多久开始输出，完整请求延迟衡量多久生成完，平均 token 间隔… |
| …。记录同一请求在各阶段的开始和结束时间，就能看出 TTFT 主要花在哪些环节。第五章将展开加速器提交、传输与完成的具体时序。 | …。记录同一请求在各阶段的开始和结束时间，就能看出 TTFT 主要花在哪些环节。第 5 章将展开加速器提交、传输与完成的具体时序。 |
| … token 数，再统计输出节奏。reasoning（推理）模型在给出可见回答前生成中间推理内容，这些内容称为思考 token。这类模型又增加了一个观察点：首个内部 token 出现时，用户未必已看到答案。因此，内部生成、用户可见输出和任务完成分别对应不同的时间点。语音的首次播放还要再经过解码与设备缓冲，稍后讨论。 | … token 数，再统计输出节奏。reasoning（推理）模型在给出可见回答前会生成中间推理内容，这些内容称为思考 token。这类模型又增加了一个观察点：首个内部 token 出现时，用户未必已看到答案。因此，内部生成、用户可见输出和任务完成分别对应不同的时间点。语音的首次播放还要再经过解码与设备缓冲，第 3.3.3 节再讨论。 |
| …研究观察到了这种现象，并用客户组成与时间变化重建请求分布。[^servegen] 因此，下面保持请求总量和平均长度相同，只改变它们到来的顺序。 | …研究观察到了这种现象，并用客户组成与时间变化重建请求分布。[^servegen] |
| 将 $P_i$ 与 $S_i$ 代入第二章的公式，投影和前馈网络（FFN，对每个 token 分别变换特征）只处理新增 … | 将 $P_i$ 与 $S_i$ 代入第 2 章的公式，投影和前馈网络（FFN，对每个 token 分别变换特征）只处理新增 … |
| …旧内容继续占据上下文；替换前面的内容可以缩短上下文，但其后的缓存可能需要重建。第八章将根据重建计算量、状态大小与复用间隔，比较保留、淘汰和重算的代价。[^cont… | …旧内容继续占据上下文；替换前面的内容可以缩短上下文，但其后的缓存可能需要重建。第 8 章将根据重建计算量、状态大小与复用间隔，比较保留、淘汰和重算的代价。[^cont… |
| …较两种编辑方式：在末尾追加 100 个 token，只增加 100 行新输入；若在已有 2000 个 token 的第 501 个 token 改写一个 token，变化会影响其后的表示，最长可直接复用的前缀只剩 500 个 token。缓存收益不仅取决于改了多少字… | …较两种编辑方式：在末尾追加 100 个 token，只增加 100 行新输入；若把已有 2000 个 token 中的第 501 个改掉，其后的表示都会变化，最长可直接复用的前缀只剩 500 个 token。缓存收益不仅取决于改了多少字… |
| …待时间换出状态。因此，负载记录还应包含稳定前缀、改写位置、分支依赖和复用间隔。第八章将使用这些信息选择上下文与缓存策略，第九章再加入状态位置和传输时间。 | …待时间换出状态。因此，负载记录还应包含稳定前缀、改写位置、分支依赖和复用间隔。第 8 章将使用这些信息选择上下文与缓存策略，第 9 章再加入状态位置和传输时间。 |
| …KV 存储和上下文读取的开销就越小。因此，检索器返回的内容会直接影响模型负载。第八章将结合具体任务比较不同检索方案。[^retrieval] | …KV 存储和上下文读取的开销就越小。因此，检索器返回的内容会直接影响模型负载。第 8 章将结合具体任务比较不同检索方案。[^retrieval] |
| **例题 3-2：加速首轮模型计算能缩短多少代码任务时间？** 用第一章的 Amdahl 分析，假设后续行为与质量不变。 | **例题 3-2：加速首轮模型计算能缩短多少代码任务时间？** 用第 1 章的 Amdahl 定律分析，假设后续行为与质量不变。 |
| …秒。此时 $f=36.375/76.510$，$s=2$，总加速比约为 1.31；首轮速度提高到两倍的收益受其原有时间占比限制。首轮占总时间约 47.5%，将这一段减半节省约 23.8% 的总时间；其余阶段… | …秒。此时 $f=36.375/76.510$，$s=2$，总加速比约为 1.31。首轮占总时间约 47.5%，将这一段减半节省约 23.8% 的总时间；其余阶段… |
| …务累加。工具执行时间越长，状态保留得越久，系统能同时处理的请求数也会受到影响，第八、九章将在这条时间线上安排缓存与交接。 | …务累加。工具执行时间越长，状态保留得越久，系统能同时处理的请求数也会受到影响，第 8、9 章将在这条时间线上安排缓存与交接。 |
| 把同一条会话换成 V4.1，可以看到负载需求如何推动模型结构的变化。设 Agent 已有一段公共上下文，工具随后返回新材料；如果前缀未命中，服务要重新处理较多输入，而本轮输出可能很短。为这样的输入密集场景降低 Prefill 成本，比只优化单步生成更有价值。V4.1 因而使用 20 层因果编码器和 20 层解码器：大部分输入只执行编码器主体，解码器全局 KV 从编码器末层表示投影得到；为构建解码器局部 SWA，将提示末尾最多 128 个 token 的编码器输出重新送入解码器，以近似恢复这部分局部状态。输出 token 继续经过全部 40 层。[^v41-c… | 把同一条会话换成 DeepSeek V4.1 Flash，可以看到负载需求怎样推动模型结构的变化。设 Agent 已有一段公共上下文，工具随后返回新材料；如果前缀未命中，服务要重新处理较多输入，而本轮输出可能很短。为这样的输入密集场景降低 prefill 成本，比只优化单步生成更有价值。V4.1 因而采用第 2 章介绍的因果编码器—解码器（CED）结构，使用 20 层因果编码器和 20 层解码器：大部分输入只执行编码器主体，解码器的全局 KV 从编码器末层表示投影得到；为了构建解码器的局部滑动窗口注意力（SWA）状态，把提示末尾最多 128 个 token 的编码器输出重新送入解码器，近似恢复这部分局部状态。输出 token 继续经过全部 40 层。[^v41-c… |
| …空状态开始的输入为 $P$ 个 token，则普通 40 层路径为 $40P$，论文 CED 调度为 $20P+20\min(P,128)$。当 $P=8192$ … | …空状态开始的输入为 $P$ 个 token，则普通 40 层路径为 $40P$，报告中的 CED 调度为 $20P+20\min(P,128)$。当 $P=8192$ … |
| …SWA 缺失，则先重放前缀末尾窗口。两条路径随后都要构建本轮的解码器 SWA。第八章将用 8K 与 128K 上下文的教学算例，比较保存状态和重新执行的成本。 | …SWA 缺失，则先重放前缀末尾窗口。两条路径随后都要构建本轮的解码器 SWA。第 8 章将用 8K 与 128K 上下文的教学算例，比较保存状态和重新执行的成本。 |
| 图 3-15 中，视觉编码改变了视觉 token 数和特征宽度；进入语言模型后，还会产生另一… | 图 3-15 与图 3-16 中，视觉编码改变了视觉 token 数和特征宽度；进入语言模型后，还会产生另一… |
| …** 设输入包含四张图片与 400 个文本 token，计算这些输入 token的 EC 与逻辑 KV。四张相同规格图片的 EC 为 31.25 MiB，视觉 … | …** 设输入包含四张图片与 400 个文本 token，计算这些输入 token 的 EC 与逻辑 KV。四张相同规格图片的 EC 为 31.25 MiB，视觉 … |
| 三种数据表示对应三个不同的交接位置。发送原图，接收端还要执行视觉编码；发送 EC，接收端从语言 prefill 开始；发送 KV，则交接已经完成的语言上下文。第八章安排同卡执行，第九章比较阶段交接，第十二章把相同数据流放到端、边、云链路上。 | 三种数据表示对应三个不同的交接位置。发送原图，接收方还要执行视觉编码；发送 EC，接收方从语言 prefill 开始；发送 KV，则交接已经完成的语言上下文。第 8 章安排同卡执行，第 9 章比较阶段交接，第 12 章把相同数据流放到端、边、云链路上。 |
| 保持 patch 与合并方式不变，将图片的高度和宽度都加倍，视觉 token 数变为四倍。因此 EC 和视觉 tok… | 保持 patch 与合并方式不变，把图片的高度和宽度都加倍，视觉 token 数变为四倍。因此 EC 和视觉 tok… |
| …输出还有帧内循环。以 Fish Audio 的语音生成模型 S2 Pro 为例，slow（慢速）路径每次推进一个音频帧，fast（快速）路径在帧内补齐多个码本的编号。码本是离散声音表示的候选向量集合，codec（编解码器）将这些编号还原为波形。每帧先用一次 fast 前向建立状态，再用九次预测补齐码本，因此生成一帧需要十次 fast 前向计算。由此可见，语言 token/s、声学帧/s 与每秒生成的音频时长，衡量… | …输出还有帧内循环。以 Fish Audio 的语音生成模型 S2 Pro 为例，慢速（slow）路径每次推进一个音频帧，快速（fast）路径在帧内补齐多个码本的编号。码本是离散声音表示的候选向量集合，编解码器（codec）把这些编号还原为波形。每帧先用一次快速路径前向建立状态，再用九次预测补齐码本，因此生成一帧需要十次快速路径前向计算。由此可见，语言 token/s、声学帧/s 与每秒生成的音频时长，衡量… |
| 图像生成又换了一种循环。latent 是压缩后的连续图像表示；去噪逐步把含噪表示变为目标图像。扩散 Transform… | 图像生成又换了一种循环。潜在表示（latent）是压缩后的连续图像表示；去噪逐步把含噪表示变为目标图像。扩散 Transform… |
| …gent 如果只在动作结束后观察屏幕，就可能漏掉中途出现又消失的弹窗。AOI 是研究持续观察与交互的工作，它将屏幕观测与动作执行分开：持续收集图像、音频和事件，再筛选出供模型使用的记录。增加… | …gent 如果只在动作结束后观察屏幕，就可能漏掉中途出现又消失的弹窗。AOI 是一项关于持续观察与交互的研究，它把屏幕观测与动作执行分开：持续收集图像、音频和事件，再筛选出供模型使用的记录。增加… |
| 每块采集结束后，模型处理耗时 12 ms、发送耗时 1 ms，通常再传播 5 ms。第一块在 38 ms 到达，在抖动缓冲区中等待 40 ms 后，于采集开始后的第 78 ms 播放。第三块的传播延迟被设为 50 ms，直到 123 ms 才到，错过 118 ms 的播放计划，造成… | 每块采集结束后，模型处理耗时 12 ms、发送耗时 1 ms，网络传播通常再花 5 ms。第一块在 38 ms 到达，在抖动缓冲区中等待 40 ms 后，于采集开始后的第 78 ms 播放。第三块的传播延迟设为 50 ms，直到 123 ms 才到，错过 118 ms 的播放计划，造成… |
| …合的多模态阶段。编码形成模型输入，语言模型生成回复，声学模块将回复转成音频，接收端缓冲与播放设备决定何时真正发声。](ch03/figure-3-4-realti… | …合的多模态阶段。编码形成模型输入，语言模型生成回复，声学模块将回复转成音频，接收方的缓冲与播放设备决定何时真正发声。](ch03/figure-3-4-realti… |
| …合的多模态阶段。编码形成模型输入，语言模型生成回复，声学模块将回复转成音频，接收端缓冲与播放设备决定何时真正发声。* | …合的多模态阶段。编码形成模型输入，语言模型生成回复，声学模块将回复转成音频，接收方的缓冲与播放设备决定何时真正发声。* |
| …与目标的差异来调整权重。先看最简单的一次乘法：输入 x 乘以权重 w 得到 y。输出端传回损失对 y 的敏感度后，要算损失对 w 的敏感度，就还需要当时的 x；要把敏… | …与目标的差异来调整权重。先看最简单的一次乘法：输入 x 乘以权重 w 得到 y。后一层传回损失对 y 的敏感度后，要算损失对 w 的敏感度，就还需要当时的 x；要把敏… |
| …训练批拆成更多微批次，会改变执行顺序与激活的生命周期，而总训练数据量保持不变。第十章将据此讨论分片和流水。 | …训练批拆成更多微批次，会改变执行顺序与激活的生命周期，而总训练数据量保持不变。第 10 章将据此讨论分片和流水。 |
| …算用完已准备的数据后，就必须等待新输入，取回加速器结果时又会等待相应计算完成。第五章将结合前向、反向和参数更新的执行顺序，分析数据准备与计算如何重叠。 | …算用完已准备的数据后，就必须等待新输入，取回加速器结果时又会等待相应计算完成。第 5 章将结合前向、反向和参数更新的执行顺序，分析数据准备与计算如何重叠。 |
| 把一次参数更新所需的完整批次拆成较小批次、逐次执行并累积梯度，这些较小批次称为微批次。训练阶段的名称不同，系统仍要回答同样的问题：输入有多少 token，哪些 tok… | 训练阶段的名称不同，系统仍要回答同样的问题：输入有多少 token，哪些 tok… |
| …275 增到 0.430 TFLOPs，增加约 56%。较长文档中的 token可以看到更多前文，所以在总行数不变时，上下文交互增加；投影与 FFN 仍处理同样… | …275 增到 0.430 TFLOPs，增加约 56%。较长文档中的 token 可以看到更多前文，所以在总行数不变时，上下文交互增加；投影与 FFN 仍处理同样… |
| SFT 中，模型读取的 token 与参与损失计算的 token往往不同。一条样本可以包含系统提示、用户问题、工具结果和回答，但训练只对回答部分… | SFT 中，模型读取的 token 与参与损失计算的 token 往往不同。一条样本可以包含系统提示、用户问题、工具结果和回答，但训练只对回答部分… |
| …路传播梯度，MoE 则要对实际选中的专家求梯度。[^v4-training] 第二章的计算图因而也能帮助我们判断：训练时要保存哪些中间结果，反向又要执行哪些运算。 | …路传播梯度，MoE 则要对实际选中的专家求梯度。[^v4-training] 第 2 章的计算图因而也能帮助我们判断：训练时要保存哪些中间结果，反向又要执行哪些运算。 |
| …更新只处理保留样本；保留比例下降会扩大前两个阶段，而不会按同样比例扩大更新批次。下一节将采用等长样本，给定具体的生成数量，比较这两种情况。 | …更新只处理保留样本；保留比例下降会扩大前两个阶段，而不会按同样比例扩大更新批次。第 3.5.3 节将采用等长样本，给定具体的生成数量，比较这两种情况。 |
| …值求平均，有效 token 较少的微批次中的 token 就可能获得更大权重。第十章将用具体训练方法解释这种差别。[^verl-loss] | …值求平均，有效 token 较少的微批次中的 token 就可能获得更大权重。第 10 章将用具体训练方法解释这种差别。[^verl-loss] |
| …成；异步方案允许部分阶段重叠，却要处理样本版本与滞后。本章先明确这一依赖关系，第十章再讨论交接、抢占、恢复和资源配比。 | …成；异步方案允许部分阶段重叠，却要处理样本版本与滞后。本章先明确这一依赖关系，第 10 章再讨论交接、抢占、恢复和资源配比。 |
| …估算各阶段的耗时，再按生成、反馈、更新和权重同步的顺序排列，才能得到完整周期。第四至第十章将逐步介绍相应硬件和调度方法。 | …估算各阶段的耗时，再按生成、反馈、更新和权重同步的顺序排列，才能得到完整周期。第 4 至 10 章将逐步介绍相应的硬件和调度方法。 |
| 在 $A,B,\alpha,\beta$ 均为正的拟合模型中，增加参数量会减小 $A/N^\alpha$，降低模型规模不足造成的损失；但总计算量固定时，可用于训练的数据量随之减少，$B/D^\beta$ 又会增大。令导数为零，就是寻找两种影响相抵的位置。由此得到的 $N_{\mathrm{opt}}$ 是连续取值的结果，实际设计时可… | 由此得到的 $N_{\mathrm{opt}}$ 是连续取值的结果，实际设计时可… |
| Qwen3 表 21 将特定 8B 模型的 RL 与 on-policy distillation 两条后训… | Qwen3 技术报告的表 21 把同一个 8B 模型的 RL 与 on-policy distillation 两条后训… |
| 第四章将结合芯片的算力、存储容量与带宽，分析这些负载如何在加速器上执行。本章算出的矩… | 第 4 章将结合芯片的算力、存储容量与带宽，分析这些负载如何在加速器上执行。本章算出的矩… |
| …固定训练计算量时，参数与训练 token 之间存在折中；若模型还要服务大量请求，对一个较小模型进行更充分的训练，可能用一次性训练投入换取持续较低的推理成本。Llama 3 报告说明，小模型会通过更长时间的… | …固定训练计算量时，参数与训练 token 之间存在折中；若模型还要服务大量请求，把一个较小的模型训练得更充分，就可能用一次性的训练投入换取持续较低的推理成本。Llama 3 报告说明，小模型会通过更长时间的… |
| [^model]: [第二章模型架构](02-模型架构.md)，§2.2 的请求调用约定和 Qwen3-8… | [^model]: [第 2 章模型架构](02-模型架构.md)，§2.2 的请求调用约定和 Qwen3-8… |
| …EADME.md)。本章用这一实验区分任务质量与参数更新，完整系统的组织方式见第十章。 | …EADME.md)。本章用这一实验区分任务质量与参数更新，完整系统的组织方式见第 10 章。 |

## 翻译腔审查（2026-09-11）

作者确认术语原则并统一“笔者”“N 个 token”“Queqiao”之后，再做一遍只针对翻译腔的审查（含图注里汉字数字改阿拉伯数字）。共 30 行有改动，按最终文本与审查前快照逐行对照。

| 修改前 | 修改后 |
|---|---|
| …稳定，后者则在不同时段分别需要更多输入处理能力或生成能力。智能体（Agent）是通过模型决定下一步操作、调用工具并根据结果继续工作的程序。它在模型与工具之间往返，等待工具结果时模型暂停生成，上下… | …稳定，后者则在不同时段分别需要更多输入处理能力或生成能力。智能体（Agent）是让模型决定下一步操作、调用工具，再根据结果继续工作的程序。它在模型与工具之间往返，等待工具结果时模型暂停生成，上下… |
| …B 状态持续占用空间 10 秒，对应 10 GiB·s。状态没有变大，但这段时间内无法将空间用于其他请求。若任务以平均每秒 $\lambda$ 个的速度持续到达，每个任务都占用 $M_… | …B 状态持续占用空间 10 秒，对应 10 GiB·s。状态没有变大，但这段时间里这块空间不能给其他请求用。若任务以平均每秒 $\lambda$ 个的速度持续到达，每个任务都占用 $M_… |
| ![1 GiB 状态持续占用空间十秒，对应面积为 10 GiB·s。横轴为状态驻留时间，纵轴为占用空间；面积描述这… | ![1 GiB 状态持续占用空间 10 秒，对应面积为 10 GiB·s。横轴为状态驻留时间，纵轴为占用空间；面积描述这… |
| *图 3-3　1 GiB 状态持续占用空间十秒，对应面积为 10 GiB·s。横轴为状态驻留时间，纵轴为占用空间；面积描述这… | *图 3-3　1 GiB 状态持续占用空间 10 秒，对应面积为 10 GiB·s。横轴为状态驻留时间，纵轴为占用空间；面积描述这… |
| …实际实例回放两种到达序列的结果。完整请求 p95 接近，首响应 p95 则相差约十六秒；模型、加速器、KV 池与并发上限均按正文实验条件固定。](ch03/figu… | …实际实例回放两种到达序列的结果。完整请求 p95 接近，首响应 p95 则相差约 16 秒；模型、加速器、KV 池与并发上限均按正文实验条件固定。](ch03/figu… |
| …实际实例回放两种到达序列的结果。完整请求 p95 接近，首响应 p95 则相差约十六秒；模型、加速器、KV 池与并发上限均按正文实验条件固定。* | …实际实例回放两种到达序列的结果。完整请求 p95 接近，首响应 p95 则相差约 16 秒；模型、加速器、KV 池与并发上限均按正文实验条件固定。* |
| 多轮对话的下一次请求通常带着上下文。第一轮通常输入系统提示和用户问题；第二轮除了新问题，还可能包含上一轮回答；之后每轮继续累积… | 多轮对话的下一次请求通常带着上下文。第一轮输入系统提示和用户问题；第二轮除了新问题，还可能包含上一轮回答；之后每轮继续累积… |
| …1.5 单位，成功率至少应达到多少，才能使每个成功任务的平均成本不高于旧方法？对于需要多次采样的任务，说明如何将生成、验证和失败尝试的开销计入总成本。 | …1.5 单位，成功率至少应达到多少，才能使每个成功任务的平均成本不高于旧方法？对需要多次采样的任务，说明生成、验证和失败尝试的开销应怎样计入总成本。 |
| 检索增强问答也包含类似的处理过程：检索器找到文档，再将文档交给语言模型生成答案。在达到同样回答质量的前提下，检索内容越少，后续 prefill、KV 存储和上下文读取的开销就越小。因此，… | 检索增强问答也包含类似的处理过程：检索器找到文档，再将文档交给语言模型生成答案。回答质量相同时，检索内容越少，后续 prefill、KV 存储和上下文读取的开销就越小。因此，… |
| …了其中 710 个用例，另 303 个仍共享了子列表。[^agent] 两组检查分别对应两种程序性质，解释了为什么任务完成标准必须具体到可执行的验证。 | …了其中 710 个用例，另 303 个仍共享了子列表。[^agent] 两组检查对应两种不同的程序性质，这说明任务的完成标准必须具体到可执行的验证。 |
| …这一段减半节省约 23.8% 的总时间；其余阶段仍合计耗时约 40.1 秒。优化收益由被缩短阶段的原有占比决定。[^agent-speedup] | …这一段减半节省约 23.8% 的总时间；其余阶段仍合计耗时约 40.1 秒。优化的收益取决于缩短的那一段原来占多大比例。[^agent-speedup] |
| ![两个工具存在前后依赖时的任务时间线。模型先运行两秒，工具 A 用六秒，工具 B 用十秒，模型最后运行三秒，总计二十一秒。](ch03/figure-3-tool-dependency.svg) | ![两个工具存在前后依赖时的任务时间线。模型先运行两秒，工具 A 用 6 秒，工具 B 用 10 秒，模型最后运行 3 秒，总计 21 秒。](ch03/figure-3-tool-dependency.svg) |
| *图 3-11　两个工具存在前后依赖时的任务时间线。模型先运行两秒，工具 A 用六秒，工具 B 用十秒，模型最后运行三秒，总计二十一秒。* | *图 3-11　两个工具存在前后依赖时的任务时间线。模型先运行两秒，工具 A 用 6 秒，工具 B 用 10 秒，模型最后运行 3 秒，总计 21 秒。* |
| ![两个工具独立时可以同时开始，模型在较慢的工具 B 完成后继续，任务共十五秒。与前图使用相同时间尺度；工具工作总量仍为十六秒。](ch03/figure-3-tool-parallel.svg) | ![两个工具独立时可以同时开始，模型在较慢的工具 B 完成后继续，任务共 15 秒。与前图使用相同时间尺度；工具工作总量仍为 16 秒。](ch03/figure-3-tool-parallel.svg) |
| … 3-12　两个工具独立时可以同时开始，模型在较慢的工具 B 完成后继续，任务共十五秒。与前图使用相同时间尺度；工具工作总量仍为十六秒。* | … 3-12　两个工具独立时可以同时开始，模型在较慢的工具 B 完成后继续，任务共 15 秒。与前图使用相同时间尺度；工具工作总量仍为 16 秒。* |
| …等待，驻留量按任务累加。工具执行时间越长，状态保留得越久，系统能同时处理的请求数也会受到影响，第 8、9 章将在这条时间线上安排缓存与交接。 | …等待，驻留量按任务累加。工具执行时间越长，状态保留得越久，系统能同时处理的请求数就越少。第 8、9 章将在这条时间线上安排缓存与交接。 |
| …v)$。步数加倍，运算量也随之加倍；分辨率提高会增加图像潜在表示中的空间位置数，从而增加单步内部的矩阵行数和位置间交互。文本生成不断追加上下文，每步访问量随之增长；图像去噪反复更新同一组 laten… | …v)$。步数加倍，运算量也随之加倍；分辨率提高会增加图像潜在表示中的空间位置数，单步内部的矩阵行数和位置间交互也随之增加。文本生成不断追加上下文，每步访问量随之增长；图像去噪反复更新同一组 laten… |
| …s。[^audio-real] 到达时间说明数据何时可用，播放时钟则决定音频何时被播放。下面用一条时间线说明两者之间的缓冲如何避免声音中断。 | …s。[^audio-real] 到达时间说明数据何时可用，播放时钟则决定音频何时播出。下面用一条时间线说明两者之间的缓冲如何避免声音中断。 |
| ![八块音频的教学播放时间线。每块长二十毫秒，圆点标到达，短竖线标原定播放时刻，色条标实际播放；第三块晚到五毫秒，后续播放随之顺延。](ch03/figure-3-audio-timing.s… | ![八块音频的教学播放时间线。每块长 20 ms，圆点标到达，短竖线标原定播放时刻，色条标实际播放；第三块晚到 5 ms，后续播放随之顺延。](ch03/figure-3-audio-timing.s… |
| *图 3-18　八块音频的教学播放时间线。每块长二十毫秒，圆点标到达，短竖线标原定播放时刻，色条标实际播放；第三块晚到五毫秒，后续播放随之顺延。* | *图 3-18　八块音频的教学播放时间线。每块长 20 ms，圆点标到达，短竖线标原定播放时刻，色条标实际播放；第三块晚到 5 ms，后续播放随之顺延。* |
| …目标回答继续训练模型，常把指令与回答放在一起输入，只对回答计算损失。估算资源时，需要把这些差异体现为输入形状、参与监督的 token 和更新频率。 | …目标回答继续训练模型，常把指令与回答放在一起输入，只对回答计算损失。估算资源时，要把这些差异落实为输入形状、参与监督的 token 数和更新频率。 |
| …402.591 TFLOPs 的粗算修正到 431.368 TFLOPs，需要先解释哪些工作被漏掉、哪些被多算。查询与上下文之间的矩阵乘法不对应一份模型参数，却在前反向中贡献 59.381 … | …402.591 TFLOPs 的粗算修正到 431.368 TFLOPs，需要先说明哪些工作漏算了、哪些多算了。查询与上下文之间的矩阵乘法不对应一份模型参数，却在前反向中贡献 59.381 … |
| …随输入行数增长，注意力随查询—键配对数增长，优化器更新随参数量和更新次数增长。若通过重计算减少保存的激活，容量下降，但被重做的前向运算还要加回 $F_{\mathrm{train}}$。这些变化都能沿… | …随输入行数增长，注意力随查询—键配对数增长，优化器更新随参数量和更新次数增长。若用重计算减少保存的激活，容量需求下降，但重做的前向运算还要加回 $F_{\mathrm{train}}$。这些变化都能沿… |
| > 从 $Y=XW$ 推导输入梯度和权重梯度的尺寸，说明三次矩阵乘法为何具有相同的运算量。对 Qwen3-8B，按每参数 18 字节计算长期训练状态的总容量，再将梯度改… | > 从 $Y=XW$ 推导输入梯度和权重梯度的尺寸，说明三次矩阵乘法的运算量为何相同。对 Qwen3-8B，按每参数 18 字节计算长期训练状态的总容量，再将梯度改… |
| …近似满足 $C=6ND$。增加 $N$ 就必须减少 $D$。要在两者之间作选择，需要知道它们分别怎样影响质量，Scaling Law（规模规律）用实验拟合模型规模、训练数据和损失之间的关系。… | …近似满足 $C=6ND$。增加 $N$ 就必须减少 $D$。要在两者之间作选择，就要知道它们分别怎样影响质量。Scaling Law（规模规律）用实验拟合模型规模、训练数据和损失之间的关系。… |
| 训练本身也存在波动。同样的数据与模型规模，改变随机初始化就可能得到不同的学习曲线；较大模型在训… | 训练本身也有波动。同样的数据与模型规模，改变随机初始化就可能得到不同的学习曲线；较大模型在训… |
| 长期服务会把每次调用的成本累积到训练预算之上。较小模型可以通过更长训练达到目标质量，前期投入更多成本，之后每次调用读取的权重更少、执行的运算也更少。Beyond Chinchill… | 长期服务会把每次调用的成本累积到训练预算之上。较小模型可以靠更长的训练达到目标质量：前期投入更多，之后每次调用读取的权重更少、执行的运算也更少。Beyond Chinchill… |
| GPU 小时衡量加速器数量与使用时间的乘积。若持续使用 $n_{\mathrm{GPU}}$ 张… | GPU 小时是加速器数量与使用时间的乘积。若持续使用 $n_{\mathrm{GPU}}$ 张… |
| …与长期服务的最优规模，也可能不同。固定训练计算量时，参数与训练 token 之间存在折中；若模型还要服务大量请求，把一个较小的模型训练得更充分，就可能用一次性的训练投入… | …与长期服务的最优规模，也可能不同。固定训练计算量时，参数与训练 token 之间要取舍；若模型还要服务大量请求，把一个较小的模型训练得更充分，就可能用一次性的训练投入… |
| …询—键配对的数量，标签与词表头策略改变输出工作，更新次数改变优化器成本；RL 还包含未被采用的回答的生成与反馈。 | …询—键配对的数量，标签与词表头策略改变输出工作，更新次数改变优化器成本；RL 还要为没有采用的回答付出生成与反馈的开销。 |
