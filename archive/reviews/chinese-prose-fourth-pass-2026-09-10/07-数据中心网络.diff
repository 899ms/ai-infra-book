--- before/07-数据中心网络.md
+++ manuscripts/07-数据中心网络.md
@@ -352,7 +352,7 @@
 
 > **实验 7-5 · 延伸：用满远程读取带宽需要多少在途事务**
 >
-> 将例题 7.3 中槽位从发出请求到可以复用的时间从 2 μs 增为 4 μs，分别求每次传输 256 B 和 4 KiB 时，维持目标带宽所需的在途事务数。固定 100 ns 启动间隔，求达到 40 GB/s 所需的最小事务大小。最后将独立地址改成指针链，解释完成整条指针链的读取所需时间如何随链长增长。
+> 在例题 7.3 中，将槽位的占用时间从 2 μs 增为 4 μs；该时间从请求发出算起，到槽位可以复用为止。分别求每次传输 256 B 和 4 KiB 时，维持目标带宽所需的在途事务数。固定 100 ns 启动间隔，求达到 40 GB/s 所需的最小事务大小。最后将独立地址改成指针链，解释完成整条指针链的读取所需时间如何随链长增长。
 
 ## 7.4 数据交接、顺序与状态管理
 
@@ -435,7 +435,7 @@
 
 再假设所有操作共用一个处理单元：从 A 开始写入到 B 完成通知，这个单元一直被占用，C 只能随后执行。此时资源使用本身增加了发布链到 C 的顺序边，两种安排都到 112 μs 完成。程序依赖和资源依赖因此要画在同一张图里：减少程序中的顺序约束后，资源约束仍会限制并行程度。
 
-> **设计背景：为什么全局顺序很有吸引力？**
+> **设计背景：全局顺序如何简化复制与协作？**
 >
 > 笔者在 1Pipe 研究中探索过由网络提供全局全序的设计。所有参与者按同一顺序观察操作，上层的复制和协作就能使用更简单的顺序模型。进一步处理故障时，顺序与交付需要分别解决：发送者在部分发送后退出，系统要确定哪些节点已收到；接收者永久退出，系统要决定哪些工作继续执行。这些问题推动我们把顺序保证和交付、恢复机制分开，并把应用真正需要的依赖清楚地表达出来。
 
@@ -519,7 +519,7 @@
 
 **链路流控**让相邻接收端在缓冲不足时暂停上游，防止接收端缓冲区溢出。**端到端拥塞控制**把瓶颈信息送回发送者，降低发送速率。前者迅速阻止局部溢出，后者让源端的发送速率适应整条路径的传输能力。
 
-沿用 80 GB/s 到达与 50 GB/s 出口，设缓冲总量为 512 KiB，已经占用 256 KiB。每微秒多到达的 30 KB 数据会占用剩余空间，只能支撑约 8.7 μs。反馈若到 20 μs 才使发送减速，会新产生 600 KB 超额数据，扣除剩余空间，约 338 KB 被丢弃。[^feedback]
+沿用 80 GB/s 到达与 50 GB/s 出口，设缓冲总量为 512 KiB，已经占用 256 KiB。每微秒多到达的 30 KB 数据会占用剩余空间，这些空间只能支撑约 8.7 μs。反馈若到 20 μs 才使发送减速，会新产生 600 KB 超额数据，扣除剩余空间，约 338 KB 被丢弃。[^feedback]
 
 将剩余缓冲记为 $Q_{\mathrm{free}}$，从发现拥塞到降速的时延为 $T_f$，避免这次溢出需要
 
@@ -547,7 +547,7 @@
 
 ### 7.5.3 多路径与重传
 
-反馈控制通过减少发送速率缓解拥塞；如果其他路径尚有带宽，还可以把流量分过去。Clos 网络提供多条可选路径。将不同连接分散到不同路径，可以分散热点链路的流量；将同一条消息中的报文分到多条路径，可以同时利用多条链路的带宽。后一种做法也把不同路径的时延差带到了接收端。
+反馈控制通过减少发送速率缓解拥塞；如果其他路径尚有带宽，还可以把流量分过去。Clos 网络提供多条可选路径。将不同连接分散到不同路径，可以分散原本集中在热点链路上的流量；将同一条消息中的报文分到多条路径，可以同时利用多条链路的带宽。后一种做法也把不同路径的时延差带到了接收端。
 
 取一个 8 KiB 隐藏向量，拆成八个 1 KiB 报文，轮转到两条独立的 1 GB/s 路径。每条发送四个报文，约需 4.1 μs。两条路径的传播时延都为 1 μs 时，整条消息约在 5.1 μs 交付；同样八个报文只走一条路径则约需 9.2 μs。两条路径各承担了一半发送工作，并行完成传输。[^packets]
 
@@ -573,7 +573,7 @@
 
 > **实验 7-9 · 延伸：路径时延差与重传如何推迟按序交付**
 >
-> 将第二条路径的传播时延从 1 μs 扫描到 9 μs，求均分到两条路径与仅使用第一条路径的完成时间相等时，第二条路径的传播时延。再令两条路径分别承担五个和三个报文，求第一条路径传播时延为 1 μs、第二条为 9 μs 时的完成时间。保持首包丢失，把恢复等待从 20 μs 增为 40 μs，计算首次能够按序交付数据的时刻，以及此前需要缓存的数据量。
+> 让第二条路径的传播时延在 1 μs 至 9 μs 之间变化，求均分到两条路径与仅使用第一条路径的完成时间相等时，第二条路径的传播时延。再令两条路径分别承担五个和三个报文，求第一条路径传播时延为 1 μs、第二条为 9 μs 时的完成时间。保持首包丢失，把恢复等待从 20 μs 增为 40 μs，计算首次能够按序交付数据的时刻，以及此前需要缓存的数据量。
 
 ### 7.5.4 死锁
 
@@ -593,13 +593,13 @@
 
 虚拟通道通过逻辑上分开的队列实现资源划分。把请求与响应分入不同通道，再规定资源申请方向，可以建立无环的依赖结构。这里真正发挥作用的是独立容量和使用次序。
 
-内存访问还可能需要进行地址转换、读取页表。原请求等待地址转换，地址转换又要发起一次远程访问，二者若争用已耗尽的同一组槽位，也会形成相同的环。笔者参与 UB 设计时，需要一并分析事务处理、内存访问和链路传输之间的依赖。 为完成通知、地址转换和故障恢复预留资源，才能避免系统在拥塞或故障时陷入停滞。
+内存访问还可能需要进行地址转换、读取页表。原请求等待地址转换，地址转换又要发起一次远程访问，二者若争用已耗尽的同一组槽位，也会形成相同的环。笔者参与 UB 设计时，需要一并分析事务处理、内存访问和链路传输之间的依赖。为完成通知、地址转换和故障恢复预留资源，才能避免系统在拥塞或故障时陷入停滞。
 
 ## 7.6 从通信改进到任务完成
 
 ### 7.6.1 关键路径从数据就绪开始
 
-前面沿一份数据追踪了传输、等待、完成通知和空间释放。要判断这些改进能节省多少训练时间，还必须结合产生和使用这份数据的计算过程来分析。到这里，我们已经减少了归约需要传输的数据量，配置了足够的并发请求，并让请求记录与数据缓冲区可以再次使用。最后还要确定通信在什么时候开始。某张卡进入集合调用很早，另一张卡的梯度尚未生成，前者会在调用内部等待后者。这段时间出现在通信接口中，来源却是前面的计算或数据准备。
+前面沿一份数据追踪了传输、等待、完成通知和空间释放。要判断这些改进能节省多少训练时间，还必须结合产生和使用这份数据的计算过程来分析。到这里，我们已经减少了归约需要传输的数据量，配置了足够的并发请求，并让请求记录与数据缓冲区可以再次使用。最后还要确定通信在什么时候开始。某张卡进入集合调用很早，另一张卡的梯度尚未生成，前者会在调用内部等待后者。这段等待被计入通信调用的耗时，来源却是前面的计算或数据准备。
 
 考虑四个需要进行本地归约的参与者，分别在 0、0、0、2 ms 就绪，全部到齐后执行 0.4 ms 的交换。按第 7.4 节的依赖图，交换的开始时刻等于四个就绪时刻中的最大值，全组在 2.4 ms 完成。将交换耗时减半，完成时间变为 2.2 ms；如果四个参与者都在起点准备好，完成时间就变为 0.4 ms。两个优化分别作用于依赖图上的不同节点。[^tail]
 
@@ -667,7 +667,7 @@
 
 连续环约为 27.8 ms，分层归约约为 25.6 ms。若数据更早在 12 ms 就绪，分层通信约在 18.6 ms 结束，早于 20 ms 的计算结束时刻，整步只需 22 ms。继续缩短通信不再改变这条关键路径，此时需要缩短计算或更新，才能进一步减少总时间。
 
-**讨论：出口带宽下降后，归约是否重新成为训练步的瓶颈？** 将共享出口从 40 GB/s 降至 20 GB/s，分层方案的远端传输时间增加一倍，通信约为 11.6 ms。数据在 17 ms 就绪时，整步约为 30.6 ms。原本可以与计算重叠的通信，现在延迟到了计算结束之后。流量模型计算传输时间，并发模型判断实际吞吐能否达到链路带宽，依赖模型据此计算训练步耗时。
+**讨论：出口带宽下降后，归约是否重新成为训练步的瓶颈？** 将共享出口从 40 GB/s 降至 20 GB/s，分层方案的远端传输时间增加一倍，通信约为 11.6 ms。数据在 17 ms 就绪时，整步约为 30.6 ms。通信耗时增加后，其中更多部分要到计算结束后才能完成。流量模型计算传输时间，并发模型判断实际吞吐能否达到链路带宽，依赖模型据此计算训练步耗时。
 
 再按顺序比较这些时间线：减少通信量会缩短蓝色条带，并发不足会把它拉长，数据提早就绪则让它向左移动。绿色更新要等计算和通信都结束后才能开始。这三个模型也给出了清楚的改进顺序。先通过分层归约减少跨服务器传输，再通过并发提交和处理请求，充分利用链路带宽；达到带宽上限之后，寻找可以提早开始的通信；通信时间被计算时间完全覆盖后，优化此时决定完成时间的计算或更新操作。
 
@@ -707,7 +707,7 @@
 
 大梯度、小消息和故障恢复分别突出了带宽、启动与依赖等待。部署方案的比较也应从这些工作负载出发。**InfiniBand** 是支持低时延消息和 RDMA 的交换互联体系；**RoCE** 将 RDMA 承载在以太网上；**UB** 是前文讨论的统一互联体系。它们分别提供具体的连接与通信机制。选择部署方案时，先用本章的三个模型比较各部署方案的通信路径。GPU 到 NIC 的连接、共享接口和交换拓扑决定可用带宽；请求提交与完成处理机制决定实际吞吐；应用的同步与恢复机制决定执行顺序。
 
-计算可用带宽时，需要先明确物理端口的连接方式。例如，昇腾 950 中承载于以太网的 UB 通信（UBoE）与 UB Link 链路按预先配置的方式共用串行器／解串器（SerDes，连接芯片内部并行数据与链路串行信号的电路），一组 SerDes 的用途由端口配置确定。选择端口用途之后，才能计算该方向实际拥有的链路容量。[^ubspec] 接口统一使程序使用共同的访问方式，端口配置则决定数据经过哪些链路，以及这些链路有多少带宽。
+计算可用带宽时，需要先明确物理端口的连接方式。例如，昇腾 950 中承载于以太网的 UB 通信（UBoE）与 UB Link 链路按预先配置的方式共用串行器／解串器（SerDes，连接芯片内部并行数据与链路串行信号的电路），一组 SerDes 的用途由端口配置确定。选择端口用途之后，才能计算该方向实际可用的链路带宽。[^ubspec] 接口统一使程序使用共同的访问方式，端口配置则决定数据经过哪些链路，以及这些链路有多少带宽。
 
 公开 AllReduce 运行记录可以用来分析这些因素的共同影响。16 GiB 输入、输入与输出分别使用不同缓冲区时，两台八卡 H100 服务器约需 68.7 ms，四台约需 91.0 ms。[^allreduce] 增加服务器带来了更多参与者和更长的协作路径。
 
@@ -727,7 +727,7 @@
 >
 > 复算例题 7.6，画出连续环、分层归约、在途请求数不足、提前通信与出口退化的整步时间线。求分层通信恰好被 20 ms 计算覆盖时的数据最晚就绪时刻。再换成 36 层、每层两次 8 KiB 归约，分别考虑将带宽提高到原来的三倍，以及将每次启动开销降至 2 μs，解释两类负载的改进顺序为何不同。
 
-[^capacity]: 模型规模与状态大小的计算方法见[模型资源计算](../case-studies/model-resource-accounting.md)及[第七章扩写资料](../outlines/extensions/07-数据中心网络.md#detail-7.1)。混合精度 Adam 算例按每参数 16 bytes 保存训练状态；万亿参数推理例子按每参数 0.5 byte 计算权重容量。
+[^capacity]: 模型规模与状态大小的计算方法见[模型资源计算](../case-studies/model-resource-accounting.md)及[第七章扩写资料](../archive/outlines/extensions/07-数据中心网络.md#detail-7.1)。混合精度 Adam 算例按每参数 16 bytes 保存训练状态；万亿参数推理例子按每参数 0.5 byte 计算权重容量。
 
 [^ub]: [UB 规范与操作系统参考设计核对记录](../references/UB-ASCEND-NOTES.md)。
 
