# 第 10 章语言通读：逐句修改对照

本文件记录 2026-09-11 全书通读在本章留下的净改动。

## 全书通读（2026-09-11，按最终文本与通读前快照逐行对照）

共 97 行有改动；英文术语按“首次出现保留中文注释、之后用英文”的原则已回退，下表是回退之后的净改动。省略号表示只截取了改动附近的文字。

| 修改前 | 修改后 |
|---|---|
| …整参数更新的优化器。Qwen3-8B 的 BF16 权重约为 16.4 GB，而采用混合精度 Adam 进行全参数训练时，系统除模型权重外，还需保存梯度（损失对参数的导数）、主权重（用于高… | …整参数更新的优化器。Qwen3-8B 的 BF16 权重约为 16.4 GB，而用混合精度 Adam 做全参数训练时，系统除模型权重外，还需保存梯度（损失对参数的导数）、主权重（用于高… |
| …向传播由输入得到预测，反向传播沿运算依赖求导；求导时还要读取前向阶段保留的激活，即中间计算结果，执行期间还需要用于参数聚合和格式转换的缓冲区。设计训练系统，首先要确定这些张量和缓冲区存放在哪里，以及需要保留多久。 | …向传播由输入得到预测，反向传播沿运算依赖求导；求导时还要读取前向阶段保留的激活，也就是中间计算结果；执行期间还需要参数收集和格式转换用的缓冲区。设计训练系统，首先要确定这些张量和缓冲区存放在哪里，以及需要保留多久。 |
| 把训练状态分到更多加速器上，可以减轻单卡的显存压力，但计算往往需要等待其他加速器传来的数据。把状态移到主机内存，可以腾出显存，却又增加了主机与 GPU 之间的传… | 把训练状态分到更多卡上，可以减轻单卡的显存压力，但计算往往要等其他卡传来的数据。把状态移到主机内存，可以腾出显存，却又增加了主机与 GPU 之间的传… |
| …等到相应的反向计算结束后才能释放，而参数收集缓冲区可能只在某个模块执行期间使用。加速器 $i$ 的显存峰值取决于同一时刻驻留的训练状态、激活和临时缓冲区： | …等到相应的反向计算结束后才能释放，而参数收集缓冲区可能只在某个模块执行期间使用。第 $i$ 张卡的显存峰值取决于同一时刻驻留的训练状态、激活和临时缓冲区： |
| 设算法所需的总计算量为 $F$，加速器数为 $p$，单卡峰值为 $P$，在所统计的时间内，平均计算效率为 $\eta$，则 | 设算法所需的总计算量为 $F$，卡数为 $p$，单卡峰值为 $P$，统计时段内的平均计算效率为 $\eta$，则 |
| 模型 FLOPs 利用率（MFU）等于模型所需的浮点运算量，除以全部加速器的峰值算力与实际耗时的乘积。若实际耗时取完整的训练迭代时间，通信等待已经计入其中；若只统计加速器执行计算的时间，得到的就是单卡计算效率。本章的设计案例采用后一种分解：假定单卡计算效率为 40%，随后把无法与加速器内计算重叠的通信时间，以及等待输入数据的时间逐项加上。这样可以直接观察每项系统设… | 模型 FLOPs 利用率（MFU）等于模型所需的浮点运算量，除以全部卡的峰值算力与实际耗时的乘积。若实际耗时取完整的训练迭代时间，通信等待已经计入其中；若只统计加速器执行计算的时间，得到的就是单卡计算效率。本章的设计案例采用后一种分解：假定单卡计算效率为 40%，随后把无法与卡内计算重叠的通信时间，以及等待输入数据的时间逐项加上。这样可以直接观察每项系统设… |
| ![容量下界与不同效率下的加速器数](ch10/figure-10-2-budget.svg) | ![容量下界与不同效率下的卡数](ch10/figure-10-2-budget.svg) |
| *图 10-3：矩阵计算效率为 30% 时的必要加速器数。任务为 Qwen3-8B 处理 100B token，序列长 8192，全部… | *图 10-3：矩阵计算效率为 30% 时所需的卡数。任务为 Qwen3-8B 处理 100B token，序列长 8192，全部… |
| ![矩阵计算效率提高到 40%，同一任务所需加速器数下降；橙色菱形仍表示状态容量下限](ch10/figure-10-budget-1.svg) | ![矩阵计算效率提高到 40% 时所需的卡数](ch10/figure-10-budget-1.svg) |
| *图 10-4：矩阵计算效率提高到 40%，同一任务所需加速器数下降；橙色菱形仍表示状态容量下限。各下限均以加速器张数计量；容量项表示显存约束要求的最少张数。* | *图 10-4：矩阵计算效率提高到 40%，同一任务所需的卡数下降；橙色菱形仍表示状态容量下限。各下限均以卡数计量；容量项是显存约束要求的最少卡数。* |
| ![矩阵计算效率为 50% 时的加速器数](ch10/figure-10-budget-2.svg) | ![矩阵计算效率为 50% 时所需的卡数](ch10/figure-10-budget-2.svg) |
| *图 10-5：矩阵计算效率为 50% 时的加速器数。三图使用相同的加速器顺序、纵轴范围、任务与 30 天执行期限。各下限均以加速器张数计量；容量项表示显存约束要求的最少张数。* | *图 10-5：矩阵计算效率为 50% 时所需的卡数。三图使用相同的型号顺序、纵轴范围、任务与 30 天执行期限。各下限均以卡数计量；容量项是显存约束要求的最少卡数。* |
| …12 个和 8 个单序列微批次，共同完成一个包含 384 条序列的全局批次。增加加速器改变了工作分配，训练目标保持相同。 | …12 个和 8 个单序列微批次，共同完成一个包含 384 条序列的全局批次。增加卡数改变了工作分配，训练目标保持不变。 |
| 同步数据并行（DP）让不同加速器处理不同样本，再用归约后的梯度更新各自的参数。由于各加速器得到的新参数相同，各副本通常保存相同的权重与优化器状态。加速器数越多，复制的状态越多。微软研究者在 2019 年提出、于 2020 年发表的 ZeRO（Zero Redundancy Optimizer，零冗余优化器），针对大模型训练中状态副本占用大量显存的问题，通过跨加速器分片减少重复保存。它分阶段划分这些状态的所有权：某张卡只负责一部分参数的更新计算，其他卡通过通信获得执行所需的数据。 | 同步数据并行（DP）让不同的卡处理不同的样本，再用归约后的梯度更新各自的参数。各卡得到的新参数相同，因此每张卡通常都保存一份相同的权重与优化器状态；卡越多，复制的状态越多。微软研究者在 2019 年提出、于 2020 年发表的 ZeRO（Zero Redundancy Optimizer，零冗余优化器），针对大模型训练中状态副本占用大量显存的问题，通过跨卡分片减少重复保存。它分阶段划分这些状态的归属：每张卡只负责一部分参数的更新，其他卡通过通信取得执行所需的数据。 |
| …rallel，FSDP）围绕参数和梯度分片组织执行。下面先用四张卡逐步改变状态的所有权，再分析这种执行方式。每列始终对应同一张卡；“完整”表示该卡保存整份状态，“片”表示只保存该卡负责的四… | …rallel，FSDP）围绕参数和梯度分片组织执行。下面先用四张卡逐步改变状态的归属，再分析这种执行方式。图中每列始终对应同一张卡；“完整”表示该卡保存整份状态，“片”表示只保存该卡负责的四… |
| 前两个阶段仍为每张卡保留完整模型权重。图 10-9 再划分权重的所有权，使长期驻留空间进一步下降，同时要求执行模块前通过通信取得所需权重。 | 前两个阶段仍为每张卡保留完整模型权重。图 10-9 再划分权重的归属，长期驻留的空间进一步下降，代价是执行每个模块前要通过通信取得所需的权重。 |
| …执行：先通过全收集（AllGather）取得所需参数，执行前向或反向，再通过归约散播（ReduceScatter）汇总梯度并分配梯度分片，使负责该分片的加速器得到完整梯度贡献，最后各自更新所负责的参数。FSDP 的全分片执行也围绕这条路径组织。若… | …执行：先通过全收集（AllGather）取得所需参数，执行前向或反向，再通过归约后分散（ReduceScatter）汇总梯度并分配梯度分片，让负责该分片的卡得到全部梯度贡献，最后各自更新所负责的参数。FSDP 的全分片执行也围绕这条路径组织。若… |
| 一组使用 PyTorch 第二代 FSDP 实现（FSDP2）的 CPU 小模型实验将参与者从两个增加到四个，参数更新后保留的状态从约 18 MiB 减到 9 MiB，而执行峰值从约 39 MiB 减到 30 MiB。长期保存的部分减半，峰值只减少约 23%… | 一组用 PyTorch 第二代 FSDP 实现（FSDP2）做的 CPU 小模型实验，把参与的进程从两个增加到四个：参数更新后保留的状态从约 18 MiB 减到 9 MiB，执行峰值却只从约 39 MiB 减到 30 MiB。长期保存的部分减半，峰值只减少约 23%… |
| …要在使用前重新算出。参数分片已经减少了权重和优化器状态的复制，**激活重计算**进一步通过额外计算减少保存这些中间值所需的空间。 | …要在使用前重新算出。参数分片已经减少了权重和优化器状态的复制，**激活重计算**则用额外的计算换取保存这些中间值所需的空间。 |
| 通用矩阵乘法记作 GEMM。原方案为这些 GEMM 保存的输入每层共占 15 MiB，其中上述三个乘积占 10 MiB，其余 Q、K… | 原方案为这些通用矩阵乘法（GEMM）保存的输入每层共占 15 MiB，其中上述三个乘积占 10 MiB，其余 Q、K… |
| …等待反向计算的微批次数量又会下降。因此，重计算与流水调度共同决定需要保存多少数据，新增计算是否延长每步训练时间，则取决于它在执行时间线上的位置。 | …等待反向计算的微批次数量又会下降。因此，重计算与流水调度共同决定需要保存多少数据；新增的计算是否延长每步训练时间，则取决于它在时间线上的位置。 |
| 当利用已保存结果重建数据的开销较小时，适合采用重计算。另一种办法是保留数据本身，把它移到容量更大的主机内存，使用前再传回 GPU，这就是**卸载**。选择卸载以后，原先的显存问题就变成了传输能否及时完成的问题。假设一份状态大小为 $V$，链路有效带宽为 $B$，从开始传输到算子需要这些数据之间有 $W$ 秒可用，则超出这段可用时间的传输耗时为 | 当用已保存的结果重建数据的开销较小时，适合重计算。另一种办法是保留数据本身，把它移到容量更大的主机内存，使用前再传回 GPU，这就是**卸载**。选择卸载以后，原先的显存问题就变成了传输能否及时完成的问题。假设一份状态大小为 $V$，链路有效带宽为 $B$，从可以开始传输到算子需要这些数据之间有 $W$ 秒，则超出这段时间的传输耗时为 |
| …（gate）的输出经过激活函数后，用于调节前馈子网络中另一支投影的对应元素。一个 gate 梯度形状为 $[12288,4096]$，BF16 大小 $G=96$ MiB，… | …（gate）的输出经过激活函数后，用于调节前馈子网络中另一支投影的对应元素。一个门控投影的梯度形状为 $[12288,4096]$，BF16 大小 $G=96$ MiB，… |
| 图 10-14 把差别集中在跨越链路的箭头上：GPU 虽然转换更快，却要多传 96 MiB。链路越… | 图 10-14 与图 10-15 的差别集中在跨越链路的箭头上：GPU 虽然转换更快，却要多传 96 MiB。链路越… |
| 存储容量还带来另一项限制。GPU 转换期间原始 96 MiB 与输出 192 MiB 共存，峰值为 288 MiB；先传 BF16 的路径则只需在 GPU 上保留参与这次转换的 96 MiB 梯度。选择 GPU 转换需要额外 192 MiB 缓冲区。SuperOffload 将转换位置与主机优化器执行共同安排，利用的正是转换速度、传输数据量和缓冲区的使用时间之间的取舍。整步收益则由最后一桶梯度、CPU 上的参数更新和权重回传何时… | 显存容量还带来另一项限制。GPU 转换期间原始 96 MiB 与输出 192 MiB 共存，峰值为 288 MiB；先传 BF16 的路径则只需在 GPU 上保留参与这次转换的 96 MiB 梯度。选择 GPU 转换需要额外 192 MiB 缓冲区。SuperOffload 把转换位置和主机上的优化器执行一起安排，利用的正是转换速度、传输数据量和缓冲区使用时间之间的取舍。整步收益则由最后一桶梯度、CPU 上的参数更新和权重回传何时… |
| …别改变存储量、计算量和传输量。把这些办法组合成完整系统时，还要确定它们作用于哪些加速器和通信组。第 6、7 章的并行方法在训练中划分不同的数据和计算：张量并行（TP）切分层内矩阵，流水线并行（PP）划分层，数据并行（DP）划分样本，专家并行（EP）划分专家。第 6.1.4 节的一览表还包括序列并行（SP）与上下文并行（CP）：前者让特定逐 token 算子的激活沿序列分片，常复用 TP 组；后者把同一长序列的注意力上下文分到多卡，保留跨位置依赖。它们延续第 5 章“分块—安排输入—汇合结果”的方法，训练再沿相同计算图逆… | …别改变存储量、计算量和传输量。把这些办法组合成完整系统时，还要确定它们作用于哪些卡和通信组。第 6、7 章的并行方法在训练中划分不同的数据和计算：张量并行（TP）切分层内矩阵，流水线并行（PP）划分层，数据并行（DP）划分样本，专家并行（EP）划分专家。第 6.1.4 节的一览表还包括序列并行（SP）与上下文并行（CP）：前者让逐 token 算子的激活沿序列分片，通常复用张量并行组；后者把同一条长序列的注意力上下文分到多张卡，保留跨位置的依赖。它们延续第 5 章“分块—安排输入—汇合结果”的方法，训练再沿相同计算图逆… |
| **例：显存不足时，应增加分片加速器还是重计算激活？** 取每卡 24 GiB 可用显存，Qwen3-8B ZeRO… | **例：显存不足时，应增加分片的卡数还是重计算激活？** 取每卡 24 GiB 可用显存，Qwen3-8B ZeRO… |
| 这两个选择对应不同的代价：八卡方案增加重建计算，十六卡方案增加加速器并改变通信组。可以先从图中找出满足显存要求的方案，再比较这些方案的每步耗时。 | 这两个选择对应不同的代价：八卡方案增加重建计算，十六卡方案增加卡数并改变通信组。可以先从图中找出满足显存要求的方案，再比较这些方案的每步耗时。 |
| …与 48 卡方案均采用全组 ZeRO-3，TP=PP=1，每卡微批次为一条序列。为加速器运行时预留空间后，教学方案给每卡 22 GiB 可用显存，并为同时保留的激活、完整模块参数… | …与 48 卡方案均采用全组 ZeRO-3，TP=PP=1，每卡微批次为一条序列。扣除运行时占用的空间后，教学方案给每卡 22 GiB 可用显存，并为同时保留的激活、完整模块参数… |
| …只处理一条，每张卡执行的矩阵形状相同；全局有效 token 数和目标函数也相同。较多加速器减少每卡承担的微批次数量，使单卡计算时间从约 78.3 s 降到 52.2 s。参数收集和梯度通信则随执行路… | …只处理一条，每张卡执行的矩阵形状相同；全局有效 token 数和目标函数也相同。卡越多，每卡承担的微批次越少，单卡计算时间从约 78.3 s 降到 52.2 s。参数收集和梯度通信则随执行路… |
| 式中的 $m$ 项对应微批次的实际计算工作，$p-1$ 项来自填充和排空。加速器时间利用率 $u$ 因而随微批次数量增加而提高。取 $p=4,m=8$，得到 $… | 式中的 $m$ 项对应微批次的实际计算工作，$p-1$ 项来自填充和排空。卡的时间利用率 $u$ 因而随微批次数量增加而提高。取 $p=4,m=8$，得到 $… |
| 1F1B 更早开始反向，也使后续前向等待反向释放计算资源。在阶段 0，前四个微批次于 40 ms 完成前向，第一份传回的梯度于 106 ms 到达。随后反向执行到 126 ms，第五个微批次才开始前向。这种交错逐级影响下游到达时间。阶段 3 在 93 ms 完成第二个微批次反向，但第三个前向输入到 9… | 1F1B 更早开始反向，后续的前向也就要等反向释放计算资源。在阶段 0，前四个微批次于 40 ms 完成前向，第一份传回的梯度于 106 ms 到达。随后反向执行到 126 ms，第五个微批次才开始前向。这种交错逐级影响下游的到达时间。阶段 3 在 93 ms 完成第二个微批次反向，但第三个前向输入到 9… |
| …冲区的最大显存占用从约 2.57 GB 降到 0.97 GB，减少约 62%。若为这些中间结果和缓冲区只留 1 GB，提前反向使这个训练步能够执行；容量充裕时，337 ms 的调度完成得更早。选择哪种调度方式，由“何时释放张量”和“何时得到下一份输入”共同… | …冲区的最大显存占用从约 2.57 GB 降到 0.97 GB，减少约 62%。若只给这些中间结果和缓冲区留 1 GB，只有提前反向的调度能执行这个训练步；容量充裕时，337 ms 的填满排空调度完成得更早。选择哪种调度方式，由“何时释放张量”和“何时得到下一份输入”共同… |
| 设归约需 3 ms，就绪后尚有 5 ms 独立计算，归约可以与这段计算同时进行。若共享链路此前已安排 4 ms 的专家交换，归约只能利用余下 1 ms，另有 2 ms 延伸到计算之后。这部分无法与计算重叠、会延长训练步骤的时间，称为**通信等待时间**。数据依赖给出最早开始时刻，资源争用将它推迟，后续算子的依赖关系决定等待是否延长整个训练步骤。 | 设归约需要 3 ms，梯度就绪后还有 5 ms 与它无关的计算，归约可以与这段计算同时进行。若共享链路此前已安排 4 ms 的专家交换，归约只能利用余下 1 ms，另有 2 ms 延伸到计算之后。这部分无法与计算重叠、会延长训练步的时间，称为**通信等待时间**。数据依赖给出最早开始时刻，资源争用把它推迟，后续算子的依赖关系决定这段等待是否延长整个训练步。 |
| …固定了梯度产生的时刻。实际训练还可以改变计算顺序，让某些梯度更早产生，从而把归约向左移动。矩阵反向中，输入梯度 $dX$ 使前一层继续反向，参数梯度 $dW$ 使本层开… | …固定了梯度产生的时刻。实际训练还可以改变计算顺序，让某些梯度更早产生，从而把归约在时间轴上提前。矩阵反向中，输入梯度 $dX$ 使前一层继续反向，参数梯度 $dW$ 使本层开… |
| 本章的设计案例将参数收集、梯度归约并分片等通信中会延长训练步的等待时间合计为每步 4 s，将参数更新和加速器内的非矩阵运算包含在前面给定的单卡计算效率内。两套方案在输入就绪时的每步耗时因而分别为 $78.3+4=82.3$ s 和 … | 本章的设计案例把参数收集、归约后分散等通信中会延长训练步的等待合计为每步 4 s；参数更新和卡内的非矩阵运算则已包含在前面给定的单卡计算效率里。两套方案在输入就绪时的每步耗时因而分别为 $78.3+4=82.3$ s 和 … |
| > **案例：全局批次不变时，加速器数增至四倍能加速多少？** 大规模分布式训练系统 MegaScale 在 175B 模型上保持全局批次为 6144，将加速器从 3072 增至 12288。原每步耗时约 23.7 s，若加速器数增至原来的四倍就能带来四倍加速，每步应缩短至约 5.9 s，实际约 6.3 s。总加速约 3.7 倍，对应约 93% 扩展效率。加速器增加后，每卡工作减少，跨组同步和负载不均衡造成的等待，在每步耗时中所占的比例随之上升；实际每… | > **案例：全局批次不变时，卡数增至四倍能加速多少？** 大规模分布式训练系统 MegaScale 在 175B 模型上保持全局批次为 6144，把卡数从 3072 增至 12288。原每步耗时约 23.7 s，若卡数增至四倍就能带来四倍加速，每步应缩短至约 5.9 s，实际约 6.3 s。总加速约 3.7 倍，对应约 93% 的扩展效率。卡数增加后，每卡的工作减少，跨组同步和负载不均衡造成的等待，在每步耗时中所占的比例随之上升；实际每… |
| …nse）的每次前向会使用各层全部前馈权重，其数据并行中的工作划分较规则。MoE 路由会使各加速器收到不同数量的 token，序列长短不一也会使相同 token 数对应不同计算量。因此，分析每步耗时，要看工作如何分到各加速器上，以及哪个加速器最后完成。 | …nse）的每次前向会使用各层全部前馈权重，其数据并行中的工作划分较规则。MoE 的路由会让各卡收到数量不同的 token，序列长短不一也会让相同的 token 数对应不同的计算量。因此，分析每步耗时，要看工作怎样分到各张卡上，以及哪张卡最后完成。 |
| **专家分派不均如何延长最忙加速器的计算时间。** 设两个加速器上的专家分别接到 96、32 次 token 分派，每次分派把一个 token … | **专家分派不均如何延长最忙的卡的计算时间。** 设两张卡上的专家分别接到 96、32 次 token 分派，每次分派把一个 token … |
| 均衡专家的计算量以后，数据传输仍可能让加速器等待。MoE 的前向 token 交换、反向输入梯度传输和参数梯度归约会争用同一… | 均衡专家的计算量以后，数据传输仍可能让卡等待。MoE 的前向 token 交换、反向输入梯度传输和参数梯度归约会争用同一… |
| 专家负载取决于加速器上的专家接到了多少次 token 分派。长上下文还有另一层差别：即使 token… | 专家负载取决于每张卡上的专家接到了多少次 token 分派。长上下文还有另一层差别：即使 token… |
| … token 消除。打包减少填充 token（padding），按长度分组则让各加速器承担的三角形面积更接近；上下文并行进一步把长序列的计算分到多个加速器，并通过交换 K/V 取得其他加速器保存的数据。它们分别改变无效工作、工作分配和数据到达路径。用相同总 token … | … token 消除。打包减少填充 token（padding），按长度分组则让各卡承担的三角形面积更接近；上下文并行进一步把长序列的计算分到多张卡，并通过交换 K、V 取得其他卡保存的数据。它们分别改变无效工作、工作分配和数据到达路径。用相同总 token … |
| …算速度，还需要检查点保存可恢复的训练状态。本节把二者加入章首的完成时间：输入决定加速器能否持续工作，恢复决定已经做过的工作有多少需要重做。 | …算速度，还需要检查点保存可恢复的训练状态。本节把二者加入章首的完成时间：输入决定卡能否持续工作，恢复决定已经做过的工作有多少需要重做。 |
| …序列，两个数据准备进程只有四条/s，低于需求；三个数据准备进程达到六条/s。传输数据量很少，CPU 准备过程仍能限制整个集群。 | …序列，两个数据准备进程只有四条/s，低于需求；三个数据准备进程达到六条/s。传输的数据量虽少，CPU 上的准备过程仍可能拖住整个集群。 |
| …训练消耗速度，可以在短暂变慢之后重新补满预取队列。设计题再给每步 0.5 s 的未被预取消除的平均输入等待时间，32 卡和 48 卡的每步训练时间由此成为约 82.8 s 和 56.7 s。… | …训练消耗速度，可以在短暂变慢之后重新补满预取队列。设计题再给每步 0.5 s 的平均输入等待时间，表示预取没能消除的等待，32 卡和 48 卡的每步训练时间由此成为约 82.8 s 和 56.7 s。… |
| …同而打包时剩余的 token 不同，下一条训练序列也会改变。前面的激活重计算解决一次前反向中哪些中间值可以重建，检查点则要支持中断后的整个训练过程继续执行。 | …同而打包时剩余的 token 不同，下一条训练序列也会改变。前面的激活重计算解决的是一次前向、反向之内哪些中间值可以重建；检查点要解决的是中断之后整个训练过程怎样继续。 |
| **例：检查点如何从四路张量并行重分片为八路？** gate 权重形状为 $[12288,4096]$，BF16 大小 96 MiB。沿输出维四分，每片 3072 行、24 MiB；八分后，每片 1536 行、12 MiB。通信参与者的编号称为 rank；目标 rank $r$ 读取旧分片 $\lfloor r/2\rfloor$，偶数 rank 取前半，奇数 rank 取后半。全局行坐标将旧布局和新布局联系起来。 | **例：检查点如何从四路张量并行重分片为八路？** 门控投影的权重形状为 $[12288,4096]$，BF16 大小 96 MiB。沿输出维四分，每片 3072 行、24 MiB；八分后，每片 1536 行、12 MiB。每张卡在通信组里的编号称为 rank；编号为 $r$ 的目标卡读取旧分片 $\lfloor r/2\rfloor$，偶数编号取前半，奇数编号取后半。全局行坐标将旧布局和新布局联系起来。 |
| …分片分成前后两半后，分别存入两个新分片。模型权重的内容和顺序保持相同，改变的是各加速器负责的行范围；BF16 权重总量始终为 96 MiB。上方“旧”编号标识原分片，… | …分片分成前后两半后，分别存入两个新分片。模型权重的内容和顺序保持相同，改变的是各卡负责的行范围；BF16 权重总量始终为 96 MiB。上方“旧”编号标识原分片，… |
| …teCheckpoint 一类系统用全局形状、偏移和片段长度描述这些张量，使恢复端按目标布局读取需要的范围。文件分片是存储安排，全局坐标标明每个分片属于哪个张量、… | …teCheckpoint 一类系统用全局形状、偏移和片段长度描述这些张量，使恢复程序按目标布局读取需要的范围。文件分片是存储安排，全局坐标标明每个分片属于哪个张量、… |
| 前面解决了“保存哪些数据”和“恢复到哪些加速器”的问题。接下来要看保存过程怎样与训练同时进行，以及一份快照什么时候才真正可用。 | 前面解决了“保存哪些数据”和“恢复到哪些卡”的问题。接下来要看保存过程怎样与训练同时进行，以及一份快照什么时候才真正可用。 |
| …整快照的元数据。一组 CPU 实验中，保存接口（API，即供程序调用的功能接口）约 7 ms 返回，恢复元数据约 48 ms 才提交；提交前终止进程，恢复端选择上一份完整检查点。数据文件与提交记录共同构成可用快照，恢复程序据此判断哪一份检查点已经完整… | …整快照的元数据。一组 CPU 实验中，保存接口（API，即供程序调用的功能接口）的调用约 7 ms 就返回，恢复元数据约 48 ms 才提交；提交前终止进程，恢复程序选择上一份完整的检查点。数据文件与提交记录共同构成可用快照，恢复程序据此判断哪一份检查点已经完整… |
| **例：加速器规模与故障率如何决定检查点保存间隔？** 先取 1024 张卡，每卡平均 365 天… | **例：卡数与故障率如何决定检查点保存间隔？** 先取 1024 张卡，每卡平均 365 天… |
| …十分钟，新增的重做时间就超过了节省的保存时间。表中合计由未经四舍五入的数值计算。加速器数翻倍会使故障率翻倍，最优间隔缩短为原来的 $1/\sqrt2$；保存速度翻倍也… | …十分钟，新增的重做时间就超过了节省的保存时间。表中合计由未经四舍五入的数值计算。卡数翻倍会使故障率翻倍，最优间隔缩短为原来的 $1/\sqrt2$；保存速度翻倍也… |
| …重做与恢复合计约增加 0.90% 时间，48 卡约增加 0.95%。48 卡方案遭遇加速器故障中断的概率更高，但附加比例只多约 0.05 个百分点，远小于它缩短单卡计算时… | …重做与恢复合计约增加 0.90% 时间，48 卡约增加 0.95%。48 卡方案被卡故障中断的概率更高，但附加比例只多约 0.05 个百分点，远小于它缩短单卡计算时… |
| …信号。DeepSeek V4 用多教师 OPD 将领域专家的能力合入统一模型，第三章已将教师前向列为独立的一项计算工作。[^opd] | …信号。DeepSeek V4 用多教师 OPD 将领域专家的能力合入统一模型，第 3 章已把教师前向列为一项独立的计算工作。[^opd] |
| …师前向与权重同步的成本，是否减少总 GPU 小时或完成时间仍要实测。SFT 描述监督目标，offline 描述数据是否预先固定；也可以在线收集数据后使用监督损失。online 表示数据随训练过程持续产生，on-policy 则进一步要求采样分布与所优化的策略匹配。异步 online RL 可能使用滞后策略，OPD 也会遇到生成与训练引擎的计算差异。因此，在线采样用于缓解数据分布偏移，训推一致性还需要对齐生… | …师前向与权重同步的成本，是否减少总 GPU 小时或完成时间仍要实测。SFT 描述的是监督目标，离线（offline）描述的是数据是否预先固定；也可以在线收集数据后使用监督损失。在线（online）表示数据随训练过程持续产生，同策略（on-policy）则进一步要求采样分布与所优化的策略一致。异步的在线 RL 可能使用滞后的策略，OPD 也会遇到生成引擎与训练引擎的计算差异。因此，在线采样用于缓解数据分布偏移，训推一致性还需要对齐生… |
| …参与前向计算，损失则按选中的回答 token 归一化。因此，生成长度影响计算成本，损失掩码（mask）为各 token 标明是否参与损失计算，它决定哪些 token 参与梯度计算，两者有不同作用。[^verl] | …参与前向计算，损失则按选中的回答 token 归一化。因此，生成长度影响计算成本；损失掩码（mask）为每个 token 标明是否参与损失计算，决定哪些 token 参与梯度计算。两者的作用不同。[^verl] |
| …才获得区分两条回答的方向。系统即使完成了前向、反向和优化器调用，也可能遇到第一种全零优势批次。统计训练吞吐量时，应计算每秒实际用于训练的轨迹或 token 数；模型能力… | …才获得区分两条回答的方向。系统即使完成了前向、反向和优化器调用，也可能遇到第一种优势全为零的批次。统计训练吞吐量时，应计算每秒实际用于训练的轨迹或 token 数；模型能力… |
| > **案例：参数发生变化，模型能力为何没有提高？** 固定小模型实验使用强化学习训练框架 verl 与推理引擎 vLLM 协作，算术精确奖励使同组回答取得相同奖励，优势和策略梯度为零，参数变化来自 AdamW 权重衰减。AdamW 在 Adam 的梯度更新之外，单独按比例缩小权重；即使当前… | > **案例：参数发生变化，模型能力为何没有提高？** 一次固定的小模型实验用强化学习训练框架 verl 和推理引擎 vLLM 协作运行：算术题的精确匹配奖励让同组回答得到相同的奖励，优势和策略梯度都为零，参数变化只来自 AdamW 的权重衰减。AdamW 在 Adam 的梯度更新之外，单独按比例缩小权重；即使当前… |
| …道工序，前一道做得更快，只有后一道接得住，才会增加最终产出。先把三者的处理能力换成相同工作单位。设它们分别为 $r_g,r_v,r_l$，验证后符合策略版本要求的比例… | …道工序，前一道做得更快，只有后一道接得住，才会增加最终产出。先把三者的处理能力换算成同一种工作单位。设它们分别为 $r_g,r_v,r_l$，验证后符合策略版本要求的比例… |
| 图 10-31 先把三道工序画在各自独立的资源上。若改用同一组加速器轮流执行，就能减少所需加速器数，但生成和学习之间需要切换整套显存数据。 | 图 10-31 先把三道工序画在各自独立的资源上。若改用同一组卡轮流执行，就能减少所需的卡数，但生成和学习之间要切换整套显存里的数据。 |
| **共享加速器**让同一组加速器轮流训练和生成，节省重复资源；**独立部署**让两侧独立执行，获得重叠机会，但要在两组加速器间发送权重。共享加速器的关键成本发生在切换：旧状态尚未释放，新状态已经开始加载，… | **共享加速器**让同一组卡轮流训练和生成，节省重复资源；**独立部署**让两侧独立执行，获得重叠机会，但要在两组卡之间发送权重。共享加速器的关键成本发生在切换：旧状态尚未释放，新状态已经开始加载，… |
| …16 专家权重为 423 GiB，采用 16 路专家并行（EP16）时，每个接收端只需要约 26.4 GiB。逐一向 16 个接收端发送完整专家集合，共发送 $16\times423=6768$ GiB；按各自分片发送，合计为 423 GiB。确定接收端需要的参数分片，再组织分发，可以同时减少接收缓冲和源端发送量。 | …16 专家权重为 423 GiB，采用 16 路专家并行（EP16）时，每个接收方只需要约 26.4 GiB。逐一向 16 个接收方发送完整专家集合，共发送 $16\times423=6768$ GiB；按各自的分片发送，合计为 423 GiB。先确定每个接收方需要的参数分片，再组织分发，可以同时减少接收缓冲和发送方的发送量。 |
| ![图 10-34 固定版本服务与策略训练的权重生命周期。上方 ROM 重复提供同一版本；下方训练产生新版本并发布给生成端。KV 写入与权重更新使用不同的数据通路。](ch10/figure-10-weight-update.svg) | ![固定版本服务与策略训练的权重生命周期](ch10/figure-10-weight-update.svg) |
| *图 10-34　固定版本服务与策略训练的权重生命周期。上方 ROM 重复提供同一版本；下方训练产… | *图 10-34：固定版本服务与策略训练的权重生命周期。上方 ROM 重复提供同一版本；下方训练产… |
| …old}}$，当前优化策略为 $\pi_\theta$。起点策略随优化轮次更新，KL 正则使用的参考模型则保持固定。对同一 token 和前缀，在三个概率有定义且分母非零的… | …old}}$，当前优化策略为 $\pi_\theta$。起点策略随优化轮次更新，用 KL 散度做正则时所用的参考模型则保持固定。对同一 token 和前缀，在三个概率有定义且分母非零的… |
| 在 PPO 一类更新中，概率比决定优势对梯度的权重，也参与裁剪判断。若实现差异被误认为参数更新，样本的贡献就可能被错误放大或压低，也可能错误触发裁剪；若使用序列概率比，各 token 位置的 logprob 差还会相加。因此，较长轨迹会积累更多概率偏差，使更新更容易波动。[^identi… | 在近端策略优化（PPO）一类更新中，概率比决定优势对梯度的权重，也参与裁剪判断。若实现差异被误认为参数更新，样本的贡献就可能被错误放大或压低，也可能错误触发裁剪；若使用序列概率比，各 token 位置的 logprob 之差还会相加。因此，较长轨迹会积累更多概率偏差，使更新更容易波动。[^identi… |
| 图中的生成阶段被画成一段连续工作。轨迹越长，这一段越容易跨过参数更新或遇到中断，于是除了筛选完成的样本，还要考虑尚未完成的轨迹怎样继续。长轨迹也会延长生成样本与使用样本训练之间的时间差。KV 是给定权重对前缀执行的结果：使用相同权重恢复时，可继续使用已保存的 KV，换权重后则需重新执行前缀来生成对应 KV。Qwen3-8B 的 8K BF16 KV 占 1.125 GiB，保存和取回 KV 缓存需要存储空间与传输时间，重建 KV 缓存则需要重新执行预填充计算。恢复方案据此比较取回时间与重建时间。 | 图中的生成阶段画成了一段连续的工作。轨迹越长，这一段越容易跨过参数更新或遇到中断，于是除了筛选完成的样本，还要考虑尚未完成的轨迹怎样继续。长轨迹也会延长生成样本与使用样本训练之间的时间差。KV 是给定权重对前缀执行的结果：使用相同权重恢复时，可继续使用已保存的 KV，换权重后则需重新执行前缀来生成对应 KV。Qwen3-8B 的 8K BF16 KV 占 1.125 GiB，保存和取回 KV 缓存需要存储空间与传输时间，重建 KV 缓存则需要重新执行 prefill 计算。恢复方案据此比较取回时间与重建时间。 |
| V4.1 将部署时的稀疏访问与恢复方式纳入训练。稀疏注意力从训练之初就使用 64K 序列，后训练加入层级候选限制，使训练和推理使用相同搜索域；量化感知训练适应 FP4 主 KV，只重放有限长度输入来重建解码器 SWA 的过程也在后训练中模拟。模型由此学习使用部署路径实际提供的表示与局部状态。[^v41-… | DeepSeek V4.1 把部署时的稀疏访问与恢复方式纳入训练。稀疏注意力从训练之初就使用 64K 序列，后训练加入层级候选限制，使训练和推理使用相同搜索域；量化感知训练适应 FP4 主 KV，只重放有限长度的输入来重建解码器滑动窗口注意力（SWA）状态的过程，也在后训练中模拟。模型由此学习使用部署路径实际提供的表示与局部状态。[^v41-… |
| 上一节区分了策略版本、采样定义与数值执行造成的概率差异。MoE 将数值差异进一步变成离散路径差异：即使权重相同，生成端与训练端的数值计算差异也可能改变专家选择。由于专家… | 上一节区分了策略版本、采样定义与数值执行造成的概率差异。MoE 把数值差异进一步变成离散的路径差异：即使权重相同，生成端与训练端的数值计算差异也可能改变专家选择。由于专家… |
| **Routing Replay** 记录生成时的逻辑专家 ID，训练时按照这些 ID 选择专家，再用当前权重计算路由分数、专家输出和梯度。样本、token… | **路由重放**（Routing Replay）记录生成时选中的逻辑专家 ID（编号），训练时按这些 ID 选择专家，再用当前权重计算路由分数、专家输出和梯度。样本、token… |
| …录如何随 token 一起移动：序列打包改变 token 的位置，上下文切分改变加速器归属，重计算再次读取同一层记录。让 ID 与 token 的原始位置共同经过这些变换，训练端才能选中原先那… | …录如何随 token 一起移动：序列打包改变 token 的位置，上下文切分改变 token 所在的卡，重计算再次读取同一层的记录。让 ID 与 token 的原始位置共同经过这些变换，训练端才能选中原先那… |
| > **案例：路由重放对概率误差、训练耗时与奖励的影响。** 作者公布的四组路由重放（R3）开关对照各训练 100 步，开启后日志中记录的对数概率误差中位数均下降，每步总耗时中位数… | > **案例：路由重放对概率误差、训练耗时与奖励的影响。** NVIDIA 公布的路由重放（R3）验证报告有四组开关对照，各训练 100 步，开启后日志中记录的对数概率误差中位数均下降，每步总耗时中位数… |
| …的计划停顿相同，保存与恢复也只占很小一段。主要差别在蓝色的基础训练时间：48 卡将微批次分配给更多加速器并行处理，显著缩短了这一段。 | …的计划停顿相同，保存与恢复也只占很小一段。主要差别在蓝色的基础训练时间：48 卡把微批次分给更多的卡并行处理，显著缩短了这一段。 |
| …间超过 30 天而被排除；48 卡约 26.1 天完成，剩余约 3.9 天。增加加速器的主要价值是减少每卡承担的训练工作，额外故障成本只抵消很小一部分收益。 | …间超过 30 天而被排除；48 卡约 26.1 天完成，剩余约 3.9 天。增加卡数的主要价值是减少每卡承担的训练工作，额外故障成本只抵消很小一部分收益。 |
| …approx50\%$。于是，两条改进路线可以直接比较：增加两台主机，或者在原有加速器上把单卡计算效率从 40% 提高到约 50%。 | …approx50\%$。于是，两条改进路线可以直接比较：增加两台主机，或者在原有的卡上把单卡计算效率从 40% 提高到约 50%。 |
| 通信预算必须落实到物理接口。若某主机的八个 rank 共享同一网卡，每个 rank 的发送量都要经过该接口，各 rank 的流量会在这个接口共同排队。以普通 DP 的完整 BF16 梯度环形 AllReduce 为独立例子，32 个参与者每 rank 发送约 $2(31/32)\times16.4\approx31.7$ GB。一个独占 25 GB/s 接口的发送过程约需 1.3 s；多个 rank 共享时，应把它们穿过该接口的字节相加，再按就绪时刻排列。ZeRO-3 则按各模块的参数收集与梯度归约并分片建立同样的接口时间轴。 | 通信预算必须落实到物理接口。若一台主机的八张卡共用同一张网卡，每张卡的发送量都要经过这个接口，各卡的流量会在这里一起排队。以普通数据并行的完整 BF16 梯度环形 AllReduce 为独立的例子，32 个参与者中每张卡发送约 $2(31/32)\times16.4\approx31.7$ GB。独占一个 25 GB/s 接口时，发送约需 1.3 s；多张卡共用时，应把它们穿过这个接口的字节相加，再按就绪时刻排列。ZeRO-3 则按各模块的参数收集与归约后分散建立同样的接口时间轴。 |
| …再比较加速器租用、能源和准备成本。更强的单项指标是否值得购买，取决于它能减少多少任务完成时间或加速器数量。 | …再比较加速器租用、能源和准备成本。更强的单项指标是否值得购买，取决于它能减少多少完成时间或卡数。 |
| 前面保持模型不变，比较了加速器数量和加速器性能。若把模型本身放大，计算量又会怎样增长？最后把同一方法扩展到模型规模。采用稠密模… | 前面保持模型不变，比较了卡数和加速器型号。若把模型本身放大，计算量又会怎样增长？最后把同一方法扩展到模型规模。采用稠密模… |
| 图 10-41 将 90 天期限画成加速器数边界。选择某个模型规模后，边界上方的计算资源能够在该效率下完成工作，边界下方需… | 图 10-41 把 90 天期限画成卡数边界。选择某个模型规模后，边界上方的计算资源能够在该效率下完成工作，边界下方需… |
| ![给定期限下模型规模与加速器数边界](ch10/figure-10-20-scale.svg) | ![给定期限下模型规模与卡数边界](ch10/figure-10-20-scale.svg) |
| *图 10-41：固定 90 天执行期限，Dense 规模增大要求更多加速器。数据量为 20T token，算法工作为 $6ND$，效率为 50%，使用 BF16 dense 峰值。各线为向上取整前的连续计算边界；横线标出 16,384 张卡。* | *图 10-41：固定 90 天执行期限，稠密模型规模增大要求更多的卡。数据量为 20T token，算法工作为 $6ND$，效率为 50%，使用 BF16 稠密矩阵峰值。各线为向上取整前的连续计算边界；横线标出 16,384 张卡。* |
| **误区：分片数翻倍，训练显存峰值就减半。** 分片只直接缩小分给各加速器的常驻训练状态。小模型例中常驻训练状态从 18 MiB 减到 9 MiB，执行期… | **误区：分片数翻倍，训练显存峰值就减半。** 分片只直接缩小分给各卡的常驻训练状态。小模型例中常驻训练状态从 18 MiB 减到 9 MiB，执行期… |
| …量减少到多少、将期限延长到多久，或将计算效率提高到多少，才能完成训练。实验部分选择一条路线进行测量，替换教学参数。 | …量减少到多少、将期限延长到多久，或将计算效率提高到多少，才能完成训练。实验部分选一条路线实际测量，替换教学参数。 |
| …可表示为每秒处理的批次比例。实验部分追踪一批样本的标识、奖励、有效损失位置和接收端权重。 | …可表示为每秒处理的批次比例。实验部分追踪一批样本的标识、奖励、有效损失位置和接收方的权重。 |
| …eriments/ch10/10-01/fsdp-cpu/README.md)。 CPU 小模型四种配置得到的参数与未分片参考结果一致；训练期间分配的内存峰值为 … | …eriments/ch10/10-01/fsdp-cpu/README.md)。CPU 小模型四种配置得到的参数与未分片参考结果一致；训练期间分配的内存峰值为 … |
| …ts/pipeline-gemm-recompute-products.md)。 GEMM 输入采用 FP32 参考表示，九层的可重建乘积为 90 MiB，最大逐… | …ts/pipeline-gemm-recompute-products.md)。GEMM 输入采用 FP32 参考表示，九层的可重建乘积为 90 MiB，最大逐… |
| …ch10/10-01/deepspeed-offload/README.md)。 转换路径从 GPU 梯度就绪开始，到 CPU 可读取 FP32 梯度结束；两条路… | …ch10/10-01/deepspeed-offload/README.md)。转换路径从 GPU 梯度就绪开始，到 CPU 可读取 FP32 梯度结束；两条路… |
| …66／6.34 s，扩展效率约 93.3%；对应固定全局批次的整套系统扩展实验。 [MegaScale 正式论文](../references/proceedin… | …66／6.34 s，扩展效率约 93.3%；对应固定全局批次的整套系统扩展实验。[MegaScale 正式论文](../references/proceedin… |
| …tudies/expert-dispatch-and-resizing.md)。 72 MiB 分三桶、16 GiB/s 有效数据传输速率、0.1 ms 启动和三… | …tudies/expert-dispatch-and-resizing.md)。72 MiB 分三桶、16 GiB/s 有效数据传输速率、0.1 ms 启动和三… |
| …10/10-06/input-io-contention/README.md)。 共享通路算例使用 112 GB 快照、8 GB/s 总传输速率和 3 GB/s … | …10/10-06/input-io-contention/README.md)。共享通路算例使用 112 GB 快照、8 GB/s 总传输速率和 3 GB/s … |
| …0/10-06/input-state-recovery/README.md)。 2,000＋6,192 是解释打包时剩余的 token 的教学例。DCP 小模型… | …0/10-06/input-state-recovery/README.md)。2,000＋6,192 是解释打包时剩余的 token 的教学例。DCP 小模型… |
| …nts/ch10/10-07/save-baseline/README.md)。 原正常路径 API 为约 7.25 ms、元数据提交约 48.30 ms；故障实… | …nts/ch10/10-07/save-baseline/README.md)。原正常路径 API 为约 7.25 ms、元数据提交约 48.30 ms；故障实… |
| …ns/results/checkpoint-interval-book.md)。 精确数据量为 114,670,295,040 bytes，保存成本约 14.33… | …ns/results/checkpoint-interval-book.md)。精确数据量为 114,670,295,040 bytes，保存成本约 14.33… |
| …lations/results/weight-handoff-book.md)。 40 GiB 为分片／卸载后显存占用的教学输入；两个切换峰值精确约为 83.25… | …lations/results/weight-handoff-book.md)。40 GiB 为分片／卸载后显存占用的教学输入；两个切换峰值精确约为 83.25… |
| …s/ch10/10-10/public-training/README.md)。 SmolLM3 记录含 384、288、192 rank 阶段，最后一次恢复实际… | …s/ch10/10-10/public-training/README.md)。SmolLM3 记录含 384、288、192 rank 阶段，最后一次恢复实际… |
| 训练所需的内存容量由状态的存储格式、加速器间的分配方式和生命周期共同决定。ZeRO 减少状态复制，重计算减少前向到反向之间… | 训练所需的内存容量由状态的存储格式、各卡之间的分配方式和生命周期共同决定。ZeRO 减少状态复制，重计算减少前向到反向之间… |

## 翻译腔审查（2026-09-11）

作者确认术语原则并统一“笔者”“N 个 token”“Queqiao”之后，再做一遍只针对翻译腔的审查（含图注里汉字数字改阿拉伯数字）。共 21 行有改动，按最终文本与审查前快照逐行对照。

| 修改前 | 修改后 |
|---|---|
| …预留 5 天给启动、评估和计划性停顿，留下 25 天用于训练、检查点与故障恢复。这使平均有效吞吐要求从约 38,600 token/s 提高到约 46,300 token/s。 | …预留 5 天给启动、评估和计划性停顿，留下 25 天用于训练、检查点与故障恢复。平均有效吞吐的要求因此从约 38,600 token/s 提高到约 46,300 token/s。 |
| …预算，32 卡在加入通信前已经超时，48 卡则留下约 15.7 s。下一步要判断，两套方案如何安排状态，以及这 15.7 s 会被哪些等待消耗。 | …预算，32 卡在加入通信前已经超时，48 卡则留下约 15.7 s。下一步要判断两套方案怎样安排状态，以及哪些等待会消耗掉这 15.7 s。 |
| …数相同，因此每张卡通常都保存一份相同的权重与优化器状态；卡越多，复制的状态越多。微软研究者在 2019 年提出、于 2020 年发表的 ZeRO（Zero Redundancy Optimizer，零冗余优化器），针对大模型训练中状态副本占用大量显存的问题，通过跨卡分片减少重复保存。它分阶段划分这些状态的归属：每张卡只负责一部分参数的更新，其他卡通过通信取得执… | …数相同，因此每张卡通常都保存一份相同的权重与优化器状态；卡越多，复制的状态越多。ZeRO（Zero Redundancy Optimizer，零冗余优化器）由微软研究者在 2019 年提出、2020 年发表，用跨卡分片来减少大模型训练中重复保存的状态副本。它分阶段划分这些状态的归属：每张卡只负责一部分参数的更新，其他卡通过通信取得执… |
| 当用已保存的结果重建数据的开销较小时，适合重计算。另一种办法是保留数据本身，把它移… | 用已保存的结果重建数据的开销较小时，适合重计算。另一种办法是保留数据本身，把它移… |
| …时需要 10 GiB，总量约 25.3 GiB，超出约 1.3 GiB。此时可以通过重计算减少这 1.3 GiB，也可以增至十六卡，将常驻训练状态降到约 7.6 GiB，总量… | …时需要 10 GiB，总量约 25.3 GiB，超出约 1.3 GiB。此时可以用重计算省下这 1.3 GiB，也可以增至十六卡，将常驻训练状态降到约 7.6 GiB，总量… |
| 微批次的划分保持了训练目标，却改变了计算的先后顺序。对流水线并行而言，顺序还决定每个阶段什么时候可以开始计算，以及多少份激活需要等待反向。流水线并行把模型分成多个阶段，每个微批次依次前向，再沿反方向传播梯度。**… | 微批次的划分保持了训练目标，却改变了计算的先后顺序。在流水线并行里，顺序还决定每个阶段什么时候能开始计算，以及有多少份激活要等着反向。流水线并行把模型分成多个阶段，每个微批次依次前向，再沿反方向传播梯度。**… |
| 两种调度做相同的前后向工作，但 1F1B 在这些依赖下多用约 3% 时间；与此同时，图中所示的中间结果与缓冲区的最大显存占用从约 2.57 GB 降到 0.97 GB，减少约 62%。若只给这些中间结果和… | 两种调度做相同的前后向工作，1F1B 在这些依赖下多用约 3% 的时间，图中中间结果与缓冲区的最大显存占用却从约 2.57 GB 降到 0.97 GB，减少约 62%。若只给这些中间结果和… |
| 稠密模型（Dense）的每次前向会使用各层全部前馈权重，其数据并行中的工作划分较规则。MoE 的路由会让各卡收到数量不同的 token，序列长短不一也会让相同… | 稠密模型（Dense）的每次前向都使用各层全部前馈权重，数据并行时的工作划分比较规则。MoE 的路由会让各卡收到数量不同的 token，序列长短不一也会让相同… |
| **专家分派不均如何延长最忙的卡的计算时间。** 设两张卡上的专家分别接到 96、32 次 token 分派，… | **专家分派不均如何延长最忙那张卡的计算时间。** 设两张卡上的专家分别接到 96、32 次 token 分派，… |
| 训练中的路由均衡机制作用于样本如何选择专家；专家副本和拓扑映射则作用于这些选择在哪里执行。 | 训练中的路由均衡机制决定样本怎样选择专家；专家副本和拓扑映射则决定这些选择在哪里执行。 |
| … 10-25 的队列只能暂时填补准备速度的下降；若数据长期准备得更慢，队列终究会被取空。输入与检查点共享存储时，就可能出现这种持续不足。 | … 10-25 的队列只能暂时填补准备速度的下降；若数据长期准备得更慢，队列终究会取空。输入与检查点共享存储时，就可能出现这种持续不足。 |
| 对这里采用 Adam 的训练，需要保存权重、一阶矩与二阶矩、步数、学习率状态、随机状态和下一批数据的组成。学习… | 本章采用 Adam 的训练需要保存权重、一阶矩与二阶矩、步数、学习率状态、随机状态和下一批数据的组成。学习… |
| …固定数据 SFT 的潜在优势，主要是达到目标能力所需的学习样本或更新次数可能减少；它同时增加学生在线生成、教师前向与权重同步的成本，是否减少总 GPU 小时或完成… | …固定数据 SFT 的潜在优势，主要是达到目标能力所需的学习样本或更新次数可能减少。它同时增加学生在线生成、教师前向与权重同步的成本，是否减少总 GPU 小时或完成… |
| …\approx1.28$。参数没有变化，样本权重却增加了约 28%。保留行为概率使训练端能够区分策略变化与计算差异。[^identity] | …\approx1.28$。参数没有变化，样本权重却增加了约 28%。保留行为概率，训练端才能区分策略变化与计算差异。[^identity] |
| …近端策略优化（PPO）一类更新中，概率比决定优势对梯度的权重，也参与裁剪判断。若实现差异被误认为参数更新，样本的贡献就可能被错误放大或压低，也可能错误触发裁剪；若使用序列概率比，各 token 位置的 logprob 之差还会相加… | …近端策略优化（PPO）一类更新中，概率比决定优势对梯度的权重，也参与裁剪判断。若把实现差异误认为参数更新，样本的贡献就可能被错误地放大或压低，也可能错误地触发裁剪；若使用序列概率比，各 token 位置的 logprob 之差还会相加… |
| 重要性加权通过概率比调整已采样事件的贡献，因此采样分布必须覆盖目标事件。截断采样排除的 tok… | 重要性加权用概率比调整已采样事件的贡献，因此采样分布必须覆盖目标事件。截断采样排除的 tok… |
| …长而更容易被丢弃。设生成期间的中断率为 $\lambda$，持续 $t$ 的轨迹在不中断的情况下生成完毕的概率为 $e^{-\lambda t}$；随着 $t$ 增长，进入学习的数据比… | …长而更容易被丢弃。设生成期间的中断率为 $\lambda$，持续 $t$ 的轨迹能不受中断、完整生成的概率为 $e^{-\lambda t}$；随着 $t$ 增长，进入学习的数据比… |
| … 64K 序列，后训练加入层级候选限制，使训练和推理使用相同搜索域；量化感知训练适应 FP4 主 KV，只重放有限长度的输入来重建解码器滑动窗口注意力（SWA）状态的过程，也在后训练中模拟。模型由此学习使用部署路径实际提供的表示与局部状态。[^v41-… | … 64K 序列，后训练加入层级候选限制，使训练和推理使用相同搜索域；量化感知训练让模型适应 FP4 的主 KV；只重放有限长度的输入来重建解码器滑动窗口注意力（SWA）状态，这个过程也在后训练中模拟。模型由此学习使用部署路径实际提供的表示与局部状态。[^v41-… |
| …时间变为约 69.6 s，加上通信与输入为约 74.1 s，超过目标。对这套方案而言，单卡计算效率的这一下降对能否按期完成的影响，比带宽减半更大。 | …时间变为约 69.6 s，加上通信与输入为约 74.1 s，超过目标。对这套方案来说，单卡计算效率降到 30%，比带宽减半对能否按期完成的影响更大。 |
| …耗速率计算，求维持原训练速度至少需要预存多少条序列，以及四个准备进程全部恢复后，将预取队列中的序列数补充到暂停发生前的数量所需的时间。再将检查点与输入放在同一条 8 GB/s 通路，分析保存期间的数据输入速度。 | …耗速率计算，求维持原训练速度至少需要预存多少条序列，以及四个准备进程全部恢复后，要多久才能把预取队列补回暂停前的数量。再将检查点与输入放在同一条 8 GB/s 通路，分析保存期间的数据输入速度。 |
| …k-v4.pdf)，§5.1 的领域专家与多教师同策略蒸馏；阶段工作划分参见[第三章](03-推理与训练负载.md)。 | …k-v4.pdf)，§5.1 的领域专家与多教师同策略蒸馏；阶段工作划分参见[第 3 章](03-推理与训练负载.md)。 |
