# 第 10 章 训练系统

一个模型能够在单张加速卡上生成文本，是否意味着它也能在这张卡上训练？对于 Qwen3-8B，BF16 权重约为 16.38 GB。如果采用本章规定的混合精度 Adam 配置，权重、梯度、主权重和优化器状态合计约为 131.05 GB，尚未加入激活与临时工作区。推理时能够驻留的模型，在训练时可能连持久状态都放不下。

增加设备可以解决部分容量问题，但状态分片之后，计算常常要等待参数收集或梯度归约。把状态移到主机内存，可以减少设备驻留，却会增加传输和缓冲。即使每一步运行得很快，供数停顿、保存检查点和故障重做仍可能使训练超过期限。因此，训练系统的目标必须同时包含完成工作、保持更新语义和保留进展。

本章沿一个明确任务推进：使用 Qwen3-8B 完成 100B 有效 token 的领域继续训练，期限为 30 天。先估算容量与计算下界，再安排状态放置和训练步时序，随后加入数据供给与恢复。强化学习（RL）把推理、验证、学习和权重分发接成循环，我们将在同一方法上加入样本身份与策略版本。最后回到硬件和规模选择，检查章首预算是否能够兑现。

文中的“教学计算”使用显式假设分析设计取舍；“已有实测”指配套实验中已经保存的运行结果；历史论文结果保留其模型、硬件和版本条件。GB、TB 采用十进制，GiB、MiB 采用二进制。硬件标称容量与运行时可用容量分别处理。

## 10.1 从训练任务到容量与时间预算

### 10.1.1 任务、更新范围与完成目标

估算训练系统前，首先要确定什么工作必须完成。本章基线采用全参数更新、混合精度 Adam、8,192-token 序列和 100B 有效 token。有效 token 是计入任务预算的位置；填充到固定形状的 padding 可能消耗计算，但不能因此增加已完成的数据量。质量验收由任务另行规定，处理完 token 预算只是完成条件的一部分。

预训练、领域继续训练和监督微调可以使用相似的训练执行机制，但其数据量、有效长度和更新范围并不相同。LoRA 冻结大部分基础权重，只训练少量适配参数，梯度和优化器状态随可训练参数变化；冻结权重的前向执行以及为适配器反向所需的激活仍可能占据大量资源。低比特冻结权重也不能直接作为全参数低精度更新的容量依据。

还要区分三个时间范围。**训练步时间**包括一次参数更新所需的计算与同步；**稳态运行时间**覆盖多次更新，可能已经包含部分数据或保存开销；**日历时间**还包括启动、等待资源、故障和恢复。比较两项优化时，必须使计时范围一致。若检查点以前的工作在故障后丢失，曾经完成的迭代数也不能全部算作保留下来的进展。

本章先给出必要下界。通过下界检查的方案只是候选，后续还需要逐卡容量、实际依赖和质量证据。这样的估算顺序可以减少需要实测的组合：下界已经超过期限的配置，没有必要先花费数天完成全面调优。

### 10.1.2 参数、梯度、优化器与激活

设模型有 $N$ 个参数。在本章的 Adam 状态约定下，每个参数对应五项持久存储：

| 状态 | 每参数字节 | Qwen3-8B 容量 / GB | 用途 |
|---|---:|---:|---|
| BF16 模型权重 | 2 | 16.381 | 前向与反向使用的模型副本 |
| BF16 梯度 | 2 | 16.381 | 累积本次更新的梯度 |
| FP32 主权重 | 4 | 32.763 | 保持更新精度 |
| Adam 一阶矩 | 4 | 32.763 | 梯度历史统计 |
| Adam 二阶矩 | 4 | 32.763 | 梯度平方历史统计 |
| 合计 | 16 | 131.052 | 不含激活与临时缓冲 |

官方配置对应 $N=8\,190\,735\,360$，因此

$$
M_{\mathrm{persistent}}=16N=131\,051\,765\,760\ \mathrm{bytes}
\approx122.051\ \mathrm{GiB}.
$$

“混合精度”并不唯一确定上述格式。如果梯度以 FP32 常驻，合计变为每参数 18 bytes。主权重是否独立保存、优化器状态如何量化，也都应按具体实现重新核算。对于 MoE，未被某个 token 激活的专家仍是可训练参数；优化器状态不能按每 token 的 top-$k$ 比例缩小。[^state]

激活是另一类存储。前向中的某些输入和中间结果要保留到反向使用；其大小受序列长度、微批、注意力实现和保存策略影响。设备峰值应写成同一时刻各项存活量的最大值：

$$
M_{\mathrm{peak},i}=\max_t\left[M_{\mathrm{persistent},i}(t)+M_{\mathrm{activation},i}(t)+M_{\mathrm{temporary},i}(t)\right].
$$

不能分别求三项的峰值后，默认它们一定同时发生；也不能只统计步骤结束后仍可见的张量，遗漏执行中的参数聚合和转换副本。

![训练持久状态与梯度卸载的两种转换位置](ch10/figure-10-1-state.svg)

*图 10-1：上半部按官方参数数目分解未分片 Adam 状态，BF16 推理权重作为对照。下半部沿同一 gate 梯度比较传输 BF16 后在 CPU 转换，以及先在 GPU 转换再传输 FP32；转换路径只覆盖梯度到 CPU 可消费的过程。激活、完整更新与权重回传另计。*

> **实验 10-1 · 延伸：训练状态的容量预算**
>
> 根据上表计算 BF16 与 FP32 梯度两种配置的持久状态。分别与 24 GB、80 GB 标称容量比较，再预留激活和临时空间。读完 10.2 后加入分片与卸载，说明哪些状态被移动、哪些状态只是减少复制。换为 MoE 时，分别使用总参数和激活参数计算容量与工作量。

### 10.1.3 计算需求、MFU 与资源下界

30 天共有 $2\,592\,000$ 秒。若全部时间均可用于训练，100B token 要求平均供给

$$
R_{\mathrm{required}}=\frac{10^{11}}{30\times24\times3600}
\approx38\,580\ \mathrm{token/s}.
$$

配套计算沿 Qwen3-8B 的实际矩阵形状逐项核算前向与反向。100B token 包含 $12\,207\,031$ 条完整的 8,192-token 序列，以及一条 2,048-token 尾序列；矩阵工作总计约 $5.266\times10^{21}$ FLOPs。这个数包括声明范围内的矩阵工作，不包括全部逐元素操作、重计算和系统开销。[^deadline]

设 $F$ 是该范围的算法工作量，$p$ 是卡数，$P$ 是匹配精度与累加方式的单卡峰值，$\eta$ 是相同口径下的有效效率，则

$$
T_{\mathrm{compute}}=\frac{F}{pP\eta},\qquad
p_{\mathrm{compute}}\ge\left\lceil\frac{F}{P\eta T_{\mathrm{available}}}\right\rceil.
$$

若 $F$ 只计矩阵子账，$\eta$ 就是这个子账相对硬件峰值的效率。通常所称的模型 FLOPs 利用率（MFU）也采用算法工作除以峰值与时间，但不同框架可能采用不同算法工作口径。不能把另一个框架报告的完整训练 MFU 不加调整地代入本式。

**例：计算与容量给出的设备下界。** 取矩阵口径效率 40%，Qwen3-8B 的计算下界在 RTX 4090、A100 80GB SXM、H100 SXM 和 B200 上分别为 31、17、6、3 张；只考虑理想分片后的持久状态，容量下界分别为 6、2、2、1 张。两种下界的较大者只是必要条件。图 10-2 同时画出 30%、40%、50% 效率假设，表明设备需求对效率估计相当敏感。

![容量下界与不同效率下的计算设备数](ch10/figure-10-2-budget.svg)

*图 10-2：100B 有效 token、30 天、8,192-token 序列，使用配套结果中匹配 BF16 输入、FP32 累加及 dense 执行的设备峰值。容量下界仅计每参数 16 bytes 的持久状态。柱高是向上取整的设备数，不是经过部署验证的集群规模。*

若 30 天中有 5 天不能推进训练，应把可用时间改为 25 天，所需有效 token/s 提高 20%。若某项停顿已经包含在测得的效率中，就不应再次从日历时间扣除。记账可以从工作量出发，也可以从实测步时出发，但同一项损失只能计入一次。

> **实验 10-2 · 延伸：从任务反推资源**
>
> 用给定矩阵工作量计算三档效率下的设备下界。将可用天数改为 25 天，并把序列长度改为 32,768；解释为什么后一变化不能只把相同每 token 工作量乘以总 token 数。列出从下界走向可执行方案尚缺少的条件。

## 10.2 训练状态的分片、重计算与卸载

### 10.2.1 数据并行与 ZeRO／FSDP 状态分片

普通同步数据并行让每个副本处理不同样本，再汇合梯度。各副本往往保存相同权重和相同优化器状态，因此增加副本会复制大量持久状态。ZeRO 通过逐步划分优化器、梯度和参数的所有权来减少复制；FSDP 的全分片配置也围绕参数分片、按需收集和梯度分散组织执行。具体收集次数仍取决于分片粒度、预取和前向后是否重新分片。

对 $d$ 个数据并行参与者，忽略补齐和临时聚合时，本章格式下的逐卡状态为

$$
\begin{aligned}
M_0&=16N,\\
M_1&=4N+\frac{12N}{d},\\
M_2&=2N+\frac{14N}{d},\\
M_3&=\frac{16N}{d}.
\end{aligned}
$$

下标表示 ZeRO 阶段。第一阶段将 FP32 主权重和两份 Adam 状态分片；第二阶段再分片梯度；第三阶段再分片模型权重。Qwen3-8B 在 $d=8$ 时得到下表。

| 配置 | 持久状态 / GiB·每卡 | 相对普通 DP 的变化 |
|---|---:|---|
| 普通 DP | 122.051 | 每卡完整复制 |
| ZeRO-1 | 41.955 | 优化器相关状态分片 |
| ZeRO-2 | 28.606 | 再分片梯度 |
| ZeRO-3 | 15.256 | 再分片参数 |

这些节省通过通信换取。一个模块计算前可能需要 AllGather 参数；反向后需要 ReduceScatter 梯度；更新之后各分片持有新的参数内容。若前向结束即释放完整参数，反向可能再次收集；若保留到反向，又会延长参数驻留。状态所有权和执行时序必须放在一起看。

已有 CPU FSDP2 小模型记录提供了一个直接检验：2／4 进程更新后可见状态约为 18／9 MiB，但训练分配峰约为 39.16／30.14 MiB。分片节省确实存在，临时聚合却不能忽略。这组记录验证了与未分片参考的更新一致性，不给出 GPU 集群的性能结论。[^fsdp]

### 10.2.2 激活保存与选择性重计算

反向传播并不需要永久保存全部前向结果，但必须在使用时得到所需值。以 $Y=XW$ 为例，反向计算

$$
dX=dY W^{\mathsf T},\qquad dW=X^{\mathsf T}dY.
$$

计算 $dW$ 需要 $X$；如果 $X$ 已释放，就必须从更早保留的状态重建。**激活重计算**选择少量保存边界，在反向时重做边界之间的前向。保存更少意味着生命周期较短或对象更少，也意味着新增工作。

以一个模块为单位，若重计算节省 $\Delta M$ bytes、增加 $\Delta F$ FLOPs，选择仍不能简化为一个固定的 bytes/FLOP 比值。新增工作可能处于流水空隙，也可能推迟最晚梯度；它还可能与参数预取、通信或其他内核竞争。需要把改变后的张量生命周期交给下一节的时间轴重新计算。

配套 Qwen3-8B 流水分析对 GEMM 保存身份去重：gate/up 共用的输入只计一次，GQA 的唯一 K/V 与已有注意力概率也分别登记。其一种教学策略只重建逐元素乘积，而不重做全部矩阵；每阶段每微批新增长期保存从约 141.56 MB 降到 47.19 MB，同时增加标量运算和约 6.29 MB 工作区。这个例子说明模块内部可以选择不同保存策略，数值不是实际 BF16 autograd 的完整分配峰。[^pipeline]

实现上，细粒度重计算需要与融合、图重放和通信调度兼容。一个后端支持某种重计算模式，并不意味着它与任意 CUDA Graph 或 MoE 重叠模式都能组合；兼容性和收益均应在相同配置下验证。

### 10.2.3 CPU／GPU 卸载与缓冲寿命

**卸载**将暂时不用的状态放到主机内存或其他较大存储中，使用前再取回。它增加了一个到达期限：状态必须在消费者开始前就绪。若取回服务时间是 $V/B$，可用提前窗口是 $W$，则至少暴露 $\max(0,V/B-W)$ 的等待；共享链路、准备与启动会进一步增加代价。

容量也要按时间计。主机可能同时持有锁页输入和转换输出，GPU 可能同时持有原始张量和转换副本。异步复制提交后，源缓冲仍可能被 DMA 读取；提前复用会破坏内容。较少的设备常驻量必须扣除取回暂存，才能得到净节省。

**例：梯度在哪一端转换？** Qwen3-8B 的一个 gate 梯度形状为 $[12288,4096]$，BF16 大小为 $G=96$ MiB，FP32 为 $2G=192$ MiB。两种转换都读取 BF16、写入 FP32，共访问 $3G$ bytes。设 CPU、GPU 的有效转换吞吐分别为 $C_c=100$ GB/s 和 $C_g=1500$ GB/s，单向链路吞吐为 $B$，串行路径时间为

$$
T_c=\frac{G}{B}+\frac{3G}{C_c},\qquad
T_g=\frac{3G}{C_g}+\frac{2G}{B}.
$$

两者相等时

$$
B^*=\frac{1}{3(1/C_c-1/C_g)}\approx35.714\ \mathrm{GB/s}.
$$

在 32 GB/s 时，两条路径约为 6.166 ms 和 6.493 ms，CPU 转换略快；在 300 GB/s 时约为 3.355 ms 和 0.872 ms，GPU 转换更快。后者需要额外 192 MiB GPU 转换缓冲。两条路径都从“GPU 梯度已就绪”计到“CPU 可消费 FP32 梯度”，不含 Adam 更新、权重回传和完整训练。[^offload]

SuperOffload 的研究把转换位置、主机优化器和紧耦合互联共同考虑。这种分析也解释了已有 CPUAdam 卸载记录中的反例：同一 gate 形状下，多传字节的 GPU 转换路径在该实现中更快，同时占用更高的 GPU 分配峰。较少字节是否更快，取决于省下的链路服务能否补偿另一端的计算和内存访问。

### 10.2.4 并行组合下的逐卡状态与拓扑映射

状态策略确定后，再把它放回第 6、7 章的并行组和物理路径。TP 决定矩阵与部分结果怎样切分，PP 决定哪些层驻留在同一阶段，DP 决定哪些样本贡献同一次更新，EP 决定专家状态的归属。不同参数可能属于不同通信组，因此不能总用一个乘法式就完整描述任意框架的 rank 布局。

候选表至少包含四类信息：每卡持久状态、同时存活的激活与临时量、每步必要通信，以及消息经过的最窄接口。任何一张卡超过净可用容量，候选即不满足容量条件。通信总量足够小也不能消除单个网卡、某一流水边界或最忙专家的瓶颈。

![分片容量筛选与通信路径检查](ch10/figure-10-3-candidates.svg)

*图 10-3：左图由 Qwen3-8B 状态公式计算 $d=8$ 的持久量，并显式加入 10 GiB 的假定同时存活附加量；24 GiB 是教学净预算，不代表 24 GB 产品的实测可用容量。右图说明通过容量筛选之后，仍需检查必要通信、步时和日历时间。*

例如，ZeRO-3 在八卡上每卡持久状态为 15.256 GiB。若额外同时存活量为 10 GiB，总量为 25.256 GiB，连教学中的 24 GiB 净预算也不能满足。只读状态表而忽略临时量，会错误地将它列为可运行方案。反过来，通过容量条件，也只意味着可以继续检查通信和期限。

> **实验 10-3 · 核心：4090 集群的可行范围**
>
> 先为本章任务建立两到三组明确的 DP／TP／PP 候选，说明主机、PCIe、网卡与逐卡状态。容量不满足的组合先排除，保留其原因。读完 10.3、10.4 后补齐步时与恢复，在 10.6.2 完成期限判断。将模型换成 Qwen3-235B 时，重新计算全部专家状态和实际激活工作，指出最值得补测的一个量。

## 10.3 训练步的流水调度与通信重叠

### 10.3.1 微批、梯度累积与更新边界

将一个大批次拆成多个微批，可以减少一次前向所需的激活，也能让流水阶段处理不同数据。但微批是执行划分，一次优化器更新所对应的目标函数必须明确保持。

设本次更新共有 $K$ 个微批，第 $k$ 个微批中有效位置集合为 $S_k$，总有效位置数为 $L=\sum_k|S_k|$。若目标是 token 平均损失，则

$$
\mathcal L=\frac{1}{L}\sum_{k=1}^{K}\sum_{j\in S_k}\ell_j,
\qquad
\nabla\mathcal L=\sum_{k=1}^{K}\nabla\left(\frac{1}{L}\sum_{j\in S_k}\ell_j\right).
$$

各微批使用同一分母 $L$，依次反向累积，最后裁剪并更新一次。若每个微批先按自身长度平均，再对微批平均，不等长数据就会得到不同权重；若已经用总分母，累积结束后又除以 $K$，梯度会被额外缩小。

在等大样本且普通 DP 条件下，全局 batch 常写成“微批样本数 × 累积次数 × DP 大小”。不等长 token、样本加权和不同 RL 目标需要更明确的分母。延后梯度通信是另一个实现选择：它可以减少同步次数，但不能由“做了梯度累积”推断框架一定只在最后微批通信。

### 10.3.2 流水线调度、气泡与激活寿命

流水并行把模型分成若干阶段，每个微批先依次前向，再反向返回。最简单的**填满排空**策略先完成所有微批前向，再执行反向；**1F1B** 在预热之后交错执行一次前向和一次反向，使较早微批的激活能够更快释放。

先取理想条件：$p$ 个平衡阶段、$m$ 个微批，阶段前向均为 $t_f$、反向均为 $t_b$，忽略通信和更新。填满排空的完成时间为

$$
T=(m+p-1)(t_f+t_b),\qquad
u=\frac{m}{m+p-1}.
$$

这里 $u$ 是设备时间利用率，不是 MFU。取 $p=4,m=8$，得到 $u=8/11\approx72.7\%$。增加微批可以摊薄填充与排空，但也会增加全局 batch，或在固定 batch 时缩小每微批并改变内核效率。

**例：激活更少，步时却更长。** 配套事件模型取 Qwen3-8B 的 36 层，每阶段 9 层，8 个单序列微批，每序列 128 token；各阶段前向 10 ms、反向 20 ms，边界正反向传输各 1 ms，优化器更新 1 ms。时间是教学输入，不是由模型 FLOPs 自动测得。填满排空完成于 337 ms，1F1B 完成于 347 ms。与此同时，预留激活范围的最大阶段峰从约 2.566 GB 降至 0.966 GB。[^pipeline]

![由事件记录生成的两种训练流水及激活预留](ch10/figure-10-4-pipeline.svg)

*图 10-4：两条时间轴直接读取配套调度结果，显示计算事件与间隙；间隙包括依赖和传输等待。下图为同一模型中已声明保存对象与收发缓冲的峰值，不包含全部激活、参数和分配器开销。此例的序列长度为 128，区别于章首 8,192-token 任务。*

这组结果的意义是分开判断两个目标：缩短激活寿命和缩短整步时间。1F1B 改变了通信与计算的就绪顺序，在特定资源约束下可能暴露更多等待。双向流水等更复杂调度也应按同样方式评价：先画模型块、状态和事件，再计算收益。DualPipe 提供一种具体设计路线，但其额外模型块放置和调度条件不能省略。

> **实验 10-4 · 延伸：微批与流水线气泡**
>
> 在上述事件模型中扫描 1、4、8、16 个微批。比较气泡、激活预留与总 batch；再将一个阶段的反向时间增大，解释最忙阶段如何限制收益。保持总 batch 后重新分微批，指出哪些时间输入必须重新测量。

### 10.3.3 反向依赖、梯度分桶与通信重叠

“通信与计算可以重叠”需要两个条件：要传输的数据已经产生，且后续计算暂时不依赖通信结果。梯度通常按反向顺序逐层就绪；分桶可以让较早完成的一部分先开始归约，但桶太小会增加启动次数，桶太大又会等待最后一个成员。

对于一块梯度，若归约服务时间为 $t_c$，从它就绪到更新必须使用它之间有 $w$ 的可用窗口，理想暴露尾部是 $\max(0,t_c-w)$。实际窗口还可能被参数收集、MoE token 交换或其他梯度占用。多条 CUDA stream 表示执行队列不同，不表示它们拥有独立网卡、内存带宽或计算单元。

一个反向 GEMM 还可能同时产生输入梯度 $dX$ 和参数梯度 $dW$。前者关系到前一层能否开始反向，后者关系到本层参数能否完成更新。优先执行哪一个，会改变流水推进与梯度通信的窗口。第 6 章的 TP 切法在这里必须扩展成完整前后向依赖图。

MegaScale 的历史实验提供了固定任务扩展的例子：175B 模型、固定 batch 6144，从 3072 卡的 23.66 s/步扩展到 12288 卡的 6.34 s/步，四倍设备获得约 $23.66/6.34=3.73$ 倍步时加速，扩展效率约 93.3%。这说明该系统在所声明条件下能够有效组织多种并行与重叠；不能把整套系统的改进归给单个通信技巧，也不能把历史效率直接用于 Qwen3。[^megascale]

### 10.3.4 MoE 与长上下文的调度变化

MoE 增加 token 到专家的分派和返回。训练时，前向专家交换、反向激活梯度、参数梯度归约可能争用同一跨机链路。热门专家使工作不均衡，最大完成时间由最忙设备决定；平均每专家 token 数不能完整描述步时。

训练中的均衡机制还可能改变优化目标。DeepSeek-V3 的路由偏置和补充序列级损失分别承担均衡作用，不能简单理解为“完全没有任何辅助损失”。推理 EPLB 可以通过物理副本与位置调整分担负载；训练若改变逻辑路由或损失，需要同时检查质量与稳定性。[^moe]

以某个 Qwen3 专家的 72 MiB FP32 梯度为例，若给定链路留下三个 2 ms 空隙，分成三个 24 MiB 块也不自动意味着可以填入：每块必须先就绪，通信服务加启动时间必须适合空隙，最后还要确认其他交换未占用链路。FSMoE 的切块思路适合解释前向与反向为什么可能需要不同安排，具体时间要由当前模型和执行图校准。

长上下文则增加激活和注意力通信。Megatron 中的序列并行常用于在 TP 组中分摊部分激活；上下文并行沿序列划分注意力计算，需要获得其他分片所需的 K/V 或等价信息。某些论文把沿头交换的方案也称为序列并行，名称相同不代表数据流相同。

即使总 token 数相同，长度分布也会改变工作。例如两条互不注意的序列长度为 4096、4096，其有效因果配对数总计 16,781,312；改为 7168、1024，总计 26,218,496，约增加 56.2%。打包可以减少 padding，却不会把后一组的真实注意力依赖消掉。改变分组时应保持 mask、位置和有效标签权重。[^length]

## 10.4 数据供给、检查点与故障恢复

### 10.4.1 数据读取、打包与预取

一个计算性能很高的集群也可能等待输入。训练供给通常经过读取、解码或分词、样本筛选、长度打包、锁页暂存和 H2D 传输。每一级都需要足够的长期服务能力，有限队列则用来吸收短期抖动。

若只传输 int64 token ID，章首的 38,580 token/s 约对应 0.309 MB/s 的逻辑输入。这个数量级远小于模型状态传输，但不能据此认定输入管线没有瓶颈：原始文本读取、CPU 预处理、样本索引、图像和音频数据都可能改变供给，低吞吐的小文件访问也不由逻辑 token 字节决定。

预取需要明确消费位置。已派发给 worker 的样本，可能仍在处理；已经送入队列的 batch，也可能尚未参与更新。检查点恢复若按派发位置续读，会跳过尚未消费的样本。输入优化与可恢复性从这里开始发生联系。

检查点后台写入还会与输入读取争用存储、CPU 和主机内存。配套供给模型分别模拟共享和独立存储，计算设备等待与队列驻留；所用服务时间是教学输入。已有小负载实验尚未建立稳定的大规模存储争用结论，因此正文不把理想模型的差值当作实际文件系统损失。[^supply]

> **实验 10-5 · 延伸：数据与检查点的带宽需求**
>
> 先按 token ID 估算最低输入字节，再加入一项明确的原始数据和预处理需求。给定检查点大小及周期，判断共享存储的平均供给是否足够；随后加入有限缓冲和一次长尾读取，说明平均带宽通过后仍可能出现什么等待。

### 10.4.2 可恢复状态与布局变换

检查点需要保存什么，取决于恢复后要继续什么过程。独立推理可能只需要权重；训练通常还需要优化器矩、步数、学习率状态、随机数、数据消费位置和打包残留。相同参数配上不同 Adam 动量，下一次更新一般不同；相同数据游标配上不同 token buffer，也可能构造不同样本。

更改并行布局时，恢复还要解决逻辑张量与文件分片之间的对应。ByteCheckpoint 用全局形状、偏移和片段长度描述状态，使加载端按目标布局查找需要的字节。逻辑坐标负责表达数据身份，文件名和当前 rank 编号并不足以完成重分片。[^checkpoint]

**例：从 TP=4 恢复到 TP=8。** gate 权重 $[12288,4096]$ 为 96 MiB。沿输出维分成四片，每片 24 MiB；改成八片，每片 12 MiB。目标 rank $r$ 从旧文件 $\lfloor r/2\rfloor$ 的前半或后半读取。再加入 FP32 主权重和 Adam 两项状态，同一逻辑矩阵合计

$$
50\,331\,648\times(2+4+4+4)=672\ \mathrm{MiB}.
$$

14 bytes/参数的检查点口径不含梯度，因此与训练中 16 bytes/参数的持久状态并不矛盾。融合、转置、量化或优化器展平会改变片段映射，需要对应的布局规划。

已有 CPU DCP 小模型记录把两份行分片恢复为三份列分片和一份完整状态，并核对下一步训练；保留完整状态时与不中断参考一致。另一组真实文本管线记录表明，遗漏打包残留或错用预取位置会跳样并改变模型。这些实验验证了指定恢复路径，不证明改变全局 batch 后仍有相同优化轨迹。[^resume]

### 10.4.3 同步、异步保存与带宽竞争

同步保存暂停训练，形成稳定快照并等待保存完成。异步保存把其中部分工作移到后台，使训练继续执行，但它引入暂存容量和尚未完成的恢复点。至少应区分快照捕获、staging 完成、API 返回和持久化提交。

**例：返回很快，恢复点仍然很旧。** 取整 8B 模型按每参数 14 bytes 保存，载荷为 112 GB；全局有效写带宽为 8 GB/s，传输服务时间为 14 s。若每 10 s 请求一份检查点，平均请求速率为 11.2 GB/s，超过存储能力。无限队列会持续增长，有限缓冲最终导致背压。

改为每 20 s 保存一次，平均速率降为 5.6 GB/s，通过这一必要条件。假设在第 20、40 s 捕获快照，各有 0.5 s staging，再串行写入，则两份恢复点分别在 34.5、54.5 s 完成。第 50 s 故障只能使用第一份，恢复位置是捕获时的第 20 s。将写带宽加倍后，两份完成点变为 27.5、47.5 s，同一故障可以使用第二份。

![检查点持久化时间与保存周期的损失模型](ch10/figure-10-5-recovery.svg)

*图 10-5：上图使用 112 GB 取整教学载荷，两条写带宽路径的 staging 暂停相同，50 s 故障时可用恢复点不同。下图改用官方 Qwen3-8B 的 114.67 GB 载荷、8 GB/s 同步保存和独立故障模型，展示保存频率与重做损失的取舍；两面板的假设分别给出。*

这里恢复点的新鲜度由完整提交决定。已有 CPU 故障注入记录在数据文件写完、元数据提交前终止进程：新目录虽然已有数据，DCP 仍无法加载，只能回到旧检查点。正常路径中，一次 API 在约 7.25 ms 返回，元数据到约 48.30 ms 才提交完成。实验中的人为屏障是故障定位手段，不是低带宽测量。[^fault]

因此异步保存的评价应包括三项：前台暂停、后台对训练吞吐的影响，以及故障时最新可恢复进展。只比较 API 耗时，会遗漏后两项。

### 10.4.4 故障规模、保存周期与有效进展

设每次同步保存成本为 $c$，相邻保存之间完成 $\tau$ 秒有用计算；作业中断率为 $\lambda$，一次恢复耗时为 $r$。在故障稀少、近似独立、保存成本固定的条件下，每单位有用计算的一阶损失为

$$
L(\tau)\approx\frac{c}{\tau}+\lambda\frac{\tau}{2}+\lambda r.
$$

第一项是频繁保存的代价，第二项来自故障前平均丢失半个间隔，第三项是恢复开销。对 $\tau$ 求导，得到

$$
\tau^*=\sqrt{\frac{2c}{\lambda}}.
$$

若 1024 张设备的单设备 MTBF 都为 365 天，每次设备故障均中断作业且彼此独立，则作业 MTBF 约为 $365\times86400/1024=30\,796.875$ s。官方 Qwen3-8B 的 14-byte 检查点为 114.67 GB，在 8 GB/s 下 $c\approx14.334$ s，得到 $\tau^*\approx939.6$ s，即约 15.7 分钟。[^interval]

这不是通用推荐周期。相关故障会改变 $\lambda$；保存期间也可能故障；恢复可能失败；异步路径还存在多个在途恢复点。配套较完整的 Poisson 重试模型在同组输入下给出约 930.1 s，接近一阶结果，说明此处一阶模型适合估计量级。故障率很高时，一阶损失甚至可能超过 1，届时不能把它当作精确比例。

> **实验 10-6 · 延伸：保存周期与预期损失**
>
> 复算 939.6 s 的一阶最优值，并比较 300、900、1800 s 三个周期。增加一个每日发生一次的作业共同冲击，重新计算；解释为何不能把该共同冲击对每张卡重复累加。再讨论异步保存中应使用哪一个已提交快照定义重做量。

## 10.5 RL：阶段协作、权重交接与样本一致性

### 10.5.1 一轮真实的生成、验证与更新

RL 训练需要模型先生成数据。一次循环从 prompt 出发，经 rollout 生成回答，取得奖励或环境反馈，构造优势与训练样本，完成梯度更新，再把新权重交给生成端。不同算法和任务启用的角色不同，不应为一个未使用 critic 的配方强行计入完整 critic 模型。

配套已有实验使用 Qwen2.5-0.5B-Instruct、固定 verl 与 vLLM，沿真实官方路径完成上述循环。它承担流程与更新核验；本章 Qwen3 与 V4 的规模预算仍需各自计算，不能从 0.5B 的两步时长按参数比例外推。

**例：四个 prompt 如何变成一次更新？** 每题生成两条回答，共八条；有效回答长度为 2、3、2、3、3、2、3、2，共 20 token，包含 EOS。输入计算使用的有效位置共 366，但这不是该配方的损失分母。八个单回答微批都以 20 为共同分母，累积后裁剪，再执行一次 AdamW 更新。[^verl]

用一个教学标量检查归一化：设短回答每 token 损失为 $\theta/5$，长回答为 $2\theta/5$，则

$$
\mathcal L=\frac{8\theta/5+12(2\theta/5)}{20}=\frac{8\theta}{25},\qquad
\frac{d\mathcal L}{d\theta}=0.32.
$$

若每条回答先平均、再平均八条，梯度变为 0.30；若共同分母后再除以八，变为 0.04。这个简单例子说明执行切分怎样无意间改变训练目标，标量损失并非对实验模型梯度的重新测量。

已有实测还揭示了“参数变化”与“学习”的区别。算术精确奖励的 GRPO 主任务中，同组回答奖励相同，优势全零，策略梯度也为零；FP32 参数的变化来自 AdamW 权重衰减。独立控制使用与正确率无关的固定符号奖励，两步有非零梯度和权重更新，最终算术验证却只有 2/4 正确。链路、更新和质量必须分别检查。

### 10.5.2 阶段配比、共置与分置、权重交接

设生成、验证和学习的服务能力分别为 $r_g,r_v,r_l$。若工作单位一致、阶段可流水执行、没有其他瓶颈，稳态供给受最慢阶段限制。再假设验证后只有比例 $q$ 符合版本准入规则，则学习输入的上界为

$$
r_{\mathrm{effective}}\le\min\left(r_l,q\min(r_g,r_v)\right).
$$

例如教学输入为 12、6、8 条轨迹/s，$q=0.75$，有效上界为 4.5 条/s。继续扩生成池无助于这个上界。真实轨迹长度不同，阶段预算应按 token、验证工作和更新工作进一步分解；$q$ 也不是任何框架固定的丢弃率。

**共置**在同一组设备上切换训练与生成，节省重复资源，但交接时可能同时保留训练状态、新权重、接收缓冲和 KV 池。**分置**允许阶段独立扩展与重叠，却需要跨组分发权重，并为两侧分别准备容量。

给 Qwen3-8B 一个明确的教学共置预算：训练状态 40 GiB、生成 KV 池 24 GiB、共有量 4 GiB，BF16 权重按官方参数计算为 15.256 GiB。先恢复全部状态再释放旧状态时，峰值约为 83.256 GiB；先同步权重、释放训练状态，再分配空 KV 池，峰值约为 59.256 GiB。在 64 GiB 净预算下，执行顺序改变了可行性。这里的 40 GiB 是给定驻留假设，不能理解为未分片全参数 Adam 的全部状态。[^handoff]

![RL 的阶段协作与权重交接峰值](ch10/figure-10-6-rl.svg)

*图 10-6：上半部追踪样本、更新和权重版本，反馈箭头表示下一次 rollout 使用已接收的新版本。下半部使用声明的 Qwen3-8B 共置预算比较峰值。图中没有用 0.5B 小模型的阶段时长冒充大模型性能。*

分置后应按目标分片发送权重。Qwen3-235B 的 BF16 专家权重为 423 GiB，EP16 时每 rank 所需专家部分为 26.4375 GiB；非专家权重可能仍需复制。整份逐端单播与按需传输的字节比不能直接当成端到端加速比，生产端网卡、训练侧重组、接收缓冲和装载处理都可能限制交接。

生成优化也会改变配比。历史 token 草稿在当前策略验证后可以加速生成，RhymeRL 用这一思路利用历史轨迹；接受率和维护成本决定净收益。教师固定时，缓存 Qwen3-8B 的 8K 最终 hidden 约需 64 MiB，完整 BF16 logits 约需 2374 MiB；较小缓存会在重用时补做输出投影。缓存版本、重用次数和下游瓶颈共同决定是否值得。[^rl-survey]

> **实验 10-7 · 核心：RL 的阶段配比与吞吐**
>
> 阅读固定 verl 记录，追踪八条回答、奖励、优势、一次更新和接收端权重摘要。解释零梯度主任务与非零更新控制各证明了什么。随后用同一有效样本目标建立生成、验证、学习和交接预算，改变一个阶段的能力并重新分配资源；单独列出版本准入和质量尚需的证据。

### 10.5.3 异步供给、策略版本与长轨迹恢复

同步循环在更新和交接完成后再开始下一批生成。异步循环允许生成与学习重叠，从而减少空闲，但样本可能由旧版本策略产生。数据结构应保留实际行为策略、更新前训练策略和当前优化策略的身份。

令实际采样分布为 $\mu$，更新前策略为 $\pi_{\mathrm{old}}$，当前策略为 $\pi_\theta$。同一 token 与条件前缀下，

$$
r_{\mathrm{update}}=\frac{\pi_\theta}{\pi_{\mathrm{old}}},\qquad
w=\frac{\pi_{\mathrm{old}}}{\mu},\qquad
r_{\mathrm{behavior}}=r_{\mathrm{update}}w.
$$

这个乘积关系在未裁剪时成立，不表示不同裁剪位置等价。若真实行为与当前 logprob 都为 $-3$，比值本应为 1；错误地用 $-3.25$ 的重算值覆盖行为分母，会得到 $\exp(0.25)\approx1.284$，即使参数根本没有更新。重算可以有明确用途，但不能丢失原行为记录。[^identity]

长轨迹恢复又增加状态选择。同权重抢占后，可以保存并取回有效 KV；权重更新后，相同 token 前缀并不意味着旧 KV 仍有效，需要按新权重重建，或保留旧权重继续生成并处理滞后。Qwen3-8B 的 8K BF16 KV 为 1.125 GiB，重建成本应从 prefill 工作计算，不能由容量直接换算成一次内存读取。

若故障后丢弃未完成轨迹并从头重采样，较长轨迹可能更容易被中断，改变进入训练的长度分布。V4 报告中的 token 级日志和恢复机制、AReaL 的部分 rollout 流程，分别提供按 token 与版本追踪状态的设计案例；具体恢复边界仍取决于实现。跨角色恢复、备用资源和环境副作用在第 11 章继续分析。

**例：异步更快到什么程度才有收益？** 设每批生成 40 s、训练 16 s、阻塞两侧的交接 4 s，同步周期为 60 s。若资源独立且允许跨批重叠，理想稳态周期为 $\max(40,16)+4=44$ s。若异步每周期只有比例 $q$ 的工作可计入声明的有效进展，则需 $q/44>1/60$，即 $q>11/15\approx73.3\%$。这个条件忽略填充、排空、排队和 KV 重建，但揭示了单看阶段重叠无法完成判断。

### 10.5.4 MoE Routing Replay 与训练一致性

即使 token 和权重相同，推理与训练引擎也可能因精度、矩阵分块和 batch 形状不同而产生微小数值差异。在 MoE 中，接近边界的路由分数可能使 top-$k$ 专家集合改变。离散选择的变化会沿层传播，产生与策略更新无关的 logprob 差异。

Routing Replay 记录生成时每层、每 token 的逻辑专家 ID，在训练前向重放这些离散选择；路由分数与专家输出仍在当前权重下计算，以保留相应梯度。它既不是重放旧输出，也不能消除所有浮点误差或异步滞后。物理专家副本的迁移不应改变逻辑 ID，因而需要与推理 EPLB 分开表示。

![路由记录在生成端与训练端之间的身份传递](ch10/figure-10-7-replay.svg)

*图 10-7：虚线表示保存下来的离散专家选择，实线表示当前权重下重新计算的值。记录还必须带有可核对的 token、层和版本身份。下方以受支持配方所用 Qwen3-30B-A3B 的 48 层、top-8、8,192 token 计算 ID 载荷，未包括 KV、mask 和其他元数据。*

以 uint16 保存 ID，其载荷为

$$
M_{\mathrm{route}}=8192\times48\times8\times2=6\ \mathrm{MiB}.
$$

改用 int32 即为 12 MiB。多轮前缀复用、packing、上下文切分和重计算会改变记录的查找与排列，容量够小并不能保证对齐正确。应先检查专家集合、token 身份与同权重 logprob，再比较阶段时间和训练质量。[^replay]

已有作者公开 R3 日志的四对配置各有 100 个训练步，日志级 logprob 误差中位数都下降，但 reward 没有一致改善，总步时中位数均略长。四种设置共用一个 seed，不能作为多个独立随机重复；训练 reward 也不是独立测试准确率。这组证据支持局部一致性指标的改善，尚不足以得出普遍收敛或质量结论。

> **实验 10-8 · 延伸：Routing Replay 与训练一致性**
>
> 沿受支持的 Qwen3-30B-A3B 配方或已有记录，比较开关前后的专家集合不一致、同权重 logprob、记录字节与阶段时间。加入一次 packing 重排或缺失记录，说明应在哪一步发现身份错误。质量结论需使用单独的训练和评估证据。

## 10.6 从系统方案到完成期限与硬件选择

### 10.6.1 汇总容量、步时、停顿与恢复

现在回到章首任务。容量表给出能够继续分析的组合；训练步时间轴给出单位工作成本；供数和检查点决定持续运行的停顿；故障模型决定要重做多少工作。若每步完成 $b$ 个有效 token，目标数据量为 $D$，稳定步时为 $t_s$，则最简单的基线为 $\lceil D/b\rceil t_s$，再加入尚未包含的准备、保存、故障与恢复。

一个利用率数字不能替代这份分解。假设 100 张同型卡观察一小时，总容量为 360,000 GPU·s；单卡分配时间合计 350,000，分配率为 97.22%。但若所有必要资源同时就绪的时间为 300,000 GPU·s，已保存进展对应执行为 240,000 GPU·s，同一进展的理想计算基准为 120,000 GPU·s，则

$$
\frac{300000}{360000}\times\frac{240000}{300000}\times\frac{120000}{240000}
=\frac{120000}{360000}=33.33\%.
$$

这组教学数据借鉴 MPG 的分层计量思想。组不齐、运行中断和程序执行效率影响不同部分；不能把 97.22% 与 50% 随意相乘。跨作业汇总应先累加相同定义的设备时间再求比值。计算基准只描述数学工作相对峰值的关系，仍需第 4 章的多资源下界判断瓶颈。[^progress]

### 10.6.2 4090 集群的可行范围

“4090 能否训练这个模型”必须补齐卡数、连接、更新范围和期限。对于本章任务，理想持久容量下界只有六张，但 40% 矩阵口径效率下，计算需求至少为 31 张。更严格地说，即使把匹配峰值的效率设为 100%，也至少需要 13 张；因此八张 4090 无法在声明的 dense BF16 矩阵执行口径下完成 30 天任务，容量如何优化都不能推翻这一计算下界。

31 张仍不是推荐集群。实际部署需要兼容整数分组，还要检查逐卡激活和临时状态，以及PCIe和网卡的最忙路径。若选择八卡一组的主机结构，可能需要从 32 张开始形成候选，但不能把 31 向上取整到 32 就视为可行性已经证明。

以普通 DP 的 BF16 梯度 AllReduce 为示意，完整梯度大小为 16.381 GB。理想环算法每 rank 发送约 $2(d-1)G/d$；$d=32$ 时约为 31.74 GB。若所有这些字节必须经过给定的 25 GB/s 单接口，单向发送服务下界约为 1.27 s，尚未加入启动。它可能与反向部分重叠；分层通信或分片改变通路后，必须重新算接口字节。这个例子说明需要检查具体布局，不能直接作为所有 4090 方案的步时。[^deadline]

完成实验 10-3 时，可将候选标为“被下界排除”“通过必要条件但待测”“在已验证条件下满足目标”。对于第二类，最有用的下一步是能改变判断的测量，例如最忙 rank 的全步时间、参数聚合峰值或跨机归约尾部。

### 10.6.3 替换硬件后的瓶颈与选择

更快的计算单元、更多容量和更强互联分别改善不同约束。同形态 A100／A800 的比较应先固定本地工作与内存条件，再核对实际互联差异；不能因为型号改变就把所有带宽同步降低。H20 也应按具体训练步、精度和软件支持评价，产品定位不能替代任务预算。

设原方案中占比 $f$ 的暴露时间来自某一资源，将其服务能力降低到原来的 $r$ 倍，若其他时间不变且忽略新的重叠变化，则

$$
\frac{T'}{T}=1-f+\frac{f}{r}.
$$

如果该资源只有 5% 的暴露占比，吞吐减半使总时间增加 5%；若占比为 50%，则增加 50%。这正是替换硬件前要先测量瓶颈的原因。图 10-8 用这一教学模型展示敏感性，不给缺少匹配实测的设备填入虚构训练成绩。

![硬件替换时暴露通信比例对完成时间的影响](ch10/figure-10-8-hardware.svg)

*图 10-8：横轴为候选硬件有效通信能力相对基线的比例，纵轴为完成时间比。曲线使用固定暴露通信占比的教学假设，并非 A100、A800 或 H20 的测量。真实替换可能改变重叠、内核效率与容量可行性，需重新校准。*

现有资料尚无 Qwen3-8B／235B 在 A100、A800、H20 上同条件的稳态训练日志。某个 H20 记录的约 3.5 s/步来自 LoRA，不能作为全参数训练校准。此时应报告有效性能区间或使结论翻转的临界值，而不是给出看似精确的跨设备排名。[^hardware]

> **实验 10-9 · 延伸：替换硬件后的训练瓶颈**
>
> 为同一候选方案给出计算、通信与存储的暴露时间。保持任务和本地工作不变，改变一个经过核对的硬件条件，重算期限。若缺少有效性能，求出满足目标的最低 token/s 或最小链路吞吐，并说明公开记录能否支持这个临界条件。

### 10.6.4 模型规模、数据量与期限的敏感性

最后将方法扩展到更大 Dense 教学模型。使用第 3 章的粗略 $6ND$ 公式，固定数据量 $D=20$T token、16,384 张卡、BF16 dense 峰值和 50% MFU。1T 参数模型需要的时间在 A100 80GB SXM、H100 SXM、B200 上分别约为 543、171、75 天。[^scale]

![固定数据量下模型规模与训练完成天数](ch10/figure-10-9-scale.svg)

*图 10-9：配套 Dense 规模模型的条件式结果。卡数为 16,384，数据量为 20T token，MFU 为 50%；虚线标出 90 与 180 天期限。纵轴使用对数刻度，曲线未加入容量、通信可行性和日历停顿的额外修正。*

固定 $D$ 时，工作量与 $N$ 成正比，5T 参数需要五倍时间；若同时取 $D=20N$，则 $6ND=120N^2$，5T 相对 1T 变为 25 倍工作。问题的约束一旦改变，“能训练多大模型”的答案也随之改变。

MoE 不应直接把总参数代入 Dense 的 $6ND$ 作为执行工作。全部可训练参数决定状态容量，每 token 的激活路径决定大部分矩阵工作，专家交换、注意力和不均衡另外计算。真实 Qwen／V4 的方案需要回到官方结构；1T、5T、10T Dense 只是帮助理解规模律的教学情景。

公开训练记录可以校准估算，但要区分阶段配置、累计 token 和本次恢复后的实际运行。已有 SmolLM3 记录中存在 384、288、192 rank 的阶段配置，最后一次恢复实际运行 14,000 步；终点累计 token 包含此前工作，不能将累计工作全部除以本次运行时间。[^public]

满足期限之后，才比较包含设备、能源、准备和重做的总成本。最终方案应交付逐卡状态、训练步时间轴、日历损失、质量条件与未知量。RL 还需交付阶段配比、权重交接峰值和样本准入条件，供下一章配置资源池与验证环境。

> **实验 10-10 · 核心：模型规模与训练完成期限**
>
> 先汇总 Qwen3-8B 的 100B-token、30 天任务，列出能否通过容量、步时、供数和恢复检查。再扫描 Dense 的 1T／5T／10T、90／180 天及不同卡数，分别固定数据量与令数据量随参数增长。选择一份公开训练记录检查工作和时间口径，说明哪些结论能够校准、哪些仍属于教学预测。

## 本章小结

训练状态的容量由表示与所有权决定，峰值还取决于生命周期；状态分片、重计算与卸载以新增通信或工作换取容量。微批与流水可以改善执行组织，但比较必须保持同一更新目标，并把就绪依赖、共享资源和暴露等待放回整步时间。长期训练还受数据供给、检查点提交与故障重做约束。

RL 在这些约束之外引入生成数据和权重反馈。阶段吞吐、样本身份、策略版本与训练质量需要共同评价；恢复可以改变样本分布，数值一致性优化也不能替代质量验证。硬件选择应建立在完整任务预算上：先排除不满足必要条件的候选，再用最能改变判断的测量缩小范围。

## 参考资料与进一步阅读

[^state]: [官方参数与 Adam／ZeRO 状态复算](../calculations/results/training-state-book.md)；[FP32 梯度变体](../calculations/results/training-state-fp32-gradient.md)。本章使用规定的五项状态表示，其他优化器与低精度状态需另算。
[^deadline]: [训练期限与设备下界](../calculations/results/training-deadline-book.md)及[日历时间变体](../calculations/results/training-deadline-calendar.md)。精度、稀疏性与硬件来源在 JSON 中逐项记录。
[^fsdp]: [已有 CPU FSDP2 四配置记录](../experiments/ch10/10-01/fsdp-cpu/README.md)。
[^pipeline]: [填满排空事件模型](../calculations/results/training-pipeline-gpipe-m8.md)、[1F1B 事件模型](../calculations/results/training-pipeline-1f1b-m8.md)、[GEMM 保存身份](../calculations/results/pipeline-gemm-save-1f1b.md)、[选择性重计算](../calculations/results/pipeline-gemm-recompute-products.md)。
[^offload]: [梯度转换复算](../calculations/results/gradient-cast-book.md)、[快链路变体](../calculations/results/gradient-cast-fast-link.md)、[SuperOffload 阅读与转换案例](../case-studies/training-offload-and-casting.md)、[已有 CPUAdam 卸载记录](../experiments/ch10/10-01/deepspeed-offload/README.md)。
[^megascale]: [MegaScale 正式论文](../references/proceedings/NSDI/2024/selected/nsdi24-jiang-ziheng.pdf)及[固定任务、历史配置与计量笔记](../case-studies/collective-paths-and-diagnosis.md)。
[^moe]: [DeepSeek-V3 技术报告](../references/files/papers/deepseek-v3.pdf)、[专家交换与反向分桶案例](../case-studies/expert-dispatch-and-resizing.md)。
[^length]: [训练工作与长度分布推算](../case-studies/training-compute.md)。
[^supply]: [训练供给与共享存储模型](../calculations/results/training-supply-shared-starvation.md)、[已有输入与保存共存实验](../experiments/ch10/10-06/input-io-contention/README.md)。
[^checkpoint]: [检查点布局、ByteCheckpoint 与后台保存阅读](../case-studies/checkpoint-layout-and-loading.md)、[逻辑重分片计算](../calculations/results/checkpoint-reshard-book.md)、[112 GB 时间线](../calculations/results/checkpoint-async-rounded.md)。
[^resume]: [DCP 恢复记录](../experiments/ch10/10-06/README.md)、[真实文本管线恢复](../experiments/ch10/10-06/input-state-recovery/README.md)。
[^fault]: [已有异步保存与提交前故障实验](../experiments/ch10/10-07/README.md)及[无保存／同步／异步对照](../experiments/ch10/10-07/save-baseline/README.md)。
[^interval]: [保存周期与 Poisson 重试模型](../calculations/results/checkpoint-interval-book.md)。
[^verl]: [固定 verl 小模型真实闭环](../experiments/ch10/10-08/README.md)、[loss 与更新的源码核对](../research/2026-infra-survey/qa/verl-recipe-loss-closure.md)。实验目录沿用重组前编号；当前对应实验 10-7。
[^handoff]: [Qwen3-8B 共置峰值](../calculations/results/weight-handoff-qwen8.md)、[Qwen3-235B 分片权重交接](../calculations/results/weight-handoff-book.md)。
[^rl-survey]: [历史草稿与 rollout](../case-studies/history-drafts-and-rollout.md)、[教师缓存复算](../calculations/results/teacher-cache-book.md)、[多作业调度与角色恢复](../case-studies/rl-scheduling-and-recovery.md)。
[^identity]: [RL 状态、概率比与版本](../case-studies/rl-state-and-reproducibility.md)、[DeepSeek-V4 技术报告](../references/files/papers/deepseek-v4.pdf)。
[^replay]: [路由元数据预算](../calculations/results/routing-metadata-book.md)、[作者公开 R3 日志及分析边界](../experiments/ch10/10-09/source-readiness/README.md)。当前对应实验 10-8。
[^progress]: [MPG 论文阅读与设备时间教学例](../case-studies/kernel-and-fleet-efficiency.md)。
[^hardware]: [匹配训练日志的来源缺项](../experiments/ch10/10-04/README.md)。
[^scale]: [Dense 规模与期限](../calculations/results/dense-training-scale-book.md)、[数据量随参数增长的变体](../calculations/results/dense-training-scale-proportional.md)。
[^public]: [SmolLM3 原始训练记录核验](../experiments/ch10/10-10/public-training/README.md)。
