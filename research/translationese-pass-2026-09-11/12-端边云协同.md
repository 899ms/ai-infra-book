# 第 12 章 端边云协同 — 翻译腔清扫记录

章节文件：`manuscripts/12-端边云协同.md`
改动总数：16 处

| # | 修改前 | 修改后 |
|---|---|---|
| 1 | …它在 123 ms 到达，比原定播放的 118 ms 晚 5 ms。 | …它在 123 ms 到达，比原定的 118 ms 晚了 5 ms。 |
| 2 | …包含 15,360 bits，约 $15360/(256000-130000)\approx0.12$ 秒便会耗尽。 | …包含 15,360 bits，大约 $15360/(256000-130000)\approx0.12$ 秒就会耗尽。 |
| 3 | …因此单轮节省的不到一秒时间累计起来，就能让整个任务提前二十多秒完成。 | …因此每轮节省的虽不到一秒，累计起来就能让整个任务提前二十多秒完成。 |
| 4 | …约 765 万参数，它的权重、梯度、主权重与两份矩合计每参数 16 bytes，约 0.12 GB，加上 16.38 GB 的 BF16 基础权重共约 16.5 GB。 | …约 765 万参数。它的权重、梯度、主权重与两份矩合计每参数 16 bytes，约 0.12 GB；再加上 16.38 GB 的 BF16 基础权重，共约 16.5 GB。 |
| 5 | 这时要让另一台机器上的编码方案更快，编码与排队合计只需少花 0.15 ms 以上；在原有的慢速上行链路上，则要省出超过 9 秒。 | 这时，编码与排队合计只要少花 0.15 ms 以上，另一台机器上的编码方案就更快；在原有的慢速上行链路上，则要省出超过 9 秒。 |
| 6 | …每个阶段的启动耗时为 $\alpha$，所以这些归约的单 token 启动时间为 | …每个阶段的启动耗时为 $\alpha$，所以生成一个 token 时，这些归约的启动时间合计为 |
| 7 | …接收方的流量控制根据接收方的缓冲空间，限制发送方还能发送多少数据。 | …接收方的流量控制根据自己的缓冲空间，限制发送方还能发送多少数据。 |
| 8 | …发送方却要等下一批确认到来才能继续发送；扩大可用窗口才会提高这时的实际传输速率。 | …发送方却要等下一批确认到来才能继续发送；只有扩大可用窗口，才能提高这时的实际传输速率。 |
| 9 | …是 Mathis 上界的约 1,850 倍：在丢包不含拥塞信息的路径上，直接测量带宽得到的速率比从丢包反推的上限高出三个数量级。 | …是 Mathis 上界的约 1,850 倍。也就是说，在丢包不含拥塞信息的路径上，直接测量带宽比从丢包反推得到的速率上限高出三个数量级。 |
| 10 | 例如，将语音模型集中部署在美东，美西用户或接入服务就需要跨地域调用。 | 例如，将语音模型集中部署在美东，美西的用户或接入服务都需要跨地域调用。 |
| 11 | 在这里的数据中心间部署中，客户端在调用方的服务器上… | 在这样的数据中心间部署中，客户端在调用方的服务器上… |
| 12 | …可以把另一条上的部分字节移给它，使原本较晚完成的部分提前结束。最优比例因而是让两条路径同时完成，即 $x=B_1/(B_1+B_2)$。 | …可以把另一条上的部分字节移给它，让原本较晚完成的部分提前结束。因此，最优比例应让两条路径同时完成，即 $x=B_1/(B_1+B_2)$。 |
| 13 | 原服务每轮 3.5 秒，继续执行需要 70 秒，因此需要换一种执行方案。 | 原服务每轮 3.5 秒，继续执行需要 70 秒，因此必须换一种执行方案。 |
| 14 | 两者相除，保存约 41 秒，占用的显存就会抵消本次省下的计算。 | 两者相除：状态保存约 41 秒之后，占用的显存就会抵消本次省下的计算。 |
| 15 | （练习 12-1 (c)）…标出限制整个任务进一步缩短耗时的步骤。 | （练习 12-1 (c)）…标出限制任务耗时进一步缩短的步骤。 |
| 16 | 预期节省的时间或成本，还需要留出一部分来承担波动和恢复开销。 | 预期节省的时间或成本，还要留出一部分来应对波动和恢复带来的开销。 |

## 发现但未改动的存疑句

- 12.3.5 节："早期测试轮换 146–405 KB 的八个文件，再以最后一个文件大小描述整组中位数。"——"以最后一个文件大小描述整组中位数"语义含混（是"用最后一个文件的大小来标注中位数"，还是"按最后一组的文件大小取中位数"？），无法在不接触原始测试记录的情况下确定含义，未改。
- 12.1.5 节："……按四条 x16 通道计，合计 85.6 GB/s。"等"合计/共计"挂尾在本章多为并列算式收尾，符合上下文节奏，未视为翻译腔。
