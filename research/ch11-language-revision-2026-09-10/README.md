# 第 11 章中文表达修订

通读正文、图注、练习和配套说明，按语境替换生硬表达：候选改为方案、模型设置或调度器选定的节点；核秒在正文改为累计 CPU 时间，表格采用已定义的 CPU·秒；忙核改为实际使用的 CPU 核数；槽位改为并发调用数或并发容量。环境策略明确写为每轮重新建立环境，在模型调用期间完成准备。

暴露等待改为调用后等待环境就绪的时间；按时质量成功改为按时通过测试；翻转改为具体说明哪一种方案费用更低。保留关键路径、驻留、幂等等已有技术术语，正文通过执行过程解释。同步更新绘图脚本和阅读版，不修改原始计算数据与历史资料。

修订前稿见 before-manuscript.md。
