# 第 11 章连贯性与深入推导修订

本轮保留五节、十八小节的 outline，重写全章论证。修订前正文与绘图脚本保存在本目录的 before-manuscript.md 和 before-build.py。

贯穿题从每秒 10 项三轮任务出发，依次推导 30 忙核、600 GiB、270 模型槽位；提前创建降低内存至 180 GiB，以每项 0.3 核秒换取容量。快速模型把任务从 30 秒降到 21 秒，最终在两个满足 24 秒期限的方案间比较成本。轮间等待的 2.05 秒交点解释模型加速为何改变环境策略。

常驻方案使用提前初始化并循环复用的环境池；工具阶段包含结果持久化与工作状态清理。提前创建的两秒窗口包含文件取得和运行时初始化。两种方案的时间与按使用量费用定义放在计算之前。

新增 platform_design.py 与 platform-design.json 是确定性教学数据，不改动既有 calculations、survey 或实验记录。脚本以精确分数枚举 1,000 项等间隔任务，用半开区间事件扫描复算资源面积及峰值。原始来源继续由 sources.json 锁定。

删除段末自我辩护与反复限定，把实测范围、历史版本、指标口径放进资料注释。小节按条件、机制、计算、结论推进；练习改为改变条件后重新选择。图内不含图号，恢复图将同一策略的两种完成口径相邻分组。

验证：52 项章节检查通过；108 个公式渲染成功；8 张图的 SVG、PNG、PDF 与离线 HTML 已更新，1440／390 像素页面检查通过。八图拼图已人工查看，无可见裁切或文字碰撞。
