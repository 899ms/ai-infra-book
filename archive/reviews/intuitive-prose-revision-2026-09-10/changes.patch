--- a/manuscripts/01-初识 AI Infra.md
+++ b/manuscripts/01-初识 AI Infra.md
@@ -30,7 +30,7 @@
 
 ### 1.1.2 从任务到设备的六层分工
 
-先看 AI 系统怎样处理一次请求。应用提出回答质量和响应时间要求，模型按照自身结构对输入执行一组运算，服务系统安排这些运算在设备上执行。设备保存的数据、完成的计算和交换的结果，共同构成 AI Infra 的工作。图 1-2 按这一过程划分六个层次；后面的估算将逐层追踪同一个请求。
+用户提交一段代码，请模型找出错误。应用把代码和问题交给模型服务，服务选定执行请求的设备，设备读入权重与输入、完成计算，再把生成的回答交回应用。若多张设备共同执行，还要在它们之间传递中间结果。沿着这条路径，我们就能逐步找到等待发生在哪里、哪份数据需要搬运。图 1-2 将这些工作划分为六个层次；各层分别决定任务要求、模型运算、执行安排和物理资源，后面的估算将逐层追踪同一个请求。
 
 ![从应用到设备的六层分工。沿箭头向下，任务逐步落实为运算、程序和物理资源；调度、观测等跨层能力由正文说明。](ch01/figure-1-1-panorama.svg)
 
--- a/manuscripts/02-模型架构.md
+++ b/manuscripts/02-模型架构.md
@@ -295,9 +295,11 @@
 
 ## 2.3 上下文状态表示与访问机制
 
-这里的上下文状态，是模型处理已有上下文后保存、供后续计算复用的表示，例如 KV 缓存或递推状态。上下文长度按 token 位置计数，状态容量按实际保存的字节数计量。
-
-完整上下文 KV 的容量随序列长度增长，累计读取又随生成次数增加。改变模型结构，可以从几个方面降低这些开销：减少每个上下文位置的表示宽度，压缩或选择上下文条目，或者把上下文信息汇总到固定大小的状态中。这些选择影响的对象不同，也会产生不同的新增工作。比较时需要同时计算状态占用的空间，以及选择、读取和更新状态所需的工作。
+模型继续生成时，需要利用前面已经读过的内容。先想象两种记录办法：一种逐页留下记录，要查哪一页就取哪一页；另一种只保留一张定长摘要，每读到新内容，就改写这张摘要。前一种记录随历史增长，后一种大小固定，但不能保证找回每个位置的全部细节。
+
+对应到模型，保存逐位置 KV 的机制留下各位置的键和值，有限状态递推则把历史贡献汇入固定大小的状态。这里的“记录”和“摘要”实际都是供计算使用的数值向量或矩阵，并非自然语言笔记。模型处理已有上下文后保存、供后续计算复用的这些表示，称为上下文状态。上下文长度按 token 位置计数，状态容量按实际保存的字节数计量。
+
+比较这些机制时，先看历史留下了什么，再看新一步怎样取得所需信息：每个位置可以保存得更窄，也可以只直接访问近期位置、将多个位置汇总为较少条目，或先筛选再读取。另一类机制持续更新固定大小的状态，查询从中取得结果。这些安排分别改变表示宽度、条目数量和访问方式；省下的存储与读取，要同新增的选择、压缩和更新工作一起计算。
 
 ### 2.3.1 多头注意力与 KV 共享
 
@@ -353,7 +355,7 @@
 
 ### 2.3.3 局部窗口、上下文压缩与稀疏索引
 
-减少上下文表示宽度之后，还可以改变保留哪些上下文、每次访问哪些上下文。局部窗口只关注最近的一段位置；压缩机制把若干位置合成较少的条目；稀疏选择先确定相关条目，再把它们送入主注意力。窗口决定保留的近期范围，压缩决定长期条目的数量，索引决定当前查询读取哪些条目。索引器先扫描用于筛选的索引表示，再把选中的主状态交给注意力，索引扫描和主状态读取都需要单独计算开销。
+假设模型已经读过一段很长的文本，现在要处理一个新位置。它可以直接读取最近一段位置，也可以读取由多个旧位置汇成的条目，还可以先找出与当前查询相关的条目，再读取它们的主状态。这三种动作分别引出局部窗口、上下文压缩与稀疏选择。它们可以组合使用，但要分别计量：窗口限制近期范围，压缩减少长期条目，索引选择当前要读哪些条目。筛选本身也要读取索引表示并计算分数，不能只统计最后选中的主状态。
 
 先把这里的三个对象分开。**局部滑动窗口注意力（Sliding-Window Attention，SWA）**直接读取最近的一段位置；窗口长 128 时，较早位置退出这一局部窗口。**全局主 KV**是供主注意力读取的长期表示，可能已将多个位置合并；“全局”说明它覆盖较长历史，不表示每次都读取全部条目。**索引 K**是用于检索的另一份较小表示：索引器先用它给条目打分，再让主注意力读取选中的主 KV。索引分数用于选位置，主注意力另用自己的查询计算最终汇总权重。
 
@@ -389,7 +391,7 @@
 
 ### 2.3.4 线性注意力与有限状态递推
 
-线性注意力通过重新组织查询、键和值的计算，避免显式计算所有位置对的注意力分数。在特征维度固定时，下面这种递推形式处理整段序列的运算量随序列长度线性增长，状态矩阵的大小保持不变。
+前面几种机制仍能区分保存下来的各个上下文条目。有限状态递推采用另一种过程：每来一个新位置，就把它的贡献合入已有状态，当前查询直接使用更新后的状态；下一步继续改写这份状态。这对应 2.3 节开头不断改写定长摘要的图景。下面这类线性注意力通过重新组织查询、键和值的计算，避免显式计算所有位置对的注意力分数。在特征维度固定时，状态矩阵大小不变，处理整段序列的运算量随序列长度线性增长。
 
 外积把一个键向量与一个值向量的每对元素相乘，组成二维的关联表。考虑一个由键值外积累加得到的矩阵 $\mathcal S_t$。每个新位置到来时，先读取旧状态，再写入本位置的贡献，当前查询直接从状态矩阵读出结果：
 
--- a/manuscripts/03-推理与训练负载.md
+++ b/manuscripts/03-推理与训练负载.md
@@ -72,7 +72,7 @@
 
 ### 3.1.3 请求分布
 
-TTFT 和输出间隔描述单个请求的体验。多个请求陆续到达时，这些指标还取决于它们是否争用同一时段的处理能力。因此，只知道平均长度和平均请求率还不够，还要看长短请求怎样组合、何时到达。
+前一分钟设备有空闲，后一分钟突然来了许多需要长时间生成的请求，后来的请求仍然要排队：先前空闲的计算时间无法留到现在使用。TTFT 和输出间隔描述单个请求的体验，多个请求陆续到达时，这些指标还取决于它们是否争用同一时段的处理能力。下面保持两分钟的请求总量与平均长度相同，观察到达顺序怎样改变积压。
 
 负载描述应保留到达时间、输入与输出长度的联合分布。长请求既增加自身服务时间，也可能延长状态驻留；短请求提前完成，则可能让后续请求较早开始。因此，平均工作量相同，也可能有不同的等待时间、完成时间和状态占用峰值。先用章首的两分钟负载把这个区别算清，再观察真实系统。
 
@@ -359,7 +359,9 @@
 
 ### 3.4.1 前向计算、反向传播与参数更新
 
-推理用权重求输出，训练还需要计算怎样调整权重才能降低损失。对线性层 $Y=XW$，输入 $X$ 为 $[m,k]$，权重 $W$ 为 $[k,n]$。损失 $\mathcal L$ 是衡量模型输出与训练目标差异的数值，训练希望降低它；梯度描述损失对某个输入或参数的小变化有多敏感。反向传播从输出端沿依赖逆向传递这些敏感度。设后一层反向传来 $\partial\mathcal L/\partial Y$，当前层需要计算两种梯度：
+推理用权重求输出，训练还要利用输出与目标的差异来调整权重。先看最简单的一次乘法：输入 x 乘以权重 w 得到 y。输出端传回损失对 y 的敏感度后，要算损失对 w 的敏感度，就还需要当时的 x；要把敏感度传给前一步，则需要 w。因此，前向把结果交给下一层以后，某些输入还要保留，等反向用完才能释放。
+
+对线性层 $Y=XW$，输入 $X$ 为 $[m,k]$，权重 $W$ 为 $[k,n]$。损失 $\mathcal L$ 是衡量模型输出与训练目标差异的数值，训练希望降低它；梯度描述损失对某个输入或参数的小变化有多敏感。反向传播从输出端沿依赖逆向传递这些敏感度。设后一层反向传来 $\partial\mathcal L/\partial Y$，当前层需要计算两种梯度：
 
 $$
 \frac{\partial\mathcal L}{\partial X}=\frac{\partial\mathcal L}{\partial Y}W^{\mathsf T},\qquad\frac{\partial\mathcal L}{\partial W}=X^{\mathsf T}\frac{\partial\mathcal L}{\partial Y}.
--- a/manuscripts/04-加速器架构.md
+++ b/manuscripts/04-加速器架构.md
@@ -110,7 +110,9 @@
 
 ### 4.2.1 矩阵单元为什么适合神经网络
 
-矩阵单元的优势之一，是让多个乘加单元共享操作数。计算一个 $m\times n$ 输出块时，同一行输入参与 $n$ 个输出元素的计算，同一列权重参与 $m$ 个输出元素的计算。把这些乘加组织到阵列中，操作数便可以在相邻计算位置间传递，部分和留在本地更新；一条指令描述整块工作，控制成本由大量运算共同分摊。
+大矩阵要拆成许多小块计算，矩阵单元正是执行这些小块的硬件。它支持的矩阵指令形状是有限的，程序需要把大任务组织成相应的小步；一张加速器上的多个计算单元可以同时处理不同块。先看其中一块怎样计算，再看形状不合适时会发生什么。
+
+计算一个 $m\times n$ 输出块时，同一行输入参与 $n$ 个输出元素的计算，同一列权重参与 $m$ 个输出元素的计算。矩阵单元让多个乘加位置共享这些操作数：数据在相邻计算位置间传递，部分和留在本地更新。一条指令描述整块工作，控制成本也由大量运算共同分摊。第五章将进一步说明，软件怎样组织块大小和遍历顺序，让这些小步持续取得所需数据。
 
 ![三行三列的乘加阵列示意。输入沿行传递，权重沿列传递，每个乘加位置保留自己的部分和。这里用小阵列解释操作数复用。](ch04/figure-4-matrix-array.svg)
 
@@ -294,7 +296,9 @@
 
 ### 4.3.2 缓存、片上缓冲与寄存器
 
-图 4-9 只回答了数据能否放下。即使容量足够，每次计算都从片外取得操作数也会造成大量等待；将重复使用的数据留在片上，可以减少这部分传输。寄存器保存线程与指令正在使用的值，缓存自动保留最近访问的数据，软件管理的缓冲则由程序明确安排加载和释放。它们把高频使用的数据留在计算单元附近，但片上存储占用芯片面积，容量也比片外内存小得多。
+算一块输出时，输入中的同一个数往往还要参与邻近输出的计算，尚未算完的部分和也要继续累加。如果每做一次乘加都回到片外取数、写回结果，就会产生大量往返。把常用数据留在计算单元附近，像把当前要用的材料放在手边的工作台上，可以减少这些搬运；但近处能放的东西有限。
+
+寄存器保存线程与指令正在使用的值，缓存自动保留最近访问的数据，软件管理的缓冲则由程序明确安排加载和释放。它们都能保存反复使用的数据，但容量、访问方式与管理者各不相同。片上存储还要占用芯片面积，容量通常比片外内存小得多。下面跟踪两块输入和一块部分和，计算它们需要多少空间。
 
 考虑一个 $m=n=128,k=64$ 的乘加块。BF16 输入与权重块各为 16 KiB，合计 32 KiB；FP32 输出累加器为 $128\times128\times4=64$ KiB。设某计算组有 96 KiB 局部缓冲，若操作数与累加器都放在其中，恰好用满。要同时准备下一对操作数，便需要 $2\times32+64=128$ KiB。
 
@@ -362,7 +366,7 @@
 
 ### 4.4.1 数据布局与寻址
 
-数学上用行列索引表示矩阵元素，硬件则通过内存地址访问它们。一个元素占 $b$ bytes、行步长为 $s_r$ bytes、列步长为 $s_c$ bytes 的二维视图，元素地址为
+在矩阵上画出一个小方块，只确定了要取哪些元素。这些元素在内存里未必紧挨着：若原矩阵按行存放，小方块的一行读完后，还要越过未选中的元素，到原矩阵下一行的对应位置继续读取。数学上的分块可以只改变索引与访问范围，不必先复制出一份独立的小矩阵。硬件需要根据步长找到这些元素。一个元素占 $b$ bytes、行步长为 $s_r$ bytes、列步长为 $s_c$ bytes 的二维视图，元素地址为
 
 $$
 \operatorname{addr}(i,j)=\operatorname{base}+is_r+js_c.
@@ -388,7 +392,9 @@
 
 ### 4.4.2 异步搬运与双缓冲
 
-前面的地址图说明了如何找到并取出一块数据。找到之后，还要安排它何时到达：如果总等当前块算完才读取下一块，计算单元就会反复停下来。先看加载和计算这两个阶段。共 $n$ 块数据，每块加载耗时 $t_l$、计算耗时 $t_c$。若取得一块、算完一块，再开始下一块，总时间为 $n(t_l+t_c)$。准备两个缓冲，就能在计算当前块时把下一块装入另一缓冲。加载与计算使用独立资源时，第一块先加载、再计算，后续各块以较慢阶段的节奏完成：
+前面的地址图说明了如何找到并取出一块数据。接下来要让下一块及时到达：如果总等当前块算完才开始读取，计算单元就会反复停下来。准备两个缓冲后，可以一边使用第一个缓冲中的数据，一边把下一块装入第二个缓冲；当前块用完，两块缓冲交换角色。这就是双缓冲。它需要额外空间，换来的是加载与计算在时间上的重叠。
+
+共 $n$ 块数据，每块加载耗时 $t_l$、计算耗时 $t_c$。若取得一块、算完一块，再开始下一块，总时间为 $n(t_l+t_c)$。加载与计算使用独立资源时，采用双缓冲，第一块先加载、再计算，后续各块以较慢阶段的节奏完成：
 
 $$
 T_{\mathrm{pipe}}=t_l+t_c+(n-1)\max(t_l,t_c).
--- a/manuscripts/05-算子与运行时.md
+++ b/manuscripts/05-算子与运行时.md
@@ -1,6 +1,8 @@
 # 第 5 章 算子与运行时
 
-一次矩阵乘需要完成约 103 GFLOPs 的运算，输入、权重和输出总共占 128 MiB。采用一种分块方式时，计算过程中的读写量却达到约 3 GiB。换用更大的输出块，读写量可以减半，但每个块所需的局部存储空间也从 24 KiB 增至 80 KiB，能够同时执行的块因而减少。同一个矩阵乘，仅仅改变分块方式，就会同时改变数据复用程度和并行度。
+同一份输入，刚用于一个输出，接着又要用于旁边的输出：如果它还留在计算单元附近，下一次就可以直接使用；如果已经被换走，就要重新搬来。矩阵乘法的程序需要安排这些使用次序，并给输入和部分和留下合适的空间。仅仅改变一次处理多少输出、先算哪一块，就会改变重复搬运的次数，以及能够同时执行的任务数。
+
+本章的矩阵乘算例需要约 103 GFLOPs 的运算，输入、权重和输出总共占 128 MiB，一种分块实现的读写量却达到约 3 GiB。扩大输出块可以让输入多用几次、使读写量减半，但每个块的局部存储需求也从 24 KiB 增至 80 KiB。这些数字将帮助我们判断，减少搬运能否补偿同时执行的块数减少所带来的影响。
 
 第四章介绍了设备的计算能力、存储容量和数据传输带宽。本章讨论程序如何利用这些资源。我们以一段前馈网络（FFN）为例，先说明主机如何发起计算、设备如何完成任务，再分析矩阵乘的循环与分块，以及相邻算子如何共享中间结果。随后讨论编译器和运行时如何实现这些优化，最后计算这些优化能让完整请求节省多少时间。
 
@@ -93,7 +95,9 @@
 
 ## 5.2 单个算子的分块、布局与实际访存
 
-矩阵乘中，同一个输入元素参与多个输出的计算。同一个权重元素又被多行输入使用。如果将读入的数据保留在局部存储中，就能用一次读取完成多次乘加。要实现这种复用，需要同时考虑循环顺序、局部存储容量和任务分配。
+大矩阵计算可以像切豆腐一样分块：先分成适合处理的小块，再逐块完成。不过，切成什么形状，要同时看计算单元能处理什么，以及它附近能放下多少数据。第四章已经说明，硬件矩阵指令支持有限的形状；整张加速器上的多个计算单元可以同时处理许多块，每份工作内部仍要组织成硬件能够执行的小步。
+
+分块还要让搬来的数据多用几次。沿用第四章的工作台比喻，当前输入和尚未算完的部分和要放在计算单元附近，用完才能腾出空间。输出块太小，同一份输入可能为不同输出块反复搬运；块太大，又会占用更多局部存储，挤掉其他可以同时执行的任务。下面先跟踪一个输出需要哪些数据，再把多个输出组织成块，分析块大小、访问顺序和局部容量怎样共同影响复用。
 
 ### 5.2.1 矩阵形状与重复读取
 
@@ -140,11 +144,11 @@
 
 ### 5.2.2 容量约束下的分块复用
 
-先为一个 $m\times n$ 输出块分配累加缓冲区，每次沿 K 维读入一对输入块：A 块为 $m\times k$，W 块为 $k\times n$。它们相乘，更新同一份输出累加器；换入下一对输入块时，已经得到的部分和继续保留。直到沿 K 完成全部累加，才把结果转为 BF16 并写出。
+先选 C 的一小块作为当前要完成的输出，为这个 $m\times n$ 输出块分配累加缓冲区。每次沿 K 维读入一对输入块：A 块为 $m\times k$，W 块为 $k\times n$。它们相乘，更新同一份输出累加器；换入下一对输入块时，已经得到的部分和继续保留。直到沿 K 完成全部累加，才把结果转为 BF16 并写出。这里安排的是软件为复用数据而组织的块，其内部还可以分解为多次硬件矩阵指令。分块本身不要求先把原矩阵复制成许多独立的小矩阵，具体搬运由执行程序安排。
 
 ![图 5-3 输入块更换而输出累加器保留](ch05/figure-5-tile-working-set.svg)
 
-*图 5-3：一次块乘更新整个 m×n 输出块。A、W 输入为 BF16，累加器为 FP32（每个元素占 4 bytes 的 32 位浮点格式）；沿 K 换入新输入块时，累加器一直保留。图中尺寸符号表示形状，方框面积不按字节数缩放。*
+*图 5-3：固定当前 m×n 输出块，沿 K 依次搬入对应的 A、W 块，反复更新同一份部分和，全部累加结束后才写回。A、W 输入为 BF16，累加器为 FP32（每个元素占 4 bytes 的 32 位浮点格式）。图示为软件分块，一次块乘可包含多次硬件矩阵指令；尺寸符号表示形状，方框面积不按字节数缩放。*
 
 A 块中的一个元素可以用于 n 列输出，W 块中的一个元素可以用于 m 行输出。扩大输出块，就能让一次读入的数据参与更多乘加；同时也要保存更多输入和输出部分和。
 
@@ -228,7 +232,7 @@
 
 这里 SiLU 是激活函数，$\operatorname{SiLU}(x)=x/(1+e^{-x})$；符号 $\odot$ 表示对应位置的元素相乘。SiLU 和乘法都是逐元素运算。Z[i,j] 只依赖 G[i,j] 与 U[i,j]，不同位置之间没有归约。因此，读入同一位置的两个元素后，就能直接算出该位置的最终结果，而无需先生成全部 SiLU 结果。
 
-先比较中间结果 T 的去向。分开执行时，完整 T 要写到下一级存储，再被乘法读回；融合后，一小块 SiLU 结果可以直接交给同一 GPU 内核程序（kernel）中的乘法，用完就释放局部空间。
+先比较中间结果 T 的去向。分开执行时，完整 T 要写到下一级存储，再被乘法读回；融合后，一小块 SiLU 结果可以直接交给同一 GPU 内核程序（kernel）中的乘法，用完就释放局部空间。这就像把工作台上刚处理好的材料直接交给下一道工序，省去送回远处再取出的往返。对应到这里，省去的是中间结果的写回与重读，两步运算仍然都要完成。
 
 ![图 5-8 融合前后中间结果经过的路径](ch05/figure-5-fusion-path.svg)
 
@@ -282,9 +286,11 @@
 
 ### 5.3.3 FlashAttention：分块与在线 Softmax
 
-FlashAttention 由斯坦福大学的 Tri Dao 等研究者与纽约州立大学布法罗分校的合作者于 2022 年提出，针对长序列注意力中间矩阵过大、显存读写量高的问题，重新组织计算顺序与数据存储。[^fa]
-
-前面的流水在一块数据准备好后就开始计算，随后释放该块缓冲。逐元素运算很容易这样处理；注意力的输出却依赖整行分数，处理完当前块时，后面的分数还没有读到。要继续沿用逐块计算和复用缓冲的方式，就必须找到能够正确合并各块结果的归约方法。其中 Q 是查询向量，K 是供查询匹配的键向量，V 是最终按权重汇总的值向量。Softmax 将一行分数转换为总和为 1 的权重。标准注意力先计算 $S=QK^\mathsf{T}/\sqrt d$，再计算 $P=\operatorname{softmax}(S)$，最后计算 $O=PV$。
+计算一行注意力输出时，可以先把各位置的分数变成尚未归一化的权重，用这些权重对值求加权和，最后除以权重总和。可以先处理一块，记下它对加权和与权重总和的贡献，再处理下一块；只要能正确合并贡献，就不必一直保留已处理过的全部分数。困难在于 Softmax 要根据分数求指数，后面出现更大的分数时，还要调整前面累计结果所用的数值基准。
+
+FlashAttention 围绕这一逐块计算过程安排存储，减少长序列注意力中间矩阵的显存读写。它由斯坦福大学的 Tri Dao 等研究者与纽约州立大学布法罗分校的合作者于 2022 年提出。[^fa]
+
+其中 Q 是查询向量，K 是供查询匹配的键向量，V 是最终按权重汇总的值向量。Softmax 将一行分数转换为总和为 1 的权重。标准注意力先计算 $S=QK^\mathsf{T}/\sqrt d$，再计算 $P=\operatorname{softmax}(S)$，最后计算 $O=PV$。同一输出依赖整行分数，当前块之后的贡献也要计入，因此逐块处理需要能够正确合并各块结果的归约方法。
 
 把这三个运算分别交给通用算子，可以直接按公式组合程序，算子之间通过中间张量交换结果。掌握完整注意力计算的数据依赖后，便可以采用另一种编译与执行方式：利用 Softmax 的归约关系，跨过独立算子的边界，在小块数据上连续计算。下面先算出原来完整保存中间张量的开销，再推导逐块计算所需保存的状态。
 
@@ -318,7 +324,7 @@
 
 右侧求和只遍历新块中的 j，旧块的全部贡献已经保存在 $\ell$ 与 u 中，无需重新读回旧分数。
 
-FlashAttention 正是利用这一递推关系省去了完整的 S 和 P。每处理完一块，就更新 m、$\ell$ 和 u；该块的分数和概率用完即可丢弃，其缓冲可以留给下一块。只保存这些统计量，就能继续计算后续块。
+FlashAttention 正是利用这一递推关系省去了完整 S 和 P 的存储。每处理完一块，就更新 m、$\ell$ 和 u；该块的分数和概率用完即可丢弃，其缓冲可以留给下一块。旧块对输出的贡献已经进入统计量，后续无需读回旧分数。在相同注意力范围内，该算的查询与键的匹配、值的加权仍然要做；改变的是计算顺序与中间结果的保存方式，浮点执行还会有舍入差异。
 
 图 5-13 中，旧块留下的是三个统计量，分数缓冲可以反复使用。这样就把存储完整分数矩阵的问题，变成了在快速存储中安排当前块的问题。接下来要确定：一次处理多少行 Q、多少行 K/V，才能更好地利用这块空间。设快速缓冲为 128 KiB，一次保留 a 行 Q 与 FP32 输出累加器，分数块大小为 a×b。K 与 V 按阶段复用同一个 b×d 输入槽，分数和概率复用一个 a×b 槽，另为逐行统计和更新暂存分配三个 FP32 向量。局部存储需求为：
 
@@ -367,6 +373,8 @@
 编译器可以按三个步骤选择实现。首先，把数学运算表示为循环和数组访问，明确可以变换的循环及其读写关系；其次，根据读写依赖和数值规则检查合法性；最后，结合容量和实测时间比较执行成本。下面先用可读的循环展示前两步，再看自动算子代码生成系统 AKG 怎样通过多面体编译组织整个过程。
 
 ### 5.4.2 用循环变换表达分块与融合
+
+前面决定了每次处理多大一块，现在还要决定这些块按什么顺序计算。同样切好的数据，如果刚用过就换走，过一会儿又要重新搬回来，仍然会浪费访问。循环变换让程序表达这些安排：split 将循环拆成“第几块”和“块内第几个位置”；reorder 调整遍历顺序，让即将再次使用的数据有机会留在近处。再选择在哪一层循环保存或使用中间结果，就能明确它要留下多久。
 
 取 $Y=\operatorname{SiLU}(AW)$，沿用 $M=1024,K=4096,N=12288$，矩阵乘用 FP32 累加，结果舍入为 BF16 后再激活。最直接的程序先生成完整 C，再遍历 C 生成 Y：
 
--- a/manuscripts/06-超节点.md
+++ b/manuscripts/06-超节点.md
@@ -101,7 +101,7 @@
 
 ### 6.2.2 张量并行与层内数据交换
 
-多实例能同时处理更多请求，却不会缩短某个请求在单张卡上的执行时间。要让多张卡共同加速一个请求，可以采用**张量并行**（tensor parallelism，TP），把同一算子的矩阵分给多张卡。理解它的关键，是看清局部计算产生完整结果中的哪一部分。
+多实例能同时处理更多请求，却不会缩短某个请求在单张卡上的执行时间。要让两张卡共同完成一次矩阵乘，先要决定各算什么：可以让它们分别算出不同的输出元素，也可以让它们各算同一个输出的一部分贡献。前一种结果需要拼接，后一种结果需要相加。
 
 先用一次完整的矩阵乘法说明两种切分方式。输入为 $x=[2,3]$，权重矩阵为 $W=\begin{bmatrix}1&4\\2&5\end{bmatrix}$，输出是 $xW=[8,23]$。按权重矩阵的列切分时，即把输出特征分给不同设备，卡 0 保存第一列，得到输出的第一个元素 8；卡 1 保存第二列，得到第二个元素 23。两个元素并排拼接，就得到完整输出。
 
@@ -115,7 +115,7 @@
 
 *图 6-4：按权重矩阵的行切分时，两卡产生形状相同的输出部分和。对应位置相加，恢复完整乘法的结果。*
 
-这两种切法在前馈网络 FFN 中可以首尾相接。这里采用第二章的 **SwiGLU**：将输入送入门控投影与上投影，门控结果经过激活函数 SiLU，再与另一支逐元素相乘，最后执行下投影。SiLU 的定义是 $\operatorname{SiLU}(z)=z/(1+e^{-z})$。设本次一起处理 $m$ 个 token，隐藏宽度为 $h$，中间宽度为 $f$。输入为 $X\in\mathbb{R}^{m\times h}$，上投影为 $W_g,W_u\in\mathbb{R}^{h\times f}$，下投影为 $W_d\in\mathbb{R}^{f\times h}$：
+把同一算子的矩阵分给多张卡，称为**张量并行**（tensor parallelism，TP）。上面两种切法在前馈网络 FFN 中可以首尾相接。这里采用第二章的 **SwiGLU**：将输入送入门控投影与上投影，门控结果经过激活函数 SiLU，再与另一支逐元素相乘，最后执行下投影。SiLU 的定义是 $\operatorname{SiLU}(z)=z/(1+e^{-z})$。设本次一起处理 $m$ 个 token，隐藏宽度为 $h$，中间宽度为 $f$。输入为 $X\in\mathbb{R}^{m\times h}$，上投影为 $W_g,W_u\in\mathbb{R}^{h\times f}$，下投影为 $W_d\in\mathbb{R}^{f\times h}$：
 
 $$
 Y=\left[\operatorname{SiLU}(XW_g)\odot(XW_u)\right]W_d.
@@ -336,7 +336,9 @@
 
 ### 6.4.1 根据模型切分方式确定通信需求
 
-Dense 与 MoE 的分工不同，但它们都需要拼接分片、合并部分结果或发送专家输入。把这些操作按开始和结束时的数据分布归类，就得到下面四种集合通信。
+先看四张卡怎样汇总一个四元素向量。每张卡都有四个元素的本地贡献，目标是让每张卡得到完整的逐元素和。可以分两步完成：先求和，并让卡 0 留元素 0 的完整和、卡 1 留元素 1 的完整和，依次类推；再交换这些已求和的元素，让四张卡都取得完整向量。
+
+第一步称为 ReduceScatter，第二步称为 AllGather，两步合起来完成一次 AllReduce。它们分别改变数值和数值所在的位置。Dense 与 MoE 还会需要拼接其他分片、把专家输入发给不同设备；按开始与结束时各卡持有的数据，可以区分下面四种集合通信。
 
 | 操作 | 开始时各卡持有 | 完成时各卡持有 | 用途 |
 |---|---|---|---|
@@ -345,9 +347,7 @@
 | AllGather | 一个张量的不同分片 | 完整拼接结果 | 恢复完整输入 |
 | All-to-All | 发往不同目的地的数据 | 各源卡发给本卡的数据 | 专家派发与结果返回 |
 
-操作名称规定了计算方式和结果在各卡上的分布，算法则规定消息怎样分轮传递。一个 AllReduce 可以先 ReduceScatter，让各卡各自获得一块完整归约结果，再 AllGather，交换这些已归约的数据块。
-
-用四元素向量说明两个阶段各自的作用。各卡开始时都有四个元素的贡献；第一阶段求和后，卡 0 留元素 0，卡 1 留元素 1，依次类推。第二阶段交换这四个已求和的元素。第一阶段中，每卡的输入都是形状与完整输出相同的部分和，第二阶段的输入是已经归约好的四分之一结果。这正对应“先对每块求和，再分发求和结果”。
+操作名称规定了计算方式和结果在各卡上的分布，具体算法则规定消息怎样分轮传递。上述两步方案中，第一步开始时每卡持有完整向量的部分贡献，结束时持有四分之一向量的完整和；第二步只交换这些已经求好的和。下面将继续跟踪其中一块，看看它怎样沿环传递。
 
 各卡开始通信的时刻也会影响等待时间。设四卡分别在 0、0、0、2 ms 发起一次集合通信，所有卡都发起通信后，再用 0.4 ms 完成交换。前三卡等待 2.4 ms，最后一卡等待 0.4 ms；把交换加快一倍只省 0.2 ms，消除最后一卡的计算延误却省 2 ms。前三张卡的调用时间较长，是因为它们还要等待最后一张卡完成前面的计算。[^collective-path]
 
--- a/manuscripts/07-数据中心网络.md
+++ b/manuscripts/07-数据中心网络.md
@@ -358,11 +358,11 @@
 
 ### 7.4.1 数据分片的生成、使用与缓冲区复用
 
-前一节把槽位占用时间作为已知条件。现在要解释它由哪些事件决定：数据已发送、接收方已能读取、接收方已经用完，是三个不同的时刻。
+数据送到以后，接收方可能还没开始用，也可能正在使用。这像把装有材料的托盘交给下一道工序：托盘已经送到，对方仍要花时间处理其中的材料，在用完之前不能把托盘中的材料换成下一批。对应到通信，发送完成、数据对接收方可见、接收方消费完成，需要分别判断。前一节把槽位占用时间作为已知条件，这一节沿这些事件确定缓冲区何时可以释放。
 
 在分层归约中，卡 0 将本地分片交给卡 4，卡 4 把它与自己的分片相加。发送者需要知道源缓冲何时可以改写，接收者需要知道目的缓冲何时可以读取，下一次操作需要知道这块目的缓冲何时可以覆盖。三者对应不同的事件。
 
-一份分片从生成到释放，要经过以下过程：生成数据、写入远端、使数据对接收方可见、发出就绪通知、读取并处理数据，最后释放空间。发送完成后，源缓冲区可以复用；接收方看到就绪通知后，可以开始使用数据；接收方使用完毕后，目的缓冲区才可以存放下一份数据。
+一份分片从生成到释放，要经过以下过程：生成数据、写入远端、使数据对接收方可见、发出就绪通知、读取并处理数据，最后释放空间。源缓冲能否复用，要看所用接口的完成语义是否保证传输不再读取它；接收方开始使用数据，需要保证写入已对它可见，并按约定发布和观察就绪通知；目的缓冲能否覆盖，还要等接收方不再使用其中的数据。网络传输完成本身不能代替消费完成通知。
 
 例如，发送在 5 μs 完成，消费者在 8 μs 开始读取、12 μs 结束。源端可以按发送完成条件复用自己的源缓冲，目的缓冲则要到 12 μs 才能覆盖。如果下一次写入在 6 μs 覆盖目的地址，消费者读到的就是下一份数据；网络传输的每个字节都正确，接收程序却使用了错误的数据。
 
--- a/manuscripts/08-单实例推理.md
+++ b/manuscripts/08-单实例推理.md
@@ -154,7 +154,9 @@
 
 ### 8.2.2 从固定批次到连续批处理
 
-前面的 batch 分析假定一轮中有 $b$ 条请求参与计算。但请求的输出长度不同，它们不会同时结束；能否及时补入新请求，决定了下一轮还能有多少行。固定批处理先收集一组请求，等这组请求全部结束，再开始处理下一组。假设两条请求同时开始，一条输出八个 token，另一条只输出两个。短请求结束后，长请求还要继续生成六个 token，短请求空出的批次执行位置却一直闲置。这里的执行位置表示本轮允许接纳的一条活跃请求，与第七章记录通信请求的槽位属于不同资源。**连续批处理**在每次迭代结束后接收新请求，下一轮就能使用这个空位。
+两条请求同时开始生成，一条需要八个 token，另一条只需要两个。短请求结束以后，长请求还要继续六步。如果一定要等这两条都结束才接下一组，短请求空出的执行位置就会一直闲置。**固定批处理**按整组接收、整组结束；**连续批处理**则在迭代边界重新安排活跃请求，空出位置后即可接纳新请求。
+
+前面的 batch 分析假定一轮中有 $b$ 条请求参与计算，实际行数会随请求结束和新请求加入而变化。这里的执行位置表示本轮允许接纳的一条活跃请求，与第七章记录通信请求的槽位属于不同资源。能否及时补入新请求，决定了下一轮还能有多少行参与计算。
 
 新请求更早进入，也意味着它的输入计算更早占用设备。若新请求带有很长的提示，prefill 就会延长已有请求两次 decode 之间的等待。调度器因此要在两件事之间分配设备时间：处理新请求的输入，以及让已有请求继续生成。[^schedule-survey]
 
@@ -221,7 +223,9 @@
 
 第 8.1 节按最大输出长度为每条请求预留空间，保证它能持续生成。如果请求提前结束，预留空间的尾部就一直没有使用。另一种办法是随生成过程逐步扩展内存，但若必须保持空间连续，邻接区域被其他请求占用时，就需要搬运数据或等待。
 
-**分页分配**将序列分成固定大小的块。**逻辑块**按序列位置编号，**物理块**是实际保存 KV 的内存区域。每条请求维护一张**块表**，记录逻辑块对应哪个物理块。序列增长到需要新块时，引擎从空闲池分配一个物理块，并在块表中增加映射。注意力计算按序列顺序查询块表，再读取相应的 KV，因此物理块可以分散存放。
+可以把这种分配想成活页册：阅读顺序由页码确定，各页不必放在相邻位置，只要记得每一页在哪里。序列增长时，找一个空位放新页，再补上它的位置记录，就不必为了连续排列而搬动整册内容。
+
+**分页分配**将序列分成固定大小的块。**逻辑块**按序列位置编号，相当于页码；**物理块**是实际保存 KV 的内存区域；每条请求维护的**块表**记录逻辑块对应哪个物理块。序列增长到需要新块时，引擎从空闲池分配一个物理块，并增加映射。注意力计算根据块表找到所需位置的 KV，因此物理块可以分散存放。这里讨论的是 KV 在内存中的分块寻址，多级存储中的换入换出将在后文另行讨论。
 
 ![从逻辑块查到物理块](ch08/figure-8-page-map.svg)
 
--- a/manuscripts/09-分布式推理.md
+++ b/manuscripts/09-分布式推理.md
@@ -87,7 +87,7 @@
 
 第 8.2 节已经用分块 prefill 缩短了长输入对 decode 的阻塞。本节保持相同模型和请求，进一步改变执行位置。PD 分离把两个阶段放入不同的服务池，使它们拥有独立的队列、批处理和资源配比。DistServe 与 Splitwise 是研究 P、D 分离部署的推理系统。同构设备可通过独立队列减少阶段间的调度干扰，异构设备还可以利用各自更擅长的阶段。[^pd]
 
-PD 将 prefill 和 decode 分配给两个服务池，但两个池都要执行完整的前向计算。Qwen3-8B 的 P 处理 8192 个输入位置，D 随后调用 128 次；每次都经过 attention 和 FFN。于是 P、D 都需要相应权重，而上下文 KV 还要从 P 交给 D。独立批处理带来的收益，首先要补偿这次新增交接。
+这种分工像是先由一个人读完材料，再交给另一个人继续处理：接手者需要已有的工作记录，才能接着往下做。对本例模型，这份需要交接的记录就是上下文 KV。P 和 D 分别处理同一请求的不同阶段，每个阶段都要执行完整的前向计算。Qwen3-8B 的 P 处理 8192 个输入位置，D 随后调用 128 次；每次都经过 attention 和 FFN。因此，两个池都需要相应权重，KV 则从 P 交给 D。独立批处理带来的收益，首先要补偿这次新增交接。
 
 应用通过一次调用请求完整的模型推理，服务内部则可以按阶段安排不同资源。模型的数据依赖确定了阶段之间的切分位置，KV 给出了两阶段之间需要交接的状态，负载比例决定各池需要多少资源。利用这些信息，服务可以在统一调用接口之下，为输入处理和逐步生成分别选择批次与设备。接下来用传输量和处理能力计算这种分工的收益。
 
@@ -193,7 +193,9 @@
 
 ### 9.3.2 CPU／GPU 协作
 
-第 8.4.3 节将权重暂存主存，使用时搬到 GPU，计算位置保持不变。这里比较另一种选择：权重留在主存，由 CPU 执行专家计算。KTransformers 展示了这类 CPU／GPU 协作的实际路径。GPU 可以处理 attention 和部分常驻专家，CPU 处理分配给自己的专家；提交 CPU 工作后，GPU 执行可并行的分支，最后同步并合并结果。热点专家、共享专家以及 prefill 阶段可以采用不同的设备分工。分支汇合时，下一层必须等两侧都完成后才能开始。[^kt]
+一份专家权重已经放在主存里，GPU 上的输入需要用它计算。可以把整份权重搬到 GPU，也可以把输入送到 CPU，让 CPU 在权重所在的位置完成计算，再把结果传回。两条路径交付同一份输出，跨链路的对象却分别是权重与输入、输出。先沿图中的箭头看清传什么，再比较字节数和计算时间。
+
+第 8.4.3 节采用第一条路径，将权重暂存主存，使用时搬到 GPU。KTransformers 展示了第二类 CPU／GPU 协作的实际路径：GPU 可以处理 attention 和部分常驻专家，CPU 处理分配给自己的专家；提交 CPU 工作后，GPU 执行可并行的分支，最后同步并合并结果。热点专家、共享专家以及 prefill 阶段可以采用不同的设备分工。分支汇合时，下一层必须等两侧都完成后才能开始。[^kt]
 
 ![专家权重搬运与 CPU 就地计算](ch09/figure-9-5-local.svg)
 
--- a/manuscripts/10-训练系统.md
+++ b/manuscripts/10-训练系统.md
@@ -152,7 +152,9 @@
 
 ### 10.2.2 激活保存与选择性重计算
 
-参数分片减少了权重和优化器状态的复制，前向保存的激活仍会占用显存。要继续节省空间，需要了解反向计算为什么要读取这些激活。考虑矩阵运算 $Y=XW$，其中 $X$ 为输入、$W$ 为权重、$Y$ 为输出。记损失为 $\mathcal L$，$dY=\partial\mathcal L/\partial Y$ 是后续计算传来的梯度，$dX$ 和 $dW$ 分别是损失对输入与权重的梯度；上标 $\mathsf T$ 表示矩阵转置。反向要计算
+前向计算留下的激活，像草稿纸上记录的中间步骤：反向计算回到这一层时，还要用到其中一些值。草稿全部保留，取用方便，却占空间；只保留部分步骤，缺失的值就要在使用前重新算出。参数分片已经减少了权重和优化器状态的复制，**激活重计算**进一步用额外计算换取这些中间值的存储空间。
+
+考虑矩阵运算 $Y=XW$，其中 $X$ 为输入、$W$ 为权重、$Y$ 为输出。前向计算结束后，$X$ 还要等到求权重梯度时使用。记损失为 $\mathcal L$，$dY=\partial\mathcal L/\partial Y$ 是后续计算传来的梯度，$dX$ 和 $dW$ 分别是损失对输入与权重的梯度；上标 $\mathsf T$ 表示矩阵转置。反向要计算
 
 $$
 dX=dY W^{\mathsf T},\qquad dW=X^{\mathsf T}dY.
@@ -162,7 +164,7 @@
 
 *图 10-11：矩阵反向产生两路梯度：dX 交给前一层继续反向，dW 交给本层的归约和参数更新。两路计算都读取上游梯度 dY。*
 
-计算参数梯度需要前向输入 $X$。保存 $X$ 需要让它从前向一直保留到反向计算用完它；释放 $X$ 则要求反向使用前重新生成它。**激活重计算**只保留部分中间结果。反向计算需要其他结果时，再从已保留的结果出发，重新执行相应的前向计算。这样增加了计算量，却减少了需要长期保存的数据。
+计算参数梯度需要前向输入 $X$。保存 $X$，就要让它从前向一直留到反向计算用完它；释放 $X$，则要在反向使用前从已保留的输入或中间结果出发，重新执行相应的前向计算。选择哪些值保留、哪些值重算，要看省下多少空间，以及重新得到它们需要多少工作。
 
 **例：反向传播前重算门控乘积能节省多少激活存储？** Qwen3-8B 的一个多层感知机（MLP，即本例的前馈子网络）先得到门控结果 $a$ 和上投影结果 $u$，再形成 $h=a\odot u$，最后计算 $Y=hW_{\mathrm{down}}$。下投影的参数梯度需要 $h$。如果已为非线性算子的反向计算保留了 $a,u$，可以在下投影反向前重新相乘，就不必一直保存 $h$。
 
@@ -369,7 +371,9 @@
 
 ### 10.4.2 可恢复状态与布局变换
 
-上一节已经说明，恢复训练要从正确的数据位置继续。模型一侧也有同样要求：下一次参数更新既取决于当前权重，也取决于优化器保留的历史。因此，检查点需要权重、Adam 的一阶矩与二阶矩、步数、学习率状态、随机状态和下一批数据的组成。学习率控制更新步长，随机状态决定后续随机采样的序列；保存它们可以从中断处继续同一训练过程。权重相同而优化器的矩状态不同，下一次参数更新的方向便可能不同；数据读取位置相同而打包时剩余的 token 不同，下一条训练序列也会改变。
+设训练刚完成一次参数更新，此时停机。恢复后，要接着训练，需要知道当时的权重、下一批该读哪些数据，以及优化器已经累积了哪些历史。只拿到权重，可以执行前向推理，却不足以确定训练的下一次更新。因此，训练检查点要把能共同确定后续执行的状态保存在同一个一致的进度上。
+
+对这里采用 Adam 的训练，需要保存权重、一阶矩与二阶矩、步数、学习率状态、随机状态和下一批数据的组成。学习率控制更新步长，随机状态决定后续随机采样的序列。权重相同而优化器的矩状态不同，下一次参数更新便可能不同；数据读取位置相同而打包时剩余的 token 不同，下一条训练序列也会改变。前面的激活重计算解决一次前反向中哪些中间值可以重建，检查点则要支持中断后的整个训练过程继续执行。
 
 一次训练迭代结束后，下一批梯度由反向重新产生。此时保存权重 2 bytes、主权重 4 bytes 和两份 Adam 状态 8 bytes，共每参数 14 bytes。Qwen3-8B 的这部分检查点约为 115 GB。在一次训练迭代结束时，把模型状态和已处理到的数据位置一同保存，就能确定恢复后该用哪些参数、从哪一批数据继续训练。
 
--- a/manuscripts/11-资源调度与运行环境.md
+++ b/manuscripts/11-资源调度与运行环境.md
@@ -1,8 +1,8 @@
 # 第 11 章 资源调度与运行环境
 
-本章把用户提交、需要交付一个最终结果的工作称为**任务**；一次模型请求或一次工具执行只是其中的一个步骤。**控制器**保存任务记录并决定下一步调用什么，**执行器**实际运行工具，**运行环境**承载工具所需的文件、进程和内存。
-
-代码修复服务收到一项任务后，需要请求模型、修改文件、执行测试，并根据测试结果继续下一轮。模型调用使用推理服务，工具使用 CPU，工作目录和进程占用内存。一项任务需要协调使用这些资源，最终交付通过测试的补丁。
+代码修复服务收到用户提交的问题后，先让模型生成修改，再让工具改文件、执行测试，把测试结果交回模型，继续下一轮，直到得到通过测试的补丁。这项需要交付最终结果的工作称为**任务**；一次模型请求或一次工具执行只是其中的一个步骤。
+
+**控制器**保存任务记录并决定下一步调用什么，**执行器**实际运行工具，**运行环境**承载工具所需的文件、进程和内存。模型调用使用推理服务，工具执行使用 CPU，环境中的进程和缓存占用内存。沿着每次调用、返回和等待，可以分别计算这些资源在什么时候被使用、使用多久。
 
 前两章讨论了推理和训练服务的构建。本章研究怎样把模型服务、工具环境和共享资源组织成一个任务运行系统。我们从一个平台设计题开始，逐步讨论何时创建和释放环境、如何安排任务、如何选择模型，以及如何从失败中恢复。
 
@@ -27,6 +27,8 @@
 
 每轮先由模型用 9 秒生成工具调用，再由工具执行 1 秒。下一轮模型调用需要本轮的测试结果，因此三轮必须依次进行，总时间为 $3\times(9+1)=30$ 秒。第一轮结束于第 10 秒，第二轮结束于第 20 秒，第三轮结束于第 30 秒。
 
+等待模型给出下一次工具调用时，工具暂时不占用 CPU，但本方案仍保留它的环境。这像一项工作暂停了，材料还摊在桌面上：执行停下来，并不自动腾出已占的空间。这里具体留下的是环境的进程与内存；工作目录和工具结果已经在持久存储中，是否值得销毁环境、下轮再建，将在后文比较。
+
 在这 30 秒里，工具只占用一个 CPU 核执行三次，每次 1 秒，累计 CPU 时间为 3 秒。这里的 CPU 时间是各个核实际执行时间的总和，记作 CPU·秒：一个核执行 3 秒，或三个核各执行 1 秒，都是 3 CPU·秒。环境始终占用 2 GiB 内存，内存占用量与时间的乘积为 $2\times30=60$ GiB·秒。下面先画模型与工具的执行顺序，再以相同横轴画环境内存。
 
 ![模型调用、工具工作和环境驻留的三轮时间线](ch11/figure-11-1-timeline.svg)
--- a/manuscripts/12-端边云协同.md
+++ b/manuscripts/12-端边云协同.md
@@ -155,7 +155,9 @@
 
 ### 12.2.1 完整模型调用与分阶段执行
 
-前面按图片大小估算上传时间，默认传送的是原始输入。将部分模型移到端侧后，这个默认条件就变了。把整个模型放到远端，需要上传输入并下载结果。把编码器（将图片或音频转换为模型使用的数值特征的子网络）留在端侧，则需要上传编码器的输出。这样，即使执行同一个任务，上传的也可能是图片或特征；两者的数据量取决于各自的编码方式。
+同一张截图，可以先发给服务器，让服务器把它转成模型特征；也可以先在终端生成特征，再把特征发过去。两条路径中，跨网络的对象已经不同。前面的图片算例说明了文件传得慢会让计算等待；现在还要看，把一部分计算移到端侧以后，真正需要发送的东西变成了什么。
+
+将图片或音频转换为模型所用数值特征的子网络，称为编码器。把整个模型放到远端，需要上传输入并下载结果；把编码器留在端侧，需要上传编码器的输出。图片文件和特征张量的数据量由各自的表示方式决定，端侧先做了计算，并不保证上传量随之减少。
 
 对于截图 Agent，压缩图片中的纹理和重复区域可以用较少字节表示；视觉编码器却要把图片展开为每个视觉位置的通道值，以供语言模型读取。前者追求紧凑存储，后者服务于后续计算。在端侧运行编码器，既改变了执行编码的设备，也把上传对象从图片换成了特征。
 
