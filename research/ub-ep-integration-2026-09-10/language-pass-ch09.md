# 第 9 章语言通读：逐句修改对照

本文件记录 2026-09-11 全书通读在本章留下的净改动。

## 全书通读（2026-09-11，按最终文本与通读前快照逐行对照）

共 124 行有改动；英文术语按“首次出现保留中文注释、之后用英文”的原则已回退，下表是回退之后的净改动。省略号表示只截取了改动附近的文字。

| 修改前 | 修改后 |
|---|---|
| 第八章讨论了怎样通过批处理、请求调度、缓存和推测解码，提高给定计算资源组合的推理效率。这些方法既适用于单卡，也适用于多卡协同执行的实例。本章进一步研究计算… | 第 8 章讨论了怎样用批处理、请求调度、缓存和推测解码，提高一组给定计算资源的推理效率。这些方法既适用于单卡，也适用于多卡协同执行的实例。本章进一步研究计算… |
| …。将 prefill 与 decode 分开，可以为两个阶段分别配置计算资源；将 attention 与专家计算分开，可以利用不同计算资源的计算和存储能力；共享 KV 则让请求使用其他执行位置留下的状态。这些组织方式可能发生在一个推理实例内部，也可能连接多个独立调度的服务池。下面直接分析执行位置、调度关系和数据流向。 | …。将 prefill 与 decode 分开，可以为两个阶段分别配置计算资源；将注意力与专家计算分开，可以利用不同计算资源的计算和存储能力；共享 KV 则让请求使用其他执行位置留下的状态。这些组织方式可能发生在一个推理实例内部，也可能连接多个独立调度的服务池。下面依次分析执行位置、调度关系和数据流向。 |
| …fill 的 A 类执行单元（worker）和四个擅长 decode 的 B 类 worker，怎样处理每秒到达的四个请求？每个请求输入 8192 个 token、输出 12… | …fill 的 A 类执行单元（worker）和四个擅长 decode 的 B 类执行单元，怎样处理每秒到达的四个请求？每个请求输入 8192 个 token、输出 12… |
| 先区分硬件组织与服务组织。GPU、NPU 等加速器及 CPU 提供计算能力和相应存储资源，进程运行模型及通信逻辑；rank 是进程在一个通信组中的编号。一个推理实例可以由多个这样的进程协同执行。完整服务副本则拥有完成整个模型推理所需的权重和执行能力，可以与其他副本分别处理请求。 | 先区分硬件组织与服务组织。GPU、NPU 等加速器和 CPU 提供计算能力与相应的存储资源，进程在它们上面运行模型和通信逻辑；通信组内的每个进程有一个编号（rank）。一个推理实例可以由多个这样的进程协同执行。完整服务副本则拥有完成整个模型推理所需的权重和执行能力，可以和其他副本分别处理请求。 |
| …各自接收请求。后两种进一步改变分工：PD 按生成阶段划分，AF 按层内算子划分。各组织方式由相应的调度器管理其负责的计算。 | …各自接收请求。后两种进一步改变分工：PD 按生成阶段划分，AF 按层内算子划分。每种组织方式都有自己的调度器管理它负责的计算。 |
| …注意力一侧产生的激活交给前馈一侧，结果再返回。与一次阶段交接相比，这条数据通路要随着模型层和生成步骤反复经过。 | …注意力一侧产生的激活交给前馈一侧，结果再返回。与一次阶段交接相比，这条数据通路要在每一层、每一步生成中反复走过。 |
| 比较这些部署方式时，先使用第八章得到的运行配置和处理速度，再改变计算资源之间的分工。这里将 P、D 由同一组执行资源处理的方式称为共置。考虑一个已经调优的共置实例。请求的输入长度为 $L$，总输出长度为 $G$，prefill 计算最后一个输入 token 的 **logits**，即转换成 token 概率之前的词表分数，并由此采样首个输出。因此，在本章采用的生成约定下，后续 decode 调用为 $G-1$ 次。最后一个返回 token 尚未送入模型，因此输出数比后续 decode 调用数多一。 | 比较这些部署方式时，先使用第 8 章得到的运行配置和处理速度，再改变计算资源之间的分工。这里把 P、D 由同一组执行资源处理的方式称为**共置**。考虑一个已经调优的共置实例。请求的输入长度为 $L$，总输出长度为 $G$，prefill 计算最后一个输入 token 的 **logits**，即转换成 token 概率之前的词表分数，并由此采样首个输出。在本章采用的生成约定下，后续 decode 调用为 $G-1$ 次：最后一个返回的 token 尚未送入模型，所以输出数比后续 decode 调用数多一。 |
| …作的资源单元。若恰好只有两个单元，突发到达的额外请求就会一直积压，因为两个单元都在忙于处理后续请求；增加可用资源，才能处理突发请求。 | …作的资源单元。若恰好只有两个单元，突发到达的额外请求就会一直积压，因为两个单元都忙于处理源源不断的后续请求；只有增加资源，才能消化突发。 |
| … 部署方式（张量并行 TP 将专家矩阵二分，专家并行 EP 将专家分成四组）用八个 rank 处理同一批请求：每两个 rank 分担专家矩阵，四个 EP 组分别持有不同专家。attention 和 KV 在四个 EP 组间复制，因此一条请求的 8K 上下文在整个组内占 5.875 GiB，是单份逻辑 KV 的四倍。这八个 rank 共同组成一个推理实例；要增加独立处理请求的副本，还需另外安排完整的权重与状态。[^ownership] | … 部署方式（张量并行 TP 将专家矩阵二分，专家并行 EP 将专家分成四组）用八张卡处理同一批请求：每两张卡分担专家矩阵，四个 EP 组分别持有不同的专家。注意力和 KV 在四个 EP 组间各复制一份，因此一条请求 8192 个 token 的上下文在整个实例内占 5.875 GiB，是单份逻辑 KV 的四倍。这八张卡共同组成一个推理实例；要增加能独立处理请求的副本，还要另外安排一套完整的权重与状态。[^ownership] |
| …上下文状态的保存与读取范围。这些部署方式改变了系统的不同部分，可以逐项引入，也可能组合使用。 | …上下文状态的保存与读取范围。这些部署方式改变了系统的不同部分，可以逐项引入，也可以组合使用。 |
| …stServe 与 Splitwise 是研究 P、D 分离部署的推理系统。同构计算资源可通过独立队列减少阶段间的调度干扰，异构计算资源还可以利用各自更擅长的阶段。[^pd] | …stServe 与 Splitwise 是研究 P、D 分离部署的推理系统。同构的计算资源可以通过独立队列减少两个阶段之间的调度干扰，异构的计算资源还可以各自承担更擅长的阶段。[^pd] |
| …的 P 处理 8192 个输入 token，D 随后调用 128 次；每次都经过 attention 和 FFN。因此，两个池都需要相应权重，KV 则从 P 交给 D。独立批处理带来的收益，首先要补偿这次新增交接。 | …的 P 处理 8192 个输入 token，D 随后调用 128 次；每次都经过注意力和前馈网络。因此两个池都需要完整的模型权重，KV 则从 P 交给 D。独立批处理带来的收益，首先要抵得过这次新增的交接。 |
| … 数据，固定开销只占万分之一左右，提高载荷带宽更有助于缩短等待时间。而在 AF 的小消息交接中，固定开销会占据更大比例。[^handoff] | … 数据，固定开销只占万分之一左右，提高载荷带宽更有助于缩短等待时间。而在 AF 逐层交接的小数据量传输中，固定开销会占更大的比例。[^handoff] |
| …份双缓冲交接时，传输结束前源端保留完整状态，目的端分配完整接收缓冲。因此，源端和接收端各占用 1.125 GiB，共 2.25 GiB。采用分块传输时，接收端确认收到某一块后，源端便可提前释放该块，缩短两端重复驻留的时间；接收端通过完成标记确定哪些连续块已经可用。若经主存中转，状态则依次占用源端、主存和接收端缓冲，主存到 GPU 的拷贝成为新的依赖边。 | …份双缓冲交接时，传输结束前源端保留完整状态，目的端分配完整接收缓冲。因此，源端和目的端各占用 1.125 GiB，共 2.25 GiB。采用分块传输时，目的端确认收到某一块后，源端就可以提前释放这一块，缩短两端重复驻留的时间；目的端通过完成标记确定哪些连续的块已经可用。若经主存中转，状态则依次占用源端、主存和目的端的缓冲，主存到 GPU 的拷贝成为新的依赖边。 |
| …要约 9.7 GB/s 的传输带宽；每秒 16 次则需要约 19.3 GB/s。某条 11 GB/s 链路可能满足前者的平均需求，却仍让每个请求等待传输；对于后者，即使两池有空闲计算能力，网络队列也会持续增长。前一种情形下，每份状态仍需约 110 ms 才能传完，后一种情形还会每秒积累约 8.3 GB 待传数据。传输时间决定了单个请求至少要等待多久，而数据到达… | …要约 9.7 GB/s 的传输带宽；每秒 16 次则需要约 19.3 GB/s。一条 11 GB/s 的链路能满足前者的平均需求，但每个请求仍要等传输完成；对于后者，即使两池有空闲计算能力，网络队列也会持续增长。前一种情形下每份状态要约 110 ms 才能传完，后一种情形每秒还会积累约 8.3 GB 待传数据。传输时间决定了单个请求至少要等待多久，而数据到达… |
| …阶段的实际处理速度。以一个假设的计时实验为例：若 A 连续处理 20 份互不复用的 8192-token 输入，总共占用一个 worker 10 秒，则 $r_P=20\times8192/10=16384$ 新 token/s，每请求消耗 $10/20=0.5$ 资源秒。若同一批请求的 2560 次 decode 调用共占用 40 秒，则 $r_D=64$ 调用/s，每请求消耗 2 资源秒。批内请求共享一次调用时，应累计所有请求完成的解码步，但计算资源占用时间时，同一次批执行只计一次。这样计算的资源占用时间可以相加，输入 token/s 与输出 token/s 则先要换算成同一请求的需求。 | …阶段的实际处理速度。以一个假设的计时实验为例：若 A 连续处理 20 份互不复用、各 8192 个 token 的输入，总共占用一个 worker 10 秒，则 $r_P=20\times8192/10=16384$ 新 token/s，每请求消耗 $10/20=0.5$ 资源秒。若同一批请求的 2560 次 decode 调用共占用 40 秒，则 $r_D=64$ 调用/s，每请求消耗 2 资源秒。批内多个请求共享一次调用时，完成的解码步要把所有请求都算上，但资源占用时间只按这一次批执行计一次。这样得到的资源占用时间可以直接相加；输入 token/s 与输出 token/s 则要先换算成同一个请求的需求。 |
| … P 的数量，其余分给 D。每格取两池能力与 25 GB/s 链路容量的最小值，单位为请求/s，不含排队。颜色表示可持续请求率，单位为请求/s；A、B 是例 9.2 中分别更擅长预填充和解码的两类 worker。* | … P 的数量，其余分给 D。每格取两池能力与 25 GB/s 链路容量的最小值，不含排队；颜色深浅表示可持续的请求率，单位为请求/s。A、B 是例 9.2 中分别更擅长预填充和解码的两类 worker。* |
| 现在只改变前缀命中。P 已经拥有 6144 个 token，只需处理剩余 2048 个 token，即原来的四分之一。一个 A 的 P 能力于是从 2 增至 8 请求/s；若仍给… | 现在只改变一个条件：前缀命中。P 已经缓存了前 6144 个 token，只需处理剩下的 2048 个，即原来的四分之一。一个 A 的 P 能力于是从 2 增至 8 请求/s；若仍给… |
| 再恢复原始输入，把输出从 129 增加到 1025 token。D 调用数从 128 增至 1024，增至原来的八倍；A、B 的 … | 再恢复原始输入，把输出从 129 个增加到 1025 个 token。D 调用数从 128 增至 1024，增至原来的八倍；A、B 的 … |
| 图 9-11 将三次调整画在同一组计算资源上。每个方块都是原来的一台 worker，变化的只是它承担的阶段。前缀命中后，P 的工作减少，可以让出两台 A；输出变长后，D 的工作增加，则需要再给 D 一台 A。 | 图 9-11 将三次调整画在同一组计算资源上。每个方块都是原来的一个 worker，变化的只是它承担的阶段。前缀命中后 P 的工作减少，可以让出两个 A；输出变长后 D 的工作增加，则要再给 D 一个 A。 |
| …两个和一个。前缀命中减少 P 的新工作，输出增长增加 D 的重复工作，两者都要求增加 D 的资源比例，却只有前者保持原来的输出长度。由此可以把调度分成两个时间尺度：请求到达时选择队列，负载分布持续变化时调整池规模。队列分配决定眼前等待，池规模决定积压能否长期消退。 | …两个和一个。前缀命中减少 P 的新工作，输出增长增加 D 的重复工作，两者都要求提高 D 的资源比例，只是前者的输出长度没有变。由此可以把调度分成两个时间尺度：请求到达时选择进哪个队列，负载分布持续变化时调整池的规模。队列分配决定眼前等待，池规模决定积压能否长期消退。 |
| AF 指 attention 与 FFN 的分工。与 PD 一次划分两个生成阶段不同，AF 在模型层内划分计算：attention 产生隐状态，FFN 或被选中的专家处理它，再将结果返回，供后续算子使用。下一层通常要等这一结果，因此小消息的启动与同步可能重复很多次。 | AF 指注意力与前馈网络的分工。与 PD 一次划分两个生成阶段不同，AF 在模型层内划分计算：注意力产生隐状态，前馈网络或被选中的专家处理它，再把结果送回，供后续算子使用。下一层通常要等这个结果，因此小数据量传输的启动与同步会重复很多次。 |
| …型（MoE）由路由器为每个 token 选择部分专家网络，为这种分工提供了一个特殊机会。总专家权重很大，但一个 token 只选择其中一部分。若大多数专家保存在主存，可以将被选中的权重搬给 GPU，也可以让 CPU 使用本地权重完成计算，再传递较小的激活。前一条路径用链路带宽换取 GPU 算力，后一条路径用 CPU 算力换取较少的链路流量。选择取决于本次分派给同一个专家的 token 数；每个 token 的特征向量占该专家输入矩阵的一行，这些向量复用同一份专家权重。 | …型（MoE）由路由器为每个 token 选择部分专家网络，为这种分工提供了一个特别的机会。总专家权重很大，但一个 token 只选择其中一部分。若大多数专家保存在主存，可以将被选中的权重搬给 GPU，也可以让 CPU 使用本地权重完成计算，再传递较小的激活。前一条路径花链路带宽换 GPU 算力，后一条路径花 CPU 算力换较少的链路流量。怎么选取决于本次分派给同一个专家的 token 数：每个 token 的特征向量占该专家输入矩阵的一行，这些行复用同一份专家权重。 |
| …采用第一条路径，将权重暂存主存，使用时搬到 GPU。KTransformers 展示了第二类 CPU／GPU 协作的实际路径：GPU 可以处理 attention 和部分常驻专家，CPU 处理分配给自己的专家；提交 CPU 工作后，GPU 执行可并行的分支，最后同步并合并结果。热点专家、共享专家以及 prefill 阶段可以… | …采用第一条路径，将权重暂存主存，使用时搬到 GPU。KTransformers 是采用第二条路径的实际系统：GPU 处理注意力和部分常驻专家，CPU 处理分给自己的专家；提交 CPU 的工作后，GPU 执行可以并行的分支，最后同步并合并结果。热点专家、共享专家以及 prefill 阶段可以… |
| …时之和，权重每批读取一次。每个专家只收到一个 token 的特征向量时，CPU 可以直接读取本地主存中的权重，避免了较慢链路上的 36 MiB 搬运；随着每个专家的输入行数增加，一次权重传输的开销由更多行计算分摊，而 CPU 更早受到算力限制。 | …时之和，权重每批读取一次。每个专家只收到一个 token 的特征向量时，CPU 直接读本地主存里的权重，省去了在较慢链路上搬 36 MiB；随着每个专家的输入行数增加，搬一次权重的开销由更多行的计算分摊，CPU 却更早碰到算力上限。 |
| …ms，GPU 路径约 12.5 ms，此时在 GPU 上计算更快。图 9-14 展示了中间过程：按题设参数计算，当每个专家的输入从 78 个 token 增至 79 个时，GPU 开始比 CPU 更快；交点附近两条路径均约 12.4 ms。每个专家多处理一个 token 的特征向量时，CPU 完成新增矩阵计算所需的时间远长于 GPU；权重搬运保持不变，因而 GPU 从交点之后取得优势。[^locality] | …ms，GPU 路径约 12.5 ms，此时在 GPU 上计算更快。图 9-14 画出了中间过程：按题设参数，每个专家的输入从 78 个 token 增到 79 个时，GPU 开始比 CPU 快；交点附近两条路径都约 12.4 ms。每个专家每多处理一个 token，CPU 多花的计算时间远多于 GPU，而权重搬运的开销不变，所以过了交点 GPU 就占优。[^locality] |
| 硬件变化可以用同一组数估计。若有效主存带宽从 200 降到 100 GB/s，八份权重的读取时间从约 1.5 增至 3.0 ms；单 token 情形明显变慢，而每专家 128 个 token 时，CPU 仍主要受约 19.3 ms 的矩阵计算时间限制。就地计算是否有利，由当前的主要瓶颈决定：读取受限时提高主存带宽有效，计算受限时提高矩阵吞吐有效。非统一内存访问（NUMA）把内存分到不同 CPU 附近，跨节点读取要经过处理器… | 硬件变化可以用同一组公式估计。若有效主存带宽从 200 降到 100 GB/s，八份权重的读取时间从约 1.5 增至 3.0 ms；单 token 情形明显变慢，而每专家 128 个 token 时，CPU 仍主要受约 19.3 ms 的矩阵计算时间限制。就地计算是否划算，由当前的主要瓶颈决定：读取受限时提高主存带宽有用，计算受限时提高矩阵吞吐有用。非统一内存访问（NUMA）把内存分到不同 CPU 附近，跨节点读取要经过处理器… |
| …不下这组常驻专家；还需在多卡间切分，或减少每层驻留数量。把每层驻留数减到 16，可将这部分权重减到约 53 GiB，同时也把更多专家执行交给主存一侧。容量选择由此直接改变请求的执行路径。 | …不下这组常驻专家；还需在多卡间切分，或减少每层驻留数量。把每层驻留数减到 16，这部分权重降到约 53 GiB，但也把更多专家的执行交给了主存一侧。容量选择由此直接改变请求的执行路径。 |
| …约 4.43 GiB，接近上述均匀覆盖情形。随着批量继续增长，被访问的不同专家数至多达到 128，而分派次数仍线性增加，新增 token 才越来越多地复用已经读入的权重。 | …约 4.43 GiB，接近上述均匀覆盖情形。随着批量继续增长，被访问的不同专家数最多到 128 就不再增加，分派次数却继续线性增加，新增的 token 于是越来越多地复用已经读入的权重。 |
| …的服务器，更适合每个专家输入行数较少的负载。DeepSeek V4-Flash 的公开单 RTX 5090 配置配合至少 200 GB 主存，Kimi K2 采用 Q4_K_M 分组量化的配置（以… | …的服务器，更适合每个专家输入行数较少的负载。DeepSeek V4-Flash 公开的单张 RTX 5090 配置要配至少 200 GB 主存，Kimi K2 采用 Q4_K_M 分组量化的配置（以… |
| 在 MoE 中，可以把路由专家放到独立的专家资源池，由保留 attention 与 KV 的 worker 发送激活，并接收专家结果。这称为**专家分离（Expert Disaggregation）**，也常被称为 EP 分离；本书用“专家分离”指部署边界，用 EP 指专家并行度。它是本节 attention 与专家计算分工的一种形式，具体系统还需说明共享专家、路由器、前后投影分别放在哪一侧。 | 在 MoE 中，可以把路由专家放到独立的专家资源池，由保留注意力与 KV 的 worker 发送激活并接收专家结果。这称为**专家分离**（Expert Disaggregation），也常叫 EP 分离；本书用“专家分离”指部署边界，用 EP 指专家并行度。它是本节注意力与专家计算分工的一种形式，具体系统还要说明共享专家、路由器和前后投影分别放在哪一侧。 |
| **大 EP** 则指一个专家并行组跨越较多加速器。它可以让每卡只保存少量专家，并汇聚多个 attention worker 的输入，使专家获得足够大的矩阵批次。大 EP 不必采用独立专家池：同一张卡也可以同时执行 attention 和自己持有的专家；独立专家池也不必只有一个很大的 EP 组。DeepSeek-V3/R1 的公开推理报告分别给出 prefill EP32 与 decode EP144，并采用冗余专家及多类负载均衡；这说明大 EP 的实际用途，但这组规模本身不能证明 attention 与专家已分离到两套设备。[^ep-system] | **大 EP** 则指一个专家并行组跨越较多的卡。它让每张卡只保存少量专家，并汇聚多个 attention worker 的输入，使专家拿到足够大的矩阵批次。大 EP 不必采用独立专家池：同一张卡也可以同时执行注意力和自己持有的专家；独立专家池也不必只有一个很大的 EP 组。DeepSeek-V3/R1 的公开推理报告分别给出 prefill EP32 与 decode EP144，并采用冗余专家和多类负载均衡；这说明大 EP 的实际用途，但这组规模本身不能证明注意力与专家已经分到两套设备上。[^ep-system] |
| …24,k=8,E=256$ 时为 32 行；只把同样的 256 个专家从 32 卡移到 128 卡，这个平均值仍是 32，减少的是每卡专家数。汇聚更多输入才能增大每专家批次，却也会扩大通信范围、同步范围和排队时间。固定批次下，参与组越大，每卡平均任务越少，少量集中分派或晚到就越容易影响尾部；偏斜是否加剧仍要由实际路由与放置验证。 | …24,k=8,E=256$ 时为 32 行；只把同样的 256 个专家从 32 张卡摊到 128 张卡，这个平均值仍是 32，减少的只是每卡的专家数。汇聚更多输入才能增大每个专家的批次，却也会扩大通信范围、同步范围和排队时间。批次固定时，参与的卡越多，每卡平均任务越少，少量集中分派或晚到就越容易拖长尾部；偏斜是否加剧仍要用实际路由与放置验证。 |
| …力／内存配比，并从多个 attention worker 汇聚任务。代价是原本可能本地执行的专家输入也要跨池，网络成为两池共享资源。若多个独立 EP 组各保存完整专家集合，它们才是可分别接收任务的服务副本；只添加保存部分专家的卡，并不能让每条请求任意绕开繁忙专家。共享专家池的队列、显存、模型版本和接纳策略需要作为整体管理。 | …力／内存配比，并从多个 attention worker 汇聚任务。代价是原本可以在本地执行的专家输入也要跨池传送，网络成了两池共享的资源。只有多个独立 EP 组各保存一套完整的专家集合，它们才是可以分别接收任务的服务副本；只添加保存部分专家的卡，并不能让每条请求随意绕开繁忙的专家。共享专家池的队列、显存、模型版本和接纳策略需要作为整体管理。 |
| 将 AF 从同机扩展到跨机后，消息启动时间、链路带宽以及各节点开始执行的时间差都会产生更大影响。MegaScale-Infer 是采用注意力与专家计算分离的分布式推理系统。它用微批次在 attention 与专家节点之间交错执行，形成交替流水：一个微批次执行注意力时，另一个微批次执行专家计算。理想情况下，若两个阶段每… | 把 AF 从同一台服务器扩展到跨服务器后，每次传输的启动时间、链路带宽以及各节点开始执行的时间差都会有更大影响。MegaScale-Infer 是采用注意力与专家计算分离的分布式推理系统。它让微批次在注意力节点与专家节点之间交错执行，形成交替的流水：一个微批次执行注意力时，另一个微批次执行专家计算。理想情况下，若两个阶段每… |
| …均为 BF16，生产请求的输入／输出长度中位数为 571／159 token。在 TPOT 不超过 150 ms 的评估约束下，在八个节点、每节点八张 80 GB Ampere GPU 的同构集群中，Scaled-MoE 的每 GPU decode 吞吐达到 TensorRT-LLM 的 1.90 倍。这个结果包含分离放置、并行选择、流水和通信库的共同效果，不能当作“只开启 EP 分离”的通用加速比。论文也明确按专家热度配置冗余并最小化最忙节点成本，说明独立专家池仍需处理负载偏斜。[^ep-papers] | …均为 BF16，生产请求的输入／输出长度中位数为 571／159 token。在每个输出 token 的平均耗时（TPOT）不超过 150 ms 的评估约束下，在八个节点、每节点八张 80 GB Ampere GPU 的同构集群上，Scaled-MoE 的每 GPU decode 吞吐达到 TensorRT-LLM 的 1.90 倍。这个结果包含分离放置、并行选择、流水和通信库的共同效果，不能当作“只开启 EP 分离”的通用加速比。论文也明确按专家热度配置冗余副本并最小化最忙节点的成本，说明独立专家池仍要处理负载偏斜。[^ep-papers] |
| 例如，attention 与专家阶段每微批次分别需要 2 ms、3 ms。四个微批次串行执行共需 20 ms；理想双阶段流水只需 $2+3+3\times3=14$ ms，省下 6 ms。若拆小微批次后，每阶段的实际服务时间变长，或无法与计算重叠的往返通信超过 6 ms，这项收益就会消失。因此，通信和计算效率下降引入的额外耗时合计必须小于 6 ms，流水执行才会更快。[^af] | 例如，注意力与专家阶段每个微批次分别需要 2 ms、3 ms。四个微批次串行执行共需 20 ms；理想双阶段流水只需 $2+3+3\times3=14$ ms，省下 6 ms。若拆小微批次后每阶段的实际服务时间变长，或者无法与计算重叠的往返通信超过 6 ms，这项收益就没有了。因此，通信和计算效率下降带来的额外耗时合计必须小于 6 ms，流水执行才更快。[^af] |
| 这段时间短于例 9.1 中的一次 PD 交接，但两者对应不同工作范围。若生成 129 个输出，后续 128 次 decode 的 AF 串行交接累计约 49.1 ms；8192 个输入 token 的 prefill 若也按整批 dense AF 交接，激活载荷为 4.5 GiB，约需 193 ms 纯传输，再加启动。一次 PD 搬运与逐层 AF 往返的区别在此显现：前者随上下文长度增长，后者还随生成步数反复发生。 | 这段时间短于例 9.1 中的一次 PD 交接，但两者对应的工作范围不同。若生成 129 个输出，后续 128 次 decode 的 AF 串行交接累计约 49.1 ms；8192 个输入 token 的 prefill 若也按整批稠密 AF 交接，激活载荷为 4.5 GiB，约需 193 ms 纯传输，再加启动。一次 PD 搬运与逐层 AF 往返的区别在这里显现出来：前者随上下文长度增长，后者还随生成步数反复发生。 |
| ![消息数量与启动开销](ch09/figure-9-8-handoff.svg) | ![传输次数与启动开销](ch09/figure-9-8-handoff.svg) |
| *图 9-17：相同总载荷下，消息数放大启动开销。两条曲线总载荷均为 1.125 GiB、有效带宽均为 25 GB/s，分别串行发送 1 次和 72 次；差值为 $71\alpha$，纵轴从 48.2 ms 起以显示差异。这样保持总字节数不变，只比较消息数量的影响，实际一步 AF 仅传 576 KiB。* | *图 9-17：相同总载荷下，传输次数放大启动开销。两条曲线总载荷均为 1.125 GiB、有效带宽均为 25 GB/s，分别串行发送 1 次和 72 次；差值为 $71\alpha$，纵轴从 48.2 ms 起以显示差异。这样保持总字节数不变，只比较传输次数的影响；实际一步 AF 只传 576 KiB。* |
| …71\alpha$，在本例中约为 0.36 ms。PD 往往先受大块 KV 搬运影响，AF 则更容易受到小消息延迟和操作间依赖的影响。下一节考察 MoE 中的激活传输：一个输入可能发往多个执行位置，完成时还要等待它所选择的各个专家返回结果并汇合。 | …71\alpha$，在本例中约为 0.36 ms。PD 往往先受大块 KV 搬运的影响，AF 则更容易受小数据量传输的延迟和操作间依赖的影响。下一节考察 MoE 中的激活传输：一个输入可能发往多个执行位置，完成时还要等它选中的各个专家都返回结果再汇合。 |
| …同样的 512 次分派，考察各张卡何时完成计算。即使全系统的总计算量不变，最忙的加速器也可能使整层推迟完成。对加速器 $r$，记矩阵计算量为 $F_r$、需要读取的权重字节数为 $W_r$，有效算力和带宽分别为 $C_r$、$B_r$。该加速器计算与权重读取所需时间至少为 $\max(F_r/C_r,W_r/B_r)$；合并各加速器的结果时，必须等耗时最长的加速器完成。 | …同样的 512 次分派，考察各张卡何时完成计算。即使全系统的总计算量不变，最忙的那张卡也可能让整层推迟完成。对卡 $r$，记矩阵计算量为 $F_r$、需要读取的权重字节数为 $W_r$，有效算力和带宽分别为 $C_r$、$B_r$。这张卡完成计算与权重读取的时间至少为 $\max(F_r/C_r,W_r/B_r)$；合并各卡的结果时，必须等耗时最长的卡完成。 |
| 通信也依赖 token 的初始归属。在第 9.1 节的 TP2×EP4 布局中，各 EP 组处理同一批请求，每个 EP 成员已拥有相同的隐状态输入，跨 EP 的输入分发（dispatch）可以为零，结果通过部分和归约合并。另一种布局让各加速器持有各自不同的输入 token，则可能需要将激活分派给远端专家。输入已复制时，通信集中在结果合并；输入分属不同加速器时，执行前还多出远端分派。通信量由 token 的起点、专家的位置与结果的归属共同确定。 | 通信量也取决于 token 最初在哪张卡上。在第 9.1 节的 TP2×EP4 布局中，各 EP 组处理同一批请求，每个 EP 组的成员已经持有相同的隐状态输入，跨 EP 组的派发（dispatch）可以为零，结果通过部分和归约合并。另一种布局让各张卡持有各自不同的输入 token，就可能要把激活派发给远端专家。输入已复制时，通信集中在结果合并；输入分属不同的卡时，执行前还多出一次远端派发。通信量由 token 的起点、专家的位置与结果的归属共同决定。 |
| …时，它才近似等于计算量偏斜。对通信阶段 $X\in\{D,C\}$，把矩阵按发送端、接收端和物理割集统计，有 | …时，它才近似等于计算量偏斜。对通信阶段 $X\in\{D,C\}$，把矩阵按发送方、接收方和物理割集统计，有 |
| …相加，不能把多条流各自除以一份完整带宽后假定同时完成。带宽 skew 在这里指各端点、链路的需求或占用不均，硬件额定带宽未必改变。 | …相加，不能把多条流各自除以一份完整带宽后假定同时完成。带宽 skew 在这里指各张卡、各条链路的需求或占用不均，硬件的额定带宽并没有变。 |
| …信量相同，为什么热点让两次交换都变慢？** 沿用第 7.2.3 节的 1024 token、top-8 与 8 KiB 向量，现在全部分派跨入四个专家组，每个组由一张卡执行、拥有独立的 25 GB/s 双向接口。四个来源各发送 2048 份，dispatch 总计 64 MiB，combine 也为 64 MiB。两池之间每方向割集带宽为 100 GB/s。均衡时每组接收 20… | …信量相同，为什么热点让两次交换都变慢？** 沿用第 7.2.3 节的 1024 个 token、每 token 选 8 个专家与 8 KiB 向量，现在全部分派都跨入四个专家组，每个组由一张卡执行、拥有独立的 25 GB/s 双向接口。四个来源各发送 2048 份，dispatch 总计 64 MiB，combine 也是 64 MiB。两池之间每方向割集带宽为 100 GB/s。均衡时每组接收 20… |
| …也不会同比缩短 BF16 combine 或专家计算；表中假设量化不改变计算时间且忽略尺度元数据，用于分离这几项影响，不是量化性能预测。 | …也不会同比缩短 BF16 combine 或专家计算；表中假设量化不改变计算时间，并忽略尺度元数据，目的是分开看这几项影响，不是量化性能的预测。 |
| …专家复制让同一个逻辑专家拥有多个物理副本，调度器可以把任务分给这些副本。只要副本权重相同、路由权重使用正确且结果合并无误，复制不必改变每个 token 选择分数最高的 k 个专家这一规则（top-k）；修改专家选择规则则会改变模型行为，两者应分别评价。vLLM、SGLang 的专家负载均衡机制（EPLB）根据路由统计，调整各… | …专家复制让同一个逻辑专家拥有多个物理副本，调度器可以把任务分给这些副本。只要副本的权重相同、路由权重用得正确、结果合并无误，复制就不必改变每个 token 选分数最高的 k 个专家这条规则（top-k）；修改专家选择规则会改变模型行为，两者要分开评价。vLLM、SGLang 的专家负载均衡机制（EPLB）根据路由统计，调整各… |
| …8 MiB。两者相差近 12 倍。CRAFT 是研究专家副本与显存分配的服务系统。它要解决的各层显存分配问题由此产生：应把空间花在最能缩短等待的层，而不是机械地给每层同样多的副本。[^moe] | …8 MiB。两者相差近 12 倍。CRAFT 是研究专家副本与显存分配的服务系统，它要解决的正是这个各层显存怎样分配的问题：把空间花在最能缩短等待的层，而不是机械地给每层同样多的副本。[^moe] |
| **例 9.5　热点专家副本需要多少批次才能收回复制成本？** 在八个 rank 间增加七份专家副本，共 252 MiB；七张接收卡各增加 36 MiB，均放得进 64 MiB 的额外可用空间。假设这次复制准备需要约 10.6 ms，此… | **例 9.5　热点专家副本需要多少批次才能收回复制成本？** 在八张卡之间增加七份专家副本，共 252 MiB；七张接收卡各增加 36 MiB，都放得进 64 MiB 的额外可用空间。假设这次复制准备需要约 10.6 ms，此… |
| …这支持“把副本预算优先给收益大的层”，不表示任意热点复制都能得到这一增幅；其输入分为 4096-token 块、输出固定为 256 token 等实验条件和 goodput 的容量拐点定义也不能直接替代线上 P99 指标。[^ep-papers] | …这支持“把副本预算优先给收益大的层”，不表示任意热点复制都能得到这一增幅；其输入按 4096 个 token 分块、输出固定为 256 个 token 等实验条件，以及按有效吞吐（goodput）定义的容量拐点，也不能直接替代线上的 P99 指标。[^ep-papers] |
| 副本策略需要预测的是热点还会持续多久。若统计窗口结束时热点只剩 16 批，复制将多花约 4.2 ms；若还剩 64 批，则净省约 15.2 ms。每次重新复制都要再花费 10.6 ms，所以频繁跟随热点变化迁移副本，会延后收回复制成本的时间。将容量优先分给持续拥挤的层，才能让累计节省的等待时间超过准备… | 副本策略要预测的是热点还会持续多久。若统计窗口结束时热点只剩 16 批，复制将多花约 4.2 ms；若还剩 64 批，则净省约 15.2 ms。每次重新复制都要再花 10.6 ms，所以频繁跟着热点迁移副本，会推迟收回复制成本的时间。将容量优先分给持续拥挤的层，才能让累计节省的等待时间超过准备… |
| 均衡策略还需要沿完整路径选择控制对象。来源侧要平衡 token 数与 attention 就绪时间；专家侧要平衡有效行数、实际 GEMM 时间和接收量；网络侧要检查共享出口、跨机架流量与返回方向。DeepSeek 的公开报告把 prefill、decode 与 EP 负载均衡分别处理，原因就在这里。长上下文 decode 即使每卡请求数相同，attention 的 KV 读取量仍可能不同，最终形成不同的 dispatch 发起时刻。[^ep… | 均衡策略还要沿完整路径选择控制对象。来源侧要平衡 token 数与注意力的就绪时间；专家侧要平衡有效行数、实际 GEMM 时间和接收量；网络侧要检查共享出口、跨机架流量与返回方向。DeepSeek 的公开报告把 prefill、decode 与 EP 负载均衡分别处理，原因就在这里。长上下文 decode 即使每卡请求数相同，注意力的 KV 读取量仍可能不同，最终形成不同的 dispatch 发起时刻。[^ep… |
| 在独立专家池中，还要控制跨来源的排队。多个 attention worker 可以同时选择同一热点专家，即使它们各自的小批次看起来均衡，合并流也可能超过该专家的服务能力。若池每秒接纳 $\lambda$ 批，每批平均在专家卡 $j$ 上产生 $\mathbb E[F_j]$ FLOPs，则稳定运行至少需要 $\lambda\mathbb E[F_j]<C_j$；每条链路 $\ell$ 还需满足 $\lambda\mathbb E[V_\ell]<B_\ell$。平均负载满足这些条件只是必要条件，突发和重尾输入仍可造成很长的队列。共享池有时能汇聚批次、平滑独立来源的波动，有时会放大相关热点，不能直接假定分离会消除 skew。 | 在独立专家池中，还要控制跨来源的排队。多个 attention worker 可能同时选中同一个热点专家，即使它们各自的小批次看起来均衡，汇合后的流量也可能超过这个专家的服务能力。若池每秒接纳 $\lambda$ 批，每批平均在专家卡 $j$ 上产生 $\mathbb E[F_j]$ FLOPs，则稳定运行至少需要 $\lambda\mathbb E[F_j]<C_j$；每条链路 $\ell$ 还需满足 $\lambda\mathbb E[V_\ell]<B_\ell$。平均负载满足这些条件只是必要条件，突发和重尾输入仍会造成很长的队列。共享池有时能汇聚批次、平滑各来源的波动，有时会放大相关的热点，不能直接假定分离会消除 skew。 |
| …置接纳额度、限制单个来源的在途量，可以避免它耗尽整个池的缓冲；因此增加池容量时，需同时调整接纳与背压。丢弃溢出 token、改变 top-k 或替换逻辑专家会改变模型行为，不… | …置接纳额度、限制单个来源的在途量，可以避免它耗尽整个池的缓冲；因此增加池容量时，要同时调整接纳额度与背压。丢弃溢出 token、改变 top-k 或替换逻辑专家会改变模型行为，不… |
| …ing the Mixture of Experts Serving Tax》从阶段微基准解释另一种影响：prefill 的计算不均会让最慢卡拖住整批，decode 的专家集中却可能通过减少活跃权重和 padding 降低访存成本。这与第 6.3.2 节的复用分析一致。论文的 Mixtral／Qwen2 MoE 使用八张 A100，DeepSeek-V3 使用八张 B200，通信微基准另有 8／16 张 H200；这些结果不能合成一个跨集群端到端加速比。它提供的是需要同时测量路由、实际矩阵形状、通信和内存访问的证据。[^ep-papers] | …ing the Mixture of Experts Serving Tax》从各阶段的微基准解释另一种影响：prefill 的计算不均会让最慢的卡拖住整批，decode 的专家集中却可能通过减少活跃权重和 padding 降低访存成本。这与第 6.3.2 节的复用分析一致。论文的 Mixtral／Qwen2 MoE 使用八张 A100，DeepSeek-V3 使用八张 B200，通信微基准另有 8／16 张 H200；这些结果不能合成一个跨集群的端到端加速比。它提供的是证据，说明路由、实际矩阵形状、通信和内存访问需要同时测量。[^ep-papers] |
| …mbine 分别需要 0.2、0.6、0.2 ms，完全串行为 1.0 ms。若可均分成两个微批次，各阶段时间减半，理想三阶段流水为 $0.1+0.3+0.1+0… | …mbine 分别需要 0.2、0.6、0.2 ms，完全串行为 1.0 ms。若能均分成两个微批次，各阶段时间减半，理想三阶段流水为 $0.1+0.3+0.1+0… |
| 沿图 9-23 的时间线看，第一个微批次计算时，可以分派第二个微批次；第一个微批次合并时，第二个微批次已开始计算。首批分派负责填充流水，末批合并负责收尾，两段共同构成流水之外的串行时间。 | 沿图 9-23 的时间线看，第一个微批次计算时可以派发第二个微批次；第一个微批次合并时，第二个微批次已经开始计算。第一批的派发负责填充流水，最后一批的合并负责收尾，这两段是流水之外的串行时间。 |
| 上面的流水假设两个微批次大小均匀。真实专家分离中，一个 token 的路径仍然是“dispatch → 所选专家计算 → combine”，其中有**两次通信阶段**，夹着专家计算。dispatch 的数据就绪偏斜来自 attention 的完成时间和发送队列；专家计算的偏斜又成为 combine 的发送就绪偏斜。即使两次交换字节完全对称，返回阶段也可能因为数据晚到而拖长。 | 上面的流水假设两个微批次大小均匀。真实的专家分离中，一个 token 的路径仍然是“dispatch → 所选专家计算 → combine”，**两次通信阶段**夹着专家计算。dispatch 的数据就绪偏斜来自注意力的完成时间和发送队列；专家计算的偏斜又变成 combine 的发送就绪偏斜。即使两次交换的字节数完全对称，返回阶段也可能因为数据晚到而拖长。 |
| 这里每条路径内含共享资源排队，不能把独立运行的阶段时间直接代入并假定互不干扰。若某 token 的两条分支分别在 0.5 ms 和 1.4 ms 返回，它要等到 1.4 ms 后才能合并；只把快分支降到 0.3 ms 不改变它的完成时刻。逐 token 或逐块推进时，未选择慢专家的 token 可以更早继续；整批屏障会让它们一起等待。运行时是否真正支持细粒度推进、后续矩阵是否还要求凑批，决定等待会传播多远。 | 这里每条路径都包含共享资源上的排队，不能把各阶段单独运行的时间直接代入并假定互不干扰。若某 token 的两条分支分别在 0.5 ms 和 1.4 ms 返回，它要等到 1.4 ms 后才能合并；只把快的那条分支降到 0.3 ms 不会改变它的完成时刻。逐 token 或逐块推进时，没有选中慢专家的 token 可以更早继续；整批屏障则让它们一起等。运行时是否真正支持细粒度推进、后续矩阵是否还要求凑批，决定了等待会传播多远。 |
| …ken 选中的两条专家分支，教学时长分别为 0.5 ms 与 1.4 ms。每条均依次经过 dispatch、计算与 combine；同一 token 要等两份结果，图中忽略本地加权合并时间。灰色表示快分支结果已到达后的等待。* | …ken 选中的两条专家分支，教学时长分别为 0.5 ms 与 1.4 ms。每条都依次经过 dispatch、计算与 combine；同一个 token 要等两份结果，图中忽略本地加权合并的时间。灰色表示快分支的结果到达后的等待。* |
| 这也解释了为什么不能总用一个 $\max(t_A,t_F)$ 预测专家池流水周期。该式要求阶段耗时稳定、资源独立且工作能够持续供给；实际周期还受最忙专家、往返链路、返回缓冲和跨层依赖约束。同一专家池若为多个层或多个模型服务，必须合计它们占用的资源。首个微批次仍承受完整往返延迟，提高稳态吞吐并不等于缩短单个 token 的响应时间。 | 这也解释了为什么不能总用一个 $\max(t_A,t_F)$ 来预测专家池的流水周期。这个式子要求各阶段耗时稳定、资源独立、工作能持续供给；实际周期还受最忙专家、往返链路、返回缓冲和跨层依赖的约束。同一个专家池若为多个层或多个模型服务，必须合计它们占用的资源。第一个微批次仍要承受完整的往返延迟，提高稳态吞吐并不等于缩短单个 token 的响应时间。 |
| NCCL 提供通用集合通信与点对点原语，专用 EP 通信库则把路由、打包、传输和合并协同实现。DeepEP 提供 MoE dispatch／combine 和低精度传输支持；V2 的公开接口使用 ElasticBuffer，底层采用 NCCL Gin，且移除了 V1 的零 SM RDMA 低延迟 EP 路径。这说明库名相同也不能省略版本条件。比较实现时，应固定版本、消息形状、精度和拓扑，分别记录发送／接收字节、expert GEMM 行数、padding、每阶段就绪及完成事件，再看整个层和请求的 P50／P99。独立通信带宽只能解释其中一部分。[^ep-system] | NCCL 提供通用的集合通信与点对点原语，专用的 EP 通信库则把路由、打包、传输和合并放在一起实现。DeepEP 提供 MoE dispatch／combine 和低精度传输支持；V2 的公开接口使用 ElasticBuffer，底层采用 NCCL Gin，并移除了 V1 的零 SM RDMA 低延迟 EP 路径。这说明库名相同也不能省略版本条件。比较实现时，应固定版本、数据形状、精度和拓扑，分别记录发送／接收字节数、expert GEMM 行数、padding、每阶段的就绪与完成事件，再看整个层和整条请求耗时的 P50／P99。单独测得的通信带宽只能解释其中一部分。[^ep-system] |
| …接重现，最后一项要在执行时间线中观察等待如何传播。这样才能区分流量偏斜、计算偏斜与仅仅晚到造成的等待，而不会把所有慢通信都归因于网络带宽不足。[^ep-calc] | …接重现，最后一项要在执行时间线中观察等待如何传播。这样才能区分流量偏斜、计算偏斜和单纯晚到造成的等待，不会把所有慢通信都归咎于网络带宽不足。[^ep-calc] |
| 专家执行存在两项相互作用的成本：补齐扩大矩阵计算量，并发传输又可能减慢这些工作的执行。上述流水通过重叠节省了 0.2 ms，一旦计算累计多花同样的时间，重叠收益便被完全耗尽。[^expert-exp] | 专家执行有两项相互作用的成本：补齐扩大了矩阵计算量，并发的传输又可能拖慢这些计算。上面的流水靠重叠省下 0.2 ms，一旦计算累计多花同样的时间，重叠的收益就全部抵消了。[^expert-exp] |
| …、状态格式以及必要的编码配置都可能参与缓存标识的计算。例如，同样的 token 使用不同 adapter，会在投影后得到不同的 K、V；同样的后缀接在不同前文后，attention 的上下文也发生变化。缓存键需要沿前缀链标识生成这份状态的计算。滑动窗口状态或递推状态则还要携带对应的 token 位置索引与更新时刻，才… | …、状态格式以及必要的编码配置都可能参与缓存标识的计算。例如，同样的 token 配上不同的 adapter，投影后会得到不同的 K、V；同样的后缀接在不同的前文后面，注意力看到的上下文也不同。缓存键需要沿前缀链标识出生成这份状态的整个计算。滑动窗口状态或递推状态则还要携带对应的 token 位置索引与更新时刻，才… |
| …其他实例怎样读取。容量相加并不会自动形成共享缓存。两个实例各有 4 GiB 私有主机内存（host memory）缓存时，一份 1.125 GiB 前缀可能在两端各占一份，共占 2.25 GiB；另一个实例却仍无法直接复用它。若二者连接到同一共享存储后端（接收和提供缓存对象的存储服务），一份对象可以服务两端，但每端在生成前可能还要取回本地。SGLang 的分层缓存 HiCache、vLLM 的多级缓存路径和 KV 缓存管理系统 LMCache 提供了不同的组织方式，取回是否经 CPU 决定下一节的路径时间。[^cache] | …其他实例怎样读取。容量相加并不会自动形成共享缓存。两个实例各有 4 GiB 私有的主机内存（host memory）缓存时，同一份 1.125 GiB 前缀会在两边各存一份，共占 2.25 GiB，而任何一个实例都用不上对方那一份。若两者连接到同一个共享存储后端（接收和提供缓存对象的存储服务），一份对象就能服务两边，但每一边在生成前可能还要把它取回本地。SGLang 的分层缓存 HiCache、vLLM 的多级缓存路径和 KV 缓存管理系统 LMCache 提供了不同的组织方式；取回是否经过 CPU，决定下一节的路径时间。[^cache] |
| 状态产生后，需要先保存并发布，其他实例才能查询、取回并使用它。路由器使用的缓存事件通常是存储位置和缓存标识等元数据，不携带完整 KV。GPU 副本被淘汰时 CPU 副本可能仍在。**目录**保存缓存标识与位置的映射，**对象**保存实际 KV 字节；恢复… | 状态产生后，需要先保存并发布，其他实例才能查询、取回并使用它。路由器用到的缓存事件通常只有存储位置和缓存标识这类元数据，不带完整的 KV。GPU 上的副本被淘汰时，CPU 上的副本可能还在。**目录**保存缓存标识与位置的映射，**对象**保存实际 KV 字节；恢复… |
| … 链路，一次载荷取回约 48.3 ms。若 100 次 decode 调用都重新远程读取这份不变前缀，累计占用链路约 4.83 s。若这 100 次读取分布在一秒内，仅这份上下文就要求约 121 GB/s，是给定链路能力的 4.8 倍。将读取与计算并行安排，也无法使 25 GB/s 链路在一秒内送完这些字节。 | … 链路，一次载荷取回约 48.3 ms。若 100 次 decode 调用都重新从远端读取这份不变的前缀，累计占用链路约 4.83 s。若这 100 次读取要在一秒内完成，仅这份上下文就要求约 121 GB/s，是给定链路能力的 4.8 倍。把读取与计算并行安排，也不能让 25 GB/s 的链路在一秒内送完这些字节。 |
| 一次取回并驻留将频次从“每步”降到“每次复用”，代价是占用本地 HBM。设一份状态占 $V$ bytes，保留 $\tau$ 秒，其内存占用与保留时长的乘积可用 $V\tau$ 个 byte-seconds 表示。保留时间相同时，大对象占用更多容量；大小相同时，保留越久，这一乘积越大。因此可把不活跃会话放到远端，在会话恢复时取回，并在连续 decode 期间保留在本地。这样，数据传输主要发生在会话恢复和暂停时。 | 取回一次并驻留把传输频次从“每步一次”降到“每次复用一次”，代价是占用本地 HBM。设一份状态占 $V$ bytes、保留 $\tau$ 秒，内存占用与保留时长的乘积为 $V\tau$，单位为字节·秒。保留时间相同时，大对象占用更多容量；大小相同时，保留越久，这一乘积越大。因此可以把不活跃的会话放到远端，会话恢复时取回，在连续 decode 期间留在本地。这样数据传输主要发生在会话恢复和暂停的时候。 |
| …慢；复用两次时为 170 ms，比两次重算的 200 ms 少 30 ms。保存是否值得取决于未来复用次数。若链路变慢使每次取回需要 120 ms，复用越多反而损失越大；此时应重算，或设法把取回提前到关键路径之外。 | …慢；复用两次时为 170 ms，比两次重算的 200 ms 少 30 ms。保存值不值得，取决于未来的复用次数。若链路变慢，每次取回要 120 ms，复用越多反而亏得越多；这时应该重算，或者设法把取回挪到关键路径之外提前做。 |
| …5 次，8 GiB 池完整命中 12 次。多出的 4 GiB 保住了七次本来会丢失的完整前缀；而再次请求所需的状态若已在本地驻留，就无须重复取回。于是，共享容量的收益要沿“保存多久—避免几次重算—增加几次搬运”来比较。[^shared] | …5 次，8 GiB 池完整命中 12 次。多出的 4 GiB 保住了七次本来会丢掉的完整前缀；再次请求所需的状态若已经在本地，就不必重复取回。所以共享容量的收益要按“保存多久、避免几次重算、增加几次搬运”来比较。[^shared] |
| …，恢复过程需要重新确认哪些计算结果已经保存。持久化让状态在实例重启或任务暂停后仍可使用。完整保存减少恢复时的重算，周期检查点减少写入但增加恢复工作，部分重算则利用已保留的前缀补齐缺失部分。检查点要保存继续计算所需的全部状态：完整 GQA 保存上下文 K、V；De… | …，恢复过程需要重新确认哪些计算结果已经保存。持久化让状态在实例重启或任务暂停后仍然可用。完整保存减少恢复时的重算，周期性检查点减少写入但增加恢复工作，部分重算则用已保留的前缀补齐缺的部分。检查点要保存继续计算所需的全部状态：完整 GQA 保存上下文 K、V；De… |
| 先考虑完整 GQA 页。Qwen3-8B 每个 16-token 逻辑页为 2.25 MiB。若各层 K、V 分片分别搬运，可能形成许多小连续段；若按页汇集，消息数量和所需的布局转换也会不同。一个逻辑页分成 36 层的 K、V 后有 72 个 32 KiB 连续段；按页汇集后则成为一个 2.25 MiB 对象。前者更容易受每秒操作次数限制，后者把更多时间用于连续载荷传输。 | 先考虑完整的 GQA 页。Qwen3-8B 每个含 16 个 token 的逻辑页为 2.25 MiB。若各层的 K、V 分片分别搬运，会形成许多小的连续段；若按页汇集，传输次数和所需的布局转换都不同。一个逻辑页按 36 层的 K、V 分开后有 72 个 32 KiB 的连续段；按页汇集后则是一个 2.25 MiB 的对象。前者更容易受每秒操作次数的限制，后者把更多时间花在连续载荷的传输上。 |
| 一组重启后的请求读取了 64 页，覆盖 1024 个 token，但可复用的连续前缀只有 1008 个 token，即 63 页。多读的一页占 2.25 MiB，却没有消除相应的重算。对于这个请求，读取量为 144 MiB，有效复用量约为 142 MiB；更有解释力的差别是“读入 64 页，使用 63 页”。这里的限制发生在读取之后：只有通过匹配、能够连续接到已有上下文上的页，才能替代计算。把磁盘读得更快会缩短读取时间，却不会改变这一请求需要处理的末页。[^restart] | 重启后的一个请求读取了 64 页，覆盖 1024 个 token，但可复用的连续前缀只有 1008 个 token，即 63 页。多读的那一页占 2.25 MiB，却没有省掉相应的重算。对这个请求，读取量为 144 MiB，有效复用量约为 142 MiB；更能说明问题的说法是“读入 64 页，用上 63 页”。这里的限制发生在读取之后：只有匹配成功、能连续接到已有上下文上的页，才能替代计算。把磁盘读得更快能缩短读取时间，却改变不了这个请求要处理的末页。[^restart] |
| … 9-26：读入的页不一定全部成为可复用前缀。正常重启后的这一请求读入 64 个 16-token 页，只复用前 63 页；末页仍需处理。每页 2.25 MiB，该请求的匹配边界为… | … 9-26：读入的页不一定全部成为可复用前缀。正常重启后的这一请求读入 64 个各含 16 个 token 的页，只复用前 63 页；末页仍需处理。每页 2.25 MiB，该请求的匹配边界为… |
| 页面缺失时，需要比较继续等待与重新计算所需的时间。例 9.6 中本地重算只需 180 ms；若取回一个不可用对象已等待 1 秒，仅等待这一对象所花的时间，就已超过五次重算所需的时间。实际截断页实验里，无条件等待的请求在 60 秒观察窗口内仍未完成，采用允许放弃等待的策略时，请求则通过重算得到结果。完成当前请求之后，还需要隔离或修复坏页，否则下一次请求还会遇到同样的等待。[^fault] | 页面缺失时，要比较继续等待和重新计算各需要多久。例 9.6 中本地重算只要 180 ms；若为取回一个不可用的对象已经等了 1 秒，光这一次等待就超过了五次重算的时间。实际的截断页实验里，无条件等待的请求在 60 秒观察窗口内一直没有完成，采用允许放弃等待的策略后，请求靠重算得到了结果。完成当前请求之后，还要隔离或修复坏页，否则下一个请求还会遇到同样的等待。[^fault] |
| …哪台机器。缓存所在的机器可能很忙，另一台机器虽然需要取回或重算，却可能先完成。因而路由时要比较的是请求完成时间。设 GPU 最早可执行时刻为 $Q$，状态从请求到达起的就绪时刻为 $… | …哪台机器。缓存所在的机器可能很忙，另一台机器虽然需要取回或重算，却可能先完成。因此路由时要比较的是请求的完成时间。设 GPU 最早可执行时刻为 $Q$，状态从请求到达起的就绪时刻为 $… |
| 如果取回必须等到 GPU 排队结束后才开始，排队时间与取回时间就需要相加；如果逐层流水，则需要更细的执行图。例如 $Q=80$ ms、取回耗时 60 ms、剩余计算 10 ms，并行就绪时共需 90 ms；等 GPU 空闲后再启动取回则需 150 ms。计算量和传输字节数相同，仅改变依赖关系，耗时就相差 60 ms。 | 如果取回要等 GPU 排完队才能开始，排队时间与取回时间就要相加；如果逐层流水，就需要更细的执行图。例如 $Q=80$ ms、取回耗时 60 ms、剩余计算 10 ms，并行就绪时共需 90 ms；等 GPU 空闲后再启动取回则需 150 ms。计算量和传输字节数都相同，只是依赖关系不同，耗时就差了 60 ms。 |
| **例 9.6　缓存命中与排队等待如何共同决定请求路由？** 对同一 8K 前缀，A 有 HBM 命中，但需等 250 ms；B 等 20 ms，完整重算需 180 ms；命中后的剩余工作为 10 ms。远端读取固定开销 10 ms，整份先到 host，再通过 25 GB/s 搬到 GPU。 | **例 9.6　缓存命中与排队等待如何共同决定请求路由？** 对同一份 8192 个 token 的前缀，A 在 HBM 里有命中，但要等 250 ms；B 只等 20 ms，但完整重算要 180 ms；命中后的剩余工作为 10 ms。远端读取的固定开销为 10 ms，整份数据先到主机内存，再经 25 GB/s 搬到 GPU。 |
| \| B：远端 5 GB/s，经 host 取回 \| 约 310 ms \| | \| B：远端 5 GB/s，经主机内存取回 \| 约 310 ms \| |
| \| B：远端 20 GB/s，经 host 取回 \| 约 129 ms \| | \| B：远端 20 GB/s，经主机内存取回 \| 约 129 ms \| |
| …10 ms 计算，状态必须在 190 ms 内就绪。再扣除查找的 10 ms 与 host→GPU 的约 48.3 ms，远端读取只剩约 131.7 ms。用 1.125 … | …10 ms 计算，状态必须在 190 ms 内就绪。再扣除查找的 10 ms 与主机内存到 GPU 的约 48.3 ms，远端读取只剩约 131.7 ms。用 1.125 … |
| 同一会话由此可以根据状态与资源的位置重新安排。应用保留连续的上下文，服务选择继续使用本地状态、迁移状态或重新计算；选择依据是这三条路径何时能让后续模型执行开始。第八章根据内容判断可复用的前缀，本节进一步将排队时间与链路带宽纳入路由决策。 | 同一个会话由此可以按状态与资源所在的位置重新安排。应用保留连续的上下文，服务在继续使用本地状态、迁移状态和重新计算之间选择，依据是这三条路径各自何时能让后续的模型执行开始。第 8 章根据内容判断可复用的前缀，本节进一步把排队时间与链路带宽纳入路由决策。 |
| 上面的比较假定缓存位置已知且数据可用。路由器通常依据缓存事件预测位置，事件延迟或对象淘汰会使预测失准。为了看清这种误差怎样影响响应时间，现在把 A 的队列改为 80 ms，且缓存只以概率 $p$ 真正可用。命中时为 90 ms，所有层次都失效时为 260 ms。其期望为 | 上面的比较假定缓存位置已知且数据可用。路由器通常根据缓存事件预测位置，事件延迟或对象被淘汰会让预测失准。为了看清这种误差怎样影响响应时间，把 A 的排队改为 80 ms，并设缓存只以概率 $p$ 真正可用。命中时为 90 ms，所有层次都失效时为 260 ms，期望为 |
| 平均优于 B 的 200 ms 需要 $p>6/17$。取 $p=0.9$，平均仅 107 ms，但 10% 的请求仍为 260 ms，因此该两点分布的 p99 为 260 ms，达不到 220 ms 的目标。为达到 220 ms 的 p99 目标，这组两点时间模型要求命中概率至少达到 99%；提高到 90% 已经显著改善均值，却仍让十分之一请求走上 260 ms 的慢路径。[^events] | 平均值要优于 B 的 200 ms，需要 $p>6/17$。取 $p=0.9$，平均只有 107 ms，但 10% 的请求仍是 260 ms，所以这个两点分布的 p99 为 260 ms，达不到 220 ms 的目标。要让 p99 达到 220 ms，这个两点时间模型要求命中概率至少 99%；90% 已经明显改善了均值，却仍让十分之一的请求走 260 ms 的慢路径。[^events] |
| 同时运行两个任务的负载压力实验，更直观地显示了这种取舍。缓存优先时，目标请求约 1.38 秒完成，两个任务也在此时全部结束；把目标请求移到空闲实例后，其完成时间缩短到约 0.33 秒，两个任务却约 1.54 秒才全部完成。目标快了约 1.05 秒，全部任务完成的时刻却推迟了约 0.16 秒。路由决策之前必须确定优化的是目标请求的响应时间，还是全部任务的完成时间。[^pressure] | 同时运行两个任务的负载压力实验更直观地显示了这种取舍。缓存优先时，目标请求约 1.38 秒完成，两个任务也在这时全部结束；把目标请求移到空闲实例后，它的完成时间缩短到约 0.33 秒，两个任务却要到约 1.54 秒才全部完成。目标请求快了约 1.05 秒，全部任务的完成时刻却推迟了约 0.16 秒。做路由决策之前，必须先确定优化的是目标请求的响应时间，还是全部任务的完成时间。[^pressure] |
| 将第 8 章的状态恢复过程计入路由决策后，需要同时比较实例何时空闲与状态何时准备好。设实例 A 保留全局 KV 与编码器 SWA 但有排队，实例 B 可立即执行却需要取回全局 KV 并恢复编码器 SWA。A 的优势是能够复用状态，B 的优势是计算资源可以立即开始工作；路由器需要比较它们的预计完成时间，而不是只看单个缓存命中标志。 | 把第 8 章的状态恢复过程计入路由决策后，要同时比较实例何时空闲与状态何时准备好。设实例 A 保留了全局 KV 与编码器的滑动窗口注意力（sliding window attention，SWA）状态，但有排队；实例 B 可以立即执行，却要取回全局 KV 并恢复编码器 SWA 状态。A 的优势是能复用状态，B 的优势是计算资源可以马上开工；路由器要比较的是两者的预计完成时间，而不是只看一个缓存命中标志。 |
| 设两端后续的新增输入处理、解码器窗口重放与生成耗时相同，准备工作串行执行。B 的全局传输按第七章为 4.666 ms，编码器 SWA 恢复假设为 8 ms，共需 12.666 ms。A 保留了这两份状态，准备时间就是排队时间：排队 10 ms 时，A 更早开始处理新增输入；排队增至 20 ms 时，B 更早。12.666 ms 由此成为本例切换路由选择的排队阈值。 | 设两边后续的新增输入处理、解码器窗口重放与生成耗时相同，准备工作串行执行。B 的全局状态传输按第 7 章的算例为 4.666 ms，编码器 SWA 状态的恢复假设为 8 ms，共需 12.666 ms。A 保留了这两份状态，准备时间就是排队时间：排队 10 ms 时 A 更早开始处理新增输入；排队增到 20 ms 时 B 更早。12.666 ms 由此成为本例中路由选择翻转的排队阈值。 |
| 前面的路由比较都从已有可用实例开始。扩容或故障恢复时，新实例要先完成启动，等待中的请求却仍在到达。因此，本节把启动和状态恢复纳入同一条服务时间线。一个新副本还要完成进程与分词器（tokenizer，将文本转换为 token 编号的组件）初始化、权重读取与分片、编译、KV 容量探测和图捕获。这些准备的总时间决定新副本何时开始分担请求。首次试运行触发编译时，编译时间已经包含在试运行中，应按先后依赖计算总时间。Breaking the Ice 对历史 vLLM 版本的阶段分析说明了这个问题；把包含关系展开后，启动总时间由从进程创建到可以接收请求的… | 前面的路由比较都从已经有可用实例开始。扩容或故障恢复时，新实例要先完成启动，而等待中的请求还在不断到达。因此，本节把启动和状态恢复纳入同一条服务时间线。一个新副本还要完成进程与分词器（tokenizer，将文本转换为 token 编号的组件）初始化、权重读取与分片、编译、KV 容量探测和图捕获。这些准备的总时间决定新副本何时开始分担请求。首次试运行触发编译时，编译时间已经包含在试运行里，应按先后依赖计算总时间。Breaking the Ice 对历史 vLLM 版本的分阶段分析说明了这个问题；把包含关系展开后，启动总时间由从进程创建到可以接收请求的… |
| 假设准备执行图使启动多花 $T_s$，但此后每个执行步都能节省 $\delta$，经过 $N$ 步后的净时间收益为 $N\delta-T_s$。多花 10 秒、每步省 0.2 ms，需要 50000 步才持平，从第 50001 步起，累计节省的时间才超过额外启动时间。若每步为 32 个请求各生成一个 token，50000 步对应约 160 万个输出 token。每执行一个批次，就能节省一次时间，因此应按批执行次数计算累计收益。 | 假设准备执行图让启动多花 $T_s$，但此后每个执行步都能省 $\delta$，经过 $N$ 步后的净收益为 $N\delta-T_s$。多花 10 秒、每步省 0.2 ms，要 50000 步才持平，从第 50001 步起累计节省的时间才超过额外的启动时间。若每步为 32 个请求各生成一个 token，50000 步对应约 160 万个输出 token。每执行一个批次省一次时间，所以累计收益要按批执行的次数算。 |
| 启动期间的新到达还会积压。若原有服务完全不可用、到达率保持 $\lambda$，启动时间为 $T_s$，启动结束时积压约为 $\lambda T_s$。就绪后能力为 $\mu>\lambda$，在理想连续流量模型中，额外排空时间为 | 启动期间新到达的请求还会积压。若原有服务完全不可用、到达率保持 $\lambda$、启动时间为 $T_s$，启动结束时积压约为 $\lambda T_s$。就绪后的服务能力为 $\mu>\lambda$，在理想的连续流量模型中，额外的排空时间为 |
| … 10 秒，期间每秒到达 4 个请求，就绪时已有 40 个请求积压。若就绪后每秒服务 8 个，新请求仍占去 4 个，实际每秒只能处理完 4 个积压请求，还需 10 秒排空；若服务能力只有 5 请求/s，每秒仅能处理完一个积压请求，要再等 40 秒。相同的 10 秒启动，对应从开始启动算起 20 秒或 50 秒的排空时间，如图 9-32 所示。 | … 10 秒，期间每秒到达 4 个请求，就绪时已有 40 个请求积压。若就绪后每秒能服务 8 个，新请求仍占去 4 个，每秒实际只能消化 4 个积压请求，还要 10 秒才能排空；若服务能力只有 5 请求/s，每秒只能消化一个积压请求，要再等 40 秒。同样是 10 秒启动，从开始启动算起的排空时刻分别是第 20 秒和第 50 秒，如图 9-32 所示。 |
| 并发预取还可能改变谁先开始执行。一个 1024 token 的请求在单独运行时复用 1008 个 token；八个请求同时到达时，前四个请求各登记了 1024 个 token 的预取，合计占用 4096 个 token，后续请求因超过容量限额而未登记。首个完成的请求恰来自后者，在没有命中任何缓存的情况下开始 prefill。它没有等缓存，因而更早进入执行队列；因此，即使缓存对象存在，请求也要先获得预取所需的空间，才能使用这份缓存。预取空间用尽后，后续请求会直接重算，而已经开始预取的请求仍在等待数据。[^branch] | 并发预取还可能改变谁先开始执行。一个 1024 个 token 的请求单独运行时复用了 1008 个 token；八个请求同时到达时，前四个请求各登记了 1024 个 token 的预取，合计占用 4096 个 token，后面的请求因为超过容量限额而没有登记。最先完成的请求恰恰来自后者：它没有命中任何缓存，直接开始 prefill；正因为没有等缓存，它更早进入了执行队列。所以，即使缓存对象存在，请求也要先拿到预取所需的空间，才能用上这份缓存。预取空间用完后，后面的请求直接重算，而已经开始预取的请求还在等数据。[^branch] |
| 启动解决的是新实例何时可用；运行中的迁移还要让目标实例接着源端的进度执行。改变 TP、EP 或服务池规模，需要重新安排权重与请求状态。目标加速器获得参数只是第一步，还可能需要建立通信组、准备新图、转换布局并恢复接收请求。这些准备与数据搬运形成先后依赖，共同决定何时能够把新请求交给目标。 | 启动解决的是新实例何时可用；运行中的迁移还要让目标实例接着源端的进度执行。改变张量并行度、专家并行度或服务池的规模，需要重新安排权重与请求状态。目标卡拿到参数只是第一步，还可能要建立通信组、准备新的执行图、转换布局并恢复接收请求。这些准备与数据搬运有先后依赖，共同决定何时能把新请求交给目标。 |
| 计划迁移可以利用源端仍在运行这一条件。先在后台复制不再改变的上下文，源端继续生成；最终短暂停止，复制完迁移期间新增的状态并将执行权交给目标。设还有 1 GiB 待复制，复制速率为 2 GiB/s，源端每秒… | 计划内的迁移可以利用源端仍在运行这个条件：先在后台复制不再改变的上下文，源端继续生成；最后短暂停下，复制完迁移期间新增的状态，再把执行权交给目标。设还有 1 GiB 待复制，复制速率为 2 GiB/s，源端每秒… |
| 运行时切换并行方式，将同一组加速器重新组织为 TP 或 SP×TP。这里 TP 切分层内矩阵，序列并行（SP）按 token 位置分配部分计算与中间状态，SP×TP 表示组合使用两种划分。ArcticInference 与 vLLM 的相关案例展示了这种组织方式：切换改变每步参与计算的加速器与 token 分布，也改变所需权重、KV 布局和执行图。预先保留两种模式的权重与图可以缩短切换等待，但额外保存这些权重与图会减少 KV 可用空间。Elastic EP 扩大专家组时，新加速器先获得专家权重，随后才能承担分派；处理完整请求还需要相应的 attention 状态。[^switch] | 运行时切换并行方式，是把同一组卡重新组织成张量并行（TP）或序列并行与张量并行的组合（SP×TP）。这里 TP 切分层内矩阵，序列并行（SP）按 token 位置分配一部分计算与中间状态。ArcticInference 与 vLLM 的相关案例展示了这种做法：切换改变每一步参与计算的卡与 token 的分布，也改变所需的权重、KV 布局和执行图。预先保留两种模式的权重与执行图可以缩短切换的等待，但这些额外保存的权重与图会减少 KV 的可用空间。Elastic EP 扩大专家组时，新加入的卡要先拿到专家权重，随后才能承担派发来的任务；处理完整的请求还需要相应的注意力状态。[^switch] |
| 目标追上源端的状态更新后，两端的数据才能保持一致。此外，目标还要完成通信组和执行图的准备。下面保持数据量不变，比较提高传输带宽究竟能缩短多少迁移时间。一个从四路张量并行（TP4）切换到八路张量并行（TP8）的迁移例子需要约 16.4 GB 网络传输，在本地构建目标张量还需要读写约 4.7 GB 数据。把同一份网络载荷放到有效 5 GB/s 和 25 GB/s 的路径，单看搬运约需 3.3 秒和 0.66 秒，相差五倍。 | 目标追上源端的状态更新后，两边的数据才一致。此外，目标还要完成通信组和执行图的准备。下面保持数据量不变，看提高传输带宽究竟能缩短多少迁移时间。一个从四路张量并行（TP4）切换到八路张量并行（TP8）的迁移例子需要约 16.4 GB 的网络传输，在本地构建目标张量还要读写约 4.7 GB 数据。把同一份网络载荷放到有效带宽 5 GB/s 和 25 GB/s 的路径上，单看搬运约需 3.3 秒和 0.66 秒，相差五倍。 |
| 但若另假设建立通信组、执行图准备与恢复接收请求在传输后串行占用 9 秒，总时间便约为 12.3 秒和 9.7 秒，只缩短约 21%。继续提高带宽的收益已经被这 9 秒限制。即使网络传输趋近于零，总时间也只能趋近 9 秒。若能提前建立通信组并准备好执行图… | 但若再假设建立通信组、准备执行图与恢复接收请求在传输之后串行占用 9 秒，总时间就约为 12.3 秒和 9.7 秒，只缩短约 21%。继续提高带宽的收益已经被这 9 秒限制住：即使网络传输趋近于零，总时间也只能趋近 9 秒。若能提前建立通信组并准备好执行图… |
| 计划迁移时，源端还能提供最后一次更新；故障发生后，源端的数据可能已经无法读取，只能依靠提前保存的状态和输出记录。恢复首先要确定用户已经收到哪一段输出。故障后有三种不同目标：恢复足够状态继续 decode；根据已经确定的 token 重做 prefill；重新采样一段输出。前两种方法沿用户已经收到的序列继续生成，第三种重新执行随机选择，可能产生不同后缀。对于强化学习（RL）的采样轨迹生成（rollout），这还改变了样本的生成过程。 | 计划内迁移时源端还能提供最后一次更新；故障发生后，源端的数据可能已经读不到，只能依靠提前保存的状态和输出记录。恢复首先要确定用户已经收到哪一段输出。故障后有三种不同的目标：恢复足够的状态继续 decode；根据已经确定的 token 重做 prefill；重新采样一段输出。前两种沿用户已经收到的序列继续生成，第三种重新做随机选择，可能产生不同的后缀。对强化学习（RL）的采样轨迹生成（rollout）来说，这还改变了样本的生成过程。 |
| …用户返回 129 个输出，但最近保存的 KV 仅覆盖原输入。恢复时，若这些输出已可靠记录，只需把前 128 个输出补入模型，重建到 8320 个 token 的 KV，再处理第 129 个输出以继续生成。若已有 8320 token 的兼容 KV，则可省去这 128 个 token 的重放。 | …用户返回 129 个输出，但最近保存的 KV 仅覆盖原输入。恢复时，若这些输出已经可靠记录，只需把前 128 个输出补入模型，重建覆盖 8320 个 token 的 KV，再处理第 129 个输出继续生成。若已经有覆盖 8320 个 token 的兼容 KV，就可以省去这 128 个 token 的重放。 |
| 图 9-34 将两种记录的末端分开画出。输出已经返回，并不表示对应 KV 已经保存；恢复时用已记录的 token 补算，就能重建相同前缀，而无须重新采样这些输出。 | 图 9-34 把两种记录的末端分开画出。输出已经返回，不表示对应的 KV 已经保存；恢复时用已记录的 token 补算，就能重建相同的前缀，不必重新采样这些输出。 |
| 故障范围由同步组决定。TP 或 EP 的一个 rank 丢失，可能使整个协作实例上的请求停住；完整副本的其他实例则可能继续服务。故障恢复依次经历检测、实例重建与请求恢复；恢复后的处理能力决定积压请求还需多久才能完成。第 10 章进一步分析这些选择对 RL 样本与训练进展的影响，第 … | 故障的影响范围由同步组决定。张量并行组或专家并行组丢失一张卡，可能让整个协作实例上的请求停住；其他完整副本则可以继续服务。故障恢复依次经过检测、实例重建与请求恢复；恢复后的处理能力决定积压的请求还要多久才能完成。第 10 章进一步分析这些选择对 RL 样本与训练进展的影响，第 … |
| …哪些计算资源、状态怎样取回，以及启动和故障期间会积压多少请求。下面把这些结果用于开篇的八 worker 服务，先确定状态传输方式，再比较整个服务能否满足到达率和恢复期限。P 产生的 KV 可以直接交给 D，也可以先进入共享池再由 D 取回；D 新生成的状态若要用于下一轮 P，还需确认它们已经保存并发布。直接交接与经池中转可能读取相同内容，却占用不同链路和缓冲。 | …哪些计算资源、状态怎样取回，以及启动和故障期间会积压多少请求。下面把这些结果用于章首的八 worker 服务，先确定状态传输方式，再比较整个服务能否满足到达率和恢复期限。P 产生的 KV 可以直接交给 D，也可以先进入共享池再由 D 取回；D 新生成的状态若要用于下一轮 P，还需确认它们已经保存并发布。直接交接与经池中转读取的可能是相同的内容，占用的链路和缓冲却不同。 |
| 若状态只被 D 使用一次，这次中转没有获得复用收益。若后面还有另一个实例要继续处理同一会话，它便有机会用约 48.3 ms 的读取替代一次 180 ms 的重算，节省约 132 ms，超过先前多花的约 48.3 ms。因此，后续请求复用这些 KV 时省下的计算时间，可以抵消额外的搬运时间。共享池将一次会话留下的计算成果用于后续请求；AF 则改变当前请求每一层的执行位置，两者分别改变时间线上的不同部分。 | 若状态只被 D 用一次，这次中转就没有复用收益。若后面还有另一个实例要继续处理同一个会话，它就有机会用约 48.3 ms 的读取替代一次 180 ms 的重算，省下约 132 ms，超过先前多花的约 48.3 ms。因此，后续请求复用这些 KV 省下的计算时间，可以抵消额外的搬运时间。共享池把一次会话留下的计算成果用于后续请求；AF 则改变当前请求每一层的执行位置，两者改变的是时间线上不同的部分。 |
| 同样的规则适用于视觉。E 产生的 EC 与语言 KV 使用不同的缓存标识，占用的空间也不同；图像缓存命中可能减少 E 的工作，却不会自动消除后续语言模型的 P 阶段。在 E→P→D 的执行图中，EC 命中使 E 的需求下… | 同样的规则也适用于视觉输入。E 产生的 EC 与语言 KV 使用不同的缓存标识，占用的空间也不同；图像缓存命中可以减少 E 的工作，却不会自动省掉后续语言模型的 P 阶段。在 E→P→D 的执行图中，EC 命中使 E 的需求下… |
| …192 个 token、输出 129 个 token。有效能力沿用例 9.2，两阶段在同一 worker 上执行时，依次占用该资源，表中有效能力对应下述驻留限制。链路为共享的 25 GB/s 载荷通道，各次搬运占用同一份链路带宽。比… | …192 个 token、输出 129 个 token。有效能力沿用例 9.2，两个阶段在同一个 worker 上执行时依次占用该资源，表中的有效能力对应下面的驻留限制。链路为共享的 25 GB/s 载荷通道，各次搬运占用同一份链路带宽。比… |
| 现在比较完整副本、直接交接的 PD、经共享池交接的 PD。共享池按整份写入后读取，因此每请求在公共通道上搬两次；其读写端均能达到该通道的带宽。 | 现在比较完整副本、直接交接的 PD、经共享池交接的 PD。共享池按整份写入后再读取，因此每个请求在公共通道上搬两次；池的读写两侧都能达到这条通道的带宽。 |
| …吞吐率相同，但直接 PD 更适合这组请求。这批请求的前缀不会被后续请求复用，中转使通道传输量增加一倍，每请求还多等待约 48.3 ms。64 GiB 共享池至多容纳 56 份完整的 1.125 GiB 前缀，保留下来的每一份在本次工作负载中都不会再次使用。直接交接完成相同工作，留出更多链路余量，并缩短交接等待。因此本例选择四 A 做 P、四 B 做 D、P→D 直传。 | …吞吐率相同，但直接 PD 更适合这组请求。这批请求的前缀不会被后续请求复用，中转却让通道传输量翻倍，每个请求还多等约 48.3 ms。64 GiB 的共享池最多容纳 56 份完整的 1.125 GiB 前缀，保留下来的每一份在这个负载里都不会再被用到。直接交接完成同样的工作，留出更多链路余量，交接等待也更短。因此本例选择四 A 做 P、四 B 做 D、P→D 直传。 |
| …方案能在 20 秒内排空，但执行单元的处理速度稍低时是否仍能满足 25 秒期限？将问题反过来，就能求出达到这个期限所需的最低服务率。启动占去 10 秒，剩余 15 秒要消除 40 个请求… | …方案能在 20 秒内排空，但执行单元的处理速度稍低时是否仍能满足 25 秒期限？把问题反过来，就能求出满足这个期限所需的最低服务率。启动占去 10 秒，剩余 15 秒要消除 40 个请求… |
| …求，则每小时完成 14400 个请求，成本约为每千请求 0.56 元。若到达率仍为四，具有八请求/s 的能力不会凭空产生另外四个请求；留下的能力用于消化突发和启动积压。 | …求，则每小时完成 14400 个请求，成本约为每千请求 0.56 元。若到达率仍是每秒四个，八请求/s 的能力不会凭空多出四个请求来处理；多出来的能力用于消化突发和启动积压。 |
| …与八请求/s 对应每千请求约 0.69 元与 0.28 元。若业务另设完成时限，后一方案仅有四分之一请求合格，每秒合格的请求数便降到两个，每千个合格请求的成本升至约 1.11 元。一般写成 | …与八请求/s 对应每千请求约 0.69 元与 0.28 元。若业务另设完成时限，而后一方案只有四分之一的请求合格，每秒合格的请求数就降到两个，每千个合格请求的成本升到约 1.11 元。一般写成 |
| 现在改变例 9.7 中“前缀不再复用”这一条件。设每次留下的 1.125 GiB 上下文 KV 缓存还会被另一个实例复用一次，重算耗时为 180 ms，取回耗时为 48.3 ms。先前为中转多付出的 48.3 ms，加后续取回的 48.3 ms，共约 96.6 ms，低于后续重算的 180 ms，一次后续复用就净省约 83 ms。共享池由此获得了具体用途… | 现在改变例 9.7 中“前缀不再复用”这个条件。设每次留下的 1.125 GiB 上下文 KV 缓存还会被另一个实例复用一次，重算耗时为 180 ms，取回耗时为 48.3 ms。先前为中转多付的 48.3 ms 加上后续取回的 48.3 ms，共约 96.6 ms，低于重算的 180 ms，一次后续复用就净省约 83 ms。共享池由此获得了具体用途… |
| 这项收益同时增加通道需求。每次原始交接写一次、读一次，后续复用再读一次，共搬 3.375 GiB。每秒到达四组这样的请求对时，共需约 14.5 GB/s；通道最多供给约 6.9 组请求对/s。与只传输一次相比，后续复用增加了数据读取量，占用了原有的带宽余量。共享池省去重算的同时，也降低了通道能够支持的请求对吞吐上限。 | 这项收益同时增加了通道的需求。每次原始交接写一次、读一次，后续复用再读一次，共搬 3.375 GiB。每秒到达四组这样的请求对时，共需约 14.5 GB/s；通道最多支持约 6.9 组请求对/s。与只传一次相比，后续复用增加了读取量，用掉了原有的带宽余量。共享池省去重算的同时，也降低了通道能支持的请求对吞吐上限。 |
| 共享池的比较改变了传输次数。下面固定 D 没有缓存，只改变 P 需要重新计算多少输入，看看资源分配是否也应调整。假定 P 已在本地命中 6144 个 token，D 仍为空。沿第 9.2 节计算，完整副本合计约 5.88 请求/s，原来四 P、四 D 为八请求/s，改成两个 A 做 P、其余六个 worker 做 D 则为九请求/s。若仍采用相同的 10 秒启动与四请求/s 到达，三者从开始启动算起分别约在第 31.3、20 和 18 秒排空。前缀复用使完整副本也能处理稳定到达的请求，但它仍达不到第 25 秒排空的目标；两 P、六 D 则进一步缩短积压持续时间。 | 共享池改变的是传输次数。下面固定 D 没有缓存，只改变 P 要重新计算多少输入，看资源分配是否也要调整。假定 P 已在本地命中 6144 个 token，D 仍为空。沿第 9.2 节计算，完整副本合计约 5.88 请求/s，原来四 P、四 D 为八请求/s，改成两个 A 做 P、其余六个 worker 做 D 则为九请求/s。若仍采用相同的 10 秒启动与四请求/s 到达，三者从开始启动算起分别约在第 31.3、20 和 18 秒排空。前缀复用让完整副本也能处理持续到达的请求，但它仍达不到第 25 秒排空的目标；两 P、六 D 则进一步缩短了积压持续的时间。 |
| 再把输出改为 1025 个 token，并恢复无命中输入，最佳一 P、七 D 也只有约 1.19 请求/s。此时改变 P、D 实例的数量比例已无法让原来的八个 worker 处理每秒四个请求。复制四组这样的八 worker 部署，可得到约 4.75 请求/s 的合计能力；若还要求相同的启动与排空期限，所需能力仍为至少 $20/3$ 请求/s，五组只能提供约 5.94，六组约为 7.13，因而要配置六组。保持原有计算资源组合按组扩展时，持续处理到达的请求需要 32 个 worker，满足恢复期限需要 48 个。额外计算资源的用途是加快积压消退。 | 再把输出改为 1025 个 token，并恢复无命中的输入，最佳的一 P、七 D 也只有约 1.19 请求/s。这时无论怎样改变 P、D 实例的数量比例，原来的八个 worker 都处理不了每秒四个请求。复制四组这样的八 worker 部署，合计能力约 4.75 请求/s；若还要求相同的启动与排空期限，所需能力仍至少为 $20/3$ 请求/s，五组只能提供约 5.94，六组约 7.13，因此要配置六组。按组扩展原有的计算资源组合时，持续处理到达的请求需要 32 个 worker，满足恢复期限需要 48 个。额外计算资源的用途是加快积压的消退。 |
| 这也给出了把本章推导用于新系统的顺序。先把每个请求的工作量换算成各类资源需求；再按整数资源分配找出最忙的资源池或执行单元；按执行顺序计入状态传输时间与内存占用；最后用服务率减去到达率得到的剩余处理能力求出突发或启动积压的持续时间。若瓶颈是 D，增加 P 不会减少积压；若瓶颈是共享通道，增加计算副本只会让更多请求等候取回。 | 这也给出了把本章的推导用于新系统的顺序。先把每个请求的工作量换算成各类资源需求；再按整数的资源分配找出最忙的资源池或执行单元；按执行顺序计入状态传输时间与内存占用；最后用服务率减去到达率得到的剩余处理能力，求出突发或启动积压持续的时间。若瓶颈是 D，增加 P 不会减少积压；若瓶颈是共享通道，增加计算副本只会让更多请求等着取回。 |
| PD 的八 worker 案例和 MoE 的专家案例在这里相接。前者将不同阶段交给更擅长处理该阶段的计算资源，后者通过批内复用减少权重读取，再通过专家副本分散最忙执行单元上的工作。共享 KV 则保存已完成的计算结果，供下一次请求复用，启动与恢复决定这些计算资源何时能够提供服务。一次部署选择的依据，是这些部署方式共同改变了多少计算量和等待时间，以及消除积压所需的时间。 | PD 的八 worker 案例和 MoE 的专家案例在这里相接。前者把不同阶段交给更擅长该阶段的计算资源，后者靠批内复用减少权重读取，再靠专家副本分散最忙执行单元上的工作。共享 KV 则保存已完成的计算结果供下一次请求复用；启动与恢复决定这些计算资源何时能提供服务。一次部署选择的依据，是这些部署方式一共改变了多少计算量和等待时间，以及消除积压需要多久。 |
| 单个阶段变快也会改变实例配比。设链路充足，每个 P 实例每秒处理 20 条请求，每个 D 实例每秒完成 5 条相同请求，1P 配 4D 时两池能力相等。若 D 阶段加速四倍，沿用原配比会使 D 池能力增至 80 请求/s，而 P 池仍为 20；改为 1P 配 1D，两池都提供 20 请求/s，并释放三个 D 实例。加速一个阶段的收益由此体现为完成相同工作所需的资源减少。 | 单个阶段变快也会改变实例配比。设链路充足，每个 P 实例每秒处理 20 个请求，每个 D 实例每秒完成 5 个同样的请求，一个 P 配四个 D 时两池能力相等。若 D 阶段加速四倍，沿用原配比会让 D 池能力增到 80 请求/s，P 池仍是 20；改为一个 P 配一个 D，两池都提供 20 请求/s，并释放三个 D 实例。加速一个阶段的收益，由此体现为完成同样的工作所需的资源减少。 |
| …用数，再枚举将八个 worker 分配给 P、D 两阶段的所有数量组合。允许复制相同的八 worker 组合，分别求两种目标下所需的最少组数：持续处理新到达的请求；… | …用数，再枚举将八个 worker 分配给 P、D 两阶段的所有数量组合。允许复制同样的八 worker 组合，分别求两种目标下所需的最少组数：持续处理新到达的请求；… |
| 这个十行边界与图 9-14 的 78／79 行边界回答不同问题。十行是 CPU 从带宽受限转为计算受限的分界；78／79 行比较包括权重搬运的完整 CPU、GPU 两条路径。CPU 在十行后已经计算受限，但仍能继续胜过需要先搬权重的 GPU 路径，直到累计计算代价超过这笔搬运成本。 | 这个十行边界与图 9-14 的 78／79 行边界回答的是不同的问题。十行是 CPU 从带宽受限转为计算受限的分界；78／79 行比较的是包含权重搬运的完整 CPU、GPU 两条路径。CPU 过了十行已经计算受限，但仍能胜过要先搬权重的 GPU 路径，直到累计的计算代价超过这笔搬运成本。 |
| …/cache-route-fast-remote.md)。具体公式包含整份远端→host→GPU 串行路径。 | …/cache-route-fast-remote.md)。具体公式包含整份远端→主机内存→GPU 的串行路径。 |
| …t-model]: 例 9.7 的容量、共享通道、启动时间、排空目标和成本，以及 SLO 通过比例、9.4.3 的重叠时间均为教材假设，用于推导条件变化。单位成本用完整小… | …t-model]: 例 9.7 的容量、共享通道、启动时间、排空目标和成本，以及服务目标（SLO）的通过比例、9.4.3 的重叠时间均为教材假设，用于推导条件变化。单位成本用完整小… |
| …式推理把一条请求的计算与状态分配到多个位置。PD、AF、专家放置和共享 KV 都需要比较局部执行收益与新增交接，容量、服务率、启动和恢复共同决定部署是否合适。路由不仅选择空闲计算资源，也… | …式推理把一条请求的计算与状态分配到多个位置。PD、AF、专家放置和共享 KV 都要比较局部执行的收益与新增的交接，容量、服务率、启动和恢复共同决定部署是否合适。路由不仅选择空闲计算资源，也… |

## 翻译腔审查（2026-09-11）

作者确认术语原则并统一“笔者”“N 个 token”“Queqiao”之后，再做一遍只针对翻译腔的审查（含图注里汉字数字改阿拉伯数字）。共 26 行有改动，按最终文本与审查前快照逐行对照。

| 修改前 | 修改后 |
|---|---|
| …，包括专家网络）交给不同资源。**KV** 是上下文 token 的键和值，保存了继续进行注意力计算所需的状态。 | …，包括专家网络）交给不同资源。**KV** 是上下文 token 的键和值，保存着后续注意力计算所需的状态。 |
| …求的到达率，每秒积压 0.8 个请求。问题由此变得明确：同样八个 worker，能否通过重新分工消除这 0.8 请求/s 的缺口？ | …求的到达率，每秒积压 0.8 个请求。问题由此变得明确：同样八个 worker，重新分工能不能补上这 0.8 请求/s 的缺口？ |
| …ve 与 Splitwise 是研究 P、D 分离部署的推理系统。同构的计算资源可以通过独立队列减少两个阶段之间的调度干扰，异构的计算资源还可以各自承担更擅长的阶段。[… | …ve 与 Splitwise 是研究 P、D 分离部署的推理系统。同构的计算资源靠独立队列减少两个阶段之间的调度干扰，异构的计算资源还可以各自承担更擅长的阶段。[… |
| …目的端确认收到某一块后，源端就可以提前释放这一块，缩短两端重复驻留的时间；目的端通过完成标记确定哪些连续的块已经可用。若经主存中转，状态则依次占用源端、主存和目的端… | …目的端确认收到某一块后，源端就可以提前释放这一块，缩短两端重复驻留的时间；目的端靠完成标记确定哪些连续的块已经可用。若经主存中转，状态则依次占用源端、主存和目的端… |
| …s。一条 11 GB/s 的链路能满足前者的平均需求，但每个请求仍要等传输完成；对于后者，即使两池有空闲计算能力，网络队列也会持续增长。前一种情形下每份状态要约 110 … | …s。一条 11 GB/s 的链路能满足前者的平均需求，但每个请求仍要等传输完成；后者则不同：即使两池有空闲计算能力，网络队列也会持续增长。前一种情形下每份状态要约 110 … |
| 对于这三种请求，最合适的 P 单元数分别是四个、两个和一个。前缀命中减少 P 的新工作，输出增长… | 这三种请求最合适的 P 单元数分别是四个、两个和一个。前缀命中减少 P 的新工作，输出增长… |
| 混合专家模型（MoE）由路由器为每个 token 选择部分专家网络，为这种分工提供了一个特别的机会。总专家权重很大，但一个 token 只选择其中一部分。若大多数专家保存在主存，… | 混合专家模型（MoE）由路由器为每个 token 选择一部分专家网络，特别适合这种分工。总专家权重很大，但一个 token 只选择其中一部分。若大多数专家保存在主存，… |
| …，并不能让每条请求随意绕开繁忙的专家。共享专家池的队列、显存、模型版本和接纳策略需要作为整体管理。 | …，并不能让每条请求随意绕开繁忙的专家。共享专家池的队列、显存、模型版本和接纳策略要当作一个整体来管理。 |
| …为 BF16，生产请求的输入／输出长度中位数为 571／159 个 token。在每个输出 token 的平均耗时（TPOT）不超过 150 ms 的评估约束下，在八个节点、每节点八张 80 GB Ampere GPU 的同构集群上，Scal… | …为 BF16，生产请求的输入／输出长度中位数为 571／159 个 token。评估约束是每个输出 token 的平均耗时（TPOT）不超过 150 ms；在八个节点、每节点八张 80 GB Ampere GPU 的同构集群上，Scal… |
| … 的实测进一步表明，复制收益与层有关。该研究基于 SGLang v0.4.8，在由八个 AWS p4de.24xlarge 节点组成的 A100 80GB 集群上运行 BF16 DeepSeek-R1 和 Kimi K2，比较在显存预算内按层分配专家副本与既有复制策略。论文报告端到端吞吐平均达到对照的 1.14 倍、最高 1.2 倍。这支持“把副本预算优先给收益大的层”，不表示任意热点复制都能得到这一增幅；其输入按 4096 个 token 分块、输… | … 的实测进一步表明，复制收益与层有关。该研究基于 SGLang v0.4.8，在八个 AWS p4de.24xlarge 节点组成的 A100 80GB 集群上运行 BF16 的 DeepSeek-R1 和 Kimi K2，比较显存预算内按层分配专家副本与既有复制策略的效果。论文报告端到端吞吐平均达到对照的 1.14 倍、最高 1.2 倍。这说明把副本预算优先给收益大的层是对的，但不表示任意热点复制都能得到这一增幅；其输入按 4096 个 token 分块、输… |
| …另有 8／16 张 H200；这些结果不能合成一个跨集群的端到端加速比。它提供的是证据，说明路由、实际矩阵形状、通信和内存访问需要同时测量。[^ep-papers] | …另有 8／16 张 H200；这些结果不能合成一个跨集群的端到端加速比。它提供的证据说明，路由、实际矩阵形状、通信和内存访问要一起测量。[^ep-papers] |
| 前两节通过改变执行位置和任务分配，减少了当前请求的耗时。另一种节省计算的方法，是让后续请求… | 前两节靠改变执行位置和任务分配，减少了当前请求的耗时。另一种节省计算的方法，是让后续请求… |
| 假定状态将在未来复用 $k$ 次，暂不考虑排队时间，也不考虑它占用空间后导致其他缓存内容被淘汰的影响。保存后再取回比重新计算更快的条件为 | 假定状态将在未来复用 $k$ 次，暂不考虑排队时间，也不考虑它占用空间后挤掉其他缓存内容的影响。保存后再取回比重新计算更快的条件为 |
| …的时间。实际的截断页实验里，无条件等待的请求在 60 秒观察窗口内一直没有完成，采用允许放弃等待的策略后，请求靠重算得到了结果。完成当前请求之后，还要隔离或修复坏页，否则下一个请求还… | …的时间。实际的截断页实验里，无条件等待的请求在 60 秒观察窗口内一直没有完成，改为允许放弃等待后，请求靠重算得到了结果。完成当前请求之后，还要隔离或修复坏页，否则下一个请求还… |
| ![缓存亲和性与空闲执行位置的比较。A 保留全局 KV 与编码器 SWA；B 需要 4.666 ms 全局传输和假设的 8 ms 编码器恢复。两端共同的解码器重放等后续工作略去，只比较不同的串行准备时间；A 的排队时间从 10 ms 增到 20 ms 时，更快的方案由 A 变为 B。](ch09/figure-9-v41-routing.svg) | ![缓存亲和性与空闲执行位置的比较](ch09/figure-9-v41-routing.svg) |
| *图 9-31　缓存亲和性与空闲执行位置的比较。A 保留全局 KV 与编码器 SWA；B 需要 … | *图 9-31：缓存亲和性与空闲执行位置的比较。A 保留全局 KV 与编码器 SWA；B 需要 … |
| …后串行占用 9 秒，总时间就约为 12.3 秒和 9.7 秒，只缩短约 21%。继续提高带宽的收益已经被这 9 秒限制住：即使网络传输趋近于零，总时间也只能趋近 9 秒。若能提前建立通信组并准备好执行… | …后串行占用 9 秒，总时间就约为 12.3 秒和 9.7 秒，只缩短约 21%。这 9 秒卡住了继续提高带宽的收益：即使网络传输趋近于零，总时间也只能趋近 9 秒。若能提前建立通信组并准备好执行… |
| 若状态只被 D 用一次，这次中转就没有复用收益。若后面还有另一个实例要继续处理同一个会话，它就有机会用… | 若 D 只用一次这份状态，这次中转就没有复用收益。若后面还有另一个实例要继续处理同一个会话，它就有机会用… |
| 完整副本每秒只能完成 3.2 个请求，持续少于到达的四个，因此首先被排除。两种 PD 方案的通道能力都高于八请求/s，均由计算池限制。在每秒四个请求… | 完整副本每秒只能完成 3.2 个请求，持续少于到达的四个，因此首先排除。两种 PD 方案的通道能力都高于八请求/s，均由计算池限制。在每秒四个请求… |
| 两种方案的稳态吞吐率相同，但直接 PD 更适合这组请求。这批请求的前缀不会被后续请求复用，中转却让通道传输量翻倍，每个请求还多等约 48.3 ms。64 GiB 的共享池最多容纳 56 份完整的 1.125 GiB 前缀，保留下来的每一份在这个负载里都不会再被用到。直接交接完成同样的工作，留出更多链路余量，交接等待也更短。因此本例选择四 … | 两种方案的稳态吞吐率相同，但直接 PD 更适合这组请求。后续请求不会复用这批请求的前缀，中转却让通道传输量翻倍，每个请求还多等约 48.3 ms。64 GiB 的共享池最多容纳 56 份完整的 1.125 GiB 前缀，在这个负载里，保留下来的每一份都不会再用到。直接交接完成同样的工作，留出更多链路余量，交接等待也更短。因此本例选择四 … |
| 现在改变例 9.7 中“前缀不再复用”这个条件。设每次留下的 1.125 GiB 上下文 KV 缓存还会被另一个实例复用一次，重算耗时为 180 ms，取回耗时为 48.3 ms。先前为中转多付的… | 现在改变例 9.7 中“前缀不再复用”这个条件。设另一个实例还会把每次留下的 1.125 GiB 上下文 KV 缓存复用一次，重算耗时为 180 ms，取回耗时为 48.3 ms。先前为中转多付的… |
| **9-1　三种推理部署的调用次数与状态交接。** 对于输入为 8192 个 token、输出为 129 个 token 的请求，画出完整副本、PD 和共享 KV 三种部署方式… | **9-1　三种推理部署的调用次数与状态交接。** 对输入 8192 个 token、输出 129 个 token 的请求，画出完整副本、PD 和共享 KV 三种部署方式… |
| … ms。比较全部串行执行与理想双阶段流水的总耗时，再求额外通信和计算效率下降合计可增加多少关键路径耗时，才能使流水方案仍比串行方案更快。 | … ms。比较全部串行执行与理想双阶段流水的总耗时，再求额外通信和计算效率下降合计最多能增加多少关键路径耗时，流水方案仍比串行方案快。 |
| …子。结合已有双引擎记录，解释共享 CPU 池从 4 GiB 增至 8 GiB 时，哪些复用机会得以保留。 | …子。结合已有双引擎记录，解释共享 CPU 池从 4 GiB 增至 8 GiB 时保住了哪些复用机会。 |
| **9-8　KV 缺页与版本不兼容时的恢复选择。** 解释读取 1024 个 token 的 KV，但只复用其中 1008 个 token 的 KV 的计量差异。对缺页、截断页、版本不兼容分别提出继续等待、部分重算与放弃缓存的判断条件。… | **9-8　KV 缺页与版本不兼容时的恢复选择。** 解释读取了 1024 个 token 的 KV、却只复用其中 1008 个 token 时，两种计量方式的差异。对缺页、截断页、版本不兼容分别提出继续等待、部分重算与放弃缓存的判断条件。… |
| … 的容量、共享通道、启动时间、排空目标和成本，以及服务目标（SLO）的通过比例、9.4.3 的重叠时间均为教材假设，用于推导条件变化。单位成本用完整小时成本除以（3600×… | … 的容量、共享通道、启动时间、排空目标和成本，以及服务目标（SLO）的通过比例、第 9.4.3 节的重叠时间均为教材假设，用于推导条件变化。单位成本用完整小时成本除以（3600×… |
