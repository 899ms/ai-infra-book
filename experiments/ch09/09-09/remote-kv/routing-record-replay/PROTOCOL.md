# Remote-aware routing record replay

Use four threshold16 Chat consumer states and twelve default256 Agent consumer states, preserving their measured request service and device/host/storage hits. Alternative: the identical input/output from cache-disabled control, pass1 or pass2. Pass0 is calibration only; retain its warmup effects. Each decision is an independent state snapshot, not a state-evolving online trace.

Queue scenarios transplant each of six observed busy_remaining_at_dispatch_s values from the earlier live queue-pressure experiment, with either the cache worker or recomputation worker busy. These are explicit additive no-overlap queue scenarios, not newly measured remote-worker concurrency or observed Chat queue delays. Idle residual0. One request behind a nonpreemptive job. No claim of shared-GPU interference prediction.

Queue-first chooses least residual queue, tie recomputation. Cache-first chooses most reusable tokens (device plus eligible remote prefix), tie least queue then recomputation. Predicted completion uses queue plus estimated service. Recompute estimate is pass0 for the same input. Cache estimate is pass0 plus eligible missing pages times mean GET-page RPC from the independent earlier cross-mac-005 run. For device-only states use previous frozen completion-prediction warm_after_busy_s. Remote admission requires at least16/256 missing tokens according to the measured configuration. Estimate excludes existence-check costs and is deliberately evaluated with error, not fitted to target consumer latency. Tie recomputation. Decision inputs and estimates are stored before realized service is evaluated.

16 inputs ×6 prior queue values ×2 busy orientations ×2 held-out cold passes ×3 rules =1152 decisions. No new GPU calls. Realized completion is assumed queue residual plus recorded service; preserve request/token hit rates, level totals and sample GET sums. Oracle regret compares both recorded alternatives in the same scenario and is descriptive, not optimal online routing. All outputs must match; every source is hashed. Small repeated samples are not independent evidence for production p95.
