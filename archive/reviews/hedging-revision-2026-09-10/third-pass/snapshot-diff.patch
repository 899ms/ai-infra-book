--- before/manuscripts/01-初识 AI Infra.md
+++ after/manuscripts/01-初识 AI Infra.md
@@ -347,7 +347,7 @@
 
 ### 1.3.3 用实测检验估算
 
-前面的模型预测了两个趋势：batch 较小时，吞吐随 batch 增加；整批时间则受到一次权重读取的限制。将模型用于真实程序时，还要检查各请求新增的上下文访问和计算。图 1-14、1-15 给出 Qwen3-8B 在 RTX PRO 6000 Blackwell 上的测量：使用 BF16 权重。vLLM 是加州大学伯克利分校等机构的研究者于 2023 年提出的开源推理服务系统，最初重点解决 KV 缓存浪费显存、限制批量的问题；它组织模型请求并执行推理。本次采用 0.23 版本的 eager 模式，即按程序运行过程逐项提交设备工作。每请求输入 2048 个 token、生成 256 个 token，每档 batch 测量三次。[^measurement] 吞吐表示单位时间内产生的输出数；TPOT（time per output token）在这里表示客户端观察到的平均输出间隔。下面先看整批输出速度，再看单个请求的生成节奏。
+前面的模型预测了两个趋势：batch 较小时，吞吐随 batch 增加；整批时间则受到一次权重读取的限制。真实程序中，每条新增请求还会增加上下文访问与计算。图 1-14、1-15 给出 Qwen3-8B 在 RTX PRO 6000 Blackwell 上的测量：使用 BF16 权重。vLLM 是加州大学伯克利分校等机构的研究者于 2023 年提出的开源推理服务系统，最初重点解决 KV 缓存浪费显存、限制批量的问题；它组织模型请求并执行推理。本次采用 0.23 版本的 eager 模式，即按程序运行过程逐项提交设备工作。每请求输入 2048 个 token、生成 256 个 token，每档 batch 测量三次。[^measurement] 吞吐表示单位时间内产生的输出数；TPOT（time per output token）在这里表示客户端观察到的平均输出间隔。下面先看整批输出速度，再看单个请求的生成节奏。
 
 ![Qwen3-8B 实测的整批输出吞吐。四档请求数等距排列，纵轴从零开始；模型、精度、输入输出长度和计时范围保持一致。](ch01/figure-1-measured-throughput.svg)
 
@@ -473,17 +473,17 @@
 
 [^budget]: [70B 近似预算复算](../calculations/results/decode-budget-base.md)及配套场景；[单位与逐卡容量复算](../calculations/results/basics-70b-bf16-balanced.md)。1.3 的时间估算图读取这组固定的 70B 近似结果；1.2.3 的容量图使用真实权重索引与分组量化结果。
 
-[^roofline]: Williams、Waterman、Patterson，[Roofline 论文](../references/files/papers/roofline.pdf)。本章只引入算术强度和资源上界的直觉，实际层次、访问量和执行效率在第 4、5 章展开。
+[^roofline]: Williams、Waterman、Patterson，[Roofline 论文](../references/files/papers/roofline.pdf)。
 
 [^measurement]: [练习 1-4 记录说明](../experiments/ch01/01-04/README.md)、[结构化分析结果](../experiments/ch01/01-04/results/analysis.json)及[后续补测](../experiments/ch01/01-04/FOLLOWUP.md)。正文和图 1-14、图 1-15 采用最初记录的短输入组。
 
 [^tpu]: Jouppi 等，[TPU v1 论文](../references/files/papers/tpu-v1.pdf)，第 2 节关于起源、架构与实现的说明。三分钟语音搜索是论文记载的历史需求预测。
 
-[^nic]: [作者博士论文](../references/files/papers/bojieli-phd-thesis.pdf)，第 4.2.1 节简单转发基线；[核数预算复算](../calculations/results/nic-budget-book.md)。约六千万包每秒来自 40 Gb/s 与每帧占用 84 字节线时的计算，3 至 6 核未包含完整虚拟化工作。
+[^nic]: [作者博士论文](../references/files/papers/bojieli-phd-thesis.pdf)，第 4.2.1 节简单转发基线；[核数预算复算](../calculations/results/nic-budget-book.md)。约六千万包每秒来自 40 Gb/s 与每帧占用 84 字节线时的计算，3 至 6 核对应简单转发基线。
 
 [^ub]: 项目参与者回顾：[《Unified Bus 背后的思考》](../references/files/documents/ub-reflection.md)，“UB 超节点”一节记述研究先于 GPT-3、2020 年后扩大投入的过程。
 
-[^abstraction]: 这一对照是本书的分析视角。背景与适用边界见[抽象边界调研](../research/system-abstraction-boundary/report.md)，其中保存了 OSDI／SOSP、基础模型报告与隔离机制的来源；基础模型承接多种下游任务的讨论见 [Stanford CRFM](https://crfm.stanford.edu/report.html)。
+[^abstraction]: 相关研究见[抽象边界调研](../research/system-abstraction-boundary/report.md)；基础模型承接多种下游任务的讨论见 [Stanford CRFM](https://crfm.stanford.edu/report.html)。
 
 [^request]: 请求图是通用职责示意；调度与状态管理可参照 [vLLM Scheduler 文档](https://docs.vllm.ai/en/v0.26.0/api/vllm/v1/core/sched/scheduler/)。路由策略、tokenizer 位置以及 prefill／decode 是否分离属于部署选择。
 
--- before/manuscripts/02-模型架构.md
+++ after/manuscripts/02-模型架构.md
@@ -313,7 +313,7 @@
 
 共享 $K$、$V$ 改变了被查询的数据，32 个 $Q$ 头仍分别计算分数和加权输出。因此，KV 投影和保存量按组数减少，$QK^{\mathsf T}$ 与 $AV$ 交互仍按查询头数累计。一次读入的 $K$、$V$ 还可服务同组多个查询头，从而在实际计算中复用同一份数据。
 
-可以直接由组数预测资源变化。将 $n_{\mathrm{KV}}$ 减半，键值投影参数和上下文状态容量都减半；查询头数 $n_Q$ 不变，查询与上下文交互的运算量仍按原来的头数累计。减少存储与减少计算是两个不同结果，需要分别计算各组件的开销才能判断。
+可以直接由组数预测资源变化。将 $n_{\mathrm{KV}}$ 减半，键值投影参数和上下文状态容量都减半；查询头数 $n_Q$ 不变，查询与上下文交互的运算量仍按原来的头数累计。
 
 ![固定 Qwen 层数、头宽、8192 个上下文位置与 BF16，只改变 KV 组数。实际模型采用 GQA；其余两柱是用于分析共享比例的结构变体。](ch02/figure-2-4-cache.svg)
 
@@ -612,7 +612,7 @@
 
 DeepSeek V4-Flash 的 MTP 辅助块接收主干特征和 token ID，预测后续位置。对单个 token，即 $B=1,T=1$，一次辅助计算约需 1.80 GFLOPs，其中共享词表头约需 1.06 GFLOPs。共享词表头占这次计算的一半以上：虽然它不增加一份独立权重，每次调用仍要执行词表投影。[^source-5]
 
-辅助预测提醒我们，一次前向执行、一次采样和一个有效输出可能是不同的单位。第三章将把编码、生成、迭代及实时输出组织成完整负载；第八章再计算生成草稿与验证所需的时间，以及由此节省的生成时间。
+辅助预测使一次前向执行能够提供多个候选，经验证和采样后形成有效输出。第三章将把编码、生成、迭代及实时输出组织成完整负载；第八章再计算生成草稿与验证所需的时间，以及由此节省的生成时间。
 
 设一次普通 decode 耗时为 $T_d$，一轮推测解码的草稿和验证合计耗时为 $T_s+T_v$，平均得到 $a$ 个有效输出。每输出分摊时间为 $(T_s+T_v)/a$，有收益的条件是
 
@@ -1154,7 +1154,7 @@
 
 [^architecture-motivation]: [DeepSeek V4 技术报告](../references/text/deepseek-v4.txt)第 2.2 节说明 mHC 的多路表示与稳定传播，第 2.3 节说明 CSA／HCA；[Kimi K3 技术报告](../references/text/kimi-k3.txt)说明 Attention Residuals 沿深度选择信息的动机与块式实现。
 
-[^codesign]: [MQA 原文](../references/text/mqa.txt)摘要；[GQA 原文](../references/text/gqa.txt)摘要；[Llama 3 报告](../references/text/llama3.txt)第 6.1 节。容量示例是本书按固定服务状态推导的教学预算，不是这些模型的历史设计记录。
+[^codesign]: [MQA 原文](../references/text/mqa.txt)摘要；[GQA 原文](../references/text/gqa.txt)摘要；[Llama 3 报告](../references/text/llama3.txt)第 6.1 节。容量示例按题设的服务状态推导。
 
 [^core-calculation]: 本章条件比较的[逐项复算](../calculations/results/core-principles.json)与[计算程序](../calculations/core_principles.py)。
 
--- before/manuscripts/03-推理与训练负载.md
+++ after/manuscripts/03-推理与训练负载.md
@@ -237,7 +237,7 @@
 
 *图 3-13　两个生成分支指向同一份公共前缀，并各自保存新增尾部。箭头表示引用关系，共享前缀只计一份容量。*
 
-若分支还要分别生成文本，公共前缀可以共享，分支尾部独立增长。设共享前缀长 $H_0$，第 $i$ 个分支新增 $h_i$ 个位置，每位置状态为 $c_{\mathrm{KV}}$，则总状态为 $c_{\mathrm{KV}}(H_0+\sum_i h_i)$。并行一方面缩短关键路径，另一方面让更多分支的状态同时占用内存，需要同时检查时间和容量。
+若分支还要分别生成文本，公共前缀可以共享，分支尾部独立增长。设共享前缀长 $H_0$，第 $i$ 个分支新增 $h_i$ 个位置，每位置状态为 $c_{\mathrm{KV}}$，则总状态为 $c_{\mathrm{KV}}(H_0+\sum_i h_i)$。并行缩短了关键路径，也让更多分支的状态同时占用内存。
 
 前述代码修复任务的实测记录中，工具调用很短，大部分时间都花在模型计算上。若工具操作改为耗时 10 秒的编译，模型暂停的同时，下一轮所需上下文仍占据容量；若多个任务同时等待，驻留量按任务累加。工具执行时间越长，状态保留得越久，系统能同时处理的请求数也会受到影响，第八、九章将在这条时间线上安排缓存与交接。
 
--- before/manuscripts/05-算子与运行时.md
+++ after/manuscripts/05-算子与运行时.md
@@ -105,9 +105,9 @@
 
 *图 5-2：先算一个输出，再算相邻输出。蓝色表示所用的 A 行，橙色表示所用的 W 列，绿色表示本次得到的输出元素。图中矩阵缩小为示意尺寸，实际算例的 K 为 4096。*
 
-如果这行 A 在两次使用之间仍留在局部存储或缓存中，就能直接复用；若已被替换，就需要再次向下一层请求。转到下一行输出时，W 中的元素也会再次使用。同一份数据在数学运算中需要反复使用。程序怎样安排这些运算，决定了这份数据要经过存储接口多少次。
-
-本节以工作缓冲和下一层存储之间的接口为计数位置，累计程序每次读入与写出的字节数。后面约 3 GiB 的访问量，也按这一接口计算。[^tiles]
+如果这行 A 在两次使用之间仍留在局部存储或缓存中，就能直接复用；若已被替换，就需要再次向下一级存储请求数据。转到下一行输出时，W 中的元素也会再次使用。同一份数据在数学运算中需要反复使用。程序怎样安排这些运算，决定了这份数据要经过存储接口多少次。
+
+本节以工作缓冲和下一级存储之间的接口为计数位置，累计程序每次读入与写出的字节数。后面约 3 GiB 的访问量，也按这一接口计算。[^tiles]
 
 把图中的逐元素计算写成循环，就是：
 
@@ -136,7 +136,7 @@
 
 循环顺序还决定每次访问的地址。W 按行连续存储时，上面的内层循环依次读取 W[k,j]、W[k+1,j]，地址相差 N 个元素。若改成 i、k、j 的顺序，内层便依次读取 W[k,j]、W[k,j+1]，同时将 A[i,k] 用于多个输出。这样一次连续传输取得的数据能立即参与计算。
 
-不过，新顺序也要同时保存一段输出的累加结果。若这些结果放不进局部存储，每轮更新都可能增加向下一层的读写。接下来用分块限制同时保留的数据量，让输入复用与输出保存相互配合。
+不过，新顺序也要同时保存一段输出的累加结果。若这些结果放不进局部存储，每轮更新都可能增加对下一级存储的读写。接下来用分块限制同时保留的数据量，让输入复用与输出保存相互配合。
 
 ### 5.2.2 容量约束下的分块复用
 
@@ -172,7 +172,7 @@
 
 完整数值如下，算术强度用同一矩阵乘的 FLOPs 除以上述接口字节数计算：
 
-| 输出块 | 局部存储需求 | 工作缓冲与下一层之间的访问 | 算术强度 |
+| 输出块 | 局部存储需求 | 工作缓冲与下一级存储之间的访问 | 算术强度 |
 | --- | ---: | ---: | ---: |
 | 32×32 | 8 KiB | 6168 MiB | 15.9 FLOP/byte |
 | 64×64 | 24 KiB | 3096 MiB | 31.7 FLOP/byte |
@@ -184,7 +184,7 @@
 
 *图 5-5：条形总长均表示 96 KiB。24 KiB 的工作集能放四份，80 KiB 的只能放一份。*
 
-同时驻留的线程块可以交错发起访存：一些块等待数据时，另一些块继续工作。工作集从四份减为一份，可能降低利用其他计算掩盖访存延迟的能力。因此，大块虽然少读数据，实际获得的带宽却可能下降。
+同时驻留的线程块可以交错发起访存：一些块等待数据时，另一些块继续工作。工作集从四份减为一份，可能减少在等待访存期间执行其他计算的机会。因此，大块虽然少读数据，实际获得的带宽却可能下降。
 
 假设两种实现都受这一层的带宽限制，有效带宽分别为 $B_{64}$ 和 $B_{128}$，则：
 
@@ -228,13 +228,13 @@
 
 这里 SiLU 是激活函数，$\operatorname{SiLU}(x)=x/(1+e^{-x})$；符号 $\odot$ 表示对应位置的元素相乘。SiLU 和乘法都是逐元素运算。Z[i,j] 只依赖 G[i,j] 与 U[i,j]，不同位置之间没有归约。因此，读入同一位置的两个元素后，就能直接算出该位置的最终结果，而无需先生成全部 SiLU 结果。
 
-先比较中间结果 T 的去向。分开执行时，完整 T 要写到下一层存储，再被乘法读回；融合后，一小块 SiLU 结果可以直接交给同一 GPU 内核程序（kernel）中的乘法，用完就释放局部空间。
+先比较中间结果 T 的去向。分开执行时，完整 T 要写到下一级存储，再被乘法读回；融合后，一小块 SiLU 结果可以直接交给同一 GPU 内核程序（kernel）中的乘法，用完就释放局部空间。
 
 ![图 5-8 融合前后中间结果经过的路径](ch05/figure-5-fusion-path.svg)
 
-*图 5-8：上半图的完整 T 经过一次写出与一次读回；下半图中局部片段 t 直接传给乘法。两种方式仍读取 G、U 并写出 Z，图中省略这些共同的输入输出边。融合保持原来的中间舍入语义。*
-
-**例 5-4：激活算子融合如何减少中间张量读写？** 分开执行时，先计算 $T=\operatorname{SiLU}(G)$，读取 G、写出 T，共 48 MiB；再计算 $Z=T\odot U$，读取 T、U，写出 Z，共 72 MiB。总访问为 120 MiB。将两步融合，每次读取 G、U 的一片，在局部完成 SiLU 和乘法，写出 Z，只需 72 MiB。
+*图 5-8：上半图的完整 T 经过一次写出与一次读回；下半图中局部片段 t 直接传给乘法。两种方式仍读取 G、U 并写出 Z，图中省略这些共同的输入输出边。融合保持原来的中间结果的舍入规则。*
+
+**例 5-4：激活算子融合如何减少中间张量读写？** 分开执行时，先计算 $T=\operatorname{SiLU}(G)$，读取 G、写出 T，共 48 MiB；再计算 $Z=T\odot U$，读取 T、U，写出 Z，共 72 MiB。总读写量为 120 MiB。将两步融合，每次读取 G、U 的一片，在局部完成 SiLU 和乘法，写出 Z，只需 72 MiB。
 
 省下的 48 MiB 正是 T 的一次写回和一次读取。沿用上一节的带宽模型，取两种程序的有效带宽相同，时间比为 $72/120=0.6$，加速约 1.7 倍。实际激活链对照从约 14.2 μs 降到 7.7 μs，约快 1.8 倍。理论计算与实测结果都说明了同一个收益来源：省去完整 T 的写回和重读。[^exp2]
 
@@ -246,7 +246,7 @@
 
 中间张量的容量与访问量在这里具有不同含义。设每步先分配输出，完成后释放已用完的输入。独立执行 SiLU 时，G、U、T 同时存活，峰值为 72 MiB；三步融合时，G、U 与最终输出同时存活，峰值为 60 MiB。读写减少 96 MiB，峰值却只减少 12 MiB，因为同一块存储空间可以被反复读写。[^fusion]
 
-逐元素运算的每个输出只依赖对应位置的输入，因此可以在读入后立即计算，减少中间结果的保存。要让这种复用顺利发生，还得让前后两步使用相容的数据布局，并保证缓冲在最后一次读取结束前不会被覆盖。下面沿着一个数据块的使用过程，分析这两个条件。
+逐元素运算的每个输出只依赖对应位置的输入，因此可以在读入后立即计算，减少中间结果的保存。要让这种复用顺利发生，还得让前后两步使用彼此兼容的数据布局，并保证缓冲在最后一次读取结束前不会被覆盖。下面沿着一个数据块的使用过程，分析这两个条件。
 
 ### 5.3.2 布局转换、缓冲复用与双缓冲
 
@@ -330,7 +330,7 @@
 
 将 b 缩为 1，能放下 a=166，扫描次数降为 50，访问降至 $50\times4+4=204$ MiB。但每次扫描从 128 个 K/V 块变成 8192 个块，在线状态必须更频繁地更新。
 
-| K/V 块行数 b | 可容纳的 Q 行数 a | K/V 完整扫描次数 | 缓冲与下一层之间的访问 | Q 块与 K/V 块之间的状态更新次数 |
+| K/V 块行数 b | 可容纳的 Q 行数 a | K/V 完整扫描次数 | 缓冲与下一级存储之间的访问 | Q 块与 K/V 块之间的状态更新次数 |
 | --- | ---: | ---: | ---: | ---: |
 | 1 | 166 | 50 | 204 MiB | 409600 |
 | 64 | 110 | 75 | 304 MiB | 9600 |
@@ -338,7 +338,7 @@
 
 ![图 5-14 注意力块大小在访问量与更新次数之间的取舍](ch05/figure-5-8-attention-tradeoff.svg)
 
-*图 5-14：快速缓冲为 128 KiB，序列长 8192、头维度 128，无掩码；每个点对应正文表格的一种 K/V 块大小。横轴为缓冲与下一层之间的访问量，纵轴为在线更新次数，采用对数刻度。从 b=64 的点移到 b=1 的点，读取减少，更新次数却约增至 43 倍。*
+*图 5-14：快速缓冲为 128 KiB，序列长 8192、头维度 128，无掩码；每个点对应正文表格的一种 K/V 块大小。横轴为缓冲与下一级存储之间的访问量，纵轴为在线更新次数，采用对数刻度。从 b=64 的点移到 b=1 的点，读取减少，更新次数却约增至 43 倍。*
 
 图 5-14 把两项代价放在同一平面上，越靠左表示少读数据，越靠下表示少做更新。从 64 行块缩到 1 行块，少读 100 MiB，却把状态更新从 9600 次增至 409600 次，约为原来的 43 倍。每次更新都要执行循环控制、最大值比较、指数与输出缩放；每次只处理一行 K/V，也使矩阵乘的一个维度过小，难以充分利用矩阵计算单元。较小的 K/V 块能减少读取，却会增加更新次数；较大的块则有利于矩阵计算，并减少循环开销。因此，选择块大小时，需要同时比较这两类开销。
 
@@ -360,7 +360,7 @@
 
 ### 5.4.1 为什么要比较多种实现
 
-回顾逐元素链：SiLU 对每个数执行非线性变换，再与另一输入对应相乘，最后量化成低位宽表示。仍看这条 SiLU—乘法—量化链。两处相邻边界各有“保留”和“融合”两种选择，共有四种划分：三步独立、融合前两步、融合后两步、三步融合。如果一条链包含十个算子，就有 $2^9=512$ 种相邻边界划分。每一种划分还可以选择块大小、局部布局和流水深度。
+回顾逐元素链：SiLU 对每个数执行非线性变换，再与另一输入对应相乘，最后量化成低位宽表示。仍看这条 SiLU—乘法—量化链。两处相邻算子之间，都可以选择分开执行或融合执行，共有四种划分：三步独立、融合前两步、融合后两步、三步融合。如果一条链包含十个算子，就有 $2^9=512$ 种相邻边界划分。每一种划分还可以选择块大小、局部布局和流水深度。
 
 这些选择会相互影响。合并两步可以消除中间写回，也会增加同一时刻需要保存的值；临时数据增多，又会改变能够采用的块大小。因而，编译器要同时表达计算内容、执行顺序和数据保存位置。只记录一个算子名，无法推导这些代价。
 
@@ -403,7 +403,7 @@
         store_tile(Y, io, jo, silu(c))
 ```
 
-这里 `load_tile` 和 `matmul_accumulate` 分别表示成块读取和矩阵累加；尾块通过有效坐标掩码处理。伪代码的缩进清楚地标出了这些先后关系：输入块在 ko 循环内更新，累加器在整个 ko 循环期间持续保存，激活在 ko 循环结束后发生。
+这里 `load_tile` 和 `matmul_accumulate` 分别表示成块读取和矩阵累加；最后一个不足整块的数据块用掩码标出有效位置。伪代码的缩进清楚地标出了这些先后关系：输入块在 ko 循环内更新，累加器在整个 ko 循环期间持续保存，激活计算在 ko 循环结束后执行。
 
 ![图 5-15 循环层级决定临时数据的保存时间](ch05/figure-5-9-polyhedral.svg)
 
@@ -417,11 +417,11 @@
 
 ### 5.4.3 依赖与舍入怎样限制变换
 
-刚才把激活移入输出 tile，但仍安排在 ko 循环结束后执行，原因可以用两个数说明。设一个输出的两个部分和为 1 与 −1，完整和为 0，SiLU(0)=0。若在每个部分和之后先做 SiLU，再相加，结果为 $\operatorname{SiLU}(1)+\operatorname{SiLU}(-1)\approx0.4621$。移动激活越过归约边界，已经改变了运算。
+刚才把激活移入输出 tile，但仍安排在 ko 循环结束后执行，原因可以用两个数说明。设一个输出的两个部分和为 1 与 −1，完整和为 0，SiLU(0)=0。若在每个部分和之后先做 SiLU，再相加，结果为 $\operatorname{SiLU}(1)+\operatorname{SiLU}(-1)\approx0.4621$。把激活计算移到完整求和之前，已经改变了运算。
 
 ![图 5-16 激活与归约的顺序](ch05/figure-5-activation-order.svg)
 
-*图 5-16：同样两个部分和，先相加得到零，再做 SiLU 仍为零；先对各部分做 SiLU 再相加，得到约 0.4621。两个数说明变换越过归约后改变了函数。*
+*图 5-16：同样两个部分和，先相加得到零，再做 SiLU 仍为零；先对各部分做 SiLU 再相加，得到约 0.4621。两个数说明把激活计算移到求和之前会改变结果。*
 
 编译器需要遵守的依赖关系因此有两个层次：同一输出的部分和要按归约规则合并，后续算子要等所需的结果算好。不同输出可以并行计算；同一输出的归约和后续运算则必须按依赖顺序执行。
 
@@ -471,9 +471,9 @@
 
 前面的量化例子已经能够按读写量比较两种实现。面对更多分块与融合组合，编译器也按同样的思路逐步缩小范围：先检查依赖和数值规则，再排除局部存储不足以容纳的实现，最后估计执行开销，决定优先测试哪些实现。5.2 节已给出一种估计：用访问量和有效带宽比较两个 tile。编译器还可以加入矩阵指令利用率、寄存器需求和启动成本，确定各实现的测试顺序。
 
-程序实际能达到多高的运算速率，通常需要通过测量确定。同一转置程序的 RTX 对照中，16×16 块约需 7.9 μs，32×32 块约需 2.9 μs，扩大后快约 2.7 倍。块的每个维度扩大一倍，内部元素数扩大四倍，存储布局和线程协作也一起改变。读取生成代码，再结合访存与时间记录，就能从指令、访存和同步解释不同实现的时间差异。[^exp5]
-
-TVM 的自动调优根据硬件实测结果搜索分块、循环顺序等调度方案；让能够调用编译和测试工具的智能体（Agent）修改调度或内核代码，还可以尝试原有调度模板之外的实现。无论采用哪种方法，都需要生成代码、编译、检查数值结果并测量时间。源码中分开表达的算子，还可能被编译器融合为一个内核，因此应比较编译后实际执行的程序。[^exp4] 搜索方法还要解决另一个问题：用什么负载评价这些实现。
+同一转置程序的 RTX 对照中，16×16 块约需 7.9 μs，32×32 块约需 2.9 μs，扩大后快约 2.7 倍。块的每个维度扩大一倍，内部元素数扩大四倍，存储布局和线程协作也一起改变。[^exp5]
+
+TVM 的自动调优根据硬件实测结果搜索分块、循环顺序等调度方案；让能够调用编译和测试工具的智能体（Agent）修改调度或内核代码，还可以尝试原有调度模板之外的实现。两种搜索都以编译后的程序为评价对象，通过数值检查筛选正确实现，再以执行时间评价调度。编译器可以将源码中分开表达的算子融合为一个内核。[^exp4] 搜索方法还要解决另一个问题：用什么负载评价这些实现。
 
 **例 5-9：输入形状的调用比例如何改变算子实现的选择？** 某个算子有 A、B 两种输入形状，原实现处理它们都需要 10 μs。新实现处理 A 需要 5 μs，处理 B 需要 20 μs：A 的耗时减半，B 的耗时翻倍。若两种形状各出现一次，总时间从 20 μs 增至 25 μs，新实现慢 25%；若 A 调用 100 次、B 调用一次，总时间从 1010 μs 降至 520 μs，新实现约快 1.9 倍。
 
@@ -647,7 +647,7 @@
 
 上下文增长后，仍需执行的索引扫描会产生更大的开销。128K 时，两代模型的全局历史状态与索引的逻辑读取量分别约为 65.085 与 33.422 MiB。V4.1 后续解码索引器只访问候选池，初始索引仍需遍历全局。因此，即使后续层索引的访问范围固定，整个 decode 的开销仍会随上下文长度增长。
 
-除了数据从哪里取得，程序是否真正省去了计算也会改变开销。候选池通过缩小后续索引的搜索范围来节省工作。公开参考实现先对完整索引缓存执行点积，再屏蔽候选池之外的位置；论文生产路径直接减少后续索引扫描的条目。前者已经完成了池外点积，后者在发起计算时就省去这部分工作。同一选择规则由此对应两种执行成本。
+除了数据从哪里取得，程序是否真正省去了计算也会改变开销。候选池通过缩小后续索引的搜索范围来节省工作。公开参考实现先对完整索引缓存执行点积，再屏蔽候选池之外的位置；论文中的生产实现直接减少后续索引扫描的条目。前者已经完成了池外点积，后者在发起计算时就省去这部分工作。同一选择规则由此对应两种执行成本。
 
 ![图 5-30 全局驻留与逐层逻辑读取分开比较。上方仅计全局 KV 容量，下方读取包含全局条目、局部窗口和索引；两栏各自采用相同横轴尺度。数值按生产布局计算。](ch05/figure-5-v41-traffic.svg)
 
--- before/manuscripts/06-超节点.md
+++ after/manuscripts/06-超节点.md
@@ -184,7 +184,7 @@
 
 TP 将一层拆开，**流水线并行**（pipeline parallelism，PP）则将层按顺序分给不同设备。前一阶段完成若干层，传递激活，后一阶段继续计算。各阶段保存所负责模型层的权重，阶段之间只传递中间结果。
 
-**微批次（microbatch）**是送入流水线的一小组输入；一个批次可以拆成多个微批次，使不同阶段同时处理不同组输入。设模型分成 $q$ 个阶段，每阶段包含激活传输的执行时间为 $t$，输入由 $b$ 个已经就绪的独立微批次组成。先看四阶段、每阶段 1 ms 的情形。微批次 0 在 0 ms 进入阶段 0，经过四阶段，在 4 ms 完成。阶段 0 在 1 ms 已能接收微批次 1，后者在 5 ms 完成。其余微批次依次跟进，完成间隔为 1 ms。
+**微批次（microbatch）**是送入流水线的一小组输入；一个批次可以拆成多个微批次，使不同阶段同时处理不同组输入。设模型分成 $q$ 个阶段，每阶段的执行时间（含激活传输）为 $t$，输入由 $b$ 个已经就绪的独立微批次组成。先看四阶段、每阶段 1 ms 的情形。微批次 0 在 0 ms 进入阶段 0，经过四阶段，在 4 ms 完成。阶段 0 在 1 ms 已能接收微批次 1，后者在 5 ms 完成。其余微批次依次跟进，完成间隔为 1 ms。
 
 ![四阶段流水的填充与排空](ch06/figure-6-4-pipeline.svg)
 
@@ -347,7 +347,7 @@
 
 操作名称规定了计算方式和结果在各卡上的分布，算法则规定消息怎样分轮传递。一个 AllReduce 可以先 ReduceScatter，让各卡各自获得一块完整归约结果，再 AllGather，交换这些已归约的数据块。
 
-用四元素向量说明两个阶段各自的作用。各卡开始时都有四个元素的贡献；第一阶段求和后，卡 0 留元素 0，卡 1 留元素 1，依次类推。第二阶段交换这四个已求和的元素。第一阶段中，每卡的输入都是完整形状的部分和，第二阶段的输入是已经归约好的四分之一结果。这正对应“先对每块求和，再分发求和结果”。
+用四元素向量说明两个阶段各自的作用。各卡开始时都有四个元素的贡献；第一阶段求和后，卡 0 留元素 0，卡 1 留元素 1，依次类推。第二阶段交换这四个已求和的元素。第一阶段中，每卡的输入都是形状与完整输出相同的部分和，第二阶段的输入是已经归约好的四分之一结果。这正对应“先对每块求和，再分发求和结果”。
 
 各卡开始通信的时刻也会影响等待时间。设四卡分别在 0、0、0、2 ms 发起一次集合通信，所有卡都发起通信后，再用 0.4 ms 完成交换。前三卡等待 2.4 ms，最后一卡等待 0.4 ms；把交换加快一倍只省 0.2 ms，消除最后一卡的计算延误却省 2 ms。前三张卡的调用时间较长，是因为它们还要等待最后一张卡完成前面的计算。[^collective-path]
 
@@ -514,7 +514,7 @@
 
 *图 6-23：把所有发送者的消息累加到各条有向链路上，取每轮最大值。递归前三轮均为 4 MiB，Swing 为 4、2、2 MiB。*
 
-主机中转是同一问题的另一种形式。非统一内存访问（NUMA）结构把主机内存分到不同 CPU 附近，跨 CPU 读取要经过处理器间接口。G0、G1 接 NUMA 节点 A，G2、G3 接 B，四卡 ring 共发送 48 MiB。把全部中转缓冲放在 A，CPU 间每个方向承担 24 MiB；把缓冲移到发送卡附近，并让同 NUMA 的卡在环中相邻，每方向降到 12 MiB。若再将环改成交替跨 NUMA，载荷又回到 24 MiB。若 CPU 间链路的单向带宽为 8 GB/s，三种方案的传输时间至少分别为约 3.15、1.57、3.15 ms。缓冲位置和卡的通信次序共同决定搬运路径。[^numa]
+主机中转是同一问题的另一种形式。非统一内存访问（NUMA）结构把主机内存分到不同 CPU 附近，跨 CPU 读取要经过处理器间接口。G0、G1 接 NUMA 节点 A，G2、G3 接 B，四卡 ring 共发送 48 MiB。把全部中转缓冲放在 A，CPU 间每个方向承担 24 MiB；把缓冲移到发送卡附近，并让同 NUMA 的卡在环中相邻，每方向降到 12 MiB。若再将环改成交替跨 NUMA，每个方向的传输量又回到 24 MiB。若 CPU 间链路的单向带宽为 8 GB/s，三种方案的传输时间至少分别为约 3.15、1.57、3.15 ms。缓冲位置和卡的通信次序共同决定搬运路径。[^numa]
 
 **设计案例：从单跳延迟估算全局同步时间。** 作者参与 OpenTallas 设计时，需要把局部数据交换扩展为所有设备参与的数据交换。用 60 层、每层两次同步、100 μs token 目标的教学例说明：若通信占 30%，120 次同步每次只有 0.25 μs。相邻设备间传输一跳需要 100 ns，看起来足够；通信关键路径若经过 15 跳，仅传播就需 1.5 μs，120 次累计 180 μs。从单跳传输扩大到所有设备间的同步后，传播时间的累积就可能超过单个 token 的时间预算。减少通信跳数、降低同步次数或调整模型切分，都可以缩短这部分等待。[^opentallas]
 
@@ -558,7 +558,7 @@
 
 端口、距离和功率共同限制了系统规模。NVIDIA 的多代系统展示了这些条件下，互联如何从机箱内直连发展到机柜级交换网络。DGX-1 用八张 GPU 组成 NVLink 混合立方体网格，部分 GPU 直接相连，其他 GPU 之间的通信需要中转。此时将频繁通信的计算任务分配到相邻 GPU，就能减少中转与共享链路压力。
 
-NVLink 是 NVIDIA 的设备互联，NVSwitch 是组织 NVLink 交换网络的交换芯片。DGX-2 使用 16 张 V100 和 NVSwitch。交换结构把“某对 GPU 是否具有直连”转化为“交换层能同时服务多少流量”，扩大了可用协作范围。DGX A100 又以八卡服务器为基本单元，配备 NVLink／NVSwitch。这些系统采用不同的 GPU 数量与互联结构，以适应芯片接口、机箱空间和应用需求。[^nvidia]
+NVLink 是 NVIDIA 的设备互联，NVSwitch 是组织 NVLink 交换网络的交换芯片。DGX-2 使用 16 张 V100 和 NVSwitch。交换结构把“某对 GPU 是否具有直连”转化为“交换层能同时转发多少数据”，扩大了可用协作范围。DGX A100 又以八卡服务器为基本单元，配备 NVLink／NVSwitch。这些系统采用不同的 GPU 数量与互联结构，以适应芯片接口、机箱空间和应用需求。[^nvidia]
 
 GB200 NVL72 将 72 张 GPU 和 36 个 Grace CPU 放进机柜级 NVLink 系统。机柜内的更多 GPU 因而能够通过 NVLink 频繁交换数据。以第 6.5.1 节的八卡任务为例：在两台服务器之间，每轮启动耗时增加，使单步从约 4.28 ms 增到 6.32 ms；若所需八卡能放在同一个满足前述低延迟条件的互联域内，便可省去这部分跨服务器通信开销。更大的互联域也可以容纳多个两卡或四卡实例，各自完成自己的请求。[^nvl]
 
@@ -683,7 +683,7 @@
 
 ### 6.7.1 更大的协作组与更多推理实例
 
-至此，已经知道数据能否存下、每步如何执行，以及连接和内存位置怎样改变执行时间。下面利用这些结果，为章首给定的八张卡安排多个会话。四个会话都已完成 prefill，各自保存了 8192 个上下文位置的 KV。每个会话还有一个已经生成、尚未写入 KV 的 token，用作下一步输入。现在，四个会话同时请求再生成八个 token。一个实例一次服务一个会话，完成这八步后再接下一个会话；请求按实例轮转分配。采用 6.4 的低延迟八卡连接，全部设备持续占用到四个会话都完成。
+至此，已经知道数据能否存下、每步如何执行，以及连接和内存位置怎样改变执行时间。下面利用这些结果，为章首给定的八张卡安排多个会话。四个会话都已完成 prefill，各自保存了 8192 个上下文位置的 KV。每个会话还有一个已经生成、尚未写入 KV 的 token，用作下一步输入。现在，四个会话同时请求再生成八个 token。一个实例一次服务一个会话，完成这八步后再接下一个会话；请求按实例轮转分配。采用 6.4 的低延迟八卡连接，全部设备一直分配给这组任务，直到四个会话都完成。
 
 每步时间来自式（6-5）。续写时，式（6-4）的上下文长度依次为 8192 到 8199，KV 访问量逐步增加。将八步相加，就得到会话的服务时间。等待中的会话只保存各自的 KV，同一实例中的会话共享模型权重；四种部署方式的每卡内存需求均不超过 24 GB。
 
@@ -712,7 +712,7 @@
 
 八卡协作使单个会话的续写时间从约 132 ms 缩短到 34 ms，但四个会话排成一队，所有会话约 137 ms 才完成。采用四个两卡实例时，单个会话的续写时间较长，却能同时推进四个会话，约 69 ms 全部完成。八个单卡实例中，只有四个分配到了会话，多出来的空闲实例无法加速正在执行的请求。
 
-这里同时使用了模型内部与请求之间的并行关系。层内矩阵可以切分给多卡，独立会话可以分配给多个实例；两种并行共同竞争这八张卡。掌握这两层关系，才能根据请求的完成期限选择协作组大小。服务调度给出同时推进的会话数，模型切分给出每步通信量，互联拓扑给出交接时间，三者共同决定表中的完成时刻。
+这里同时使用了模型内部与请求之间的并行关系。层内矩阵可以切分给多卡，独立会话可以分配给多个实例；两种并行共同竞争这八张卡。掌握这两层关系，才能根据请求的完成期限选择协作组大小。服务调度给出同时推进的会话数，模型切分给出每步通信量，互联拓扑决定数据交换与同步的耗时，三者共同决定表中的完成时刻。
 
 图中色块的横向长度表示单个会话的执行时间，纵向轨道数表示多少会话可以同时推进。完成期限决定了这两者怎样取舍。只有一个会话、要求 40 ms 完成，八卡实例满足目标；四个会话、要求 90 ms 全部完成，两卡和四卡部署方式都满足，两卡更早完成。较短的单会话完成期限需要更大协作组，足够多的独立会话则可以利用更多实例。
 
@@ -788,7 +788,7 @@
 
 [^variants]: [MeshSlice 的矩阵形状与流水推算](../case-studies/mesh-shape-and-slicing.md)、[Shift Parallelism 的状态与额外驻留](../case-studies/parallel-switching-and-state.md)。MeshSlice 的部分重叠为预测，Arctic 的 SP 定义及动态切换边界按固定实现说明。
 
-[^pipeline]: [有限槽位与背压计算](../calculations/results/pipeline-finite-1-slots.md)、[基础 TP／PP 通信路径](../calculations/results/dense-comm-qwen8-tp8-pp1-t1.md)。声明的时序、缓冲和线格式决定所计范围。
+[^pipeline]: [有限槽位与背压计算](../calculations/results/pipeline-finite-1-slots.md)、[基础 TP／PP 通信路径](../calculations/results/dense-comm-qwen8-tp8-pp1-t1.md)。声明的执行时序、缓冲安排和线上传输格式决定所计范围。
 
 [^ownership]: [Qwen235 所有权连接练习](../research/2026-infra-survey/parallel-moe-ownership/NOTES.md)及[独立核对结果](../research/2026-infra-survey/parallel-moe-ownership/results.json)。本例采用给定的 TP／EP 教学布局。
 
@@ -810,7 +810,7 @@
 
 [^numa]: [PCIe 中转与 NUMA 放置](../case-studies/pcie-staging-and-numa.md)、[发送端附近缓冲的计算](../calculations/results/staging-local-grouped.md)。TCCL 采用作者材料与源码。
 
-[^physical]: [端口与互联介质审查](../research/draft11-outline-audit/report.md#radix)明确 radix、整套 fabric、CPO 和功率的推导边界。本章端口与功率例题为独立教学输入。
+[^physical]: [端口数量与互联介质分析](../research/draft11-outline-audit/report.md#radix)。端口与功率算例采用题设输入。
 
 [^nvidia]: [V100 架构白皮书](../references/files/specs/nvidia-v100.pdf)，NVLink 拓扑与 DGX-1 附录；[A100 架构白皮书](../references/files/specs/nvidia-a100.pdf)，NVLink／NVSwitch 与 DGX A100 附录；DGX-2 的 16 卡历史配置亦见[ZeRO 论文](../references/files/papers/zero.pdf)平台说明。
 
--- before/manuscripts/07-数据中心网络.md
+++ after/manuscripts/07-数据中心网络.md
@@ -709,7 +709,7 @@
 
 计算可用带宽时，需要先明确物理端口的连接方式。例如昇腾 950 的以太网上的 UB 通信（UBoE）与 UB Link 链路静态复用串行器／解串器（SerDes，连接芯片内部并行数据与链路串行信号的电路），一组 SerDes 的用途由端口配置确定。选择端口用途之后，才能计算该方向实际拥有的链路容量。[^ubspec] 接口统一使程序使用共同的访问方式，端口配置则决定数据经过哪些链路，以及这些链路有多少带宽。
 
-公开 AllReduce 运行记录可以用来分析这些因素的共同影响。16 GiB 输入、输入与输出分别使用不同缓冲区时，两台八卡 H100 服务器约需 68.7 ms，四台约需 91.0 ms。[^allreduce] 增加服务器带来了更多参与者和更长的协作路径。沿本章的方法，应先按拓扑计算各轮在每条链路上的传输量，再检查请求提交和完成通知的处理速度，最后定位最晚完成的参与者。
+公开 AllReduce 运行记录可以用来分析这些因素的共同影响。16 GiB 输入、输入与输出分别使用不同缓冲区时，两台八卡 H100 服务器约需 68.7 ms，四台约需 91.0 ms。[^allreduce] 增加服务器带来了更多参与者和更长的协作路径。
 
 本章的推导也解释了四种常见误区。
 
@@ -729,13 +729,13 @@
 
 [^capacity]: 模型规模与状态大小的计算方法见[模型资源计算](../case-studies/model-resource-accounting.md)及[第七章扩写资料](../outlines/extensions/07-数据中心网络.md#detail-7.1)。16 bytes／参数是本章声明的混合精度 Adam 教学配置；万亿参数推理例子按每参数 0.5 byte 计算权重容量。
 
-[^ub]: 李博杰，[《Unified Bus 背后的思考》](../references/files/documents/ub-reflection.md)，2025-09-28。笔者在文中讨论早期设计的动机与不同方案；协议版本与产品实现见[UB 规范核对记录](../references/UB-ASCEND-NOTES.md)。KV-Direct、1Pipe 与 URPC 的经历不用于推断未记录的产品实现。
+[^ub]: 李博杰，[《Unified Bus 背后的思考》](../references/files/documents/ub-reflection.md)，2025-09-28。笔者在文中讨论早期设计的动机与不同方案；协议版本与产品实现见[UB 规范核对记录](../references/UB-ASCEND-NOTES.md)。
 
 [^gradient]: [两级梯度归约计算](../calculations/results/gradient-fp32-hierarchical-nic2.md)、[连续平坦环](../calculations/results/gradient-fp32-flat-contiguous-nic2.md)、[交错平坦环](../calculations/results/gradient-fp32-flat-interleaved-nic2.md)。固定模型形状，带宽与轮间屏障为教学输入。计算程序检查每个参与者的输入是否恰好参与一次求和；浮点加法顺序变化可能改变舍入结果。正文时间按本地 200 GB/s、每方向共享出口 40 GB/s、两 NIC 各 25 GB/s、每轮启动 2 μs 计算；各轮有屏障，未计归约、传播与其他任务干扰。
 
 [^parallel]: [模型并行案例](../case-studies/model-parallelism.md)、[第六章正文](06-超节点.md)。本章 EP 算例使用 1024 个 token、每 token 八次分派、每份 8 KiB、跨边界比例二分之一，路由为教学构造。
 
-[^fuselink]: Ren 等，*Enabling Efficient GPU Communication over Multiple NICs with FuseLink*，OSDI 2025；[正式会议稿](../references/proceedings/OSDI/2025/selected/osdi25-ren.pdf)、[已有阅读与路径推算](../case-studies/resource-sharing-and-placement.md)。本文中继路径的 NIC 40 GB/s、本地互联剩余 60 GB/s 为教学条件，插件实现与实验平台见原论文。
+[^fuselink]: Ren 等，*Enabling Efficient GPU Communication over Multiple NICs with FuseLink*，OSDI 2025；[正式会议稿](../references/proceedings/OSDI/2025/selected/osdi25-ren.pdf)、[通信路径推算](../case-studies/resource-sharing-and-placement.md)。本文中继路径的 NIC 40 GB/s、本地互联剩余 60 GB/s 为教学条件，插件实现与实验平台见原论文。
 
 [^allreduce]: [实验 7-3 公开运行记录核验](../experiments/ch07/07-03/README.md)。数据来自官方项目中的用户提交记录。
 
@@ -753,23 +753,23 @@
 
 [^reclaim]: [完成通知处理与槽位释放](../calculations/results/completion-reclaim-book.md)、[增加槽位](../calculations/results/completion-reclaim-more-slots.md)。轮询周期、每次轮询处理的完成项数量与槽位寿命为教学条件。
 
-[^queue]: [周期需求与队列](../calculations/results/periodic-queue-aligned.md)、[错峰](../calculations/results/periodic-queue-staggered.md)、[相位漂移](../calculations/results/periodic-queue-drift.md)。模型未模拟运行时拥塞控制改变预设的到达速率。
-
-[^cassini]: Rajasekaran 等，*CASSINI: Network-Aware Job Scheduling in Machine Learning Clusters*，NSDI 2024；[已有论文阅读与适用条件](../case-studies/network-planning-and-collectives.md)。本文保留其周期、作业放置与相位校正的设计条件。论文主实验为 24 台单 A100 40 GB 服务器、50 Gbps 网卡及 2:1 超售逻辑网络；各作业独占训练设备、共享网络。
+[^queue]: [周期需求与队列](../calculations/results/periodic-queue-aligned.md)、[错峰](../calculations/results/periodic-queue-staggered.md)、[相位漂移](../calculations/results/periodic-queue-drift.md)。算例的到达速率按给定周期变化。
+
+[^cassini]: Rajasekaran 等，*CASSINI: Network-Aware Job Scheduling in Machine Learning Clusters*，NSDI 2024；[作业放置与相位调度](../case-studies/network-planning-and-collectives.md)。论文主实验为 24 台单 A100 40 GB 服务器、50 Gbps 网卡及 2:1 超售逻辑网络；各作业独占训练设备、共享网络。
 
 [^phase]: [实验 7-8：真实 CPU 训练与一次性错峰](../experiments/ch07/07-08/README.md)。三轮结果计入初始 50 ms 延迟，模型与优化器状态核验一致；这一对照用于分析 CPU 执行顺序。环境为共享 CPU、Gloo loopback，三轮完成时间增加 2.27%–3.32%。
 
-[^feedback]: [有限缓冲与反馈延迟](../calculations/results/feedback-queue-overflow.md)。20 μs 的反馈时刻和反馈后速率是显式输入，不是 DCQCN 或 C-AQM 控制器输出。
-
-[^packets]: [两路径均延迟](../calculations/results/packet-reorder-balanced.md)、[路径偏斜](../calculations/results/packet-reorder-skewed.md)、[指定丢包恢复](../calculations/results/packet-reorder-loss.md)。数据只含载荷，不含协议头、ACK 或编码。固定载荷、轮转分配与指定恢复时刻，不模拟 ACK、超时估计或虚假重传；8 KiB 后缀重发仅为字节对照，不是 Go-Back-N 时序结果。
-
-[^megascale]: Jiang 等，*MegaScale: Scaling Large Language Model Training to More Than 10,000 GPUs*，NSDI 2024；[正式论文](../references/proceedings/NSDI/2024/selected/nsdi24-jiang-ziheng.pdf)、[已读诊断与历史条件](../case-studies/collective-paths-and-diagnosis.md)。本文采用等待来源分析，没有将系统全部收益归因于网络。
-
-[^tail]: [就绪偏差教学记录](../calculations/results/collective-tail-book.md)、[100 条混合记录](../calculations/results/collective-tail-mixed.md)、[两条恢复记录](../calculations/results/collective-tail-fault-heavy.md)。p99 取排序后第 $\lceil0.99N\rceil$ 项，不对稀疏教学样本作总体 SLO 推断。
+[^feedback]: [有限缓冲与反馈延迟](../calculations/results/feedback-queue-overflow.md)。反馈时刻设为 20 μs，反馈后的发送速率按题设调整。
+
+[^packets]: [两路径均延迟](../calculations/results/packet-reorder-balanced.md)、[路径偏斜](../calculations/results/packet-reorder-skewed.md)、[指定丢包恢复](../calculations/results/packet-reorder-loss.md)。数据只含载荷，不含协议头、ACK 或编码。报文采用固定载荷与轮转分配，恢复时刻由题设给定；8 KiB 后缀重发用于计算额外载荷。
+
+[^megascale]: Jiang 等，*MegaScale: Scaling Large Language Model Training to More Than 10,000 GPUs*，NSDI 2024；[正式论文](../references/proceedings/NSDI/2024/selected/nsdi24-jiang-ziheng.pdf)、[集合通信诊断](../case-studies/collective-paths-and-diagnosis.md)。
+
+[^tail]: [就绪偏差教学记录](../calculations/results/collective-tail-book.md)、[100 条混合记录](../calculations/results/collective-tail-mixed.md)、[两条恢复记录](../calculations/results/collective-tail-fault-heavy.md)。p99 取排序后第 $\lceil0.99N\rceil$ 项。
 
 [^readiness]: [实验 7-10：四进程 Gloo 就绪偏差](../experiments/ch07/07-10/rank-readiness/README.md)。Apple M2 Max、本机 CPU，180 组正式记录；全组完成从最早屏障返回到最晚调用返回，最后到达后尾段仍包含归约、调度与唤醒。正文采用不同样本统计的中位数，逐样本时间线见原记录。
 
-[^planning]: [网络规划与集合通信案例](../case-studies/network-planning-and-collectives.md)、[已有独立算术](../research/2026-infra-survey/torus-allocation-arithmetic.json)。小消息算例设每轮启动 5 μs、每个参与者有效带宽 25／75 GB/s；前述大块归约用每轮 2 μs。环形模型计载荷传输与启动。
+[^planning]: [网络规划与集合通信案例](../case-studies/network-planning-and-collectives.md)、[网络规划计算](../research/2026-infra-survey/torus-allocation-arithmetic.json)。小消息算例设每轮启动 5 μs、每个参与者有效带宽 25／75 GB/s；前述大块归约用每轮 2 μs。环形模型计载荷传输与启动。
 
 [^ubspec]: [UB 与昇腾 950 原件核对](../references/UB-ASCEND-NOTES.md)，包含 UB 基础规范 2.0.1、操作系统参考设计 2.0 及[昇腾 950 官方白皮书](../references/files/specs/ascend-950-official.pdf)。域、传输模式、双向带宽与 SerDes 复用分别核算。
 
--- before/manuscripts/08-单实例推理.md
+++ after/manuscripts/08-单实例推理.md
@@ -684,33 +684,33 @@
 
 推测采样的接受与修正规则奠定了保持目标分布的基础，后续上下文查询、特征草稿和并行草稿主要改变每轮成本及接受长度。完整 Agent 任务还包含工具和外部阶段，公开速度与任务记录可结合第 8.6 节的阶段分解阅读。[^spec][^task]
 
-[^batch]: 固定模型的[批处理计算](../calculations/results/batch-reuse-h100-2k.md)及[8K 对照](../calculations/results/batch-reuse-h100-8k.md)。完整 BF16 权重为 16,381,470,720 bytes，一步理想共享矩阵权重读取为 15,136,811,008 bytes；embedding 按请求查行，驻留仍包含完整 embedding。读取表先用精确字节计算，再舍入各项。逻辑权重共享、KV 读写、设备峰值和未计工作区分别注明。
-
-[^batch-measure]: [批量扫描原始记录](../experiments/ch08/08-01/README.md)。RTX PRO 6000、vLLM 0.23.0、BF16，三轮；正文报告中位数，吞吐覆盖剩余 prefill 和生成，排除加载与预热；8K 条件先建立共享前缀，不能当成互不共享的 8K 容量测量。
+[^batch]: 固定模型的[批处理计算](../calculations/results/batch-reuse-h100-2k.md)及[8K 对照](../calculations/results/batch-reuse-h100-8k.md)。完整 BF16 权重为 16,381,470,720 bytes，一步理想共享矩阵权重读取为 15,136,811,008 bytes；embedding 按请求查行，驻留仍包含完整 embedding。读取表先用精确字节计算，再舍入各项。
+
+[^batch-measure]: [批量扫描原始记录](../experiments/ch08/08-01/README.md)。RTX PRO 6000、vLLM 0.23.0、BF16，三轮；正文报告中位数，吞吐覆盖剩余 prefill 和生成，排除加载与预热；8K 条件先建立共享前缀。
 
 [^schedule-survey]: [Orca 原文](../references/files/papers/orca.pdf)与[分块计算和版本说明](../case-studies/chunking-and-state-transfer.md)。
 
 [^schedule]: [固定批次](../calculations/results/iteration-batching-fixed.md)、[连续批处理](../calculations/results/iteration-batching-continuous.md)、[分块策略](../calculations/results/iteration-batching-chunked.md)，显式教学成本，最终输出的 pending 状态与容量口径见各文件。
 
-[^chunk]: [逐块计算和封存测量](../calculations/results/chunk-history-book.md)。11 次请求、每次 16 块，首末块时间中位数 25.30、35.07 ms；CUDA event 区间包括完整模型路径和可能的主机间隙，不含外部 logits／采样。位置、内容、末块处理和顺序同时变化，未独立识别上下文长度的因果影响。
-
-[^nanoflow]: [OSDI 2025 调研](../research/2026-infra-survey/reading-osdi-2025.md)、[NanoFlow 正式稿](../references/proceedings/OSDI/2025/selected/osdi25-zhu-kan.pdf)与[资源共享算例](../case-studies/resource-sharing-and-placement.md)。采用切分新增读取与并发争用，论文硬件和吞吐条件单列。
-
-[^replay]: [六请求实测](../experiments/ch08/08-02/README.md)及[图执行取舍](../case-studies/graph-execution-tradeoffs.md)。固定顺序、共享 GPU、合成文本，无饱和服务率或任务质量结论。
+[^chunk]: [逐块计算和封存测量](../calculations/results/chunk-history-book.md)。11 次请求、每次 16 块，首末块时间中位数 25.30、35.07 ms；CUDA event 区间包括完整模型路径和可能的主机间隙，不含外部 logits／采样。各块按原始顺序执行，输入位置与内容逐块变化。
+
+[^nanoflow]: [OSDI 2025 调研](../research/2026-infra-survey/reading-osdi-2025.md)、[NanoFlow 正式稿](../references/proceedings/OSDI/2025/selected/osdi25-zhu-kan.pdf)与[资源共享算例](../case-studies/resource-sharing-and-placement.md)。
+
+[^replay]: [六请求实测](../experiments/ch08/08-02/README.md)及[图执行取舍](../case-studies/graph-execution-tradeoffs.md)。实验在共享 GPU 上按固定顺序执行六条合成文本请求。
 
 [^pages]: [分页、取消与抢占](../experiments/ch08/08-03/README.md)、[四分支共享](../experiments/ch08/08-03/branch-sharing/README.md)、[冷分支对照](../experiments/ch08/08-03/cold-branches/README.md)。共享分支的输出相同；冷、热分支具有相同的块峰值，但 prefill 量不同。
 
-[^lora]: [MLSys 2024 调研](../research/2026-infra-survey/reading-mlsys-2024.md)与[多 LoRA 计算](../case-studies/multi-lora-serving.md)。Punica、S-LoRA 的历史模型、adapter 与基线条件不替代当前框架实测。
-
-[^context]: [作者上下文工程材料与来源范围](../case-studies/author-context-and-design.md)。稳定前缀、替换与追加、摘要调用分别计量；普通 KV 不按任意缓存拼接解释。
-
-[^hybrid]: [混合状态恢复与检查点计算](../case-studies/hybrid-prefix-state.md)、[MLSys 2025 调研](../research/2026-infra-survey/reading-mlsys-2025.md)。Marconi 的历史机制与后续框架支持分开；Kimi K3 的容量按指定存储格式和并行配置计算。
-
-[^prefix]: [12 轮实际输入的缓存回放](../experiments/ch08/08-04/README.md)，每轮仅生成一个 token，源任务失败记录保留，非完整 Agent 任务时间测量；请求串行，没有逐块淘汰轨迹，不能仅由命中数还原状态寿命。
+[^lora]: [MLSys 2024 调研](../research/2026-infra-survey/reading-mlsys-2024.md)与[多 LoRA 计算](../case-studies/multi-lora-serving.md)。
+
+[^context]: [上下文工程案例](../case-studies/author-context-and-design.md)。
+
+[^hybrid]: [混合状态恢复与检查点计算](../case-studies/hybrid-prefix-state.md)、[MLSys 2025 调研](../research/2026-infra-survey/reading-mlsys-2025.md)。Kimi K3 的容量按指定存储格式和并行配置计算。
+
+[^prefix]: [12 轮实际输入的缓存回放](../experiments/ch08/08-04/README.md)。回放采用原任务的输入序列，串行执行请求，每轮生成一个 token。
 
 [^files]: [固定模型文件与业务预算](../case-studies/inference-training-scenarios.md)及[235B 分片元数据](../references/outline-checks/2026-09-07/systems-cases/qwen235-gguf-metadata.json)。精确字节、GB 与 GiB 分别报告。
 
-[^kv-format]: [KV 格式和执行成本](../case-studies/kv-quantization-and-execution.md)。GGML 分组类型、FP8 scale 粒度与历史 H100 拟合条件均有固定来源；未将历史交点作为一般设备阈值。
+[^kv-format]: [KV 格式和执行成本](../case-studies/kv-quantization-and-execution.md)。资料包含 GGML 分组格式、FP8 缩放粒度及 H100 上的拟合结果。
 
 [^offload]: [权重卸载与预取计算](../case-studies/weight-offload-execution.md)。24／384 GiB/s 均为教学有效单向带宽；多缓冲、CPU 就地计算和映射读取分开计量。
 
@@ -718,19 +718,19 @@
 
 [^mac]: [M2 Max 小模型容量记录](../experiments/ch08/08-07/README.md)及[32K 补测](../experiments/ch08/08-07/context32k/README.md)。各槽依次执行 prefill。
 
-[^quality]: [KV 与 Q 精度对照](../experiments/ch08/08-08/README.md)、[固定 FP8 权重的 Q 控制](../experiments/ch08/08-08/quantized-q-control/README.md)、[并发容量记录](../experiments/ch08/08-08/concurrency/README.md)。归档 ID 8-8 对应当前练习 8-6；正文采用 BF16 权重组。另一组固定 FP8 权重时，BF16 Q 与默认路径分别为 32/32、28/32，未与正文合并。质量题数量和重复运行数分列。
-
-[^retry]: [自然输出与重试成本](../experiments/ch08/08-08/retry-cost/README.md)。使用已知答案校验，保留错误首答，不外推开放任务的判断成本。
+[^quality]: [KV 与 Q 精度对照](../experiments/ch08/08-08/README.md)、[固定 FP8 权重的 Q 控制](../experiments/ch08/08-08/quantized-q-control/README.md)、[并发容量记录](../experiments/ch08/08-08/concurrency/README.md)。正文采用 BF16 权重组。另一组固定 FP8 权重时，BF16 Q 与默认路径分别为 32/32、28/32。
+
+[^retry]: [自然输出与重试成本](../experiments/ch08/08-08/retry-cost/README.md)。实验使用已知答案校验，重试成本包含错误首答的生成成本。
 
 [^spec]: [推测采样原文](../references/files/papers/speculative-sampling.pdf)、[推测解码原文](../references/files/papers/speculative-decoding.pdf)、[框架计数与预算笔记](../case-studies/speculative-execution.md)、[调研中的框架覆盖](../research/2026-infra-survey/framework-coverage.md)。基础概率算法、草稿路线和具体实现分别引用。
 
-[^history]: [上下文草稿的分布与成本计算](../case-studies/history-drafts-and-rollout.md)及[有理数穷举结果](../research/2026-infra-survey/history-speculation-arithmetic.json)。二符号目标为教学模型，不冒充实际模型接受率。
-
-[^spec-measure]: [DFlash 同执行器对照](../experiments/ch08/08-05/README.md)及[逐请求阶段观测](../experiments/ch08/08-05/kernel-trace/README.md)。归档 ID 8-5 对应当前练习 8-7；低并发、窄题集、固定顺序和观察开销限制外推。并发 1 通过短题的普通、K7、K15 时间中位数分别为 90.02、30.58、29.43 ms；配对输出共 32 对。
+[^history]: [上下文草稿的分布与成本计算](../case-studies/history-drafts-and-rollout.md)及[有理数穷举结果](../research/2026-infra-survey/history-speculation-arithmetic.json)。算例采用二符号目标分布。
+
+[^spec-measure]: [DFlash 同执行器对照](../experiments/ch08/08-05/README.md)及[逐请求阶段观测](../experiments/ch08/08-05/kernel-trace/README.md)。实验在低并发下，按固定顺序执行给定短题集。并发 1 通过短题的普通、K7、K15 时间中位数分别为 90.02、30.58、29.43 ms；配对输出共 32 对。
 
 [^service]: [服务与能量原始记录](../experiments/ch08/08-09/README.md)。同一设备、八道题、七条件各三个 16 请求窗口；同题跨条件输出一致，42 个错误来自同一道题。吞吐表为各窗口指标中位数。每窗口 16 请求的 nearest-rank p95 即最大值。NVML 与 RAPL 组件计数窗口略宽于请求窗口，其他服务仍在运行；完整窗口能量保留在原始记录。
 
-[^task]: [公开生成条件与 Agent 原记录](../experiments/ch08/08-06/README.md)，归档 ID 8-6 对应当前练习 8-8。非流式记录不补造阶段时间；图示为独立教学计算。
+[^task]: [生成实验与 Agent 运行记录](../experiments/ch08/08-06/README.md)。Agent 记录采用非流式调用；阶段时间图采用题设参数。
 
 [^gguf]: [Q2_K 的逐张量布局](../calculations/results/gguf-layout-q2k.md)、[8K 文件容量](../calculations/results/gguf-inventory-8k.md)与[32K 对照](../calculations/results/gguf-inventory-32k.md)。文件总量 85,691,002,112 bytes，码值／浮点载荷 69,178,275,840 bytes，量化元数据 16,506,720,256 bytes，文件头与填充 6,006,016 bytes。上述文件大小按已归档文件头与发布元数据计算。
 
--- before/manuscripts/09-分布式推理.md
+++ after/manuscripts/09-分布式推理.md
@@ -501,7 +501,7 @@
 
 **预写日志**（write-ahead log，WAL）在对外确认之前可靠保存待恢复的操作或结果。这里的 token 级 WAL 保存已经确定的输出序列，KV 保存的是这段序列已经完成的计算。前者决定恢复后应继续哪条输出，后者决定还要重做多少工作。DeepSeek V4 的生成服务采用这类机制；恢复时还要保持权重版本、位置和解码状态一致。[^v4]
 
-故障范围由同步组决定。TP 或 EP 的一个 rank 丢失，可能使整个协作实例上的请求停住；完整副本的其他实例则可能继续服务。恢复时应依次观察检测、重建、恢复后首个有效 token 的返回时间，以及积压请求处理完毕的时间，并记录哪些输出已经返回用户。第 10 章进一步分析这些选择对 RL 样本与训练进展的影响，第 11 章再处理工具和环境的任务恢复。
+故障范围由同步组决定。TP 或 EP 的一个 rank 丢失，可能使整个协作实例上的请求停住；完整副本的其他实例则可能继续服务。故障恢复依次经历检测、实例重建与请求恢复；恢复后的处理能力决定积压请求还需多久才能完成。第 10 章进一步分析这些选择对 RL 样本与训练进展的影响，第 11 章再处理工具和环境的任务恢复。
 
 ## 9.7 部署方案的综合比较
 
@@ -649,7 +649,7 @@
 
 [^route]: [缓存路由计算](../calculations/results/cache-route-book.md)、[快速远端对照](../calculations/results/cache-route-fast-remote.md)。具体公式包含整份远端→host→GPU 串行路径。
 
-[^events]: [缓存事件与路由判断](../case-studies/cache-events-and-routing.md)、[缓存失效两点分布](../calculations/results/cache-route-stale.md)。Dynamo、vLLM 与 SGLang 的事件范围按各自固定集成条件解释。
+[^events]: [缓存事件与路由判断](../case-studies/cache-events-and-routing.md)、[缓存失效两点分布](../calculations/results/cache-route-stale.md)。
 
 [^pressure]: [真实路由压力的配对核算](../calculations/results/router-pressure-book.md)，区分目标请求和完整任务对的完成时间，原始条件随结果保存。
 
--- before/manuscripts/10-训练系统.md
+++ after/manuscripts/10-训练系统.md
@@ -12,7 +12,7 @@
 
 本章围绕一个任务展开：使用 Qwen3-8B 处理 100B token，完成特定领域的继续训练，期限为 30 天。我们先估算至少需要多少资源，再比较由 32 张和 48 张 RTX 4090 组成的两套方案。比较从显存需求开始，逐步加入计算、通信、数据准备和故障恢复的耗时，最后确定哪套方案能够按期完成。为了讲清各项机制，文中穿插较小的张量和流水线算例。强化学习（RL）部分则进一步分析生成、验证和学习如何组成反馈循环。
 
-GB、TB 表示十进制容量，GiB、MiB 表示二进制容量。教学方案的处理时间和容量预留在首次使用时给定；论文及实验结果标明来源，完整配置见章末资料。
+GB、TB 表示十进制容量，GiB、MiB 表示二进制容量。
 
 ## 10.1 根据训练任务估算容量与时间需求
 
@@ -582,7 +582,7 @@
 
 > **案例：路由重放对概率误差、训练耗时与奖励的影响。** 作者公布的四组路由重放（R3）开关对照各训练 100 步，开启后日志中记录的对数概率误差中位数均下降，每步总耗时中位数略长，训练奖励没有一致提高。重放减少了路由差异，但保存记录和执行重放也增加了耗时。比较两种方案时，分别观察概率误差、训练时间和固定评估任务上的表现。[^replay]
 
-因此，定位 RL 不稳定性时，应先冻结同一份权重与同一批已生成 token，逐位置比较生成端记录和训练端重算的 logprob，排除模型更新与随机采样带来的干扰。先核对输入、mask 和采样定义，再比较精度、批次形状与算子路径；MoE 额外检查专家 ID。除了平均误差，还要观察较大误差的分布、概率比偏离 1 的程度，以及这些偏差是否在尚未更新时就触发裁剪。同版本的计算差异受控后，再引入异步执行，按策略滞后程度观察概率比与样本保留情况，才能区分数值误差和旧样本问题。
+同一份权重、同一批 token 下，生成端与训练端的 logprob 差异反映计算路径和采样定义的影响；MoE 的专家 ID 则标明离散路由是否一致。这些差异会传入概率比，使部分样本在参数尚未更新时就触发裁剪。异步训练又引入策略滞后，进一步改变概率比与样本保留比例。数值一致性和策略新鲜度由此共同影响有效训练样本数。
 
 RL 把生成、环境验证和参数更新组织成一个完整的反馈过程。生成端提供采样概率与专家选择，训练端据此组织更新，资源调度再安排各阶段重叠。贯通这些信息，可以同时优化数据交接、执行一致性与资源利用。前面的异步周期和路由重放例子分别量化了省下的等待与增加的记录成本，最终用有效样本和训练进展评价整个反馈循环。
 
@@ -764,7 +764,7 @@
 
 [^identity]: [RL 状态、概率比与版本](../case-studies/rl-state-and-reproducibility.md)、[DeepSeek V4 技术报告](../references/files/papers/deepseek-v4.pdf)。
 
-[^replay]: [路由元数据预算](../calculations/results/routing-metadata-book.md)、[作者公开 R3 日志及分析边界](../experiments/ch10/10-09/source-readiness/README.md)。四对设置共用 seed42；reward 为训练批指标，logprob 为作者日志级统计。
+[^replay]: [路由元数据预算](../calculations/results/routing-metadata-book.md)、[作者公开 R3 日志与分析](../experiments/ch10/10-09/source-readiness/README.md)。四对设置共用 seed42；reward 为训练批指标，logprob 为作者日志级统计。
 
 [^scale]: [Dense 规模与期限](../calculations/results/dense-training-scale-book.md)、[数据量随参数增长的变体](../calculations/results/dense-training-scale-proportional.md)。
 
--- before/manuscripts/11-资源调度与运行环境.md
+++ after/manuscripts/11-资源调度与运行环境.md
@@ -17,7 +17,7 @@
 | 活跃工具环境的内存 | 2 GiB/个 |
 | 工具平台可用资源 | 40 个 CPU 核、640 GiB 内存 |
 | 模型服务并发容量 | 最多同时处理 300 次调用 |
-| 业务目标 | 至少 95% 的提交在 24 s 内通过测试 |
+| 业务目标 | 至少 95% 的提交任务在 24 s 内通过测试 |
 
 先考察一种简单的方案：任务开始时，从预先准备好的环境池中分配一个环境，直到任务结束才释放；清理工作状态后，这个环境可以交给下一项任务。接下来依次考虑环境创建开销、请求突发、模型选择和失败恢复，在同一组任务条件下比较各方案的资源需求。[^design]
 
@@ -39,7 +39,7 @@
 
 模型调用结束时，控制器取得完整工具参数，随后把参数发给执行器。执行器修改文件、启动测试、保存结果，再将结果交回控制器。工具必须等模型给出完整参数才能执行，因此在任务的大部分时间里都不占用 CPU。
 
-模型采用流式输出时，可以更早返回部分文本，但工具仍需等待完整的调用参数：在收到目标文件、修改内容和操作类型之后，执行器才能进行这次修改。模板下载和运行时初始化只依赖预先已知的环境类型，可以提前放到模型生成期间。第 11.2 节将利用这段时间准备环境。[^platform]
+模型采用流式输出时，可以更早返回部分文本，但工具仍需等待完整的调用参数：在收到目标文件、修改内容和操作类型之后，执行器才能进行这次修改。模板下载和运行时初始化只依赖预先确定的环境类型，可以提前放到模型生成期间。第 11.2 节将利用这段时间准备环境。[^platform]
 
 将这个例子推广到 $R$ 轮。在各阶段依次执行的情况下，任务总时间为
 
@@ -48,7 +48,7 @@
 \sum_{r=1}^{R}(T_{\mathrm{model},r}+T_{\mathrm{tool},r})+T_{\mathrm{commit}}.
 $$
 
-上式依次计入排队、环境准备、各轮模型调用与工具执行，以及最终提交结果的时间。模型调用时间包括处理请求和返回结果，工具执行时间包括保存本轮结果，$T_{\mathrm{commit}}$ 则表示最终提交还需花费的时间。如果在模型调用期间准备环境，工具只需等两者都完成后即可开始执行。原本串行的两个阶段有了重叠，任务总时间和环境占用时间也随之改变。
+上式依次计入排队、环境准备、各轮模型调用与工具执行，以及最终提交结果的时间。模型调用时间包括处理请求和返回结果，工具执行时间包括保存本轮结果，$T_{\mathrm{commit}}$ 则表示最终提交还需花费的时间。如果在模型调用期间准备环境，两者都完成后，工具即可开始执行。原本串行的两个阶段有了重叠，任务总时间和环境占用时间也随之改变。
 
 ### 11.1.2 CPU 工作量、环境驻留与并发容量
 
@@ -85,7 +85,7 @@
 
 *图 11-4：每次模型调用由 9 s 延长到 12 s，CPU 需求仍为 30 核；并发调用增至 360，环境内存增至 780 GiB。虚线为 100% 容量。*
 
-除了缩短环境保留时间，还可以错开工具执行，减少同时占用内存的进程。一组四进程 CPU 工具实验中，同时启动与错开启动的累计 CPU 时间分别约为 0.29 秒和 0.27 秒，工作量接近；驻留集大小（RSS）是进程当前驻留在物理内存中的页所占的容量。采样 RSS 峰值却从约 320 MiB 降到 80 MiB，整组完成时间从约 0.10 秒延长到 0.54 秒。错开启动后，同时运行的进程减少，内存峰值随之下降，但整组任务也需要更长时间才能完成。[^environment]
+除了缩短环境保留时间，还可以错开工具执行，减少同时占用内存的进程。一组四进程 CPU 工具实验中，同时启动与错开启动的累计 CPU 时间分别约为 0.29 秒和 0.27 秒，工作量接近；驻留集大小（RSS）是进程当前驻留在物理内存中的页所占的容量。采样得到的 RSS 峰值从约 320 MiB 降到 80 MiB，整组完成时间从约 0.10 秒延长到 0.54 秒。错开启动后，同时运行的进程减少，内存峰值随之下降，但整组任务也需要更长时间才能完成。[^environment]
 
 ### 11.1.3 任务完成条件与必须保留的状态
 
@@ -97,11 +97,11 @@
 | 工作目录与文件 | 仓库版本、文件摘要、补丁版本 | 从持久副本取回或重新应用补丁 |
 | 进程与内存 | 环境实例、进程状态、运行时条件 | 恢复快照或重新执行工具 |
 | 工具结果 | 工具操作、执行尝试、输入文件版本 | 查询已保存结果或再次执行 |
-| 外部操作 | 目标系统中的操作身份与提交状态 | 查询、去重或按业务规则补偿 |
-
-任务、工具操作和每一次执行尝试需要各自的标识。一次测试结果可以被重复传送；同一测试也可能实际执行两次。前一种情况需要消息去重，后一种情况已经发生了两次资源消耗。文件版本不相同时，两次同名测试甚至不是同一个工具操作。
-
-测试结果决定控制器的下一步。如果程序输出错误，控制器就需要修复程序；如果工具进程因资源不足而中止，则需要恢复执行。两类失败对应不同的后续工作，也会产生不同的完成概率。强化学习（RL）根据样本的验证结果更新模型。RL 验证也需要区分这两种情况：答案是否正确用于计算学习反馈，执行故障则先由运行系统处理。
+| 外部操作 | 目标系统中的操作标识与提交状态 | 查询、去重或按业务规则补偿 |
+
+任务、工具操作和每一次执行尝试需要各自的标识。一次测试结果可以被重复传送；同一项测试也可能实际执行两次。前一种情况需要消息去重，后一种情况已经发生了两次资源消耗。文件版本不相同时，两次同名测试甚至不是同一个工具操作。
+
+测试结果决定控制器的下一步。如果程序运行结果不正确，控制器就需要修复程序；如果工具进程因资源不足而中止，则需要恢复执行。两类失败对应不同的后续工作，也会产生不同的完成概率。强化学习（RL）根据样本的验证结果更新模型。RL 验证也需要区分这两种情况：答案是否正确用于计算学习反馈，执行故障则先由运行系统处理。
 
 一次提交可以触发多次尝试。平台为每次尝试支付模型、工具和环境成本，用户得到的是最后完成的任务。第 11.5 节将沿这些分支累积支出，再除以通过测试的任务数。
 
@@ -139,7 +139,7 @@
 
 确定隔离方式以后，平台还需为环境分配资源并装入程序所需的数据。工具平台通过 CPU 配额、内存上限以及文件和设备访问权限来分配资源。给一个等待模型的环境增加 CPU 配额，其文件和内存内容保持原样；销毁这个环境，则会释放内存，同时要求下一次执行前重建这些内容。CUDA 是 NVIDIA GPU 的编程与运行平台；验证 CUDA 程序还需向设备池申请 GPU。设备直通让客体直接操作分配给它的物理设备，可由虚拟机程序 QEMU 配合 Linux 的 VFIO 设备访问接口完成。[^ub]
 
-创建环境需要取得模板、建立私有状态并准备首次工作所需的数据。最简单的方式是完整复制模板；另一种方式是共享只读内容，仅复制写入页；还可以按访问需求装入数据。创建时少加载一些数据，可以缩短启动时间，但程序首次访问这些数据时仍需等待加载。
+创建环境需要取得模板、建立私有状态并准备首次工作所需的数据。最简单的方式是完整复制模板；另一种方式是共享只读内容，仅复制需要写入的页面；还可以按访问需求装入数据。创建时少加载一些数据，可以缩短启动时间，但程序首次访问这些数据时仍需等待加载。
 
 ![模板保存共享的文件与依赖，私有内容保存环境修改；程序运行时还会占用进程内存和缓冲区](ch11/figure-11-template-runtime.svg)
 
@@ -159,13 +159,13 @@
 
 设取回与安装都经过同一条有效速率为 1 GiB/s 的串行通路。模板尚未缓存在本机时，需要取回 2 GiB，再安装 0.5 GiB，数据传输与安装时间至少为 2.5 秒；模板已在缓存中时，只需安装 0.5 GiB，降至 0.5 秒。两秒差额来自省去的模板传输。一般地，这条通路的传输与安装时间下界为 $(V_{\mathrm{fetch}}+V_{\mathrm{install}})/B$。
 
-如果工具首次执行时又访问 1 GiB 尚未装入的页，这条通路便再工作 1 秒。按需加载使创建接口更早返回，将尚未访问的数据留到真正需要时取得。E2B 的页与文件读取采用了这样的组织：启动过程先提供可运行的环境，随后的访问逐步补齐内容。因此，要判断按需加载能否缩短任务时间，需要从环境启动一直计时到首次工具执行完成，将启动等待和首次访问数据的等待一并计入。[^e2b]
+如果工具首次执行时又访问 1 GiB 尚未装入的页，这条通路便再工作 1 秒。按需加载使创建接口更早返回，将尚未访问的数据留到真正需要时取得。E2B 的页与文件读取采用了这样的组织：启动过程先提供可运行的环境，随后的访问逐步补齐内容。首次工具执行的完成时间包含环境启动，以及首次访问缺失页时的等待。按需加载将一部分准备工作推迟到了工具执行期间。[^e2b]
 
 ### 11.2.3 暂停、恢复与环境重建
 
 上一节减少了每次创建时复制的数据。另一种节省内存的办法，是在两次工具执行之间暂时释放整个环境。在章首的三轮任务中，第一轮工具在第 10 秒完成，第二轮工具要到第 19 秒才开始，中间有 9 秒模型调用。始终驻留需要 $2\times9=18$ GiB·秒。设暂停和恢复各用 1 秒，这两个阶段仍占 2 GiB：第 10–11 秒保存状态，第 18–19 秒恢复，其余 7 秒释放内存。该间隔占用降至 4 GiB·秒，节省 14 GiB·秒，下一次工具仍在第 19 秒开始。
 
-从图 11-11 改为图 11-12 的安排后，省去的是等待期间的一段内存占用。要实现这段空闲，必须先保存下一轮仍需使用的状态；因此，暂停收益与保存什么、怎样恢复密切相关。
+从图 11-11 改为图 11-12 的安排后，省去的是等待期间的一段内存占用。要在这段时间内释放内存，必须先保存下一轮仍需使用的状态；因此，暂停收益与保存什么、怎样恢复密切相关。
 
 ![同一段模型等待中的环境驻留与暂停恢复](ch11/figure-11-pause.svg)
 
@@ -244,7 +244,7 @@
 
 ### 11.3.1 异构资源、成组分配与放置约束
 
-算出单个任务的资源需求后，还要判断共享资源池能否同时提供这些资源。一个多卡作业通常需要特定卡型、每卡显存、配套 CPU 与主机内存，以及适合其通信模式的位置。空闲 GPU 总数满足要求，仅是可行性的一个必要条件。
+算出单个任务的资源需求后，还要判断共享资源池能否同时提供这些资源。一个多卡作业通常需要特定卡型、每卡显存、配套 CPU 与主机内存，以及适合其通信模式的位置。空闲 GPU 总数满足要求，只是作业能够启动的必要条件之一。
 
 调度器选定的节点必须同时满足 GPU 型号、数量、CPU 和内存需求，作业中的所有进程才能一起启动。章首 CPU 工具每次独立使用一个核，多卡训练则必须让一组设备共同推进。因此，多卡作业能否启动，不仅取决于空闲资源的总量，还取决于这些资源分布在哪些节点上。
 
@@ -258,7 +258,7 @@
 
 *图 11-21：将占用 8 核的 CPU 任务从节点 1 迁到节点 2 后，节点 1 同时拥有四张 A 卡和 16 核，可以启动新作业。*
 
-假设等待节点 1 的原任务结束需要 12 秒，迁移这个任务需要 4 秒，新作业在本地执行需要 20 秒。等待后执行共需 32 秒，迁移后执行共需 24 秒，整理资源让新作业提前 8 秒完成。若另一种兼容的跨节点配置准备需 1 秒、执行需 32 秒，则立即分散启动共需 33 秒，反而更晚。三种方案的完成时间分别为
+假设等待节点 1 的原任务结束需要 12 秒，迁移这个任务需要 4 秒，新作业在本地执行需要 20 秒。等待后执行共需 32 秒，迁移后执行共需 24 秒，重新安排资源后，新作业提前了 8 秒完成。若另一种兼容的跨节点配置准备需 1 秒、执行需 32 秒，则立即分散启动共需 33 秒，反而更晚。三种方案的完成时间分别为
 
 $$
 T_{\mathrm{wait}}+T_{\mathrm{run,local}},\qquad
@@ -290,7 +290,7 @@
 
 同一批工具调用还需要足够的环境内存。每个环境占 2 GiB，80 个已准备环境需要 160 GiB。始终驻留的方案已有约 600 GiB，占满了 640 GiB 中的大部分空间；第 11.2 节的每轮重建环境方案平均只占 180 GiB，为突发请求预留了更大的空间。减少等待期间保留的环境，可以腾出内存供新调用使用，使空闲 CPU 能及时开始执行。
 
-为突发请求预留容量后，还要决定请求之间的先后顺序。优先级决定谁先使用这份余量。在线任务要求快速开始，离线任务可以等待，平台便可以让离线工作暂借资源，并在在线调用到达时收回。设在线任务最多等 2 秒，而离线作业保存状态需要 30 秒。这组资源即使可以抢占，也要在 30 秒后才交还；因此平台预留另一组能在 2 秒内分配给任务的资源供在线使用。
+为突发请求预留容量后，还要决定请求之间的先后顺序。优先级决定谁先使用这份余量。在线任务要求快速开始，离线任务可以等待，平台便可以让离线工作暂借资源，并在在线调用到达时收回。设在线任务最多等 2 秒，而离线作业保存状态需要 30 秒。这组资源即使可以抢占，也要在 30 秒后才交还；因此，平台需要另行预留能在 2 秒内分配给在线任务的资源。
 
 一次抢占还会丢失未保存的进度。一个已经执行 20 秒、恢复需 2 秒的作业，从最近的检查点恢复执行与从头重做之间相差多少，取决于检查点之后积累的工作。调度器既要考虑新任务能提前多久开始，也要计入保存和恢复状态的开销，以及原作业因此增加的延迟。
 
@@ -340,7 +340,7 @@
 
 临时实例提前被收回时，损失的不仅是尚未利用的容量，还有已经生成的内容。保存生成前缀可以把已完成工作带到新实例。新实例读入“原输入＋保存输出”，执行预填充（prefill，处理完整前缀）建立注意力上下文键值缓存（KV），然后继续生成。PolyRL 的一个批量分支按组内最短前缀对齐：两条响应已收到 4,000 和 1,000 token 时，各采用 1,000 token，共复用 2,000 token，较长那条的 3,000 token 要重新生成。将前缀长度对齐后，可以统一安排后续生成，但较长前缀中超出的部分就需要重新计算。
 
-在一次 Qwen3-8B 对照中，保留前缀省去了 256 个重复采样 token，完整时间却约为 12.8 秒，而从头重做约为 11.6 秒。节省的 token 对应生成阶段，完整路径还包含前缀重建与重新调度。评估恢复收益时，把重建前缀并继续生成的总时间，与从头生成到相同停止条件的总时间相比较，才能知道节省的生成工作是否转化为更早完成。[^recovery]
+在一次 Qwen3-8B 对照中，保留前缀省去了 256 个重复采样 token，完整时间却约为 12.8 秒，而从头重做约为 11.6 秒。节省的 token 对应生成阶段，完整路径还包含前缀重建与重新调度。这组对照中，前缀重建与重新调度的额外时间超过了省下的生成时间。[^recovery]
 
 还可以从成本判断增加临时实例是否划算。按 RLBoost 使用的历史价格，训练实例每小时约 84 美元，六个额外实例各约 5.3 美元，总费率约为每小时 116 美元。全部实例全程计费时，处理同一批训练任务的吞吐量需达到原来的约 $116/84\approx1.38$ 倍，单位工作成本才下降。吞吐量只达到原来的 1.2 倍，完成时间缩短，但成本约增至原来的 1.15 倍；达到原来的 1.6 倍，成本则约降至 0.86 倍。[^rollout]
 
@@ -370,11 +370,11 @@
 
 *图 11-28：九条验证各用 1 s，一条用 100 s。虚线是已经执行的 10 s；到此时仍未结束的样本只可能来自 100 s 那一类，剩余时间为 90 s。*
 
-估计出各条样本的剩余时间后，便可以据此安排其他验证，尽量不延长整批等待。假设批次中有一条样本最早要到第 120 秒才能完成。一项后续执行上限为 100 秒的工作，最迟在第 20 秒启动，便可以利用前面的空隙而不推迟整批；若其环境准备需要 5 秒，准备最迟在第 15 秒启动。由整批的完成时刻可以反推出各项工作的最迟开始时间，调度器据此推迟不紧迫的工作，把资源先交给更早到期的批次。
+估计出各条样本的剩余时间后，便可以据此安排其他验证，尽量不延长整批的等待时间。假设批次中有一条样本最早要到第 120 秒才能完成。一项后续执行时间上限为 100 秒的工作，最迟在第 20 秒启动，便可以利用前面的空隙而不推迟整批；若其环境准备需要 5 秒，准备最迟在第 15 秒启动。由整批的完成时刻可以反推出各项工作的最迟开始时间，调度器据此推迟不紧迫的工作，把资源先交给更早到期的批次。
 
 推迟不紧迫的验证可以减少验证资源占用，但一旦推迟了整批完成，也会让训练组继续等待。假设验证少用 60 GPU·秒，却使仍持有 64 张相同 GPU 的训练组多等 2 秒。训练组新增 $64\times2=128$ GPU·秒，整个系统净增 68 GPU·秒。若训练组只有 16 张卡，新增等待为 32 GPU·秒，系统反而净省 28 GPU·秒。验证资源是否值得缩减，因而取决于被它阻塞的训练组有多大。
 
-安排好完成时刻以后，还要确认结束或超时的任务确实停止了执行，资源才能交给下一项任务。一次受控验证中，等待方返回超时后，后台线程仍工作约 130 ms；主动终止子进程后，约 0.8 ms 观察到退出。如果超时一到就把同一资源交给下一项任务，仅报告超时而未终止后台线程的做法，就会导致两项任务同时使用资源。对于 CUDA 性能验证，这种竞争还可能改变测得的执行时间和奖励。[^validation]
+安排好完成时刻以后，还要确认结束或超时的任务确实停止了执行，资源才能交给下一项任务。一次受控验证中，等待方返回超时后，后台线程仍工作约 130 ms；主动终止子进程后，约 0.8 ms 后观察到进程退出。如果超时一到就把同一资源交给下一项任务，仅报告超时而未终止后台线程的做法，就会导致两项任务同时使用资源。对于 CUDA 性能验证，这种竞争还可能改变测得的执行时间和奖励。[^validation]
 
 ## 11.4 模型服务的选择与调用成本
 
@@ -382,7 +382,7 @@
 
 每轮重建环境已经把章首平台内存需求从 600 GiB 降到 180 GiB，任务完成时间仍为 30 秒，超过 24 秒目标。接下来要改变关键路径上的模型调用。如果改用一个同样满足任务通过率要求的快速服务，每次完整调用由 9 秒降到 6 秒，三轮便从 30 秒降到 21 秒。若环境始终驻留，每项占用也从 60 降到 42 GiB·秒，整个平台的内存需求从 600 降到 420 GiB；如果每轮重建环境，仍只需在各轮准备和执行期间保留环境，累计占用保持在 18 GiB·秒。模型服务的选择因此同时改变时间和环境容量。[^design]
 
-一次模型调用的时间和成本，包括处理输入、生成思考内容和生成可见输出三部分。思考 token 是模型用于中间推理而非直接展示给用户的生成内容。思考预算决定允许生成多长的内部推理，实际用量决定这次调用支付多少成本。先在输出都能通过测试的条件下，比较两种思考长度设置的单次调用成本，再比较两种服务完成三轮任务所需的成本。
+一次模型调用的时间和成本，包括处理输入、生成思考内容和生成可见输出三部分。思考 token 是模型用于中间推理而非直接展示给用户的生成内容。思考预算决定允许生成多长的内部推理，实际用量决定每次调用需要支付多少费用。先在输出都能通过测试的条件下，比较两种思考长度设置的单次调用成本，再比较两种服务完成三轮任务所需的成本。
 
 **例 11-5：思考长度缩短到十分之一，总成本减少多少？** 设两种思考长度设置都能通过相同测试，每次输入 10,000 token，可见输出 200 token，实际思考分别为 1,000 和 100 token。输入每百万 2 个成本单位，生成每百万 10 个成本单位，思考包含在生成项中。那么单次成本分别是
 
@@ -429,7 +429,7 @@
 
 本例保持十轮生成和工具执行相同，比较的成本差额全部来自输入。第一次使用缓存花费 0.024，比普通处理的 0.020 多出 0.004；此后每轮只花 0.0056，比普通处理少 0.0144。因此第二次访问就已收回首次创建的溢价。共访问 $n$ 次时，使用缓存更省的条件是 $np_u>p_w+(n-1)p_h$。缓存有效期越长，就越有机会通过重复读取收回首次创建的额外成本。[^platform]
 
-再看两次调用，一次前缀长 1,000 token，一次长 9,000 token。若只有短前缀命中，请求命中率为 50%，缓存读取却只占总前缀的 10%；若只有长前缀命中，请求命中率仍为 50%，读取占比变成 90%。按 token 收费时，后者省下的输入处理远多于前者。输入长度给每次命中赋予了不同权重。
+再看两次调用，一次前缀长 1,000 token，一次长 9,000 token。若只有短前缀命中，请求命中率为 50%，缓存读取的 token 数却只占两次调用前缀总长度的 10%；若只有长前缀命中，请求命中率仍为 50%，读取占比变成 90%。按 token 收费时，后者节省的输入处理费用远多于前者。输入长度给每次命中赋予了不同权重。
 
 ### 11.4.3 排队、限流与模型路由
 
@@ -460,7 +460,7 @@
 
 ![成本曲线与按时通过测试的要求的交点](ch11/figure-11-7-routing.svg)
 
-*图 11-31：B 的成功任务成本随命中率提高而下降，在约 84.9% 处等于 A。两者的成本先按各自成功数归一化；本图只比较成本，下一图单独加入期限。*
+*图 11-31：B 的成功任务成本随命中率提高而下降，在约 84.9% 处等于 A。两者的总成本先分别除以各自成功完成的任务数；本图只比较成本，下一图单独加入期限。*
 
 ![只有 B 命中的 4 s 路径满足 6 s 期限；再乘 98% 质量概率，按时成功比例为 0.98h](ch11/figure-11-routing-deadline.svg)
 
@@ -478,9 +478,9 @@
 F+Nv<Nc,\qquad N>\frac{F}{c-v}\quad(c>v).
 $$
 
-若 $c\leq v$，API 每项成本已低于自建新增成本，固定投入又为正，因此 API 在所有使用量上都更省。若两者成功率不同，分别以 $Np_{\mathrm{self}}$ 和 $Np_{\mathrm{api}}$ 为分母，便将比较转换为每个成功任务的成本。
-
-取固定支出 1,000 成本单位、自建每项新增 0.002、API 每项 0.012，并给定两种服务均能在期限内处理 200,000 项。交点为 100,000 项提交。50,000 项时自建成本 1,100，API 为 600；200,000 项时分别为 1,400 和 2,400。固定投入在低使用量下难以摊薄，高使用量下则被较低的新增成本抵消。
+若 $c\leq v$，API 每项成本已低于自建新增成本，固定投入又为正，因此 API 在所有使用量下都更省。若两者成功率不同，分别以 $Np_{\mathrm{self}}$ 和 $Np_{\mathrm{api}}$ 为分母，便将比较转换为每个成功任务的成本。
+
+取固定支出 1,000 成本单位、自建每项新增 0.002、API 每项 0.012，并给定两种服务均能在期限内处理 200,000 项。成本相等时的任务量为 100,000 项。50,000 项时自建成本 1,100，API 为 600；200,000 项时分别为 1,400 和 2,400。固定投入在低使用量下难以摊薄，高使用量下则被较低的新增成本抵消。
 
 图 11-33 中，自建曲线较高的起点来自固定支出，较缓的斜率来自较低的单项成本。优化执行效率会进一步降低这条线的斜率，使自建服务在更少的任务量下就能收回固定投入。
 
@@ -500,7 +500,7 @@
 
 前面选择模型时，主要考虑了调用正常完成的情况。实际运行中，还需要处理调用失败、结果丢失和工具异常。如果控制器没有收到测试完成的确认消息，就无法区分两种情况：测试尚未完成，或者测试已经完成但确认消息丢失。
 
-恢复时，控制器先查询已持久化的结果，核对操作身份和输入文件版本。如果结果存在，就可以继续；如果不存在，再判断是否允许重试。幂等操作指以同一操作身份重复调用时，最终业务效果与调用一次相同。对只读查询或具有幂等语义的操作，重复执行通常容易处理；对外部付款、创建资源或发送消息，重复调用可能使同一操作实际发生两次，需要目标系统支持操作标识、事务或补偿。
+恢复时，控制器先查询已持久化的结果，核对操作标识和输入文件版本。如果结果存在，就可以继续；如果不存在，再判断是否允许重试。幂等操作指以同一操作标识重复调用时，最终业务效果与调用一次相同。对只读查询或具有幂等语义的操作，重复执行通常容易处理；对外部付款、创建资源或发送消息，重复调用可能使同一操作实际发生两次，需要目标系统支持操作标识、事务或补偿。
 
 ![外部系统已执行并提交操作 K，确认却丢失](ch11/figure-11-commit-ack.svg)
 
@@ -508,7 +508,7 @@
 
 假设执行一次工具需要 3 CPU·秒，结果被发送两次。如果接收端按操作标识去重，累计 CPU 时间仍为 3 秒；如果控制器因确认丢失而真的再执行一次，累计 CPU 时间就增至 6 秒。两种情况最后都可能只有一条结果，却消耗不同资源。消息去重解决前一种重复，持久结果和目标系统的幂等协议才有助于避免后一种重复。环境外已经提交的操作在快照恢复后仍然存在，恢复流程要先查询其提交状态。[^records]
 
-从已保存状态继续执行，可以保留已经完成的工作；换用能力更强的模型，则可能提高下一步求解的成功率。若原模型已完成两轮并保存补丁，新模型可以从该补丁和测试结果继续；这样可以保留前两轮的工作，只需再支付新一次调用的成本。失败类型决定这次调用的成功概率，因此恢复树在每个节点使用“已经到达此处的任务”的条件概率。
+从已保存状态继续执行，可以保留已经完成的工作；换用能力更强的模型，则可能提高下一步求解的成功率。若原模型已完成两轮并保存补丁，新模型可以从该补丁和测试结果继续；这样可以保留前两轮的工作，只需再支付一次新模型调用的成本。失败类型决定这次调用的成功概率，因此恢复树中每个节点的概率，都以任务已经到达该节点为条件计算。
 
 ### 11.5.2 成功任务的完整成本
 
@@ -519,7 +519,7 @@
 \overline C_{q,d}=\frac{C_{\mathrm{all}}}{N_{q,d}}.
 $$
 
-把一次失败尝试从分子删除，会把它使用的模型和工具资源隐去。比如十项任务共花 1 个成本单位，五项成功，平台每完成一项实际支付 0.2。若只统计五项成功路径自身的 0.5，就会得到 0.1，恰好漏掉另一半资源。总支出除以完成数，使成功与失败共同决定单位产出成本。
+把一次失败尝试从分子删除，会漏算它消耗的模型和工具资源。比如十项任务共花 1 个成本单位，五项成功，平台每完成一项实际支付 0.2。若只统计五项成功路径自身的 0.5，就会得到 0.1，恰好漏掉另一半资源。总支出除以完成数，使成功与失败共同决定单位产出成本。
 
 有限重试树可以在部署前用于初步估算。对路径 $\pi$，令路径概率为 $p_\pi$，各步骤成本之和为 $c_\pi$，则 $E[C]=\sum_\pi p_\pi c_\pi$。成功概率是所有最终成功的执行路径概率之和。每个节点的概率都应根据此前经历的执行结果计算；如果两类失败的后续成功率不同，应拆成不同节点。
 
@@ -556,7 +556,7 @@
 
 恢复把成功比例从 80% 提高到约 99.7%，但其中约 4.7% 的提交在第 22 秒才完成。业务期限为 20 秒时，这部分工作占用资源，却没有增加按时完成数。收紧期限改变的是哪些终点被计入分母；限制重试次数则会改变执行树本身。前者改变成功任务的统计范围，后者改变实际执行的工作。[^records]
 
-模型变快以后，环境准备可能无法再与模型调用完全重叠，从而开始影响任务完成时间。假设下一次工具调用必然发生，模型还需 10 秒思考，环境创建需 6 秒，从模型开始时提前创建即可隐藏这 6 秒。若模型阶段缩短至 1 秒，同样时机开始创建仍要再等 5 秒。[^core-calculation]先只更换模型设备，保持准备策略不变；再比较更早准备、保留已就绪环境和按需等待的成本，并计入提前占用的资源，以及最终未使用环境时的准备开销。
+模型变快以后，环境准备可能无法再与模型调用完全重叠，从而开始影响任务完成时间。假设下一次工具调用必然发生，模型还需 10 秒思考，环境创建需 6 秒，在模型开始思考时创建环境，就能让这 6 秒准备时间与模型思考重叠。若模型阶段缩短至 1 秒，同样时机开始创建仍要再等 5 秒。[^core-calculation]先只更换模型设备，保持准备策略不变；再比较更早准备、保留已就绪环境和按需等待的成本，并计入提前占用的资源，以及提前准备的环境最终未被使用时所产生的开销。
 
 ![图 11-38 模型加速后，环境创建成为工具开始执行前的等待来源。两种情况都在零时刻开始创建环境；工具须同时等待模型决策与环境就绪。横轴为秒。](ch11/figure-11-environment-overlap.svg)
 
@@ -574,11 +574,11 @@
 
 接着比较环境策略。普通服务配合每轮重建环境，准备工作虽与模型调用重叠，但三轮仍为 30 秒。换快速服务后，模型总时间降到 18 秒，加工具 3 秒，共 21 秒。此时始终驻留与每轮重建环境都满足期限，需要进一步比较资源与成本。
 
-图 11-39 先按期限筛选方案：普通模型的最后一轮落在线外，快速模型的三轮都在线内。接下来只需在使用快速模型的两种环境策略之间比较成本，同时检查它们是否超过平台容量。
+图 11-39 先按期限筛选方案：普通模型的最后一轮结束于期限之后，快速模型的三轮则都能在期限内完成。接下来只需在使用快速模型的两种环境策略之间比较成本，同时检查它们是否超过平台容量。
 
 ![普通模型与快速模型的三轮完成时间](ch11/figure-11-decision.svg)
 
-*图 11-39：两种方案均在模型调用期间准备环境，工具执行均为每轮 1 秒。模型调用从每轮 9 秒降至 6 秒，三轮结束时刻从第 30 秒移到第 21 秒，越过 24 秒期限线。*
+*图 11-39：两种方案均在模型调用期间准备环境，工具执行均为每轮 1 秒。模型调用从每轮 9 秒降至 6 秒，三轮结束时刻从第 30 秒移到第 21 秒，从 24 秒期限之后提前到期限之内。*
 
 | 方案 | 完成时间 / s | 累计 CPU 时间 / CPU·秒 | 每项累计内存占用 / GiB·秒 | 平均内存 / GiB | 每项提交成本 | 按时通过测试的比例 |
 | --- | ---: | ---: | ---: | ---: | ---: | ---: |
@@ -624,7 +624,7 @@
 
 [^environment]: 本书 [environment-resources 固定计算](../calculations/results/environment-resources-book.md)及[工具子进程记录](../experiments/ch11/11-01/process-resources/README.md)。工具实验包含九组、36 个进程，累计内存占用由采样 RSS 对时间积分得到。
 
-[^lifecycle]: [environment-lifecycle 固定预算](../calculations/results/environment-lifecycle-default.md)区分共享／私有容量、串行字节下界和局部预热实测。本章使用固定容量预算比较四种内容安装方式；云端完整环境峰值与启动时间属于单独的测量项目。
+[^lifecycle]: [environment-lifecycle 固定预算](../calculations/results/environment-lifecycle-default.md)区分共享／私有容量、串行字节下界和局部预热实测。本章使用固定容量预算比较四种内容安装方式。
 
 [^platform]: [调度、模型路由与云端环境研究](../case-studies/platform-routing.md)，包含 ASI、RLBoost、DistRS、SpecBox。ASI 研究覆盖六个月、155,410 张 GPU，其分配比例度量设备归属。计费公式采用题设价格。
 
@@ -640,7 +640,7 @@
 
 [^recovery]: [Qwen3-8B 抢占恢复实验](../experiments/ch11/11-04/README.md)，分别记录生成、客户端收到和恢复时采用的 token 数量。
 
-[^reward]: [验证剩余时间与框架入口调研](../case-studies/reward-deadlines-and-feedback.md)，包括 DistRS 的负载、配置、算法和评估边界，以及固定 verl 入口的静态核对；其中框架部分采用静态源码阅读。
+[^reward]: [DistRS 验证调度与 verl 实现分析](../case-studies/reward-deadlines-and-feedback.md)。
 
 [^validation]: [超时与资源释放实验](../experiments/ch11/11-05/README.md)、[双批次调度记录](../experiments/ch11/11-05/batch-scheduling/README.md)。实验使用受控 CPU 进程。
 
@@ -652,7 +652,7 @@
 
 [^records]: [任务记录与证据范围](../case-studies/evaluation-and-agent-records.md)、[文件队列故障注入](../experiments/ch11/11-09/collector-faults/README.md)、[任务退出状态记录](../experiments/ch11/11-09/exit-status/README.md)。这些记录分别用于说明结果持久化、故障诊断和成本核算。
 
-[^retry]: 精确结果：成功概率 0.99744，通过测试且按时完成概率 0.9504，每项期望成本 0.01456，累计 CPU 时间 3.376 秒，驻留 23.008 GiB·秒。计算结果分别列出资源用量和成本；各节点的给定成本已经是总价，无需再按资源用量重复计费。见[retry-paths 有限条件图](../calculations/results/retry-paths-book.md)。各节点概率与成本为教学输入，期限不终止执行，不假设无限独立重试。
+[^retry]: 精确结果：成功概率 0.99744，通过测试且按时完成概率 0.9504，每项期望成本 0.01456，累计 CPU 时间 3.376 秒，驻留 23.008 GiB·秒。计算结果分别列出资源用量和成本；各节点的给定成本已经是总价，无需再按资源用量重复计费。见[retry-paths 有限条件图](../calculations/results/retry-paths-book.md)。各节点概率与成本按题设给定，重试沿有限执行树展开；超过评价期限的任务仍继续执行到终点。
 
 [^design]: 贯穿平台与例 11-9 是确定性教学设计，输入、逐阶段时间表、成本、成本相等的条件与复算见[贯穿设计数据](ch11/platform-design.json)及[验证程序](ch11/verify.py)。40 核、640 GiB 内存、最多 300 次并发模型调用、两种服务时间／价格和 95% 质量概率为题设。完整环境准备含文件取得与初始化共 2 秒、0.1 CPU·秒，结果保存与工作状态清理包含在工具的 1 秒内；原模板容量算例单独说明内容安装。
 
@@ -666,7 +666,7 @@
 
 章首平台平均使用 30 个 CPU 核，环境却占用 600 GiB 内存。原因是三轮任务中有 27 秒在等待模型，而工作状态始终存在。在模型调用期间准备环境，把每项任务的累计内存占用从 60 降到 18 GiB·秒，准备工作多用 0.3 秒 CPU 时间，就能释放大量内存。
 
-这项改进利用了完整任务的执行关系。环境管理掌握模型等待区间后，可以把创建提前，把进程寿命缩短到工具真正使用它的阶段；持久化文件保存各轮之间需要保留的状态。模型服务和环境平台共同安排准备时机，使原来贯穿任务全程的资源占用变成按阶段分配。
+这项改进利用了完整任务的执行关系。环境管理器确定模型调用期间可用于准备的时间后，可以把创建提前，让进程仅在工具准备和执行期间存在；持久化文件保存各轮之间需要保留的状态。模型服务和环境平台共同安排准备时机，使原来贯穿任务全程的资源占用变成按阶段分配。
 
 释放内存以后，任务仍需 30 秒，模型调用成为满足 24 秒期限的关键。将调用从 9 秒降到 6 秒，把三轮任务从 30 秒降到 21 秒，也将平均并发模型调用数从 270 次降到 180 次。结合每轮重建环境，现有 40 核、640 GiB 内存和最多 300 次并发模型调用的容量即可支持给定任务流。成本比较随后在满足质量和期限的方案之间完成。
 
--- before/manuscripts/12-端边云协同.md
+++ after/manuscripts/12-端边云协同.md
@@ -302,7 +302,7 @@
 
 NewReno 是根据丢包与确认调整拥塞窗口的算法，CUBIC 在拥塞避免阶段按时间的立方函数调节窗口；慢启动是连接初期随确认快速增加窗口的阶段。在相同的数据填充方式与 NewReno 配置下，将立即 ACK 改为每两包确认或最多等待 10 ms，实际传输的数据从约 39.6 MB 降至 38.2 MB，完整请求却从约 14.52 秒增至 14.56 秒，晚约 43 ms。[^ack] 这组对照揭示了反馈的双重作用：少发确认减少传输时间，确认晚到延长窗口等待。最终能否更快完成，要比较省下的传输时间与增加的等待时间。
 
-ACK 改变窗口继续发送的时刻，拥塞控制则决定窗口本身如何变化。比较拥塞控制算法时，应先找出请求经历了哪个阶段。相同路径的大图比较中，NewReno 与 CUBIC 完整响应均约为 14.5 秒；窗口轨迹显示 CUBIC 一直在慢启动。[^ack] 本次请求在进入立方拥塞避免阶段之前就已结束，因此观察到的是启动阶段的行为。要研究长流阶段，就延长负载，使窗口经历拥塞事件并进入相应控制过程。
+ACK 改变窗口继续发送的时刻，拥塞控制则决定窗口本身如何变化。比较拥塞控制算法时，应先找出请求经历了哪个阶段。相同路径的大图比较中，NewReno 与 CUBIC 完整响应均约为 14.5 秒；窗口轨迹显示 CUBIC 一直在慢启动。[^ack] 本次请求在进入立方拥塞避免阶段之前就已结束，因此观察到的是启动阶段的行为。
 
 ### 12.3.3 多流传输与优先级
 
@@ -330,7 +330,7 @@
 
 **鹊桥是什么，解决什么问题？** 鹊桥（Queqiao）是作者开发的开源、自托管广域网传输系统，在可控制的客户端与可信网关之间承载 TCP 和 UDP 流量。应用通过本地 SOCKS5 代理接入客户端，客户端跨广域网连接网关，网关再将请求转交目标服务。在这里的数据中心间部署中，客户端位于调用方服务器，网关靠近远端推理服务；因此，需要优化的长距离链路集中在客户端与网关之间。应用仍调用原有的 ASR、TTS 接口，鹊桥负责传送请求与结果。[^queqiao-readme]
 
-项目 README 描述了这一实际需求：模型运行在美国，客户端分布在不同地方。ASR 上传几百 KB 音频，返回一句识别文本；TTS 提交一段文本，返回几百 KB 音频。README 中的 TTS 场景在模型完成后集中返回音频，属于一次突发传输；12.1 中逐块生成、边收边播的流水线则需要另外检查首段与连续播放。对于这类短请求，即使链路能在几毫秒内发送完全部字节，连接建立、窗口增长和丢包恢复仍可能各自引入长距离反馈等待。
+项目 README 描述了这一实际需求：模型运行在美国，客户端分布在不同地方。ASR 上传几百 KB 音频，返回一句识别文本；TTS 提交一段文本，返回几百 KB 音频。README 中的 TTS 场景在模型完成后集中返回音频，属于一次突发传输；12.1 节的流式场景则在生成首块后开始传输，接收与播放随生成过程持续推进。对于这类短请求，即使链路能在几毫秒内发送完全部字节，连接建立、窗口增长和丢包恢复仍可能各自引入长距离反馈等待。
 
 鹊桥的设计目标是减少这些额外等待，使短请求尽量接近必要传播、数据发送和模型处理所需的时间，同时提高长流对可用带宽的利用。它让多条应用流共享客户端—网关路径的测量与传输状态，复用连接，并根据路径情况安排发送和恢复；前向纠错通过增加冗余数据，让接收端有机会不等下一轮重传就恢复缺包。交互流与大文件共存时，还需要通过发送调度控制交互等待。前面讨论的连接、窗口和多流机制，在这里共同服务于跨地域调用。传播距离本身仍然决定必须付出的时间，优化后的完整请求能否满足业务期限，决定了集中部署是否适合这项任务。
 
@@ -369,9 +369,7 @@
 
 实验的运行顺序也会影响比较结果。交替测试两条路径，可以让一组对比中的两次请求在相近时间运行，减小网络变化对比较的影响；同期 RTT 为约 197–205 ms，跨度约 8 ms。调优后的两条请求相差约 4 ms，小于同期 RTT 的波动幅度。在这组实验中，两条路径的请求时间都已接近 240 ms。
 
-**实验结果与配置选择。**
-
-调优对照方案后，下一步优化的重点也会改变。模型从 30 ms 加速到 3 ms，最多省 27 ms；将 RTT 从 200 ms 降至 20 ms，可省约 180 ms。只要附近执行新增的准备与计算少于这 180 ms，它就优于原远端路径。这与 12.1 的比较方法相同：先算出更换服务器可以少等多久，再扣除新增的计算和准备时间。
+**实验结果与配置选择。** 调优对照方案后，下一步优化的重点也会改变。模型从 30 ms 加速到 3 ms，最多省 27 ms；将 RTT 从 200 ms 降至 20 ms，可省约 180 ms。只要附近执行新增的准备与计算少于这 180 ms，它就优于原远端路径。这与 12.1 的比较方法相同：先算出更换服务器可以少等多久，再扣除新增的计算和准备时间。
 
 音频到达后，还需要由播放器连续播放。另一组采用静音输出设备回调的实验中，完整 PCM 最终全部收到，回调却累计有约 0.36–1.44 秒取不到足够样本。[^physical] 请求结束只记录最后一批数据到达，播放器则在此之前就要持续读取音频。请求总时间与播放停顿时间描述的是两个不同的问题。
 
@@ -655,7 +653,7 @@
 
 [^raw]: [RAW 图片精修案例与条件预算](../case-studies/raw-retouching.md)。RAW／DNG 与信息保留由归档的华为专业模式及 Adobe 材料支持；本章 30 MB／5 MB／0.3 秒为题设值。
 
-[^stream]: 分三块的两种计算分别为 12.8 s 与 12.36 s，正文以约 0.4 s 差额解释重叠收益；额外预览另增编码与传输，不等同完整成片。来源：[整图屏障](../calculations/results/image-stream-whole-image-without-preview.md)、[独立分块](../calculations/results/image-stream-independent-blocks-without-preview.md)及[额外预览](../calculations/results/image-stream-independent-blocks-with-preview.md)。各自采用对应的依赖与编码条件。
+[^stream]: 分三块的两种计算分别为 12.8 s 与 12.36 s，正文以约 0.4 s 差额解释重叠收益；额外预览另增编码与传输。来源：[整图屏障](../calculations/results/image-stream-whole-image-without-preview.md)、[独立分块](../calculations/results/image-stream-independent-blocks-without-preview.md)及[额外预览](../calculations/results/image-stream-independent-blocks-with-preview.md)。各自采用对应的依赖与编码条件。
 
 [^audio]: [音频块时序计算](../calculations/results/audio-timing-base.md)，输入帧与输出音频块一一对应是该模型的简化；该简化用于说明逐块处理与按序播放的依赖。
 
@@ -669,7 +667,7 @@
 
 [^survey]: [FlexSP／llm.npu 论文与实现资料](../references/framework-history/2026-09-09/sequence-and-npu/NOTES.md)。
 
-[^quic]: [RFC 9000：QUIC 传输](../references/files/standards/rfc9000.txt)，§2、§7、§9、§13；[RFC 9001：TLS](../references/outline-checks/2026-09-07/rfc9001.txt)；[RFC 9114：HTTP/3](../references/outline-checks/2026-09-07/edge-media/rfc9114.txt)；[RFC 9221：不可靠 Datagram](../references/outline-checks/2026-09-07/rfc9221.txt)。HTTP/3 是 HTTP 的传输映射，
+[^quic]: [RFC 9000：QUIC 传输](../references/files/standards/rfc9000.txt)，§2、§7、§9、§13；[RFC 9001：TLS](../references/outline-checks/2026-09-07/rfc9001.txt)；[RFC 9114：HTTP/3](../references/outline-checks/2026-09-07/edge-media/rfc9114.txt)；[RFC 9221：不可靠 Datagram](../references/outline-checks/2026-09-07/rfc9221.txt)。HTTP/3 是 HTTP 的传输映射。
 
 [^closed]: 精确分包为上传 25,685 包、下载 4,281 包；共 29,966 个数据包和同数 ACK，线上 39,554,832 bytes，完整响应 14.36339008 s。正文用约三万包、39.6 MB 和 14.4 s 说明量级。来源：[完整图片参考闭环](../calculations/results/closed-loop-book-30mb-5mb.md)。包长、立即 ACK、恢复与数值量化是明确输入，该结果用于解释逐包反馈如何增加请求时间。
 
@@ -683,17 +681,17 @@
 
 [^wireless-survey]: [计算机网络的新黄金时代（三）](../references/files/documents/network-golden-3.md)，介绍 TACK、Link Turbo 与代理部署的历史背景。
 
-[^mixed]: [媒体优先与聚合 ACK 的混合反馈计算](../calculations/results/media-feedback-mixed-priority-aggregate.md)及同目录 FIFO／立即 ACK 对照。模型处理时间和预定播放时刻由算例设定，截图动作未完成的结果保留。
+[^mixed]: [媒体优先与聚合 ACK 的混合反馈计算](../calculations/results/media-feedback-mixed-priority-aggregate.md)及同目录 FIFO／立即 ACK 对照。模型处理时间和预定播放时刻由算例设定。
 
 [^dual]: [双进程双 TCP 连接实验](../experiments/ch12/12-06/README.md)：实验通过双进程、双 TCP 连接模拟两条传输路径，30 次正式尝试包含共同端点失败的情况。
 
-[^queqiao-readme]: 系统定位与跨数据中心动机见 [Queqiao README 固定归档](../references/author-context/queqiao-496ca627/README.md)，对应 [GitHub 提交 `496ca627`](https://github.com/bojieli/queqiao/blob/496ca6278e359c02b5107dfab77c6a3db585f80d/README.md)，读取于 2026-09-10。系统介绍采用该版本；下文性能分析仍采用 `168ff4b` 的固定文件对照，不混用不同版本的性能结果。
+[^queqiao-readme]: 系统定位与跨数据中心动机见 [Queqiao README 固定归档](../references/author-context/queqiao-496ca627/README.md)，对应 [GitHub 提交 `496ca627`](https://github.com/bojieli/queqiao/blob/496ca6278e359c02b5107dfab77c6a3db585f80d/README.md)，读取于 2026-09-10。系统介绍依据此版本，性能对照数据来自提交 `168ff4b`。
 
 [^queqiao]: [固定归档的路径记录](../references/author-context/queqiao-168ff4b/PATH-CHARACTER-DC-20260826.md)与[设计记录](../references/author-context/queqiao-168ff4b/DESIGN-DC-PROFILE.md)，来自提交 `168ff4b`。文件、调优设置和计时起止点均采用原记录中的定义。
 
 [^queqiao-calc]: 新建连接的直接／Queqiao p50 为 1185.3／301.6 ms，调优保持连接为 240.9／236.5 ms；两个中位数之比分别约 3.93／1.019。文档另给逐轮配对比中位数 3.96／1.03，统计量不同。正文将后一组写成均约 0.24 s。来源：[Queqiao 同条件统计](../calculations/results/queqiao-records-conditions.md)。
 
-[^physical]: 8 次完整接收测试中，收到的 PCM 与播放回调读取的 PCM 均逐位一致，播放回调累计有 360–1440 ms 无法取得足够音频样本；设备输出静音，无声学起播测量。3 次取消早于首个回调，旧验收要求回调非空而报错；根据已收到的字节、连接关闭事件和远端 EOF，可以判断这些请求在首次播放前就已取消；统计时保留了零回调记录。完整四轮数据见[配套证据说明](ch12/evidence-notes.md)。来源：[显式物理接口绑定的音频实验](../experiments/ch12/12-07/audio-physical/README.md)，固定提交 `496ca627`，基线含 socket 绑定适配。
+[^physical]: 8 次完整接收测试中，收到的 PCM 与播放回调读取的 PCM 均逐位一致，播放回调累计有 360–1440 ms 无法取得足够音频样本；实验使用静音输出设备，记录包含接收与播放回调事件。另有 3 次请求在首个播放回调之前取消，接收字节、连接关闭事件和远端 EOF 记录了取消过程。完整四轮数据见[配套证据说明](ch12/evidence-notes.md)。来源：[显式物理接口绑定的音频实验](../experiments/ch12/12-07/audio-physical/README.md)，固定提交 `496ca627`，基线含 socket 绑定适配。
 
 [^region]: [计算机网络的新黄金时代（二）](../references/files/documents/network-golden-2.md)的云际网络与 Regionless 讨论。本章地域方案采用题设价格。
 
