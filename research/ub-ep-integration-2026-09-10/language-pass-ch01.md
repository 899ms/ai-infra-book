# 第 1 章语言通读：逐句修改对照

本文件记录 2026-09-11 全书通读在本章留下的净改动。

## 全书通读（2026-09-11，按最终文本与通读前快照逐行对照）

共 20 行有改动；英文术语按“首次出现保留中文注释、之后用英文”的原则已回退，下表是回退之后的净改动。省略号表示只截取了改动附近的文字。

| 修改前 | 修改后 |
|---|---|
| …的程序流程，再把用户问题与检索到的材料交给语言模型生成答案。这里的**上下文**是本次模型调用可以使用的输入内容，包括任务指令、参考材料和此前的交互结果。保持模… | …的程序流程，再把用户问题与检索到的材料交给语言模型生成答案。这里的**上下文**（context）是本次模型调用可以使用的输入内容，包括任务指令、参考材料和此前的交互结果。保持模… |
| …型又能根据不同上下文处理多种任务，既能支持多样的应用行为，又能让底层针对模型执行进行专门优化。**上层可编程性的变化，为跨层联合优化打开了新的空间。** 本书通过量… | …型又能根据不同上下文处理多种任务，既能支持多样的应用行为，又能让底层针对模型执行做专门优化。**上层可编程性的变化，为跨层联合优化打开了新的空间。** 本书通过量… |
| …求组织成可执行的工作。它接收请求或作业，安排批次，管理运行状态，并决定怎样使用一台或多个加速器。在推理中，它要协调正在生成的请求和新进入的请求；在训练中，它还要组… | …求组织成可执行的工作。它接收请求或作业，安排批次，管理运行状态，并决定怎样使用一个或多个加速器。在推理中，它要协调正在生成的请求和新进入的请求；在训练中，它还要组… |
| 上下文（context）是模型当前可使用的输入信息，包括用户输入、已有对话、检索结果和此前生成的内容。应用将这些信息组织成请求，交给模型服务入口。入口核验访问权限、检查配额和请求内容后，路由器依据目标模型、实例负载等条件选择副本。这里的副本指一个能够独立完成请求的模型实例，可以运行在一张卡上，也可以分布在多张卡上。路由选定实例之后，实例内部再安排这些加速器协作。 | 上下文包括用户输入、已有对话、检索结果和此前生成的内容。应用把这些信息组织成请求，交给模型服务入口。入口核验访问权限、检查配额和请求内容后，路由器依据目标模型、实例负载等条件选择一个实例；一个实例可以运行在一张卡上，也可以分布在多张卡上。选定实例之后，实例内部再安排这些卡协作。 |
| 超节点内部的互联通常称为 **scale-up**，重点是让一组加速器以较低的通信开销共同计算；多个超节点再通过网卡与交换网络 **scale-out**，扩大集群范围。两者都要考虑带宽、延迟、拥塞与故障，但协作范围和代价不同。以 NVIDIA 的 GB200 NVL72 为一个具体例子，官方设计把 36 个 Grace CPU 和 72 个 Blackwell GPU 组织在一个机架中，72 个 GPU 通过 NVIDIA 的 GPU 高速互联 NVLink 直接协作，形成同一个互联域，集群网络则承担向外连接。这一组织把紧密协作范围从单机扩大到机架，使模型可以在更多加速器之间分配权重和状态，同时通过机架内互联完成频繁交接。[^datacenter] | 超节点内部的互联通常称为 **scale-up**（纵向扩展），重点是让一组加速器以较低的通信开销共同计算；多个超节点再通过网卡与交换网络相连，称为 **scale-out**（横向扩展），用来扩大集群范围。两者都要考虑带宽、延迟、拥塞与故障，但协作范围和代价不同。以 NVIDIA 的 GB200 NVL72 为例，官方设计把 36 个 Grace CPU 和 72 个 Blackwell GPU 组织在一个机柜中，72 个 GPU 通过 NVIDIA 的 GPU 高速互联 NVLink 直接协作，形成同一个互联域，集群网络则承担向外连接。这一组织把紧密协作的范围从单机扩大到机柜，使模型可以在更多加速器之间分配权重和状态，同时通过机柜内互联完成频繁交接。[^datacenter] |
| 这就是 Amdahl 定律。即使把这部分加速到耗时可以忽略，总加速比也不超过 $1/(1-f)$。前面… | 这就是 Amdahl（阿姆达尔）定律。即使把这部分加速到耗时可以忽略，总加速比也不超过 $1/(1-f)$。前面… |
| **例：同样使用 AI coding，为什么不同团队的整体加速比不同？** 假设完成一项开发任务，需要依次经历沟通、编码、评审和审批等环节，AI coding 让编码速度提高到原来的 5 倍，其他环节的耗时保持不变。在一个协调层级较多的大公… | **例：同样使用 AI 编程工具，为什么不同团队的整体加速比不同？** 假设完成一项开发任务，需要依次经历沟通、编码、评审和审批等环节，AI 编程工具让编码速度提高到原来的 5 倍，其他环节的耗时保持不变。在一个协调层级较多的大公… |
| **阿姆达尔定律适用于任何只加速部分环节的完整流程。** CPU、模型推理和软件开发都可以用… | **Amdahl 定律适用于任何只加速部分环节的完整流程。** CPU、模型推理和软件开发都可以用… |
| …分**就是让不同加速器分别保存并计算模型的一部分。两张卡之间需要交换中间结果，第六章将讨论具体的切分与通信方法。 | …分**就是让不同加速器分别保存并计算模型的一部分。两张卡之间需要交换中间结果，第 6 章将讨论具体的切分与通信方法。 |
| …约占 $73.73+2.68+2.15=78.56$ GB，符合这一容量预算。第二章将从模型层数与注意力结构推导 KV 大小，并计算更长上下文和更多并发请求的内存… | …约占 $73.73+2.68+2.15=78.56$ GB，符合这一容量预算。第 2 章将从模型层数与注意力结构推导 KV 大小，并计算更长上下文和更多并发请求的内存… |
| …en 数增加时，同一权重矩阵处理更多 token 的特征向量，运算量随之增加。第二章将展开真实模型，在这部分线性工作之外加入随上下文长度变化的注意力交互。 | …en 数增加时，同一权重矩阵处理更多 token 的特征向量，运算量随之增加。第 2 章将展开真实模型，在这部分线性工作之外加入随上下文长度变化的注意力交互。 |
| …$B$ 增大则增加本次运算量：同一份权重要与更多请求当前输入的 token 向量进行乘加运算。第二章将进一步加入各请求独立的上下文状态。 | …$B$ 增大则增加本次运算量：同一份权重要与更多请求当前输入的 token 向量做乘加运算。第 2 章将进一步加入各请求独立的上下文状态。 |
| …速度加倍，权重减半则让待读取的数据减少一半，两种办法在这条算式中取得相同结果。第二、五章将讨论低位宽表示的格式和转换过程。 | …速度加倍，权重减半则让待读取的数据减少一半，两种办法在这条算式中取得相同结果。第 2 章和第 5 章将讨论低位宽表示的格式和转换过程。 |
| …/\beta$ 时，读取所需时间更长；超过这个比值后，计算所需时间更长。这就是第四章 Roofline 模型的出发点。[^roofline] | …/\beta$ 时，读取所需时间更长；超过这个比值后，计算所需时间更长。这就是第 4 章 Roofline 模型的出发点。[^roofline] |
| …tch 测量三次。[^measurement] 吞吐表示单位时间内产生的输出数；TPOT（time per output token）在这里表示客户端观察到的平均输出间隔。下面先看整批输出速度，再看单个请求的生成… | …tch 测量三次。[^measurement] 吞吐表示单位时间内产生的输出数；**每输出 token 时间**（time per output token，TPOT）在这里表示客户端观察到的平均输出间隔。下面先看整批输出速度，再看单个请求的生成… |
| …所有请求的平均值。前者描述整个实例的输出速度，后者描述单个用户经历的生成节奏。第三章将进一步分析请求到达、排队和少量慢请求对这些指标的影响。 | …所有请求的平均值。前者描述整个实例的输出速度，后者描述单个用户经历的生成节奏。第 3 章将进一步分析请求到达、排队和少量慢请求对这些指标的影响。 |
| …核留给租户的应用，又能快速更新网络隔离、转发和访问控制规则。云服务器上的网络处理就是一个例子：CPU 除了运行应用，还要处理收发的数据包。虚拟机是在一台物理主机上划分出的独… | …核留给租户的应用，又能快速更新网络隔离、转发和访问控制规则。云服务器上的网络处理占用的正是这些 CPU 核：CPU 除了运行应用，还要处理收发的数据包。虚拟机是在一台物理主机上划分出的独… |
| …备更方便地访问其他设备上的内存和数据。UB 因而尝试统一设备之间的访问与通信机制。统一互联希望降低不同设备之间访问和交接的代价，使上层可以采用更灵活的资源组织。统一的访问机制使设备能够直接使用更大范围内的资源，而拓扑决定这些访问的距离与带宽。模型分工与互联设计由此紧密相连。6.5.5 节从超节点规模的角度讨论 UB 的组织方式，7.3 节和 7.4 节沿着一次远程访问的路径推导它的延迟、请求速率和连接状态。 | …备更方便地访问其他设备上的内存和数据。UB 因而尝试统一设备之间的访问与通信机制，降低不同设备之间访问和交接的代价，让上层可以更灵活地组织资源。统一的访问机制使设备能够直接使用更大范围内的资源，拓扑则决定这些访问的距离与带宽。模型分工与互联设计由此紧密相连。第 6.5.5 节从超节点规模的角度讨论 UB 的组织方式，第 7.3 节和第 7.4 节沿着一次远程访问的路径推导它的延迟、请求速率和连接状态。 |
| …变执行时间的下界；若优化的是完整任务的一部分，还要用 Amdahl 定律检查收益上限。 | …变执行时间的下界；若优化的是完整任务的一部分，还要用 Amdahl 定律检查收益的上限。 |
| …与读取所需的最短时间，完整执行还要沿算子顺序加入其他运算、实际访存和启动时间。第二至第五章将逐项建立这些计算。 | …与读取所需的最短时间，完整执行还要沿算子顺序加入其他运算、实际访存和启动时间。第 2 至第 5 章将逐项建立这些计算。 |

## 翻译腔审查（2026-09-11）

作者确认术语原则并统一“笔者”“N 个 token”“Queqiao”之后，再做一遍只针对翻译腔的审查（含图注里汉字数字改阿拉伯数字）。共 20 行有改动，按最终文本与审查前快照逐行对照。

| 修改前 | 修改后 |
|---|---|
| …计问题：数据能否放下，回答要等多久，增加加速器或改变数据表示能带来多少收益。最后通过真实架构的形成过程，理解需求如何推动系统设计。 | …计问题：数据能否放下，回答要等多久，增加加速器或改变数据表示能带来多少收益。最后回顾几个真实架构的形成过程，看需求怎样推动系统设计。 |
| …执行做专门优化。**上层可编程性的变化，为跨层联合优化打开了新的空间。** 本书通过量化分析研究这些优化机会，解释原有设计取舍如何改变，以及新的实现怎样提高训练和推理的性能。 | …执行做专门优化。**上层可编程性的变化，为跨层联合优化打开了新的空间。** 本书用量化分析研究这些优化机会，解释原有的设计取舍怎样改变，新的实现又怎样提高训练和推理的性能。 |
| 下一层是**模型与负载**。模型结构规定要进行哪些计算、保留哪些数据；实际负载则决定这些工作何时到来、持续多久。一个简短的问题… | 下一层是**模型与负载**。模型结构规定要做哪些计算、保留哪些数据；实际负载则决定这些工作何时到来、持续多久。一个简短的问题… |
| …块之间跨越的是片内存储、显存还是网络。第 6 章会沿着这条线索解释各种并行策略，无需把它们当作互不相关的技巧。 | …块之间跨越的是片内存储、显存还是网络。第 6 章会沿着这条线索解释各种并行策略，不必把它们当作互不相关的技巧。 |
| 这些层次之间的接口，也是开展联合优化的切入点。模型改变状态表示，会改变存储和通信需求；编译器将相邻算子融合，… | 这些层次之间的接口，也是联合优化的切入点。模型改变状态表示，会改变存储和通信需求；编译器将相邻算子融合，… |
| …一个模型可以支持问答、代码生成等多种应用；一次模型调用又可能分布在许多加速器上，使后续计算必须等待网络传输完成。基础设施既要服务独立请求，也要组织加速器之间的紧密协作。 | …一个模型可以支持问答、代码生成等多种应用；一次模型调用又可能分布在许多加速器上，后续计算就必须等网络传输完成。基础设施既要服务独立请求，也要组织加速器之间的紧密协作。 |
| 文本随后被转换成 token 序列。词元（token）是模型处理文本的基本单位，可能对应一… | 文本随后转换成 token 序列。词元（token）是模型处理文本的基本单位，可能对应一… |
| …同一个互联域，集群网络则承担向外连接。这一组织把紧密协作的范围从单机扩大到机柜，使模型可以在更多加速器之间分配权重和状态，同时通过机柜内互联完成频繁交接。[^datacenter] | …同一个互联域，集群网络则承担向外连接。这一组织把紧密协作的范围从单机扩大到机柜，模型可以在更多加速器之间分配权重和状态，频繁的交接则由机柜内互联完成。[^datacenter] |
| …以省去解码器主体计算，生成新 token 时仍依次经过编码器与解码器。输入与输出在执行路径上的非对称性，使模型能够针对 Agent 输入多、输出相对少的负载重新安排计算投入。[^v41-case… | …以省去解码器主体计算，生成新 token 时仍依次经过编码器与解码器。输入与输出走的执行路径并不对称，模型因此能针对 Agent 输入多、输出相对少的负载重新安排计算投入。[^v41-case… |
| …1 ms，总时间就是 201 ms。把计算速度提高一倍，只能省下 0.5 ms；通过连续放置相关数据，将寻道次数从 20 次减为 10 次，则省下 100 ms。两种优化分别作用于… | …1 ms，总时间就是 201 ms。把计算速度提高一倍，只能省下 0.5 ms；把相关数据连续放置，将寻道次数从 20 次减为 10 次，则省下 100 ms。两种优化分别作用于… |
| 把时间分解后，优化方向就可以通过计算来判断：某一部分在原总时间中占比越大，缩短它越有可能改善整体。计算只占 $1/201$… | 把时间分解后，优化方向就可以算出来：某一部分在原总时间中占比越大，缩短它越有可能改善整体。计算只占 $1/201$… |
| …似表示原来的 16 位浮点权重，并用缩放因子将整数还原为相应范围内的近似值。每个被量化的权重由 2 字节减为 1 字节，因而能显著降低内存占用。 | …似表示原来的 16 位浮点权重，并用缩放因子将整数还原为相应范围内的近似值。每个量化后的权重从 2 字节减为 1 字节，内存占用因而明显下降。 |
| 计算量也可以先做一个近似。对于一次矩阵与向量的乘法，每个权重通常参与一次乘法，所得乘积再累加到结果中；按乘法与加法分别计数，大约对… | 计算量也可以先做一个近似。一次矩阵与向量的乘法中，每个权重通常参与一次乘法，所得乘积再累加到结果中；按乘法与加法分别计数，大约对… |
| 另一种办法是减少需要读取的数据。在运算量不变的条件下，将权重大小减半，读取下界也降到约 10.45 ms。带宽翻倍让读取速度加倍，权重减半则让待… | 另一种办法是减少需要读取的数据。运算量不变时，把权重减半，读取下界也降到约 10.45 ms。带宽翻倍让读取速度加倍，权重减半则让待… |
| … 上使用 BF16 权重的测量结果。vLLM 是加州大学伯克利分校等机构的研究者于 2023 年提出的开源推理服务系统，最初重点解决 KV 缓存浪费显存、限制批量的问题；它组织模型请求并执行推理。本次采用 0.23 版本的 eager 模式，即按程序运行过程逐项提交加速器工… | … 上使用 BF16 权重的测量结果。vLLM 是加州大学伯克利分校等机构的研究者在 2023 年提出的开源推理服务系统，负责组织模型请求并执行推理，最初重点解决的是 KV 缓存浪费显存、限制批量的问题。本次采用 0.23 版本的 eager 模式，即按程序运行过程逐项提交加速器工… |
| …同使用模型权重，但各自保存上下文状态。并发增加时，一次矩阵运算会同时处理更多请求的当前输入 token 所对应的向量，权重得到更多复用；与此同时，注意力计算和上下文访问也随请求数增加。 | …同使用模型权重，但各自保存上下文状态。并发增加时，一次矩阵运算会同时处理更多请求当前输入 token 的向量，权重得到更多复用；与此同时，注意力计算和上下文访问也随请求数增加。 |
| …供 Accelerated Networking（加速网络）服务。FPGA 是可通过编程配置硬件逻辑的芯片，使网络处理能够随策略更新而改变。[^azure] | …供 Accelerated Networking（加速网络）服务。FPGA 是可以用编程配置硬件逻辑的芯片，网络处理因此能随策略更新而改变。[^azure] |
| …制，降低不同设备之间访问和交接的代价，让上层可以更灵活地组织资源。统一的访问机制使设备能够直接使用更大范围内的资源，拓扑则决定这些访问的距离与带宽。模型分工与互联设计由此… | …制，降低不同设备之间访问和交接的代价，让上层可以更灵活地组织资源。统一的访问机制让设备能直接使用更大范围内的资源，拓扑则决定这些访问的距离与带宽。模型分工与互联设计由此… |
| [^nic]: [作者博士论文](../references/files/papers/bojiel… | [^nic]: [笔者博士论文](../references/files/papers/bojiel… |
| 应用通过模型与上下文表达行为，使许多任务共享相近的底层计算。掌握模型中的计算依赖和状态驻留时间，就能统筹考虑原来分属不同… | 应用用模型与上下文表达行为，许多任务因此共享相近的底层计算。掌握模型中的计算依赖和状态驻留时间，就能统筹考虑原来分属不同… |
