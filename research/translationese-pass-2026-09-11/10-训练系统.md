# 第 10 章《训练系统》翻译腔清扫（2026-09-11）

改动总数：18 处。改动以理顺句式为主，均为语言层面的调整，未改动任何技术内容、数字、公式、单位、代码块、图注路径、脚注与标题。本章此前的语言通读已比较充分，本轮只针对仍带翻译腔的句子做了点修，已通顺的句子一律未动。

| 修改前 | 修改后 |
|---|---|
| …文献在已训练的最大模型收敛时测得的值约为一到二百万 token。 | …文献在已训练的最大模型上测得，收敛时约为一到二百万 token。 |
| …而“混合精度”这个名称本身没有给出梯度格式。 | …而“混合精度”这个名称本身并不规定梯度格式。 |
| 除了各类数据占多大空间，还要区分它们需要保留多久。 | 除了要知道各类数据占多大空间，还要区分它们需要保留多久。 |
| 已发表的训练使用 8 到 32K 的缩放因子。 | 已发表的训练工作使用的缩放因子在 8 到 32K 之间。 |
| 与 BF16 对照训练相比，其相对损失误差低于 0.25%。 | 与 BF16 的对照训练相比，相对损失误差低于 0.25%。 |
| …九层阶段因此从 135 MiB 降到 45 MiB，即约 142 MB 降到 47 MB。 | …九层阶段因此从 135 MiB 降到 45 MiB，即从约 142 MB 降到 47 MB。 |
| 设微批次 $k$ 的有效 token 的索引集合为 $S_k$… | 设微批次 $k$ 中有效 token 的索引集合为 $S_k$… |
| 矩阵反向中，输入梯度 $dX$ 使前一层继续反向，参数梯度 $dW$ 使本层开始归约。 | 矩阵反向中，输入梯度 $dX$ 交给前一层继续反向，参数梯度 $dW$ 交给本层的归约。 |
| …这也是训练调度需要同时查看前后向依赖的原因。 | …这也是训练调度要同时考虑前后向依赖的原因。 |
| …训练检查点要保存与同一训练进度对应的全部必要状态，使恢复后能够接着执行。 | …训练检查点要保存与同一训练进度对应的全部必要状态，恢复后才能接着训练。 |
| …两份 Adam 状态 8 bytes，共每参数 14 bytes。 | …两份 Adam 状态 8 bytes，每参数合计 14 bytes。 |
| 采用故障发生频率较低且彼此独立的一阶模型，单位有用训练时间的附加成本为 | 假设故障发生频率较低且彼此独立，在一阶模型下，单位有用训练时间的附加成本为 |
| 一张慢到 $\mu+3\sigma$ 的卡让其余各卡比没有它时多等多久，取决于其余 $N-1$ 张卡的最大值本来有多大： | 一张慢到 $\mu+3\sigma$ 的卡，会让其余各卡比没有它时多等多久？这取决于其余 $N-1$ 张卡的最大值本来有多大： |
| OPD 相比固定数据 SFT 的潜在优势，主要是达到目标能力所需的学习样本或更新次数可能减少。 | 与固定数据的 SFT 相比，OPD 的潜在优势在于：达到目标能力所需的学习样本或更新次数可能减少。 |
| 权重同步中还有一类权重的生命周期不同。固定权重加速器可以运行… | 权重同步中还有一类权重，生命周期不同：固定权重加速器可以运行… |
| 这里首先出现差异的是生成端与训练端的前向计算；反向会把训练前向构造出的损失差异传入梯度。 | 两端最先出现差异的是前向计算；反向会把训练前向构造出的损失差异传入梯度。 |
| 让训练与部署使用同一条路径的思路，在实际模型中已经出现。 | 让训练与部署使用同一条路径的做法，在实际模型中已经出现。 |
| 数值一致性和采样策略相对于当前策略的滞后程度由此共同影响有效训练样本数。 | 数值是否一致、采样策略滞后当前策略多少，由此共同影响有效训练样本数。 |
| 两者都满足容量要求，32 卡方案因完成时间超过 30 天而被排除；48 卡约 24.6 天完成… | 两者都满足容量要求；32 卡方案完成时间超过 30 天，予以排除；48 卡约 24.6 天完成… |
