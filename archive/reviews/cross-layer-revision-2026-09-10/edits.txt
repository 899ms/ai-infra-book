01 **编程抽象从操作系统到模型上下文的迁移是计算机系统领域数十年来最重要的变化之一。** 计算机系统领
01 沿这六层向下看，任务逐步变成具体的计算与数据访问；向上看，设备容量、算力与带宽决定能够承接多大的模型
01 应用通过模型与上下文表达行为，使许多任务共享相近的底层计算。掌握模型的计算关系与状态寿命，就能把原来
02 本章从一个 token 的数值表示开始，沿着一层网络追踪它如何变成下一个 token 的分数，再计算
02 上下文变为 16K 时，每条请求占 2.25 GiB，同一空间只能放两条；变为 4K 时，则能放九条
03 从应用看，上下文是编排任务的接口；从系统看，它会展开为工作量与状态寿命。追加工具返回、替换早期内容、
04 专用化利用执行中已经确定的条件重新分配硬件资源。固定算子种类使指令和控制简化，固定数据流使通信提前安
04 设计中哪些部分允许重新配置，决定了模型改变后需要更新什么。SambaNova 的数据流组织与 现场可
05 前面的流水在一块数据准备好后就开始计算，随后释放该块缓冲。逐元素运算很容易这样处理；注意力的输出却依
05 如果专用设备把模型执行从 100 μs 缩短到 10 μs，而主机每轮准备仍需 20 μs，在可重叠
06 八卡协作使单个会话的续写时间从约 132 ms 缩短到 34 ms，但四个会话排成一队，所有会话约
07 这种变化可以用第 7.2 节的消息模型解释。一条 8 KiB 消息在 25 GB/s 链路上的数据传
07 恢复时间若增至 200 μs，串行安排中的 C 要到 232 μs 才完成，独立安排中的 C 仍在
08 独立保存需要 416 块，共享后减少约 69%。这项节省来自对公共前缀的去重，36 个私有尾块仍随分
08 因此，“总 token 更少”与“本轮计算更少”并不等价。应用可以保留稳定指令和材料的位置，把变化较
09 PD 将 prefill 和 decode 分配给两个服务池，但两个池都要执行完整的前向计算。Qwe
09 B 要在 200 ms 内返回，扣除命中后 10 ms 计算，状态必须在 190 ms 内就绪。再扣
10 把训练状态分到更多设备上，可以减轻单卡的显存压力，但计算往往需要等待其他设备传来的数据。把状态移到主
10 RL 把生成、环境验证和参数更新连接成同一个反馈应用。生成端提供采样概率与专家选择，训练端据此组织更
11 模型根据上下文选择动作，执行器在已经配置好权限与资源的环境中运行工具。两条路径在同一个任务控制器处汇
11 章首平台平均使用 30 个 CPU 核，却保留 600 GiB 环境内存。原因是三轮任务中有 27
12 从片上存储到端边云，设计选择始终依赖对完整执行过程的了解。注意力的归约关系使中间矩阵可以按块处理，重
12 最终选择先满足质量和期限，再比较成本。即使平均计算速度和传输速率足够，任务仍可能因慢速轮次或丢失进度
