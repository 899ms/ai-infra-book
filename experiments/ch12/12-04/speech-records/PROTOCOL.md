# Fixed speech records for the controlled-network experiment

Use the exact Fish PCM already recorded in12-7, with its corrected WAV length fields. No new microphone recording or voice reference. Run the cached faster-whisper-base.en checkpoint on RTX/CUDA FP16, one warmup then three formal repetitions, fixed English/beam5/temperature0/noVAD/no previous-text conditioning. Time through consumption of the segment iterator, not merely its creation. Preserve every segment/word and whole recognition result; compare against the original synthesis text after declared normalization and report errors rather than requiring perfect recognition.

This is actual ASR preparation and model-time calibration for network-record replay. The existing audio is an actual TTS result and its source timing remains separate. Future transport replay must deliver the actual result bytes and retain the model service-time provenance. Replaying recorded model service is not rerunning the model under each protocol and must be labeled accordingly. Do not treat an echo payload as a recognition result.

ASR transport extension: two Docker network-none netem profiles (80ms configured RTT,20Mbit/s,loss0/0.1%), previously calibrated. Each runs three shuffled repetitions of HTTP1.1/HTTP3 × connection reuse off/on; each cell has two sequential requests, plus one warmup per protocol (26 requests/profile,52 total). Upload actual WAV; return actual UTF-8 recognition result after replaying the measured median GPU service time. Only one request is active, so no fictitious model concurrency or queue overlap is introduced. Record actual sleep start/end; this wait represents measured model service, not new GPU inference. Source is adapted from the verified transport runner; fixed upload/response SHA and protocol records are checked independently. No window/chunk/recovery tuning yet.
