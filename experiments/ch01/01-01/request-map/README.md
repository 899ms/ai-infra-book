# 1-1 基础变体：定位一次文本生成请求

这是当前正文新增的基础读图题配套材料。复用一条已经成功运行的原生生成请求，给出输入、执行、输出的路径标签，让读者将其放回六层图，区分安排工作与提供计算、存储的职责。

- 先做 [练习](EXERCISE.md)，其中附可填写的 [PNG](worksheet.png)、[SVG](worksheet.svg)、[PDF](worksheet.pdf)。
- 再看 [参考答案](ANSWER.md)及 [标注PNG](answer-map.png)、[SVG](answer-map.svg)、[PDF](answer-map.pdf)。
- 带工具、文件和环境活动的六轮Agent进阶变体仍在 [父目录](../README.md)。

这条固定查询请求7,235输入token、46输出token，正常停止，三个键的JSON值均与完整输入记录相符。它展示应用检查与模型返回的区别，不提供通用模型质量、逐算子计时或跨主机网络性能结论。

## 独立执行

```sh
python3 run.py
```

只依赖Python标准库，无需GPU、模型或相邻实验目录。脚本核对原件SHA、成功运行退出、实际接收token IDs、返回事件顺序、JSON答案及输入文本中实际键值，生成`result.json`。

重新画图使用Matplotlib：

```sh
python3 plot.py
```

`evidence/`保留原始四请求文件的完整副本而非重新包装成伪造的单请求原始日志；本题只选取`n512-r0`。来源是4-2的原生只读采集运行，`provenance.json`记录原路径及SHA。`driver.py`只作为源码证据，不由本题执行；其中模型路径、CUDA依赖和关联capture模块不属于本题的离线执行要求。原始route文件未复制，本题也不分析或宣称核验完整路由。

层次映射根据实际请求记录、配置和驱动源码解释，属于教材职责分类；并非从硬件trace自动识别出的六个串行阶段。图片为英文短标签，中文释义完整写在练习和参考答案中。

`manifest.json`封存本目录交付文件；`python3 verify.py`核验封存。原进阶实验仍独立封存，未重新运行模型、工具或计算项目。此基础材料补齐不代表全书最终复核已经进行。
