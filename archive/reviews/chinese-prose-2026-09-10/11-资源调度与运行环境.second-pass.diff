--- second-pass-before/11-资源调度与运行环境.md
+++ manuscripts/11-资源调度与运行环境.md
@@ -1,8 +1,8 @@
 # 第 11 章 资源调度与运行环境
 
-本章把用户提交、需要交付一个最终结果的工作称为**任务**；一次模型请求或一次工具执行只是其中的一个步骤。**控制器**保存任务记录并决定下一步调用什么，**执行器**实际运行工具，**运行环境**承载工具所需的文件、进程和内存。
-
-代码修复服务收到一项任务后，需要请求模型、修改文件、执行测试，并根据测试结果继续下一轮。模型调用使用推理服务，工具使用 CPU，工作目录和进程占用内存。一项任务需要协调使用这些资源，最终交付通过测试的补丁。
+代码修复服务收到用户提交的问题后，先让模型生成修改，再让工具改文件、执行测试，把测试结果交回模型，继续下一轮，直到得到通过测试的补丁。这项需要交付最终结果的工作称为**任务**；一次模型请求或一次工具执行只是其中的一个步骤。
+
+**控制器**保存任务记录并决定下一步调用什么，**执行器**实际运行工具，**运行环境**承载工具所需的文件、进程和内存。模型调用使用推理服务，工具执行使用 CPU，环境中的进程和缓存占用内存。沿着每次调用、返回和等待，可以分别计算这些资源在什么时候被使用、使用多久。
 
 前两章讨论了推理和训练服务的构建。本章研究怎样把模型服务、工具环境和共享资源组织成一个任务运行系统。我们从一个平台设计题开始，逐步讨论何时创建和释放环境、如何安排任务、如何选择模型，以及如何从失败中恢复。
 
@@ -27,6 +27,8 @@
 
 每轮先由模型用 9 秒生成工具调用，再由工具执行 1 秒。下一轮模型调用需要本轮的测试结果，因此三轮必须依次进行，总时间为 $3\times(9+1)=30$ 秒。第一轮结束于第 10 秒，第二轮结束于第 20 秒，第三轮结束于第 30 秒。
 
+等待模型给出下一次工具调用时，工具暂时不占用 CPU，但本方案仍保留它的环境。这像一项工作暂停了，材料还摊在桌面上：执行停下来，并不自动腾出已占的空间。这里具体留下的是环境的进程与内存；工作目录和工具结果已经在持久存储中，是否值得销毁环境、下轮再建，将在后文比较。
+
 在这 30 秒里，工具只占用一个 CPU 核执行三次，每次 1 秒，累计 CPU 时间为 3 秒。这里的 CPU 时间是各个核实际执行时间的总和，记作 CPU·秒：一个核执行 3 秒，或三个核各执行 1 秒，都是 3 CPU·秒。环境始终占用 2 GiB 内存，内存占用量与时间的乘积为 $2\times30=60$ GiB·秒。下面先画模型与工具的执行顺序，再以相同横轴画环境内存。
 
 ![模型调用、工具工作和环境驻留的三轮时间线](ch11/figure-11-1-timeline.svg)
@@ -60,7 +62,7 @@
 \text{资源需求率}=\text{任务到达率}\times\text{每任务的平均资源需求}.
 $$
 
-每项任务累计使用 3 秒 CPU 时间，所以每秒到达的任务合计需要 30 秒 CPU 时间，平均有 30 个 CPU 核在执行工具；每项需要 60 GiB·秒，所以平台的环境平均占用 600 GiB 内存；每项有三次 9 秒模型调用，所以平均有 $10\times3\times9=270$ 次模型调用正在处理。
+每项任务累计使用 3 秒 CPU 时间，所以每秒到达的任务合计需要 30 秒 CPU 时间，平均有 30 个 CPU 核在执行工具；每项任务的累计内存占用为 60 GiB·秒，所以平台的环境平均占用 600 GiB 内存；每项有三次 9 秒模型调用，所以平均有 $10\times3\times9=270$ 次模型调用正在处理。
 
 **例 11-1：代码修复平台为何先受环境内存容量限制？** 将需求除以章首给定的容量，CPU 占用为 $30/40=75\%$，模型调用的并发容量使用率为 $270/300=90\%$，内存占用为 $600/640\approx94\%$。内存只剩 40 GiB，可以再容纳 20 个环境；CPU 还剩 10 核。此时增加 CPU 核数，仍然无法解决环境内存不足的问题。
 
@@ -117,7 +119,7 @@
 
 *图 11-5：代码任务经过模型服务与工具环境两条路径。控制器从模型取得参数，再向环境平台发起工具操作；执行结果成为下一轮输入。虚线框标出工具环境所在的执行节点。*
 
-进程通常共享同一操作系统内核，通过地址空间和权限进行基本隔离。容器增加命名空间（让进程看到各自的进程编号、网络等资源视图）、文件系统视图和资源控制，但通常仍共享宿主内核。完整虚拟机具有客体内核（在虚拟机内部运行的操作系统内核）和虚拟硬件边界；微型虚拟机（microVM）保留虚拟机隔离模型，通过精简虚拟设备和管理功能来降低开销。沙箱是对执行约束的描述，不专指某一种实现。
+进程通常共享同一操作系统内核，通过地址空间和权限进行基本隔离。容器增加命名空间（让进程看到各自的进程编号、网络等资源视图）、文件系统视图和资源控制，但通常仍共享宿主内核。完整虚拟机运行自己的操作系统内核，并通过虚拟硬件与宿主隔离；微型虚拟机（microVM）保留虚拟机隔离模型，通过精简虚拟设备和管理功能来降低开销。沙箱是对执行约束的描述，不专指某一种实现。
 
 ![进程拥有独立地址空间，通过权限限制访问；同一节点上的进程共享宿主内核](ch11/figure-11-isolation-0.svg)
 
@@ -127,29 +129,29 @@
 
 *图 11-7：容器为任务建立各自的资源视图和配额，底层仍使用同一宿主内核。*
 
-![microVM 让每个任务环境运行独立客体内核，通过虚拟硬件访问宿主资源](ch11/figure-11-isolation-2.svg)
-
-*图 11-8：microVM 让每个任务环境运行独立客体内核，通过虚拟硬件访问宿主资源。*
-
-E2B 是面向工具执行的环境平台，Firecracker 是亚马逊云服务（Amazon Web Services，AWS）为无服务器计算开发、于 2018 年开源的轻量虚拟机监控器；其 2020 年 NSDI 论文讨论了如何在大量短时任务之间兼顾硬件虚拟化隔离、启动速度和内存开销。[^firecracker-history]E2B 使用 Firecracker microVM 运行工具环境，模板可由容器镜像构建。镜像提供文件与依赖，microVM 提供运行时的客体内核和设备边界。创建时，平台从模板建立环境；运行时，工具通过客体内核读写文件和启动进程。容器镜像决定环境中有哪些文件和依赖，microVM 则决定这些程序运行时如何与宿主系统隔离。[^e2b]
+![microVM 让每个任务环境运行独立虚拟机内核，通过虚拟硬件访问宿主资源](ch11/figure-11-isolation-2.svg)
+
+*图 11-8：microVM 让每个任务环境运行独立虚拟机内核，通过虚拟硬件访问宿主资源。*
+
+E2B 是面向工具执行的环境平台，Firecracker 是亚马逊云服务（Amazon Web Services，AWS）为无服务器计算开发、于 2018 年开源的轻量虚拟机监控器；其 2020 年 NSDI 论文讨论了如何在大量短时任务之间兼顾硬件虚拟化隔离、启动速度和内存开销。[^firecracker-history] E2B 使用 Firecracker microVM 运行工具环境，模板可由容器镜像构建。镜像提供文件与依赖，microVM 提供运行时的虚拟机内核和设备边界。创建时，平台从模板建立环境；运行时，工具通过虚拟机内核读写文件和启动进程。容器镜像决定环境中有哪些文件和依赖，microVM 则决定这些程序运行时如何与宿主系统隔离。[^e2b]
 
 模型根据上下文选择动作，执行器在已经配置好权限与资源的环境中运行工具。两条路径在同一个任务控制器处汇合：它知道模型调用何时开始、工具何时使用环境，以及哪些文件需要跨轮保存。这些信息让平台能够分别安排模型状态与工具状态的生命周期。模型计算期间可以准备下一轮环境，工具完成后可以保存文件并释放进程；后文将据此计算容量与时间开销，选择环境的创建、保留和恢复方式。
 
 ### 11.2.2 资源分配与环境创建
 
-确定隔离方式以后，平台还需为环境分配资源并装入程序所需的数据。工具平台通过 CPU 配额、内存上限以及文件和设备访问权限来分配资源。给一个等待模型的环境增加 CPU 配额，其文件和内存内容保持原样；销毁这个环境，则会释放内存，同时要求下一次执行前重建这些内容。CUDA 是 NVIDIA GPU 的编程与运行平台；验证 CUDA 程序还需向设备池申请 GPU。设备直通让客体直接操作分配给它的物理设备，可由虚拟机程序 QEMU 配合 Linux 的 VFIO 设备访问接口完成。[^ub]
-
-创建环境需要取得模板、建立私有状态并准备首次工作所需的数据。最简单的方式是完整复制模板；另一种方式是共享只读内容，仅复制需要写入的页面；还可以按访问需求装入数据。创建时少加载一些数据，可以缩短启动时间，但程序首次访问这些数据时仍需等待加载。
+确定隔离方式以后，平台还需为环境分配资源并加载程序所需的数据。工具平台通过 CPU 配额、内存上限以及文件和设备访问权限来分配资源。给一个等待模型的环境增加 CPU 配额，并不会改变其文件和内存状态；销毁这个环境，则会释放内存，同时要求下一次执行前重建这些内容。CUDA 是 NVIDIA GPU 的编程与运行平台；验证 CUDA 程序还需向设备池申请 GPU。设备直通让虚拟机直接操作分配给它的物理设备，可由虚拟机程序 QEMU 配合 Linux 的 VFIO 设备访问接口完成。[^ub]
+
+创建环境需要取得模板、建立私有状态并准备首次工作所需的数据。最简单的方式是完整复制模板；另一种方式是共享只读内容，仅复制需要写入的页面；还可以按访问需求加载数据。创建时少加载一些数据，可以缩短启动时间，但程序首次访问这些数据时仍需等待加载。
 
 ![模板保存共享的文件与依赖，私有内容保存环境修改；程序运行时还会占用进程内存和缓冲区](ch11/figure-11-template-runtime.svg)
 
-*图 11-9：模板保存共享的文件与依赖，私有内容保存环境修改；程序运行时还会占用进程内存和缓冲区。模板安装所需空间与活跃环境的内存占用分别计算。*
-
-**例 11-2：按需加载与只读共享能节省多少环境内存？** 为分析创建开销，先取章首 300 个环境中的 100 个，比较将模板内容装入这 100 个环境的内存需求。每份模板为 2 GiB，工具访问其中 512 MiB，修改 128 MiB，另有 4 MiB 私有管理开销。逐份完整复制时，每个环境需占用 2,052 MiB 本地内存，100 份约为 200 GiB；只安装访问到的内容时，每份为 516 MiB，总量约为 50 GiB。这样，同样一组工具任务所需安装的数据减少了约 150 GiB。[^lifecycle]
-
-还可以让环境共享只读内容，仅保留 128 MiB 脏页（相对于模板已经被写入修改的内存页）和 4 MiB 管理开销。此时每份为 132 MiB，100 份约为 13 GiB。未装入本地的只读页在访问时取得。若每个环境再保留 256 MiB 热点只读页，本地总量回升到约 38 GiB，换来较少的远端访问。
-
-四种安装方式依次占用约 200、50、13 和 38 GiB，并另保留一份共享的 2 GiB 模板。这组计算比较了不同数据加载方式的容量开销。章首为每个活跃环境分配的 2 GiB 内存，还包括进程、缓冲区等工具运行所需的空间。共享只读内容可以减少重复存储，将常用数据保留在本地则可以减少远端读取。具体保留多少，应根据工具首次访问哪些页，以及以后多久会再次访问这些页来决定。
+*图 11-9：模板保存共享的文件与依赖，私有内容保存环境修改；程序运行时还会占用进程内存和缓冲区。加载模板所需的空间与活跃环境的内存占用分别计算。*
+
+**例 11-2：按需加载与只读共享能节省多少环境内存？** 为分析创建开销，先取章首 300 个环境中的 100 个，比较将模板内容加载到这 100 个环境中所需的内存。每份模板为 2 GiB，工具访问其中 512 MiB，修改 128 MiB，另有 4 MiB 私有管理开销。逐份完整复制时，每个环境需占用 2,052 MiB 本地内存，100 份约为 200 GiB；只加载访问到的内容时，每份为 516 MiB，总量约为 50 GiB。这样，同样一组工具任务需要加载到内存的数据减少了约 150 GiB。[^lifecycle]
+
+还可以让环境共享只读内容，仅保留 128 MiB 脏页（相对于模板已经被写入修改的内存页）和 4 MiB 管理开销。此时每份为 132 MiB，100 份约为 13 GiB。尚未加载到本地内存的只读页在访问时取得。若每个环境再保留 256 MiB 热点只读页，本地总量回升到约 38 GiB，换来较少的远端访问。
+
+四种加载方式依次占用约 200、50、13 和 38 GiB，并另保留一份共享的 2 GiB 模板。这组计算比较了不同数据加载方式的容量开销。章首为每个活跃环境分配的 2 GiB 内存，还包括进程、缓冲区等工具运行所需的空间。共享只读内容可以减少重复存储，将常用数据保留在本地则可以减少远端读取。具体保留多少，应根据工具首次访问哪些页，以及以后多久会再次访问这些页来决定。
 
 图 11-10 把每个环境本地保存的内容展开。共享方案的主要收益来自省去重复的只读内容；把常用页放回本地，则是在容量与读取延迟之间折中。接下来还要计算：少保存这些数据，是否会让工具在首次读取时等待更久。
 
@@ -157,9 +159,9 @@
 
 *图 11-10：四种方案采用相同的横轴尺度，比较每个环境本地保存的数据。另有一份共享的 2 GiB 模板，不计入各环境的柱长。数值来自例 11-2；管理开销为每环境 4 MiB。*
 
-设取回与安装都经过同一条有效速率为 1 GiB/s 的串行通路。模板尚未缓存在本机时，需要取回 2 GiB，再安装 0.5 GiB，数据传输与安装时间至少为 2.5 秒；模板已在缓存中时，只需安装 0.5 GiB，降至 0.5 秒。两秒差额来自省去的模板传输。一般地，这条通路的传输与安装时间下界为 $(V_{\mathrm{fetch}}+V_{\mathrm{install}})/B$。
-
-如果工具首次执行时又访问 1 GiB 尚未装入的页，这条通路便再工作 1 秒。按需加载使创建接口更早返回，将尚未访问的数据留到真正需要时取得。E2B 的页与文件读取采用了这样的组织：启动过程先提供可运行的环境，随后的访问逐步补齐内容。首次工具执行的完成时间包含环境启动，以及首次访问缺失页时的等待。按需加载将一部分准备工作推迟到了工具执行期间。[^e2b]
+设取回与加载到内存都经过同一条有效速率为 1 GiB/s 的串行通路。模板尚未缓存在本机时，需要取回 2 GiB，再将 0.5 GiB 数据加载到内存，取回和加载合计至少需要 2.5 秒；模板已在缓存中时，只需将 0.5 GiB 数据加载到内存，降至 0.5 秒。两秒差额来自省去的模板传输。一般地，这条通路取回与加载数据的时间下界为 $(V_{\mathrm{fetch}}+V_{\mathrm{install}})/B$。
+
+如果工具首次执行时又访问 1 GiB 尚未加载到内存的页，这条通路便再工作 1 秒。按需加载使创建接口更早返回，将尚未访问的数据留到真正需要时取得。E2B 的页与文件读取采用了这样的组织：启动过程先提供可运行的环境，随后的访问逐步补齐内容。首次工具执行的完成时间包含环境启动，以及首次访问缺失页时的等待。按需加载将一部分准备工作推迟到了工具执行期间。[^e2b]
 
 ### 11.2.3 暂停、恢复与环境重建
 
@@ -191,7 +193,7 @@
 
 已知第一轮模型将在第 9 秒返回，平台可以在第 7 秒开始准备环境，第 9 秒执行工具，第 10 秒保存结果并销毁环境。后两轮分别在第 17 秒和第 27 秒开始准备。三次准备都在模型调用期间完成，任务仍在第 30 秒完成，环境只需在每轮准备和执行的 3 秒内保留。
 
-每项内存占用由 60 降到 $3\times(2+1)\times2=18$ GiB·秒，平台平均内存由 600 降到 180 GiB。代价是每秒创建 30 个环境，准备工作平均额外占用 $30\times0.1=3$ 个 CPU 核，CPU 需求从 30 增到 33 核。平台增加少量环境创建工作，便可节省 420 GiB 内存，释放了原先等待模型时保留的状态。
+每项任务的累计内存占用由 60 降到 $3\times(2+1)\times2=18$ GiB·秒，平台平均内存占用由 600 降到 180 GiB。代价是每秒创建 30 个环境，准备工作平均额外占用 $30\times0.1=3$ 个 CPU 核，CPU 需求从 30 增到 33 核。平台增加少量环境创建工作，便可节省 420 GiB 内存，释放了原先等待模型时保留的状态。
 
 图 11-13 和图 11-14 将这项收益放回整条任务时间线。工具的三个执行时刻没有改变，消失的是长段模型等待期间的内存占用。这里能准确安排准备时刻，是因为下一轮使用什么工具、模型何时返回都已给定；下面再考虑需要预测工具类型的情况。
 
@@ -228,9 +230,9 @@
 
 *图 11-17：预测正确且提前 2 s：准备恰好在第 4 s 调用到达时完成。*
 
-![预测正确且提前 3 s：准备在第 3 s 完成，橙色部分为等待调用的 1 s 就绪空闲](ch11/figure-11-prewarm-3.svg)
-
-*图 11-18：预测正确且提前 3 s：准备在第 3 s 完成，橙色部分为等待调用的 1 s 就绪空闲。*
+![预测正确且提前 3 s：准备在第 3 s 完成，橙色部分为等待调用的 1 s 已就绪但尚未使用的时间](ch11/figure-11-prewarm-3.svg)
+
+*图 11-18：预测正确且提前 3 s：准备在第 3 s 完成，橙色部分为等待调用的 1 s 已就绪但尚未使用的时间。*
 
 ![预测错误：第 2—4 s 准备了不需要的环境（灰色）；第 4 s 取消后，再花 2 s 准备实际所需环境](ch11/figure-11-prewarm-wrong.svg)
 
@@ -238,7 +240,7 @@
 
 由此可以比较三种减少等待的办法。缓存模板可以省去重复获取数据的开销；保留已经启动的环境，可以在调用到达后立即执行，但需要持续占用内存；每轮重新建立环境，则可以利用模型调用的时间完成准备。章首任务已经知道下一轮所需的运行时，因此在模型调用期间准备下一轮环境；多种工具交替出现时，还需在计算中计入预测命中率。
 
-本地工具重放展示了过早准备的后果：提前量从 50 ms 扩展到整段模型等待后，累计工具调用时间仍约为 0.26–0.28 秒，按采样结果计算的累计内存占用却从约 0.019 增到 0.47 GiB·秒，约增至 24 倍。增加的时间主要落在环境已经就绪、调用尚未到达的区间。只要能赶在工具调用到达前准备好环境，就没有必要更早创建。环境提前就绪后多等待的时间，都会增加内存占用。[^prewarm]
+本地工具重放展示了过早准备的后果：从提前 50 ms 准备改为在模型等待一开始就准备后，累计工具调用时间仍约为 0.26–0.28 秒，按采样结果计算的累计内存占用却从约 0.019 增到 0.47 GiB·秒，约增至 24 倍。增加的时间主要落在环境已经就绪、调用尚未到达的区间。只要能赶在工具调用到达前准备好环境，就没有必要更早创建。环境提前就绪后多等待的时间，都会增加内存占用。[^prewarm]
 
 ## 11.3 共享资源池的分配与调度
 
@@ -258,7 +260,7 @@
 
 *图 11-21：将占用 8 核的 CPU 任务从节点 1 迁到节点 2 后，节点 1 同时拥有四张 A 卡和 16 核，可以启动新作业。*
 
-假设等待节点 1 的原任务结束需要 12 秒，迁移这个任务需要 4 秒，新作业在本地执行需要 20 秒。等待后执行共需 32 秒，迁移后执行共需 24 秒，重新安排资源后，新作业提前了 8 秒完成。若另一种兼容的跨节点配置准备需 1 秒、执行需 32 秒，则立即分散启动共需 33 秒，反而更晚。三种方案的完成时间分别为
+假设等待节点 1 的原任务结束需要 12 秒，迁移这个任务需要 4 秒，新作业在本地执行需要 20 秒。等待后执行共需 32 秒，迁移后执行共需 24 秒，重新安排资源后，新作业提前 8 秒完成。若另一种兼容的跨节点配置准备需 1 秒、执行需 32 秒，则立即分散启动共需 33 秒，反而更晚。三种方案的完成时间分别为
 
 $$
 T_{\mathrm{wait}}+T_{\mathrm{run,local}},\qquad
@@ -294,7 +296,7 @@
 
 一次抢占还会丢失未保存的进度。一个已经执行 20 秒、恢复需 2 秒的作业，从最近的检查点恢复执行与从头重做之间相差多少，取决于检查点之后积累的工作。调度器既要考虑新任务能提前多久开始，也要计入保存和恢复状态的开销，以及原作业因此增加的延迟。
 
-抢占说明了让某项任务提前执行的代价。长期运行时，还需要避免同一批任务总被推迟，因此也要明确按什么标准衡量分配是否公平。两项任务各得两张 GPU，看起来份额相同；如果一种卡执行同样工作需要另一种的两倍时间，完成进度就不同。主导资源公平（DRF）把每个任务在各类资源中占比最高的一类称为主导资源，并以主导资源份额进行公平分配，Gavel 和 Pollux 等研究则进一步考虑不同设备的处理能力或训练进度。按资源份额分配，关注的是各任务获得多少资源；按完成进度分配，关注的是各任务推进得多快；限制最长等待时间，则可以避免某些任务长期得不到执行。[^scheduling]
+抢占说明了让某项任务提前执行的代价。长期运行时，还需要避免同一批任务总被推迟，因此也要明确按什么标准衡量分配是否公平。两项任务各得两张 GPU，看起来份额相同；如果一种卡执行同样工作所需的时间是另一种卡的两倍，完成进度就不同。主导资源公平（DRF）把每个任务在各类资源中占比最高的一类称为主导资源，并以主导资源份额进行公平分配，Gavel 和 Pollux 等研究则进一步考虑不同设备的处理能力或训练进度。按资源份额分配，关注的是各任务获得多少资源；按完成进度分配，关注的是各任务推进得多快；限制最长等待时间，则可以避免某些任务长期得不到执行。[^scheduling]
 
 从单次工具调用扩大到 RL 训练，资源需求还会随阶段切换。rollout 指用当前策略生成供训练使用的交互轨迹，包括模型输出及工具或环境反馈。下一节计算增加这类轨迹生成设备实际能缩短多少训练时间。
 
@@ -328,7 +330,7 @@
 T_{\mathrm{all}}\geq\max\left(4.8,\frac{6\times30}{200/8}\right)=7.2\ \mathrm{s}.
 $$
 
-六个实例全部收到权重，至少需要 7.2 秒；第一个实例可以更早收到自己的 30 GB。随后接收端把权重装入 GPU 并确认版本，调度器再向它分配请求。PolyRL 是利用临时资源扩展 RL 生成池的系统。在这里，它先将完整权重接收到 CPU 缓冲区，再交给采用张量并行（TP，将层内矩阵分给多张卡）的 GPU 组。因此，TP=2 将实例内部的计算分配到两张卡上，每个实例从外部接收的权重仍为 30 GB。
+六个实例全部收到权重，至少需要 7.2 秒；第一个实例可以更早收到自己的 30 GB。随后接收端把权重加载到 GPU 显存并确认版本，调度器再向它分配请求。PolyRL 是利用临时资源扩展 RL 生成池的系统。在这里，它先将完整权重接收到 CPU 缓冲区，再交给采用张量并行（TP，将层内矩阵分给多张卡）的 GPU 组。因此，TP=2 将实例内部的计算分配到两张卡上，每个实例从外部接收的权重仍为 30 GB。
 
 图 11-25 标出六份权重共同经过的出口。接收实例增加时，需要发送的总字节数也增加，发送端带宽却没有随之增加。权重传完后还要加载到 GPU，这些准备时间都要从临时实例可用的时间中扣除。
 
@@ -336,9 +338,9 @@
 
 *图 11-25：每个接收实例都需要一份完整的 30 GB 权重，六路传输共用 200 Gbit/s 发送出口。每个接收端为 50 Gbit/s，单份传输至少 4.8 秒，而发送端累计发送 180 GB 至少需要 7.2 秒；图中六个接收框各代表一个独立实例。*
 
-临时实例分配到资源后，需要先完成准备，剩余时间才能用于生成。设一个实例从获得资源到开始生成共需 8 秒，存活 20 秒，就只剩 12 秒生成；若存活 5 秒，则在开始工作之前已经被收回。令存活时间为 $L$、准备与恢复时间为 $T_0$，可工作的窗口就是 $\max(L-T_0,0)$。实例可用的时间越长，准备开销所占的比例越小，用于实际生成的时间比例就越大。
-
-临时实例提前被收回时，损失的不仅是尚未利用的容量，还有已经生成的内容。保存生成前缀可以把已完成工作带到新实例。新实例读入“原输入＋保存输出”，执行预填充（prefill，处理完整前缀）建立注意力上下文键值缓存（KV），然后继续生成。PolyRL 的一个批量分支按组内最短前缀对齐：两条响应已收到 4,000 和 1,000 token 时，各采用 1,000 token，共复用 2,000 token，较长那条的 3,000 token 要重新生成。将前缀长度对齐后，可以统一安排后续生成，但较长前缀中超出的部分就需要重新计算。
+临时实例分配到资源后，需要先完成准备，剩余时间才能用于生成。设一个实例从获得资源到开始生成共需 8 秒，可用时间为 20 秒，就只剩 12 秒用于生成；若只能使用 5 秒，则在开始工作之前已经被收回。令可用时间为 $L$、准备与恢复时间为 $T_0$，可工作的窗口就是 $\max(L-T_0,0)$。实例可用的时间越长，准备开销所占的比例越小，用于实际生成的时间比例就越大。
+
+临时实例提前被收回时，损失的不仅是尚未利用的容量，还有已经生成的内容。保存生成前缀可以把已完成工作带到新实例。新实例读入“原输入＋保存输出”，执行预填充（prefill，处理完整前缀）建立注意力上下文键值缓存（KV），然后继续生成。PolyRL 的一个批量分支按组内最短前缀对齐：两条响应已收到 4,000 和 1,000 token 时，各采用 1,000 token，共复用 2,000 token，较长那条的 3,000 token 要重新生成。将前缀长度对齐后，可以统一安排后续生成，但较长回答超出共同前缀的部分就需要重新生成。
 
 在一次 Qwen3-8B 对照中，保留前缀省去了 256 个重复采样 token，完整时间却约为 12.8 秒，而从头重做约为 11.6 秒。节省的 token 对应生成阶段，完整路径还包含前缀重建与重新调度。这组对照中，前缀重建与重新调度的额外时间超过了省下的生成时间。[^recovery]
 
@@ -380,7 +382,7 @@
 
 ### 11.4.1 任务质量与思考预算
 
-每轮重建环境已经把章首平台内存需求从 600 GiB 降到 180 GiB，任务完成时间仍为 30 秒，超过 24 秒目标。接下来要改变关键路径上的模型调用。如果改用一个同样满足任务通过率要求的快速服务，每次完整调用由 9 秒降到 6 秒，三轮便从 30 秒降到 21 秒。若环境始终驻留，每项占用也从 60 降到 42 GiB·秒，整个平台的内存需求从 600 降到 420 GiB；如果每轮重建环境，仍只需在各轮准备和执行期间保留环境，累计占用保持在 18 GiB·秒。模型服务的选择因此同时改变时间和环境容量。[^design]
+每轮重建环境已经把章首平台内存需求从 600 GiB 降到 180 GiB，任务完成时间仍为 30 秒，超过 24 秒目标。接下来要改变关键路径上的模型调用。如果改用一个同样满足任务通过率要求的快速服务，每次完整调用由 9 秒降到 6 秒，三轮便从 30 秒降到 21 秒。若环境始终驻留，每项占用也从 60 降到 42 GiB·秒，整个平台的内存需求从 600 降到 420 GiB；如果每轮重建环境，仍只需在各轮准备和执行期间保留环境，累计占用保持在 18 GiB·秒。模型服务的选择因此会同时影响任务耗时和环境内存需求。[^design]
 
 一次模型调用的时间和成本，包括处理输入、生成思考内容和生成可见输出三部分。思考 token 是模型用于中间推理而非直接展示给用户的生成内容。思考预算决定允许生成多长的内部推理，实际用量决定每次调用需要支付多少费用。先在输出都能通过测试的条件下，比较两种思考长度设置的单次调用成本，再比较两种服务完成三轮任务所需的成本。
 
@@ -478,7 +480,7 @@
 F+Nv<Nc,\qquad N>\frac{F}{c-v}\quad(c>v).
 $$
 
-若 $c\leq v$，API 每项成本已低于自建新增成本，固定投入又为正，因此 API 在所有使用量下都更省。若两者成功率不同，分别以 $Np_{\mathrm{self}}$ 和 $Np_{\mathrm{api}}$ 为分母，便将比较转换为每个成功任务的成本。
+若 $c\leq v$，API 每项成本已不高于自建新增成本，固定投入又为正，因此 API 在所有使用量下都更省。若两者成功率不同，分别以 $Np_{\mathrm{self}}$ 和 $Np_{\mathrm{api}}$ 为分母，便将比较转换为每个成功任务的成本。
 
 取固定支出 1,000 成本单位、自建每项新增 0.002、API 每项 0.012，并给定两种服务均能在期限内处理 200,000 项。成本相等时的任务量为 100,000 项。50,000 项时自建成本 1,100，API 为 600；200,000 项时分别为 1,400 和 2,400。固定投入在低使用量下难以摊薄，高使用量下则被较低的新增成本抵消。
 
@@ -488,9 +490,9 @@
 
 *图 11-33：自建总成本为 1,000＋0.002N，API 为 0.012N，其中 N 为提交任务数。两者成功率相同，并有足够能力满足期限。自建曲线起点较高、斜率较小，在 100,000 项时与 API 相交。*
 
-执行效率提高后，自建服务开始更省的任务量也会变化。以一组教学估算为例：用 4 张 B200 运行 DeepSeek V4-Flash，同时处理 32 项任务，每项保留 200K token 的上下文。设并发任务数和设备租价保持不变。每个输出 token 约需 25.3 ms，其中逐 token 解码生成（decode）15.9 ms，其余输入处理和上下文重建 9.4 ms。净 decode 加速两倍后，每个输出 token 所需时间降为 $15.9/2+9.4\approx17.4$ ms，总加速约 1.46 倍，每百万输出成本由约 6.0 降到 4.1。[^purchase]
-
-原先每个输出 token 所需时间中，约 63% 用于 decode，其余 37% 保持不变，因而两倍 decode 加速只能将总时间缩短约 31%。即使把 decode 时间降到零，其余处理仍需要 9.4 ms。进一步降低成本应转向输入处理和上下文重建；同一组设备在单位时间内生成更多 token，固定租价才能由更多产出分摊。
+执行效率提高后，自建服务开始更省的任务量也会变化。以一组教学估算为例：用 4 张 B200 运行 DeepSeek V4-Flash，同时处理 32 项任务，每项保留 200K token 的上下文。设并发任务数和设备租价保持不变。每个输出 token 约需 25.3 ms，其中逐 token 解码生成（decode）15.9 ms，其余输入处理和上下文重建 9.4 ms。decode 阶段的实际速度提高到两倍后，每个输出 token 所需时间降为 $15.9/2+9.4\approx17.4$ ms，总加速约 1.46 倍，每百万输出成本由约 6.0 降到 4.1。[^purchase]
+
+原先每个输出 token 所需时间中，约 63% 用于 decode，其余 37% 保持不变，因而两倍 decode 加速只能将总时间缩短约 31%。即使把 decode 时间降到零，其余处理仍需要 9.4 ms。若要突破这一限制，需进一步优化输入处理和上下文重建；同一组设备在单位时间内生成更多 token，固定租价才能由更多产出分摊。
 
 章首的代码修复平台每秒接收 10 项任务，每项调用模型三次，因此每秒调用模型 30 次。每次价格增加 0.002，平台每秒便多支出 0.06；如果这次换服务让任务从 30 秒降到 21 秒，这笔额外成本就使任务能够在 24 秒期限内完成。章末将把这笔新增模型费与节省的环境占用放在同一张方案表里。
 
@@ -504,7 +506,7 @@
 
 ![外部系统已执行并提交操作 K，确认却丢失](ch11/figure-11-commit-ack.svg)
 
-*图 11-34：外部系统已执行并提交操作 K，确认却丢失。恢复环境不会撤回外部效果；控制器按同一操作 ID 查询结果后接续任务。实线表示请求与执行，虚线表示确认及恢复查询。*
+*图 11-34：外部系统已执行并提交操作 K，确认却丢失。恢复环境不会撤回外部操作的结果；控制器按同一操作 ID 查询结果后接续任务。实线表示请求与执行，虚线表示确认及恢复查询。*
 
 假设执行一次工具需要 3 CPU·秒，结果被发送两次。如果接收端按操作标识去重，累计 CPU 时间仍为 3 秒；如果控制器因确认丢失而真的再执行一次，累计 CPU 时间就增至 6 秒。两种情况最后都可能只有一条结果，却消耗不同资源。消息去重解决前一种重复，持久结果和目标系统的幂等协议才有助于避免后一种重复。环境外已经提交的操作在快照恢复后仍然存在，恢复流程要先查询其提交状态。[^records]
 
@@ -519,7 +521,7 @@
 \overline C_{q,d}=\frac{C_{\mathrm{all}}}{N_{q,d}}.
 $$
 
-把一次失败尝试从分子删除，会漏算它消耗的模型和工具资源。比如十项任务共花 1 个成本单位，五项成功，平台每完成一项实际支付 0.2。若只统计五项成功路径自身的 0.5，就会得到 0.1，恰好漏掉另一半资源。总支出除以完成数，使成功与失败共同决定单位产出成本。
+把一次失败尝试从分子删除，会漏算它消耗的模型和工具资源。比如十项任务共花 1 个成本单位，五项成功，平台每完成一项实际支付 0.2。若只统计五项成功尝试花费的 0.5 个成本单位，就会得到 0.1，恰好漏掉另一半资源。总支出除以完成数，使成功与失败共同决定单位产出成本。
 
 有限重试树可以在部署前用于初步估算。对路径 $\pi$，令路径概率为 $p_\pi$，各步骤成本之和为 $c_\pi$，则 $E[C]=\sum_\pi p_\pi c_\pi$。成功概率是所有最终成功的执行路径概率之和。每个节点的概率都应根据此前经历的执行结果计算；如果两类失败的后续成功率不同，应拆成不同节点。
 
@@ -556,19 +558,19 @@
 
 恢复把成功比例从 80% 提高到约 99.7%，但其中约 4.7% 的提交在第 22 秒才完成。业务期限为 20 秒时，这部分工作占用资源，却没有增加按时完成数。收紧期限改变的是哪些终点被计入分母；限制重试次数则会改变执行树本身。前者改变成功任务的统计范围，后者改变实际执行的工作。[^records]
 
-模型变快以后，环境准备可能无法再与模型调用完全重叠，从而开始影响任务完成时间。假设下一次工具调用必然发生，模型还需 10 秒思考，环境创建需 6 秒，在模型开始思考时创建环境，就能让这 6 秒准备时间与模型思考重叠。若模型阶段缩短至 1 秒，同样时机开始创建仍要再等 5 秒。[^core-calculation]先只更换模型设备，保持准备策略不变；再比较更早准备、保留已就绪环境和按需等待的成本，并计入提前占用的资源，以及提前准备的环境最终未被使用时所产生的开销。
+模型变快以后，环境准备可能无法再与模型调用完全重叠，从而开始影响任务完成时间。假设下一次工具调用必然发生，模型还需 10 秒思考，环境创建需 6 秒，在模型开始思考时创建环境，就能让这 6 秒准备时间与模型思考重叠。若模型阶段缩短至 1 秒，同样时机开始创建仍要再等 5 秒。[^core-calculation] 先只更换模型设备，保持准备策略不变；再比较更早准备、保留已就绪环境和按需等待的成本，并计入提前占用的资源，以及提前准备的环境最终未被使用时所产生的开销。
 
 ![图 11-38 模型加速后，环境创建成为工具开始执行前的等待来源。两种情况都在零时刻开始创建环境；工具须同时等待模型决策与环境就绪。横轴为秒。](ch11/figure-11-environment-overlap.svg)
 
 *图 11-38　模型加速后，环境创建成为工具开始执行前的等待来源。两种情况都在零时刻开始创建环境；工具须同时等待模型决策与环境就绪。横轴为秒。*
 
-V4.1 会话在等待工具时，同时保留两组状态：操作系统管理进程、文件与环境，模型服务管理全局 KV 和局部 SWA。恢复任务需要分别准备这两组状态，再在工具结果进入下一轮模型调用时汇合；已执行的外部操作通过查询目标系统确认结果。[^v41-case]环境准备与 KV 取回能够并行时，两者中较晚完成的一项决定恢复等待。
+V4.1 会话在等待工具时，同时保留两组状态：操作系统管理进程、文件与环境，模型服务管理全局 KV 和局部 SWA。恢复任务需要分别准备这两组状态，再在工具结果进入下一轮模型调用时汇合；已执行的外部操作通过查询目标系统确认结果。[^v41-case] 环境准备与 KV 取回能够并行时，两者中较晚完成的一项决定恢复等待。
 
 ### 11.5.3 CPU、GPU、内存与服务额度的扩容选择
 
 下面根据章首提出的 24 秒完成期限，选择模型服务与环境管理方案。此前已经得到三项结果：普通服务每次调用 9 秒，三轮需要 30 秒；快速服务每次 6 秒，三轮需要 21 秒；每轮重建环境把每项环境占用压到 18 GiB·秒，准备工作额外使用 0.3 秒 CPU 时间。
 
-**例 11-9：如何组合模型服务与环境生命周期以满足期限、降低成本？** 沿用章首条件：每 0.1 秒到达一项任务，每项执行三轮；工具平台提供 40 核和 640 GiB 内存，模型服务最多同时处理 300 次调用。两种模型处理同一任务集时，最终通过测试的概率均为 95%，且任务能否通过测试与到达时刻无关。普通服务每次 0.010 成本单位，快速服务每次 0.012，均已包含输入、生成与缓存成本。CPU 按每 CPU·秒 0.0001 计费，环境内存按每 GiB·秒 0.0001 计费，均按实际使用量计算。文件持久化成本已包含在每轮工具执行中；平台共有的固定支出在四方案中相同。[^design]
+**例 11-9：如何选择模型服务与环境管理方式，才能按期完成且成本最低？** 沿用章首条件：每 0.1 秒到达一项任务，每项执行三轮；工具平台提供 40 核和 640 GiB 内存，模型服务最多同时处理 300 次调用。两种模型处理同一任务集时，最终通过测试的概率均为 95%，且任务能否通过测试与到达时刻无关。普通服务每次 0.010 成本单位，快速服务每次 0.012，均已包含输入、生成与缓存成本。CPU 按每 CPU·秒 0.0001 计费，环境内存按每 GiB·秒 0.0001 计费，均按实际使用量计算。文件持久化成本已包含在每轮工具执行中；平台共有的固定支出在四方案中相同。[^design]
 
 首先排除“只增加 CPU”。三轮工具共 3 秒，即使将它们加速两倍，普通服务仍需 $27+1.5=28.5$ 秒；把工具时间降到零，仍有 27 秒模型调用。CPU 扩容无法使这条串行路径达到 24 秒。
 
@@ -589,13 +591,13 @@
 
 以最后一行为例，三次模型调用花费 $3\times0.012=0.036$，CPU 成本为 $3.3\times0.0001=0.00033$，内存成本为 $18\times0.0001=0.0018$，合计约 0.0381。每秒进入 10 项任务，工具平均使用 33 个 CPU 核和 180 GiB 内存，模型服务平均同时处理 180 次调用，均在现有容量以内。
 
-表中的平均需求满足容量要求，接下来还要确认各阶段能否按时安排。均匀到达使我们可以直接写出一个满足容量限制的时间表。快速服务下，以每项任务的到达时刻为起点，分别在第 4–6、11–13、18–20 秒准备环境，在第 6–7、13–14、20–21 秒运行工具；准备工作累计使用 0.1 秒 CPU 时间，均匀分布在两秒内。任务每 0.1 秒到达一项，稳定运行后，每一轮的工具执行各占 10 个核，每一轮的准备工作各占一个核；活跃环境最多约 90 个，共 180 GiB。模型三轮各平均有 60 次调用正在处理，合计 180 次并发调用。按这个时间表执行，既满足各阶段的先后依赖，也没有超过平台容量。
+表中的平均需求满足容量要求，接下来还要确认各阶段能否按时安排。均匀到达使我们可以直接写出一个满足容量限制的时间表。快速服务下，以每项任务的到达时刻为起点，分别在第 4–6、11–13、18–20 秒准备环境，在第 6–7、13–14、20–21 秒运行工具；准备工作累计使用 0.1 秒 CPU 时间，均匀分布在两秒内。任务每 0.1 秒到达一项，稳定运行后，每一轮的工具执行各占 10 个核，每一轮的准备工作各占一个核；活跃环境最多约 90 个，共 180 GiB。三个模型调用阶段各平均有 60 次调用正在处理，合计 180 次并发调用。按这个时间表执行，既满足各阶段的先后依赖，也没有超过平台容量。
 
 **因此选择快速服务，每轮重建环境，并在模型调用期间完成准备。** 这个方案使用现有工具硬件即可满足 24 秒要求，并在两个可行方案中成本较低。每项按时通过测试的任务的平均成本约为 $0.0381/0.95\approx0.0401$；快速服务＋始终驻留约为 $0.0405/0.95\approx0.0426$，每轮重建环境可使这项成本降低约 6%。普通服务＋每轮重建环境虽然更便宜，但通过测试时已经超过期限。
 
-第 11.5.2 节还说明，增加重试会改变服务目标。这里正常任务在第 21 秒完成，剩余期限只有 3 秒；若最终失败后再加一次 4 秒局部修复，结果将到第 25 秒才出现。因此，本题在三轮后结束任务；未通过测试的任务直接返回失败结果。若业务把期限延长到 25 秒，局部修复才有机会增加按时成功数，届时再将条件成功概率和额外资源放入恢复树。
-
-以上选择建立在模型每轮调用 6 秒的条件下。模型继续加速时，环境能释放的空闲时间也会缩短，原来的环境策略就需要重新比较。保留环境还是重新建立环境，可以根据两种方案成本相等时的等待长度来判断。设两次工具执行之间需要等待模型 $G$ 秒。保持 2 GiB 环境，驻留成本为 $2G\times0.0001$；销毁后在下一轮末尾重新准备，内存成本为 $2\times2\times0.0001$，准备所需的 CPU 成本为 $0.1\times0.0001$，共 0.00041。两者在 $G=2.05$ 秒时相等。等待 6 秒时应重建；等待缩短到 2 秒时，保留环境开始更省，准备也占满全部可重叠窗口。模型继续加速，会改变最合适的环境策略。
+第 11.5.2 节还说明，增加重试会改变按时完成的比例。这里正常任务在第 21 秒完成，剩余期限只有 3 秒；若最终失败后再加一次 4 秒局部修复，结果将到第 25 秒才出现。因此，本题在三轮后结束任务；未通过测试的任务直接返回失败结果。若业务把期限延长到 25 秒，局部修复才有机会增加按时成功数，届时再将条件成功概率和额外资源放入恢复树。
+
+以上选择建立在模型每轮调用 6 秒的条件下。模型继续加速时，环境能释放的空闲时间也会缩短，原来的环境策略就需要重新比较。保留环境还是重新建立环境，可以根据两种方案成本相等时的等待长度来判断。设两次工具执行之间需要等待模型 $G$ 秒。保持 2 GiB 环境，驻留成本为 $2G\times0.0001$；销毁后在下一轮模型调用末尾重新准备，内存成本为 $2\times2\times0.0001$，准备所需的 CPU 成本为 $0.1\times0.0001$，共 0.00041。两者在 $G=2.05$ 秒时相等。等待 6 秒时应重建；等待缩短到 2 秒时，保留环境开始更省，准备也占满全部可重叠窗口。模型继续加速，会改变最合适的环境策略。
 
 还可以用同一个公式概括局部加速对总时间的影响。令原时间中可加速部分占比为 $f$，将这部分加速 $s$ 倍，剩余部分保持原时长，则
 
@@ -603,7 +605,7 @@
 S=\frac{1}{(1-f)+f/s}.
 $$
 
-章首工具只占 10% 时间，工具加速两倍的整体加速为 $1/(0.9+0.1/2)\approx1.05$；模型调用占 90%，若完整调用加速两倍，则为 $1/(0.1+0.9/2)\approx1.82$。因此，应先按执行顺序找出任务的主要耗时，再增加能够缩短这部分时间的资源。
+章首工具只占 10% 时间，工具加速两倍的整体加速为 $1/(0.9+0.1/2)\approx1.05$；模型调用占 90%，若完整调用加速两倍，则为 $1/(0.1+0.9/2)\approx1.82$。同样加速两倍，模型调用带来的任务时间收益更大，因为它占用了更高比例的执行时间。
 
 到这里，我们已经选定模型服务、安排好环境的创建与释放，并核对了资源容量。第 12 章将在相同任务和完成目标下，进一步比较端侧、边缘和云端的执行方式，分析传输时间对任务完成时间的影响。
 
@@ -615,20 +617,20 @@
 2. **实验 11-2〔核心〕：环境重建与快照恢复的耗时和驻留比较。** 一个环境中有可重新下载的依赖、未保存的编辑缓冲、已持久化的补丁和已提交的外部写入。逐项说明发生故障后能从哪里恢复，以及哪些内容无法直接恢复。设完整重建环境需要 5 秒，从快照恢复需要 2 秒。保存快照需要 1 秒，可以与耗时 9 秒的模型调用重叠；画出两种方案从开始恢复到下一次工具执行完成的时间线，再计算各自需要保留环境的时长。
 3. **实验 11-3：提前准备环境能省多少等待，又占用多少内存？** 创建环境需要 2 秒，对下一次调用所需环境的预测准确率为 0.6。调用到达后取消错误预测所对应的环境；每个提前准备的环境从准备开始就占用 2 GiB。分别在调用到达前 1、2、4 秒开始准备，计算调用到达后的期望等待时间，以及错误准备造成的期望累计内存占用（以 GiB·秒计）。预测错误时，在调用到达后重新创建所需环境。若正确率提高到 0.9，哪些收益变大，哪些浪费减少？
 4. **实验 11-4：不同作业完成目标下，迁移何时优于等待。** 等待资源释放需 12 秒，本地执行需 20 秒；迁移时间为未知量 $m$，被迁移任务额外推迟 $m+2$ 秒。分别以新作业最早完成、两个作业新增等待之和最小为目标，求迁移优于等待的条件。说明为何两个目标可以作出不同选择。
-5. **实验 11-5：启动耗时如何影响短期实例的有效产出。** 每个新实例传输与加载共需 8 秒，之后以每秒 100 个有效 token 的速率工作。三个实例分别存活 10、20、40 秒，求总产出及有效工作时间占比。若准备时间降到 4 秒，比较三个实例各自的产出改善，并解释短存活实例为何更敏感。
+5. **实验 11-5：启动耗时如何影响短期实例的有效产出。** 每个新实例传输与加载共需 8 秒，之后以每秒 100 个有效 token 的速率工作。三个实例分别存活 10、20、40 秒，求总产出及有效工作时间占比。若准备时间降到 4 秒，比较三个实例各自的产出改善，并解释可用时间较短的实例为何更敏感。
 6. **实验 11-6：验证进程数量与长尾样本如何决定整批完成时间。** 三条样本在 0、6、12 秒到达，验证分别需 10、2、8 秒。先求只有一个执行进程时，按到达顺序处理各条样本的开始和完成时刻；再求使用两个执行进程时，整批最早何时完成。把最后一条样本的验证时间改为 80 秒，解释增加执行进程所能节省的时间为什么会变化。
-7. **实验 11-7：缓存命中与成功率相关时的任务成本和按时成功率。** 沿用例 11-7 的 B 服务成本，改为命中时成功率 99%、未命中时 90%。推导平均每个成功任务的成本随命中率变化的关系，以及在 6 秒内通过测试的任务比例。求至少让 90% 的任务按时通过测试所需的最低命中率，并与原独立概率模型比较。
+7. **实验 11-7：缓存命中与结果正确性相关时，如何计算成本与按时成功率？** 沿用例 11-7 的 B 服务成本，改为命中时成功率 99%、未命中时 90%。推导平均每个成功任务的成本随命中率变化的关系，以及在 6 秒内通过测试的任务比例。求至少让 90% 的任务按时通过测试所需的最低命中率，并与原独立概率模型比较。
 8. **实验 11-8：自建与 API 服务的成功任务成本交点。** 自建服务的固定支出为 1,000 个成本单位，每处理一项任务另需 0.002，任务成功率为 90%；API 服务每项任务收费 0.012，成功率为 98%。两者均有足够能力满足期限。假设两种服务处理相同数量的任务，求它们的平均成功任务成本相等时的任务量。再将自建处理上限设为每月 80,000 项，讨论交点怎样影响购买决定。
 9. **实验 11-9：任务期限与提前停止如何改变重试成本和成功率。** 沿用例 11-8，将期限改为 14、18、22 秒，分别求按时通过测试的比例。再设计一种在剩余时间不足以完成下一节点时停止的策略，重新计算期望成本，并解释它与仅改变评价期限的差别。
 10. **实验 11-10〔核心〕：到达率提高后如何扩容并选择环境驻留策略。** 沿用例 11-9，将到达率增加到每秒 15 项、快速服务调用时间降到 2 秒，每项任务仍执行三轮，每轮工具执行仍需 1 秒。CPU、内存、模型调用的并发容量和单价保持原值。比较始终驻留与每轮重建环境，决定应增加哪项资源、至少增加多少，并说明环境策略是否需要改变。最后将到达方式改为每秒有 15 项任务同时到达，画出工具执行的时间线，并标出 CPU 并发占用的峰值。
 
 [^environment]: 本书 [environment-resources 固定计算](../calculations/results/environment-resources-book.md)及[工具子进程记录](../experiments/ch11/11-01/process-resources/README.md)。工具实验包含九组、36 个进程，累计内存占用由采样 RSS 对时间积分得到。
 
-[^lifecycle]: [environment-lifecycle 固定预算](../calculations/results/environment-lifecycle-default.md)区分共享／私有容量、串行字节下界和局部预热实测。本章使用固定容量预算比较四种内容安装方式。
+[^lifecycle]: [environment-lifecycle 固定预算](../calculations/results/environment-lifecycle-default.md)区分共享／私有容量、串行字节下界和局部预热实测。本章使用固定容量预算比较四种内容加载方式。
 
 [^platform]: [调度、模型路由与云端环境研究](../case-studies/platform-routing.md)，包含 ASI、RLBoost、DistRS、SpecBox。ASI 研究覆盖六个月、155,410 张 GPU，其分配比例度量设备归属。计费公式采用题设价格。
 
-[^e2b]: [E2B 固定架构](../references/outline-checks/2026-09-07/platform-routing/e2b-architecture.md)、[暂停与快照契约核对](../experiments/ch11/11-02/README.md)。实现采用 Firecracker；保存文件／内存与重建连接分别说明。
+[^e2b]: [E2B 固定架构](../references/outline-checks/2026-09-07/platform-routing/e2b-architecture.md)、[暂停与快照接口行为核对](../experiments/ch11/11-02/README.md)。E2B 使用 Firecracker。
 
 [^ub]: [UB 操作系统参考设计](../references/files/specs/UB-Software-Reference-Design-for-OS-2.0-zh.pdf)中的设备虚拟化与交付路径，作为 QEMU／VFIO 的实现例证；这里描述支持设备直通的虚拟机配置。
 
@@ -648,13 +650,13 @@
 
 [^routing]: [routing-cost 固定结果](../calculations/results/routing-cost-book.md)、[交点结果](../calculations/results/routing-cost-crossover.md)及[完整教学推导](../case-studies/routing-cost-and-completion.md)。计算采用条件独立的质量概率，成功任务数按给定概率计算。
 
-[^purchase]: 原计算精确时间为 25.317 ms，其中 decode 15.918 ms、其余 9.399 ms；正文按用途取约一位小数。此处将逐层固定开销和周期全量重建设为教学输入，详见[持续 Agent 比较基线](../experiments/ch13/13-06/single-agent-serving/COMPARISON-RESULTS.md)、[优化审计](../experiments/ch13/13-06/single-agent-serving/OPTIMIZATION-AUDIT.md)、[时间归因与敏感性](../experiments/ch13/13-06/single-agent-serving/AUDIT-RESULTS.md)。
+[^purchase]: 计算结果为 25.317 ms，其中 decode 为 15.918 ms，其余阶段共 9.399 ms。此处将逐层固定开销和周期全量重建设为教学输入，详见[持续 Agent 比较基线](../experiments/ch13/13-06/single-agent-serving/COMPARISON-RESULTS.md)、[优化审计](../experiments/ch13/13-06/single-agent-serving/OPTIMIZATION-AUDIT.md)、[时间归因与敏感性](../experiments/ch13/13-06/single-agent-serving/AUDIT-RESULTS.md)。
 
 [^records]: [任务记录与证据范围](../case-studies/evaluation-and-agent-records.md)、[文件队列故障注入](../experiments/ch11/11-09/collector-faults/README.md)、[任务退出状态记录](../experiments/ch11/11-09/exit-status/README.md)。这些记录分别用于说明结果持久化、故障诊断和成本核算。
 
 [^retry]: 精确结果：成功概率 0.99744，通过测试且按时完成概率 0.9504，每项期望成本 0.01456，累计 CPU 时间 3.376 秒，驻留 23.008 GiB·秒。计算结果分别列出资源用量和成本；各节点的给定成本已经是总价，无需再按资源用量重复计费。见[retry-paths 有限条件图](../calculations/results/retry-paths-book.md)。各节点概率与成本按题设给定，重试沿有限执行树展开；超过评价期限的任务仍继续执行到终点。
 
-[^design]: 贯穿平台与例 11-9 是确定性教学设计，输入、逐阶段时间表、成本、成本相等的条件与复算见[贯穿设计数据](ch11/platform-design.json)及[验证程序](ch11/verify.py)。40 核、640 GiB 内存、最多 300 次并发模型调用、两种服务时间／价格和 95% 质量概率为题设。完整环境准备含文件取得与初始化共 2 秒、0.1 CPU·秒，结果保存与工作状态清理包含在工具的 1 秒内；原模板容量算例单独说明内容安装。
+[^design]: 贯穿平台与例 11-9 是确定性教学设计，输入、逐阶段时间表、成本、成本相等的条件与复算见[贯穿设计数据](ch11/platform-design.json)及[验证程序](ch11/verify.py)。40 核、640 GiB 内存、最多 300 次并发模型调用、两种服务时间／价格和 95% 质量概率为题设。完整环境准备含文件取得与初始化共 2 秒、0.1 CPU·秒，结果保存与工作状态清理包含在工具的 1 秒内；原模板容量算例单独说明内容加载。
 
 [^firecracker-history]: Agache 等，AWS，[Firecracker: Lightweight Virtualization for Serverless Applications](../references/text/firecracker.txt)，NSDI 2020。
 
@@ -664,7 +666,7 @@
 
 ## 本章小结
 
-章首平台平均使用 30 个 CPU 核，环境却占用 600 GiB 内存。原因是三轮任务中有 27 秒在等待模型，而工作状态始终存在。在模型调用期间准备环境，把每项任务的累计内存占用从 60 降到 18 GiB·秒，准备工作多用 0.3 秒 CPU 时间，就能释放大量内存。
+章首平台平均使用 30 个 CPU 核，环境却占用 600 GiB 内存。原因是三轮任务中有 27 秒在等待模型，而工具环境始终占用内存。在模型调用期间准备环境，把每项任务的累计内存占用从 60 降到 18 GiB·秒，准备工作多用 0.3 秒 CPU 时间，就能释放大量内存。
 
 这项改进利用了完整任务的执行关系。环境管理器确定模型调用期间可用于准备的时间后，可以把创建提前，让进程仅在工具准备和执行期间存在；持久化文件保存各轮之间需要保留的状态。模型服务和环境平台共同安排准备时机，使原来贯穿任务全程的资源占用变成按阶段分配。
 
