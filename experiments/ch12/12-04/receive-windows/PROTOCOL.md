# Receive-window rerun with explicit TCP_NODELAY

The socket-defaults diagnostic in the identical pinned Docker image found that asyncio only applies its default TCP_NODELAY when the Python socket metadata reports IPPROTO_TCP. The earlier manual socket.socket() paths used proto0 and the fresh diagnostic reproduced NODELAY0 on client and accepted server sockets. Earlier implicit asyncio socket paths reproduced NODELAY1. Historical completed sockets were not retrospectively inspected. Preserve their window comparisons as conditional results; do not pool their TCP absolute latency with the earlier reuse batches.

This full rerun creates AF_INET/SOCK_STREAM/IPPROTO_TCP sockets and explicitly sets TCP_NODELAY=1 on the listener and client before use. Every socket_record (listener,client ready/close,server samples/upload record) asserts and records NODELAY1 and proto6. This holds the setting consistent with the earlier implicit asyncio baseline rather than attributing its difference to receive-window tuning.

Run the entire default/64KiB/4MiB receive matrix for image,ASR,TTS and the complete Computer Use trace. Preserve original source data,model/CPU service replay,294 TTS releases/PCM availability,and8 Computer Use rounds. Each workload uses three position-balanced mode orders default/64k/4m,64k/4m/default,4m/default/64k. Warmups and formal counts match earlier window runs:54 image,54 ASR,54 TTS,162 Computer exchanges=324 total,252 formal72 warmups. No new GPU inference or browser actions occur.

Every subrun uses a fresh Docker network-none/NET_ADMIN container,2CPU/2GiB,netem80ms RTT/20Mbit/s/0.1% random loss. Only one benchmark container runs at a time. Common120s request deadline,20s handshake. TCP changes only receive buffer across window modes; QUIC initial connection/stream receive credit is varied and may grow. Both protocols are rerun, including the unchanged QUIC path, so this is a fresh full batch, not cherry-picked replacement of TCP samples. Do not pool old and new batches. Report source bytes,service waits,usable-result timing,full transport/socket checks and cleanup; do not claim acoustic playback or fault recovery from a window test.
