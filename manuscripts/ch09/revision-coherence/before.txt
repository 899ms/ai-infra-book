# 第 9 章 分布式推理

一个推理实例可以跨越多张加速卡，却仍然像一台机器那样承接请求：输入进入同一个调度域，权重和历史状态由这个域共同管理，输出按同一条生成序列返回。第 6、7 章讨论了怎样构造这样的实例，第 8 章讨论了怎样利用批处理、缓存和推测执行提高它的服务能力。本章把视野扩大到实例之间：当请求越来越多、阶段需求越来越不均衡、会话状态需要在不同位置续接时，应该怎样组织整个推理服务？

最直接的办法是增加完整副本。它容易分配独立请求，也便于隔离故障，但每个副本都要准备模型，并同时承担 prefill 与 decode。另一种办法是让不同设备承担不同的工作：把两个生成阶段分开，把 attention 与专家计算分开，或者把历史状态保存在可共享的层次中。这些组织方式通常增加通信。它们的价值取决于节省的计算、驻留和等待是否超过新增代价。

本章先用 Qwen3-8B 的同一份 8K 历史比较阶段交接与缓存复用，再用 Qwen3-235B-A22B 的同一组专家比较权重搬移、就地计算和副本。前一个例子说明数据搬几次，后一个例子说明每次搬来的权重能服务多少计算；两者最终汇合为完整服务的选择。

## 9.1 从单实例到分布式服务

### 9.1.1 共置服务基线与扩展需求

考虑一个已经调优的共置实例。请求的输入长度为 $L$，总输出长度为 $G$，prefill 计算最后一个输入位置的 logits，并由此采样首个输出。因此，在本章采用的生成约定下，后续 decode 调用为 $G-1$ 次。最后一个返回 token 不必再送入模型，不能把输出 token 数直接当作 decode 调用数。

对给定请求分布，记每个请求在资源 $r$ 上消耗的平均服务时间为 $d_r$，资源具有 $n_r$ 份可独立使用的同类能力。一个必要的流量约束是

$$
\lambda d_r < n_r,
$$

其中 $\lambda$ 是到达率。若多个操作争用同一个资源，应先把它们的需求相加；只有不同资源可以独立供给时，才分别列出约束。这是稳态资源预算，尚未包含到达突发、调度限制与尾延迟。

设某类副本的 prefill 与 decode 每请求分别需要 $d_P$、$d_D$ 个副本资源秒，而且两阶段占用同一执行资源、按串行方式处理，那么 $N$ 个相同共置副本的请求率上界为

$$
\mu_{\mathrm{co}}=\frac{N}{d_P+d_D}.
$$

例如，一个请求的 P 阶段占用 0.5 秒、D 阶段占用 2 秒，每个副本每秒最多完成 $1/2.5=0.4$ 个请求。四个副本合计为 1.6 请求/s；到达率若为 2 请求/s，每秒便多积压 0.4 个请求。增加到八个副本后，上界提高到 3.2 请求/s。下一节将保持这八个副本的总资源，考察重新分工能否进一步增加服务能力。

这里的副本可以由多张卡共同构成。例如，Qwen3-235B 的一种 TP2×EP4 放置用八个 rank 处理同一批请求：每两个 rank 分担专家矩阵，四个 EP 组分别持有不同专家。attention 和 KV 在四个 EP 组间复制，因此一条请求的 8K 历史在整个组内占 5.875 GiB，是单份逻辑 KV 的四倍。八个 rank 提供的是这一实例的协作能力；要增加独立请求副本，还需另外安排完整的权重与状态。[^ownership]

### 9.1.2 完整副本、阶段分离与状态共享

增加完整副本改变请求的分配；PD 分离改变 prefill、decode 的执行位置；AF 分离改变 attention 与 FFN、专家的执行位置；共享 KV 改变历史状态的保存与读取范围。这些动作作用于不同的对象，可以逐项引入，也可能组合使用。

| 组织动作 | 希望获得的收益 | 新增的主要约束 |
| --- | --- | --- |
| 增加完整副本 | 承接更多独立请求，分散排队 | 模型复制、缓存分散、启动与保温 |
| PD 分离 | 分别配置两个阶段的能力 | KV 交接、双端驻留、两个队列 |
| AF 分离 | 让算子使用适合的计算与存储资源 | 逐层激活往返、同步、流水平衡 |
| 共享 KV | 跨实例复用历史，减少重复 prefill | 状态身份、取回、发布、失效与回收 |

这一分类避免了一种常见误判：认为“分布式程度更高”就应当更快。如果两个阶段的资源需求相近，完整副本已经能够均衡服务，分离可能只增加交接。如果许多请求共享长前缀，跨实例缓存可能比进一步拆分计算更有价值。选择的依据是当前瓶颈及其变化，而不是机制数量。

### 9.1.3 同一请求的执行路径与状态图

图 9-1 把一次多轮请求的计算阶段与状态寿命放在同一时间线上。带视觉输入时，编码阶段 E 产生编码结果 EC，P 消费这些结果与文本，D 逐步生成输出；工具返回后，新一轮 P 处理新增信息。权重通常跨请求保留，激活主要跨相邻算子传递，KV 随已处理的位置增长。工具等待期间计算可以暂停，但历史状态仍可能占据内存。

![请求阶段、实例分工与状态寿命](ch09/figure-9-1-state.svg)

*图 9-1　状态跨过计算暂停期继续存活。示意同一请求从 prefill、decode 到工具等待和下一轮的状态寿命；横轴按阶段排列，不表示相等时长，EC 仅在有视觉输入时出现。*

以稍后使用的 Qwen3-8B 为例，一份 8192 位置的 BF16 历史占 1.125 GiB。工具执行 10 秒时，GPU 可以处理其他请求，但保留这份历史仍占用 11.25 GiB·s 的容量时间；十个这样的会话同时等待，就占 11.25 GiB。暂停计算没有暂停状态的寿命，这正是共享缓存和换出策略要解决的问题。

这条请求若随后返回 129 个 token，P 先产生第一个输出，128 次 decode 再处理前 128 个生成位置。此时连续 KV 覆盖 $8192+128=8320$ 个位置，最后一个返回 token 尚未送回模型。下一轮在这 8320 个位置之后继续，先处理这个尾 token 和新增输入。保存、续接和恢复都必须遵守同一位置边界。

## 9.2 PD：阶段分离与 KV 交接

### 9.2.1 Prefill 与 Decode 的干扰及分离动机

Prefill 可以在一次调用中处理许多新位置，较大的矩阵往往有较高的权重复用。Decode 每个请求每步通常只贡献一个位置，需要反复读取权重与历史状态。两者混合执行时，一个较长的 prefill 可能推迟已有请求的下一个输出；过度优先 decode 又会延长新请求的等待。

第 8 章的分块 prefill 通过缩短单次占用来改善这种冲突。PD 分离进一步把两个阶段放入不同的服务池，使它们拥有独立的队列、批处理和资源配比。DistServe 与 Splitwise 展示了这一设计方向，但资源池的分离并不要求设备异构。相同设备也可能因调度隔离受益；异构设备是否额外有利，需要比较各自执行两个阶段的实际能力。[^pd]

PD 分开的是执行时段，两个池都要执行完整前向。Qwen3-8B 的 P 处理 8192 个输入位置，D 随后调用 128 次；每次都经过 attention 和 FFN。于是 P、D 都需要相应权重，而历史 KV 还要从 P 交给 D。独立批处理带来的收益，首先要补偿这次新增交接。

### 9.2.2 KV 交接路径与双端驻留

P 完成输入处理后，D 需要得到历史 KV 才能继续生成。对完整 GQA 状态，设层数为 $n$，历史长度为 $L$，KV 头数为 $h_{kv}$，每头维度为 $d_h$，每元素为 $b$ bytes，则逻辑状态大小为

$$
V_{KV}=2nLh_{kv}d_hb.
$$

系数 2 分别对应 K 与 V。Qwen3-8B 的固定配置为 36 层、8 个 KV 头、每头 128 维。BF16 状态每个位置占 144 KiB；8192 个位置占

$$
V_{KV}=8192\times144\ \mathrm{KiB}=1.125\ \mathrm{GiB}.
$$

**例 9.1　一次完整 KV 交接。** 假设有效单向载荷带宽为 25 GB/s，采用整份状态直接交接。纯载荷传输时间为

$$
T_{\mathrm{payload}}=\frac{1.125\times2^{30}}{25\times10^9}
\approx48.3\ \mathrm{ms}.
$$

再加一次 5 μs 的启动与同步，总时间仍约 48.3 ms：对这份大快照，固定开销只占万分之一左右，提高载荷带宽更可能改变等待时间。到 AF 的小消息交接时，这个比例会反过来。[^handoff]

在传输结束前，源端可能仍需保留完整状态，目的端也已分配接收缓冲。整份双缓冲模型因而占据两端各 1.125 GiB，共 2.25 GiB。分块传输可以缩短部分缓冲的寿命，但必须保证尚未使用完的块不被覆盖，且接收端对可用前缀有一致认识。如果经过 host staging，还需加入主存缓冲、GPU—CPU 拷贝与实际串行依赖。

吞吐与单次等待有不同的限制。每秒 8 次上述交接需要约 9.7 GB/s 的载荷服务；每秒 16 次则需要约 19.3 GB/s。某条 11 GB/s 链路可能满足前者的平均需求，却仍让每个请求等待传输；对于后者，即使两池有空闲计算能力，网络队列也会持续增长。平均流量能通过是必要条件，不是 TTFT 保证。

### 9.2.3 阶段能力、设备选择与池配比

阶段能力应先换算到相同请求单位。设一个请求有 $L_{new}$ 个需要 P 新处理的位置、$G-1$ 次 decode 调用。某类 worker 的有效能力分别为 $r_P$ 个新位置/s 与 $r_D$ 次 decode/s，则它的每请求资源需求为

$$
d_P=\frac{L_{new}}{r_P},\qquad d_D=\frac{G-1}{r_D}.
$$

两种 token/s 的分母对应不同工作，不能直接相除得到设备配比。对异构 worker，应先求各类每请求需求，再枚举整数分配。这里的 worker 可以是一张卡，也可以是一个已经满足容量与并行条件的多卡实例。

**例 9.2　固定八个 worker 的配比。** 使用 8192 个输入位置、129 个输出 token。两类 worker 各四个，有效阶段能力按下表给定。这些能力是教学输入，不对应 A100、H20 的峰值或实测。

| worker 类型 | P 能力，新位置/s | D 能力，调用/s | 每请求 P 资源秒 | 每请求 D 资源秒 |
| --- | ---: | ---: | ---: | ---: |
| A，偏向 prefill | 16384 | 64 | 0.5 | 2 |
| B，偏向 decode | 4096 | 256 | 2 | 0.5 |

共置时，每个 worker 消耗 2.5 资源秒/请求，八个合计 3.2 请求/s。把四个 A 分给 P、四个 B 分给 D，两池分别提供 8 请求/s。25 GB/s 链路在该载荷下的容量约 20.7 请求/s，于是联合上界为

$$
\mu_{PD}=\min\left(\mu_P,\mu_D,\frac{B_{net}}{V_{KV}}\right)=8\ \text{请求/s}.
$$

分离后，两类 worker 都避开了各自较慢的阶段，请求率上界从 3.2 增至 8 请求/s，提高了 2.5 倍。模型仍处理同样的 8192 个输入位置与 128 次 decode，差别在于由谁执行。

再作一个对照：假设八个 worker 完全相同，每请求 P、D 各占 1 秒。共置上界为 $8/(1+1)=4$ 请求/s；四个做 P、四个做 D，两池也都只能提供 4 请求/s。此时分工没有节省资源秒，增加的 KV 交接只能由调度隔离等其他收益来补偿。[^pool]

![阶段配比与请求率](ch09/figure-9-2-pd.svg)

*图 9-2　阶段配比如何限制请求率。两类 worker 各四个，能力见例 9.2；横纵轴为分给 P 的数量，其余分给 D。每格取两池能力与 25 GB/s 链路容量的最小值，单位为请求/s，不含排队。*

A100 80GB SXM 做 P、H20 SXM5 96GB 做 D，可以按同样方法评价：先在同一模型、精度和请求形状下测得每种设备的两阶段能力，再与共置、同构 PD 和角色互换比较。跨代卡之间的路径应按 PCIe、NIC、RDMA 或 host staging 核实，不能假定存在直接 NVLink 互联。

### 9.2.4 不同请求与负载下的收益边界

现在只改变前缀命中。P 已经拥有 6144 个位置，只需处理剩余 2048 个位置，即原来的四分之一。一个 A 的 P 能力于是从 2 增至 8 请求/s；若仍给 P 四个 A，它们合计能处理 32 请求/s，D 却仍只能处理 8 请求/s。

把两个 A 调到 D，留下的两个 A 提供 16 请求/s 的 P 能力；D 则由两个 A 与四个 B 提供 $2\times0.5+4\times2=9$ 请求/s。联合上界提高到 9 请求/s。再调走一个 A，P 只剩 8 请求/s，反而更慢。D 假定没有前缀，因此两种配比仍须交接完整的 1.125 GiB；P 少算四分之三，并没有让这条边少搬四分之三。

再恢复原始输入，把输出从 129 增加到 1025 token。D 调用数从 128 增至 1024，扩大八倍；A、B 的 D 能力分别降为 $1/16$ 和 $1/4$ 请求/s。一个 A 做 P 已能提供 2 请求/s，其余三个 A 与四个 B 做 D，合计为 $3/16+4/4=19/16$，约 1.19 请求/s。两个 A 做 P 则只剩 1.125 请求/s 的 D 能力，更不划算。与原来四 P、四 D 相比，长输出将资源需求推向 D；若仍以 4 请求/s 到达，队列就会持续增长。

实际服务还必须测量 TTFT、ITL 与 SLO 内有效吞吐。分离可能缩短 decode 干扰，却增加首 token 之后的交接间隔；请求变长会改变矩阵效率，也会使固定 token/s 假设失效。应当用参数扫描寻找候选，再用请求重放校准最可能改变排序的量。

## 9.3 AF：算子分工与逐层交接

### 9.3.1 Attention、FFN 与专家的执行边界

AF 指 attention 与 FFN 的分工。与 PD 一次划分两个生成阶段不同，AF 在模型层内划分计算：attention 产生 hidden state，FFN 或被选中的专家处理它，再把结果返回后续依赖。下一层通常要等这一结果，因此小消息的启动与同步可能重复很多次。

MoE 为这种分工提供了一个特殊机会。总专家权重很大，但一个 token 只选择其中一部分。若大多数专家保存在主存，可以将被选中的权重搬给 GPU，也可以让 CPU 使用本地权重完成计算，再传递较小的激活。两条路径节省的资源不同，不能因为它们都使用主存就按相同流量预算。

### 9.3.2 CPU／GPU 协作：搬权重还是搬激活

KTransformers 展示了 CPU／GPU 协作的实际路径。GPU 可以处理 attention 和部分常驻专家，CPU 处理分配给自己的专家；提交 CPU 工作后，GPU 执行可并行的分支，最后同步并合并结果。热点专家、共享专家和 prefill 的放置可以与其他工作不同。实际分工应从固定配置和执行路径还原。[^kt]

![专家权重搬运与CPU就地计算](ch09/figure-9-3-local.svg)

*图 9-3　两种使用主存专家权重的方法。上方传输权重，GPU 计算；下方权重留在 CPU，往返传递激活。右侧的汇合是后续计算的必要依赖，GPU 常驻分支可在等待期间执行。示意图不代表某一项目的全部默认放置。*

对 Qwen3-235B-A22B，一个专家包含三个矩阵，参数数为 $3\times4096\times1536=18874368$。BF16 权重为 36 MiB。若同一个专家本批收到 $m$ 个 token，其矩阵工作为 $F_e(m)=2mP_e$ FLOPs，权重至少需要被访问一次。设有效 CPU 算力、DRAM 带宽分别为 $C_C,B_D$，GPU 算力、HBM 带宽为 $C_G,B_H$，交接带宽为 $B_L$。一个简化的串行路径模型是

$$
T_C(m)=2\alpha+\frac{2mhb}{B_L}
+\max\left(\frac{2mP_e}{C_C},\frac{W_e}{B_D}\right),
$$

$$
T_G(m)=\alpha+\frac{W_e}{B_L}
+\max\left(\frac{2mP_e}{C_G},\frac{W_e}{B_H}\right).
$$

前式先传输入与输出激活，再计 CPU 资源服务下界；后式先搬一份权重，再计 GPU 服务。公式中的 $\alpha$ 是每次交接的固定代价。两式不包含格式转换、激活访存与其他专家的不均衡，因此用于比较这个专家的条件边界，不能直接当作整层实测时间。

**例 9.3　复用使执行位置发生变化。** 取 $C_C=2$ TFLOP/s、$B_D=200$ GB/s、$C_G=100$ TFLOP/s、$B_H=1000$ GB/s、$B_L=25$ GB/s，$\alpha=5$ μs，BF16 激活宽度为 4096。CPU 在 $m=1$ 时只需读取本地主存中的权重，避免了较慢链路上的 36 MiB 搬移；随 $m$ 增加，GPU 的一次权重搬移被更多任务分摊，而 CPU 更早进入计算受限区。

先看八个热点专家各处理一个 token：权重合计 288 MiB。在 200 GB/s 主存上读取约 1.5 ms，加激活交接后 CPU 路径约 1.6 ms；搬到 GPU 的路径仅传权重就约 12.1 ms，连同启动与 GPU 服务约 12.4 ms。低复用时，避免大块权重搬移比提高矩阵算力更有价值。

若这八个专家各处理 128 个 token，权重仍是 288 MiB，计算量却增加 128 倍。CPU 路径增至约 20.1 ms，GPU 路径约 12.5 ms，选择随之翻转。图 9-4 展示了中间过程：在题设模型中，整数交点位于每专家 78 与 79 个 token 之间；交点附近两条路径均约 12.4 ms。这个整数边界可以精确求解，但如此接近的时间意味着很小的内核效率变化就会移动交点。[^locality]

硬件变化可以用同一组数估计。若有效主存带宽从 200 降到 100 GB/s，八份权重的读取时间从约 1.5 增至 3.0 ms；单 token 情形明显变慢，而每专家 128 个 token 时，CPU 仍主要受约 19.3 ms 的矩阵工作限制。线程绑核、NUMA 放置和 AMX／AVX-512 内核分别改变这些供给能力，测量应当针对实际行数和访问位置。量化还会同时改变权重载荷与计算效率。[^kt-audit]


![专家复用与执行位置边界](ch09/figure-9-4-reuse.svg)

*图 9-4　专家复用改变执行位置的选择。八个专家各有 36 MiB BF16 权重，有效 CPU／GPU 算力为 2／100 TFLOP/s，DRAM／HBM 带宽为 200／1000 GB/s，交接为 25 GB/s、每次启动 5 μs。曲线按例 9.3 计算单层专家路径，未含 NUMA 和格式转换。*

### 9.3.3 容量、并发与专家复用的服务边界

容量可行与服务可行是两项判断。Qwen3-235B 每层 128 个 BF16 专家共占 4.5 GiB。若每层固定 32 个专家驻留 GPU，单层为 1.125 GiB，94 层合计约 106 GiB。即使一张卡能腾出 80 GiB，也装不下这组常驻专家；还需在多卡间切分，或减少每层驻留数量。“32 个”必须对应明确的层与设备，才有容量意义。

一个 token 选择八个专家，并不意味着一个批次也只访问八个专家。设一批有 64 个 token，每个选择八个专家，共有 512 次分派。如果它们均匀覆盖全部 128 个专家，每专家处理四行；如果所有 token 都选择同样的八个专家，每专家处理 64 行。两种情况下专家矩阵的有效计算量都是 $512\times2\times18874368$，约 19.3 GFLOPs。

假定批内每份专家权重只读取一次，均匀分派读取 $128\times36=4608$ MiB，即 4.5 GiB；集中分派读取 $8\times36=288$ MiB，相差 16 倍。在同一条 200 GB/s 供数路径上，单看权重读取约为 24.2 ms 与 1.5 ms。计算量相同，访问的不同权重却相差一个数量级。

于是，总专家数决定需要安排多少权重，分派次数决定这批的有效矩阵计算，批内专家集合决定需要访问哪些权重。下一步要问的是：四行与 64 行矩阵能获得多高的执行效率，以及这些任务落在哪些设备上。

真实路由未必恰好均匀或完全集中。若仅为分析而假设每个 token 独立、均匀地从 $E$ 个专家中选择 $K$ 个不同专家，批内 $M$ 个 token 的活跃专家数期望为

$$
\mathbb{E}[U]=E\left[1-\left(1-\frac{K}{E}\right)^M\right].
$$

代入 $E=128,K=8,M=64$，期望约 126 个专家，理想权重读取约 4.43 GiB，接近上述均匀覆盖情形。随着批量继续增长，不同专家数至多达到 128，而任务数仍线性增加，新增 token 才越来越多地复用已经读入的权重。

这也解释了大主存配少量 GPU 的适用范围。V4-Flash 的公开单 RTX 5090 配置配合至少 200 GB 主存，Kimi K2 Q4_K_M 的配置需要约 600 GB 主存。主存解决了总权重的容纳问题；请求批量增大后，瓶颈仍可能从权重读取转到 CPU 矩阵计算。评价这种配置，需要沿图 9-4 的方法改变每专家行数，而不能只看模型是否加载成功。[^capacity]

### 9.3.4 跨机 AF、微批流水及与 PD 的比较

把 AF 从同机扩展到跨机，会放大启动、链路和就绪偏差的作用。MegaScale-Infer 用微批在 attention 与专家节点之间交错执行，形成 ping-pong 流水。理想情况下，若两个阶段每微批需要 $t_A,t_F$，忽略交接，处理 $q$ 个独立微批的双阶段流水需要 $t_A+t_F+(q-1)\max(t_A,t_F)$。真实逐层执行还要满足每个微批的返回依赖，加入激活往返、填充排空和共享链路占用。

例如，attention 与专家阶段每微批分别需要 2 ms、3 ms。四个微批串行执行共需 20 ms；理想双阶段流水只需 $2+3+3\times3=14$ ms，省下 6 ms。若拆小微批后，每阶段的实际服务时间变长，或往返通信暴露超过 6 ms，这项收益就会消失。微批设计因而要同时考察可重叠工作、矩阵行数和新增消息，而非只追求更多批次。[^af]

**例 9.4　字节与交接频次。** 在 Qwen3-8B 的教学放置中，把 36 层 dense FFN 放在另一侧，每层发送一个完整 BF16 hidden vector，并返回同样大小的结果。一次方向交接为 $4096\times2=8192$ bytes；一步 decode 合计 72 次、576 KiB。25 GB/s 下纯载荷约 23.6 μs，72 次 5 μs 启动合计 360 μs，串行交接约 0.384 ms。

这比例 9.1 的一次 PD 交接短，但两者对应不同工作范围。若生成 129 个输出，后续 128 次 decode 的 AF 串行交接累计约 49.1 ms；8192 个输入位置的 prefill 若也按整批 dense AF 交接，激活载荷为 4.5 GiB，约需 193 ms 纯传输，再加启动。真实系统可以改变微批与重叠，仍必须把完整请求和计算放回时间线。

![消息数量与启动开销](ch09/figure-9-5-handoff.svg)

*图 9-5　相同总载荷下，消息数放大启动开销。两条曲线总载荷均为 1.125 GiB、有效带宽均为 25 GB/s，分别串行发送 1 次和 72 次；差值为 $71\alpha$，纵轴从48.2 ms起以显示差异。这是隔离频次影响的控制，实际一步 AF 仅传 576 KiB。*

相同总字节控制更能看清频次：把同一份 1.125 GiB 载荷分成 72 次串行交接，载荷时间不变，启动开销比一次交接多 $71\alpha$，在本例中约为 0.36 ms。PD 往往先受大块 KV 搬移影响，AF 则更容易对小消息延迟和依赖敏感。MoE 的实际 dispatch、top-k 扇出与共享专家分支要另算，不能把 dense 教学边界机械用于所有专家模型。


## 9.4 MoE 服务：专家放置与负载均衡

### 9.4.1 路由倾斜与最忙设备

AF 决定算子怎样分工，专家放置决定同类专家工作分布到哪些设备。即使全系统的总计算量不变，最忙的设备也可能使整层推迟完成。对设备 $r$，设专家任务数为 $N_r$、需要读取的不同权重为 $W_r$，一个粗略的局部资源下界是 $\max(F_r/C_r,W_r/B_r)$；跨设备汇合至少要等待其中最大者。

把上一节的 512 次分派放到八张卡上，可以看到集中路由的另一面。若各卡恰好分到 64 次任务，每卡处理约 2.42 GFLOPs；若八个热点专家都位于一张卡，该卡处理全部约 19.3 GFLOPs。假设每卡有效算力都为 100 TFLOP/s，只看计算，最忙卡的服务时间从约 24 μs 增到 193 μs，扩大八倍。

集中路由把不同权重从 4.5 GiB 减到 288 MiB，却可能把最忙卡的计算扩大八倍。前者有利于读取受限的执行，后者不利于计算受限的执行。均衡策略因此要先识别限制资源，再决定是追求复用还是分散工作。

通信也依赖 token 的初始归属。在第 9.1 节引用的 TP2×EP4 同请求布局中，每个 EP 成员已拥有相同 hidden 输入，跨 EP 的输入 dispatch 可以为零，结果通过部分和归约合并。另一种布局让各设备持有不同源 token，则可能需要将激活分派给远端专家。两者不能共用一份未经修改的 All-to-All 字节表。把“MoE 必然有某种固定流量”当作模型属性，会忽略部署引入的复制与所有权差异。

### 9.4.2 专家副本、放置与动态调整

专家复制让同一逻辑专家拥有多个物理副本，调度器可以把任务分给这些副本。只要权重相同、路由权重与汇合正确，复制不必改变逻辑 top-k；修改专家选择规则则会改变模型行为，两者应分别评价。vLLM、SGLang 的 EPLB 路径把路由统计连接到物理映射，但具体可调整粒度仍需按固定实现检查。

副本消耗权重容量，也会挤占 KV 和缓冲。每卡每层增加一个 36 MiB 专家，94 层共需 3384 MiB，约 3.30 GiB；若只为八个最拥挤的层各增加一个副本，则只需 288 MiB。两者相差近 12 倍。CRAFT 的逐层预算问题由此产生：应把空间花在最能缩短等待的层，而不是机械地给每层同样多的副本。[^moe]

**例 9.5　副本需要多久才能回本？** 在八个 rank 间增加七份专家副本，共 252 MiB；七张接收卡各增加 36 MiB，均放得进 64 MiB 的额外预算。假设这次复制准备需要约 10.6 ms，此后每批从约 0.92 ms 缩短到 0.52 ms，节省约 0.40 ms。按这组路径的未舍入时间，严格获益所需批数满足

$$
N\Delta t>T_{copy},\qquad
N_{min}=\left\lfloor\frac{T_{copy}}{\Delta t}\right\rfloor+1=27.
$$

热点持续 16 批时，节省约 6.4 ms，还不足以偿还 10.6 ms 的准备；持续 64 批时，节省约 25.8 ms，扣除准备后净省约 15.2 ms。图 9-6 将一次投入与逐批收益画在同一条线上。若接收卡只有 32 MiB 空间，连一份 36 MiB 专家也放不下，此时无论热点持续多久，都应先改变放置或腾出容量。[^replica]

![专家副本的准备与回本](ch09/figure-9-6-experts.svg)

*图 9-6　热点持续多久，专家复制才值得。一次准备约 10.6 ms，每批节省约 0.40 ms；曲线使用例 9.5 未舍入时间计算，第 27 批开始净获益。七张接收卡各需额外 36 MiB，假设容量足够且热点不变。*

观察窗口决定调整速度。过短的窗口可能追逐短暂热点，造成频繁迁移；过长的窗口又可能落后于请求分布。迁移后应测量最忙卡、通信与请求尾延迟，而不能只看路由直方图是否更平。

### 9.4.3 Dispatch、计算与 Combine 的重叠

Grouped GEMM 把多个专家矩阵组织成一个执行批次，但不保证所有补齐已经消除。例如八个本地专家收到 $[32,16,8,4,2,1,1,0]$ 个 token，共 64 个有效任务行。若每个非空专家补齐到 32 行，需要 224 行；若八个专家都按最大值补齐，需要 256 行。实际执行工作分别为有效行数的 3.5 倍和 4 倍。真实后端可能使用不同 tile 或跳过策略，应按实际内核核验。

假设 dispatch、专家计算与 combine 分别需要 0.2、0.6、0.2 ms，完全串行为 1.0 ms。若可均分成两个微批，各阶段时间减半，理想三阶段流水为 $0.1+0.3+0.1+0.3=0.8$ ms，只省下 0.2 ms。若共享 HBM 使计算阶段每微批从 0.3 增至 0.4 ms，同一流水就回到 $0.1+0.4+0.1+0.4=1.0$ ms。通信确实重叠了，整批却没有变快。

这给出了测量顺序：先看同样 64 行任务实际执行了多少补齐行，再看并发传输是否延长计算，最后比较整批完成时间。只观察通信是否与计算同时出现，还不能判断那 0.2 ms 是否真正省下。[^expert-exp]

## 9.5 共享 KV：状态复用与请求路由

### 9.5.1 缓存身份、共享范围与可用状态

前缀缓存的价值来自避免重复执行，但“相同文本”不足以唯一确定所有可复用状态。模型与 adapter 版本、token 序列、位置、状态格式以及必要的编码配置都可能参与身份。对于窗口、压缩或循环状态，还要确定保存的是哪个时刻、哪些状态分量，不能只保存一段看起来相同的张量。

容量相加并不会自动形成共享缓存。两个实例各有 4 GiB 私有 host 缓存时，一份 1.125 GiB 前缀可能在两端各占一份，共占 2.25 GiB；另一个实例却仍无法直接复用它。若二者连接到同一共享后端，一份对象可以服务两端，但每端在生成前可能还要取回本地。HiCache、vLLM 的多级路径和 LMCache 提供了不同的组织方式，取回是否经 CPU 决定下一节的路径时间。[^cache]

状态从写入到复用至少经过产生、保存、发布、查询、取回和消费。路由器使用的缓存事件通常是位置与身份元数据，不携带完整 KV。GPU 副本被淘汰时 CPU 副本可能仍在；恢复目录索引也不等于恢复丢失的数据。事件遗漏、延迟或缓存回收都可能让预测位置失效。

### 9.5.2 多级存储：取回、驻留还是重算

共享 KV 的三种典型路径是本地重新 prefill、从远端取回一次并在本地驻留、每一步都从远端供数。它们可能使用同样的历史内容，却有完全不同的频次。

沿用 1.125 GiB 前缀和 25 GB/s 链路，一次载荷取回约 48.3 ms。若 100 次 decode 调用都重新远程读取这份不变前缀，累计链路服务约为 4.83 s，增长中的 KV 尚未计入。这不是单次请求延迟的自动预测：计算与传输可能重叠，状态也可能分片；但反复读取占用的资源不能因异步而消失。

一次取回并驻留将频次从“每步”降到“每次复用”，代价是占用本地 HBM。设一份状态占 $V$ bytes，保留 $\tau$ 秒，其驻留成本可用 $V\tau$ 个 byte-seconds 表示。这个量不是价格，却便于比较多个状态对稀缺容量的长期占用。远端存储可以容纳许多不活跃会话，但它能否持续供给活跃 decode，仍受访问速率与链路约束。

对于预计未来复用 $k$ 次的状态，先忽略排队和容量影子价格，保存并取回相对于重算的条件为

$$
T_{write}+kT_{read}<kT_{recompute}.
$$

例如，保存需要 50 ms，每次取回需要 60 ms，而重算需要 100 ms。复用一次时，保存加取回为 110 ms，比重算慢；复用两次时为 170 ms，比两次重算的 200 ms 少 30 ms。保存是否值得取决于未来复用次数。若链路变慢使每次取回需要 120 ms，复用越多反而损失越大；此时应重算，或设法把取回提前到关键路径之外。

容量也会改变未来能兑现多少次复用。在一组同卡双引擎实验中，12 次前缀复用机会里，4 GiB 共享 CPU 池完整命中 5 次，8 GiB 池完整命中 12 次。多出的 4 GiB 保住了七次本来会丢失的完整前缀；而已在本地驻留的再次请求，无须重复取回。于是，共享容量的收益要沿“保存多久—避免几次重算—增加几次搬移”来比较。[^shared]

### 9.5.3 持久化、检查点与部分重算

持久化把状态寿命扩展到实例重启或任务暂停之后。完整保存减少恢复时的重算，周期检查点减少写入但增加恢复工作，部分重算则利用保存前缀补齐缺失部分。DeepSeek-V4 的压缩、窗口和不完整压缩块需要分别处理；一个层次中可恢复的状态不必与另一个模型的完整 GQA 页具有相同结构。[^v4]

先考虑完整 GQA 页。Qwen3-8B 每个 16-token 逻辑页为 2.25 MiB。若各层 K、V 分片分别搬运，可能形成许多小连续段；若按页汇集，则消息数和布局转换不同。SSD 的小块 IOPS、写放大和后台队列都会影响恢复，持续顺序带宽不能直接代替这些成本。

一组重启后的请求读取了 64 页，覆盖 1024 个位置，但可复用的连续前缀只有 1008 个位置，即 63 页。多读的一页占 2.25 MiB，却没有消除相应的重算。对于这个请求，读取字节为 144 MiB，有效复用约 142 MiB；更有解释力的差别是“读入 64 页，使用 63 页”。下一步应追踪最后一页在哪个匹配边界被排除，而不是继续提高磁盘带宽。[^restart]

![读取页与可复用连续前缀](ch09/figure-9-7-cache.svg)

*图 9-7　读入的页不一定全部成为可复用前缀。正常重启后的这一请求读入 64 个 16-token 页，只复用前 63 页；末页仍需处理。每页 2.25 MiB，末页排除来自该次实现的匹配边界，非通用固定损失。*

缺失页也可能改变等待的价值。例 9.6 中本地重算只需 180 ms；若取回一个不可用对象已等待 1 秒，单为它阻塞便超过五次重算时间。实际截断页实验里，无条件等待的请求在 60 秒观察窗口内仍未完成，允许放弃等待的策略则通过重算返回。完成当前请求之后，还需要隔离或修复坏页，否则下一次请求会再次付出等待。[^fault]

### 9.5.4 缓存亲和性与完成时间路由

将请求送到有缓存的机器，可以减少计算，却可能增加排队。设 GPU 最早可执行时刻为 $Q$，状态从请求到达起的就绪时刻为 $R$，状态可用后的剩余工作为 $C$。在整份状态到齐后才执行、取回可与 GPU 等待重叠的模型中，首 token 时间为

$$
T_{first}=\max(Q,R)+C.
$$

如果取回必须等到 GPU 排队结束后才开始，依赖就变成相加；如果逐层流水，则需要更细的执行图。公式必须随路径改变。

**例 9.6　有缓存的机器还是空闲机器？** 对同一 8K 前缀，A 有 HBM 命中，但需等 250 ms；B 等 20 ms，完整重算需 180 ms；命中后的剩余工作为 10 ms。远端读取固定开销 10 ms，整份先到 host，再通过 25 GB/s 搬到 GPU。

| 路径 | 首 token 时间，教学计算 |
| --- | ---: |
| A：本地命中 | $250+10=260$ ms |
| B：直接重算 | $20+180=200$ ms |
| B：远端 5 GB/s，经 host 取回 | 约 310 ms |
| B：远端 20 GB/s，经 host 取回 | 约 129 ms |

B 要在 200 ms 内返回，扣除命中后 10 ms 计算，状态必须在 190 ms 内就绪。再扣除查找的 10 ms 与 host→GPU 的约 48.3 ms，远端读取只剩约 131.7 ms。用 1.125 GiB 除以这个预算，得到约 9.2 GB/s 的等时带宽。

把远端提高到 20 GB/s 后，单请求取回明显占优；但每秒 16 次取回需要约 19.3 GB/s，已用去约 97% 的链路能力。少量突发就可能吃掉剩余等待预算。这解释了为什么单请求的带宽阈值还必须接到持续负载的队列。[^route]

现在把 A 的队列改为 80 ms，且缓存只以概率 $p$ 真正可用。命中时为 90 ms，所有层次都失效时为 260 ms。其期望为

$$
\mathbb{E}[T_A]=90p+260(1-p)=260-170p\ \mathrm{ms}.
$$

平均优于 B 的 200 ms 需要 $p>6/17$。取 $p=0.9$，平均仅 107 ms，但 10% 的请求仍为 260 ms，因此该两点分布的 p99 为 260 ms，达不到 220 ms 的目标。路由评分中的缓存权重既不是这个概率，也不是毫秒时间预测。[^events]

一组两任务压力实验把这个冲突表现得更直接。缓存优先时，目标请求与整个任务对均约 1.38 秒完成；把目标移到空闲实例后，它缩短到约 0.33 秒，任务对却约 1.54 秒才全部完成。目标快了约 1.05 秒，整体完成却晚了约 0.16 秒。路由决策之前必须确定优化的是目标请求响应，还是所有任务排空。[^pressure]

## 9.6 服务生命周期：启动、重配置与恢复

### 9.6.1 启动、预热与准备成本的摊销

稳态吞吐假定服务已经准备好。一个新副本还要完成进程与 tokenizer 初始化、权重读取与分片、编译、KV 容量探测和图捕获。阶段日志可能嵌套：首次试运行触发编译时，不能把包含编译的试运行时间再与编译时间相加。Breaking the Ice 对历史 vLLM 版本的阶段分析说明了这个问题；当前部署仍应按自己的依赖图测量。[^startup]

假设增加图准备使启动多花 $T_s$，但每次真实执行步在关键路径上节省 $\delta$，经过 $N$ 步后的净时间收益为 $N\delta-T_s$。多花 10 秒、每步省 0.2 ms，需要 50000 步才持平，严格获益从第 50001 步开始。执行步不等于输出 token 数：一个批次可能同时为许多请求生成位置，不能用总输出数代替步数。

启动期间的新到达还会积压。若原有服务完全不可用、到达率保持 $\lambda$，启动时间为 $T_s$，启动结束时积压约为 $\lambda T_s$。就绪后能力为 $\mu>\lambda$，在理想连续流量模型中，额外排空时间为

$$
T_{drain}=\frac{\lambda T_s}{\mu-\lambda}.
$$

例如，启动需要 10 秒，期间每秒到达 4 个请求，就绪时已有 40 个请求积压。若就绪后每秒服务 8 个，新请求仍占去 4 个，实际每秒只能消掉 4 个旧请求，还需 10 秒排空；若服务能力只有 5 请求/s，每秒仅消掉一个旧请求，要再等 40 秒。相同的 10 秒启动，对应从开始启动算起 20 秒或 50 秒的排空时间，如图 9-9 所示。

并发预取还可能改变谁先开始执行。一个 1024 位置的请求在单独运行时复用 1008 个位置；八个请求同时到达时，前四个各登记 1024 位置预取，占用达到 4096，后续请求因超过容量限额而未登记。首个完成的请求恰来自后者，以零命中开始 prefill。它没有等缓存，因而更早进入执行队列；目录中的数据是否存在，并不单独决定启动后的请求顺序。[^branch]

### 9.6.2 运行中重配置与状态交接

改变 TP、EP 或服务池规模，需要重新安排权重与请求状态。目标设备获得参数只是第一步，还可能需要建立通信组、准备新图、转换布局并恢复接流。静止快照的网络载荷下界不能替代整个切换时间。

计划迁移可以利用源端仍在运行这一条件。先后台复制不再改变的历史，源端继续生成；最终短暂停止，追平新增状态并将执行权交给目标。若状态增长速率不低于复制能力，后台追赶就不能在简单模型中收敛。可变压缩状态、窗口与循环状态还需要一致交接点，不能只复制一组过期的数组。

运行中并行切换提供了另一种粒度。ArcticInference 与 vLLM 的固定案例比较 TP 和 SP×TP，要求检查 KV 布局、额外权重、图准备及每步实际 token 数。额外驻留会挤占 KV，切换阈值应由完整请求分布校准。Elastic EP 改变专家组规模时，也必须区分专家权重已经移动和请求 KV 已经完成交接。[^switch]

一个 TP4→TP8 迁移例子需要约 16.4 GB 网络传输，另有约 4.7 GB 本地物化读写。把同一份网络载荷放到有效 5 GB/s 和 25 GB/s 的路径，单看搬移约需 3.3 秒和 0.66 秒，相差五倍。

但若另假设建组、图准备与接流在传输后串行占用 9 秒，总时间便约为 12.3 秒和 9.7 秒，只缩短约 21%。继续提高带宽的收益已经被这 9 秒限制。下一步应测量准备能否提前、哪些状态必须在暂停时交接，而不只是追求更快的网络。[^migration]

### 9.6.3 部分故障与流式生成恢复

生成服务已经向用户交付的 token 不能被当作从未发生。故障后有三种不同目标：恢复足够状态继续 decode；根据已经确定的 token 重做 prefill；重新采样一段输出。前两者努力保持已交付序列，第三种可能改变用户结果，在 RL 中还可能改变采样分布。

假设原输入有 8192 个位置，已经交付 129 个输出，但最近保存的 KV 仅覆盖原输入。恢复时，若这些输出已可靠记录，只需把前 128 个输出补入模型，重建到 8320 个 KV 位置，再处理第 129 个输出以继续生成。若已有 8320 位置的兼容 KV，则可省去这 128 个位置的重放。

Token 级 WAL 保存的是已经确定的序列，KV 保存的是这段序列已经完成的计算。前者决定恢复后应继续哪条输出，后者决定还要重做多少工作。DeepSeek-V4 的生成服务采用这类机制；恢复时还要保持权重版本、位置和解码状态一致。[^v4]

故障范围由同步组决定。TP 或 EP 的一个 rank 丢失，可能使整个协作实例上的请求停住；完整副本的其他实例则可能继续服务。恢复时应依次观察检测、重建、首个有效 token 与积压消退，并记录哪些输出已交付。第 10 章进一步分析这些选择对 RL 样本与训练进展的影响，第 11 章再处理工具和环境的任务恢复。

## 9.7 完整部署的选择与验证

### 9.7.1 组合副本、PD、AF 与共享 KV

前面各节回答的是局部问题，部署决定必须把它们放回同一请求。P 产生的 KV 可以直接交给 D，也可以先进入共享池再由 D 取回；D 新生成的状态若要用于下一轮 P，还需确认它们已经保存并发布。直接交接与经池中转可能读取相同内容，却占用不同链路和缓冲。

![直接交接与共享池中转](ch09/figure-9-8-composition.svg)

*图 9-8　直接交接与共享池中转的搬移次数。一份历史为 1.125 GiB；直传经过一条边，中转经过两条边。后续实例可从池中取回同一状态，是否值得取决于它替代了多少重算。假设整份写入后发布，不含元数据和格式转换。*

对本章的 1.125 GiB 历史，直接 P→D 交接搬一次；经池中转则在 P→池、池→D 两条边各搬一次，总载荷为 2.25 GiB。假设两条边都为 25 GB/s，且整份写入后才能读取，中转等待约 96.6 ms，比直接交接多约 48.3 ms。

若状态只被 D 使用一次，这次中转没有获得复用收益。若后面还有另一个实例要续接，它便有机会用约 48.3 ms 的读取替代一次 180 ms 的重算，节省约 132 ms，超过先前多花的约 48.3 ms。于是，多一次搬移可以值得，但必须有后续复用来偿还它。AF 的逐层激活仍按每层每步另外发生，不能与这笔跨轮收益抵消后省略。

同样的规则适用于视觉。E 产生的 EC 与语言 KV 具有不同身份和容量；图像缓存命中可能减少 E 的需求，却不自动消除后续语言 P。扩展到 E→P→D 时，应沿第 3 章的同一图像和请求核算，不把编码阶段视为免费输入。

### 9.7.2 同质量、同资源约束下的方案比较

比较部署至少需要固定模型与允许的质量范围、请求分布、资源上限和服务目标。资源上限可以是设备数、费用或功率，但并不要求所有设备属于同一个同步域。若一组卡组成多个独立副本更适合负载，就应进入候选。

先完成两套固定配置的比较，便于归因；再给双方相同调优机会，允许调整量化、并行、批处理、副本数和池配比。最低延迟、SLO 内最大吞吐与单位有效产出成本可能对应不同方案。可以定义

$$
C_{effective}=\frac{\text{比较窗口内的完整费用}}
{\text{满足质量与时限的有效产出}}.
$$

有效产出可以是请求、任务或合格 rollout，但两方案必须使用同一个分母。若 Agent 更早退出却没有完成任务，其较少时间和 token 不能被直接解释为服务效率提高。

例如，假设两个候选每小时完整费用都为 8 元，满足同样质量与时限的请求率分别为 3.2 和 8 请求/s。每小时有效请求分别为 11520 和 28800，单位成本约为每千请求 0.69 元和 0.28 元。若后一方案只有四分之一请求满足时限，有效能力降为 2 请求/s，成本反而升至每千请求约 1.11 元。

设备费用与平均吞吐没有变，排序却因合格产出改变。真实部署应先测得这一分母，再将主存、网络、存储和热副本费用加入分子；这里的价格与合格比例是说明方法的假设。[^cost-model]

### 9.7.3 负载变化后的选择翻转与设计交付

回到八个 worker 的例题：原始请求的最佳候选是四 P、四 D；高前缀命中转为两 P、六 D；长输出转为一 P、七 D。改变的是请求对资源的需求，不是调度器突然变得更聪明。若远端链路进一步受限，共享状态可能转为本地重算；若实例寿命缩短，准备较少的方案可能优于稳态更快的方案。


![服务能力与启动积压消退](ch09/figure-9-9-service.svg)

*图 9-9　启动相同，恢复服务能力决定积压消退速度。启动 10 秒期间无服务，每秒到达 4 请求；就绪后分别以 8 或 5 请求/s 服务，从开始启动算起在 20 或 50 秒排空。连续流量近似，未模拟离散调度与请求长尾。*

现在可以沿同一请求作出一条完整决定：长输入、短输出且阶段能力互补时，先考虑四 P、四 D；前缀复用增加后，将多余 P 能力转给 D；输出增长八倍后，更多资源必须承担 decode；若共享链路接近饱和，则优先减少重复取回。最后用启动后的 40 个积压请求检查，这套资源在变化后能否及时恢复服务。每一步都由前一笔计算指出下一项问题。

本章最重要的结论是，分布式推理的收益来自工作与资源的重新匹配。PD 改变阶段之间的匹配，AF 改变算子与计算位置的匹配，专家复制改变负载分布，共享 KV 改变历史工作与状态寿命。每一种收益都伴随具体交接、驻留与等待。只有沿完整请求把这些约束合并，局部改进才成为可持续的服务能力。

## 练习

下列练习对应本章既有实验编号。带“核心”的题目覆盖计算分工、状态复用与完整部署；公开记录题应注明数据的设备、版本与验证范围。

**9-1　服务基线。** 对输入 8192、输出 129 的请求，画出完整副本、PD 和共享状态三种候选，分别列出每请求调用数、KV 产生位置和跨实例交接。将输出改为 1，指出哪些工作消失。延伸到一次工具续接，标出尚无 KV 的最后返回位置。

**9-2　PD 配比。** 复算例 9.2 的 25 个整数候选，并把有效链路设为恰好每秒传一份 1.125 GiB 快照（约 1.21 GB/s）。求新的联合上界；到达率恰等于该上界时，说明为什么仍不能声称服务满足 SLO。随后复核高前缀命中与长输出两个情景。

**9-3　CPU 专家。** 用例 9.3 的同一专家形状，推导 CPU 从读取受限转为计算受限的位置，再将 DRAM 带宽减半。分别评价单 token 和 128 token/专家；用固定 KTransformers 公开记录检查量化、NUMA 和平台差异能否直接代入题设。

**9-4　PD 与 AF 交接〔核心〕。** 固定 Qwen3-8B、25 GB/s，分别取 1、5、20 μs 的启动开销，比较一次 1.125 GiB 交接与同字节的 72 次交接。再恢复实际 AF 载荷，累计 8192 输入、129 输出的完整请求交接。说明加入两侧计算与微批重叠后，还需哪些测量才能判断胜负。

**9-5　异构服务器的服务范围。** 从已封存 V4-Flash 或 Kimi K2 配置还原权重存放、GPU 缓冲和主存需求。增加并发时分别预测不同专家数、任务数和最忙 NUMA 节点。列出评价单位有效产出成本还缺少的质量、功耗与请求信息。

**9-6　专家副本。** 对 $[32,16,8,4,2,1,1,0]$ 比较有效行、非空专家补齐和全部补齐。随后复算例 9.5 的严格回本批数，把每卡额外容量改为 32 MiB，解释为什么应先拒绝不可行副本方案。对动态热点给出观察窗口与迁移成本的比较方法。

**9-7　共享 KV〔核心〕。** 给定一份 1.125 GiB 前缀，比较重算、一次取回并驻留、100 次逐步远程读取。增加写入时间与两次请求的复用间隔，推导保存的收益条件；设计一个改变 HBM 预算后需要撤回驻留决定的例子。使用已有双引擎记录检验机制，不把它标为跨机实测。

**9-8　持久化恢复。** 解释读取 1024 个位置但只复用 1008 个位置的计量差异。对缺页、截断页、版本不兼容分别提出继续等待、部分重算与放弃缓存的判断条件。把“请求完成”“数据修复”“输出一致”列为三个独立验收项。

**9-9　缓存路由。** 推导例 9.6 的远端等时带宽，并计算 $p=0.5,0.9,0.99$ 时 A 的均值和按逆累积分布定义的 p99。再让 CPU 副本仍可用、取回与 80 ms 排队重叠，检查二点分布模型是否仍适用。

**9-10　完整部署〔核心〕。** 在相同质量与资源上限下，给 Chat、Agent 与实时请求各选择一个完整副本基线和一个分离候选。加入 reasoning 长输出、冷启动、计划迁移与单 rank 故障，提交阶段需求、状态交接和恢复预算。先封存选择，再改变一个负载条件，报告是否需要扩大副本、调整池配比、重新共置或重算。动态并行选做应记录每步 token 数、真实模式、图准备与显存峰值。

## 资料与注释

[^ownership]: [同一 MoE 请求的权重、状态与通信归属](../research/2026-infra-survey/parallel-moe-ownership/NOTES.md)，既有 survey 的有界推算与验证。其 TP2×EP4、同 cohort 和 FP32 线格式是明确教学条件，不代表所有引擎默认布局。
[^pd]: DistServe、Splitwise 与阶段放置的已归档材料见[本章扩写资料](../outlines/extensions/09-分布式推理.md#detail-9.2)及[阶段分工与状态交接研究](../case-studies/chunking-and-state-transfer.md)。历史实验的硬件和请求不移作本章 Qwen3 测量。
[^handoff]: [Qwen3-8B 的 PD／AF 交接计算](../calculations/results/pd-af-handoff-qwen8.md)，固定官方模型形状、字节与启动假设。生成图读取同名 JSON。 25 GB/s使用十进制单位，1.125 GiB使用二进制单位；精确载荷1207959552 bytes，纯传输48.31838208 ms，加5 μs为48.32338208 ms。
[^pool]: [整数池配比](../calculations/results/pd-pool-book.md)、[同构对照](../calculations/results/pd-pool-homogeneous.md)、[前缀命中](../calculations/results/pd-pool-prefix.md)、[长输出](../calculations/results/pd-pool-long-output.md)。输入有效能力、固定分配和理想池内流量模型的限制随结果保存。
[^kt]: [权重卸载与执行位置](../case-studies/weight-offload-execution.md)及[固定版本核读范围](../references/framework-history/2026-09-08/offload-execution/README.md)。本文没有重新审查 kt-kernel 全部实现。
[^locality]: [每专家 78 个 token](../calculations/results/expert-locality-boundary78.md)、[79 个 token](../calculations/results/expert-locality-boundary79.md)。本章使用 200 GB/s 有效 DRAM 能力；较早案例笔记中的 100 GiB/s 是另一组假设，不混用其时间。
[^kt-audit]: [KTransformers 公开实验记录复核](../experiments/ch09/09-03/README.md)。教程与论文平台分开；公开质量变化为原表复核，没有在本书环境重测。 教程为双6454S＋4090，论文为双8452Y，不能移用平台计时；Expert Deferral公开质量变化有正有负。
[^capacity]: [V4-Flash 与 Kimi K2 配置记录](../experiments/ch09/09-05/README.md)。配置中的并发上限不等于实际容量扫描。
[^af]: [跨机执行与框架演进](../case-studies/framework-evolution.md)及[AF 扩写依据](../outlines/extensions/09-分布式推理.md#detail-9.3.4)，保存 MegaScale-Infer 与实验性 AFD 的版本条件。
[^moe]: [MoE Serving Tax、CRAFT 与启动研究笔记](../case-studies/moe-and-startup.md)，分别保存论文所读范围和历史平台，正文采用资源分析而非跨平台性能排名。
[^replica]: [专家副本回本计算](../calculations/results/replica-payback-book.md)。固定路由、有效能力、串行复制、逐卡额外预算，不包含真实在线迁移协议。 准备为10.60464608 ms，每批节省0.402784256 ms；正文显示值经舍入，27批边界按原值求解。
[^expert-exp]: [专家执行与后端已有记录](../experiments/ch09/09-06/README.md)，多设备复制和通信收益仍需验证。
[^cache]: [缓存层次、路径与路由研究](../case-studies/cache-tiers-and-routing.md)。本文按已核读实现区分实例私有 host 与配置共享的存储层。
[^shared]: [共享 CPU KV 池容量对照](../experiments/ch09/09-07/shared-kv/README.md)与[真实历史保留后的 Agent 回放](../experiments/ch09/09-07/history-preserved/README.md)。受控查找任务与真实 Agent 质量结论分别报告。 真实Agent回放还出现提前结束及任务质量下降，此处容量对照只讨论受控复用机会；其耗时不能作同质量加速结论。
[^v4]: [DeepSeek-V4 技术报告](../references/files/papers/deepseek-v4.pdf)的状态持久化与生成服务内容，以及[本章已有核读整理](../outlines/extensions/09-分布式推理.md#detail-9.6.3)。模型状态形状和恢复机制依固定版本解释。
[^restart]: [重启页读取与实际复用核算](../calculations/results/cache-restart-book.md)，读取 144 MiB 与复用 141.75 MiB 分列，输出核验不等于 KV 逐位一致。 原始有效复用为141.75 MiB，正文约142 MiB；1024／1008位置与64／63页保留精确整数。
[^fault]: [截断页的预取策略对照](../experiments/ch09/09-08/prefetch-policy/README.md)，60 秒为观察窗口，不是所有故障的通用超时保证。
[^route]: [缓存路由计算](../calculations/results/cache-route-book.md)、[快速远端对照](../calculations/results/cache-route-fast-remote.md)。具体公式包含整份远端→host→GPU 串行路径。
[^events]: [缓存事件与路由判断](../case-studies/cache-events-and-routing.md)、[缓存失效两点分布](../calculations/results/cache-route-stale.md)。Dynamo、vLLM 与 SGLang 的事件范围按各自固定集成条件解释。
[^pressure]: [真实路由压力的配对核算](../calculations/results/router-pressure-book.md)，区分目标请求和完整任务对的完成时间，原始条件随结果保存。
[^startup]: [Breaking the Ice 的研究整理](../case-studies/moe-and-startup.md)，采用历史 vLLM v0.10.1.1 的阶段分析方法，不沿用其时间为当前部署预测。
[^branch]: [HiCache 请求分支观测](../experiments/ch09/09-10/branch-observation/README.md)，使用同请求 ID 的事件链解释限额和有效命中；额外 trace 扰动使本批不适合性能排名。 原记录预取限额为3289位置，4096为已登记占用；八请求波是客户端并发，GPU最大运行请求数为一。
[^switch]: [动态并行与状态条件](../case-studies/parallel-switching-and-state.md)、[专家分派与扩缩容](../case-studies/expert-dispatch-and-resizing.md)，保留当前接口支持范围与未测量项。
[^migration]: [声明串行迁移子账](../calculations/results/reconfiguration-declared-serial.md)，资源下界、额外声明时间和未知实测字段分别保存。

[^cost-model]: 本段费用、SLO通过比例及9.4.3的重叠时间为教材假设，用于推导条件变化；价格不是设备报价。单位成本用完整小时费用除以（3600×有效请求率）。迁移对比沿用原载荷，另给5／25 GB/s带宽与9秒串行准备，区别于原结果的100秒声明网络时间。
