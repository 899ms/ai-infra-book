--- before/01-初识 AI Infra.md
+++ manuscripts/01-初识 AI Infra.md
@@ -26,7 +26,7 @@
 
 最下面是**互联与数据中心**。设备之间通过互联交换数据，服务器和更大的协作组通过网络连接，供电与散热决定资源能够怎样部署。第 6、7 章讨论超节点与数据中心网络，第 12 章进一步把执行位置扩展到端、边和云。
 
-这张图还需要两类补充。资源调度、运行环境、观测和计费会同时作用于多个层次，因此放在侧栏；工艺、封装、供电和散热构成物理条件，因此放在底部。网络也并非只在最下方发挥作用：需要跨设备协作的模型、算子和服务，都要经过具体互联。
+六个层次说明了主要分工，但有些能力贯穿多个层次。资源调度、运行环境、观测和计费会同时作用于多个层次，因此放在侧栏；工艺、封装、供电和散热构成物理条件，因此放在底部。网络也并非只在最下方发挥作用：需要跨设备协作的模型、算子和服务，都要经过具体互联。
 
 我在研究中接触到的“十八层宝塔”视角，强调的正是这种全栈联系。本书将它整理为六层：从上向下看，任务被逐步展开为计算与数据访问；从下向上看，容量、带宽和执行能力决定上层能够以怎样的速度与规模运行。[^panorama]
 
@@ -118,7 +118,7 @@
 
 ### 1.2.3 从参数与链路单位算起
 
-知道设备的算力和带宽后，就可以将模型的计算量和数据量换成相同单位，分别求出耗时。
+这四类指标要与具体工作对应起来，才能用于估算。第一步是把模型参数换成存储字节数，判断设备能否放下；第二步再用字节数除以带宽，估算搬移这些数据需要多久。
 
 以一个 70B 参数的教学模型为例。这里的 B 表示十亿，因此参数总数是 700 亿。若每个参数使用 BF16 存储，每参数占 2 bytes，纯权重大小就是：
 
@@ -136,9 +136,13 @@
 
 容量预算还必须落实到每张设备。两张 80 GB 卡共有 160 GB，若将 140 GB 权重均分，每张卡占 70 GB，各剩 10 GB；若权重全在第一张卡上，第一张仍短缺 60 GB。切分的意义，就是把数据放到实际执行对应计算的设备上，并为状态和工作区留下空间。
 
+![图 1-5 每卡容量与执行时的权重读取](ch01/figure-1-capacity-path.svg)
+
+*图 1-5：左图用相同尺度比较 140 GB 权重集中放置与两卡均分，每条橙色竖线表示单卡 80 GB 容量。右图改用每参数一字节的 70 GB 权重，展示显存到计算单元的读取路径。左图回答放在哪里，右图回答每步如何使用。*
+
 这里还要区分 GB 和 GiB。GB 按十进制计量，1 GB 等于 $10^9$ bytes；GiB 按二进制计量，1 GiB 等于 $2^{30}$ bytes。因此，140 GB 约为 130.4 GiB。将两者换成同一单位，才能准确求出剩余容量。
 
-网络还经常使用 bit/s。8 bits 是 1 byte，所以 400 Gb/s 等于 50 GB/s。按 50 GB/s 的有效带宽传输 1 GB 数据，带宽项为 20 ms。启动时间在每次传输时发生，协议和共享流量则影响应用实际获得的带宽；把它们加入同一条路径，便得到更细的通信时间预算。
+显存容量统一单位后，网络传输也要做同样的换算。网络还经常使用 bit/s。8 bits 是 1 byte，所以 400 Gb/s 等于 50 GB/s。按 50 GB/s 的有效带宽传输 1 GB 数据，带宽项为 20 ms。启动时间在每次传输时发生，协议和共享流量则影响应用实际获得的带宽；把它们加入同一条路径，便得到更细的通信时间预算。
 
 > **练习 1-2〔核心〕：把资源卡变成可用的数字。** 分别计算 70B 模型在每参数 2 bytes 和 1 byte 下的纯权重大小，判断能否放入一张名义 80 GB 卡。再将 400 Gb/s 换成 GB/s，计算传输 1 GB 数据的理想耗时。再为每张卡预留 8 GB 状态与工作区，重新判断单卡及两卡均分方案的容量。
 
@@ -180,13 +184,13 @@
 
 原因在于，本例每读取一个权重只做一次乘加。计算单元很快处理完已经读入的数据，又要等待后续数据。提高算力只能缩短乘加耗时，无法加快显存读取。
 
-我们还要区分理论耗时与完整执行时间。计算可以按块与读取重叠：一块数据到达后开始运算，另一块数据继续传输。若两者能够充分重叠，简化模型的下界至少是：
+这两项时间应当相加，还是取其中较大者，取决于读取与计算能否重叠。若数据分块到达，计算当前块时可以继续读取下一块；在充分重叠的简化模型中，耗时下界为：
 
 $$
 \text{理论耗时下界}=\max\!\left(\text{计算时间},\text{读取时间}\right).
 $$
 
-之所以取两项时间的最大值，是因为读取与计算可以按块重叠：计算第一块时，读取第二块；进入稳定阶段后，较慢的一项决定处理速度。本例中，读取权重远慢于矩阵计算，因此优化应首先针对这 20.90 ms 的读取时间。后文分析实际程序时，还会计入启动、流水填充和逐层等待。
+进入稳定阶段后，每块数据都要经过读取和计算，较慢的一项决定处理速度。本例中，读取权重远慢于矩阵计算，因此优化应首先针对这 20.90 ms 的读取时间。后文分析实际程序时，还会计入启动、流水填充和逐层等待。
 
 **讨论：改变资源或执行组织。** 当前纯权重读取下界已经超过 10 ms，因而在这些假设下可以排除目标；通过容量检查也不改变这个结论。现在做两个最简单的改动。
 
@@ -200,7 +204,7 @@
 
 ![教学模型的算力、带宽和批复用变化，以及独立 Qwen3-8B 实测中的吞吐和每请求时间。](ch01/figure-1-5-budget.svg)
 
-*图 1-5　资源与执行组织怎样改变时间。A、B 按 70B 教学模型的主要矩阵和权重读取预算，比较算力、带宽与批内复用。C、D 为独立的 Qwen3-8B 实测，分别呈现整批吞吐和每请求输出间隔。实测使用 RTX PRO 6000 Blackwell、Qwen3-8B BF16、vLLM 0.23 eager，输入 2048、输出 256，每档 3 轮；吞吐包含输入处理，TPOT 由客户端首末输出间隔除以 255 得到。*
+*图 1-6　资源与执行组织怎样改变时间。A、B 按 70B 教学模型的主要矩阵和权重读取预算，比较算力、带宽与批内复用。C、D 为独立的 Qwen3-8B 实测，分别呈现整批吞吐和每请求输出间隔。实测使用 RTX PRO 6000 Blackwell、Qwen3-8B BF16、vLLM 0.23 eager，输入 2048、输出 256，每档 3 轮；吞吐包含输入处理，TPOT 由客户端首末输出间隔除以 255 得到。*
 
 这也给出了理解 Roofline 模型的入口：看每读取一个字节，能够完成多少计算。本例单请求的近似算术强度是 `140 GFLOPs / 70 GB = 2 FLOPs/byte`；理想共享权重以后，8 请求批量的这一比值提高到约 16 FLOPs/byte。数据被多次用于计算，读取同样多的数据，就能完成更多运算。第 4 章会将这个关系画成计算与带宽共同限定的性能上界。[^roofline]
 
@@ -208,7 +212,7 @@
 
 ### 1.3.3 从初算到观察
 
-纸笔模型让我们知道应该关注什么，实际记录则会告诉我们还漏了什么。为了观察批量复用的效果，本书使用了一组 Qwen3-8B 的既有请求记录：设备是 RTX PRO 6000 Blackwell，框架为 vLLM 0.23，BF16、eager 执行；以下只选输入长度为 2048 token、每请求强制生成 256 token 的一组。每个批量设置有三轮正式记录。[^measurement]
+上面的估算预测：增大 batch 可以提高吞吐，但单个请求仍要等待整批执行。下面用实际记录检查这两个趋势，并观察新增计算与状态访问怎样改变结果。为了观察批量复用的效果，本书使用了一组 Qwen3-8B 的既有请求记录：设备是 RTX PRO 6000 Blackwell，框架为 vLLM 0.23，BF16、eager 执行；以下只选输入长度为 2048 token、每请求强制生成 256 token 的一组。每个批量设置有三轮正式记录。[^measurement]
 
 | 同时请求数 | 整批输出吞吐（token/s） | 客户端平均每输出间隔 TPOT（ms） |
 | ---: | ---: | ---: |
@@ -245,7 +249,7 @@
 
 ### 1.4.2 SmartNIC：改变已有工作的处理位置
 
-云服务器上的 CPU 除了运行应用，还要承担网络处理。多个虚拟机共用物理网络时，平台需要识别数据的归属、查找转发规则、完成封装，并维持隔离。网络中的字节抵达服务器，并不意味着这些工作已经完成。
+TPU 通过改变处理器配置满足新增计算需求。另一类设计则保留应用的计算方式，把占用主机资源的辅助工作移到更合适的位置。云服务器上的网络处理就是一个例子：CPU 除了运行应用，还要处理收发的数据包。多个虚拟机共用物理网络时，平台需要识别数据的归属、查找转发规则、完成封装，并维持隔离。网络中的字节抵达服务器，并不意味着这些工作已经完成。
 
 在可编程网卡研究中，我关注的一个问题是：这些网络处理工作占用了多少本可用于应用的主机资源？以论文中的历史简单转发基线为例，40 Gb/s 链路在最小帧条件下每秒需要处理约六千万个包；若一个 CPU 核每秒处理一千万至两千万个包，仅简单转发就对应约 3 至 6 个持续忙碌的核。这些核持续执行相同的包处理流程，为把这段工作交给专用数据路径提供了具体的理由。[^nic]
 
@@ -255,7 +259,7 @@
 
 ### 1.4.3 Unified Bus：扩大资源协作范围
 
-当一张设备容纳不下模型和运行状态时，最直接的办法之一是增加设备。但“总容量变大”与“任务能够高效执行”之间，还隔着工作划分、数据交换和同步。
+SmartNIC 改变了单台服务器内部的分工。若问题是单台设备的资源总量不足，就需要进一步考虑设备之间怎样协作。当一张设备容纳不下模型和运行状态时，最直接的办法之一是增加设备。但“总容量变大”与“任务能够高效执行”之间，还隔着工作划分、数据交换和同步。
 
 例如，两张卡的显存总量可能足够保存权重，但模型执行所需的数据必须落在能够使用它的设备上。若一步计算依赖另一张卡的结果，就要等待交接；若多张卡共同完成一个操作，还要组织相应的协作。设备数增加带来了资源，也增加了连接这些资源的工作。
 
@@ -265,7 +269,7 @@
 
 ![TPU、SmartNIC 与 Unified Bus 分别改变专用资源配置、处理位置和协作范围。](ch01/figure-1-6-designs.svg)
 
-*图 1-6　三种资源约束怎样改变设计。TPU 以专用计算与供数组织承接模型工作，SmartNIC 把包处理移到数据经过的位置，UB 扩大设备可以直接协作的范围。三者分别改变资源配置、处理位置与连接方式。*
+*图 1-7　三种资源约束怎样改变设计。TPU 以专用计算与供数组织承接模型工作，SmartNIC 把包处理移到数据经过的位置，UB 扩大设备可以直接协作的范围。三者分别改变资源配置、处理位置与连接方式。*
 
 再看开篇的六层图，三个案例分别从不同位置切入，却都会改变计算与数据移动的组织。应用需求推动资源专用化，处理位置改变主机与网卡之间的工作分配，更大的模型则要求重新安排设备协作。后面的章节会沿这些具体关系展开本书的主线：**数据搬移塑造了 AI Infra 的架构。**
 
@@ -290,7 +294,7 @@
 [^h100]: NVIDIA，[H100 架构白皮书](../references/files/specs/nvidia-h100.pdf)与[规格页面快照](../references/files/specs/nvidia-h100-spec.html)。本章采用 SXM 形态与 BF16 稠密矩阵运算规格；原始输入版本和校验值见[配图来源清单](ch01/sources.json)。
 [^budget]: [70B 教学预算复算](../calculations/results/decode-budget-base.md)及配套场景；[单位与逐卡容量复算](../calculations/results/basics-70b-bf16-balanced.md)。本章插图直接读取固定结果，扩展扫描使用同一个教学模型。
 [^roofline]: Williams、Waterman、Patterson，[Roofline 论文](../references/files/papers/roofline.pdf)。本章只引入算术强度和资源上界的直觉，实际层次、访问量和执行效率在第 4、5 章展开。
-[^measurement]: [实验 1-4 记录说明](../experiments/ch01/01-04/README.md)、[结构化分析结果](../experiments/ch01/01-04/results/analysis.json)及[后续补测](../experiments/ch01/01-04/FOLLOWUP.md)。正文和图 1-5 只采用最初记录的短输入组；更长输入与缓存条件不同，未合并作长度因果比较。
+[^measurement]: [实验 1-4 记录说明](../experiments/ch01/01-04/README.md)、[结构化分析结果](../experiments/ch01/01-04/results/analysis.json)及[后续补测](../experiments/ch01/01-04/FOLLOWUP.md)。正文和图 1-6 只采用最初记录的短输入组；更长输入与缓存条件不同，未合并作长度因果比较。
 [^tpu]: Jouppi 等，[TPU v1 论文](../references/files/papers/tpu-v1.pdf)，第 2 节关于起源、架构与实现的说明。三分钟语音搜索是论文记载的历史需求预测，不是本书当代负载测量。
 [^nic]: [作者博士论文](../references/files/papers/bojieli-phd-thesis.pdf)，第 4.2.1 节简单转发基线；[核数预算复算](../calculations/results/nic-budget-book.md)。约六千万包每秒来自 40 Gb/s 与每帧占用 84 字节线时的计算，3 至 6 核未包含完整虚拟化工作。
 [^ub]: [《Unified Bus 背后的思考》本地原件](../references/files/documents/ub-reflection.html)。正文仅采用研究与需求转折的时间关系；未引用其中未在本章展开核对的产品性能数字。
--- before/02-模型架构.md
+++ manuscripts/02-模型架构.md
@@ -58,7 +58,13 @@
 
 先观察三个关系。Qwen3.6 的总参数比 Qwen3-8B 多，主干宽度却只有一半：它把大量参数放进可选择的专家，每个 token 只调用其中八个。Qwen3-8B 与 V4 的主干同为 4096 维，V4 通过每层 256 个专家形成更大的参数集合。K3 又将专家计算移到 3584 维潜空间，使主干宽度与专家输入宽度分开。这些差异解释了为什么“保存多大的模型”和“一个 token 做多少工作”需要分别计算。表 2-1、2-2、2-4、2-5 将逐个展开这四个模型，V3 的表 2-3 用作 MLA 的历史参照。
 
-接下来把教学图换成真实配置。Qwen3-8B 的主干有 36 层，隐藏维度 $d$ 为 4096，FFN 中间维度 $f$ 为 12288；注意力有 32 个 Q 头、8 个 KV 头，每头维度为 128。词表大小为 151936，词嵌入和输出头不共享权重。本书对照固定配置与 checkpoint 索引得到的总参数数目为 8,190,735,360，而不是恰好 $8\times10^9$。[固定配置与逐算子结果](../calculations/results/qwen3-8b-prefill-8192.md)保留了这一核对过程。
+![图 2-2 四个模型的层数、隐藏维度与专家数](ch02/figure-2-architecture.svg)
+
+*图 2-2：三个面板分别使用统一的线性尺度，条长对应层数、隐藏维度和每层路由专家数。Qwen3.6 比 Qwen3-8B 略深、隐藏维度更小，却增加了大量专家；K3 则同时增加深度、隐藏维度和专家数。专家柱表示每个 MoE 层保存的路由专家总数，每 token 选中数见表 2-A。*
+
+这些宏观差异最终都要落实到矩阵尺寸。下面先沿 Qwen3-8B 展开一次前向，建立逐层计算的方法，再用相同方法分析另外三个模型。
+
+Qwen3-8B 的主干有 36 层，隐藏维度 $d$ 为 4096，FFN 中间维度 $f$ 为 12288；注意力有 32 个 Q 头、8 个 KV 头，每头维度为 128。词表大小为 151936，词嵌入和输出头不共享权重。本书对照固定配置与 checkpoint 索引得到的总参数数目为 8,190,735,360，而不是恰好 $8\times10^9$。[固定配置与逐算子结果](../calculations/results/qwen3-8b-prefill-8192.md)保留了这一核对过程。
 
 一次前向执行从 token ID 开始。词嵌入为每个 ID 查出一个 4096 维向量，向量依次经过 36 层注意力与 FFN，最后经过归一化和词表投影产生 logits。词嵌入按 ID 选择行，一个 token 取出一条向量，整个词表则作为常驻数据供不同 token 查询。词表输出头则将隐藏向量与词表矩阵相乘；只生成下一个 token 时，通常只需要最后一个输入位置的 logits。
 
@@ -101,7 +107,7 @@
 
 采用数学右乘记号，输入 X 经过三个线性投影得到 Q、K、V。Q 的总宽度为 $32\times 128=4096$，K 和 V 的总宽度各为 $8\times 128=1024$。因此，$W_q$ 为 $[4096,4096]$，$W_k$ 和 $W_v$ 各为 $[4096,1024]$。代码中的线性层常以 $(d_{\mathrm{out}},d_{\mathrm{in}})$ 保存权重；换成数学右乘记号后，输入宽度在前、输出宽度在后，便可逐项对照下面的表格。
 
-投影之后，将 Q 拆成 32 个头，把 K、V 拆成 8 个头。每组四个 Q 头共用一个 KV 头。对一个 Q 头，查询向量与允许访问位置的 K 做点积，缩放后加因果掩码，再做 Softmax，得到不同历史位置的权重。随后用这些权重对 V 加权求和。各 Q 头的结果拼接后，经 $[4096,4096]$ 的输出投影 $W_o$ 返回隐藏空间。
+上述投影产生了 Q、K、V，但尚未让当前位置与历史交换信息。接下来将 Q 拆成 32 个头，把 K、V 拆成 8 个头。每组四个 Q 头共用一个 KV 头。对一个 Q 头，查询向量与允许访问位置的 K 做点积，缩放后加因果掩码，再做 Softmax，得到不同历史位置的权重。随后用这些权重对 V 加权求和。各 Q 头的结果拼接后，经 $[4096,4096]$ 的输出投影 $W_o$ 返回隐藏空间。
 
 用矩阵记号表示这一过程，就是 $A=\operatorname{Softmax}(QK^{\mathsf T}/\sqrt{d_h}+M)$，$O=AV$。这里 M 表示掩码。Qwen3 还对 Q、K 做 QK Norm，并使用 RoPE 编码位置信息。QK Norm 作用于各头特征，RoPE 对对应特征对做位置相关旋转，使查询与键的相互作用携带位置信息；两者都位于注意力评分之前。经过归一化和位置处理后，Q、K 才用于计算注意力分数。
 
@@ -131,9 +137,9 @@
 
 注意力的 QKV 与输出投影合计约 4194 万个参数。将它与 FFN 的约 1.51 亿相加，FFN 占这些主要投影参数的约 78%，因此短上下文的计算中，三个 FFN 矩阵占据很大份额。随着输入变长，FFN 随位置数线性增长，全历史注意力则随 $P(P+1)/2$ 增长。两种增长率使计算重心随上下文长度移动。
 
-![图 2-2 Qwen3-8B 单层的尺寸与数据流](ch02/figure-2-2-layer.svg)
-
-*图 2-2：$m=BP$ 对应已知输入处理，$m=B$ 对应每请求补入一个 token。QK Norm、RoPE、归一化与残差均位于真实数据流中。各块的矩阵尺寸和 FLOPs 对照表 2-1；KV 容量和累计访问在下一节展开。*
+![图 2-3 Qwen3-8B 单层的尺寸与数据流](ch02/figure-2-2-layer.svg)
+
+*图 2-3：$m=BP$ 对应已知输入处理，$m=B$ 对应每请求补入一个 token。QK Norm、RoPE、归一化与残差均位于真实数据流中。各块的矩阵尺寸和 FLOPs 对照表 2-1；KV 容量和累计访问在下一节展开。*
 
 
 **表 2-1　Qwen3-8B 的单层模块与输出接口**
@@ -203,7 +209,7 @@
 
 ### 2.2.2 驻留容量与逻辑访问
 
-为了让后续模型比较使用同一分类方法，我们把数据分成权重、历史状态和临时数据三类。区分的依据是用途与寿命，而不只是它们都占用显存。
+KV 说明了缓存如何用存储换取少做计算。要判断这种交换对系统意味着什么，还需要把 KV 与权重、临时数据放在一起比较。为此，我们把数据分成权重、历史状态和临时数据三类。区分的依据是用途与寿命，而不只是它们都占用显存。
 
 | 数据类别 | 保存与复用 | 本章主要核算项 |
 | --- | --- | --- |
@@ -227,9 +233,13 @@
 
 ### 2.2.3 完整生成的计算与状态累积
 
-现在固定一个普通串行生成请求：$B=1$，已恢复前缀为 S，本次输入 $P\ge1$ 个 token，最终返回 $G\ge1$ 个 token。令 $H=S+P$。prefill 处理新输入并产生首输出，随后执行 $n_d=G-1$ 次 decode。最后返回的 token 尚未重新送入模型，所以请求结束时并不自动为它保存 KV。
-
-第 j 次 decode（j 从 0 开始）执行前，历史长度为 $H+j$。于是，若每次 decode 将此前的历史各读取一遍，累计读取量为：
+上一节比较了一步执行中的读取量。一个完整回答会重复执行许多步，同一份历史也随之被读取许多次。现在固定一个普通串行生成请求：$B=1$，已恢复前缀为 S，本次输入 $P\ge1$ 个 token，最终返回 $G\ge1$ 个 token。令 $H=S+P$。prefill 处理新输入并产生首输出，随后执行 $n_d=G-1$ 次 decode。最后返回的 token 尚未重新送入模型，所以请求结束时并不自动为它保存 KV。
+
+![图 2-4 历史读取的阶梯与批内权重复用](ch02/figure-2-history.svg)
+
+*图 2-4：左图用 $H=4$、四次 decode 展开累计读取：原来的四个位置每步都读，后来生成的位置从下一步起参与读取。已填色方块共 $4+5+6+7=22$ 个，最终保存的位置却只有 8 个。右图采用 Qwen3-8B 的实际状态宽度，每步将共享权重和各请求的旧历史各读取一遍；8K、2K 历史的读取量分别在 batch 为 13、51 时超过共享权重。*
+
+左图中的每一行都比上一行多一个已填色方块：先前追加的位置现在也成为历史。第 j 次 decode（j 从 0 开始）执行前，历史长度为 $H+j$。于是，若每次 decode 将此前的历史各读取一遍，累计读取量为：
 
 $$
 R_{\mathrm{old}}=c_{\mathrm{KV}}\sum_{j=0}^{n_d-1}(H+j)=c_{\mathrm{KV}}\left[n_dH+\frac{n_d(n_d-1)}{2}\right]
@@ -243,7 +253,7 @@
 
 累计计算也必须沿同样的调用序列完成。投影和 FFN 的单 token 工作相对固定，注意力项随 $H+j$ 增长，输出头按实际调用位置计量。如果把 G 直接当作 decode 次数，就会多算一次模型前向、一次状态追加和相应历史读取。$G=1$ 是很有用的边界检查：只有 prefill 产生首输出，后续 decode 为零。
 
-多条独立请求的状态容量与历史访问分别相加，批内权重则共同使用。长度不同时，逐请求代入各自的 $H$ 与 $n_d$：长输出让已有历史被更多次读取，输入越长，第一次 decode 需要访问的历史就越多。对实际对话，可先从每轮输入、缓存命中与返回数重建这条调用链，[已保存的 Chat 长度核算](../calculations/results/sealed-chat-known-prefill.md)给出了具体代入过程。
+图 2-4 左侧沿生成步数累加读取，右侧沿请求数累加读取。多条独立请求的状态容量与历史访问分别相加，批内权重则共同使用。长度不同时，逐请求代入各自的 $H$ 与 $n_d$：长输出让已有历史被更多次读取，输入越长，第一次 decode 需要访问的历史就越多。对实际对话，可先从每轮输入、缓存命中与返回数重建这条调用链，[已保存的 Chat 长度核算](../calculations/results/sealed-chat-known-prefill.md)给出了具体代入过程。
 
 ## 2.3 历史状态表示与访问机制
 
@@ -255,9 +265,9 @@
 
 先看一个只有四个查询头的单层例子。MHA 为四个查询头各存一份 K、V；GQA 可以让前两个查询头共享第一份 K、V，后两个共享第二份；MQA 则让四个查询头共用一份。若历史长度和头宽相同，需要保存的 KV 份数依次为四、二、一。因此，GQA 在这个例子中把历史保存量减半。四个查询头仍可给同一段历史打出四组不同的分数，这正是“共享历史表示”与“合并查询”之间的区别。
 
-![图 2-3 查询头与共享历史的对应关系](ch02/figure-2-3-sharing.svg)
-
-*图 2-3：四个查询头的教学例子。连线表示每个查询使用哪一组历史 K、V；三种结构都保留四个查询。图中组数用于解释共享关系，不是三个真实发布模型的配置。*
+![图 2-5 查询头与共享历史的对应关系](ch02/figure-2-3-sharing.svg)
+
+*图 2-5：四个查询头的教学例子。连线表示每个查询使用哪一组历史 K、V；三种结构都保留四个查询。图中组数用于解释共享关系，不是三个真实发布模型的配置。*
 
 回到 Qwen3-8B：它让每四个查询头共用一组 KV，总计 32 个查询头、8 个 KV 头。如果给每个查询头单独保存 KV，容量会是当前配置的四倍；如果全部查询共享一组，则会是当前配置的八分之一。实际 GQA 配置保存 8192 个位置需要约 1.13 GiB。下面的容量图固定历史长度与头宽，只改变 KV 组数，因此柱高之比直接呈现共享比例。
 
@@ -275,9 +285,9 @@
 
 MLA 每条历史变窄，条目数量仍随 $S$ 增长，所以历史容量和评分工作继续随长度增长。K3 的其他层采用另一种办法：把历史更新进固定大小的递推矩阵。先理解这两种表示，再将它们相加，就能得到整模型的状态曲线。
 
-![图 2-4 KV 共享与潜变量压缩](ch02/figure-2-4-cache.svg)
-
-*图 2-4：两侧均取 8192 个历史位置、BF16。左侧以同一 Qwen 形状比较 MHA／GQA／MQA 的状态宽度，其中仅 GQA 是实际基线；右侧比较 K3 MLA 的紧凑与展开路径。左侧改变 KV 组数，右侧改变缓存位于上投影之前还是之后，分别展示两种缩小历史表示的方法。*
+![图 2-6 KV 共享与潜变量压缩](ch02/figure-2-4-cache.svg)
+
+*图 2-6：两侧均取 8192 个历史位置、BF16。左侧以同一 Qwen 形状比较 MHA／GQA／MQA 的状态宽度，其中仅 GQA 是实际基线；右侧比较 K3 MLA 的紧凑与展开路径。左侧改变 KV 组数，右侧改变缓存位于上投影之前还是之后，分别展示两种缩小历史表示的方法。*
 
 > **实验 2-3 ★〔延伸〕：GQA 与 K3 MLA 的容量和访问**
 >
@@ -297,9 +307,9 @@
 
 将 43 层相加，8192 历史下的窗口、压缩历史和索引约为 59.125 MiB；另有约 11.641 MiB 的 FP32 压缩器缓冲，合计约 70.8 MiB。最后查询的主注意力与索引数据量约 27.6 MiB。[状态分解](../calculations/results/state-deepseek-v4-flash-n8192-b1-native.md)将这些分项列开，因此能看出选择条目数、保存量和每步读取量并不相等。
 
-![图 2-5 V4 的窗口、压缩与索引](ch02/figure-2-5-sparse.svg)
-
-*图 2-5：上部区分保存的窗口、压缩条目与索引；下部按 8192 历史列出整模型参考状态构成；最后查询的主注意力数据量约 17.1 MiB，索引扫描约 10.5 MiB，合计约 27.6 MiB。未完成压缩块的缓冲单列，索引扫描不包含在主注意力选中条目数中。*
+![图 2-7 V4 的窗口、压缩与索引](ch02/figure-2-5-sparse.svg)
+
+*图 2-7：上部区分保存的窗口、压缩条目与索引；下部按 8192 历史列出整模型参考状态构成；最后查询的主注意力数据量约 17.1 MiB，索引扫描约 10.5 MiB，合计约 27.6 MiB。未完成压缩块的缓冲单列，索引扫描不包含在主注意力选中条目数中。*
 
 新 token 到达时，压缩器先计算其投影并更新未完成块；块凑齐后，再生成一个长期条目。每 token 更新与块完成写入具有不同节奏：在 128 的边界，细粒度与粗粒度压缩层可同时完成条目，形成集中写入。恢复旧前缀后，这些缓冲也决定后缀怎样续算。[缓存后缀续算记录](../calculations/results/v4-prefix-flash-6144-2048.md)采用逐 token 的参考实现的增量计算过程，沿实际调用累计压缩与输出工作。
 
@@ -313,6 +323,10 @@
 
 另一种路线是把历史汇总到固定大小的矩阵中。以线性聚合 $S_t=S_{t-1}+k_tv_t^{\mathsf T}$、$o_t=q_t^{\mathsf T}S_t$ 为例，每个新位置写入一个键值外积，当前查询直接读取累积矩阵。逐位置的历史列表由一份关联矩阵替代，查询的工作由矩阵维度决定。Softmax 注意力则先对各位置评分再归一化，保留逐位置选择；两种机制在这一点上形成不同的信息组织。
 
+![图 2-8 逐位置历史与固定状态矩阵](ch02/figure-2-recurrence.svg)
+
+*图 2-8：左侧为逐位置保存的历史，查询分别使用各位置的 K、V；右侧用简单的外积累加说明有限状态递推。每个新位置修改同一份矩阵，矩阵尺寸保持不变，读写却在每步发生。KDA 在这一递推思路中进一步加入门控、衰减和误差修正。*
+
 KDA 使用更丰富的门控与 delta 更新。状态更新不仅写入新信息，还根据当前键对既有状态的预测形成修正，并通过衰减控制旧信息保留。直观上，它更新的是一张固定大小的关联矩阵，而不是不断追加一张逐 token 列表。旧信息以汇总后的关联保留，当前键控制修正的位置，衰减门控制旧贡献继续存在的程度。
 
 K3 每个 KDA 层有 96 个头，每头递推矩阵为 $128\times128$。按 FP32 保存，单层为 6 MiB，69 层为 414 MiB。一次 decode 读取旧矩阵、融合新信息并写回，理想一读一写就是 828 MiB。这个量不随历史长度增长：短历史时它是一笔显著的固定开销，长历史时则避免了逐位置扫描持续扩大。
@@ -323,17 +337,17 @@
 
 实际模型常把有限状态层与全局注意力层混合使用。这样既利用递推结构的历史压缩，也保留部分直接访问全局历史的能力。但整模型状态不再服从单一的“固定”或“线性增长”规则，而是多种分量之和。
 
-K3 的配置为 69 个 KDA 层和 24 个 MLA 层，末尾另含一个 MLA 层，逐层累计为 93 层。8192 历史、紧凑 BF16 MLA、FP32 递推状态及给定 BF16 短卷积槽位下，三项分别为 216 MiB、414 MiB 与 19.4 MiB，合计约 649.4 MiB。若使用参考展开缓存，MLA 一项就变为 11.25 GiB，整体随之改变。[混合状态计算](../calculations/results/state-kimi-k3-n8192-b1-compact.md)为两条路径保留独立结果。
+K3 共 93 层，其中 69 个 KDA 层、24 个 MLA 层；24 个 MLA 层中包含末尾的一层。8192 历史、紧凑 BF16 MLA、FP32 递推状态及给定 BF16 短卷积槽位下，三项分别为 216 MiB、414 MiB 与 19.4 MiB，合计约 649.4 MiB。若使用参考展开缓存，MLA 一项就变为 11.25 GiB，整体随之改变。[混合状态计算](../calculations/results/state-kimi-k3-n8192-b1-compact.md)为两条路径保留独立结果。
 
 Qwen3.6-35B-A3B 同样需要按层型计算。其 30 个线性注意力层加 10 个完整注意力层，使每请求每历史 token 的全局 KV 为 20 KiB，另有固定 FP32 递推状态 60 MiB 和 BF16 卷积槽 1.875 MiB。8192 历史时，三项合计 221.9 MiB。这些状态来自注意力分支；接下来的 MoE 分支在同一主干上改变权重选择，两类机制共同组成完整模型。
 
-![图 2-6 混合注意力与专家计算的组合](ch02/figure-2-6-hybrid.svg)
-
-*图 2-6：Qwen3.6-35B-A3B 的层组成。每层按配置执行线性注意力或完整注意力，随后都执行路由与共享专家。分支表示不同层型，线性注意力并不意味着没有 MoE。全模型按 30 个线性层、10 个完整注意力层分别累加，再加入 40 层 MoE。*
-
-![图 2-7 状态容量、访问与输入工作](ch02/figure-2-7-state-growth.svg)
-
-*图 2-7：$B=1$；A、B 为历史长度上的结构推算，C 固定 8192 个新输入并只执行末位置词表头。容量、已计状态访问和 prefill 矩阵运算量分面呈现。K3 容量曲线使用紧凑 MLA；状态访问将全局历史读取与递推矩阵理想读写相加，未包含所有卷积与操作数。prefill 面板按各自参考实现累计整模型工作，与前两项共同呈现保存、访问和计算之间的关系。*
+![图 2-9 混合注意力与专家计算的组合](ch02/figure-2-6-hybrid.svg)
+
+*图 2-9：Qwen3.6-35B-A3B 的层组成。每层按配置执行线性注意力或完整注意力，随后都执行路由与共享专家。分支表示不同层型，线性注意力并不意味着没有 MoE。全模型按 30 个线性层、10 个完整注意力层分别累加，再加入 40 层 MoE。*
+
+![图 2-10 状态容量、访问与输入工作](ch02/figure-2-7-state-growth.svg)
+
+*图 2-10：$B=1$；A、B 为历史长度上的结构推算，C 固定 8192 个新输入并只执行末位置词表头。容量、已计状态访问和 prefill 矩阵运算量分面呈现。K3 容量曲线使用紧凑 MLA；状态访问将全局历史读取与递推矩阵理想读写相加，未包含所有卷积与操作数。prefill 面板按各自参考实现累计整模型工作，与前两项共同呈现保存、访问和计算之间的关系。*
 
 这几条状态曲线的形状来自不同表示。Qwen3-8B 每增加一个位置，所有 GQA 层都追加 K、V；K3 只有 MLA 部分追加历史，KDA 部分保持固定矩阵；V4 则在压缩块完成时增加条目。对长历史，增长速度决定新增容量；对短历史，递推矩阵等固定部分决定起始占用。
 
@@ -349,11 +363,13 @@
 
 ## 2.4 条件计算与专家权重复用
 
-前面主要改变历史状态，现在回到 FFN 的权重。稠密 FFN 对每个 token 使用同一组矩阵；MoE 把容量分散给多个专家，由路由器为每个 token 选择少数专家。它使“模型保存多少参数”与“一个 token 激活多少参数”分离，也让 batch 内不同 token 的选择影响实际权重访问集合。
+前面几节通过共享、压缩和递推减少历史状态。对另一个主要数据来源——模型权重——也可以问同样的问题：每个 token 是否必须使用模型中的全部参数？下面从 FFN 出发分析这一问题。
+
+稠密 FFN 对每个 token 使用同一组矩阵；MoE 把容量分散给多个专家，由路由器为每个 token 选择少数专家。它使“模型保存多少参数”与“一个 token 激活多少参数”分离，也让 batch 内不同 token 的选择影响实际权重访问集合。
 
 ### 2.4.1 稠密前馈网络与专家路由
 
-一个典型 MoE 分支先从输入计算路由分数，选择 top-k 专家，再把 token 送入选中专家，最后按路由权重合并输出。有共享专家时，还会有一条所有 token 都参与的分支。专家本身通常仍由 gate、up、down 三个矩阵构成，因此可以沿上一节的 SwiGLU 计算逐个展开。
+一个典型 MoE 分支先从输入计算路由分数，选择 top-k 专家，再把 token 送入选中专家，最后按路由权重合并输出。有共享专家时，还会有一条所有 token 都参与的分支。专家本身通常仍由 gate、up、down 三个矩阵构成，因此可以按 2.1.4 的 SwiGLU 计算逐个展开。
 
 Qwen3.6-35B-A3B 的固定文本配置包含 40 层，隐藏维度 2048；每层 256 个路由专家，每 token 选 8 个，专家中间维度为 512，另有共享专家。一个路由专家包含 $3\times 2048\times 512=3,145,728$ 个参数，以 BF16 保存为 6 MiB。每层全部路由专家占 1.5 GiB，一个 token 选中的八个专家占 48 MiB。
 
@@ -374,13 +390,19 @@
 | 均匀分配 | 256 | 2 | 1.5 GiB |
 | 集中于同 8 个专家 | 8 | 64 | 48 MiB |
 
+![图 2-11 同样的专家运算量与不同的权重读取量](ch02/figure-2-expert-reuse.svg)
+
+*图 2-11：两侧均为 64 个 token，每个 token 选择 8 个专家，共形成 512 行专家输入。左侧将这些行分散到 256 份权重上，右侧集中到 8 份权重上；矩形示意输入矩阵，左侧省略了重复的小矩阵。每份专家权重为 6 MiB，分别读取 256 份与 8 份，便得到 32 倍的差距。*
+
 读取差额来自每份权重服务的输入行数：均匀分派时每专家只有两行，集中分派时每专家有 64 行。同一份权重支持的计算增加 32 倍，理想权重访问量便减少 32 倍。与此同时，原来要执行许多输入为两行的矩阵乘法，现在只需执行少数输入为 64 行的矩阵乘法；权重复用和计算效率因此都会受到影响。[均匀路由](../calculations/results/qwen36-decode-b64.md)与[集中路由](../calculations/results/qwen36-decode-b64-concentrated.md)保留了这两个分派的逐专家结果。
 
 Qwen3.6 在已有 8192 历史的 $B=1$ decode 中约需 7.33 GFLOPs，小于 Qwen3-8B 的约 20.0 GFLOPs。这个整模型差额由几项结构共同形成：2048 维主干缩小投影，30 个线性层使用递推状态，MoE 每次只执行选中的专家及共享分支。总参数增长来自专家集合，当前工作则由实际经过的计算路径决定。
 
 ### 2.4.3 共享专家与潜空间计算
 
-更复杂的专家结构继续沿同一方法展开。DeepSeek-V4-Flash 每层有 256 个路由专家，每 token 选 6 个，另有 1 个共享专家；路由专家中间维度为 2048。单专家的 gate/up 为 $[t_e,4096]\times [4096,2048]$，down 为反向尺寸，参数为 25,165,824。前三层采用哈希路由，后续采用评分路由，计算记录按对应分支展开选择过程。
+上述差距由专家接收的输入行数决定。接下来还要考虑专家本身的尺寸：共享专家处理全部输入，潜空间专家则先缩小输入维度。两者都会改变一个专家需要多少权重、执行多少运算。
+
+DeepSeek-V4-Flash 每层有 256 个路由专家，每 token 选 6 个，另有 1 个共享专家；路由专家中间维度为 2048。单专家的 gate/up 为 $[t_e,4096]\times [4096,2048]$，down 为反向尺寸，参数为 25,165,824。前三层采用哈希路由，后续采用评分路由，计算记录按对应分支展开选择过程。
 
 共享专家为所有 token 提供共同变换，路由专家提供按 token 选择的变换。V4 每层选六个路由专家，再执行一个共享专家，因此当前 token 使用七组专家矩阵，驻留时则保存 256 组路由专家和共享分支。注意力、连接与输出模块再加入各自工作，形成整模型预算。
 
@@ -418,9 +440,9 @@
 
 K3 的 AttnRes 对网络中较浅层产生的块表示加权求和，形成当前层的输入。后面的层还要使用这些块表示，所以它们需要在本次前向中继续保存；前向结束后即可释放。历史 KV 则要保留到后续生成步骤。两者的寿命不同：AttnRes 增加一次前向中的临时存储，KV 随请求跨多次调用保留。[逐层调度记录](../calculations/results/k3-attn-res-b1-t8192.md)列出了块表示的生成与释放过程。
 
-![图 2-8 稠密层与 V4 层的数据流](ch02/figure-2-8-residual.svg)
-
-*图 2-8：从同为 4096 维的子层输入比较 Qwen3 与 V4。注意力、FFN／专家与残差在图中各有位置；V4 四路残差经汇合后进入子层，专家的 $t_e$ 取决于路由。图中的汇合与分派连接了跨层表示、注意力和专家计算。*
+![图 2-12 稠密层与 V4 层的数据流](ch02/figure-2-8-residual.svg)
+
+*图 2-12：从同为 4096 维的子层输入比较 Qwen3 与 V4。注意力、FFN／专家与残差在图中各有位置；V4 四路残差经汇合后进入子层，专家的 $t_e$ 取决于路由。图中的汇合与分派连接了跨层表示、注意力和专家计算。*
 
 ### 2.5.3 多 token 预测与辅助计算
 
@@ -438,7 +460,11 @@
 
 ### 2.6.1 模型组件与资源汇总
 
-表 2-A 给出模型的主要结构参数，表 2-1 已完成稠密基线。下面保留 Qwen3.6、V4、K3 的完整模块表，并加入 V3 的历史参照，将此前的机制重新组装成模型。每张表按注意力、前馈与连接、输出接口分组；“层／调用数”给出整模型累计倍数，“矩阵 FLOPs”仍是该行单次成本。先选层型，再累加该层的注意力、前馈与连接工作，最后加入嵌入查行、末端归一化和词表头。对 Qwen3-8B，表中包含嵌入、36 层注意力与 FFN、残差和归一化、最终输出头；对 Qwen3.6，还需按层型拆开线性与完整注意力，并列路由与共享专家。对 V4，还要加入窗口、压缩、索引和 mHC；对 K3，则包括 KDA、MLA、潜空间投影、共享专家与 AttnRes。MTP 与视觉模块按本次执行范围单独加入。
+表 2-A 给出了模型的主要结构参数，表 2-1 已完成 Qwen3-8B 的逐项计算。下面用同一方法计算 Qwen3.6、V4、K3，并通过 V3 对照 MLA 的两种执行方式。
+
+阅读时可以按数据流走一遍：输入进入哪类注意力层，随后经过哪类前馈网络，最终如何得到词表分数。每张表按注意力、前馈与连接、输出接口分组；“层／调用数”给出整模型累计倍数，“矩阵 FLOPs”仍是该行单次成本。先选层型，再累加该层的注意力、前馈与连接工作，最后加入嵌入查行、末端归一化和词表头。
+
+同样的累加规则用于不同模型时，需要注意各自的分支。对 Qwen3-8B，表中包含嵌入、36 层注意力与 FFN、残差和归一化、最终输出头；对 Qwen3.6，还需按层型拆开线性与完整注意力，并列路由与共享专家。对 V4，还要加入窗口、压缩、索引和 mHC；对 K3，则包括 KDA、MLA、潜空间投影、共享专家与 AttnRes。MTP 与视觉模块按本次执行范围单独加入。
 
 | 模型分支 | 历史状态 | 权重与附加计算 | 合计时最容易遗漏的部分 |
 | --- | --- | --- | --- |
@@ -448,6 +474,12 @@
 | V4-Flash | 窗口＋压缩＋索引＋缓冲 | 专家、压缩器、mHC | 索引扫描、块边界更新、共享分支 |
 | K3 | MLA＋KDA＋卷积 | 潜空间专家、AttnRes | 展开路径、跨层临时状态、首层差异 |
 
+
+先看图 2-13 的整体结果，再读各模型的分项表。Qwen3.6 的权重比 Qwen3-8B 多，单步计算和历史状态却更少；V4 的专家权重继续增加，历史压缩又使 8K 状态进一步减小。后面的表将说明这些差异分别来自哪些矩阵和状态。
+
+![图 2-13 模型权重、单步计算与历史状态的对照](ch02/figure-2-resources.svg)
+
+*图 2-13：各面板分别采用线性尺度，并标出绝对值。四个模型按表 2-C 的执行方式计算：batch 为 1，decode 前已有 8192 个历史位置。完整权重反映模型需保存的参数总量，选中专家权重只统计一个 token 使用的路由专家；历史状态又取决于注意力结构。K3 使用紧凑 MLA。*
 
 **表 2-2　Qwen3.6-35B-A3B：混合注意力与 MoE 的模块核算**
 
@@ -704,7 +736,9 @@
 
 ### 2.6.2 参数规模、数值精度与容量约束
 
-显存容量检查把模型需求与给定设备预算连接起来。对一张设备，先扣权重，再扣工作区和其他固定预留，剩余空间才能放请求状态。对普通等长、独立 KV 的简化情形，可写为：
+分项计算给出了模型权重和每条请求状态的大小。现在把它们放进同一张显存容量表，就可以判断一台设备能同时保留多少条请求，以及量化权重能腾出多少空间。
+
+对一张设备，先扣权重，再扣工作区和其他固定预留，剩余空间才能放请求状态。对普通等长、独立 KV 的简化情形，可写为：
 
 $$
 B_{\max}=\left\lfloor\frac{M_{\mathrm{device}}-M_{\mathrm{weight}}-M_{\mathrm{workspace}}}{M_{\mathrm{state/request}}}\right\rfloor
@@ -718,9 +752,9 @@
 
 4-bit 方案的 39.500 GB 来自低位宽矩阵、高精度保留项、scale 与打包的共同贡献，比将全部参数一律按半字节计算更大。显存容量为 80 GB（十进制）、固定预留 2 GiB 时，8-bit 方案可放 1 条 8K 请求，4-bit 方案可放 14 条。历史从 8K 增至 32K 时，每请求 KV 增至四倍，4-bit 方案可同时容纳的请求数降到 3 条。压缩权重腾出的空间可以保存更多请求的历史，但每条历史越长，能同时容纳的请求就越少。[8K 容量表](../calculations/results/llama70-capacity-8k.md)与[32K 对照](../calculations/results/llama70-capacity-32k.md)采用同一格式完成这一比较。
 
-![图 2-9 权重、历史与显存可容纳的并发请求数](ch02/figure-2-9-capacity.svg)
-
-*图 2-9：左侧按十进制 GB 分解一个请求的权重、状态和 2 GiB 固定预留，并计算剩余显存可同时容纳多少条请求；右侧固定 70B 模型的 4-bit 量化格式、80 GB 容量与预留，比较每条请求的历史长度为 8K 和 32K 时，可同时容纳的请求数。每条请求的历史越长，相同剩余空间容纳的请求数越少。*
+![图 2-14 权重、历史与显存可容纳的并发请求数](ch02/figure-2-9-capacity.svg)
+
+*图 2-14：左侧按十进制 GB 分解一个请求的权重、状态和 2 GiB 固定预留，并计算剩余显存可同时容纳多少条请求；右侧固定 70B 模型的 4-bit 量化格式、80 GB 容量与预留，比较每条请求的历史长度为 8K 和 32K 时，可同时容纳的请求数。每条请求的历史越长，相同剩余空间容纳的请求数越少。*
 
 同样方法也能分析更大的部署。235B 的 BF16 权重约 470 GB，超过八张 24 GB 卡合计的 192 GB，而八张 80 GB 卡合计为 640 GB。后者为状态、工作区与复制留下约 170 GB 总余量；第六章将按具体分工把这笔总量分配到每张设备，得到逐卡预算。
 
@@ -733,6 +767,8 @@
 > 对 70B 模型的 4-bit 量化方案，将容量改为 48 GB，比较 8K 与 32K 历史。对给定八卡部署方案，检查最忙设备；多卡切分的推导在第六章完成。
 
 ### 2.6.3 请求条件与模型比较
+
+显存容量决定能同时容纳多少请求，完整调用链则决定每条请求要做多少计算。下面固定输入和输出长度，沿 prefill 与 decode 累加，再用同一文本检查任务结果，把资源消耗与模型用途联系起来。
 
 比较完整模型时，可以从两种输入出发。固定 $S,P,G,B$，得到相同调用长度下的资源需求，便于观察结构差异；固定文本和任务，则先经各模型的分词器得到输入长度，再检查输出。这两种对照分别回答“同样多的位置怎样计算”和“同一个任务需要怎样执行”。
 
@@ -759,9 +795,9 @@
 
 这组实验的部署分别为 Qwen 的 BF16／vLLM，以及 V4 的 MXFP4 专家 fallback、FP8 KV／SGLang，后者还使用 110 GiB CPU offload 与兼容适配。[配对检索记录](../experiments/ch02/02-09/paired-retrieval/README.md)保存了输入、原始响应与评分。对于这八道题，两个部署均完成任务；将其长度代回各自模型表，就能同时得到任务结果和对应资源需求。
 
-![图 2-10 请求输入与计算状态映射](ch02/figure-2-10-request.svg)
-
-*图 2-10：$S=0$、$P=128$、$G=4$ 的请求只有三次后续 decode。首输出来自输入阶段，最后输出尚未成为模型输入。下方按该调用链累计四模型的矩阵运算量；右侧八题任务展示同文本经不同分词器形成各自输入长度的过程。*
+![图 2-15 请求输入与计算状态映射](ch02/figure-2-10-request.svg)
+
+*图 2-15：$S=0$、$P=128$、$G=4$ 的请求只有三次后续 decode。首输出来自输入阶段，最后输出尚未成为模型输入。下方按该调用链累计四模型的矩阵运算量；右侧八题任务展示同文本经不同分词器形成各自输入长度的过程。*
 
 > **实验 2-9 ★★〔延伸〕：请求条件下的模型选择**
 >
@@ -802,7 +838,7 @@
 | 已知轨迹字段代入 | `python3 calculations/calc.py trace-resource-bridge --format md` |
 | 四模型请求比较 | `python3 calculations/calc.py request-model-comparison --format md` |
 
-图 2-1 至图 2-10 的数据、生成程序和来源校验见[本章配图目录](ch02/README.md)。机制图为本书自绘，数据图为指定条件下的推算；涉及实测的段落均链接原实验配置与记录。固定来源与产物哈希随配图保存，后续更新数据时应重新生成并检查正文数字。
+图 2-1 至图 2-15 的数据、生成程序和来源校验见[本章配图目录](ch02/README.md)。机制图为本书自绘，数据图为指定条件下的推算；涉及实测的段落均链接原实验配置与记录。固定来源与产物哈希随配图保存，后续更新数据时应重新生成并检查正文数字。
 
 ## 参考资料与证据范围
 
--- before/03-推理与训练负载.md
+++ manuscripts/03-推理与训练负载.md
@@ -51,6 +51,8 @@
 
 ### 3.1.3 请求分布
 
+TTFT 和输出间隔描述单个请求的体验。多个请求陆续到达时，这些指标还取决于它们是否争用同一时段的处理能力。因此，只知道平均长度和平均请求率还不够，还要看长短请求怎样组合、何时到达。
+
 负载描述应保留到达时间、输入与输出长度的联合分布。长请求既增加自身服务时间，也可能延长状态驻留；短请求提前完成，则可能让后续请求较早开始。因此，平均工作相同不能保证等待、完成时间和状态峰值都相同。先用章首的两分钟负载把这个区别算清，再观察真实系统。
 
 生产请求还会随客户组成改变。ServeGen 的研究覆盖阿里云 Model Studio 的 12 个模型、约 35.4 亿请求。收集横跨四个月，各模型在表中的分析窗口是一周或一个月。研究观察到，同一客户的请求形状相对稳定，而不同客户的速率变化会改变整体长度分布。于是，“来自哪个客户”“在什么时候到达”“输入与输出怎样配对”成为负载描述的一部分。论文中的一个规划案例，真实负载需要 24 个实例，ServeGen 估计为 25，仅采用简化负载的 NAIVE 方法估计为 12；这个结果绑定当时的 Qwen2.5-14B、A100、vLLM 配置和时限要求。[^servegen]
@@ -115,23 +117,27 @@
 
 ### 3.2.2 Reasoning、验证与成功任务成本
 
-增加推理时的计算量，主要有三种方式：延长一条回答中的思考过程，同时生成多份回答并从中选择，或者根据题目难度分配生成次数和长度。延长思考会增加串行生成时间与历史状态；生成多份回答会增加计算和验证次数；按难度分配则需要决定每道题何时停止。
+前缀缓存减少的是已有输入的重复计算。另一类方法则主动增加生成计算，希望提高答案的正确率。增加推理时的计算量，主要有三种方式：延长一条回答中的思考过程，同时生成多份回答并从中选择，或者根据题目难度分配生成次数和长度。延长思考会增加串行生成时间与历史状态；生成多份回答会增加计算和验证次数；按难度分配则需要决定每道题何时停止。
 
 比较这些方法时，先规定怎样的答案算正确，再统计获得正确答案花费的时间和费用。长思考将计算集中在一条回答上，多次采样则把计算分给不同回答，由验证器选择可以采用的结果。总成本要包括生成、验证以及未被采用的回答。
 
 **陷阱实例：生成更快，任务仍然没有完成。** 实验 3-3 对同一道题独立生成多份回答，比较逐份生成、并发生成，以及按题目规模决定生成份数三种方式。固定四道题、观察两轮后发现，无论将输出上限设为 1024 还是 4096 token，都没有得到符合预定评分标准的最终答案。[^reasoning] 这些尝试已经消耗计算资源，但由于成功数为零，还无法计算获得一个成功结果的平均成本。
 
-关闭 thinking 后的格式与答案诊断保留在[实验材料](../experiments/ch03/03-03/no-thinking/README.md)。诊断可以解释失败，预先规定的评分标准仍使用事先规定的接口与正确性要求。[^reasoning-off]
+关闭 thinking 后的格式与答案诊断保留在[实验材料](../experiments/ch03/03-03/no-thinking/README.md)。这些材料按接口格式和答案正确性分别分析失败原因，并保留实验前确定的评分标准。[^reasoning-off]
 
 单 token 变便宜也不能替代这项检查。用一个简单假设说明：新系统 token 单价降到原来的 1/10，每任务用量却增加到 20 倍，成功率由 50% 提高到 80%。只计模型费用，并假定重复尝试的分布稳定，每个成功任务的费用比为 $2\times\frac{0.5}{0.8}=1.25$。价格下降、质量提高和成功任务费用上升可以同时发生。对带历史的 Agent，可以沿实际轨迹累加各次尝试，再以通过检查的任务数作分母。[^cost]
 
+![图 3-3 每次尝试的费用与每个成功任务的费用](ch03/figure-3-success-cost.svg)
+
+*图 3-3：采用正文的教学假设，将原系统一次尝试的模型费用记为 1 单位。新系统的 token 单价降到十分之一、用量增至二十倍，因此一次尝试花费 2 单位。左图用 100 次尝试表示 50% 与 80% 的成功率；右图将全部尝试的费用除以成功数，得到 2 与 2.5 单位。*
+
 > **实验 3-3 · 延伸：Reasoning 预算与正确结果成本**
 >
 > 读取三批回答记录，分别统计截断、格式合规、数值正确与最终任务通过。保留原评分，再单列事后分析。若继续比较串行、并行和动态预算，应先得到满足同一质量要求的结果；未成功的记录仍计入消耗，成功成本保持未定义。
 
 ### 3.2.3 Agentic：模型、工具与检索的循环
 
-代码 Agent 的一轮执行可以分成四段：模型决定下一步，工具执行，工具结果返回，模型把结果接入下一次输入。编译日志、测试错误、文件内容都在这个循环中成为新数据。工具不仅增加等待，还会改变下一轮的输入长度和分支。
+验证不仅可以在最后判断答案，也可以在任务进行中提供反馈。代码 Agent 会把测试结果放回上下文，据此决定下一次修改。一轮执行可以分成四段：模型决定下一步，工具执行，工具结果返回，模型把结果接入下一次输入。编译日志、测试错误、文件内容都在这个循环中成为新数据。工具不仅增加等待，还会改变下一轮的输入长度和分支。
 
 本书选用一个具体任务：修复区间合并函数，使嵌套区间和端点相接的区间正确合并，同时不修改输入及其嵌套列表。模型可以读取指定文件、改写它、运行固定测试并结束任务，控制器不替模型修改代码。关闭 thinking 时，模型在 12 轮内读文件一次、改写五次、运行测试六次，仍未完成；开启后，四轮分别是输出截断、写文件、运行测试和结束。首轮无效生成也花了时间，必须留在任务记录中。[^agent]
 
@@ -139,7 +145,7 @@
 
 ### 3.2.4 分支、工具等待与状态驻留
 
-先看同一代码任务的两种实际轨迹。
+工具循环确定了任务的依赖关系，接下来沿实际轨迹计算时间和状态寿命。先比较同一代码任务开启与关闭思考模式的两组记录。
 
 | 观察 | Thinking 关闭 | Thinking 开启 |
 | --- | ---: | ---: |
@@ -154,9 +160,9 @@
 
 开启模式共输出 2733 个 token，其中 2553 个在 thinking 结束符之前，3 个为结束符，177 个在之后。思考占据绝大多数生成工作，工具调用则把生成的决定转为文件修改与测试。模型墙钟与工具墙钟沿轨迹依次相加，便形成用户经历的任务时间。
 
-![图 3-3 Agent 轨迹与 KV 生命周期](ch03/figure-3-3-agent.svg)
-
-*图 3-3：四轮模型和工具时间来自真实记录；每轮命中来自引擎。逻辑 KV 仅按“工具等待时保留本轮全部状态”的假设计算，未测得真实块驻留。分支图单独标为教学示意，这条实测轨迹是串行的。*
+![图 3-4 Agent 轨迹与 KV 生命周期](ch03/figure-3-3-agent.svg)
+
+*图 3-4：四轮模型和工具时间来自真实记录；每轮命中来自引擎。逻辑 KV 仅按“工具等待时保留本轮全部状态”的假设计算，未测得真实块驻留。分支图单独标为教学示意，这条实测轨迹是串行的。*
 
 **例题 3-2：加速首轮能节省多少任务时间？** 用第一章的 Amdahl 分析，假设后续行为与质量不变。
 
@@ -174,6 +180,8 @@
 
 ## 3.3 多模态与实时交互
 
+Agent 的工具结果通常以文本进入下一轮输入。若输入换成图片或连续语音，模型前面还要增加编码过程，输出端也可能需要持续播放。我们先看数据在阶段之间怎样改变形状，再看各阶段必须在何时完成。
+
 ### 3.3.1 视觉 E→P→D 与 EC／KV
 
 图片进入语言模型之前，还要经过预处理、视觉编码和投影。用 E 表示视觉编码，P 表示语言 prefill，D 表示后续 decode，一次视觉问答便有 E→P→D 这条基本依赖。图片大小影响 E，编码后视觉位置数影响语言部分；这两项工作应分别计算。
@@ -182,7 +190,11 @@
 
 但编码结果不止这一份。模型还取三个中间视觉层的 DeepStack 特征，固定实现将它们与最终输出一起输出，完整张量为 $[400,10240]$，占 8,192,000 bytes，即 7.8125 MiB。四组特征对应同一组 400 个位置：特征宽度变为四倍，语言序列仍增加 400 个位置。视觉阶段本身的已计矩阵运算量约 1.310 TFLOPs，尚不含 CPU 图片解码、resize 和后续语言计算。[^vision]
 
-编码结果称为 EC，语言主干产生的状态是 KV。该配置的语言 KV 每位置占 144 KiB，400 个视觉位置对应 56.25 MiB。于是同一张教学截图可以同时有三个大小：原始压缩文件 0.8 MB、完整 BF16 EC 7.8125 MiB、视觉位置的逻辑 KV 56.25 MiB。原图经过视觉编码形成 EC，EC 进入语言主干后形成各层 KV，表示随处理阶段逐步展开。[^epd]
+![图 3-5 图像位置合并与特征宽度的变化](ch03/figure-3-vision-shapes.svg)
+
+*图 3-5：左图展开 $2	imes2$ 空间合并：位置数从 1600 减至 400。右图把四组特征沿宽度方向拼接，得到 $[400,10240]$ 的 EC；位置数仍为 400。语言模型再为这些位置保存各层 KV，因此 EC 的字节数与 KV 的字节数不同。*
+
+图 3-5 中，视觉编码改变了位置数和特征宽度；进入语言模型后，还会产生另一种表示。编码结果称为 EC，语言主干产生的状态是 KV。该配置的语言 KV 每位置占 144 KiB，400 个视觉位置对应 56.25 MiB。于是同一张教学截图可以同时有三个大小：原始压缩文件 0.8 MB、完整 BF16 EC 7.8125 MiB、视觉位置的逻辑 KV 56.25 MiB。原图经过视觉编码形成 EC，EC 进入语言主干后形成各层 KV，表示随处理阶段逐步展开。[^epd]
 
 | 对象 | 本例大小 | 决定它能否复用的条件 |
 | --- | ---: | --- |
@@ -196,6 +208,8 @@
 
 ### 3.3.2 音频与图像生成的工作单位
 
+视觉问答仍通过语言模型逐 token 生成文本。若输出本身是声音或图像，就需要重新确定一次计算更新什么、循环多少次，不能直接把文本的生成步数套用过去。
+
 音频输出还有帧内循环。以 Fish Audio S2 Pro 为例，slow 路径沿音频时间推进，fast 路径在每帧内补齐码本，再由 codec 解成波形。固定路径每帧先做一次 fast 预热，再做九次预测，第一次未使用的 logits 也付出了计算。语言 token/s、声学帧/s 与每秒生成多少秒声音因而是不同指标。Qwen3-Omni 的 Talker、码本与波形阶段保留在[多模态计算资料](../case-studies/generative-multimodal-models.md)，同样按实际调用计数。[^media]
 
 图像生成又换了一种循环。以本书固定 Qwen-Image-2512 路径为例，$1024\times1024$ 图像形成 4,096 个 latent 位置，执行 50 个去噪步，true CFG 路径每步有两支 DiT 前向，总共是 100 次。去噪矩阵运算量约 $7.830\times10^{15}$ FLOPs；文本编码器与 VAE 还有各自的工作。latent 的位置集合在去噪期间保持固定，各步持续更新其内容，累计工作随去噪步数增加。[^image]
@@ -206,7 +220,9 @@
 
 ### 3.3.3 连续感知、首段响应与打断
 
-实时任务还要求及时响应。截图 Agent 如果只在动作结束后观察屏幕，就可能漏掉中途出现又消失的弹窗。作者参与的 AOI 研究将屏幕观测与动作执行分开：持续收集图像、音频和事件，再筛选出供模型使用的记录。增加观测频率可以发现更多短暂事件，但保存更多关键帧也会占用上下文；论文摘要报告了部分模型效果下降的现象。[^aoi] 因此，持续观测还要配合筛选，决定哪些内容值得保留并交给模型处理。
+算出声学码、图像位置和内部迭代数，只能得到工作总量。实时交互还要求这些工作按时完成：用户能否连续听到声音，取决于下一块音频是否在缓冲耗尽前到达。
+
+时间要求同时作用于输入和输出。先看输入端：截图 Agent 如果只在动作结束后观察屏幕，就可能漏掉中途出现又消失的弹窗。作者参与的 AOI 研究将屏幕观测与动作执行分开：持续收集图像、音频和事件，再筛选出供模型使用的记录。增加观测频率可以发现更多短暂事件，但保存更多关键帧也会占用上下文；论文摘要报告了部分模型效果下降的现象。[^aoi] 因此，持续观测还要配合筛选，决定哪些内容值得保留并交给模型处理。
 
 语音的时间约束更直接。用户停止说话后，系统要采集或确认输入，经过识别／编码、推理、语音生成和播放。首块在客户端到达，只说明数据已到；扬声器开始出声还取决于解码、缓冲和设备时钟。开始播放后，后续音频还必须及时到达，才能避免中断。
 
@@ -222,9 +238,9 @@
 
 将缓冲改为 60 ms，这次停顿消失，首播却推迟到 98 ms。增加缓冲可以吸收本次抖动，代价是更晚开始；若音频的平均到达速度长期低于播放速度，有限缓冲终究会耗尽。[^audio]
 
-![图 3-4 实时交互的端到端时序](ch03/figure-3-4-realtime.svg)
-
-*图 3-4：上部连接视觉和语音的阶段；中部用教学时长展示到达、播放和迟到；下部把两份历史记录的首块接收时间与未知的播放／取消项分开。教学缓冲对照不作为两份真实语音的性能解释。*
+![图 3-6 实时交互的端到端时序](ch03/figure-3-4-realtime.svg)
+
+*图 3-6：上部连接视觉和语音的阶段；中部用教学时长展示到达、播放和迟到；下部把两份历史记录的首块接收时间与未知的播放／取消项分开。教学缓冲对照不作为两份真实语音的性能解释。*
 
 打断沿控制链依次生效：应用发出取消，播放设备静音，后端停止生成，缓冲与状态释放。教学时序在 123 ms 发出打断，控制延迟 5 ms，设备每 10 ms 生效一次，于是 130 ms 静音，用户感知响应为 7 ms。后端停止生成决定后续计算何时结束，缓冲释放决定容量何时可供其他任务使用，三者分别对应声音、计算与存储的停止时刻。[^audio-interrupt]
 
@@ -264,11 +280,15 @@
 
 一个微批与一次参数更新也要区分。显存放不下完整训练批时，可以分多个微批累计梯度，最后再更新一次参数。只要目标、有效标签和归一化保持一致，微批数增加并不意味着相同数据被训练了更多轮；它首先改变的是执行顺序与激活寿命。第十章将据此讨论分片和流水。
 
+![图 3-7 推理与各训练阶段的计算和状态](ch03/figure-3-5-training.svg)
+
+*图 3-7：前向—反向—更新的依赖。8K 训练对全部位置执行词表头，矩阵柱累计前向与两个梯度；参数状态按 BF16 权重、FP32 梯度、master 权重与 Adam 两个 moment 分解。激活沿前向保存，直到对应反向完成后释放。*
+
 训练过程中还要不断准备下一批输入。CPU 读取、解码、分词并整理批次，再把张量送到设备。双缓冲可以让当前批计算时准备下一批：一个缓冲供计算读取，另一个接收新数据，交替使用。计算耗尽已准备数据时会等待输入，取回设备结果时又会等待相应计算完成。第五章将结合前向、反向和参数更新的执行顺序，分析数据准备与计算如何重叠。
 
 ### 3.4.2 预训练、中期训练与 SFT
 
-预训练和中期训练常沿用相同的自回归预测与梯度更新过程，变化主要发生在数据、序列长度、学习率和能力目标。“中期训练”并没有统一分界，读报告时应看实际阶段。
+前向、反向和参数更新构成了一次训练步骤。预训练、中期训练与 SFT 都会使用这一过程，但数据组织和监督位置不同。预训练通常从广泛的文本中学习，中期训练再围绕后续能力目标调整数据、序列长度和学习率；具体阶段应结合模型报告中的训练安排理解。
 
 Qwen3 报告给出一条具体路径：通用阶段在 4,096 长度上使用超过 30T token；随后增加 STEM、代码、推理与合成数据比例，再训练约 5T，长度仍为 4,096；长上下文阶段采用数千亿 token，将长度扩到 32,768。该阶段语料中，75% 长度在 16,384–32,768，25% 在 4,096–16,384。第二阶段虽然叫 reasoning stage，仍是这里讨论的预训练阶段，不应与后训练 RL 混为一谈。[^qwen]
 
@@ -279,6 +299,8 @@
 部分参数训练减少需要保存梯度与优化器状态的参数集合。冻结模块的权重保持不变，但梯度仍需穿过它们传到较早的可训练模块，因此相应输入梯度继续执行。沿反向图逐层判断是否求输入梯度、是否求权重梯度，就能把训练状态与计算分别算清。
 
 ### 3.4.3 从 $6ND$ 到分项训练预算
+
+确定训练数据和有效 token 数之后，就可以累计总运算量。先用线性层“训练约为前向三倍”的关系作粗算，再把注意力、词表头和状态更新逐项补回，便能看清粗算与完整模型之间的差距。
 
 现在完整算一组固定输入：Qwen3-8B，$B=1,\ T=8192$，全参数训练，所有位置执行词表头，没有重计算。逐层累计投影、FFN、QK／PV 与输出头，矩阵运算量如下。[^train-matrix]
 
@@ -294,10 +316,6 @@
 
 将有效标签数减到 4096，如果仍对全部位置计算词表投影，矩阵运算量就不变。如果先取出有监督位置的隐藏向量，再只对它们执行词表投影，总量则降到 416.074 TFLOPs。主干网络仍处理全部输入，省下的是无监督位置的词表投影；反向时再把梯度放回对应位置。[^mask]
 
-![图 3-5 推理与各训练阶段的计算和状态](ch03/figure-3-5-training.svg)
-
-*图 3-5：前向—反向—更新的依赖。8K 训练对全部位置执行词表头，矩阵柱累计前向与两个梯度；参数状态按 BF16 权重、FP32 梯度、master 权重与 Adam 两个 moment 分解。激活沿前向保存，直到对应反向完成后释放。*
-
 矩阵表之外，归一化、激活、损失与优化器还有自己的计算和访问。[非矩阵补算](../calculations/results/training-nonmatrix-8192.md)按相同 8K 输入列出这些工作。短序列下，相对固定的参数更新成本可能更突出，因此改变长度后也需要重新检查各部分占比。[^nonmatrix]
 
 模型新增的前向操作，也会增加对应的反向计算。历史压缩需要对投影、池化和状态更新求梯度，mHC 需要向各残差通路传播梯度，MoE 则要对实际选中的专家求梯度。配套 V4 计算记录逐项列出了这些操作。[^v4-training] 第二章的计算图因而也能帮助我们判断：训练时要保存哪些中间结果，反向又要执行哪些运算。
@@ -320,6 +338,8 @@
 
 ### 3.5.2 有效样本、策略更新与权重交接
 
+生成回答和计算奖励之后，还要决定哪些样本用于学习。这里的关键是区分已经生成的样本与最终进入更新的样本：前者决定生成开销，后者决定训练批次，两者的数量未必相同。
+
 假设有八道题，每题生成四份回答，共得到 32 条样本，筛选后保留 16 条用于训练。其余回答虽然没有用于策略更新，生成和评分的开销却已经发生。因此，估算整个训练循环时，还要计入被筛掉的样本。
 
 是否保留一条样本，由训练方法及筛选规则决定。有的方法把失败回答作为负反馈，有的方法排除截断或奖励无效的回答。采用组内相对优势时，还要比较同一道题多份回答的奖励。因此，保留样本数决定更新时处理多少数据，奖励和损失函数则决定这些数据产生怎样的梯度。
@@ -331,6 +351,8 @@
 更新后还要把策略权重送给生成端。若生成端继续使用旧版本，下一批数据对应的策略就不同。同步循环可以等权重就绪后再生成；异步方案允许部分阶段重叠，却要处理样本版本与滞后。第三章先保留这条依赖，第十章再讨论交接、抢占、恢复和资源配比。
 
 ### 3.5.3 同一有效样本目标下的阶段需求
+
+这种区分可以用一个固定目标来量化：每轮都要保留 16 条样本用于更新。如果回答更难通过筛选，就必须先生成更多回答。下面保持更新批次不变，只增加为得到这批样本所需的生成和评分。
 
 **例题 3-4：固定有效样本目标，样本保留比例降低需要多少额外工作？**
 
@@ -349,11 +371,11 @@
 
 参考模型处理全部 32 份回答，策略更新只使用保留的 16 条样本，两阶段处理的数据量因此不同。评分时回答已经生成，可以同时计算多个位置；生成时后一个 token 要等待前一个 token 确定，只能逐步执行。因此，即使模型结构相同，这两阶段的矩阵形状和调用次数也不同。损失、优化器与环境验证的计算量再分别加入。
 
-如果样本保留比例从 1/2 降到 1/4，但仍要求得到 16 条保留样本，教学对照改为生成 64 条。生成与 参考模型计算翻倍，更新量不变，总矩阵运算量增到 3,410.701 TFLOPs，约增加 56.3%。每个保留样本承担的矩阵运算量从 136.347 增到 213.169 TFLOPs。增加的 1229.142 TFLOPs 全部来自额外回答的生成与 参考模型计算；进入更新的 16 条样本保持不变。[^rl-low]
-
-![图 3-6 RL／OPD 的工作与数据流](ch03/figure-3-6-rl.svg)
-
-*图 3-6：上部按生成、反馈、筛选、更新和权重交接画依赖，教师支路单列；下部为 Qwen 教学批次。图示参考 V4 的工作组织，但数值不是 V4 实测。保留 16 条的目标不变，未被采用的回答仍计入生成与评分。*
+如果样本保留比例从 1/2 降到 1/4，但仍要求得到 16 条保留样本，教学对照改为生成 64 条。生成与参考模型计算翻倍，更新量不变，总矩阵运算量增到 3,410.701 TFLOPs，约增加 56.3%。每个保留样本承担的矩阵运算量从 136.347 增到 213.169 TFLOPs。增加的 1229.142 TFLOPs 全部来自额外回答的生成与参考模型计算；进入更新的 16 条样本保持不变。[^rl-low]
+
+![图 3-8 RL／OPD 的工作与数据流](ch03/figure-3-6-rl.svg)
+
+*图 3-8：上部按生成、反馈、筛选、更新和权重交接画依赖，教师支路单列；下部为 Qwen 教学批次。图示参考 V4 的工作组织，但数值不是 V4 实测。保留 16 条的目标不变，未被采用的回答仍计入生成与评分。*
 
 一次完整 BF16 Qwen3-8B 权重快照为 16.381 GB。若给每个 rollout 副本各独立发送一次，数据量为副本数乘快照大小；优化器状态不需要因此一起发送。这只是最简单的传输模型，广播、增量更新、转换与设备布局会改变实际路径。
 
@@ -389,7 +411,7 @@
 
 **例题 3-5：怎样检验一条 Scaling Law？** 从公开训练记录中固定六个较小模型的观测用于拟合，另将两个较大模型的观测留出。先选定拟合方法，再比较留出预测与观察值。
 
-**解：** 本书从 datablations 的作者 notebook 与匹配训练日志整理八个 C4 观测，以 $N\ge2\times10^9$ 的两个点作事先留出。固定指数网格拟合后，两个观测损失约为 2.574、2.337，预测为 2.583、2.363，留出 RMSE 约 0.0193 nats/token。图 3-7 保留哪些观测用于拟合、哪些用于检验，完整系数和敏感性见[八点拟合](../calculations/results/datablations-real-c4-eight-point-fit.md)。[^fit]
+**解：** 本书从 datablations 的作者 notebook 与匹配训练日志整理八个 C4 观测，以 $N\ge2\times10^9$ 的两个点作事先留出。固定指数网格拟合后，两个观测损失约为 2.574、2.337，预测为 2.583、2.363，留出 RMSE 约 0.0193 nats/token。图 3-9 保留哪些观测用于拟合、哪些用于检验，完整系数和敏感性见[八点拟合](../calculations/results/datablations-real-c4-eight-point-fit.md)。[^fit]
 
 六个较小模型的观测用于确定曲线，两个较大模型的观测用于检验预测。两次预测与观测之差得到约 0.0193 nats/token 的 RMSE。将部分数据留作检验，可以直接观察拟合曲线在新规模上的预测误差。模型配置、训练数据和拟合系数均保存在配套记录中。
 
@@ -399,9 +421,9 @@
 
 长期服务会把每次调用的费用累积到训练预算之上。较小模型可以通过更长训练达到目标质量，前期多付费用，之后每次调用读取更少权重、执行更少工作。Beyond Chinchilla-Optimal 将这种推理需求纳入分析，47 个模型覆盖 150M–6B；其中 150M 模型测试到 10,000 token／参数，较大模型测试到 1,000。[^beyond] 训练预算与服务次数共同决定较小模型的额外训练何时收回成本。
 
-![图 3-7 训练预算与生命周期成本](ch03/figure-3-7-scaling.svg)
-
-*图 3-7：左为八个公开 C4 观测的拟合与固定留出误差；右将同一拟合的目标损失 2.9 代入生命周期成本估算。成本用抽象单位，虚线标出超出拟合 N/D 范围的模型；费用线交叉表示累计服务节省抵偿前期投入。*
+![图 3-9 训练预算与生命周期成本](ch03/figure-3-7-scaling.svg)
+
+*图 3-9：左为八个公开 C4 观测的拟合与固定留出误差；右将同一拟合的目标损失 2.9 代入生命周期成本估算。成本用抽象单位，虚线标出超出拟合 N/D 范围的模型；费用线交叉表示累计服务节省抵偿前期投入。*
 
 右图固定目标损失，按运算量估算费用：请求取 $P=512,G=128$，训练运算量为 $6ND$，每次调用为 $2N[P+(G-1)]$，每 FLOP 按 $10^{-18}$ 个费用单位计价。0.1B 模型需要约 298.6B 训练 token，超过拟合所用数据量的上界，因此图中以虚线标注。按这条外推曲线计算，调用约 2.048 亿次时，0.1B 与 0.5B 模型的总费用相等。在此之前，0.1B 模型多付的训练费用尚未收回；在此之后，每次服务省下的费用累计超过了前期投入。[^lifecycle]
 
@@ -411,7 +433,9 @@
 
 ### 3.6.2 从 Llama 到 Qwen 的训练投入
 
-公开产品记录展示了参数与数据预算的实际选择。先按各报告披露的阶段与规模观察一条变化：相近的 7–8B 参数规模，训练 token 数持续增加。[^history]
+前面的拟合说明了模型规模与训练数据之间可以怎样取舍。下面回到公开模型，观察开发者实际选择了怎样的训练投入：先比较每个参数对应的训练 token 数，再将算法运算量与具体硬件上的训练时间区分开。
+
+从各报告披露的阶段和规模，可以看到一个趋势：相近的 7–8B 参数规模，训练 token 数持续增加。[^history]
 
 | 模型与报告范围 | 训练 token | $D/N$ 粗略估算 | $6ND$ 粗略估算 |
 | --- | ---: | ---: | ---: |
@@ -433,9 +457,9 @@
 
 Flash 的主干专家约 45.4495 GFLOPs/token，按 32T 累计约 $1.45438\times10^{24}$ FLOPs；Pro 约 169.2465 GFLOPs/token，按 33T 累计约 $5.58513\times10^{24}$ FLOPs。这只含专家矩阵，注意力、路由、MTP、优化器和重计算仍未加入。报告的损失权重也不是执行比例，例如 MTP 权重为 0.3，不表示只执行 30% 的辅助计算。[^training]
 
-![图 3-8 Llama 与 Qwen 的模型—数据选择](ch03/figure-3-8-history.svg)
-
-*图 3-8：左侧按公开训练记录画相近规模 dense 模型的 $D/N$；右侧将 MoE 的总参数与激活参数并列。模型年代、训练数据量与总参数、激活参数分别标注，两类参数分别反映模型容量与单次计算规模。*
+![图 3-10 Llama 与 Qwen 的模型—数据选择](ch03/figure-3-8-history.svg)
+
+*图 3-10：左侧按公开训练记录画相近规模 dense 模型的 $D/N$；右侧将 MoE 的总参数与激活参数并列。模型年代、训练数据量与总参数、激活参数分别标注，两类参数分别反映模型容量与单次计算规模。*
 
 > **实验 3-9 · 延伸：Llama 与 Qwen 的训练投入**
 >
@@ -447,9 +471,9 @@
 
 用 GPU 小时换算日历时间，要除以同期使用的卡数。对 Llama 3.1 405B，取公开的 30.84M H100 GPU 小时，并假设这项工作全部使用最大规模 16,384 卡，计算为 $30.84\times10^6/(16384\times24)\approx78.43$ 天。阶段使用的卡数更少时，相同 GPU 小时需要更长日历时间；训练资源的分配时序因此决定训练完成时间。
 
-![图 3-9 公开 GPU 小时与训练阶段](ch03/figure-3-9-gpu-hours.svg)
-
-*图 3-9：A100、H100、H800 分面，V3 按阶段拆开。Qwen2.5／3／3.5 以及 V4 的完整预训练 GPU 小时尚未披露，单列未披露；Qwen3 特定后训练分支的小时不填入预训练列。*
+![图 3-11 公开 GPU 小时与训练阶段](ch03/figure-3-9-gpu-hours.svg)
+
+*图 3-11：A100、H100、H800 分面，V3 按阶段拆开。Qwen2.5／3／3.5 以及 V4 的完整预训练 GPU 小时尚未披露，单列未披露；Qwen3 特定后训练分支的小时不填入预训练列。*
 
 Qwen3 表 21 将特定 8B 模型的 RL 与 on-policy distillation 两条后训练路线并列，投入分别为 17,920 与 1,800 GPU 小时。两者是替代路线，各自形成生成、反馈与学习工作。将 GPU 小时连接到具体阶段和设备，才能沿本章的工作图解释投入来自哪里。
 
