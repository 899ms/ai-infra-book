--- second-pass-before/02-模型架构.md
+++ manuscripts/02-模型架构.md
@@ -4,9 +4,9 @@
 
 本章从一个 token 的数值表示开始，逐步分析它经过网络各层后，如何得到下一个 token 的分数，再计算这一过程所需的存储容量 $M$、运算量 $F$ 和读写量 $R$。计算图用节点表示运算，用边表示运算之间传递的数据。先认识图上的对象，才能知道资源预算中的每个数字来自哪里。
 
-这张计算图也提供了优化实现所需的信息。哪些权重在不同请求间保持不变，哪些状态会被后续生成反复使用，哪些结果只供紧接着的运算使用，都可以从模型结构中找到。后面的存储、编译和调度设计将利用这些数据在用途上的区别，决定哪些数据可以共享、哪些需要长期保存、哪些只需临时缓冲。
-
-我们使用 Qwen3-8B 作为贯穿例子。这是一个约含 81.9 亿参数的语言模型，通过 36 层网络处理文本。先完整理解这一种模型，再改变上下文的保存方式、每个 token 使用的参数集合和层间连接，比较 Qwen3.6-35B-A3B（下文简称 Qwen3.6）、DeepSeek V4-Flash 与 Kimi K3 的资源需求。
+这张计算图也提供了优化实现所需的信息。哪些权重在不同请求间保持不变，哪些状态会被后续生成反复使用，哪些结果只供紧接着的运算使用，都可以从模型结构中找到。后面的存储、编译和调度设计将利用这些数据在用途上的区别，决定哪些数据可以共享、哪些需要长期保存、哪些只需暂存。
+
+我们使用 Qwen3-8B 作为贯穿例子。这是一个约含 81.9 亿参数的语言模型，通过 36 层网络处理文本。先完整地理解这个模型，再改变上下文的保存方式、每个 token 使用的参数集合和层间连接，比较 Qwen3.6-35B-A3B（下文简称 Qwen3.6）、DeepSeek V4-Flash 与 Kimi K3 的资源需求。
 
 统计运算量时，一次乘法加一次加法计为 2 FLOPs。存储量采用 GB 和 GiB：$1\ \mathrm{GB}=10^9\ \mathrm{bytes}$，$1\ \mathrm{GiB}=2^{30}\ \mathrm{bytes}$。BF16 是每个数占 16 位的浮点表示，每个元素占 2 字节，因此将张量的元素数乘以 2，就得到它的存储大小。
 
@@ -34,13 +34,13 @@
 
 编码器把已知输入转换为表示，解码器根据允许访问的信息逐步产生输出。现代因果语言模型常用只含解码器的结构（decoder-only），本章采用这一结构。处理已知输入时，同层位置可以并行；生成时，第一个输出确定后才能成为下一次输入。因此，输入处理沿网络深度推进，生成还增加了跨调用的串行依赖。[^source-10]
 
-> **练习 2-1〔延伸〕：从依赖图判断并行**
+> **练习 2-1〔延伸〕：RNN 与 Transformer 的依赖如何限制并行计算**
 >
 > 对于含四个已知输入位置的序列，分别画出三层 RNN 和三层因果 Transformer 的计算依赖图。再增加一个待生成的输出位置，指出计算该位置之前必须等待哪个节点完成。设每层每位置的计算耗时为一个单位、并行资源充足，分别求完成已知输入的最长依赖链长度；解释增加计算单元可以缩短哪些等待、不能缩短哪些等待。
 
 ### 2.1.2 模型配置与张量结构
 
-先把一个计算节点展开。设本次为 $m$ 个输入 token 计算投影，每个 token 用一个包含 $k$ 个特征的向量表示。把这些向量逐行排成输入矩阵 $X$，它就有 $m$ 行、$k$ 列，输出宽度为 $n$：
+先把一个计算节点展开。设本次对 $m$ 个输入 token 执行投影，每个 token 用一个包含 $k$ 个特征的向量表示。把这些向量逐行排成输入矩阵 $X$，它就有 $m$ 行、$k$ 列，输出宽度为 $n$：
 
 $$
 X\in\mathbb R^{m\times k},\qquad W\in\mathbb R^{k\times n},\qquad Y=XW\in\mathbb R^{m\times n}.
@@ -54,13 +54,13 @@
 
 $N_W$ 是权重参数的数量，$b_W$ 是每个参数占用的字节数。行数 $m$ 翻倍，运算量翻倍，权重大小保持不变；输出宽度 $n$ 翻倍，权重数量与运算量都翻倍。这两个变化分别对应“同一模型处理更多输入”与“模型的一层变得更宽”。下面的模型表都可以用这条规则逐行计算。
 
-先只看 Qwen3-8B 的一条数据路径。一次**前向执行**是使用当前权重从输入算到输出的过程；隐藏向量是网络内部传递的特征表示，其维度给出每个位置保存多少个数。**前馈网络**（feed-forward network，FFN）对每个位置分别做特征变换；**注意力**则在位置之间选择并汇总信息。这两类运算交替组成网络的一层。
+先只看 Qwen3-8B 的一条数据路径。一次**前向执行**是使用当前权重从输入算到输出的过程；隐藏向量是网络内部传递的特征表示，其维度表示每个位置包含多少个数。**前馈网络**（feed-forward network，FFN）对每个位置分别做特征变换；**注意力**则在位置之间选择并汇总信息。这两类运算交替组成网络的一层。
 
 Qwen3-8B 包含 $L=36$ 层，隐藏维度为 $d=4096$，FFN 中间维度为 $f=12288$；注意力把特征分成多组并分别建立位置间的关系，每一组称为一个头。这里有 32 个查询头和 8 组供查询使用的键值头（KV 头），每头 128 维；2.1.3 将沿查询、键和值的路径解释它们。词表包含 151936 个 token，词嵌入是一张把 token 编号映射为向量的查找表，输出头则把末层向量变成每个候选 token 的分数。两者各自保存一份权重。各层参数与这两份词表矩阵相加，模型共有约 81.9 亿个参数。[^source-1]
 
 一次前向执行从 token ID（词元在词表中的整数编号）开始。词嵌入为每个 ID 查出一个 4096 维向量，向量依次经过 36 层注意力与 FFN，最后经过归一化和词表投影产生 logits，即尚未转换为概率的候选 token 分数。归一化按向量的统计量调整各特征的尺度，使逐层计算的数值保持在适当范围内。词嵌入按 ID 选择行，一个 token 取出一条向量，整个词表则作为常驻数据供不同 token 查询。词表输出头则将隐藏向量与词表矩阵相乘；只生成下一个 token 时，通常只需要最后一个输入位置的 logits。
 
-设同时处理 $B$ 条等长请求，每条本次输入 $P$ 个已知 token。投影和 FFN 可以把它们的输入表示为 $X\in\mathbb{R}^{BP\times4096}$，每一行是一个 token 在当前层的表示，共有 $m=BP$ 行。已有上下文中从缓存读取的键和值不计入这里的 $m$。每条请求只补入一个 token 时，$m=B$。第一种情形容易形成行数较大的矩阵，第二种在小 batch 下接近矩阵与向量的乘法。权重相同，输入形状却明显不同。
+设同时处理 $B$ 条等长请求，每条本次输入 $P$ 个已知 token。投影和 FFN 可以把它们的输入表示为 $X\in\mathbb{R}^{BP\times4096}$，每一行是一个 token 在当前层的表示，共有 $m=BP$ 行。已有上下文中从缓存读取的键和值所对应的位置，不计入这里的 $m$。每条请求只补入一个 token 时，$m=B$。第一种情形容易形成行数较大的矩阵，第二种在小 batch 下接近矩阵与向量的乘法。权重相同，输入形状却明显不同。
 
 下面比较两种输入形状：一条请求一次处理 8192 个输入位置，以及一条已有 8192 个上下文位置的请求再处理一个新位置。用 $B$ 表示请求数，$S$ 表示调用前已有的上下文长度，$P$ 表示本次输入长度，$d$ 表示隐藏维度。前者取 $B=1,S=0,P=8192$，后者取 $B=1,S=8192,P=1$。
 
@@ -97,7 +97,7 @@
 | $t_e$ | 分派给专家 $e$ 的 token 数，每个 token 表示占该专家输入矩阵的一行；路由过程中不丢弃分派给专家的 token 时 $\sum_e t_e=mk_{\mathrm{top}}$ |
 | $m_{\mathrm{out}}$ | 实际进行词表投影的 token 位置数；只用每条请求的末位置预测下一个 token 时为 $B$，它不等于本次生成的全部输出长度 |
 
-计算整模型时，先按表格中每项操作的矩阵尺寸求一次调用的运算量，再乘这一类层的数量。词表头另外按实际输出位置数 $m_{\mathrm{out}}$ 计算；生成下一 token 时，每条请求只需最后一个位置的分数。归一化、激活与查表操作各有自己的运算方式，下面沿数据流说明它们的作用。
+计算整个模型的资源需求时，先按表格中每项操作的矩阵尺寸求一次调用的运算量，再乘这一类层的数量。词表头另外按实际输出位置数 $m_{\mathrm{out}}$ 计算；生成下一 token 时，每条请求只需最后一个位置的分数。归一化、激活与查表操作各有自己的运算方式，下面沿数据流说明它们的作用。
 
 ### 2.1.3 注意力计算与位置表示
 
@@ -109,7 +109,7 @@
 
 采用数学右乘记号，输入 $X$ 分别乘三个权重矩阵，得到 $Q$、$K$、$V$。$Q$ 的总宽度为 $32\times 128=4096$，$K$ 和 $V$ 的总宽度各为 $8\times 128=1024$。因此，$W_q$ 为 $[4096,4096]$，$W_k$ 和 $W_v$ 各为 $[4096,1024]$。代码中的线性层常以 $(d_{\mathrm{out}},d_{\mathrm{in}})$ 保存权重；换成数学右乘记号后，输入宽度在前、输出宽度在后，便可逐项对照下面的表格。
 
-上述投影产生了 $Q$、$K$、$V$，但尚未让当前位置与上下文交换信息。接下来将 $Q$ 拆成 32 个头，把 $K$、$V$ 拆成 8 个头。每组四个 $Q$ 头共用一个 KV 头。对一个 $Q$ 头，查询向量与允许访问位置的 $K$ 做点积，将点积除以头宽的平方根，使分数尺度随头宽保持稳定，然后屏蔽未来位置。Softmax 把允许位置的分数 $s_j$ 变成正数并归一化，系数为 $a_j=e^{s_j}/\sum_i e^{s_i}$；这些系数之和等于一，决定各上下文位置贡献多少信息。这里的“注意力权重”是本次输入算出的系数，区别于训练后保存的模型参数。随后用这些权重对 $V$ 加权求和。各 $Q$ 头的结果拼接后，经 $[4096,4096]$ 的输出投影 $W_o$ 转回隐藏向量。
+上述投影产生了 $Q$、$K$、$V$，但尚未让当前位置与上下文交换信息。接下来将 $Q$ 拆成 32 个头，把 $K$、$V$ 拆成 8 个头。每组四个 $Q$ 头共用一个 KV 头。对一个 $Q$ 头，查询向量与允许访问位置的 $K$ 做点积，将点积除以头宽的平方根，避免分数的数值尺度随头宽增大而过度增长，然后屏蔽未来位置。Softmax 把允许位置的分数 $s_j$ 变成正数并归一化，系数为 $a_j=e^{s_j}/\sum_i e^{s_i}$；这些系数之和等于一，决定各上下文位置贡献多少信息。这里的“注意力权重”是本次输入算出的系数，区别于训练后保存的模型参数。随后用这些权重对 $V$ 加权求和。各 $Q$ 头的结果拼接后，经 $[4096,4096]$ 的输出投影 $W_o$ 转回隐藏向量。
 
 用矩阵记号写出一个头的计算：
 
@@ -153,7 +153,7 @@
 
 *图 2-5　SwiGLU 的两条升维分支。gate 经 SiLU 后调节 up 的对应元素，乘积再由 down 投影降回主干维度。*
 
-注意力的 QKV 与输出投影合计约 4194 万个参数。将它与 FFN 的约 1.51 亿相加，FFN 占这些主要投影参数的约 78%，因此短上下文的计算中，三个 FFN 矩阵占据很大份额。随着输入变长，FFN 随位置数线性增长，全注意力则随 $P(P+1)/2$ 增长。两者增长速度不同，因此各部分计算量的占比会随上下文长度变化。
+注意力的 QKV 与输出投影合计约有 4194 万个参数。加上 FFN 的约 1.51 亿个参数后，FFN 占这些主要投影参数的约 78%，因此短上下文的计算中，三个 FFN 矩阵占据很大份额。随着输入变长，FFN 的运算量随位置数线性增长，全注意力的运算量则随 $P(P+1)/2$ 增长。两者增长速度不同，因此各部分计算量的占比会随上下文长度变化。
 
 残差连接把一个子层的输入保留下来，在该子层完成变换后，与输出逐元素相加。这样，一条路径执行新变换，另一条路径直接传递已有表示。下面把注意力、前馈和两次残差连接连成完整的一层。
 
@@ -205,7 +205,7 @@
 
 prefill 同时处理 8192 个输入位置，每个位置都要经过投影和 FFN。decode 只对新位置执行这些运算，旧位置的 $K$、$V$ 则从缓存读取。因此，缓存省去了旧位置的投影和 FFN 重算，当前查询与上下文的注意力计算仍要执行。下一节将计算这部分缓存和访问。
 
-> **练习 2-2〔核心〕：输入行数相同，注意力为何不同**
+> **练习 2-2〔核心〕：输入行数相同，注意力计算量为何不同**
 >
 > 使用表 2-1，取 $B=4,S=4096,P=1024$，分别计算投影与 FFN、有效因果注意力，以及最后一个位置的词表输出层所需的 FLOPs。保持 $BP=4096$，改为 $B=1,S=4096,P=4096$，先预测哪些项相同，再计算差异。最后比较两种输入处理方式：完整处理 8K 输入，或复用前 6144 个位置的 KV、只处理新增的 2048 个位置。求后一种方式节省的矩阵运算比例。
 
@@ -255,7 +255,7 @@
 B_* = \frac{R_W}{c_{\mathrm{KV}}H}.
 $$
 
-以 $R_W=15{,}136{,}811{,}008$ 字节、$c_{\mathrm{KV}}=147456$ 字节代入，$H=8192$ 时最小整数 batch 为 13；$H=2048$ 时为 51。上下文缩短到四分之一，使上下文读取量超过权重读取量所需的请求数约增至四倍。上下文越长，批内权重复用的收益就越早受到限制，因为每个新增请求都会增加一份独立上下文的读取。[^source-12]
+以 $R_W=15{,}136{,}811{,}008$ 字节、$c_{\mathrm{KV}}=147456$ 字节代入，$H=8192$ 时最小整数 batch 为 13；$H=2048$ 时为 51。上下文长度缩短到原来的四分之一，使上下文读取量超过权重读取量所需的请求数约增至四倍。上下文越长，批内权重复用的收益就越早受到限制，因为每个新增请求都会增加一份独立上下文的读取。[^source-12]
 
 沿第一章的数据路径看，加载把权重从存储送到 GPU 显存，执行再把所需权重送到计算单元。KV 和层间激活也可在设备内部保存与使用。相同的逻辑访问因此可以留在片上、经过 HBM，或跨设备传输；第四章将分别分析这些路径的存储容量与传输带宽。
 
@@ -295,9 +295,11 @@
 
 ## 2.3 上下文状态表示与访问机制
 
-这里的上下文状态，是模型处理已有上下文后保存、供后续计算复用的表示，例如 KV 缓存或递推状态。上下文长度按 token 位置计数，状态容量按实际保存的字节数计量。
-
-完整上下文 KV 的容量随序列长度增长，累计读取又随生成次数增加。改变模型结构，可以从几个方面降低这些开销：减少每个上下文位置的表示宽度，压缩或选择上下文条目，或者把上下文信息汇总到固定大小的状态中。这些选择影响的对象不同，也会产生不同的新增工作。比较时需要同时计算状态占用的空间，以及选择、读取和更新状态所需的工作。
+模型继续生成时，需要利用前面已经读过的内容。先想象两种记录办法：一种逐页留下记录，要查哪一页就取哪一页；另一种只保留一张定长摘要，每读到新内容，就改写这张摘要。前一种记录随历史增长，后一种大小固定，但不能保证找回每个位置的全部细节。
+
+对应到模型，保存逐位置 KV 的机制留下各位置的键和值，有限状态递推则把历史贡献汇入固定大小的状态。这里的“记录”和“摘要”实际都是供计算使用的数值向量或矩阵，并非自然语言笔记。模型处理已有上下文后保存、供后续计算复用的这些表示，称为上下文状态。上下文长度按 token 位置计数，状态容量按实际保存的字节数计量。
+
+比较这些机制时，先看历史留下了什么，再看新一步怎样取得所需信息：每个位置可以保存得更窄，也可以只直接访问近期位置、将多个位置汇总为较少条目，或先筛选再读取。另一类机制持续更新固定大小的状态，查询从中取得结果。这些安排分别改变表示宽度、条目数量和访问方式；省下的存储与读取，要同新增的选择、压缩和更新工作一起计算。
 
 ### 2.3.1 多头注意力与 KV 共享
 
@@ -347,13 +349,13 @@
 
 这个变换减小了每个上下文位置的保存宽度，但仍需为每个位置保存一个潜变量向量。因此，存储量和点积运算量仍随上下文长度增长。若上下文很长，紧凑表示节省的读取量更加可观；若上下文很短，查询变换本身的开销占比更高。接下来先讨论如何改变上下文条目的保存和选择方式，再研究把上下文汇总到固定大小矩阵中的方法。
 
-> **练习 2-3〔延伸〕：状态大小与累计读取**
+> **练习 2-3〔延伸〕：输出变长后，KV 容量与累计读取量如何增长**
 >
 > 取 Qwen3-8B、$B=1,S=0,P=8192$，分别返回 $G=513$ 和 $G=1025$ 个 token。求后续 decode 次数、最终 KV 大小和累计旧上下文读取量。解释输出数量接近翻倍时，新增状态、总状态与累计读取为什么有不同的比例。再将 KV 位宽减半，指出这三项中哪些随之减半。
 
 ### 2.3.3 局部窗口、上下文压缩与稀疏索引
 
-减少上下文表示宽度之后，还可以改变保留哪些上下文、每次访问哪些上下文。局部窗口只关注最近的一段位置；压缩机制把若干位置合成较少的条目；稀疏选择先确定相关条目，再把它们送入主注意力。窗口决定保留的近期范围，压缩决定长期条目的数量，索引决定当前查询读取哪些条目。索引器先扫描用于选择的表示，再把选中的主状态交给注意力，索引扫描和主状态读取都需要单独计算开销。
+假设模型已经读过一段很长的文本，现在要处理一个新位置。它可以直接读取最近一段位置，也可以读取由多个旧位置汇成的条目，还可以先找出与当前查询相关的条目，再读取它们的主状态。这三种动作分别引出局部窗口、上下文压缩与稀疏选择。它们可以组合使用，但要分别计量：窗口限制近期范围，压缩减少长期条目，索引选择当前要读哪些条目。筛选本身也要读取索引表示并计算分数，不能只统计最后选中的主状态。
 
 先把这里的三个对象分开。**局部滑动窗口注意力（Sliding-Window Attention，SWA）**直接读取最近的一段位置；窗口长 128 时，较早位置退出这一局部窗口。**全局主 KV**是供主注意力读取的长期表示，可能已将多个位置合并；“全局”说明它覆盖较长历史，不表示每次都读取全部条目。**索引 K**是用于检索的另一份较小表示：索引器先用它给条目打分，再让主注意力读取选中的主 KV。索引分数用于选位置，主注意力另用自己的查询计算最终汇总权重。
 
@@ -367,7 +369,7 @@
 
 一次 CSA 查询的主注意力最多读取窗口加 512 个压缩条目，但索引打分仍扫描已保存的压缩索引条目。在 $S=8192$ 时，一个 CSA 层保留 2048 个压缩条目：主注意力状态约 2 MiB、索引约 0.5 MiB，窗口约 0.125 MiB；主注意力选择的压缩条目最多约 0.5 MiB，索引扫描却仍需处理那 2048 个索引条目。HCA 则有 64 个已生成的条目，主注意力状态约 0.0625 MiB，并访问窗口状态与这组粗粒度压缩条目。
 
-将 43 层相加，8192-token 上下文下的窗口、压缩上下文和索引约为 59.125 MiB；另有约 11.641 MiB 的 FP32（每元素 4 字节浮点格式）压缩器缓冲，合计约 70.8 MiB。最后查询的主注意力与索引数据量约 27.6 MiB。[状态分解](../calculations/results/state-deepseek-v4-flash-n8192-b1-native.md)分别列出了这些数据，因此能看出选择条目数、保存量和每步读取量并不相等。
+将 43 层相加，8192-token 上下文下的窗口、压缩上下文和索引约为 59.125 MiB；另有约 11.641 MiB 的 FP32（每元素 4 字节浮点格式）压缩器缓冲，合计约 70.8 MiB。最后查询的主注意力与索引数据量约 27.6 MiB。[状态分解](../calculations/results/state-deepseek-v4-flash-n8192-b1-native.md)分别列出了这些数据，因此能看出选中的条目数、保存的状态大小和每步读取量分别代表什么。
 
 ![上下文压缩与索引选择的先后关系。上方只示意八个位置产生两条记录的数量关系，省略 V4 CSA 的重叠投影与加权汇总，下方展示一次查询先扫描索引再读取主状态的过程。](ch02/figure-2-5-sparse.svg)
 
@@ -389,7 +391,7 @@
 
 ### 2.3.4 线性注意力与有限状态递推
 
-线性注意力通过重新组织查询、键和值的计算，避免显式计算所有位置对的注意力分数。在特征维度固定时，下面这种递推形式处理整段序列的运算量随序列长度线性增长，状态矩阵的大小保持不变。
+前面几种机制仍能区分保存下来的各个上下文条目。有限状态递推采用另一种过程：每来一个新位置，就把它的贡献合入已有状态，当前查询直接使用更新后的状态；下一步继续改写这份状态。这对应 2.3 节开头不断改写定长摘要的图景。下面这类线性注意力通过重新组织查询、键和值的计算，避免显式计算所有位置对的注意力分数。在特征维度固定时，状态矩阵大小不变，处理整段序列的运算量随序列长度线性增长。
 
 外积把一个键向量与一个值向量的每对元素相乘，组成二维的关联表。考虑一个由键值外积累加得到的矩阵 $\mathcal S_t$。每个新位置到来时，先读取旧状态，再写入本位置的贡献，当前查询直接从状态矩阵读出结果：
 
@@ -497,9 +499,9 @@
 
 两者的全局 KV 平均增量相差约 3.95 倍，对应官方图中取整后的 3,514 与 890 B/token。全局历史之外，V4.1 的局部窗口每层最多保存 128 条记录，每条 FP8+scale 记录占 528 B，40 层合计 2.578 MiB。全局历史随上下文增长，局部窗口则在填满后保持固定容量。
 
-沿生成过程逐步观察，还能看出全局缓存的增长节奏。对 V4.1，全局缓存从偶数长度增长到下一个奇数长度只增加 356 B；到下一个偶数长度时，三个 2:1 压缩器同时完成一条记录，增加 1,424 B。窗口填满后，后续写入会覆盖旧槽位，容量不再增长，但仍然产生写入：40 层 SWA 每步合计写入 $40\times528=21{,}120$ B，压缩器更新另计。
-
-共享同一份全局缓存的各层仍需读取数据、部分层还会重新索引；8K 场景下，两代 Flash 的注意力 KV（含局部 SWA）与索引逻辑读取量约为 12.56 和 11.38 MiB，跨层共享主要减少了重复保存的空间。
+沿生成过程逐步观察，还能看出全局缓存的增长节奏。对 V4.1，全局缓存从偶数长度增长到下一个奇数长度只增加 356 B；到下一个偶数长度时，三个 2:1 压缩器各完成一条记录，增加 1,424 B。窗口填满后，后续写入会覆盖旧槽位，容量不再增长，但仍然产生写入：40 层 SWA 每步合计写入 $40\times528=21{,}120$ B，压缩器更新另计。
+
+共享同一份全局缓存的各层仍需读取数据，部分层还会重新索引；8K 场景下，两代 Flash 的注意力 KV（含局部 SWA）与索引逻辑读取量约为 12.56 和 11.38 MiB，跨层共享主要减少了重复保存的空间。
 
 缓存复用与位置选择还可以分别安排。**压缩稀疏注意力第二版（Compressed Sparse Attention 2，CSA2）**把层分成三种模式。**Full** 层完整执行“生成全局 KV、索引、选取和注意力”的流程。**Reindex** 层共享 KV，用自己的索引查询重新选取 Top-K；**Reuse** 层连同所选位置一起复用。V4.1 的 38 个全局注意力层中，有 4 个 Full、4 个 Reindex 和 30 个 Reuse；另外两个层仅使用 SWA。解码器的首个 Full 层扫描全局，并选出最多 $2048\times8=16{,}384$ 个候选位置；技术报告第 2.3.2 节将这个集合称为**候选池（candidate pool）**，其中的候选是可供注意力检索的位置。后续 Reindex 层只在池内各自选取最多 512 个条目。[^v41-candidate]后续层的搜索量因此有了上限，初始全局扫描仍随上下文增长。
 
@@ -507,9 +509,9 @@
 
 候选池让后续层的搜索量有了上限，也使首轮筛选影响后续选择：某个位置一旦落在候选池之外，本次查询的后续 Reindex 层便无法再选入它。模型训练需要适应这一搜索范围，第十章将说明 V4.1 怎样把候选限制纳入后训练。
 
-![V4.1 解码器的层级位置选择。首个 Full 层先扫描全局，构造最多 16,384 个候选；后续 Reindex 在候选内重新选 512 个，Reuse 使用已有选择。示意位置数量不按比例；各层独立 Q 与局部 SWA 未画出。](ch02/figure-2-v41-selection.svg)
-
-*图 2-22　V4.1 解码器的层级位置选择。首个 Full 层先扫描全局，构造最多 16,384 个候选；后续 Reindex 在候选内重新选 512 个，Reuse 使用已有选择。示意位置数量不按比例；各层独立 Q 与局部 SWA 未画出。*
+![V4.1 解码器的层级位置选择。首个 Full 层先扫描全局，选出最多 16,384 个候选位置；后续 Reindex 在候选内重新选 512 个，Reuse 使用已有选择。示意位置数量不按比例；各层独立 Q 与局部 SWA 未画出。](ch02/figure-2-v41-selection.svg)
+
+*图 2-22　V4.1 解码器的层级位置选择。首个 Full 层先扫描全局，选出最多 16,384 个候选位置；后续 Reindex 在候选内重新选 512 个，Reuse 使用已有选择。示意位置数量不按比例；各层独立 Q 与局部 SWA 未画出。*
 
 ## 2.4 条件计算与专家权重复用
 
@@ -550,7 +552,7 @@
 
 *图 2-23　64 个 token 各选八个专家，总计 512 次分派。分散时可覆盖 256 个专家，集中时只访问八个；每专家处理的行数随之改变。*
 
-读取差额来自每份权重服务的输入行数：均匀分派时每个专家处理两个 token，集中分派时每个专家处理 64 个 token。同一份权重支持的计算增加 32 倍，理想权重访问量便降至原来的三十二分之一。与此同时，原来要执行许多输入为两行的矩阵乘法，现在只需执行少数输入为 64 行的矩阵乘法；权重复用和计算效率因此都会受到影响。[^source-18]
+读取差额来自每份权重服务的输入行数：均匀分派时每个专家处理两个 token，集中分派时每个专家处理 64 个 token。同一份权重参与的计算量增至原来的 32 倍，理想权重访问量便降至原来的三十二分之一。与此同时，原来要执行许多输入为两行的矩阵乘法，现在只需执行少数输入为 64 行的矩阵乘法；权重复用和计算效率因此都会受到影响。[^source-18]
 
 Qwen3.6 在已有 8192-token 上下文的 $B=1$ decode 中约需 7.33 GFLOPs，小于 Qwen3-8B 的约 20.0 GFLOPs。整个模型的计算量差异来自几项结构上的变化：2048 维主干缩小投影，30 个线性层使用递推状态，MoE 每次只执行选中的专家及共享分支。专家数量增加使总参数量增大，而每次调用的工作量取决于实际执行了哪些运算。
 
@@ -578,7 +580,7 @@
 
 ### 2.5.1 深度、宽度与专家粒度
 
-先忽略词表和小向量参数。若 FFN 宽度与隐藏维度保持固定比例，稠密主干的参数量大致正比于 $Ld^2$。把隐藏维度减半、层数增至四倍，可以保持这部分参数量相近，却把逐层依赖加长了四倍。每个矩阵更小，但必须经过更多层；每层还要保存相应的上下文状态。层数与宽度由此在相近参数预算下改变了计算粒度和依赖深度。
+先忽略词表和小向量参数。若 FFN 宽度与隐藏维度保持固定比例，稠密主干的参数量大致正比于 $Ld^2$。把隐藏维度减半、层数增至四倍，可以保持这部分参数量相近，但逐层依赖链也增至原来的四倍长。每个矩阵更小，但必须经过更多层；每层还要保存相应的上下文状态。层数与宽度由此在相近参数预算下改变了计算粒度和依赖深度。
 
 专家结构也可以作类似调整，同时保持参数量和运算量不变。单层路由专家的总参数为 $3Edf$，单 token 专家运算量为 $6k_{\mathrm{top}}df$。将专家数 $E$ 减半、中间维度 $f$ 加倍、选中数 $k_{\mathrm{top}}$ 减半，这两个量都不变。Qwen3-235B-A22B 的 128 个宽度 1536 的专家、每 token 选 8 个，可以与 64 个宽度 3072 的专家、每 token 选 4 个作这一对照。路由输出维度与每专家接收行数仍会改变，从而改变分组矩阵乘法的形状。
 
@@ -776,7 +778,7 @@
 B_{\max}=\left\lfloor\frac{M_{\mathrm{device}}-M_{\mathrm{weight}}-M_{\mathrm{workspace}}}{M_{\mathrm{state/request}}}\right\rfloor
 $$
 
-分子是扣除固定驻留之后可用于请求的空间，分母是每条请求的状态。每增加一条请求，都要为它分配相应的状态空间；剩余空间放不下完整的一份状态时，就无法再增加请求，因此结果要向下取整。递推与卷积状态同样属于每请求数据，与 KV 一起计入分母。
+分子是扣除固定驻留数据后可用于请求的空间，分母是每条请求的状态大小。每增加一条请求，都要为它分配相应的状态空间；剩余空间放不下完整的一份状态时，就无法再增加请求，因此结果要向下取整。递推与卷积状态同样属于每请求数据，与 KV 一起计入分母。
 
 以 24 GB 显存运行 Qwen3-8B BF16 为例，权重占 $16{,}381{,}470{,}720$ 字节，工作区预留 2 GiB，每条请求的 8K 上下文占 1.125 GiB。可容纳请求数为
 
@@ -784,7 +786,7 @@
 B_{\max}=\left\lfloor\frac{24\times10^9-16{,}381{,}470{,}720-2\times2^{30}}{1.125\times2^{30}}\right\rfloor=4.
 $$
 
-上下文变为 16K 时，每条请求占 2.25 GiB，同一空间只能放两条；变为 4K 时，则能放九条。整数跳变来自剩余空间必须足以容纳完整的一条状态，而不是只够存放其中一部分。
+上下文变为 16K 时，每条请求占 2.25 GiB，同一空间只能放两条；变为 4K 时，则能放九条。整数跳变来自剩余空间必须足以容纳一条请求的完整状态，而不是只够存放其中一部分。
 
 保留多少历史由此成为一个同时影响应用和系统的决定。应用多保留一段材料，模型就能继续参考它；服务需要为每条请求分配更多状态空间。前面的 GQA、MLA 和递推表示进一步改变了保存这些信息的方式。模型结构、上下文组织与并发容量可以放在同一份预算里比较，任务检查则确定哪些方案能够提供所需的回答质量。
 
@@ -796,9 +798,9 @@
 
 *图 2-32　权重、固定预留和一条请求 KV 的逐项容量。短竖线标出各设备容量；KV 采用 BF16、上下文长度 8192，固定预留为 2 GiB。*
 
-![70B 的 4-bit 方案在同一张 80 GB 设备上，上下文长度从 8K 增至 32K 时可同时容纳的独立请求数。每请求状态增加，使剩余空间容纳的请求数减少。](ch02/figure-2-history-capacity.svg)
-
-*图 2-33　70B 的 4-bit 方案在同一张 80 GB 设备上，上下文长度从 8K 增至 32K 时可同时容纳的独立请求数。每请求状态增加，使剩余空间容纳的请求数减少。*
+![70B 的 4-bit 方案在同一张 80 GB 加速卡上，上下文长度从 8K 增至 32K 时可同时容纳的独立请求数。每请求状态增加，使剩余空间容纳的请求数减少。](ch02/figure-2-history-capacity.svg)
+
+*图 2-33　70B 的 4-bit 方案在同一张 80 GB 加速卡上，上下文长度从 8K 增至 32K 时可同时容纳的独立请求数。每请求状态增加，使剩余空间容纳的请求数减少。*
 
 同样方法也能分析更大的部署。235B 的 BF16 权重约 470 GB，超过八张 24 GB 卡合计的 192 GB，而八张 80 GB 卡合计为 640 GB。后者为状态、工作区与复制留下约 170 GB 总余量；第六章将按具体分工把这笔总量分配到每张卡，得到逐卡预算。
 
@@ -832,14 +834,14 @@
 
 | 模型 | 完整请求矩阵运算量（TFLOPs） | 本次计算采用的实现方式 |
 | --- | ---: | --- |
-| Qwen3-8B | 1.83 | 有效因果矩阵运算量，最后位置输出头 |
+| Qwen3-8B | 1.83 | 有效因果矩阵运算量，只在最后一个位置执行输出头 |
 | DeepSeek V4-Flash | 3.40 | 主干有效注意力／专家工作，参考索引计算 |
 | DeepSeek V4-Pro | 12.72 | 独立 Pro 配置，按该配置中各类注意力层的数量和条目选择上限计算 |
 | Kimi K3 | 27.13 | 展开 MLA 与块式 KDA 算法 |
 
 这张表中的 Kimi K3 使用展开 MLA：上下文先恢复为各头的 $K$、$V$，再执行交互。前面的紧凑 MLA 则把上投影移到查询和汇总一侧。完整请求沿同一条选定路径累计 prefill 与三次 decode，上下文从 128 增到 131；这一路径的矩阵形状与状态表示共同决定各项运算量。
 
-从一次调用扩展到完整请求之后，输入和输出的作用更加清楚。增加 $P$，主要增加一次输入处理的工作量，以及后续解码开始时的上下文长度；增加 $G$，则反复执行投影、专家与输出头，并多次读取上下文。长输入、短输出与短输入、长输出由此形成不同的资源组成，第三章将分析这两类请求持续到达时的系统负载。
+从一次调用扩展到完整请求之后，输入和输出的作用更加清楚。增加 $P$，主要增加一次输入处理的工作量，以及后续解码开始时的上下文长度；增加 $G$，则反复执行投影、专家与输出头，并多次读取上下文。长输入、短输出与短输入、长输出由此对各类资源提出不同的需求，第三章将分析这两类请求持续到达时的系统负载。
 
 > **练习 2-8〔延伸〕：前缀复用与输入改写如何改变模型调用量**
 >
@@ -853,9 +855,9 @@
 
 *图 2-35　生成四个输出的调用顺序。prefill 处理 128 个输入并产生首输出，随后三次 decode 各将前一输出送回模型；最终保留 131 个位置。*
 
-![同一 128 输入、四输出请求的矩阵运算量。模型执行范围与缓存路径见本节题设；这是逐调用累计的计算量。](ch02/figure-2-request-compute.svg)
-
-*图 2-36　同一 128 输入、四输出请求的矩阵运算量。模型执行范围与缓存路径见本节题设；这是逐调用累计的计算量。*
+![同一个输入 128 个 token、输出四个 token 的请求的矩阵运算量。模型执行范围与缓存路径见本节题设；这是逐调用累计的计算量。](ch02/figure-2-request-compute.svg)
+
+*图 2-36　同一个输入 128 个 token、输出四个 token 的请求的矩阵运算量。模型执行范围与缓存路径见本节题设；这是逐调用累计的计算量。*
 
 > **练习 2-9〔延伸〕：根据容量、计算需求与任务质量选择模型**
 >
@@ -888,7 +890,7 @@
 | 已知轨迹字段代入 | `python3 calculations/calc.py trace-resource-bridge --format md` |
 | 四模型请求比较 | `python3 calculations/calc.py request-model-comparison --format md` |
 
-本章配图、计算数据与重建方法见[配图目录](ch02/README.md)。下列资料介绍各模型的结构与设计。
+本章配图、计算数据与重建方法见[配图目录](ch02/README.md)。章末资料介绍了各模型的结构与设计。
 
 <a id="model-matrix-tables"></a>
 
@@ -1166,6 +1168,6 @@
 
 设计模型时，也可以先从设备容量中扣除服务所需的状态和工作区，再比较剩余空间能够容纳的模型规模与结构。MQA、GQA 与稀疏激活说明，提高系统效率也是选择模型结构的重要依据；在容量允许的范围内，还要比较质量、时延与成本，才能确定设计方案。
 
-本章把第一章的三个资源量展开为可计算的对象。矩阵尺寸给出权重容量与线性运算，查询—上下文位置对给出注意力交互，缓存的保存时间与模型调用次数决定状态的存储量和读取量，专家分派决定每批使用多少份权重。面对新条件，先指出它改变了哪一个对象，再重新代入相应公式：增加 batch、延长上下文、扩大专家集合和增加选中专家数，会作用于不同的项。
-
-读完本章，应能从新配置列出一次前向、一次 decode 和完整生成的需求，并解释长度或 batch 改变时哪些项增长。第三章将进一步考虑请求的到达过程与工具调用的依赖关系，使这些需求成为随时间变化的负载。
+本章把第一章的三个资源量展开为可计算的对象。矩阵尺寸决定权重容量与线性运算量，查询与上下文的位置对数决定注意力交互的运算量，缓存的保存时间与模型调用次数决定状态的存储量和读取量，专家分派决定每批使用多少份权重。面对新条件，先指出它改变了哪一个对象，再重新代入相应公式：增加 batch、延长上下文、扩大专家集合和增加选中专家数，会作用于不同的项。
+
+读完本章，应能根据新的模型配置，列出一次前向、一次 decode 和完整生成各自的资源需求，并解释长度或 batch 改变时哪些项增长。第三章将进一步考虑请求的到达过程与工具调用的依赖关系，使这些需求成为随时间变化的负载。
