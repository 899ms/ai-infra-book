# Identical effective training streams after explicit answer normalization

The newly generated raw streams remain unchanged and have already received their own3141-position training checkpoints. This separate pass normalizes accepted JSON answers to the fixed serialization from the original formal baseline for each task. Require parsed content equality before assigning the canonical output IDs; tokenizer must decode those IDs to the fixed canonical text. This is supervised-data normalization, not regenerated model output or a claim that raw token positions are identical. mapping raw_sample_sha256→canonical_sample_sha256 is retained, with3141 raw versus3000 canonical positions.

Train each of the12 new logical samples on its canonical stream with the same independently initialized rank4 adapter, one AdamW step, prompt masking, fsync/reload and independent optimizer verification. Both arrangements now consume the same two exact prompt/output streams per batch, despite different raw generation lengths. No earlier checkpoint is reused. This is a separate training pass and its cost must not be silently added to the raw-stream pass; report alternative raw/canonical pipelines separately.
