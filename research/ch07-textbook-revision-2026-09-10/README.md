# 第七章教材体例重写

本轮以两服务器、八参与者、192 MiB 梯度归约为贯穿案例。正文六节、23 小节，包含六道带解例题、十项练习、设计背景专栏与常见误区。修订前正文、生成器与提纲分别保存在本目录的 `*-before` 文件中。

## 论证结构

| 节 | 本节解决的问题 | 交给下一节的结果 |
|---|---|---|
| 7.1 | 为什么跨服务器，哪些资源限制速度 | 40 GB/s 每方向出口、8 ms 归约预算；资源模型 |
| 7.2 | 相同归约如何减少跨边界工作 | 384 MiB 远端发送、约 6.6 ms；本地带宽约 80 GB/s 的选择边界 |
| 7.3 | 怎样持续供满选好的路径 | 活跃窗口、事务率、消息粒度；并发与服务模型 |
| 7.4 | 怎样安全发布数据并使资源周转 | 应用与传输状态分离、必要依赖、完成消费；关键路径模型 |
| 7.5 | 多项工作争用时怎样继续前进 | 相位、反馈时间预算、乱序状态与无环资源依赖 |
| 7.6 | 局部改进如何缩短任务 | 同一训练步的算法、窗口、重叠和退化对照，以及小消息推理对照 |

## 与原提纲的组织对应

六节与各节的小节数量保留。7.4 原顺序讨论与状态共享两小节对调，使应用状态解释先于异常等待，并使图 7-5、7-6 顺序与首次讲解一致。原 7.4.1 的 KV 快照搬回例子移到 7.3.3，与访问形式、访问范围及复用边界一起推导。7.6 将完整任务算例集中到 7.6.2，小消息与尾部合并到 7.6.3，部署及四种常见误区位于 7.6.4。旧提纲继续作为原始写作资料，正文当前标题以本稿为准。

## 推导与配图

图 7-2 改用主线连续环每方向 336 MiB、40 GB/s 出口和 20 ms 计算。图 7-3 在结果柱之前补充连续环、交错环与分片交换机制。图 7-6 在时间线之前补充必要依赖与额外串行边。九张图继续提供 SVG、PNG、PDF，图号只在外部 caption。

新增 `manuscripts/ch07/models.py` 与 `teaching-data.json` 保存选择边界及完整例题：分层交点、流水填充、复用次数、在途窗口、隔离类数、回收服务率、可容忍相位差、多路径交点、整步关键路径。既有 calculations 与实验记录保持原值，展示精度按论证用途调整。

所有设计经历改为第一人称“笔者”。正文的前提放在推导起点，版本与测量范围进入文献注释；删除反复自我限定的段末结语，用机制解释、边界推导和下一步依赖连接段落。

校验覆盖原有来源与数值、六道例题顺序、图文结构、新增边界与关键路径。阅读版另检查桌面和手机上的公式、图片、目录锚点与页面宽度。
