--- manuscripts/01-初识 AI Infra.md
+++ manuscripts/01-初识 AI Infra.md
@@ -78,7 +78,7 @@
 
 模型先处理输入，这一阶段称为预填充（prefill）；随后逐步生成新 token，这一阶段称为解码（decode）。后续步骤会复用此前保存的状态，其中的 KV 缓存（KV cache）保存上下文位置产生的键和值两类向量，供后续位置计算注意力时复用。键和值是注意力机制使用的数据表示，第 2 章会从具体运算说明它们怎样产生。生成循环由实例内部的调度器持续推进，前一步输出成为下一步输入。产生的 token 经输出处理转换为文本，沿连接流式返回；如果输出要求调用工具，应用运行工具，把结果加入上下文，再发起后续调用。
 
-模型权重是训练得到、用于把输入变换成输出的数值参数。它们在实例启动或模型切换时从存储加载，随后保留在显存中（称为驻留），供多次请求使用。生成时，GPU 计算单元从显存读取当前层的权重和上下文状态，完成运算，再将结果交给下一层。于是，同一份数据经历了两种不同频率的搬运：加载把模型送到设备，执行则反复把所需数据送到计算单元。在多卡实例中，一张卡算出的中间结果还要传给其他卡，后续计算才能继续。中间张量是运算产生并交给后续运算的多维数值数组，矩阵就是二维张量。**外部请求可能只有一小段文字，内部搬运的数据却包括大得多的权重、状态和中间张量。**
+模型权重是训练得到、用于把输入变换成输出的数值参数。它们在实例启动或模型切换时从存储加载，随后常驻显存（称为驻留），供多次请求使用。生成时，GPU 计算单元从显存读取当前层的权重和上下文状态，完成运算，再将结果交给下一层。于是，同一份数据经历了两种不同频率的搬运：加载把模型送到设备，执行则反复把所需数据送到计算单元。在多卡实例中，一张卡算出的中间结果还要传给其他卡，后续计算才能继续。中间张量是运算产生并交给后续运算的多维数值数组，矩阵就是二维张量。**外部请求可能只有一小段文字，内部搬运的数据却包括大得多的权重、状态和中间张量。**
 
 ![权重加载一次，生成时逐步读取。蓝框表示显存中持续保存的同一份权重，三个绿框表示先后发生的计算；箭头表示读取或结果依赖，步骤间距不代表耗时。](ch01/figure-1-weight-lifetime.svg)
 
@@ -388,7 +388,7 @@
 
 其中 $\lambda_{\mathrm{packet}}$ 为包到达率，$\mu_{\mathrm{core}}$ 为单核处理率。这些核反复完成相同的包处理流程，将相应工作移到网卡，可以把 CPU 时间留给应用。[^nic]
 
-Azure 的做法是让主机软件管理复杂策略，把适合重复执行的包处理规则交给 FPGA。数据经过网卡时就完成这些处理，CPU 可以把更多时间用于客户应用。设计的关键是同时保留软件更新能力与硬件处理速度。
+Azure 的做法是让主机软件管理复杂策略，把适合重复执行的包处理规则交给 FPGA。数据经过网卡时就完成这些处理，CPU 可以把更多时间用于客户应用。设计的关键是兼顾软件更新能力与硬件处理速度。
 
 ![可编程网卡在数据进入主机前完成指定的包处理。图中实线跟踪数据；网卡承担的处理减少主机 CPU 的辅助工作。](ch01/figure-1-design-smartnic.svg)
 
--- manuscripts/02-模型架构.md
+++ manuscripts/02-模型架构.md
@@ -337,7 +337,7 @@
 
 左边先为每个上下文位置恢复键，右边先变换当前查询。对单个新查询，右边只需做一次查询变换，随后直接与潜变量点积。值向量也可在加权汇总后再上投影：先汇总低维向量，再恢复输出维度。这样，原来针对每个上下文位置执行的升维操作，改为针对当前查询和汇总结果执行。
 
-本章采用 Kimi K3 的 **Gated MLA（门控多头潜变量注意力）**作实例；门控通过学习得到的系数调节注意力输出。紧凑路径保存 512 维潜变量和参与 $QK^{\mathsf T}$ 点积的 64 维额外分支；NoPE 表示该处不使用位置编码的设置，它保留该分支的计算维度，省去旋转位置编码。24 个 MLA 层、8192-token 上下文、BF16 下，保存量为 $24\times8192\times(512+64)\times2$，即 216 MiB。
+本章采用 Kimi K3 的 **Gated MLA（门控多头潜变量注意力）**作实例；门控通过学习得到的系数调节注意力输出。紧凑路径保存 512 维潜变量和参与 $QK^{\mathsf T}$ 点积的 64 维额外分支；NoPE 表示该处不使用位置编码的设置，它保持该分支的计算维度不变，省去旋转位置编码。24 个 MLA 层、8192-token 上下文、BF16 下，保存量为 $24\times8192\times(512+64)\times2$，即 216 MiB。
 
 展开缓存将潜变量先恢复为各头的 $K$、$V$，再保存这些向量；同样的 8192 个位置需要约 11.25 GiB。紧凑缓存则保存上投影之前的潜变量。两者的容量差异因此来自缓存位于线性变换的哪一侧：紧凑缓存每步变换当前查询，展开缓存直接读取各头的上下文向量。[^source-2]
 
@@ -373,9 +373,9 @@
 
 *图 2-13　上下文压缩与索引选择的先后关系。上方只示意八个位置产生两条记录的数量关系，省略 V4 CSA 的重叠投影与加权汇总，下方展示一次查询先扫描索引再读取主状态的过程。*
 
-![DeepSeek V4-Flash 在 8K 上下文下的四项状态。窗口、压缩上下文和索引是已保留的上下文；压缩缓冲保存尚在汇总或更新中的数据，单独计入容量。](ch02/figure-2-sparse-capacity.svg)
-
-*图 2-14　DeepSeek V4-Flash 在 8K 上下文下的四项状态。窗口、压缩上下文和索引是已保留的上下文；压缩缓冲保存尚在汇总或更新中的数据，单独计入容量。*
+![DeepSeek V4-Flash 在 8K 上下文下的四项状态。窗口、压缩上下文和索引保存已处理的上下文信息；压缩缓冲保存尚在汇总或更新中的数据，单独计入容量。](ch02/figure-2-sparse-capacity.svg)
+
+*图 2-14　DeepSeek V4-Flash 在 8K 上下文下的四项状态。窗口、压缩上下文和索引保存已处理的上下文信息；压缩缓冲保存尚在汇总或更新中的数据，单独计入容量。*
 
 压缩还引入一种与查询不同的更新节奏。新 token 到达时，先更新当前压缩块；累计满压缩比要求的位置数后，再生成一个长期条目。CSA 每四个位置完成一块，HCA 每 128 个位置完成一块。因此，在处理同一个输入位置时，主注意力访问多少条目、索引扫描多少条目、压缩器是否完成一块，需要分别计算。第 128 个位置可以同时触发两种压缩块的完成。[^source-14]
 
@@ -403,7 +403,7 @@
 
 *图 2-16　固定矩阵的递推更新。新键和值的外积加入旧状态，矩阵形状保持不变；查询使用更新后的状态计算输出。本图采用简单累加递推。*
 
-**Kimi Delta Attention（KDA）**是 Kimi 使用的一种门控递推注意力，用固定大小的关联矩阵保存上下文信息。它使用门控与 delta 更新；delta 在这里指依据当前预测误差写入修正量。状态更新不仅写入新信息，还根据当前键对既有状态的预测形成修正，并通过衰减控制旧信息保留。直观上，它更新的是一张固定大小的关联矩阵，而不是不断追加一张逐 token 列表。旧信息以汇总后的关联保留，当前键控制修正的位置，衰减门决定保留多少旧信息。
+**Kimi Delta Attention（KDA）**是 Kimi 使用的一种门控递推注意力，用固定大小的关联矩阵保存上下文信息。它使用门控与 delta 更新；delta 在这里指依据当前预测误差写入修正量。状态更新不仅写入新信息，还根据当前键对既有状态的预测形成修正，并通过衰减控制旧信息的保留程度。直观上，它更新的是一张固定大小的关联矩阵，而不是不断追加一张逐 token 列表。旧信息以汇总后的关联形式保存，当前键控制修正的位置，衰减门决定保留多少旧信息。
 
 Kimi K3 每个 KDA 层有 96 个头，每头递推矩阵为 $128\times128$。按 FP32 保存，单层为 6 MiB，69 层为 414 MiB。一次 decode 读取旧矩阵、融合新信息并写回，理想一读一写就是 828 MiB。这个量不随上下文长度增长：短上下文时它是一笔显著的固定开销，长上下文时则避免了扫描量随位置数持续增加。
 
@@ -699,7 +699,7 @@
 
 *图 2-30　相同 8K 上下文下的每请求状态。上下文表示采用 BF16，递推矩阵和压缩缓冲采用相应实现的精度；各项合计对应表 2-C。*
 
-四个模型的逐矩阵展开见本章末的[模型矩阵查阅表](#model-matrix-tables)。表 2-2 至表 2-5 保留每个模块的输入输出尺寸、权重形状、单次运算量与层数。下面先用这些分项的合计解释资源差异。
+四个模型的逐矩阵展开见本章末的[模型矩阵查阅表](#model-matrix-tables)。表 2-2 至表 2-5 列出每个模块的输入输出尺寸、权重形状、单次运算量与层数。下面先用这些分项的合计解释资源差异。
 
 将矩阵尺寸相乘并按层数累计，DeepSeek V4-Flash 主干约有 2843 亿个逻辑参数，统一 BF16 表示约为 568.7 GB。发布的模型检查点（保存参数的文件集合）则采用混合存储格式，主干数据量约为 156.0 GB。前者回答结构包含多少参数，后者回答这些参数在文件中占多少字节；格式转换把二者连接起来。
 
@@ -735,7 +735,7 @@
 
 **由上下文状态大小推算单步读取量。** Qwen3-8B 每步使用 1152 MiB 旧 KV；DeepSeek V4-Flash 的主注意力读取与索引扫描合计 27.625 MiB，并更新压缩器。Kimi K3 紧凑 MLA 使用 216 MiB 上下文状态，414 MiB 递推矩阵还要一读一写，读写量合计 828 MiB；Qwen3.6 对应 160 MiB 全局注意力的 KV 缓存和 120 MiB 递推矩阵读写。递推状态每步都要读出并更新，上下文表示则要供当前查询使用。因此，即使状态大小固定，每步也仍有相应的读写开销。
 
-**量化编码与元数据共同决定权重容量。** 统一按 BF16 计算时，每个参数占 2 字节，权重大小可以直接反映参数量的差异。实际发布的权重文件（checkpoint，即模型参数的保存快照）使用各自的存储格式，DeepSeek V4-Flash 主干约为 156.02 GB，Kimi K3 文本约为 1559.97 GB。文件大小由低位宽矩阵、保留较高精度的参数以及格式元数据共同决定。模型加载后，还需要为格式转换和计算分配空间。第五章将分析存储格式与运行时实现如何影响显存占用。
+**量化编码与元数据共同决定权重容量。** 统一按 BF16 计算时，每个参数占 2 字节，权重大小可以直接反映参数量的差异。实际发布的权重文件（checkpoint，即模型参数的保存快照）使用各自的存储格式，DeepSeek V4-Flash 主干约为 156.02 GB，Kimi K3 文本约为 1559.97 GB。文件大小由低位宽矩阵、采用较高精度的参数以及格式元数据共同决定。模型加载后，还需要为格式转换和计算分配空间。第五章将分析存储格式与运行时实现如何影响显存占用。
 
 表 2-B 的参数组成解释了权重容量，表 2-C 则展示这些参数怎样参与一次调用。二者结合起来，可以区分“模型能容纳多少参数”与“处理一个 token 要使用多少资源”。[^comparison-data]
 
@@ -768,7 +768,7 @@
 
 ### 2.6.2 参数规模、数值精度与容量约束
 
-分项计算给出了模型权重和每条请求状态的大小。现在把它们放进同一张显存容量表，就可以判断一台设备能同时保留多少条请求，以及量化权重能腾出多少空间。
+分项计算给出了模型权重和每条请求状态的大小。现在把它们放进同一张显存容量表，就可以判断一台设备能同时容纳多少条请求，以及量化权重能腾出多少空间。
 
 对一台设备，先从总容量中扣除权重、工作区和其他固定预留空间，剩余空间才能放请求状态。对普通等长、独立 KV 的简化情形，可写为：
 
@@ -788,17 +788,17 @@
 
 保留多少历史由此成为一个同时影响应用和系统的决定。应用多保留一段材料，模型就能继续参考它；服务需要为每条请求分配更多状态空间。前面的 GQA、MLA 和递推表示进一步改变了保存这些信息的方式。模型结构、上下文组织与并发容量可以放在同一份预算里比较，任务检查则确定哪些方案能够提供所需的回答质量。
 
-再看 DeepSeek-R1-Distill-Llama-70B，其参数量约为 705.5 亿。其 BF16 权重为 141.107 GB，每条 8192-token BF16 KV 为 2.5 GiB。在本例采用的分组量化方案中，保留嵌入、输出头和归一化等较高精度部分，并计入 scale 与打包尾部后，8-bit 方案约为 73.726 GB，4-bit 方案约为 39.500 GB。
-
-4-bit 方案的 39.500 GB 来自低位宽矩阵、高精度保留项、scale 与打包的共同贡献，比将全部参数一律按半字节计算更大。显存容量为 80 GB（十进制）、固定预留 2 GiB 时，8-bit 方案可放 1 条 8K 请求，4-bit 方案可放 14 条。上下文从 8K 增至 32K 时，每请求 KV 增至四倍，4-bit 方案可同时容纳的请求数降到 3 条。压缩权重腾出的空间可以保存更多请求的上下文，但每条上下文越长，能同时容纳的请求就越少。[^source-23]
+再看 DeepSeek-R1-Distill-Llama-70B，其参数量约为 705.5 亿。其 BF16 权重为 141.107 GB，每条 8192-token BF16 KV 为 2.5 GiB。在本例采用的分组量化方案中，让嵌入、输出头和归一化等部分继续使用较高精度，并计入 scale 与打包尾部后，8-bit 方案约为 73.726 GB，4-bit 方案约为 39.500 GB。
+
+4-bit 方案的 39.500 GB 来自低位宽矩阵、仍采用高精度的参数、scale 与打包的共同贡献，比将全部参数一律按半字节计算更大。显存容量为 80 GB（十进制）、固定预留 2 GiB 时，8-bit 方案可放 1 条 8K 请求，4-bit 方案可放 14 条。上下文从 8K 增至 32K 时，每请求 KV 增至四倍，4-bit 方案可同时容纳的请求数降到 3 条。压缩权重腾出的空间可以保存更多请求的上下文，但每条上下文越长，能同时容纳的请求就越少。[^source-23]
 
 ![权重、固定预留和一条请求 KV 的逐项容量。短竖线标出各设备容量；KV 采用 BF16、上下文长度 8192，固定预留为 2 GiB。](ch02/figure-2-9-capacity.svg)
 
 *图 2-32　权重、固定预留和一条请求 KV 的逐项容量。短竖线标出各设备容量；KV 采用 BF16、上下文长度 8192，固定预留为 2 GiB。*
 
-![70B 的 4-bit 方案在同一张 80 GB 设备上，上下文长度从 8K 增至 32K 时允许保留的独立请求数。每请求状态增加，使剩余空间容纳的请求数减少。](ch02/figure-2-history-capacity.svg)
-
-*图 2-33　70B 的 4-bit 方案在同一张 80 GB 设备上，上下文长度从 8K 增至 32K 时允许保留的独立请求数。每请求状态增加，使剩余空间容纳的请求数减少。*
+![70B 的 4-bit 方案在同一张 80 GB 设备上，上下文长度从 8K 增至 32K 时可同时容纳的独立请求数。每请求状态增加，使剩余空间容纳的请求数减少。](ch02/figure-2-history-capacity.svg)
+
+*图 2-33　70B 的 4-bit 方案在同一张 80 GB 设备上，上下文长度从 8K 增至 32K 时可同时容纳的独立请求数。每请求状态增加，使剩余空间容纳的请求数减少。*
 
 同样方法也能分析更大的部署。235B 的 BF16 权重约 470 GB，超过八张 24 GB 卡合计的 192 GB，而八张 80 GB 卡合计为 640 GB。后者为状态、工作区与复制留下约 170 GB 总余量；第六章将按具体分工把这笔总量分配到每张设备，得到逐卡预算。
 
@@ -818,9 +818,9 @@
 
 改变模型结构时，权重预算与状态预算会一起变化。增加层数或 KV 头数会增大每请求的 $K$，留给权重的空间随之减少；跨设备部署则扩大总容量，同时增加通信。Llama 3 报告中的 405B BF16 推理使用两台、共 16 张 H100，便跨越了单机容量边界。[^codesign]模型规模、状态结构与设备数量因此需要共同选择。
 
-![图 2-34 从设备反推权重预算。24 GB 中先保留 4 条 8K 请求的 KV 和 2 GiB 工作区，余量给出 BF16 参数上界；图中容量均为十进制 GB。](ch02/figure-2-reverse-budget.svg)
-
-*图 2-34　从设备反推权重预算。24 GB 中先保留 4 条 8K 请求的 KV 和 2 GiB 工作区，余量给出 BF16 参数上界；图中容量均为十进制 GB。*
+![图 2-34 从设备反推权重预算。24 GB 中先为 4 条 8K 请求的 KV 和 2 GiB 工作区预留空间，余量给出 BF16 参数上界；图中容量均为十进制 GB。](ch02/figure-2-reverse-budget.svg)
+
+*图 2-34　从设备反推权重预算。24 GB 中先为 4 条 8K 请求的 KV 和 2 GiB 工作区预留空间，余量给出 BF16 参数上界；图中容量均为十进制 GB。*
 
 ### 2.6.3 不同请求条件下的模型比较
 
@@ -894,7 +894,7 @@
 
 ## 模型矩阵查阅表
 
-这组表用于从整模型合计追溯到具体矩阵。查阅时先确定模型与执行分支，再用单次矩阵 FLOPs 乘层数；路由专家还要按实际接收的输入行数累计。表 2-1 的 Qwen3-8B 基线保留在 2.1.4。
+这组表用于从整模型合计追溯到具体矩阵。查阅时先确定模型与执行分支，再用单次矩阵 FLOPs 乘层数；路由专家还要按实际接收的输入行数累计。表 2-1 的 Qwen3-8B 基线见 2.1.4。
 
 **表 2-2　Qwen3.6-35B-A3B：混合注意力与 MoE 各模块的资源消耗计算**
 
--- manuscripts/03-推理与训练负载.md
+++ manuscripts/03-推理与训练负载.md
@@ -62,11 +62,11 @@
 A_M=\int_{t_{\mathrm{start}}}^{t_{\mathrm{end}}}M(t)\,\mathrm dt.
 $$
 
-将各时间段占用的空间乘以持续时间再相加，就得到占用空间随时间变化的曲线下面积。它的单位是 byte·s：保留 1 GiB 状态等待 10 秒，对应 10 GiB·s。状态没有变大，但这段时间内无法将空间用于其他请求。若任务以平均每秒 $\lambda$ 个的速度持续到达，每个任务都占用 $M_0$ 字节、平均等待 $\tau$ 秒，稳定情况下仅这些等待任务的平均占用就约为 $\lambda M_0\tau$。工具等待由此也会成为容量问题。
-
-![保留 1 GiB 状态等待十秒，对应面积为 10 GiB·s。横轴为状态存活时间，纵轴为占用空间；面积描述这段等待期间的累计存储占用。](ch03/figure-3-state-time-area.svg)
-
-*图 3-3　保留 1 GiB 状态等待十秒，对应面积为 10 GiB·s。横轴为状态存活时间，纵轴为占用空间；面积描述这段等待期间的累计存储占用。*
+将各时间段占用的空间乘以持续时间再相加，就得到占用空间随时间变化的曲线下面积。它的单位是 byte·s：1 GiB 状态持续占用空间 10 秒，对应 10 GiB·s。状态没有变大，但这段时间内无法将空间用于其他请求。若任务以平均每秒 $\lambda$ 个的速度持续到达，每个任务都占用 $M_0$ 字节、平均等待 $\tau$ 秒，稳定情况下仅这些等待任务的平均占用就约为 $\lambda M_0\tau$。工具等待由此也会成为容量问题。
+
+![1 GiB 状态持续占用空间十秒，对应面积为 10 GiB·s。横轴为状态存活时间，纵轴为占用空间；面积描述这段等待期间的累计存储占用。](ch03/figure-3-state-time-area.svg)
+
+*图 3-3　1 GiB 状态持续占用空间十秒，对应面积为 10 GiB·s。横轴为状态存活时间，纵轴为占用空间；面积描述这段等待期间的累计存储占用。*
 
 成功任务成本把所有尝试的成本相加，再除以成功任务数。这样，失败生成、工具测试和环境运行都进入分子，分母对应用户得到的合格结果。一个批次零成功时，记录其总消耗与失败原因；取得成功结果后，再用全部尝试的总成本除以成功任务数。[^cost]
 
@@ -138,7 +138,7 @@
 
 > **练习 3-2〔核心〕：突发、积压与容量**
 >
-> 采用例题 3-1 后一分钟的负载强度，即每秒新增 7471.2 步 decode 工作。将这一突发负载的持续时间改为 30 秒，分别考虑处理率为 6000、8000 步/秒的情况。初始队列为空，求积压与停止到达后的排空时间。随后考虑另一种负载：每秒有两个任务进入工具等待阶段，每个任务等待 10 秒，期间保留 1 GiB 状态。求稳定状态下的平均等待任务数与状态占用；将等待延长到 30 秒后重新计算。
+> 采用例题 3-1 后一分钟的负载强度，即每秒新增 7471.2 步 decode 工作。将这一突发负载的持续时间改为 30 秒，分别考虑处理率为 6000、8000 步/秒的情况。初始队列为空，求积压与停止到达后的排空时间。随后考虑另一种负载：每秒有两个任务进入工具等待阶段，每个任务等待 10 秒，期间状态占用 1 GiB 空间。求稳定状态下的平均等待任务数与状态占用；将等待延长到 30 秒后重新计算。
 
 ## 3.2 多轮交互与 Agent 任务
 
@@ -243,7 +243,7 @@
 
 > **练习 3-4〔核心〕：关键路径与状态寿命**
 >
-> 一次任务先由模型计算 2 秒，然后调用两个工具，两个工具分别耗时 6 秒和 10 秒。两个工具都完成后，模型再计算 3 秒，任务结束。分别计算两个工具依次执行和同时执行时的任务总耗时。假设仅在等待工具返回期间保留 1 GiB 状态，求两种执行方式下状态占用量与保留时间的乘积，单位为 GiB·s。
+> 一次任务先由模型计算 2 秒，然后调用两个工具，两个工具分别耗时 6 秒和 10 秒。两个工具都完成后，模型再计算 3 秒，任务结束。分别计算两个工具依次执行和同时执行时的任务总耗时。假设仅在等待工具返回期间状态占用 1 GiB 空间，求两种执行方式下状态占用量与占用时长的乘积，单位为 GiB·s。
 >
 > 本节的代码任务实测记录显示，任务总耗时为 76.510 秒，其中首轮模型计算耗时 36.375 秒，所有工具执行合计耗时 0.078 秒。保持其余阶段不变，分别计算两种优化能节省多少任务时间：将首轮模型计算速度提高到原来的四倍；将所有工具的执行速度提高到原来的两倍。根据计算结果，解释为什么局部加速倍数不能单独决定一项优化对完整任务的收益。
 
@@ -640,9 +640,9 @@
 
 *图 3-32　采用 H100 80GB 的 Llama 3.1 训练用量。纵轴分模型规模，横轴为百万 GPU 小时；设备类别与前图分开呈现。*
 
-![DeepSeek-V3 采用 H800 的三个训练阶段。分别累计预训练、上下文扩展与后训练用量，保留各阶段的公开统计范围。](ch03/figure-3-v3-stage-hours.svg)
-
-*图 3-33　DeepSeek-V3 采用 H800 的三个训练阶段。分别累计预训练、上下文扩展与后训练用量，保留各阶段的公开统计范围。*
+![DeepSeek-V3 采用 H800 的三个训练阶段。分别累计预训练、上下文扩展与后训练用量，沿用各阶段的公开统计范围。](ch03/figure-3-v3-stage-hours.svg)
+
+*图 3-33　DeepSeek-V3 采用 H800 的三个训练阶段。分别累计预训练、上下文扩展与后训练用量，沿用各阶段的公开统计范围。*
 
 Qwen3 表 21 将特定 8B 模型的 RL 与 on-policy distillation 两条后训练路线并列，投入分别为 17,920 与 1,800 GPU 小时。两者是替代路线，各自形成生成、反馈与学习工作。把 GPU 小时对应到具体阶段和设备，再结合各阶段的执行过程，就能解释训练投入主要花在哪里。
 
--- manuscripts/04-加速器架构.md
+++ manuscripts/04-加速器架构.md
@@ -94,7 +94,7 @@
 S=\frac{1}{(1-f)+f/s}=\frac{1}{0.4+0.6/2}\approx1.43.
 $$
 
-继续把矩阵速度提高到原来的四倍，总时间降至 $60/4+40=55$ μs。第一次翻倍省下 30 μs，第二次只省下 15 μs；即使矩阵计算时间趋近于零，其余 40 μs 仍然保留。整体加速上限为 2.5 倍。剩余工作在新总时间中所占比例越来越大，资源投入的重点也随之改变。
+继续把矩阵速度提高到原来的四倍，总时间降至 $60/4+40=55$ μs。第一次翻倍省下 30 μs，第二次只省下 15 μs；即使矩阵计算时间趋近于零，其余工作仍需 40 μs。整体加速上限为 2.5 倍。剩余工作在新总时间中所占比例越来越大，资源投入的重点也随之改变。
 
 早期 TPU 的面积分配展示了计算与存储之间的实际取舍。TPU 是 Google 设计的张量处理器。第一代 TPU v1 具有 $256\times256$ 个 8-bit 乘加单元、24 MiB 的统一数据缓冲（Unified Buffer） 和 4 MiB 累加存储。论文给出的芯片面积分布中，数据缓冲约占 37%，计算单元约占 30%，控制电路约占 2%。缓冲占用的面积甚至超过计算单元，因为阵列需要反复使用输入、权重和部分和。如果每次都从片外读取，乘加器就会花大量时间等待数据。[^tpu]
 
@@ -286,7 +286,7 @@
 
 这个公式揭示了容量的阶梯效应。增加少量内存时，$B_{\max}$ 可能保持不变；只有剩余空间足以容纳一条新请求的全部 KV，才能多接收一条请求。压缩权重会增大公式分子中的剩余空间，因此也能容纳更多请求。对权重较大的模型，减少固定占用可明显提高并发；对长上下文任务，KV 已占较大比例，继续压缩权重的收益会逐渐减小。
 
-训练还使用梯度、优化器状态和保留激活。按照第三章介绍的张量保存时间计算这些状态的占用：在某一时刻同时存活的张量共同决定峰值，已经释放的中间结果腾出空间给后续工作。容量规划因而既与每份张量大小有关，也与执行顺序有关。
+训练还使用梯度、优化器状态和为反向传播保存的激活。按照第三章介绍的张量保存时间计算这些状态的占用：在某一时刻同时存活的张量共同决定峰值，已经释放的中间结果腾出空间给后续工作。容量规划因而既与每份张量大小有关，也与执行顺序有关。
 
 高带宽内存（HBM）通过堆叠存储裸片和宽接口提供带宽；图形双倍数据速率内存（GDDR）通常以多颗存储芯片配合高速接口提供显存。两者都是内存器件与接口技术，统一内存是处理器共享内存的组织方式。Apple 的 CPU 与 GPU 访问共享物理内存，两者可以先后使用同一个缓冲区，减少独立副本；相应地，CPU、GPU 与其他应用也共同消耗这份容量与带宽。[^apple] 第 4.5 节会沿主机到设备的路径分析这种组织对传输时间的影响。
 
@@ -310,7 +310,7 @@
 
 ### 4.3.3 容量、带宽与延迟分别限制什么
 
-容量是某一时刻能够保留多少数据，带宽是单位时间传输多少数据，延迟是一次请求从发出到完成经过多久。容量越大，能同时保存的数据越多；带宽越高，每秒能传输的数据越多；同时发出足够多的独立请求，则可以在等待一次访问返回时继续处理其他访问。
+容量是某一时刻能够容纳多少数据，带宽是单位时间传输多少数据，延迟是一次请求从发出到完成经过多久。容量越大，能同时保存的数据越多；带宽越高，每秒能传输的数据越多；同时发出足够多的独立请求，则可以在等待一次访问返回时继续处理其他访问。
 
 先看模型驻留和逐步执行对内存提出的不同要求。H100 SXM 的名义容量为 80 GB、标称带宽为 3.35 TB/s；H200 SXM 分别为 141 GB 和 4.8 TB/s。容量增长约 76%，带宽增长约 43%。Qwen3-235B-A22B 的一组 4-bit 方案中，权重与量化缩放系数（scale）约为 123.1 GB，八条请求各保留 8,192 个位置的 KV 约为 12.6 GB，加 2 GiB 工作区，共约 137.9 GB。H100 缺近 58 GB，H200 则留约 3.1 GB 余量。容量增加首先使这些数据能够共同驻留。[^storage]
 
@@ -638,9 +638,9 @@
 
 扩大 batch 后，新增请求的 KV 读取会继续增加。8K 时 $BK=W$ 的交点约为 $B=12.53$；32K 时交点降至约 3.13。更长上下文使 KV 更早主导。于是模型设计仍有压缩 KV 的动机，服务系统仍要管理动态状态，而原来为了放下权重而增加设备的理由可能减弱。后续章节将继续检查并行、批处理和版本更新是否还值得采用。
 
-![图 4-23 独立只读权重改变两条存储路径。上方权重与 KV 争用 HBM；下方 ROM 提供权重，HBM 保留可写状态。箭头表示读取，KV 还需写入新状态。](ch04/figure-4-rom-paths.svg)
-
-*图 4-23　独立只读权重改变两条存储路径。上方权重与 KV 争用 HBM；下方 ROM 提供权重，HBM 保留可写状态。箭头表示读取，KV 还需写入新状态。*
+![图 4-23 独立只读权重改变两条存储路径。上方权重与 KV 争用 HBM；下方 ROM 提供权重，HBM 存放可写状态。箭头表示读取，KV 还需写入新状态。](ch04/figure-4-rom-paths.svg)
+
+*图 4-23　独立只读权重改变两条存储路径。上方权重与 KV 争用 HBM；下方 ROM 提供权重，HBM 存放可写状态。箭头表示读取，KV 还需写入新状态。*
 
 ## 4.7 根据架构分析性能、选择设备
 
@@ -701,7 +701,7 @@
 
 代入未舍入速率，$I^*\approx164$ FLOPs/byte，$M^*\approx178.1$。因此从整数行数 179 起，矩阵计算时间超过数据传输时间。转折点把“较大 batch 更适合矩阵单元”变成了可以计算的行数阈值。[^pipeline-extra]
 
-提高一种资源会移动这个边界。$M=1$ 时，带宽翻倍使时间下界从 33.3 降至约 16.7 μs，矩阵能力翻倍则仍由读取决定。$M=256$ 时，矩阵能力翻倍把计算从 52 降至 26 μs，但读取仍需 37.4 μs，所以新下界为 37.4 μs；带宽单独翻倍，52 μs 的计算仍然保留。当两项耗时相等后，只加快其中一项就无法继续降低总时间下界。
+提高一种资源会移动这个边界。$M=1$ 时，带宽翻倍使时间下界从 33.3 降至约 16.7 μs，矩阵能力翻倍则仍由读取决定。$M=256$ 时，矩阵能力翻倍把计算从 52 降至 26 μs，但读取仍需 37.4 μs，所以新下界为 37.4 μs；带宽单独翻倍，计算仍需 52 μs。当两项耗时相等后，只加快其中一项就无法继续降低总时间下界。
 
 上下文长度以不同方式改变投影和注意力。全注意力 prefill 的序列长度翻倍，投影行数与计算量翻倍，而分数矩阵的行、列都翻倍，QK/PV 工作增至四倍。单步 decode 的当前投影行数由请求数决定，上下文变长则增加 KV 读取。随着上下文增长，同一层中，注意力计算和 KV 读取所占的时间比例也会增加。
 
--- manuscripts/05-算子与运行时.md
+++ manuscripts/05-算子与运行时.md
@@ -140,7 +140,7 @@
 
 ### 5.2.2 容量约束下的分块复用
 
-先保留一个 $m\times n$ 输出块，每次沿 K 维读入一对输入块：A 块为 $m\times k$，W 块为 $k\times n$。它们相乘，更新同一份输出累加器；换入下一对输入块时，已经得到的部分和继续保留。直到沿 K 完成全部累加，才把结果转为 BF16 并写出。
+先为一个 $m\times n$ 输出块分配累加缓冲区，每次沿 K 维读入一对输入块：A 块为 $m\times k$，W 块为 $k\times n$。它们相乘，更新同一份输出累加器；换入下一对输入块时，已经得到的部分和继续保留。直到沿 K 完成全部累加，才把结果转为 BF16 并写出。
 
 ![图 5-3 输入块更换而输出累加器保留](ch05/figure-5-tile-working-set.svg)
 
@@ -244,7 +244,7 @@
 
 ![图 5-9 融合边界与中间读写](ch05/figure-5-5-boundaries.svg)
 
-*图 5-9：每少保存一个 24 MiB 中间量，就少一次写出和一次读入，共 48 MiB。G、U 为 BF16，最终输出占 1 byte/元素，量化尺度预先给定。各方案保留相同的运算与舍入顺序。*
+*图 5-9：每少保存一个 24 MiB 中间量，就少一次写出和一次读入，共 48 MiB。G、U 为 BF16，最终输出占 1 byte/元素，量化尺度预先给定。各方案采用相同的运算与舍入顺序。*
 
 中间张量的容量与访问量在这里具有不同含义。设每步先分配输出，完成后释放已用完的输入。独立执行 SiLU 时，G、U、T 同时存活，峰值为 72 MiB；三步融合时，G、U 与最终输出同时存活，峰值为 60 MiB。读写减少 96 MiB，峰值却只减少 12 MiB，因为同一块存储空间可以被反复读写。[^fusion]
 
@@ -306,7 +306,7 @@
 
 *图 5-13：两个块的分数分别为 0、ln 2，值分别为 1、3。最大值增大后，将旧指数和与旧加权值同时乘以 1/2，再加上新块的贡献，最后才做除法。箭头传递的是统计量，旧分数无需保留。*
 
-小例子中，第二块把最大值从 0 提高到 ln 2。旧项原来的指数为 1，换到新基准后变成 1/2；分母中的旧贡献和分子中的旧贡献必须同时缩小，才能保留它们的相对权重。
+小例子中，第二块把最大值从 0 提高到 ln 2。旧项原来的指数为 1，换到新基准后变成 1/2；分母中的旧贡献和分子中的旧贡献必须同时缩小，才能保持它们的相对权重不变。
 
 推广到一行分数，减去共同最大值可避免指数溢出。令 $\ell=\sum_j\exp(s_j-m)$、$u=\sum_j\exp(s_j-m)V_j$，最终输出为 $u/\ell$。新块到来后，把旧状态改到新的最大值基准，再加上新块贡献：
 
@@ -332,7 +332,7 @@
 
 将 b 缩为 1，能放下 a=166，扫描次数降为 50，访问降至 $50\times4+4=204$ MiB。但每次扫描从 128 个 K/V 块变成 8192 个块，在线状态必须更频繁地更新。
 
-| K/V 块行数 b | 可保留的 Q 行数 a | K/V 完整扫描次数 | 缓冲与下一层之间的访问 | Q 块与 K/V 块之间的状态更新次数 |
+| K/V 块行数 b | 可容纳的 Q 行数 a | K/V 完整扫描次数 | 缓冲与下一层之间的访问 | Q 块与 K/V 块之间的状态更新次数 |
 | --- | ---: | ---: | ---: | ---: |
 | 1 | 166 | 50 | 204 MiB | 409600 |
 | 64 | 110 | 75 | 304 MiB | 9600 |
@@ -405,7 +405,7 @@
         store_tile(Y, io, jo, silu(c))
 ```
 
-这里 `load_tile` 和 `matmul_accumulate` 分别表示成块读取和矩阵累加；尾块通过有效坐标掩码处理。伪代码的缩进清楚地标出了这些先后关系：输入块在 ko 循环内更新，累加器在 ko 循环外保留，激活在 ko 循环结束后发生。
+这里 `load_tile` 和 `matmul_accumulate` 分别表示成块读取和矩阵累加；尾块通过有效坐标掩码处理。伪代码的缩进清楚地标出了这些先后关系：输入块在 ko 循环内更新，累加器在整个 ko 循环期间持续保存，激活在 ko 循环结束后发生。
 
 ![图 5-15 循环层级决定临时数据的保存时间](ch05/figure-5-9-polyhedral.svg)
 
@@ -419,13 +419,13 @@
 
 ### 5.4.3 依赖与舍入怎样限制变换
 
-刚才把激活移入输出 tile，却保留在 ko 循环之后，原因可以用两个数说明。设一个输出的两个部分和为 1 与 −1，完整和为 0，SiLU(0)=0。若在每个部分和之后先做 SiLU，再相加，结果为 $\operatorname{SiLU}(1)+\operatorname{SiLU}(-1)\approx0.4621$。移动激活越过归约边界，已经改变了运算。
+刚才把激活移入输出 tile，但仍安排在 ko 循环结束后执行，原因可以用两个数说明。设一个输出的两个部分和为 1 与 −1，完整和为 0，SiLU(0)=0。若在每个部分和之后先做 SiLU，再相加，结果为 $\operatorname{SiLU}(1)+\operatorname{SiLU}(-1)\approx0.4621$。移动激活越过归约边界，已经改变了运算。
 
 ![图 5-16 激活与归约的顺序](ch05/figure-5-activation-order.svg)
 
 *图 5-16：同样两个部分和，先相加得到零，再做 SiLU 仍为零；先对各部分做 SiLU 再相加，得到约 0.4621。两个数说明变换越过归约后改变了函数。*
 
-编译器需要保留的依赖因此有两个层次：同一输出的部分和要按归约规则合并，后续算子要等所需的结果算好。不同输出可以并行计算；同一输出的归约和后续运算则必须按依赖顺序执行。
+编译器需要遵守的依赖关系因此有两个层次：同一输出的部分和要按归约规则合并，后续算子要等所需的结果算好。不同输出可以并行计算；同一输出的归约和后续运算则必须按依赖顺序执行。
 
 FlashAttention 能逐块完成计算，是因为它保留了 m、$\ell$、u，并在最大值改变时，同时调整之前累加的指数和与加权值。若只保留每块已经归一化的输出，就丢失了各块分母的相对大小。比如两个块各自只有一个值 1 和 3，块内输出仍是 1 和 3；仅凭这两个输出，无法判断整行 Softmax 应给它们分配 1:2 还是 2:1 的权重。因此，分块归约必须保存足够的统计量，才能正确合并各块的结果。
 
@@ -535,7 +535,7 @@
 
 *图 5-23：先融合再重放，主机发起三次图执行，设备执行十五个 kernel。融合减少设备内核数，图重放减少主机提交次数。*
 
-图重放前，还要把新输入放到图所记录的地址。典型实现为图保留固定缓冲，上游每次将新数据写到这里，图按已记录的地址读取。若上游输出位于另一份张量中，就在图执行前增加一次复制；若上游直接写固定缓冲，这次复制便可省去。
+图重放前，还要把新输入放到图所记录的地址。典型实现为图预留固定缓冲区，上游每次将新数据写到这里，图按已记录的地址读取。若上游输出位于另一份张量中，就在图执行前增加一次复制；若上游直接写固定缓冲，这次复制便可省去。
 
 ![图 5-24 新输入与固定图地址](ch05/figure-5-graph-address.svg)
 
@@ -607,7 +607,7 @@
 
 *图 5-28：矩阵与向量资源独立、缓冲充足。每个任务另计 0.7 μs 调度与通知，首块投影完成后即可激活，八块流水约三百五十八微秒结束。*
 
-沿着图 5-27 的激活行看，首块开始后就连续执行到最后一块。这与双缓冲具有同样的规律：第一块先完成前一阶段，之后每隔较慢阶段所需的时间完成一块。任务调度增加了开销，但按块开始后续计算省下了更多等待时间。每个缓冲槽从投影开始一直保留到激活结束；没有空槽时，投影任务就要等待。因此，任务队列、完成事件和空闲缓冲共同决定下一块何时能够开始。
+沿着图 5-27 的激活行看，首块开始后就连续执行到最后一块。这与双缓冲具有同样的规律：第一块先完成前一阶段，之后每隔较慢阶段所需的时间完成一块。任务调度增加了开销，但按块开始后续计算省下了更多等待时间。每个缓冲槽从投影开始一直被占用到激活结束；没有空槽时，投影任务就要等待。因此，任务队列、完成事件和空闲缓冲共同决定下一块何时能够开始。
 
 图重放减少重复提交，特化后缓存的程序减少重复编译，持久化内核让相邻算子按块重叠执行。三者分别减少了提交、准备和等待时间。下一节将进一步计算这些改进能让完整请求的耗时缩短多少。
 
@@ -672,7 +672,7 @@
 
 替换后的内核主要缩短了一行输入的 decode 激活时间，加速比约为 2.6，合计节省约 1.5 ms。prefill 的激活耗时基本相同。轨迹中的这些激活与其他设备工作顺序执行。原内核的全部激活计算约为 13.6 ms，占约 851 ms 采集区间的 1.6%；保持其他工作不变，即使省去全部激活计算，也最多节省 13.6 ms。目前只缩短了 decode 部分，因此整个请求预计只能减少约 1 ms。
 
-在客户端交错测量替换前后的请求，共得到 11 对结果，与上述预期相符。替换前后的首 token 时间均约 436 ms，完整请求中位数分别约为 817 ms、815 ms。对每轮计算“替换前用时减去替换后用时”，配对时间差的中位数约为 1.3 ms，占原请求约 0.16%。图 5-31 保留每轮差值，显示 9 对缩短、2 对变慢的分布。
+在客户端交错测量替换前后的请求，共得到 11 对结果，与上述预期相符。替换前后的首 token 时间均约 436 ms，完整请求中位数分别约为 817 ms、815 ms。对每轮计算“替换前用时减去替换后用时”，配对时间差的中位数约为 1.3 ms，占原请求约 0.16%。图 5-31 列出每轮差值，显示 9 对缩短、2 对变慢的分布。
 
 ![图 5-31 完整请求的逐轮配对时间差](ch05/figure-5-17-request.svg)
 
--- manuscripts/06-超节点.md
+++ manuscripts/06-超节点.md
@@ -176,7 +176,7 @@
 
 随着 $p$ 增大，右侧不断缩小。即使通信每次只增加同样一小段等待，也会逐渐抵消本地执行时间的缩短。
 
-**保留输出分片如何减少重复存储。** ReduceScatter 可以把一个 64 MiB 输出分到四卡，每卡只留 16 MiB；若下一算子保持分片，总输出占用便从四份完整张量的 256 MiB 降到 64 MiB。相邻算子需要完整输入时，AllGather 再把四片收集起来。只要后续算子继续使用分片，就能维持较低的内存占用。JAX 是支持数组计算、自动求导和编译执行的数值计算框架。配套 JAX 实验给出这两条 FFN 路径的具体执行。[^tp-experiment]
+**保持输出的分片形式如何减少重复存储。** ReduceScatter 可以把一个 64 MiB 输出分到四卡，每卡只留 16 MiB；若下一算子保持分片，总输出占用便从四份完整张量的 256 MiB 降到 64 MiB。相邻算子需要完整输入时，AllGather 再把四片收集起来。只要后续算子继续使用分片，就能维持较低的内存占用。JAX 是支持数组计算、自动求导和编译执行的数值计算框架。配套 JAX 实验给出这两条 FFN 路径的具体执行。[^tp-experiment]
 
 上面的切分只沿一个矩阵维度分配设备。设备更多时，还可以同时切分两个维度，将卡排成行、列两个方向的通信组。一次 $[8192,4096]\times[4096,12288]$ 投影，将输出分片保存在 16 张卡上，每卡原始输入和权重分别为 4 MiB、6 MiB。排成 4×4 网格，横向收集其他三片输入，发送 12 MiB；纵向收集其他三片权重，发送 18 MiB。改成 2×8 后两方向为 28、6 MiB，改成 8×2 后为 4、42 MiB。独立等速链路下，4×4 所需传输时间较短，因为两个方向中较大的传输量最小。MeshSlice 沿这一布局继续分块，使收集与矩阵乘流水执行；Arctic 则在不同执行阶段切换分工，其状态迁移在第 9 章展开。[^variants]
 
@@ -296,7 +296,7 @@
 
 ![八个专家分散到四组](ch06/figure-6-expert-load-2.svg)
 
-*图 6-13：把同样的八个专家分散到四组，保留 288 MiB 的读取量，同时让四组各承担 128 次计算。三图纵轴范围相同。*
+*图 6-13：把同样的八个专家分散到四组，将读取量维持在 288 MiB，同时让四组各承担 128 次计算。三图纵轴范围相同。*
 
 图中后两种布局读取同样多的权重，完成时间却可能不同，因为参与计算的设备数不同。因此，批内复用应与专家在设备上的分布一起分析。
 
@@ -614,7 +614,7 @@
 
 ### 6.6.1 局部容量不足与全局空闲
 
-UB 将资源访问扩展到主机之间，也引出了一个具体用途：让内存不足的节点借用其他节点的空闲内存。前面介绍的 TP、PP 和 EP 将计算与权重分配到不同设备，以降低每张卡的内存需求。另一种方法是保留计算位置，只把一部分数据放到其他节点。这正是内存池的基本用途：将分散的空闲容量交给需要它的任务。
+UB 将资源访问扩展到主机之间，也引出了一个具体用途：让内存不足的节点借用其他节点的空闲内存。前面介绍的 TP、PP 和 EP 将计算与权重分配到不同设备，以降低每张卡的内存需求。另一种方法是保持计算位置不变，只把一部分数据放到其他节点。这正是内存池的基本用途：将分散的空闲容量交给需要它的任务。
 
 考虑四个各有 64 GiB 内存的节点，任务需求分别为 80、48、32、32 GiB。总需求 192 GiB，小于总容量 256 GiB，但任务 0 的需求比所在节点的容量多出 16 GiB。其余节点的空闲分别为 16、32、32 GiB，合计 80 GiB。
 
@@ -683,7 +683,7 @@
 
 ### 6.7.1 更大的协作组与更多推理实例
 
-至此，已经知道数据能否存下、每步如何执行，以及连接和内存位置怎样改变执行时间。下面利用这些结果，为章首给定的八张卡安排多个会话。四个会话都已完成 prefill，各自保存了 8192 个上下文位置的 KV。每个会话还有一个已经生成、尚未写入 KV 的 token，用作下一步输入。现在，四个会话同时请求再生成八个 token。一个实例一次服务一个会话，完成这八步后再接下一个会话；请求按实例轮转分配。采用 6.4 的低延迟八卡连接，全部设备保留到四个会话都完成。
+至此，已经知道数据能否存下、每步如何执行，以及连接和内存位置怎样改变执行时间。下面利用这些结果，为章首给定的八张卡安排多个会话。四个会话都已完成 prefill，各自保存了 8192 个上下文位置的 KV。每个会话还有一个已经生成、尚未写入 KV 的 token，用作下一步输入。现在，四个会话同时请求再生成八个 token。一个实例一次服务一个会话，完成这八步后再接下一个会话；请求按实例轮转分配。采用 6.4 的低延迟八卡连接，全部设备持续占用到四个会话都完成。
 
 每步时间来自式（6-5）。续写时，式（6-4）的上下文长度依次为 8192 到 8199，KV 访问量逐步增加。将八步相加，就得到会话的服务时间。等待中的会话只保存各自的 KV，同一实例中的会话共享模型权重；四种部署方式的每卡内存需求均不超过 24 GB。
 
@@ -724,7 +724,7 @@
 
 ### 6.7.2 功率、成本与故障范围怎样改变选择
 
-时间线给出了每个会话何时完成，也给出了八张卡要保留多久。因此，可以直接用它计算成本，并比较按时完成同一组请求需要花多少钱。设每张卡每毫秒收取 0.001 个成本单位。八张卡从四个会话同时到达时开始计费，直到所有会话完成。完成期限为 90 ms，要求四个会话中至少三个按时完成。
+时间线给出了每个会话何时完成，也给出了八张卡需要占用多久。因此，可以直接用它计算成本，并比较按时完成同一组请求需要花多少钱。设每张卡每毫秒收取 0.001 个成本单位。八张卡从四个会话同时到达时开始计费，直到所有会话完成。完成期限为 90 ms，要求四个会话中至少三个按时完成。
 
 四个两卡实例的总成本约为 $8\times69.4\times0.001\approx0.555$，四个会话全部按时完成，平均每个按时完成的会话花费约 0.139。两个四卡实例的总成本约为 0.661，平均每个按时完成的会话花费约 0.165。一个八卡实例只在期限内完成两个会话，未达到最低完成比例。八个单卡实例则全部超时。
 
@@ -780,7 +780,7 @@
 
 当权重、状态或服务需求继续扩大，单个超节点可能无法容纳一个实例。下一章沿已经得到的消息和依赖，加入网卡、数据中心交换网络、远程操作的完成通知与网络拥塞，继续计算扩大协作范围所带来的收益和代价。
 
-[^dense]: 单卡完整 BF16 权重为 16,381,470,720 bytes，8193 位置 KV 为 1,208,107,008 bytes；TP=8 每卡分别为 2,048,223,232 与 151,013,376 bytes。表中 GB 为十进制，GiB／MiB 为二进制，合计先用精确值求和再取近似；2 GiB 包含激活与其余工作区预留。详见[Qwen3-8B 逐卡放置](../calculations/results/placement-qwen8-tp8-pp1-dp1.md)及其 JSON，读取固定官方配置与权重索引；状态保留到 history+tokens。完整矩阵、KV 头复制及工作区范围见该结果，不能把未建模时间当作测量。
+[^dense]: 单卡完整 BF16 权重为 16,381,470,720 bytes，8193 位置 KV 为 1,208,107,008 bytes；TP=8 每卡分别为 2,048,223,232 与 151,013,376 bytes。表中 GB 为十进制，GiB／MiB 为二进制，合计先用精确值求和再取近似；2 GiB 包含激活与其余工作区预留。详见[Qwen3-8B 逐卡放置](../calculations/results/placement-qwen8-tp8-pp1-dp1.md)及其 JSON，读取固定官方配置与权重索引；状态保存范围为 history+tokens 个位置。完整矩阵、KV 头复制及工作区范围见该结果，不能把未建模时间当作测量。
 
 [^models]: [具体模型的并行推算](../case-studies/model-parallelism.md)，[Qwen3-235B-A22B 配置](../references/outline-checks/2026-09-07/scaling-history/qwen3-235b-config.json)，[DeepSeek V4 报告](../references/files/papers/deepseek-v4.pdf)，[Kimi K3 报告](../references/files/papers/kimi-k3.pdf)表 1 与 §2.3。模型配置与版本分别保留，DeepSeek-V3 的 671B 作为历史对照。
 
--- manuscripts/07-数据中心网络.md
+++ manuscripts/07-数据中心网络.md
@@ -139,7 +139,7 @@
 
 连续环的传输已超过 8 ms，分层归约则为归约计算、传播和排队留出约 1.4 ms。分层归约使跨服务器传输量减少约 43%，通信时间减少约 26%；两者比例不同，是因为六轮本地通信也占用时间。
 
-**讨论：本地互联要多快，分层才合算？** 暂保留跨服务器出口为 40 GB/s，把本地带宽写成 $B_L$。分层算法需要 $288\ \mathrm{MiB}/B_L$ 的本地传输时间，使跨服务器传输时间从 $336\ \mathrm{MiB}/40\ \mathrm{GB/s}$ 减到 $192\ \mathrm{MiB}/40\ \mathrm{GB/s}$，并少六轮启动。令两种时间相等，得到本地带宽约为 80 GB/s。本地带宽为 200 GB/s 时，分层归约更快；若本地带宽降到 50 GB/s，分层时间约为 11.1 ms，连续环仍约为 8.8 ms，此时连续环反而更快。
+**讨论：本地互联要多快，分层才合算？** 暂将跨服务器出口带宽固定为 40 GB/s，把本地带宽写成 $B_L$。分层算法需要 $288\ \mathrm{MiB}/B_L$ 的本地传输时间，使跨服务器传输时间从 $336\ \mathrm{MiB}/40\ \mathrm{GB/s}$ 减到 $192\ \mathrm{MiB}/40\ \mathrm{GB/s}$，并少六轮启动。令两种时间相等，得到本地带宽约为 80 GB/s。本地带宽为 200 GB/s 时，分层归约更快；若本地带宽降到 50 GB/s，分层时间约为 11.1 ms，连续环仍约为 8.8 ms，此时连续环反而更快。
 
 分层归约用额外的本地通信，减少了跨服务器传输。分层归约先通过较快的本地互联完成部分归约，再将结果通过较慢的网络发送。本地归约增加的时间少于它节省的远端传输时间时，分层归约才更快。
 
@@ -368,7 +368,7 @@
 
 ![源缓冲和目的缓冲的不同复用时刻](ch07/figure-7-10-lifetime.svg)
 
-*图 7-22：源缓冲占用到 5 μs 发送完成；目的缓冲持续保留到 12 μs 消费者用完。绿色为 8–12 μs 的读取区间。虚线标出 6 μs 提前覆盖目的地址的错误操作。*
+*图 7-22：源缓冲占用到 5 μs 发送完成；目的缓冲持续占用到 12 μs 消费者用完。绿色为 8–12 μs 的读取区间。虚线标出 6 μs 提前覆盖目的地址的错误操作。*
 
 图 7-22 中两条占用区间结束于不同事件。把“必须等哪一事件发生”画成箭头，就得到描述正确交接的**依赖图**。依赖图用有向边表达这些先后关系。节点表示工作，边 $u\to v$ 表示 $v$ 必须在 $u$ 完成后开始。对每个节点，取所有前驱的最晚完成时刻，再加自身执行时间，就得到其完成时刻。沿图从起点到终点耗时最长的链称为**关键路径**。
 
@@ -567,7 +567,7 @@
 
 图 7-34 中的水平线说明，快路径发完并不等于整条消息可以交付。两条路径节省了发送时间，却可能增加等待缺口的时间。可以直接求出双路径传输更快的条件。单路径需要发送八个报文，双路径每条发送四个。每报文串行发送时间为 1.024 μs，双路径节省约 4.1 μs。第二条路径比第一条多出的传播时延若超过这 4.1 μs，平均分配便失去时间优势。路径选择或不均匀分配应减少慢路径承担的报文，使两条路径的最后一个报文尽量同时到达。
 
-再让序号 0 丢失，在其原发送结束 20 μs 后重传，整条消息约在 23.0 μs 才能交付。此时其他七个报文均已到达，接收端保留 7 KiB，只需重传缺失的 1 KiB；若从缺口开始重发整个后缀，则再搬 8 KiB。**选择性重传**用保存接收进度的状态，换取更少的重复传输。
+再让序号 0 丢失，在其原发送结束 20 μs 后重传，整条消息约在 23.0 μs 才能交付。此时其他七个报文均已到达，接收端缓存了 7 KiB 载荷，只需重传缺失的 1 KiB；若从缺口开始重发整个后缀，则再搬 8 KiB。**选择性重传**用保存接收进度的状态，换取更少的重复传输。
 
 重传还包含一个独立问题：何时认定缺口需要恢复。等待太久，真正丢包后的停顿增长；仅因慢路径暂未到达就重发，又会增加重复流量。路径时延分布决定检测需要容忍多大乱序，接收状态决定缺口后多少工作可以保留。可靠交付与第 7.4 节的应用依赖共同决定哪些已经到达的操作可以继续执行。
 
@@ -585,7 +585,7 @@
 
 *图 7-37：两项请求分别持有 A、B，各自等待对方持有的资源。箭头表示等待关系，两个请求都无法完成并释放资源。*
 
-![为完成响应保留通路](ch07/figure-7-response-reserve.svg)
+![为完成响应预留通路](ch07/figure-7-response-reserve.svg)
 
 *图 7-38：请求、执行、响应使用独立资源并按顺序申请。响应有预留缓冲和发送机会，能够返回并释放原请求。*
 
@@ -745,11 +745,11 @@
 
 [^pcie]: Hou 等，*Understanding Routable PCIe Performance for Composable Infrastructures*，NSDI 2024；[已有正文阅读与诊断笔记](../case-studies/collective-paths-and-diagnosis.md)、[survey 阅读范围](../research/2026-infra-survey/reading-nsdi-2024.md)。论文 Gen3 平台的分配规律不作为所有 PCIe、CXL 或 UB 的保证。
 
-[^remote]: [不变 KV 快照的远程读取与取回](../calculations/results/remote-state-reused.md)。快照为 Qwen3-8B 的 1024-token、BF16 全层 KV，共 144 MiB；一次搬回固定成本约 6.201 ms、每次远程读取约 3.780 ms、本地读约 0.152 ms。10% 访问反例按相同比例缩放每次访问成本，并保留整份搬回。
+[^remote]: [不变 KV 快照的远程读取与取回](../calculations/results/remote-state-reused.md)。快照为 Qwen3-8B 的 1024-token、BF16 全层 KV，共 144 MiB；一次搬回固定成本约 6.201 ms、每次远程读取约 3.780 ms、本地读约 0.152 ms。10% 访问反例按相同比例缩放每次访问成本，仍按整份快照计算搬回成本。
 
 [^ordering]: [必要依赖与旧值反例](../calculations/results/operation-ordering-book.md)、[共享资源变体](../calculations/results/operation-ordering-shared.md)、[远端排序研究与固定 NVSHMEM 实现阅读](../case-studies/remote-ordering-and-completion.md)。目标端排序属于需要新增硬件支持的设计；当前网卡的参照实验用于观察请求与完成时序。发布链的 20＋80＋2 μs 与旧值反例的 1–6 μs 是两组独立输入。“全部依次执行”作为串行对照策略。
 
-[^states]: [活跃关系与传输状态](../calculations/results/connection-states-full.md)、[八类隔离](../calculations/results/connection-states-isolated.md)。模型保留端点、关系绑定和传输状态三类容量。
+[^states]: [活跃关系与传输状态](../calculations/results/connection-states-full.md)、[八类隔离](../calculations/results/connection-states-isolated.md)。模型分别计入端点、关系绑定和传输状态三类容量。
 
 [^reclaim]: [完成通知处理与槽位释放](../calculations/results/completion-reclaim-book.md)、[增加槽位](../calculations/results/completion-reclaim-more-slots.md)。轮询周期、每次轮询处理的完成项数量与槽位寿命为教学条件。
 
--- manuscripts/08-单实例推理.md
+++ manuscripts/08-单实例推理.md
@@ -317,7 +317,7 @@
 
 上下文编排还可以主动改变复用机会。假设已有 8K token 的上下文，下一轮追加 1K 工具结果，最多可复用原来的 8K 前缀，新增部分需处理；如果改写最前面的指令，后续 token 即使文字相同，产生 KV 时依赖的历史已经变化，不能继续把整段当作相同前缀。另一种方案是把历史总结为 2K，再追加 1K，后续状态会更短，却要支付总结与重新处理的成本，并检验信息损失。
 
-应用可以保留稳定指令和材料的位置，把变化较快的信息放在后面，使后续请求继续复用同一前缀。需要总结时，则按未来还要生成多久，比较一次重算与后续每步节约。
+应用可以固定稳定指令和材料的位置，把变化较快的信息放在后面，使后续请求继续复用同一前缀。需要总结时，则按未来还要生成多久，比较一次重算与后续每步节约。
 
 用一组教学条件求出总结的收益边界。两种方案均通过相同任务检查，后续生成相同数量的 token。总结及重建缓存合计额外耗时 300 ms；在给定设备、批次和后续长度范围内，缩短上下文使每步 decode 净减少 1 ms。若后面还有 $N$ 步生成，总结方案相对保留历史节省的时间为
 
@@ -427,7 +427,7 @@
 
 压缩减少了数据本身的大小。另一种扩展容量的方法是让数据轮流驻留在 GPU 上：只在计算需要时搬入，用完后复用这块空间。权重卸载将部分原本常驻 GPU 的权重移到主存，并在计算相应层之前搬回 GPU。省下的设备内存可以用于 KV，但每次前向计算都需要重新搬入这些权重。请求逐步生成输出时，搬运也随每轮 decode 重复。
 
-Qwen3-8B 一层 FFN 的三个 BF16 矩阵共占 288 MiB。36 层中每四层卸载一层，共卸载九份 FFN 权重，释放 2592 MiB。GPU 上还要为预取保留缓冲区：一组缓冲占 288 MiB，净省 2304 MiB；两组占 576 MiB，净省 2016 MiB。以一条 8K 输入的 1152 MiB KV 为单位，前者能多容纳两条，后者只能多容纳一条。若预留生成完毕的 1188 MiB，两种方案均只能增加一条完整请求。[^offload]
+Qwen3-8B 一层 FFN 的三个 BF16 矩阵共占 288 MiB。36 层中每四层卸载一层，共卸载九份 FFN 权重，释放 2592 MiB。GPU 上还要为预取预留缓冲区：一组缓冲占 288 MiB，净省 2304 MiB；两组占 576 MiB，净省 2016 MiB。以一条 8K 输入的 1152 MiB KV 为单位，前者能多容纳两条，后者只能多容纳一条。若预留生成完毕的 1188 MiB，两种方案均只能增加一条完整请求。[^offload]
 
 ![预取缓冲如何改变新增 KV 容量](ch08/figure-8-9-offload.svg)
 
--- manuscripts/09-分布式推理.md
+++ manuscripts/09-分布式推理.md
@@ -75,7 +75,7 @@
 
 *图 9-5：计算暂停期间，状态仍需保留。示意同一请求从 prefill、decode 到工具等待和下一轮的状态寿命；横轴按阶段排列，不表示相等时长，EC 仅在有视觉输入时出现。*
 
-以本章的 Qwen3-8B 为例，一份包含 8192 个位置的 BF16 上下文 KV 占 1.125 GiB。工具执行 10 秒时，GPU 可以处理其他请求，但这份上下文 KV 仍需占用 1.125 GiB 内存，容量与保留时间的乘积为 11.25 GiB·s；十个这样的会话同时等待，就占 11.25 GiB。计算暂停后，状态仍需保留，这正是共享缓存和换出策略要解决的问题。
+以本章的 Qwen3-8B 为例，一份包含 8192 个位置的 BF16 上下文 KV 占 1.125 GiB。工具执行 10 秒时，GPU 可以处理其他请求，但这份上下文 KV 仍需占用 1.125 GiB 内存，容量与占用时长的乘积为 11.25 GiB·s；十个这样的会话同时等待，就占 11.25 GiB。计算暂停后，状态仍需保留，这正是共享缓存和换出策略要解决的问题。
 
 这条请求若随后返回 129 个 token，P 先产生第一个输出，128 次 decode 再处理前 128 个生成位置。此时连续 KV 覆盖 $8192+128=8320$ 个位置，最后一个返回 token 尚未送回模型。下一轮在这 8320 个位置之后继续，先处理这个尾 token 和新增输入。因此，已返回的输出序列比 KV 所覆盖的序列多一个 token；恢复生成时，需要从这个位置继续计算。
 
@@ -539,7 +539,7 @@
 
 完整副本每秒只能完成 3.2 个请求，持续少于到达的四个，因此首先被排除。两种 PD 方案的通道能力都高于八请求/s，均由计算池限制。在每秒四个请求的到达负载下，直传消耗约 4.8 GB/s，中转消耗约 9.7 GB/s；在八请求/s 的排空阶段，两者分别需要约 9.7 和 19.3 GB/s。
 
-两种方案的稳态吞吐率相同，但直接 PD 更适合这组请求。这批请求的前缀不会被后续请求复用，中转使通道传输量增加一倍，每请求还多等待约 48.3 ms。64 GiB 共享池至多容纳 56 份完整的 1.125 GiB 前缀，保留下来的每一份在本次工作负载中都不会再次使用。直接交接完成相同工作，保留更多链路余量，并缩短交接等待。因此本例选择四 A 做 P、四 B 做 D、P→D 直传。
+两种方案的稳态吞吐率相同，但直接 PD 更适合这组请求。这批请求的前缀不会被后续请求复用，中转使通道传输量增加一倍，每请求还多等待约 48.3 ms。64 GiB 共享池至多容纳 56 份完整的 1.125 GiB 前缀，保留下来的每一份在本次工作负载中都不会再次使用。直接交接完成相同工作，留出更多链路余量，并缩短交接等待。因此本例选择四 A 做 P、四 B 做 D、P→D 直传。
 
 接着检查这一方案能否按期处理完启动期间积压的请求。10 秒后积压 40 个请求；直接 PD 就绪后每秒完成八个，同时每秒又来四个，净减少四个，10 秒后排空。从开始启动算起共 20 秒，满足 25 秒目标。完整副本在就绪后仍每秒增加 0.8 个请求，到第 25 秒时积压已从 40 增至 52 个。
 
@@ -574,9 +574,9 @@
 
 这也给出了把本章推导用于新系统的顺序。先把每请求工作换算成各类资源需求；再按整数资源分配找出最忙的池或设备；按执行顺序计入状态传输时间与内存占用；最后用服务率减去到达率得到的剩余处理能力求出突发或启动积压的持续时间。若限制项是 D，增加 P 不会缩短队列；若限制项是共享通道，增加计算副本只会让更多请求等候取回。
 
-PD 的八 worker 案例和 MoE 的专家案例在这里相接。前者将不同阶段交给更擅长处理该阶段的设备，后者通过批内复用减少权重读取，再通过专家副本分散最忙设备上的工作。共享 KV 则把已完成的计算保留到下一次请求，启动与恢复决定这些设备何时能够提供服务。一次部署选择的依据，是这些部署方式共同改变了多少计算量和等待时间，以及消除积压所需的时间。
-
-单个阶段变快也会改变实例配比。设链路充足，每个 P 实例每秒处理 20 条请求，每个 D 实例每秒完成 5 条相同请求，1P 配 4D 时两池能力相等。若 D 阶段加速四倍，保留原配比会使 D 池能力增至 80 请求/s，而 P 池仍为 20；改为 1P 配 1D，两池都提供 20 请求/s，并释放三个 D 实例。加速一个阶段的收益由此体现为完成相同工作所需的资源减少。
+PD 的八 worker 案例和 MoE 的专家案例在这里相接。前者将不同阶段交给更擅长处理该阶段的设备，后者通过批内复用减少权重读取，再通过专家副本分散最忙设备上的工作。共享 KV 则保存已完成的计算结果，供下一次请求复用，启动与恢复决定这些设备何时能够提供服务。一次部署选择的依据，是这些部署方式共同改变了多少计算量和等待时间，以及消除积压所需的时间。
+
+单个阶段变快也会改变实例配比。设链路充足，每个 P 实例每秒处理 20 条请求，每个 D 实例每秒完成 5 条相同请求，1P 配 4D 时两池能力相等。若 D 阶段加速四倍，沿用原配比会使 D 池能力增至 80 请求/s，而 P 池仍为 20；改为 1P 配 1D，两池都提供 20 请求/s，并释放三个 D 实例。加速一个阶段的收益由此体现为完成相同工作所需的资源减少。
 
 ## 练习
 
--- manuscripts/10-训练系统.md
+++ manuscripts/10-训练系统.md
@@ -1,8 +1,8 @@
 # 第 10 章 训练系统
 
-一个模型能够在单张加速卡上生成文本，是否意味着它也能在这张卡上训练？全参数训练会更新模型的全部可训练权重；混合精度训练用较低精度执行主要矩阵运算，同时用较高精度保留更新所需的数值。这里 BF16 是占 2 字节的 16 位浮点格式，FP32 是占 4 字节的 32 位浮点格式。损失是衡量预测与训练目标差异的数值；优化器是根据损失梯度调整参数的算法。Adam 是一种利用梯度历史调整参数更新的优化器。Qwen3-8B 的 BF16 权重约为 16.4 GB，而采用混合精度 Adam 进行全参数训练时，系统除模型权重外，还需保存梯度（损失对参数的导数）、主权重（用于高精度更新的参数副本），以及优化器跨步保留的梯度历史，合计约为 131 GB。后者约为前者的八倍。前向传播由输入得到预测，反向传播沿运算依赖求导；求导时还要读取前向保存的激活，即中间计算结果，执行期间还会出现参数聚合和转换缓冲。设计训练系统，首先要确定这些张量和缓冲区存放在哪里，以及需要保留多久。
-
-把训练状态分到更多设备上，可以减轻单卡的显存压力，但计算往往需要等待其他设备传来的数据。把状态移到主机内存，可以腾出显存，却又增加了主机与 GPU 之间的传输。训练持续数周时，等待输入、保存检查点和故障后的重复计算也会影响完成时间。理解这些取舍，需要把三个问题连起来：**哪些张量和缓冲区需要同时保留，哪些工作决定一步何时结束，已经完成的训练中，有多少能够在故障后恢复。**
+一个模型能够在单张加速卡上生成文本，是否意味着它也能在这张卡上训练？全参数训练会更新模型的全部可训练权重；混合精度训练用较低精度执行主要矩阵运算，同时用较高精度保存更新所需的数值。这里 BF16 是占 2 字节的 16 位浮点格式，FP32 是占 4 字节的 32 位浮点格式。损失是衡量预测与训练目标差异的数值；优化器是根据损失梯度调整参数的算法。Adam 是一种利用梯度历史调整参数更新的优化器。Qwen3-8B 的 BF16 权重约为 16.4 GB，而采用混合精度 Adam 进行全参数训练时，系统除模型权重外，还需保存梯度（损失对参数的导数）、主权重（用于高精度更新的参数副本），以及优化器跨步保留的梯度历史，合计约为 131 GB。后者约为前者的八倍。前向传播由输入得到预测，反向传播沿运算依赖求导；求导时还要读取前向保存的激活，即中间计算结果，执行期间还会出现参数聚合和转换缓冲。设计训练系统，首先要确定这些张量和缓冲区存放在哪里，以及需要保留多久。
+
+把训练状态分到更多设备上，可以减轻单卡的显存压力，但计算往往需要等待其他设备传来的数据。把状态移到主机内存，可以腾出显存，却又增加了主机与 GPU 之间的传输。训练持续数周时，等待输入、保存检查点和故障后的重复计算也会影响完成时间。理解这些取舍，需要把三个问题连起来：**哪些张量和缓冲区需要同时占用显存，哪些工作决定一步何时结束，已经完成的训练中，有多少能够在故障后恢复。**
 
 训练计算图明确了各类数据的产生和使用时机，为这些设计提供了依据。前向产生的激活在反向时使用，梯度在参数更新前汇集，优化器状态跨步保留。利用这些已知关系，运行时可以提前取回即将使用的参数、重新计算开销较小的激活，并让已就绪梯度的通信与后续反向计算重叠。模型求导、内存管理与设备调度由此共同决定一步训练的执行方式。
 
@@ -53,7 +53,7 @@
 
 *图 10-2：五类训练状态如何形成相当于八份 BF16 权重的容量。模型为 Qwen3-8B，采用表中数据格式；横轴表示各类状态累计占用的容量。*
 
-不同数据需要保留的时间并不相同。前向产生的激活要等到相应的反向计算结束后才能释放，而参数收集缓冲区可能只在某个模块执行期间使用。设备 $i$ 的显存峰值取决于同一时刻保留的训练状态、激活和临时缓冲区：
+不同数据需要保留的时间并不相同。前向产生的激活要等到相应的反向计算结束后才能释放，而参数收集缓冲区可能只在某个模块执行期间使用。设备 $i$ 的显存峰值取决于同一时刻驻留的训练状态、激活和临时缓冲区：
 
 $$
 M_{\mathrm{peak},i}=\max_t\left[M_{\mathrm{persistent},i}(t)+M_{\mathrm{activation},i}(t)+M_{\mathrm{temporary},i}(t)\right].
@@ -158,7 +158,7 @@
 
 *图 10-11：矩阵反向产生两路梯度：dX 交给前一层继续反向，dW 交给本层的归约和参数更新。两路计算都读取上游梯度 dY。*
 
-参数梯度需要前向输入 $X$。保存 $X$ 会使它从前向一直保留到反向传播时；释放 $X$ 则要求反向使用前重新生成它。**激活重计算**只保留部分中间结果。反向计算需要其他结果时，再从已保留的结果出发，重新执行相应的前向计算。这样增加了计算量，却减少了需要长期保存的数据。
+参数梯度需要前向输入 $X$。保存 $X$ 需要让它从前向一直驻留到反向传播时；释放 $X$ 则要求反向使用前重新生成它。**激活重计算**只保留部分中间结果。反向计算需要其他结果时，再从已保留的结果出发，重新执行相应的前向计算。这样增加了计算量，却减少了需要长期保存的数据。
 
 **例：反向传播前重算门控乘积能节省多少激活存储？** Qwen3-8B 的一个多层感知机（MLP，即本例的前馈子网络）先得到门控结果 $a$ 和上投影结果 $u$，再形成 $h=a\odot u$，最后计算 $Y=hW_{\mathrm{down}}$。下投影的参数梯度需要 $h$。如果已为非线性算子的反向计算保留了 $a,u$，可以在下投影反向前重新相乘，就不必一直保存 $h$。
 
@@ -339,7 +339,7 @@
 
 *图 10-24：相同 8192 个 token 改分为 7168 与 1024，总因果配对数增至 26218496。长三角形的面积增长超过短三角形的面积减少。*
 
-图 10-24 中，长三角形的额外面积来自真实的注意力关系，无法靠删除填充位置消除。打包减少填充位置（padding），按长度分组则让各设备承担的三角形面积更接近；上下文并行进一步把长序列的计算分到多个设备，并通过交换 K/V 取得其他设备保存的数据。它们分别改变无效工作、工作分配和数据到达路径。用相同总 token 数比较方案时，保留长度分布、可见性掩码（mask，规定各位置能读取哪些上下文位置）和损失权重，就能将调度收益与任务变化分开。
+图 10-24 中，长三角形的额外面积来自真实的注意力关系，无法靠删除填充位置消除。打包减少填充位置（padding），按长度分组则让各设备承担的三角形面积更接近；上下文并行进一步把长序列的计算分到多个设备，并通过交换 K/V 取得其他设备保存的数据。它们分别改变无效工作、工作分配和数据到达路径。用相同总 token 数比较方案时，保持长度分布、可见性掩码（mask，规定各位置能读取哪些上下文位置）和损失权重不变，就能将调度收益与任务变化分开。
 
 ## 10.4 数据输入、检查点与故障恢复
 
@@ -478,7 +478,7 @@
 
 图 10-31 先把三道工序画在各自独立的资源上。若改用同一组设备轮流执行，就能节省设备，但生成和学习之间需要切换整套显存数据。
 
-**共享设备**让同一组设备轮流训练和生成，节省重复资源；**独立部署**让两侧独立执行，获得重叠机会，但要在两组设备间发送权重。共享设备的关键成本发生在切换：旧状态尚未释放，新状态已经开始装载，两个阶段各自运行时显存都足够，切换时却可能因两套数据同时保留而超出容量。
+**共享设备**让同一组设备轮流训练和生成，节省重复资源；**独立部署**让两侧独立执行，获得重叠机会，但要在两组设备间发送权重。共享设备的关键成本发生在切换：旧状态尚未释放，新状态已经开始装载，两个阶段各自运行时显存都足够，切换时却可能因两套数据同时占用显存而超出容量。
 
 这里 KV 缓存保存注意力计算中的上下文键（K）和值（V），生成新 token 时读取它们；KV 池是预留给这些缓存的空间。
 
@@ -688,7 +688,7 @@
 
 > **习题 10-1 · 基础：训练状态分片与每卡峰值内存**
 >
-> 将本章梯度改为 FP32，计算 Qwen3-8B 长期保存的训练状态。再求八卡 ZeRO-1、ZeRO-2、ZeRO-3 的每卡容量。设执行中有 6 GiB 激活和两份各 3 GiB 的模块缓冲，分别计算两份缓冲同时保留与顺序复用时的峰值。
+> 将本章梯度改为 FP32，计算 Qwen3-8B 长期保存的训练状态。再求八卡 ZeRO-1、ZeRO-2、ZeRO-3 的每卡容量。设执行中有 6 GiB 激活和两份各 3 GiB 的模块缓冲，分别计算两份缓冲同时占用显存与顺序复用时的峰值。
 
 > **习题 10-2 · 基础：从期限反推效率**
 >
--- manuscripts/11-资源调度与运行环境.md
+++ manuscripts/11-资源调度与运行环境.md
@@ -60,7 +60,7 @@
 \text{资源需求率}=\text{任务到达率}\times\text{每任务的平均资源需求}.
 $$
 
-每项任务累计使用 3 秒 CPU 时间，所以每秒到达的任务合计需要 30 秒 CPU 时间，平均有 30 个 CPU 核在执行工具；每项需要 60 GiB·秒，所以平台平均保留 600 GiB 环境内存；每项有三次 9 秒模型调用，所以平均有 $10\times3\times9=270$ 次模型调用正在处理。
+每项任务累计使用 3 秒 CPU 时间，所以每秒到达的任务合计需要 30 秒 CPU 时间，平均有 30 个 CPU 核在执行工具；每项需要 60 GiB·秒，所以平台的环境平均占用 600 GiB 内存；每项有三次 9 秒模型调用，所以平均有 $10\times3\times9=270$ 次模型调用正在处理。
 
 **例 11-1：代码修复平台为何先受环境内存容量限制？** 将需求除以章首给定的容量，CPU 占用为 $30/40=75\%$，模型调用的并发容量使用率为 $270/300=90\%$，内存占用为 $600/640\approx94\%$。内存只剩 40 GiB，可以再容纳 20 个环境；CPU 还剩 10 核。此时增加 CPU 核数，仍然无法解决环境内存不足的问题。
 
@@ -167,7 +167,7 @@
 
 从图 11-11 改为图 11-12 的安排后，省去的是等待期间的一段内存占用。要实现这段空闲，必须先保存下一轮仍需使用的状态；因此，暂停收益与保存什么、怎样恢复密切相关。
 
-![同一段模型等待中的内存保留与暂停恢复](ch11/figure-11-pause.svg)
+![同一段模型等待中的环境驻留与暂停恢复](ch11/figure-11-pause.svg)
 
 *图 11-11：第 10—19 s 等待模型时始终保留 2 GiB 环境，占用 18 GiB·秒。横轴与下一图一致。*
 
@@ -288,9 +288,9 @@
 
 将这个关系放回平台。假设平台有 40 个空闲核，此时同时收到 80 次各需 1 秒的工具调用，第一批 40 项立即执行，第二批等待 1 秒；平均等待 0.5 秒，全部在 2 秒内完成。若增配到 80 核，这次突发在 1 秒内完成。增加 CPU 核数提高了处理突发请求的能力；任务均匀到达时，平均仍只需 30 个 CPU 核执行工具。
 
-同一批工具调用还需要足够的环境内存。每个环境占 2 GiB，80 个已准备环境需要 160 GiB。始终驻留的方案已有约 600 GiB，占满了 640 GiB 中的大部分空间；第 11.2 节的每轮重建环境方案平均只占 180 GiB，为突发保留了更大的空间。减少等待期间保留的环境，可以腾出内存供新调用使用，使空闲 CPU 能及时开始执行。
-
-为突发请求预留容量后，还要决定请求之间的先后顺序。优先级决定谁先使用这份余量。在线任务要求快速开始，离线任务可以等待，平台便可以让离线工作暂借资源，并在在线调用到达时收回。设在线任务最多等 2 秒，而离线作业保存状态需要 30 秒。这组资源即使可以抢占，也要在 30 秒后才交还；因此平台保留另一组能在 2 秒内分配给任务的资源供在线使用。
+同一批工具调用还需要足够的环境内存。每个环境占 2 GiB，80 个已准备环境需要 160 GiB。始终驻留的方案已有约 600 GiB，占满了 640 GiB 中的大部分空间；第 11.2 节的每轮重建环境方案平均只占 180 GiB，为突发请求预留了更大的空间。减少等待期间保留的环境，可以腾出内存供新调用使用，使空闲 CPU 能及时开始执行。
+
+为突发请求预留容量后，还要决定请求之间的先后顺序。优先级决定谁先使用这份余量。在线任务要求快速开始，离线任务可以等待，平台便可以让离线工作暂借资源，并在在线调用到达时收回。设在线任务最多等 2 秒，而离线作业保存状态需要 30 秒。这组资源即使可以抢占，也要在 30 秒后才交还；因此平台预留另一组能在 2 秒内分配给任务的资源供在线使用。
 
 一次抢占还会丢失未保存的进度。一个已经执行 20 秒、恢复需 2 秒的作业，从最近的检查点恢复执行与从头重做之间相差多少，取决于检查点之后积累的工作。调度器既要考虑新任务能提前多久开始，也要计入保存和恢复状态的开销，以及原作业因此增加的延迟。
 
@@ -664,7 +664,7 @@
 
 ## 本章小结
 
-章首平台平均使用 30 个 CPU 核，却保留 600 GiB 环境内存。原因是三轮任务中有 27 秒在等待模型，而工作状态始终存在。在模型调用期间准备环境，把每项任务的累计内存占用从 60 降到 18 GiB·秒，准备工作多用 0.3 秒 CPU 时间，就能释放大量内存。
+章首平台平均使用 30 个 CPU 核，环境却占用 600 GiB 内存。原因是三轮任务中有 27 秒在等待模型，而工作状态始终存在。在模型调用期间准备环境，把每项任务的累计内存占用从 60 降到 18 GiB·秒，准备工作多用 0.3 秒 CPU 时间，就能释放大量内存。
 
 这项改进利用了完整任务的执行关系。环境管理掌握模型等待区间后，可以把创建提前，把进程寿命缩短到工具真正使用它的阶段；持久化文件保存各轮之间需要保留的状态。模型服务和环境平台共同安排准备时机，使原来贯穿任务全程的资源占用变成按阶段分配。
 
--- manuscripts/12-端边云协同.md
+++ manuscripts/12-端边云协同.md
@@ -53,7 +53,7 @@
 
 接下来考虑各步骤能否同时进行。假设原图分成三块后可以逐块独立处理，每块上传需 4 秒、处理需 0.1 秒；三块输出分别为 1、2、2 MB，回传依次需 0.08、0.16、0.16 秒。第一块上传后，其处理和回传可以与第二块上传重叠；第二块同理。最后一块仍要等到第 12 秒才发完，之后再经过处理、回传和传播，完整成片约在 12.4 秒返回用户。与原来的 12.8 秒相比，前两块的处理与回传在后续上传期间就已完成，不再额外增加等待；总计 12 秒的上传时间仍然需要。[^stream]
 
-图 12-3 和图 12-4 用相同的时间轴比较两种安排：看前两块时，处理与回传都发生在后续上传期间；看最后一块时，上传结束后的等待仍然保留。
+图 12-3 和图 12-4 用相同的时间轴比较两种安排：看前两块时，处理与回传都发生在后续上传期间；看最后一块时，上传结束后仍需等待。
 
 ![分块计算怎样与后续上传重叠](ch12/figure-12-2-overlap.svg)
 
@@ -133,7 +133,7 @@
 
 截图版本也直接影响轮数。假设模型读到版本 $v$，随后界面变化，按旧截图作出的操作就可能出错，需要恢复界面并重新截图。把截图版本与动作编号绑定，让执行端在界面变化时重新截图，可以避免这类无效工作。[^author]
 
-第 12.6 节将继续分析这个截图任务：前十轮已完成，剩余二十轮需要选择执行位置。届时保留截图大小、终端工作和原路径参数，引入更快的模型服务，比较切换服务后能缩短多少时间。
+第 12.6 节将继续分析这个截图任务：前十轮已完成，剩余二十轮需要选择执行位置。届时保持截图大小、终端工作和原路径参数不变，引入更快的模型服务，比较切换服务后能缩短多少时间。
 
 ### 12.1.4 本地、附近设备与云端的初步比较
 
@@ -198,7 +198,7 @@
 
 以上比较假定每张截图都重新编码。对多轮任务，还要检查图片是否重复出现，因为缓存可以进一步减少编码次数。相同图片再次出现时，缓存命中省掉一次编码；界面内容改变后则重新编码。在一组使用相同尺寸图片的 CPU 实验中，再次输入同一张图片时可以命中缓存，修改图片内容后则需要重新编码。[^ec-test] 计算多轮任务的编码总时间时，可以用单次编码时间乘以缓存未命中的次数。这样就能把前面轮次留下的缓存也计入比较。
 
-视觉特征只能省去视觉编码；如果希望连语言模型已经做过的计算也保留下来，就要保存注意力的上下文键和值（KV）。编码缓存（encoder cache，EC）保存视觉编码器的输出；KV 则保存语言模型处理前缀时逐层产生的注意力状态。该配置每个语言位置需要 144 KiB 逻辑 KV，400 个视觉位置约为 56.3 MiB。图片、约 7.8 MiB 的视觉特征、约 56.3 MiB 的视觉位置 KV，分别对应不同的重算起点。缓存越接近模型的最终输出，再次使用时省下的计算越多，需要传送的数据也可能越大。下一节就比较传送一次状态要花多久，以及后续使用这些状态能省多少时间。
+视觉特征只能省去视觉编码；如果希望复用语言模型已经算出的结果，就要保存注意力的上下文键和值（KV）。编码缓存（encoder cache，EC）保存视觉编码器的输出；KV 则保存语言模型处理前缀时逐层产生的注意力状态。该配置每个语言位置需要 144 KiB 逻辑 KV，400 个视觉位置约为 56.3 MiB。图片、约 7.8 MiB 的视觉特征、约 56.3 MiB 的视觉位置 KV，分别对应不同的重算起点。缓存越接近模型的最终输出，再次使用时省下的计算越多，需要传送的数据也可能越大。下一节就比较传送一次状态要花多久，以及后续使用这些状态能省多少时间。
 
 ![三种保存对象对应三个重算起点](ch12/figure-12-cache-restart.svg)
 
@@ -272,7 +272,7 @@
 
 沿用 30 轮截图任务的 200 ms RTT，设建链额外需要两次往返。每轮重建会累计花 12 秒，只在第一轮建立则花 0.4 秒，省下 11.6 秒。连接复用之所以有效，是因为后续 29 轮共享了第一轮建立的状态。12.1 中的 105 秒采用已建立连接；加上首次建链后为 105.4 秒，每轮重建则为 117 秒。
 
-连接建立是一次性开销，传输则每轮都要发生。请求频繁时，保留连接可以省去下一轮的建链时间；两次请求间隔很长时，空闲连接仍会占用内存等资源。关闭连接可以释放这些资源，但下次调用需要重新建链。与会话迁移一样，这里也要比较一次准备与后续多次使用的开销。
+连接建立是一次性开销，传输则每轮都要发生。请求频繁时，保持连接可以省去下一轮的建链时间；两次请求间隔很长时，空闲连接仍会占用内存等资源。关闭连接可以释放这些资源，但下次调用需要重新建链。与会话迁移一样，这里也要比较一次准备与后续多次使用的开销。
 
 ### 12.3.2 窗口、反馈与有效吞吐
 
@@ -547,7 +547,7 @@
 | 云上行升至 8 Mbit/s | 云端 43 s、成本 0.066 | 云端方案可以按时完成，成本也更低 |
 | 云上行升至 10 Mbit/s | 云端约 39.8 s | 云端同时更快、更便宜 |
 | 附近发生一次断连，保留前九轮进度并重做第十轮 | 43.9 s | 仍在期限内 |
-| 同次断连丢失十轮进度 | 61 s | 需要改进进度保留机制 |
+| 同次断连丢失十轮进度 | 61 s | 需要改进进度保存机制 |
 
 任务执行期间更换设备，还需要迁移会话状态。12.2 的 64 MiB 会话需要约 7.7 秒准备，每轮快 0.4 秒，20 轮只净省约 0.3 秒。若带宽改善只持续十轮就恢复原状，这次迁移会净损失约 3.7 秒。因此，平台应将预测的持续收益与切换成本比较，只有预计节省的时间足以抵消迁移准备，并能容纳额外开销时，才迁移状态。在一段时间内继续使用同一台设备，可以避免在临界带宽附近反复迁移状态。
 
@@ -645,11 +645,11 @@
 
 (c) 二十轮交互中，十轮的上行速率为 6 Mbit/s，另外十轮为 10 Mbit/s，求云端的总时间，与恒定 8 Mbit/s 比较并解释差额。
 
-(d) 对十轮任务，设第八轮已执行完时断连，恢复 2 秒。分别考虑两种恢复情况：已保留前七轮的进度，只需重做第八轮；前八轮的进度全部丢失，需要全部重做。计算两种情况下完成剩余工作所需的时间，以及整项任务的总耗时。这次断连发生在远程服务路径，端侧继续正常执行；恢复成本包含在基础费率中。恢复期间连接与服务状态保留，初始化只做一次；成本按实际执行轮次及发送字节计收，按原上传单价重新判断最低成本的可行方案。
+(d) 对十轮任务，设第八轮已执行完时断连，恢复 2 秒。分别考虑两种恢复情况：已保留前七轮的进度，只需重做第八轮；前八轮的进度全部丢失，需要全部重做。计算两种情况下完成剩余工作所需的时间，以及整项任务的总耗时。这次断连发生在远程服务路径，端侧继续正常执行；恢复成本包含在基础费率中。恢复期间维持连接并保存服务状态，初始化只做一次；成本按实际执行轮次及发送字节计收，按原上传单价重新判断最低成本的可行方案。
 
 [^raw]: [RAW 图片精修案例与条件预算](../case-studies/raw-retouching.md)。RAW／DNG 与信息保留由归档的华为专业模式及 Adobe 材料支持；本章 30 MB／5 MB／0.3 秒为教学输入，不是历史服务规格。
 
-[^stream]: 分三块的两种计算分别为 12.8 s 与 12.36 s，正文以约 0.4 s 差额解释重叠收益；额外预览另增编码与传输，不等同完整成片。来源：[整图屏障](../calculations/results/image-stream-whole-image-without-preview.md)、[独立分块](../calculations/results/image-stream-independent-blocks-without-preview.md)及[额外预览](../calculations/results/image-stream-independent-blocks-with-preview.md)。各自保留依赖与编码条件。
+[^stream]: 分三块的两种计算分别为 12.8 s 与 12.36 s，正文以约 0.4 s 差额解释重叠收益；额外预览另增编码与传输，不等同完整成片。来源：[整图屏障](../calculations/results/image-stream-whole-image-without-preview.md)、[独立分块](../calculations/results/image-stream-independent-blocks-without-preview.md)及[额外预览](../calculations/results/image-stream-independent-blocks-with-preview.md)。各自采用对应的依赖与编码条件。
 
 [^audio]: [音频块时序计算](../calculations/results/audio-timing-base.md)，输入帧与输出音频块一一对应是该模型的简化；该简化用于说明逐块处理与按序播放的依赖。
 
