# 第 1 章《初识 AI Infra》翻译腔清扫对照表

章节文件：`manuscripts/01-初识 AI Infra.md`。本章原有文字整体已比较通顺，共改动 **13 处**（其中 1 处为术语一致性修正：dense → 稠密）。省略号表示只截取改动附近的文字。

| 修改前 | 修改后 |
|---|---|
| DeepSeek V4 和 V4.1 Flash 提供了一条可以沿全书追踪的线索。 | DeepSeek V4 和 V4.1 Flash 为全书提供了一条可以持续追踪的线索。 |
| 大量输入 token 因此可以省去解码器主体计算，生成新 token 时仍依次经过编码器与解码器。 | 大量输入 token 因此无需经过解码器主体的计算；生成新 token 时，模型仍依次经过编码器与解码器。 |
| 计算只占 $1/201$ 的寻道例子说明，即使这一部分完全消失，总时间也只减少约 $0.5\%$。 | 在前面的寻道例子中，计算只占总时间的 $1/201$；即使这一部分完全消失，总时间也只减少约 $0.5\%$。 |
| $B$ 增大则增加本次运算量：同一份权重要与更多请求当前输入的 token 向量做乘加运算。 | $B$ 增大则增加本次运算量：同一份权重要与更多请求的当前输入 token 向量做乘加运算。 |
| 转折前，共享读取被更多输出分摊；转折后，计算时间与输出数一起增长…… | 转折前，共享的读取由更多输出分摊；转折后，计算时间与输出数一起增长…… |
| 本次采用 0.23 版本的 eager 模式，即按程序运行过程逐项提交加速器工作。 | 本次采用 0.23 版本的 eager 模式，即按程序运行顺序逐项向加速器提交工作。 |
| …BF16 输入、FP32 累加的 dense 矩阵峰值为 503.8 TFLOP/s。 | …BF16 输入、FP32 累加的稠密矩阵峰值为 503.8 TFLOP/s。 |
| 这一趋势可以从权重与上下文状态的区别来解释。 | 这一趋势可以用权重与上下文状态的区别来解释。 |
| 并发增加时，一次矩阵运算会同时处理更多请求当前输入 token 的向量，权重得到更多复用…… | 并发增加时，一次矩阵运算会同时处理更多请求的当前输入 token 向量，权重得到更多复用…… |
| 请求量增加，现有设备的空闲能力最终会被用尽，此时可以增加通用服务器…… | 请求量增加，现有设备的空闲能力最终会用尽；此时可以增加通用服务器…… |
| FPGA 是可以用编程配置硬件逻辑的芯片，网络处理因此能随策略更新而改变。 | FPGA 是可以通过编程配置硬件逻辑的芯片，网络处理因此能随策略更新而改变。 |
| GPT-3 展现出大模型的能力后，多设备协同需求获得更广泛的认可，项目投入随之扩大。 | GPT-3 展现出大模型的能力后，业界更广泛地认识到多设备协同的需求，项目投入随之扩大。 |
| …那里使用同一张 RTX PRO 6000 Blackwell Workstation Edition、vLLM 0.23 和明确的请求长度，显示吞吐与客户端输出间隔。 | …那里在同一张 RTX PRO 6000 Blackwell Workstation Edition 上用 vLLM 0.23 和明确的请求长度测量，展示吞吐与客户端输出间隔。 |

## 发现但未改动的句子（没有把握，留待作者定夺）

- 第 152 行"从 Jeff Dean 这张历史资源表中学到的方法"："历史资源表"说法略生硬（该表实为操作延迟数字表），但不属于翻译腔，改动会牵涉措辞选择，未动。
- 第 27 行"同一模型又能根据不同上下文处理多种任务，既能支持多样的应用行为，又能让底层针对模型执行做专门优化"：嵌套的"又能……既能……又能……"略显缠绕，但语法无误、逻辑层次清楚，为避免无谓改动未动。
- 第 47 行"服务器和更大的协作组通过网络连接"："协作组"措辞少见，但含义可解，且可能是有意的概括说法，未动。
- 第 69 行先写"token 序列"再定义"词元（token）"，后文统一用 token：属术语引入顺序问题而非翻译腔，未动。
