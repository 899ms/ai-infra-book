# 原图处置与新增图清单

每章按正文阅读顺序列出图号、素材和图注。原数值数据文件及来源锁继续保留；编号由正文决定，素材文件名作为稳定引用。原图处置表覆盖基线中的全部 201 幅图。

## 第 1 章：8 → 17 幅

| 原素材 | 当前图号 | 处置 |
|---|---|---|
| `figure-1-1-panorama.svg` | 1-2 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-1-2-request.svg` | 1-3 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-1-3-datacenter.svg` | 1-5 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-1-4-numbers.svg` | 1-6 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-1-capacity-path.svg` | 1-7 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-1-5-budget.svg` | 1-9 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-1-batch-transition.svg` | 1-11 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-1-6-designs.svg` | 1-14、1-15、1-16 | 拆为三幅独立的专用设计图：TPU、网卡卸载与统一互联 |

### 当前阅读顺序

| 图号 | 素材 | 图注 |
|---|---|---|
| 1-1 | `figure-1-programmability.svg` | 应用行为的表达方式与执行路径。左侧通过代码组织程序，右侧通过上下文调用模型；实线表示向下的执行依赖，虚线表示工具执行仍需操作系统。加速器、内存与互联分别承担模型计算、数据保存和设备间传输。 |
| 1-2 | `figure-1-1-panorama.svg` | 从应用到设备的六层分工。沿箭头向下，任务逐步落实为运算、程序和物理资源；调度、观测等跨层能力由正文说明。 |
| 1-3 | `figure-1-2-request.svg` | 一次请求先由路由器选择模型实例，再由实例内部的调度器安排执行。灰色外框圈出同一个实例，实线表示请求和工作提交的方向。 |
| 1-4 | `figure-1-weight-lifetime.svg` | 权重加载一次，生成时逐步读取。蓝框表示显存中持续保存的同一份权重，三个绿框表示先后发生的计算；箭头表示读取或结果依赖，步骤间距不代表耗时。 |
| 1-5 | `figure-1-3-datacenter.svg` | 物理连接的两层视图。上方用数据中心网络连接服务与超节点，下方放大一个超节点，显示主机、网卡、GPU 与显存；线条展示数据路径，设备数量用于示意。 |
| 1-6 | `figure-1-4-numbers.svg` | Jeff Dean 2009 年演讲中的三种操作延迟。横轴为对数刻度，每相邻数量级相差十倍；先识别操作，再比较它们在串行等待中的代价。 |
| 1-7 | `figure-1-capacity-path.svg` | DeepSeek-R1-Distill-Llama-70B：BF16 权重共 141.11 GB，两卡均分后每卡约 70.55 GB；分组 8 比特量化后共 73.73 GB。各条使用相同尺度，虚线表示一张 H100 SXM 的名义 80 GB 容量。柱长只计权重及相应量化附加数据。 |
| 1-8 | `figure-1-read-path.svg` | 本例按每参数一字节，将主要权重读取量近似取为 70 GB。权重驻留显存，计算单元每步沿同一接口读取一遍；用读取量除以接口带宽，得到 20.90 ms 的读取下界。 |
| 1-9 | `figure-1-5-budget.svg` | 保持运算量与读取量不变，分别把算力或带宽翻倍。蓝条是权重读取下界，橙条是矩阵计算下界；较长的读取项决定这组条件下的优化方向。 |
| 1-10 | `figure-1-batch-reuse.svg` | 一批八个请求共享一次权重读取，各产生一个输出。整批读取仍需约 20.90 ms，除以八得到每输出分摊的服务时间；每个请求经历整批执行。 |
| 1-11 | `figure-1-batch-transition.svg` | 批内请求增加时，矩阵运算量按 $2BN$ 增长，权重读取保持每批 70 GB。约 148 个请求处两项下界相等，随后计算项主导。 |
| 1-12 | `figure-1-batch-throughput.svg` | 同一批量模型的理想输出吞吐。每批输出数除以时间下界得到曲线；竖虚线与前图对应同一个约 148 请求的转折点。 |
| 1-13 | `figure-1-measured-throughput.svg` | Qwen3-8B 实测的整批输出吞吐。四档请求数等距排列，纵轴从零开始；模型、精度、输入输出长度和计时范围保持一致。 |
| 1-14 | `figure-1-measured-tpot.svg` | 同一组实测中的每请求输出间隔。吞吐增加的同时，单个请求的平均间隔也在增大；两张图分别回答设备产出和用户等待的问题。 |
| 1-15 | `figure-1-design-tpu.svg` | 专用处理器围绕反复出现的矩阵运算组织计算阵列和输入输出缓冲。缓冲是临时保存待计算或已算完数据的存储区域；三个方框及其箭头展示数据搬运方向。 |
| 1-16 | `figure-1-design-smartnic.svg` | 可编程网卡在数据进入主机前完成指定的包处理。图中实线跟踪数据；网卡承担的处理减少主机 CPU 的辅助工作。 |
| 1-17 | `figure-1-design-ub.svg` | 统一互联连接不同设备的计算与存储资源。模型分工确定要交换什么，互联负责把数据送到后续使用它的设备。 |

## 第 2 章：15 → 33 幅

| 原素材 | 当前图号 | 处置 |
|---|---|---|
| `figure-2-1-dependencies.svg` | 2-1 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-2-architecture.svg` | 2-22 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-2-2-layer.svg` | 2-6 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-2-history.svg` | 2-7 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-2-3-sharing.svg` | 2-9 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-2-4-cache.svg` | 2-10 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-2-5-sparse.svg` | 2-13 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-2-recurrence.svg` | 2-16 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-2-6-hybrid.svg` | 2-17 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-2-7-state-growth.svg` | 2-18 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-2-expert-reuse.svg` | 2-20 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-2-8-residual.svg` | 2-21 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-2-resources.svg` | 2-25 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-2-9-capacity.svg` | 2-29 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-2-10-request.svg` | 2-32 | 按全书体例重画；复合关系分步见本章新增图 |

### 当前阅读顺序

| 图号 | 素材 | 图注 |
|---|---|---|
| 2-1 | `figure-2-1-dependencies.svg` | 循环神经网络的依赖。相同颜色的圆点表示各位置的计算，箭头指向需要前一结果的节点；同层位置沿横向逐次推进。 |
| 2-2 | `figure-2-causal-dependencies.svg` | 因果 Transformer 的层间依赖。各查询读取上一层中允许访问的位置；上一层完成后，同一层的已知输入位置可以并行计算。 |
| 2-3 | `figure-2-qkv-objects.svg` | 同一位置的输入经不同投影产生查询、键和值。注意力先用查询与键计算位置间关系，再用这些关系汇总值。 |
| 2-4 | `figure-2-causal-pairs.svg` | 两段上下文形成长方形加三角形。蓝色为三个新位置分别读取两个旧位置，绿色为新输入内部的因果访问，空白为被屏蔽的未来位置。 |
| 2-5 | `figure-2-ffn-gates.svg` | SwiGLU 的两条升维分支。gate 经 SiLU 后调节 up 的对应元素，乘积再由 down 投影降回主干维度。 |
| 2-6 | `figure-2-2-layer.svg` | Qwen3-8B 一层的骨架。先完成注意力子层，再完成前馈子层；左侧旁路保留子层输入，与变换结果相加，这种连接称为残差连接。 |
| 2-7 | `figure-2-history.svg` | 四步生成的上下文读写。每一步读取蓝色的已有位置，再追加一个橙色新位置；本图初始上下文为四，四步后保留八个位置。 |
| 2-8 | `figure-2-history-batch.svg` | 同一批次的主要投影权重可共享读取，各请求则各有一份上下文。图中固定每请求 8K 上下文，分别累计共享权重与独立 KV 的逻辑读取量。 |
| 2-9 | `figure-2-3-sharing.svg` | 固定四个查询头，分别为每头、每组和全部查询保存 KV。连线表示使用关系；共享上下文后，各查询仍分别产生自己的评分与输出。 |
| 2-10 | `figure-2-4-cache.svg` | 固定 Qwen 层数、头宽、8192 个上下文位置与 BF16，只改变 KV 组数。实际模型采用 GQA；其余两柱是用于分析共享比例的结构变体。 |
| 2-11 | `figure-2-mla-paths.svg` | 两种计算路径改变上投影的位置。展开路径先恢复上下文键，紧凑路径先变换当前查询，再直接读取潜变量；结合律保证对应点积可以按这两种次序计算。 |
| 2-12 | `figure-2-mla-capacity.svg` | Kimi K3 的 24 层 MLA 在相同 8K 上下文、BF16 下的两种保存量。紧凑路径保存上投影前的表示，展开路径保存各头的键和值。 |
| 2-13 | `figure-2-5-sparse.svg` | 上下文压缩与索引选择的先后关系。上方用八个位置、每四个合成一条演示压缩，下方展示一次查询先扫描索引再读取主状态的过程。 |
| 2-14 | `figure-2-sparse-capacity.svg` | DeepSeek V4-Flash 在 8K 上下文下的四项状态。窗口、压缩上下文和索引是已保留的上下文；压缩缓冲保存尚在汇总或更新中的数据，单独计入容量。 |
| 2-15 | `figure-2-compression-steps.svg` | 压缩比为四时的一次块完成过程。前几次输入更新同一块内缓冲，第四个位置到来后形成可供后续查询使用的压缩条目。 |
| 2-16 | `figure-2-recurrence.svg` | 固定矩阵的递推更新。新键和值的外积加入旧状态，矩阵形状保持不变；查询使用更新后的状态计算输出。本图采用简单累加递推。 |
| 2-17 | `figure-2-6-hybrid.svg` | Qwen3.6 的两类层。配置决定每层采用线性或完整注意力，随后都执行路由专家和共享专家；上方两个分支分别表示线性注意力和完整注意力，模型分别有 30 层与 10 层。 |
| 2-18 | `figure-2-7-state-growth.svg` | 上下文增长时的状态容量。横纵轴均为对数刻度；Qwen 逐位置追加 KV，Kimi K3 紧凑表示包含固定递推状态和增长的 MLA 上下文，DeepSeek V4-Flash 按压缩块增加条目。 |
| 2-19 | `figure-2-state-access.svg` | 同一组模型每步计入的状态访问。逐位置上下文计读取，递推矩阵计一读一写；这些是指定路径的逻辑数据量，用于比较容量与访问的不同增长方式。 |
| 2-20 | `figure-2-expert-reuse.svg` | 64 个 token 各选八个专家，总计 512 次分派。分散时可覆盖 256 个专家，集中时只访问八个；每专家处理的行数随之改变。 |
| 2-21 | `figure-2-8-residual.svg` | 普通残差与四路 mHC 的连接范围。普通残差保留一条子层输入旁路；mHC 汇合多路状态供子层计算，再混合输出。两种图中的子层输入均为 4096 维。 |
| 2-22 | `figure-2-architecture.svg` | 四模型的主干层数，使用同一线性尺度。层数决定同类工作沿网络深度重复多少次。 |
| 2-23 | `figure-2-architecture-width.svg` | 同一线性尺度下的主干隐藏维度。每个位置的特征宽度决定投影的输入或输出尺寸。 |
| 2-24 | `figure-2-architecture-experts.svg` | 每个 MoE 层保存的路由专家数量。Qwen3-8B 使用稠密前馈层，其路由专家数为零；每个 token 实际选中数见表 2-A。 |
| 2-25 | `figure-2-resources.svg` | 四模型完整权重统一用 BF16 表示时的容量，使用同一线性尺度。这里累计全部参数，包括每次仅选中部分的路由专家。 |
| 2-26 | `figure-2-resources-compute.svg` | 相同单请求、已有 8K 上下文时的单步矩阵运算量。按各模型在表 2-C 说明的执行路径累计，Kimi K3 采用紧凑 MLA。 |
| 2-27 | `figure-2-resources-state.svg` | 相同 8K 上下文下的每请求状态。上下文表示采用 BF16，递推矩阵和压缩缓冲采用相应实现的精度；各项合计对应表 2-C。 |
| 2-28 | `figure-2-long-context-compute.svg` | 已有 8K／200K 上下文后再处理一个 token 的矩阵运算量。蓝、橙分别对应两种上下文长度，横轴为对数刻度，柱端标出 GFLOPs。沿用表 2-C 的 decode 路径，Kimi K3 的 KDA 使用递推、MLA 使用紧凑表示。星号表示 Qwen3-8B 的 200K 条件超过本章固定配置的未缩放上下文上限，仅按原结构外推。 |
| 2-29 | `figure-2-9-capacity.svg` | 权重、固定预留和一条请求 KV 的逐项容量。短竖线标出各设备容量；KV 采用 BF16、上下文长度 8192，固定预留为 2 GiB。 |
| 2-30 | `figure-2-history-capacity.svg` | 70B 的 4-bit 方案在同一张 80 GB 设备上，上下文长度从 8K 增至 32K 时允许保留的独立请求数。每请求状态增加，使剩余空间容纳的请求数减少。 |
| 2-31 | `figure-2-reverse-budget.svg` | 从设备反推权重预算。24 GB 中先保留 4 条 8K 请求的 KV 和 2 GiB 工作区，余量给出 BF16 参数上界；图中容量均为十进制 GB。 |
| 2-32 | `figure-2-10-request.svg` | 生成四个输出的调用顺序。prefill 处理 128 个输入并产生首输出，随后三次 decode 各将前一输出送回模型；最终保留 131 个位置。 |
| 2-33 | `figure-2-request-compute.svg` | 同一 128 输入、四输出请求的矩阵运算量。模型执行范围与缓存路径见本节题设；这是逐调用累计的计算量。 |

## 第 3 章：12 → 32 幅

| 原素材 | 当前图号 | 处置 |
|---|---|---|
| `figure-3-1-stages.svg` | 3-1 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-3-2-workload-budget.svg` | 3-4 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-3-success-cost.svg` | 3-9 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-3-3-agent.svg` | 3-10 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-3-tool-dependency.svg` | 3-11 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-3-vision-shapes.svg` | 3-14 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-3-4-realtime.svg` | 3-16 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-3-5-training.svg` | 3-20 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-3-6-rl.svg` | 3-23 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-3-7-scaling.svg` | 3-25 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-3-8-history.svg` | 3-28 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-3-9-gpu-hours.svg` | 3-30 | 按全书体例重画；复合关系分步见本章新增图 |

### 当前阅读顺序

| 图号 | 素材 | 图注 |
|---|---|---|
| 3-1 | `figure-3-1-stages.svg` | 恢复 6144 个上下文位置，处理 2048 个新输入并生成四个输出。首输出来自 prefill，后续三次 decode 各追加一个位置；纵向表示调用次序，间距用于示意。 |
| 3-2 | `figure-3-request-clocks.svg` | 请求到达、首输出与末输出决定三个计时区间。首响应包含开始生成前的等待，输出间隔描述生成过程，完整请求时间从到达累计到结束。 |
| 3-3 | `figure-3-state-time-area.svg` | 保留 1 GiB 状态等待十秒，对应面积为 10 GiB·s。横轴为状态存活时间，纵轴为占用空间；面积描述这段等待消耗的空间时间。 |
| 3-4 | `figure-3-2-workload-budget.svg` | 相同两分钟总量的三种窗口组成。长输入类为 8192 输入、256 输出，长输出类为 1024 输入、2048 输出；两类混合比例改变阶段需求。 |
| 3-5 | `figure-3-stage-demand.svg` | 三种窗口组成对应的输入与后续生成需求。输入数按 token 计，后续生成按每请求的一次 decode 步计；每请求首输出已计入 prefill。 |
| 3-6 | `figure-3-queue-mechanism.svg` | 工作先进入队列，再由处理资源完成。到来快于处理时差额留在队列中；处理快于到来时，资源逐步消化已有积压。 |
| 3-7 | `figure-3-queue-backlog.svg` | 每秒完成 6144 个 decode 步的流体模型。后一分钟积压至 79,632 步，120 秒后停止到达，再经过约 12.96 秒排空。 |
| 3-8 | `figure-3-arrival-measured.svg` | 同一实际实例回放两种到达序列的结果。完整请求 p95 接近，首响应 p95 则相差约十六秒；模型、设备、KV 池与并发上限均按正文实验条件固定。 |
| 3-9 | `figure-3-success-cost.svg` | 各尝试 100 次的教学比较。所有尝试的成本都进入分子，通过检查的成功任务数进入分母；两种策略的成功任务成本分别为 2 和 2.5 单位。 |
| 3-10 | `figure-3-3-agent.svg` | 代码任务四轮调用的模型墙钟时间，各轮从自己的起点计时。模型时间合计 76.294 秒，工具合计约 0.078 秒，整任务另含控制与交接时间。 |
| 3-11 | `figure-3-tool-dependency.svg` | 两个工具存在前后依赖时的任务时间线。模型先运行两秒，工具 A 用六秒，工具 B 用十秒，模型最后运行三秒，总计二十一秒。 |
| 3-12 | `figure-3-tool-parallel.svg` | 两个工具独立时可以同时开始，模型在较慢的工具 B 完成后继续，任务共十五秒。与前图使用相同时间尺度；工具工作总量仍为十六秒。 |
| 3-13 | `figure-3-branch-state.svg` | 两个生成分支指向同一份公共前缀，并各自保存新增尾部。箭头表示引用关系，共享前缀只计一份容量。 |
| 3-14 | `figure-3-vision-shapes.svg` | 图像从像素网格变为模型位置。640 方图切成 40×40 个块，相邻 2×2 块合并成一个位置，形成 20×20、共 400 个位置。 |
| 3-15 | `figure-3-vision-state.svg` | 位置数与每位置特征宽度分别计量。四组 2560 维 BF16 编码特征占 7.8125 MiB；这些位置进入语言模型后，另产生各层的 KV 状态。 |
| 3-16 | `figure-3-4-realtime.svg` | 可组合的多模态阶段。编码形成模型输入，语言模型生成回复，声学模块将回复转成音频，接收端缓冲与播放设备决定何时真正发声。 |
| 3-17 | `figure-3-audio-timing.svg` | 八块音频的教学播放时间线。每块长二十毫秒，圆点标到达，短竖线标原定播放时刻，色条标实际播放；第三块晚到五毫秒，后续播放随之顺延。 |
| 3-18 | `figure-3-audio-interrupt.svg` | 同一教学场景中的本地打断。123 ms 发出操作，130 ms 设备静音；远端计算是否停止属于另一条控制路径。 |
| 3-19 | `figure-3-activation-lifetime.svg` | 一层激活从前向完成后保留到相应反向用完。横轴按事件排列，间距表示过程顺序；反向需要的前向输入决定这段存活区间。 |
| 3-20 | `figure-3-5-training.svg` | 训练沿前向依赖计算输出，再沿反向依赖传递梯度。当前层既向前层传输入梯度，也计算自己的权重梯度，供优化器更新。 |
| 3-21 | `figure-3-training-states.svg` | Qwen3-8B 全参数训练的参数相关状态。每参数包括两字节计算权重和四组四字节状态，共十八字节；激活和工作区由各自寿命另行核算。 |
| 3-22 | `figure-3-training-flops.svg` | Qwen3-8B 的 8192 输入全参数训练。全部位置执行词表头、没有重计算；按矩阵逐项累计，前向加反向为 431.368 TFLOPs。 |
| 3-23 | `figure-3-6-rl.svg` | 强化学习中的角色和数据流。生成端产生回答，反馈环节评价结果，筛选后送给学习器更新；新权重再用于下一批生成。 |
| 3-24 | `figure-3-rl-stage-work.svg` | 保持十六条有效样本目标，生成数由三十二增到六十四。生成输入、后续生成和参考评分随全部回答增加，策略更新处理的样本数保持相同。 |
| 3-25 | `figure-3-7-scaling.svg` | 八个公开 C4 观测的预测与实际损失。六点用于拟合，两点预先留出作检验；对角线表示预测等于观测，点到线的偏差反映误差。 |
| 3-26 | `figure-3-scaling-residual.svg` | 同一组预测误差的放大视图。F1—F6 为拟合点，H1—H2 为留出点；纵轴是预测减观测，保留正负号。 |
| 3-27 | `figure-3-lifecycle-cost.svg` | 训练与服务累计成本的题设比较。截距是训练投入，斜率是单次调用成本；虚线标出超出拟合参数或数据范围的方案，竖线为约 2.048 亿次的成本交点。 |
| 3-28 | `figure-3-8-history.svg` | 相近参数规模的模型投入不同数量的训练 token。柱值为报告训练数据量除以参数数，Qwen 采用模型家族披露的数据预算。 |
| 3-29 | `figure-3-moe-history.svg` | MoE 的总参数与每 token 激活参数分别对应容量和部分计算需求。图中采用公开报告的总量与激活量，二者都使用十亿参数为单位。 |
| 3-30 | `figure-3-9-gpu-hours.svg` | 采用 A100 80GB 的 Llama 训练用量。各柱是公开 GPU 小时，同一张图使用相同设备类别与线性尺度。 |
| 3-31 | `figure-3-h100-hours.svg` | 采用 H100 80GB 的 Llama 3.1 训练用量。纵轴分模型规模，横轴为百万 GPU 小时；设备类别与前图分开呈现。 |
| 3-32 | `figure-3-v3-stage-hours.svg` | DeepSeek-V3 采用 H800 的三个训练阶段。分别累计预训练、上下文扩展与后训练用量，保留各阶段的公开统计范围。 |

## 第 4 章：14 → 26 幅

| 原素材 | 当前图号 | 处置 |
|---|---|---|
| `figure-4-1-reuse.svg` | 4-1 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-4-2-components.svg` | 4-2 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-4-3-expert-rows.svg` | 4-5 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-4-4-attention.svg` | 4-7 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-4-5-precision.svg` | 4-8 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-4-6-capacity.svg` | 4-9 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-4-7-memory.svg` | 4-11 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-4-8-layout.svg` | 4-12 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-4-9-pipeline.svg` | 4-14 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-4-10-locality.svg` | 4-18 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-4-11-interconnect.svg` | 4-20 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-4-12-specialization.svg` | 4-22 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-4-13-roofline.svg` | 4-24 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-4-14-performance.svg` | 4-25 | 按全书体例重画；复合关系分步见本章新增图 |

### 当前阅读顺序

| 图号 | 素材 | 图注 |
|---|---|---|
| 4-1 | `figure-4-1-reuse.svg` | 两种调用都使用同一份 32 MiB 权重。上方是一行输入，下方以部分条带示意 256 行；每行分摊的权重读取从 32 MiB 减至 128 KiB。 |
| 4-2 | `figure-4-2-components.svg` | 主机、显存与芯片内部的关系。实线表示数据经过缓存、局部缓冲、矩阵单元和累加存储，虚线表示主机提交工作。图按硬件功能分组。 |
| 4-3 | `figure-4-codesign-loop.svg` | 模型与硬件跨代协同。实线沿时间向下：既有设备限制候选，软件暴露持续瓶颈，硬件回应需求，新设备重新打开模型选择空间。 |
| 4-4 | `figure-4-matrix-array.svg` | 三行三列的乘加阵列示意。输入沿行传递，权重沿列传递，每个乘加位置保留自己的部分和。这里用小阵列解释操作数复用。 |
| 4-5 | `figure-4-3-expert-rows.svg` | 一个专家的 16 行计算块。每专家只有两行时，剩余十四行填零；每专家有 64 行时，可组成四个完整块，图中展示其中一块。 |
| 4-6 | `figure-4-expert-padding-total.svg` | 相同 512 行有效输入在全部专家上的执行量。分散到 256 个专家后总计执行 4096 行，集中到八个专家时只执行 512 行。 |
| 4-7 | `figure-4-4-attention.svg` | 同一注意力块在四种资源配置下的服务周期。矩阵、共享存储和指数三项分别比较，单独增加一种能力后，其他资源可能成为较长的一项。 |
| 4-8 | `figure-4-5-precision.svg` | 相同压缩权重的两条计算路径。先展开会形成 32 MiB 的 BF16 副本；低精度路径在计算过程中完成缩放和合并。压缩权重及缩放因子共 8.5 MiB。 |
| 4-9 | `figure-4-6-capacity.svg` | 固定 24 GB 显存中的权重、工作区和 KV。8K 四请求与 16K 两请求可以容纳，8K 五请求超过虚线标出的容量上限。 |
| 4-10 | `figure-4-memory-inflight.svg` | 读取请求从发出到返回一直占用请求记录空间。多个独立请求交叠，才能在单次访问等待期间持续利用接口；图中只画四个代表请求。 |
| 4-11 | `figure-4-7-memory.svg` | 在每事务 128 bytes、返回延迟 500 ns 的题设下，增加在途请求数提高带宽上界，直到碰到接口本身的速率上限。 |
| 4-12 | `figure-4-8-layout.svg` | 每行前 256 bytes 是实际读取区间，相邻行起点相差 8192 bytes。128 行合计读取 32 KiB，行间灰色区域由步长跳过。 |
| 4-13 | `figure-4-slot-lifetime.svg` | 一个输入槽从发起到释放的完整生命周期。传输 64 tick，额外等待 128 tick，数据在 192 tick 就绪，再计算 128 tick，于 320 tick 释放。 |
| 4-14 | `figure-4-9-pipeline.svg` | 一个输入槽的四块时序。蓝条为传输，橙线为就绪，绿条为计算，浅灰为槽占用；上一块用完后才能再次发起，完成时刻为 1280 tick。 |
| 4-15 | `figure-4-pipeline-two.svg` | 两个输入槽使用相同时间尺度。前两块可提前发起，但第三块到 512 tick 才就绪，第二块在 448 tick 已结束，留下六十四 tick 空闲。 |
| 4-16 | `figure-4-pipeline-three.svg` | 三个输入槽提前发起前三块，第一槽释放后接收第四块。矩阵单元从 192 连续计算到 704 tick，第四槽不再缩短完成时间。 |
| 4-17 | `figure-4-matrix-vector-handoff.svg` | 矩阵与向量单元通过完整行组交接。QK 产生分数，Softmax 产生概率，PV 使用概率后释放槽；另一槽容纳相邻行组，使不同组可以重叠推进。 |
| 4-18 | `figure-4-10-locality.svg` | 计算集中在裸片 0 时，需要跨连接读取裸片 1 上的 32 GiB 权重。题设链路为每秒一 TiB，仅这笔跨裸片传输约需 31 ms。 |
| 4-19 | `figure-4-locality-compute.svg` | 计算放到权重所在裸片后，跨连接交换输入与结果，共 64 MiB。在相同链路上，传输约六十一微秒；权重由各裸片本地读取。 |
| 4-20 | `figure-4-11-interconnect.svg` | 8 KiB 小消息的启动与传输。固定启动两微秒，带宽从 100 增至 200 GB/s 只缩短蓝色传输项。 |
| 4-21 | `figure-4-large-message.svg` | 2 MiB 消息在相同启动条件下的传输时间。较大的蓝色传输项使带宽翻倍带来更显著的收益；本图纵轴范围与小消息图分别标注。 |
| 4-22 | `figure-4-12-specialization.svg` | 固定权重的同时，各请求仍独立读取 KV。题设每请求保留 8K 上下文，batch 从十三起 KV 读取超过共享权重读取。 |
| 4-23 | `figure-4-rom-paths.svg` | 独立只读权重改变两条存储路径。上方权重与 KV 争用 HBM；下方 ROM 提供权重，HBM 保留可写状态。箭头表示读取，KV 还需写入新状态。 |
| 4-24 | `figure-4-13-roofline.svg` | 同一 Q 投影随输入行数增加的资源时间。计算量按行数增长，片外访问同时包含固定权重和增长的输入输出；从 179 行起计算项较长。 |
| 4-25 | `figure-4-14-performance.svg` | RTX PRO 6000 的投影总耗时。每个条件测十一轮、每轮十六次调用，取每轮平均耗时的中位数；计时包含提交与同步。 |
| 4-26 | `figure-4-performance-traffic.svg` | 相同四个条件下另行采集的 DRAM 读取计数。单行均约 32 MiB，256 行复用为 256 bytes、轮换约 32.1 MiB。访问计数与常规计时分别测量。 |

## 第 5 章：23 → 30 幅

| 原素材 | 当前图号 | 处置 |
|---|---|---|
| `figure-5-1-execution.svg` | 5-1 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-reuse-steps.svg` | 5-2 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-tile-working-set.svg` | 5-3 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-2-tiles.svg` | 5-4 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-tile-residency.svg` | 5-5 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-3-banks.svg` | 5-6 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-4-reduction.svg` | 5-7 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-fusion-path.svg` | 5-8 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-5-boundaries.svg` | 5-9 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-buffer-slots.svg` | 5-10 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-6-fusion-buffer.svg` | 5-11 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-attention-storage.svg` | 5-12 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-7-online-softmax.svg` | 5-13 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-8-attention-tradeoff.svg` | 5-14 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-9-polyhedral.svg` | 5-15 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-10-quantization.svg` | 5-18 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-11-feedback.svg` | 5-19 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-12-runtime.svg` | 5-20 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-13-graph-copy.svg` | 5-25 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-14-specialization.svg` | 5-26 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-15-persistent.svg` | 5-27 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-16-critical-path.svg` | 5-29 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-5-17-request.svg` | 5-30 | 按全书体例重画；复合关系分步见本章新增图 |

### 当前阅读顺序

| 图号 | 素材 | 图注 |
|---|---|---|
| 5-1 | `figure-5-1-execution.svg` | CPU 提交、H2D 输入复制、设备内核与 D2H 结果返回依次发生。上行内核二十微秒，结果三十五微秒可用；下行内核五微秒，结果二十微秒可用。 |
| 5-2 | `figure-5-reuse-steps.svg` | 先算一个输出，再算相邻输出。蓝色表示所用的 A 行，橙色表示所用的 W 列，绿色表示本次得到的输出元素。图中矩阵缩小为示意尺寸，实际算例的 K 为 4096。 |
| 5-3 | `figure-5-tile-working-set.svg` | 一次块乘更新整个 m×n 输出块。A、W 输入为 BF16，累加器为 FP32（每个元素占 4 bytes 的 32 位浮点格式）；沿 K 换入新输入块时，累加器一直保留。图中尺寸符号表示形状，方框面积不按字节数缩放。 |
| 5-4 | `figure-5-2-tiles.svg` | 每个点标出输出块形状。扩大输出块能减少跨接口的重复读取，但需要更多局部存储。虚线为三份矩阵各经过一次的 128 MiB；比较条件为 k=32、单组输入缓冲、各输出块独立读取输入。 |
| 5-5 | `figure-5-tile-residency.svg` | 条形总长均表示 96 KiB。24 KiB 的工作集能放四份，80 KiB 的只能放一份。这里把输入缓冲和累加器合并计入一个教学预算；实际 GPU 的共享内存、寄存器分别受限，真实驻留数还取决于线程数等约束。 |
| 5-6 | `figure-5-3-banks.svg` | 同一列的 32 个不同字由 32 个 lane 同时请求。上半图行跨度为 32 个字，请求集中到同一 bank；下半图补齐为 33 个字，请求分散到 32 个 bank。图中画出前四个请求；假设每个 bank 每轮提供一个 32-bit 字，不计广播。 |
| 5-7 | `figure-5-4-reduction.svg` | 上行由一组处理完整一行，输入保留到归一化结束；下行把一行分为八段，先求局部和，再合并，最后重读输入并归一化。每行 4096 个 BF16 元素；图中仅画一行，推广到 1024 行时，第一阶段任务数由 1024 增至 8192。橙色框表示增加的原输入读取。 |
| 5-8 | `figure-5-fusion-path.svg` | 上半图的完整 T 经过一次写出与一次读回；下半图中局部片段 t 直接传给乘法。两种方式仍读取 G、U 并写出 Z，图中省略这些共同的输入输出边。融合保持原来的中间舍入语义。 |
| 5-9 | `figure-5-5-boundaries.svg` | 每少保存一个 24 MiB 中间量，就少一次写出和一次读入，共 48 MiB。G、U 为 BF16，最终输出占 1 byte/元素，量化尺度预先给定。各方案保留相同的运算与舍入顺序。 |
| 5-10 | `figure-5-buffer-slots.svg` | 块 0 在 2–5 μs 使用槽 A，所以块 2 到 5 μs 才能开始写入 A。图中抽取三个时段；4–5 μs 的块 1 已在槽 B 中等待，尚未开始计算。颜色固定表示槽 A、B，文字说明当前读写的是哪个块。 |
| 5-11 | `figure-5-6-fusion-buffer.svg` | 两个输入槽交替复用，让搬运与计算重叠。每块搬运 2 μs、计算 3 μs，资源独立，忽略同步开销；同一颜色表示同一槽，计算读取结束后才能再次写入。 |
| 5-12 | `figure-5-attention-storage.svg` | 上半图保存完整 S、P，两份 FP32 矩阵各占 256 MiB，写出与读回合计 1 GiB。下半图只传递已处理部分的最大值 m、指数和 ℓ、加权值 u；每处理完一个块，其分数缓冲即可复用。箭头概括处理顺序，完整计算还需读入 Q、K、V。 |
| 5-13 | `figure-5-7-online-softmax.svg` | 两个块的分数分别为 0、ln 2，值分别为 1、3。最大值增大后，将旧指数和与旧加权值同时乘以 1/2，再加上新块的贡献，最后才做除法。箭头传递的是统计量，旧分数无需保留。 |
| 5-14 | `figure-5-8-attention-tradeoff.svg` | 快速缓冲为 128 KiB，序列长 8192、头维度 128，无掩码；每个点对应正文表格的一种 K/V 块大小。横轴为缓冲与下一层之间的访问量，纵轴为在线更新次数，采用对数刻度。从 b=64 的点移到 b=1 的点，读取减少，更新次数却约增至 43 倍。 |
| 5-15 | `figure-5-9-polyhedral.svg` | 循环层次确定存储寿命。外层选输出块，创建十六 KiB 累加器；内层 ko 反复读取 A、W 块，全部归约结束后再舍入并激活。 |
| 5-16 | `figure-5-activation-order.svg` | 同样两个部分和，先相加得到零，再做 SiLU 仍为零；先对各部分做 SiLU 再相加，得到约 0.4621。两个数说明变换越过归约后改变了函数。 |
| 5-17 | `figure-5-quantization-scale.svg` | 一行分成两个块，后一块中的十决定整行尺度。第一项要先按这一尺度映射，再舍入到格式允许的值，最后反量化。 |
| 5-18 | `figure-5-10-quantization.svg` | 两种方案都先读完整输入以确定行尺度。保存 FP8 结果后十二列块合计重读 192 MiB；融合方案重读 FP16 输入 384 MiB。权重与输出另有相同的 204 MiB。 |
| 5-19 | `figure-5-11-feedback.svg` | 形状 A 占比超过 2/3 时，新实现的总执行时间更短。A、B 原耗时均为 10 μs，新实现分别为 5、20 μs；调用串行，图中比较稳态执行。交点由两种实现的平均时间相等确定。 |
| 5-20 | `figure-5-12-runtime.svg` | 普通提交的三次 FFN 实测。上行为主机内核启动 API，下行为设备 kernel；横轴从本段标记起点计时。十八次内核启动对应十八个设备 kernel。 |
| 5-21 | `figure-5-runtime-1.svg` | 三次 FFN 融合激活链后，主机与设备各有十五次启动和内核执行。数据来自与前图相同实验的独立标记范围。 |
| 5-22 | `figure-5-runtime-2.svg` | 三次 graph launch 对应十八个设备 kernel。图重放减少主机提交次数，设备仍执行原图各节点；本图按自身采集范围标出真实时间。 |
| 5-23 | `figure-5-runtime-3.svg` | 先融合再重放，主机发起三次图执行，设备执行十五个 kernel。融合与图重放分别改变设备边界和主机提交。 |
| 5-24 | `figure-5-graph-address.svg` | 图执行时读取记录的地址 G。新输入位于 X 时先复制到 G；上游直接写 G 时沿用同一缓冲，省去中间复制。 |
| 5-25 | `figure-5-13-graph-copy.svg` | 橙色为准备，蓝色为额外输入复制，绿色为设备计算。普通方式四十微秒；图的两 MiB 输入约二十七微秒，十六 MiB 输入约四十二微秒。 |
| 5-26 | `figure-5-14-specialization.svg` | 同一组调用反复执行时，最省时的策略随复用次数变化。曲线采用例 5-12 的形状、处理率和准备时间，整数选择边界使用未舍入数值计算。 |
| 5-27 | `figure-5-15-persistent.svg` | 粗粒度执行先完成八块投影，再启动八块激活。每块投影约 32.2 μs、激活约 39.3 μs，两次主机启动各五微秒；竖线为激活开始。 |
| 5-28 | `figure-5-persistent-blocks.svg` | 矩阵与向量资源独立、缓冲充足。每个任务另计 0.7 μs 调度与通知，首块投影完成后即可激活，八块流水约三百五十八微秒结束。 |
| 5-29 | `figure-5-16-critical-path.svg` | 收尾等待 A、B 两条分支。A 从 60 μs 缩短至 15 μs 后，较慢分支由 A 切换为 B，请求从 80 μs 降至 60 μs。准备和收尾各为 10 μs，两条分支使用独立资源。 |
| 5-30 | `figure-5-17-request.svg` | 同轮替换前的请求时间减去替换后的请求时间，正值表示替换后更快。Qwen3-8B，7239-token 输入、强制 32-token 输出，并发 1，BF16、eager，关闭前缀缓存；11 对交错计时，轮内顺序随机，期间有其他驻留服务。虚线为配对时间差的中位数约 1.3 ms。阶段表来自独立 profile。 |

## 第 6 章：21 → 36 幅

| 原素材 | 当前图号 | 处置 |
|---|---|---|
| `figure-6-1-placement.svg` | 6-1 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-2-capacity.svg` | 6-2 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-3-tp.svg` | 6-5 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-4-pipeline.svg` | 6-6 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-5-dispatch.svg` | 6-7 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-6-ep-layout.svg` | 6-8 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-7-reuse.svg` | 6-10 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-8-expert-load.svg` | 6-11 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-9-ring-rounds.svg` | 6-14 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-10-tp-time.svg` | 6-16 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-11-collectives.svg` | 6-17 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-12-resources.svg` | 6-18 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-13-ports.svg` | 6-20 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-14-topology.svg` | 6-21 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-15-torus.svg` | 6-24 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-16-systems.svg` | 6-26 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-17-pool-placement.svg` | 6-27 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-18-read-window.svg` | 6-29 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-19-memory-pool.svg` | 6-30 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-20-session-schedule.svg` | 6-31 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-6-21-scale-cost.svg` | 6-35 | 按全书体例重画；复合关系分步见本章新增图 |

### 当前阅读顺序

| 图号 | 素材 | 图注 |
|---|---|---|
| 6-1 | `figure-6-1-placement.svg` | 相同八张卡上的三种实例分组。每个外框表示一个独立推理实例，内部连线表示完成请求所需的协作。各实例一次处理一个请求时，三种部署方式分别能同时处理八个、两个和一个请求。 |
| 6-2 | `figure-6-2-capacity.svg` | 单卡与八卡 TP 的每卡内存需求。权重和 KV 随 TP 分片，2 GiB 工作区则在每张卡分别预留。虚线表示 24 GB 的每卡容量；条形长度按十进制 GB 绘制。 |
| 6-3 | `figure-6-tp-columns.svg` | 按权重矩阵的列切分时，每张卡使用完整输入，计算不同的输出元素。格内的转置符号 T 表示将方括号中横排的数解释为列向量。 |
| 6-4 | `figure-6-tp-rows.svg` | 按权重矩阵的行切分时，两卡产生形状相同的输出部分和。对应位置相加，恢复完整乘法的结果。 |
| 6-5 | `figure-6-3-tp.svg` | SwiGLU 的两卡切分。上投影按权重矩阵的列划分输出特征，逐元素运算留在本地；下投影按权重矩阵的行划分输入特征。两步均保留全部 $m$ 个 token，切分的是每个 token 的特征维度。每卡中间激活为 $m\times(f/2)$，输出部分和仍为 $m\times h$，最后逐元素求和。 |
| 6-6 | `figure-6-4-pipeline.svg` | 四个等时阶段处理四个独立微批次。每格为 1 ms，同色表示同一微批次。第一项结果在 4 ms 产生，最后一项在 7 ms 产生；左上到右下的空白来自流水填充与排空。 |
| 6-7 | `figure-6-5-dispatch.svg` | 卡 0 持有 token A，选择本地专家 1 与卡 3 上的专家 6。输入沿上方路径派发，专家输出沿下方返回并加权合并。专家选择数决定计算次数，跨设备目的地决定输入派发路径。 |
| 6-8 | `figure-6-6-ep-layout.svg` | 八张卡排成四个专家分组，每组两卡沿专家中间维分工。同一列的注意力头与 KV 重复四份；每组因此都能在本地得到相同的专家输入。 |
| 6-9 | `figure-6-ep-reduction.svg` | 横向箭头表示组内 TP 求和，纵向箭头表示四组 EP 求和。格内数字是行内求和后的一个输出元素，两列各自得到相同的完整结果 10。 |
| 6-10 | `figure-6-7-reuse.svg` | 64 个 token、每 token 八个专家，共 512 次分派。均匀覆盖与集中选择的有效计算量相同，被选中专家的权重读取相差 16 倍。每个专家 BF16 权重为 36 MiB，批内读取一次。 |
| 6-11 | `figure-6-8-expert-load.svg` | 均匀使用 128 个专家时，四个 EP 组各处理 128 次 token—专家计算，合计读取 4.5 GiB 权重。 |
| 6-12 | `figure-6-expert-load-1.svg` | 所选八个专家都在组 0：权重读取降到 288 MiB，512 次计算却全部压在同一组。 |
| 6-13 | `figure-6-expert-load-2.svg` | 把同八个专家分散到四组，保留 288 MiB 的读取量，同时让四组各承担 128 次计算。三图纵轴范围相同。 |
| 6-14 | `figure-6-9-ring-rounds.svg` | 仅跟踪块 0 的一个元素：每经过一张卡，就加入该卡的贡献。三轮后得到 1111，保存在卡 3。其他三个块同时沿环推进。 |
| 6-15 | `figure-6-ring-gather.svg` | ReduceScatter 结束时，卡 0、1、2、3 分别持有块 1、2、3、0。接下来每轮转发一块，三轮后每卡都拥有四块完整结果。 |
| 6-16 | `figure-6-10-tp-time.svg` | 根据式（6-5）和环形归约模型绘制单步时间。本地内存访问随卡数增加而减少，归约时间则增加；0.20 ms 的其他串行处理时间保持不变。条形总长度为单步执行时间。 |
| 6-17 | `figure-6-11-collectives.svg` | 八卡 ring 与未分段二项树的时间模型，固定每轮 2 μs、有效单向 50 GB/s。交点约为 184 KiB；8 KiB 的 decode 输入位于主要受启动开销影响的一侧，64 MiB 的 prefill 输入位于主要受数据传输时间影响的一侧。 |
| 6-18 | `figure-6-12-resources.svg` | 通信独立运行时，配置 B 用 0.18 ms 完成，快于 A 的 0.24 ms。 |
| 6-19 | `figure-6-resources-concurrent.svg` | 同一配置的两条色带从同一时刻开始。橙色为通信，蓝色为计算；后续工作等待两者完成。A 在 0.44 ms 完成，B 在 0.62 ms 完成。 |
| 6-20 | `figure-6-13-ports.svg` | 同一台 32 端口交换机的两种分配，每个端口单向带宽为 50 GB/s。上联链路是所有跨交换机流量的共同出口；16／16 分配提供 800 GB/s 上联带宽，24／8 分配只提供 400 GB/s。颜色条的格数与端口数一致。 |
| 6-21 | `figure-6-14-topology.svg` | 递归算法第三轮从节点 0 发往节点 4。细线为 16 节点物理环，箭头标出经过的四条链路。 |
| 6-22 | `figure-6-topology-swing.svg` | 同一物理环上，Swing 第三轮从节点 0 发往节点 3，经过三条链路。每节点该轮发送量仍为 1 MiB。 |
| 6-23 | `figure-6-topology-load.svg` | 把所有发送者的消息累加到各条有向链路上，取每轮最大值。递归前三轮均为 4 MiB，Swing 为 4、2、2 MiB。 |
| 6-24 | `figure-6-15-torus.svg` | 沿一个维度把三维环面网络分成两半，需要切断中间连接和首尾连接。每处包含 k² 条链路，合计 2k² 条。 |
| 6-25 | `figure-6-torus-growth.svg` | k 从 4 增到 8 时，设备数从 64 增到 512，二分链路从 32 增到 128。两种增长均以 k = 4 时为基准。 |
| 6-26 | `figure-6-16-systems.svg` | 三个公开系统的连接层次。NVLink 是 NVIDIA 的设备互联，TPU v4 的光电路交换机连接电互联单元，Unified Bus 将主机侧计算和内存资源接入统一互联。 |
| 6-27 | `figure-6-17-pool-placement.svg` | 借用前的物理占用。每节点容量为 64 GiB，任务 0 的需求为 80 GiB，其中 16 GiB 尚未找到存储位置。颜色表示数据所属任务。 |
| 6-28 | `figure-6-pool-after.svg` | 把任务 0 的额外 16 GiB 放在节点 1。节点 1 的绿色 48 GiB 属于任务 1，蓝色 16 GiB 属于任务 0；每个节点都没有超过 64 GiB。 |
| 6-29 | `figure-6-18-read-window.svg` | 一批读取请求发出后，要经过往返时间 L 才能收到结果。最多有 u 个请求在途、每个返回 q 字节时，在一个往返时间内最多返回 uq 字节。图中四个请求仅用于展示过程；正文算例采用 128 个请求，每个 256 字节，往返时间为 2 μs。 |
| 6-30 | `figure-6-19-memory-pool.svg` | 完整读取 16 GiB 的频率与平均带宽需求。路径带宽为 40 GB/s；每事务返回 256 bytes、往返 2 μs 时，128 个在途事务将有效带宽限制到约 16.4 GB/s。 |
| 6-31 | `figure-6-20-session-schedule.svg` | 八个单卡实例中的四个处理会话，另外四个空闲。每个会话执行八步，约 132.4 ms 完成；虚线表示 90 ms 期限。 |
| 6-32 | `figure-6-session-tp2.svg` | 四会话同时推进，每个约 69.4 ms 完成。颜色与相邻配置图中的会话一致。 |
| 6-33 | `figure-6-session-tp4.svg` | 每个实例顺序处理两个会话，分别在约 41.3 ms 和 82.7 ms 完成。 |
| 6-34 | `figure-6-session-tp8.svg` | 单会话缩短到约 34.2 ms，四会话依次执行，最后一个约 137.0 ms 完成。四图横轴使用同一尺度。 |
| 6-35 | `figure-6-21-scale-cost.svg` | 无故障时，按时完成会话的平均成本随期限变化。八卡计费至所有会话结束；曲线从至少三个会话按时完成处开始。 |
| 6-36 | `figure-6-scale-cost-fault.svg` | 20 ms 时卡 0 故障，受影响实例在 60 ms 从当前会话起点重新执行，增加恢复费 1。采用与无故障图相同的计费方法和纵轴范围。 |

## 第 7 章：21 → 48 幅

| 原素材 | 当前图号 | 处置 |
|---|---|---|
| `figure-7-1-boundaries.svg` | 7-1 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-2-cut.svg` | 7-2 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-3-hierarchy.svg` | 7-3 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-4-pipeline.svg` | 7-7 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-5-expert.svg` | 7-8 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-6-relay.svg` | 7-11 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-7-access.svg` | 7-12 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-8-snapshot.svg` | 7-17 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-9-window.svg` | 7-20 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-10-lifetime.svg` | 7-22 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-11-state.svg` | 7-23 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-12-ordering.svg` | 7-25 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-13-stale.svg` | 7-27 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-14-reclaim.svg` | 7-28 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-15-congestion.svg` | 7-30 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-16-feedback.svg` | 7-33 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-17-packets.svg` | 7-34 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-18-deadlock.svg` | 7-37 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-19-progress.svg` | 7-39 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-20-step.svg` | 7-42 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-7-21-message.svg` | 7-48 | 按全书体例重画；复合关系分步见本章新增图 |

### 当前阅读顺序

| 图号 | 素材 | 图注 |
|---|---|---|
| 7-1 | `figure-7-1-boundaries.svg` | 固定两台服务器，每台四张卡。卡的数据先经过本地互联，再由两张网卡跨服务器传输；两张网卡共用的接口在每个方向上提供 40 GB/s。 |
| 7-2 | `figure-7-2-cut.svg` | 固定跨服务器需求下的设备扩展。每方向发送 336 MiB，共享出口为 40 GB/s；计算时间从 20 ms 开始，随设备数增加而按反比缩短。曲线分别按计算与通信串行、完全重叠计算，通信项取割集传输时间。 |
| 7-3 | `figure-7-3-hierarchy.svg` | 连续环将同一服务器内的参与者排在一起。3→4 与 7→0 两条有向边跨服务器。颜色表示参与者所在服务器。 |
| 7-4 | `figure-7-ring-interleaved.svg` | 按 A、B 两服务器交替排列参与者，八条有向边都跨服务器。每参与者的总发送量保持不变。 |
| 7-5 | `figure-7-hierarchy-stages.svg` | 本地先把八份贡献中的本机四份归约成分片，再由两台服务器交换对应分片，最后在本地收集完整结果。每列从上向下执行，横向箭头表示跨服务器交换。 |
| 7-6 | `figure-7-hierarchy-bytes.svg` | 三方案的逻辑发送总量均为 2688 MiB；橙色跨服务器部分分别为 672、2688、384 MiB。两个方向合计。 |
| 7-7 | `figure-7-4-pipeline.svg` | 四个前向阶段如何处理八个微批次。每格为 1 ms，数字表示微批次编号；同一编号沿右下方移动，表示该微批次依次通过各阶段。每个阶段计算 8 ms，在 11 ms 的总时间中各空闲 3 ms。阶段耗时相同，图中忽略额外交接开销。 |
| 7-8 | `figure-7-5-expert.svg` | 将全部 32 MiB 送入一张 25 GB/s 网卡，接收至少需要约 1.34 ms。 |
| 7-9 | `figure-7-expert-path-1.svg` | 四张独立网卡各接收 8 MiB，接收阶段缩短到约 0.34 ms。 |
| 7-10 | `figure-7-expert-path-2.svg` | 32 MiB 都需要先经过 40 GB/s 的共享入口，接收阶段受限于约 0.84 ms。这里各图均只计算接收载荷的传输。 |
| 7-11 | `figure-7-6-relay.svg` | 借用相邻网卡的路径。直接路径最多传输 40 GB/s；两张中继网卡合计 80 GB/s，但必须先经过剩余带宽为 60 GB/s 的本地互联。两路在下游汇合，最后还受外部网络和接收端可用带宽限制。箭头表示传输方向。 |
| 7-12 | `figure-7-7-access.svg` | 主机 RPC 的参数从 CPU A 所在主机出发，经网卡和网络到达 CPU B 所在主机，由远端执行请求。箭头表示参数数据路径，返回结果沿反方向传送。 |
| 7-13 | `figure-7-access-1.svg` | 数据从 GPU 内存直接经过网卡到达远端 GPU 内存；虚线表示 CPU 提交请求，数据载荷无需经过 CPU 内存。映射和访问权限预先建立。 |
| 7-14 | `figure-7-access-2.svg` | GPU 通过 NVLink 访问对端 GPU 内存，由发起设备执行已授权的访问。 |
| 7-15 | `figure-7-access-3.svg` | 设备通过 URMA 提交异步读写，载荷经过 UB 互联到达目标设备；请求完成后按接口定义观察结果。 |
| 7-16 | `figure-7-snapshot-paths.svg` | 每次使用都访问远端，会重复传送同一份数据；下一图先搬回本地，再进行多次复用。两种路径处理同一份不变快照。 |
| 7-17 | `figure-7-8-snapshot.svg` | 每次读取完整 144 MiB 快照时，两种方法的累计耗时。先搬回本地需要一次固定成本，从第二次复用开始节省时间。 |
| 7-18 | `figure-7-snapshot-partial.svg` | 每次远程读取和本地读取都只访问 10%，但搬回时仍复制全部快照；因此需要更多复用才能抵消固定成本。 |
| 7-19 | `figure-7-slot-lifetime.svg` | 一项请求从分配记录开始占用槽位，经历传输与等待，完成状态被处理后归还记录。这里的 2 μs 覆盖完整占用区间。 |
| 7-20 | `figure-7-9-window.svg` | 128 个槽位在约 0.82 μs 内全部用完，要等最早的槽位在 2 μs 释放后继续提交。313 个槽位足以覆盖等待。蓝色表示载荷发送，灰色表示链路空闲。 |
| 7-21 | `figure-7-window-rate.svg` | 蓝线只考虑链路和槽位，橙线再加入每 100 ns 发起一次请求的约束。增加槽位使蓝线升高，橙线仍受 2.56 GB/s 的提交速率限制。 |
| 7-22 | `figure-7-10-lifetime.svg` | 源缓冲占用到 5 μs 发送完成；目的缓冲持续保留到 12 μs 消费者用完。绿色为 8–12 μs 的读取区间。虚线标出 6 μs 提前覆盖目的地址的错误操作。 |
| 7-23 | `figure-7-11-state.svg` | 两个应用端点分别保存身份与绑定记录，关系绑定指向同一目标的共享传输状态。共享部分维护传输进度，端点仍能区分各自请求。 |
| 7-24 | `figure-7-state-capacity.svg` | 64 个端点访问 128 个目标。蓝色为端点记录，橙色为关系绑定，绿色为传输状态。虚线为 1 MiB 容量；八类隔离复制八份目标传输状态。 |
| 7-25 | `figure-7-12-ordering.svg` | 将独立传输也排在发布之后：先写入 20 μs，恢复并使数据可见 80 μs，通知 2 μs，再执行独立传输 10 μs。 |
| 7-26 | `figure-7-ordering-independent.svg` | 写入、恢复、通知仍依次进行；使用独立资源的传输从 0 μs 开始。蓝色为写入，橙色为恢复，绿色为通知，紫色为独立传输。 |
| 7-27 | `figure-7-13-stale.svg` | 按时间依次列出实际读取、数据更新、就绪通知和结果返回。5 μs 返回的仍是 1 μs 读到的旧值；若在 4 μs 发现冲突并重读，6 μs 得到新值。 |
| 7-28 | `figure-7-14-reclaim.svg` | 蓝色表示提交到传输完成，橙色表示等待软件处理完成通知，橙色结束才释放槽位。所有请求的时刻取自事件记录。 |
| 7-29 | `figure-7-reclaim-more.svg` | 增加槽位后，后续请求更早提交。完成通知仍每 20 μs 批量处理四项，最后一个槽位仍在 80 μs 释放。两图时间轴相同。 |
| 7-30 | `figure-7-15-congestion.svg` | 两作业每 100 ms 各发送 20 ms，单作业速率 40 GB/s。不同高峰重叠程度产生不同的总到达速率，共享出口为 50 GB/s。 |
| 7-31 | `figure-7-congestion-queue.svg` | 将到达与服务速率的差额随时间累积，得到 600、150、0 MB 三种峰值。此算例使用无限缓冲计算需求积压。 |
| 7-32 | `figure-7-feedback-loop.svg` | 数据沿实线进入队列并由出口发送；虚线表示拥塞反馈返回发送端。反馈到达并生效前，原发送速率继续填充队列。 |
| 7-33 | `figure-7-16-feedback.svg` | 反馈生效之前，缓冲区还会继续填满。到达速率为 80 GB/s，出口为 50 GB/s，512 KiB 缓冲初始占用一半。8.7 μs 时填满，到 20 μs 降速前持续丢弃超额到达的数据；降至 40 GB/s 后，出口以 10 GB/s 的净速率排空积压。曲线直接取自既有有限缓冲模型，时间与降速幅度为给定条件。 |
| 7-34 | `figure-7-17-packets.svg` | 两路径传播时延均为 1 μs，八报文约在 5.1 μs 全部到齐。圆点表示到达；到达后因缺口等待的区间用横线表示。 |
| 7-35 | `figure-7-packets-1.svg` | 第二条路径传播时延增加到 9 μs。快路径后续报文先到，仍需等待前方缺口；约 13.1 μs 全部可交付。 |
| 7-36 | `figure-7-packets-2.svg` | 序号 0 丢失后按算例等待时间重传，其他七个报文保留在接收端。只补发缺失报文后，约 23 μs 全部可交付。 |
| 7-37 | `figure-7-18-deadlock.svg` | 两项请求分别持有 A、B，各自等待对方持有的资源。箭头表示等待关系，两个请求都无法完成并释放资源。 |
| 7-38 | `figure-7-response-reserve.svg` | 请求、执行、响应使用独立资源并按顺序申请。响应有预留缓冲和发送机会，能够返回并释放原请求。 |
| 7-39 | `figure-7-19-progress.svg` | 原安排中，前三参与者等待第四个在 2 ms 就绪，再交换 0.4 ms。灰色为尚未就绪，橙色为等待其他参与者，蓝色为交换。 |
| 7-40 | `figure-7-progress-1.svg` | 就绪时刻相同，交换从 0.4 ms 减到 0.2 ms，全组从 2.4 ms 提前到 2.2 ms 完成。 |
| 7-41 | `figure-7-progress-2.svg` | 四个参与者在起点同时就绪，交换仍需 0.4 ms，全组在 0.4 ms 完成。三图时间轴相同。 |
| 7-42 | `figure-7-20-step.svg` | 连续环串行安排：计算 20 ms，通信约 8.8 ms，更新 2 ms。灰为计算，蓝为通信，绿为更新；圆点为通信数据就绪。 |
| 7-43 | `figure-7-step-1.svg` | 通信缩短到约 6.6 ms，整步约 28.6 ms。 |
| 7-44 | `figure-7-step-2.svg` | 128 个在途事务限制远端吞吐，通信拉长到约 13.8 ms。 |
| 7-45 | `figure-7-step-3.svg` | 通信与剩余计算同时推进，更新等两者都结束；整步约 25.6 ms。 |
| 7-46 | `figure-7-step-4.svg` | 通信在计算结束前完成，更新从 20 ms 开始，整步 22 ms。 |
| 7-47 | `figure-7-step-5.svg` | 出口减至 20 GB/s，通信超出计算区间，整步约 30.6 ms。上述时间线采用同一横轴范围。 |
| 7-48 | `figure-7-21-message.svg` | 原配置每轮启动 5 μs、带宽 25 GB/s。绿色将带宽增至 75 GB/s，橙色将每轮启动减至 2 μs；小消息主要受启动影响，大消息主要受传输影响。 |

## 第 8 章：15 → 35 幅

| 原素材 | 当前图号 | 处置 |
|---|---|---|
| `figure-8-1-lifecycle.svg` | 8-2 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-8-2-batch.svg` | 8-3 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-8-3-scheduling.svg` | 8-5 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-8-4-attention.svg` | 8-8 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-8-5-pages.svg` | 8-11 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-8-6-prefix.svg` | 8-17 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-8-7-cache-choice.svg` | 8-21 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-8-8-kv-format.svg` | 8-22 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-8-9-offload.svg` | 8-23 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-8-10-kv-quality.svg` | 8-25 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-8-11-verification.svg` | 8-28 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-8-12-speculation.svg` | 8-31 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-8-13-service.svg` | 8-32 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-8-14-design.svg` | 8-34 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-8-15-task.svg` | 8-35 | 按全书体例重画；复合关系分步见本章新增图 |

### 当前阅读顺序

| 图号 | 素材 | 图注 |
|---|---|---|
| 8-1 | `figure-8-request-iterations.svg` | A 跨越两个迭代继续执行。B 在第一轮结束，下一轮由 C 接替其执行位置；请求状态仍按各自身份保存。 |
| 8-2 | `figure-8-1-lifecycle.svg` | 一条输出 256 个 token 的请求。排队 0.1 秒、prefill 0.3 秒，后续 255 个输出间隔各为 10 ms。横条按实际时间比例绘制，点标出首、末 token；3 秒虚线是完成时限。 |
| 8-3 | `figure-8-2-batch.svg` | 权重由批内输出共同分摊，上下文 KV 的读取量决定了曲线最终接近的下限。Qwen3-8B BF16，每份矩阵权重批内读取一次，各请求的上下文彼此独立；实线为权重与 KV 两项之和，虚线为对应上下文的 KV 项，灰线为共享权重项。横轴按倍数取值。 |
| 8-4 | `figure-8-batch-counterfactual.svg` | 每输出 token 分摊的 HBM 读取量。固定 8K 上下文，传统路径为 W/B+K，独立快速 ROM 路径为 K；曲线只表示读取量，不是实测执行时间。 |
| 8-5 | `figure-8-3-scheduling.svg` | 固定批次等待整组结束，再接纳 r2、r3。蓝色为 prefill，绿色为 decode，三角为到达时刻。各色带宽度为整次调度迭代耗时。 |
| 8-6 | `figure-8-scheduling-continuous.svg` | r1 结束后立即接纳 r2。总时间减少，但 r0 的下一次输出等待 r2 的长 prefill，最长间隔增至约 147 μs。 |
| 8-7 | `figure-8-scheduling-chunked.svg` | 每次先安排已有请求 decode，再处理一段新输入。最大间隔降至约 29 μs。三张调度图使用相同的时间尺度。 |
| 8-8 | `figure-8-4-attention.svg` | 首块四个新位置只有块内因果配对，形成 1 + 2 + 3 + 4 = 10 个绿色格。白格表示将来位置，不参与当前查询。 |
| 8-9 | `figure-8-attention-history.svg` | 八个旧位置形成 4 × 8 = 32 个蓝色格，块内仍为 10 个绿色格。块长相同，总配对从 10 增至 42。 |
| 8-10 | `figure-8-page-map.svg` | 逻辑块 0、1、2 按顺序组成序列，块表分别指向物理块 2、0、3。物理块 1 为空闲，注意力按块表恢复逻辑顺序。 |
| 8-11 | `figure-8-5-pages.svg` | 四请求长度为 9、13、5、15，按每条最大 16 个位置预留，共分配 64 个位置。蓝色已用，灰色预留未用。 |
| 8-12 | `figure-8-pages-paged.svg` | 各请求分别分配 12、16、8、16 个位置，共 52 个。灰色尾槽从 22 减到 10。 |
| 8-13 | `figure-8-pages-shared.svg` | A、B 的两个共同前缀块只保存一次，各自块表都指向它们。A、B 保留各自的私有尾块；四请求物理分配总量进一步降至 44 个位置。 |
| 8-14 | `figure-8-reference-release.svg` | A 结束后引用数从 2 降到 1，B 仍可使用；最后一个引用释放且设备已用完，块才能回到空闲池。 |
| 8-15 | `figure-8-copy-on-write.svg` | 四位置尾块已有共同 a、b、c。分支分别追加 x、y，需要不同物理尾块；此前已填满的块继续共享。 |
| 8-16 | `figure-8-cancel-lifetime.svg` | 取消接口返回表示已接收取消请求。已提交的设备操作完成后，再释放私有块和相关引用；观测中的 1.6 ms 与 31 ms 对应不同事件。 |
| 8-17 | `figure-8-6-prefix.svg` | 前四轮代码 Agent 输入的压缩前缀树。根部已有共同 206 个 token，边上是新增数量，叶子是输入轮次。分叉表示后续内容不同。 |
| 8-18 | `figure-8-prefix-lengths.svg` | 蓝色为与上一轮逐 token 相同的前缀，橙色为其余输入。此图描述输入内容的可复用程度，实际缓存命中还取决于状态是否保留。 |
| 8-19 | `figure-8-prefix-restore.svg` | 文本匹配到 10752，最近状态快照在 8192。恢复后仍需重算 2560 个位置，才能得到匹配末端的递推状态。 |
| 8-20 | `figure-8-context-edits.svg` | 同一历史的三种更新方式。蓝色表示可复用前缀，橙色表示需要重新处理的输入；总结方案还要额外执行总结并验证质量。长度以 K token 示意。 |
| 8-21 | `figure-8-7-cache-choice.svg` | 缓存容量均为 864 MiB。A 占 864 MiB，期望净节省 36 ms；三个 B 类前缀各占 288 MiB、各节省 24 ms，合计 72 ms。图中宽度表示容量，时间是按正文假设计算的期望净节省。 |
| 8-22 | `figure-8-8-kv-format.svg` | BF16、q8_0、q4_0 保存同一组 32 个数值所需的空间。横条按字节数成比例绘制；橙色为每组 2 bytes 的缩放系数。把每组总长度乘以组数，就得到整条上下文的 KV 容量。 |
| 8-23 | `figure-8-9-offload.svg` | 卸载九份 FFN 共腾出 2592 MiB。橙色为留在设备上的预取缓冲，绿色为可重新分配的净空间；一组缓冲净省 2304 MiB，两组净省 2016 MiB。 |
| 8-24 | `figure-8-offload-copy.svg` | 每轮复制量保持为 2592 MiB。24 GiB/s 至少需要约 105 ms，384 GiB/s 至少需要约 6.6 ms。 |
| 8-25 | `figure-8-10-kv-quality.svg` | BF16 KV 基线。每行是一道固定任务，四列为并发 1、4 下各两次自然生成。绿色圆圈正确，橙色叉号错误，共 28/32 正确。 |
| 8-26 | `figure-8-kv-quality-1.svg` | 同一任务与运行顺序，原 FP8 实现共 26/32 正确。这里 FP8 为每元素 8 bit 浮点格式，比较包含实现选择对 Q 精度的影响。 |
| 8-27 | `figure-8-kv-quality-2.svg` | 将查询 Q 保持为 BF16，KV 仍用 FP8，共 28/32 正确；失败位置与 BF16 基线不同。三图使用相同任务和列顺序，模型权重均为 BF16。 |
| 8-28 | `figure-8-11-verification.svg` | 贪心验证的教学示意。a、b、c、d、x 表示 token；目标模型在第三个位置选出 x，与草稿 c 不同。第四个位置及其后的验证结果被丢弃，下一轮从 a、b、x 继续生成。方框表示序列位置，不表示执行耗时。 |
| 8-29 | `figure-8-sample-A.svg` | 接受 A 的概率为 1/4，拒绝后输出 B 的概率为 3/4。两条路径合起来给出目标分布。 |
| 8-30 | `figure-8-sample-B.svg` | 接受 B 的概率为 3/4，拒绝后输出 A 的概率为 1/4。目标分布相同，草稿被接受的概率更高。 |
| 8-31 | `figure-8-12-speculation.svg` | 两符号目标分布，每轮生成四个草稿 token。每轮验证 1.4 ms，查询时间沿横轴变化；每个输出 token 的耗时用一轮成本除以平均输出数。普通 decode 为 1 ms/token。没有提前停止，额外 token 计入输出数。点标出查询为 0.1 ms 的算例，BBBB 与普通执行在查询约 1.65 ms 处相交。 |
| 8-32 | `figure-8-13-service.svg` | 每种接纳与到达条件使用三个 16 请求窗口。柱为全部完成请求的吞吐中位数，圆点为各窗口结果。时间从计划到达起计算。 |
| 8-33 | `figure-8-service-goodput.svg` | 分子仅计正确且在 3 秒内完成的请求。两图采用相同横轴顺序和纵轴范围，差额来自错误或迟到的回答。 |
| 8-34 | `figure-8-14-design.svg` | 16 条长请求的教学配置比较，坐标来自正文题设与推导。横轴为 KV 和辅助缓冲区占用，已扣除共同的权重与基本工作区；纵轴为整批请求总耗时。A 的时间是按题设计算的所需时间，其内存不足，不能在本实例上执行。阴影区域同时满足 12 GiB 和 3 秒限制。 |
| 8-35 | `figure-8-15-task.svg` | 其他阶段时间固定、没有新增准备工作的 Amdahl 教学曲线。三条曲线分别取 decode 占基线时间 20%、50%、80%。局部加速的收益随着该阶段原有占比增加。 |

## 第 9 章：17 → 33 幅

| 原素材 | 当前图号 | 处置 |
|---|---|---|
| `figure-9-1-organization.svg` | 9-1 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-9-2-state.svg` | 9-5 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-9-3-pd.svg` | 9-10 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-9-4-allocation.svg` | 9-11 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-9-5-local.svg` | 9-12 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-9-6-reuse.svg` | 9-14 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-9-7-footprint.svg` | 9-15 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-9-8-handoff.svg` | 9-17 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-9-9-balance.svg` | 9-18 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-9-10-experts.svg` | 9-20 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-9-11-overlap.svg` | 9-21 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-9-12-cache.svg` | 9-24 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-9-13-route.svg` | 9-25 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-9-14-migration.svg` | 9-30 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-9-15-recovery.svg` | 9-31 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-9-16-composition.svg` | 9-32 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-9-17-service.svg` | 9-29 | 按全书体例重画；复合关系分步见本章新增图 |

### 当前阅读顺序

| 图号 | 素材 | 图注 |
|---|---|---|
| 9-1 | `figure-9-1-organization.svg` | 八张卡共同执行一个完整模型实例，接收同一批请求。卡号表示协作组成员。 |
| 9-2 | `figure-9-organization-1.svg` | 每个副本都能完成整次推理，可以分别接收独立请求。副本自身也可以由多张卡组成。 |
| 9-3 | `figure-9-organization-2.svg` | P 处理输入，D 继续生成。两个服务池分别调度，上下文 KV 从 P 交给 D。 |
| 9-4 | `figure-9-organization-3.svg` | 注意力与 FFN／专家分别执行，隐状态作为激活在两侧往返。每层都需要这次交接。 |
| 9-5 | `figure-9-2-state.svg` | 计算暂停期间，状态仍需保留。示意同一请求从 prefill、decode 到工具等待和下一轮的状态寿命；横轴按阶段排列，不表示相等时长，EC 仅在有视觉输入时出现。 |
| 9-6 | `figure-9-kv-residency.svg` | 源 P 保留完整 1.125 GiB 状态，目的 D 同时分配 1.125 GiB 接收空间。传输过程共占 2.25 GiB。 |
| 9-7 | `figure-9-kv-publish.svg` | 数据到齐并满足可见性条件后，完成标记允许 D 读取状态。标记指示使用顺序，完整载荷已经写到目的端。 |
| 9-8 | `figure-9-kv-release.svg` | 本算例将执行权交给 D 后释放 P 的源缓冲，D 保留上下文并继续生成。源端空间用于后续请求。 |
| 9-9 | `figure-9-pd-layout.svg` | 每个 A 在 P 阶段提供 2 请求/s，每个 B 在 D 阶段也提供 2 请求/s。两池均为 8 请求/s，低于给定链路约 20.7 请求/s 的交接能力。 |
| 9-10 | `figure-9-3-pd.svg` | 阶段配比如何限制请求率。两类 worker 各四个，能力见例 9.2；横纵轴为分给 P 的数量，其余分给 D。每格取两池能力与 25 GB/s 链路容量的最小值，单位为请求/s，不含排队。 |
| 9-11 | `figure-9-4-allocation.svg` | 请求组成改变资源分配。两类 worker 各四个，处理速度沿用例 9.2；第二行只减少 P 的新输入，第三行恢复完整输入并将输出增至 1025 个 token。方块表示设备数量，每行下方为对应的吞吐率上限。 |
| 9-12 | `figure-9-5-local.svg` | 把一份 36 MiB 专家权重从主存送到 GPU，再由 GPU 读取本地输入完成计算。 |
| 9-13 | `figure-9-local-cpu.svg` | 输入从 GPU 交给 CPU，CPU 使用主存权重执行专家，结果回到 GPU。每次 token 到专家的分派，传入和传出的特征向量合计 16 KiB；并行的 GPU 常驻专家分支完成后再汇合。 |
| 9-14 | `figure-9-6-reuse.svg` | 专家复用改变执行位置的选择。八个专家各有 36 MiB BF16 权重，有效 CPU／GPU 算力为 2／100 TFLOP/s，DRAM／HBM 带宽为 200／1000 GB/s，交接为 25 GB/s、每次启动 5 μs。曲线按例 9.3 计算单层专家路径，未含 NUMA 和格式转换。 |
| 9-15 | `figure-9-7-footprint.svg` | 512 次分派覆盖 128 个专家，每专家四行。矩形宽为专家数，高为每专家行数，面积为分派次数。 |
| 9-16 | `figure-9-footprint-reuse.svg` | 八个专家各处理 64 行，总面积仍为 512。两图横纵轴相同，权重读取却从 4608 MiB 降至 288 MiB。 |
| 9-17 | `figure-9-8-handoff.svg` | 相同总载荷下，消息数放大启动开销。两条曲线总载荷均为 1.125 GiB、有效带宽均为 25 GB/s，分别串行发送 1 次和 72 次；差值为 $71\alpha$，纵轴从48.2 ms起以显示差异。这样保持总字节数不变，只比较消息数量的影响，实际一步 AF 仅传 576 KiB。 |
| 9-18 | `figure-9-9-balance.svg` | 总计 512 次专家分派均分到八张卡，每卡 64 次；虚线表示全部计算结束、可以汇合的时刻。 |
| 9-19 | `figure-9-balance-hotspot.svg` | 512 次全部落在卡 0，其他卡空闲。相同总计算量，需要等待卡 0 完成；两图使用相同时间尺度。 |
| 9-20 | `figure-9-10-experts.svg` | 热点持续多久，专家复制才值得。一次准备约 10.6 ms，每批节省约 0.40 ms；曲线使用例 9.5 未舍入时间计算，第 27 批开始净获益。七张接收卡各需额外 36 MiB，假设容量足够且热点不变。 |
| 9-21 | `figure-9-11-overlap.svg` | 整批依次分派 0.2 ms、计算 0.6 ms、合并 0.2 ms，总时间 1 ms。 |
| 9-22 | `figure-9-overlap-pipeline.svg` | 每微批次各阶段时间减半，三条轨道使用独立资源。第一微批次计算时可以分派第二微批次，总时间降到 0.8 ms。 |
| 9-23 | `figure-9-cache-directory.svg` | 虚线表示按标识查找对象位置。目录用于定位，实际 KV 对象用于恢复计算；路由前还需确认对象版本与可用性。 |
| 9-24 | `figure-9-12-cache.svg` | 读入的页不一定全部成为可复用前缀。正常重启后的这一请求读入 64 个 16-token 页，只复用前 63 页；末页仍需处理。每页 2.25 MiB，该请求的匹配边界为 1008 个位置。 |
| 9-25 | `figure-9-13-route.svg` | A 本地缓存已命中，GPU 排队 250 ms 后再计算 10 ms，首 token 在 260 ms 返回。灰为排队，绿为计算。 |
| 9-26 | `figure-9-route-1.svg` | B 在 20 ms 空闲，随后重算 180 ms，首 token 在 200 ms 返回。 |
| 9-27 | `figure-9-route-2.svg` | 取回先查找 10 ms，再按 5 GB/s 读取 1.125 GiB 至主存，最后按 25 GB/s 搬到 GPU。计算等待数据和 GPU 都就绪，首 token 约 310 ms 返回。 |
| 9-28 | `figure-9-route-3.svg` | 远端带宽提高到 20 GB/s 后，首 token 约 129 ms 返回。橙为远端读取，蓝为主存到 GPU；四图均从请求到达起计时，横轴相同。 |
| 9-29 | `figure-9-17-service.svg` | 服务余量决定启动积压的消退速度。连续流量模型，每秒到达四请求，启动 10 秒后积压 40 个。就绪后服务率为八或五请求/s，净排空率分别为四或一请求/s，从开始启动算起在第 20 或 50 秒排空。例 9.7 的排空期限为第 25 秒。 |
| 9-30 | `figure-9-14-migration.svg` | 后台复制需要赶上仍在增长的状态。开始时待复制状态为 1 GiB，源端以 0.5 GiB/s 生成新状态，目标以 2 GiB/s 复制；两条曲线相交前，垂直距离就是尚未复制的数据量。相交后，目标只需跟随源端的新增状态。 |
| 9-31 | `figure-9-15-recovery.svg` | 输出记录决定继续哪条序列，KV 检查点决定从哪里补算。假定 129 个输出均已可靠记录，但 KV 只保存了原输入。下方按输入、前 128 个输出和第 129 个输出分段示意，宽度不按 token 数量比例绘制。 |
| 9-32 | `figure-9-16-composition.svg` | P 直接向 D 交接 1.125 GiB 上下文 KV 缓存，只经过一条数据边。 |
| 9-33 | `figure-9-composition-pool.svg` | P 先向池写入完整 1.125 GiB 并发布，D 再取回同一对象，共经过两条边。后续实例还可以复用池中对象。 |

## 第 10 章：20 → 41 幅

| 原素材 | 当前图号 | 处置 |
|---|---|---|
| `figure-10-1-state.svg` | 10-2 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-2-budget.svg` | 10-3 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-3-sharding.svg` | 10-10 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-4-recompute.svg` | 10-12 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-5-casting.svg` | 10-14 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-6-candidates.svg` | 10-16 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-7-pipeline.svg` | 10-17 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-8-overlap.svg` | 10-21 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-9-attention-area.svg` | 10-23 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-10-input-queue.svg` | 10-25 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-11-resharding.svg` | 10-26 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-12-recovery.svg` | 10-28 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-13-save-interval.svg` | 10-30 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-14-rl-flow.svg` | 10-31 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-15-rl.svg` | 10-32 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-16-async-cycle.svg` | 10-36 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-17-replay.svg` | 10-38 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-18-deadline.svg` | 10-39 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-19-hardware.svg` | 10-40 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-10-20-scale.svg` | 10-41 | 按全书体例重画；复合关系分步见本章新增图 |

### 当前阅读顺序

| 图号 | 素材 | 图注 |
|---|---|---|
| 10-1 | `figure-10-update-cycle.svg` | 从输入和当前权重出发，前向得到损失，反向得到参数梯度，Adam 生成新权重。前向保存的激活用于反向；更新过程还读取高精度主权重和梯度历史。 |
| 10-2 | `figure-10-1-state.svg` | 五类训练状态如何形成相当于八份 BF16 权重的容量。模型为 Qwen3-8B，采用表中数据格式；横轴表示各类状态累计占用的容量。 |
| 10-3 | `figure-10-2-budget.svg` | 矩阵计算效率为 30% 时的必要设备数。任务为 Qwen3-8B 处理 100B token，序列长 8192，全部 30 天用于执行；蓝柱为计算需求，橙色菱形为训练状态容量下限。A100 为 80 GB SXM，H100 为 SXM。 |
| 10-4 | `figure-10-budget-1.svg` | 矩阵计算效率提高到 40%，同一任务所需设备数下降；橙色菱形仍表示状态容量下限。 |
| 10-5 | `figure-10-budget-2.svg` | 矩阵计算效率为 50% 时的设备数。三图使用相同的设备顺序、纵轴范围、任务与 30 天执行期限。 |
| 10-6 | `figure-10-zero-0.svg` | 普通数据并行（DP）：每卡保存完整权重、梯度、主权重和两份矩状态。 |
| 10-7 | `figure-10-zero-1.svg` | ZeRO-1：主权重和两份矩状态按参数分片，权重与梯度仍完整复制。 |
| 10-8 | `figure-10-zero-2.svg` | ZeRO-2：进一步划分梯度，每卡只保留归属于自己的梯度分片。 |
| 10-9 | `figure-10-zero-3.svg` | ZeRO-3：权重也分片。执行模块时，各卡通过通信取得所需的完整权重。 |
| 10-10 | `figure-10-3-sharding.svg` | 同一模块的权重平时分散在四张卡上；执行时，各卡收集完整权重。图中展开 GPU 0 的收集过程，四种颜色分别表示四份参数分片。完整权重缓冲区用完即可释放，各卡长期保存的原始分片仍然保留。 |
| 10-11 | `figure-10-gradient-branches.svg` | 矩阵反向产生两路梯度：dX 交给前一层继续反向，dW 交给本层的归约和参数更新。两路计算都读取上游梯度 dY。 |
| 10-12 | `figure-10-4-recompute.svg` | 直接保存 h：蓝色条表示一直保留的 a、u，橙色条表示从前向持续到下投影反向的乘积 h。128-token 微批次采用 FP32，h 的形状为 [128,12288]，占 6 MiB。 |
| 10-13 | `figure-10-recompute-rebuild.svg` | 保留 a、u，反向使用前重新相乘得到 h。橙色的 6 MiB 缓冲只在使用前后短暂存在；横轴表示操作顺序。 |
| 10-14 | `figure-10-5-casting.svg` | 先将 96 MiB BF16 梯度传到 CPU，再在 CPU 转为 192 MiB FP32。箭头表示数据流；转换的输入与输出均位于 CPU 一侧。 |
| 10-15 | `figure-10-casting-gpu.svg` | 先在 GPU 将 96 MiB BF16 梯度转为 192 MiB FP32，再传到 CPU。转换时输入与输出共存，GPU 峰值为 288 MiB。 |
| 10-16 | `figure-10-6-candidates.svg` | 给定附加存储需求时，分片数决定容量可行区域。曲线为 $16N/d+10$ GiB，水平线为 24 GiB 教学算例中的可用显存。曲线低于预算的区域可以容纳这些训练状态和缓冲区；纵向距离给出容量余量。 |
| 10-17 | `figure-10-7-pipeline.svg` | 填满排空先完成八个微批次的前向，再执行反向，最后更新参数。蓝色为前向，橙色为反向，绿色为参数更新。四个阶段共用同一时间刻度，按正文的计算与传输条件共需 337 ms。 |
| 10-18 | `figure-10-pipeline-1f1b.svg` | 1F1B 在预热后交错前向和反向，本例完成时间为 347 ms。蓝色为前向，橙色为反向，绿色为参数更新；与前图使用相同时间刻度。 |
| 10-19 | `figure-10-pipeline-gap.svg` | 放大 1F1B 的阶段 3：第二个微批次在 93 ms 结束反向，下一份前向输入于 95 ms 到达，形成 2 ms 等待。 |
| 10-20 | `figure-10-pipeline-memory.svg` | 两种调度中各阶段的激活与收发缓冲峰值。提前反向使激活更早释放，最大值由约 2.57 GB 降到 0.97 GB。 |
| 10-21 | `figure-10-8-overlap.svg` | 链路空闲时，3 ms 归约（橙色）可以在 5 ms 独立计算（蓝色）结束前完成。虚线标出计算结束。 |
| 10-22 | `figure-10-overlap-busy.svg` | 链路先被其他通信占用 4 ms（灰色），归约推迟到 4—7 ms。虚线后多出的 2 ms 延长训练步骤。 |
| 10-23 | `figure-10-9-attention-area.svg` | 两条 4096-token 序列的因果注意力配对。查询位置读取本序列中不晚于自己的位置，形成两个三角形；两序列互不注意，总配对数为 16781312。 |
| 10-24 | `figure-10-attention-unequal.svg` | 相同 8192 个 token 改分为 7168 与 1024，总因果配对数增至 26218496。长三角形的面积增长超过短三角形的面积减少。 |
| 10-25 | `figure-10-10-input-queue.svg` | 训练已完成批次 100，预取任务已安排到 108。中间八个批次仍需训练，其中一部分已经准备好，另一部分还在处理。虚线框示意尚在处理的批次。恢复时应保留这些数据或重新准备它们，从批次 101 继续。 |
| 10-26 | `figure-10-11-resharding.svg` | 横向位置对应原矩阵的行号，颜色表示旧分片。每份旧分片分成前后两半后，分别装入两个新分片。模型权重的内容和顺序保持相同，改变的是各设备负责的行范围；BF16 权重总量始终为 96 MiB。 |
| 10-27 | `figure-10-checkpoint-commit.svg` | 捕获一致的训练状态，复制到独立缓冲后允许训练继续；后台写完数据并提交完整快照后，恢复程序才使用这份检查点。箭头表示先后依赖。 |
| 10-28 | `figure-10-12-recovery.svg` | 两份 112 GB 快照以 8 GB/s 写入，各先花 0.5 s 复制到缓冲（橙色），随后上传（蓝色）。50 s 故障时第一份已提交，第二份尚未提交；斜线为无故障时剩余上传，空心点为原定提交时刻。 |
| 10-29 | `figure-10-recovery-fast.svg` | 相同快照以 16 GB/s 写入，于 27.5、47.5 s 提交。50 s 故障时可以恢复到 40 s 的训练状态，只需重做 10 s。 |
| 10-30 | `figure-10-13-save-interval.svg` | 蓝线为保存耗时占比，橙线为故障重做耗时占比，绿线为两者加上恢复耗时后的合计。使用 1024 卡、单卡平均故障间隔（MTBF）365 天、保存约 14.3 s 和恢复 120 s 的一阶模型。最低点出现在两项随间隔变化的代价相互平衡处。 |
| 10-31 | `figure-10-14-rl-flow.svg` | 三阶段分别最多处理 12、6、8 条等长轨迹/s。验证后保留 75%，所以只有 4.5 条/s 进入学习。返回箭头表示学习产生的新权重影响后续生成；其同步耗时在后面的阶段切换与异步算例中展开。 |
| 10-32 | `figure-10-15-rl.svg` | 先装载生成权重和 KV 池，再释放训练状态，峰值约为 83.3 GiB，超过 64 GiB 预算。横轴按操作顺序排列。 |
| 10-33 | `figure-10-rl-staged.svg` | 先装载生成权重，再释放训练状态，最后分配 KV 池。峰值降为约 59.3 GiB；两图共用 64 GiB 预算线和同一纵轴。 |
| 10-34 | `figure-10-weight-update.svg` | 固定版本服务与策略训练的权重生命周期。上方 ROM 重复提供同一版本；下方训练产生新版本并发布给生成端。可写 KV 不能替代权重更新通路。 |
| 10-35 | `figure-10-policy-versions.svg` | μ 产生训练样本，πold 标识本轮优化的起点，πθ 随本轮更新变化。虚线表示版本演进顺序。三个概率必须针对同一前缀与同一 token 计算。 |
| 10-36 | `figure-10-16-async-cycle.svg` | 同步循环依次生成本批样本、学习本批样本、同步权重，分别用时 40、16、4 s，共 60 s。 |
| 10-37 | `figure-10-async-overlap.svg` | 稳态中生成下一批与学习上一批在独立资源上重叠；两者完成后同步权重，周期为 44 s。橙色同步阶段阻塞两侧。 |
| 10-38 | `figure-10-17-replay.svg` | 离散专家 ID 连接生成端与训练端，当前权重继续参与数值计算。示意记录为样本 A、token 17、层 3 的 top-2 选择；虚线表示 ID 重放，实线表示当前计算数据流。 |
| 10-39 | `figure-10-18-deadline.svg` | 每条横条依次累计基础训练时间、保存与故障恢复的附加时间，以及预留的 5 天计划性停顿。基础训练时间已经包括通信和输入等待；橙色小段为检查点模型得到的额外耗时。两套方案使用相同任务和全局批次，虚线标出 30 天期限。 |
| 10-40 | `figure-10-19-hardware.svg` | 资源能力变化的收益由原有等待时间占比决定。曲线按 $T'/T=1-f+f/r$ 计算，固定单卡计算与依赖，通信时间随有效能力成反比。 |
| 10-41 | `figure-10-20-scale.svg` | 固定 90 天执行期限，Dense 规模增大要求更多计算设备。数据量为 20T token，算法工作为 $6ND$，效率为 50%，使用 BF16 dense 峰值。各线为向上取整前的连续计算边界；横线标出 16,384 张卡。 |

## 第 11 章：19 → 39 幅

| 原素材 | 当前图号 | 处置 |
|---|---|---|
| `figure-11-1-timeline.svg` | 11-1 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-capacity.svg` | 11-3 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-2-boundary.svg` | 11-5 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-pages.svg` | 11-10 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-pause.svg` | 11-11 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-residency.svg` | 11-13 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-3-lifecycle.svg` | 11-15 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-4-placement.svg` | 11-20 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-queue.svg` | 11-22 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-rl-stages.svg` | 11-24 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-weights.svg` | 11-25 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-5-stages.svg` | 11-26 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-thinking.svg` | 11-29 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-6-service.svg` | 11-30 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-7-routing.svg` | 11-31 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-purchase.svg` | 11-33 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-retry-tree.svg` | 11-35 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-8-retry.svg` | 11-37 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-11-decision.svg` | 11-39 | 按全书体例重画；复合关系分步见本章新增图 |

### 当前阅读顺序

| 图号 | 素材 | 图注 |
|---|---|---|
| 11-1 | `figure-11-1-timeline.svg` | 三轮串行任务共需 30 s。蓝色为三次各 9 s 的模型调用，橙色为三次各 1 s 的工具执行；工具累计消耗 3 CPU·秒。 |
| 11-2 | `figure-11-memory-area.svg` | 同一任务的环境始终占 2 GiB，持续 30 s，矩形面积为 60 GiB·秒。等待模型时仍保留环境，因而内存占用持续时间长于工具的 CPU 执行时间。 |
| 11-3 | `figure-11-capacity.svg` | 每秒 10 项任务、每次模型调用 9 s 时，三种资源的需求占容量比例。柱末给出需求量／可用容量；虚线为 100%。 |
| 11-4 | `figure-11-capacity-slow.svg` | 每次模型调用由 9 s 延长到 12 s，CPU 需求仍为 30 核；并发调用增至 360，环境内存增至 780 GiB。虚线为 100% 容量。 |
| 11-5 | `figure-11-2-boundary.svg` | 代码任务经过模型服务与工具环境两条路径。控制器从模型取得参数，再向环境平台发起工具操作；执行结果成为下一轮输入。虚线框标出工具环境所在的执行节点。 |
| 11-6 | `figure-11-isolation-0.svg` | 进程拥有独立地址空间，通过权限限制访问；同一节点上的进程共享宿主内核。 |
| 11-7 | `figure-11-isolation-1.svg` | 容器为任务建立各自的资源视图和配额，底层仍使用同一宿主内核。 |
| 11-8 | `figure-11-isolation-2.svg` | microVM 让每个任务环境运行独立客体内核，通过虚拟硬件访问宿主资源。 |
| 11-9 | `figure-11-template-runtime.svg` | 模板保存共享的文件与依赖，私有内容保存环境修改；程序运行时还会占用进程内存和缓冲区。模板安装量与活跃环境内存分别核算。 |
| 11-10 | `figure-11-pages.svg` | 四种方案采用相同的横轴尺度，比较每个环境本地保存的数据。另有一份共享的 2 GiB 模板，不计入各环境的柱长。数值来自例 11-2；管理开销为每环境 4 MiB。 |
| 11-11 | `figure-11-pause.svg` | 第 10—19 s 等待模型时始终保留 2 GiB 环境，占用 18 GiB·秒。横轴与下一图一致。 |
| 11-12 | `figure-11-pause-release.svg` | 第 10—11 s 保存状态，第 18—19 s 恢复，中间释放 7 s 内存。两个有色区间共占 4 GiB·秒，下一次工具仍在第 19 s 开始。 |
| 11-13 | `figure-11-residency.svg` | 环境从任务开始保留到结束。模型每轮 9 s、工具每轮 1 s，三轮累计占用 60 GiB·秒。与下一图使用相同横轴。 |
| 11-14 | `figure-11-residency-rebuild.svg` | 每轮只在准备 2 s 和执行 1 s 期间保留环境，三次合计 18 GiB·秒。工作目录和结果已持久化，三轮仍在第 30 s 完成。 |
| 11-15 | `figure-11-3-lifecycle.svg` | 按需创建：第 4 s 调用到达才开始准备，第 6 s 就绪，等待 2 s。蓝色为准备，虚线为调用时刻；以下四图使用相同横轴。 |
| 11-16 | `figure-11-prewarm-1.svg` | 预测正确且提前 1 s：从第 3 s 准备到第 5 s，调用在第 4 s 到达后仍需等 1 s。 |
| 11-17 | `figure-11-prewarm-2.svg` | 预测正确且提前 2 s：准备恰好在第 4 s 调用到达时完成。 |
| 11-18 | `figure-11-prewarm-3.svg` | 预测正确且提前 3 s：准备在第 3 s 完成，橙色部分为等待调用的 1 s 就绪空闲。 |
| 11-19 | `figure-11-prewarm-wrong.svg` | 预测错误：第 2—4 s 准备了不需要的环境（灰色）；第 4 s 取消后，再花 2 s 准备实际所需环境。此图采用立即取消、无资源争用的题设。 |
| 11-20 | `figure-11-4-placement.svg` | 整理前：新作业需要同节点四张 A 型 GPU 与 16 核。节点 1 缺 CPU，节点 2 的 GPU 类型不符。 |
| 11-21 | `figure-11-placement-after.svg` | 将占用 8 核的 CPU 任务从节点 1 迁到节点 2 后，节点 1 同时拥有四张 A 卡和 16 核，可以启动新作业。 |
| 11-22 | `figure-11-queue.svg` | 十次调用每隔 1 s 到达，一个执行进程每次处理 1 s。圆点为到达，蓝色为执行，每次均可立即开始。 |
| 11-23 | `figure-11-queue-burst.svg` | 十次调用同时在第 0 s 到达；灰色为等待、蓝色为执行，各行的等待依次为 0—9 s。处理器仍只工作 10 s，平均排队为 4.5 s。 |
| 11-24 | `figure-11-rl-stages.svg` | 四个阶段依次执行。生成时间由 40 秒降至 20 秒，其余三段仍共需 45 秒，因此每轮总时间由 85 秒降至 65 秒。条形长度按时间比例绘制。 |
| 11-25 | `figure-11-weights.svg` | 每个接收实例都需要一份完整的 30 GB 权重，六路传输共用 200 Gbit/s 发送出口。每个接收端为 50 Gbit/s，单份传输至少 4.8 秒，而发送端累计发送 180 GB 至少需要 7.2 秒；图中六个接收框各代表一个独立实例。 |
| 11-26 | `figure-11-5-stages.svg` | 三条样本分别在第 0、10、20 s 到达，立即提交给唯一的验证进程，每条处理 10 s，整批在第 30 s 完成。圆点标到达时刻。 |
| 11-27 | `figure-11-stages-batch.svg` | 先等三条样本全部在第 20 s 就绪，再依次验证，结束于第 50 s。与前图执行相同的 30 s 验证工作，区别来自提交时机。 |
| 11-28 | `figure-11-remaining-time.svg` | 九条验证各用 1 s，一条用 100 s。虚线是已经执行的 10 s；到此时仍未结束的样本只可能来自 100 s 那一类，剩余时间为 90 s。 |
| 11-29 | `figure-11-thinking.svg` | 两次调用的输入和可见输出相同，只改变思考长度。橙色成本缩短到原来的十分之一，蓝色和绿色成本保持不变，所以总成本由 0.032 降到 0.023，减少约 28%。价格和 token 数见例 11-5。 |
| 11-30 | `figure-11-6-service.svg` | 同一任务的模型服务与成本路径。外部应用程序接口（API）按调用用量收费，自建副本按设备预留及运行支出计价；两条路径都将成本归到任务及尝试。输入拆分为普通处理、缓存创建和缓存读取，生成单独计量。 |
| 11-31 | `figure-11-7-routing.svg` | B 的成功任务成本随命中率提高而下降，在约 84.9% 处等于 A。两者的成本先按各自成功数归一化；本图只比较成本，下一图单独加入期限。 |
| 11-32 | `figure-11-routing-deadline.svg` | 只有 B 命中的 4 s 路径满足 6 s 期限；再乘 98% 质量概率，按时成功比例为 0.98h。达到 90% 目标要求 h 至少约 91.8%。 |
| 11-33 | `figure-11-purchase.svg` | 自建总成本为 1,000＋0.002N，API 为 0.012N，其中 N 为提交任务数。两者成功率相同，并有足够能力满足期限。自建曲线起点较高、斜率较小，在 100,000 项时与 API 相交。 |
| 11-34 | `figure-11-commit-ack.svg` | 外部系统已执行并提交操作 K，确认却丢失。恢复环境不会撤回外部效果；控制器按同一操作 ID 查询结果后接续任务。实线表示请求与执行，虚线表示确认及恢复查询。 |
| 11-35 | `figure-11-retry-tree.svg` | 首次尝试在 10 s 后分为成功、局部修复、直接升级三类。框内比例以全部提交为分母；下一图展开修复的条件分支。 |
| 11-36 | `figure-11-retry-conditional.svg` | 把修复节点放大：进入此处的 12% 中，60% 修复成功，占全部提交 7.2%；40% 转入升级，占全部提交 4.8%。升级再用 8 s，因此后一条路径累计 22 s。 |
| 11-37 | `figure-11-8-retry.svg` | 同一批提交任务换用恢复策略后的成本与结果。各柱均以相应策略的全部支出为分子，再分别除以通过测试的任务数或按时通过测试的任务数；只做首次尝试的成功率为 80%，有限恢复策略约为 99.7%，其中约 95.0% 的提交在 20 秒内成功。分支概率、成本和时间见例 11-8，期限用于评价而不强制停止执行。 |
| 11-38 | `figure-11-environment-overlap.svg` | 模型加速使原本可隐藏的环境创建暴露出来。两种情况都在零时刻开始创建环境；工具须同时等待模型决策与环境就绪。横轴为秒。 |
| 11-39 | `figure-11-decision.svg` | 两种方案均在模型调用期间准备环境，工具执行均为每轮 1 秒。模型调用从每轮 9 秒降至 6 秒，三轮结束时刻从第 30 秒移到第 21 秒，越过 24 秒期限线。 |

## 第 12 章：16 → 32 幅

| 原素材 | 当前图号 | 处置 |
|---|---|---|
| `figure-12-1-raw.svg` | 12-2 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-12-2-overlap.svg` | 12-3 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-12-3-paths.svg` | 12-6 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-12-4-buffer.svg` | 12-7 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-12-5-agent.svg` | 12-8 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-12-6-placement.svg` | 12-11 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-12-7-migration.svg` | 12-13 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-12-8-sync.svg` | 12-14 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-12-9-window.svg` | 12-16 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-12-10-transport.svg` | 12-17 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-12-11-wireless.svg` | 12-19 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-12-12-multipath.svg` | 12-22 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-12-13-queqiao.svg` | 12-23 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-12-14-budgets.svg` | 12-26 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-12-15-deployment.svg` | 12-29 | 按全书体例重画；复合关系分步见本章新增图 |
| `figure-12-16-recovery.svg` | 12-30 | 按全书体例重画；复合关系分步见本章新增图 |

### 当前阅读顺序

| 图号 | 素材 | 图注 |
|---|---|---|
| 12-1 | `figure-12-image-path.svg` | 原图经上行到服务器，服务器收齐后处理，再经下行返回完整成片。实线箭头表示数据与处理顺序；连接已建立，双向传播合计 0.1 s。 |
| 12-2 | `figure-12-1-raw.svg` | 压缩和计算加速对任务完成时间的影响随上行速率而变化。本例采用原图 30 MB、成片 5 MB、下行 100 Mbit/s、RTT 0.1 秒，原处理时间 0.3 秒；压缩方案将输入减半并增加 0.15 秒编解码。三种方案的成片质量相同，连接已建立且各阶段串行。 |
| 12-3 | `figure-12-2-overlap.svg` | 整图串行：30 MB 全部上传并传播到服务器后才处理，随后回传 5 MB。上行 20 Mbit/s、下行 100 Mbit/s、单向传播 0.05 s，任务于 12.8 s 完成。 |
| 12-4 | `figure-12-overlap-chunks.svg` | 三块各上传 4 s，每块到达后独立处理 0.1 s，随后回传 1、2、2 MB。前两块的处理和回传与后续上传重叠，完整成片于 12.36 s 返回。 |
| 12-5 | `figure-12-audio-clocks.svg` | 第一块音频从采集开始计时：20 ms 就绪，32 ms 处理完，33 ms 发完，38 ms 到达；初始缓冲 40 ms 后于 78 ms 播放。圆点标出到达，最后一行是播放设备的时钟。 |
| 12-6 | `figure-12-3-paths.svg` | 第三块晚到使后续各块的播放时间都推迟 5 ms。每块长 20 ms，模型处理 12 ms、发送 1 ms；通常传播 5 ms，第三块传播 50 ms，初始缓冲 40 ms。实线段表示实际播放，浅色轮廓表示原定播放区间，圆点表示到达；所有时间均从开始采集第一块起算。图为上述教学流水的计算结果，播放器等待缺块而不丢弃。 |
| 12-7 | `figure-12-4-buffer.svg` | 缓冲量等于初始数据量加累计接收量，再减累计播放量。PCM 播放需要 256 kbit/s，接收只有 130 kbit/s，两条直线的下降速率均为 126 kbit/s。初始缓冲分别包含 60 ms 和 120 ms 音频，约在 0.12 秒和 0.24 秒耗尽；图画到各自第一次耗尽为止。 |
| 12-8 | `figure-12-5-agent.svg` | 截图 Agent 的一轮执行。箭头表示先后依赖，方框宽度不表示耗时。底部返回箭头要经过操作执行与界面更新，才能取得下一轮截图。 |
| 12-9 | `figure-12-encoder-remote.svg` | 远端编码：先发送压缩图片，在服务器执行视觉编码，再执行语言模型。 |
| 12-10 | `figure-12-encoder-local.svg` | 端侧编码：先执行视觉编码，再发送完整数值特征到语言模型。发送对象随计算的放置位置改变。 |
| 12-11 | `figure-12-6-placement.svg` | 同一图片的完整视觉特征比压缩图片需要更多传输时间。压缩图片为 0.8 MB；Qwen3-VL-4B 在预处理后 640×640 输入上产生 400 个视觉位置，最终投影及三组 DeepStack 合计 [400,10240] BF16，共 8,192,000 bytes。上行速率为 6.4 Mbit/s，柱长表示发送时间；编码、排队和转换时间另计。两种方案处理同一张图片，完成相同的视觉理解任务。 |
| 12-12 | `figure-12-cache-restart.svg` | 三种保存对象对应三个重算起点。EC 省去视觉编码；匹配模型权重、前缀与位置的 KV 进一步省去已处理前缀的语言计算。容量对应正文固定视觉配置。 |
| 12-13 | `figure-12-7-migration.svg` | 迁移 64 MiB 状态，链路 80 Mbit/s，迁移后每轮节省 0.4 秒。两条曲线分别取恢复时间 1 秒和 2 秒，净节省为 N×0.4 减去传输和恢复时间。圆点标出首次获益的整数轮数；零线以上表示迁移更快，首次获益分别在第 20、22 轮。 |
| 12-14 | `figure-12-8-sync.svg` | 张量并行的一次注意力输出归约：两卡先分别计算，再交换并求和，取得完整输出后进入前馈。横向箭头表示执行顺序，中间纵向箭头表示两卡通信。 |
| 12-15 | `figure-12-sync-ffn.svg` | 前馈网络也分别计算局部输出，再做两卡归约。完整前馈结果就绪后才能进入下一层，这一结构在 36 层中重复。 |
| 12-16 | `figure-12-9-window.svg` | 固定窗口 64 KB，链路 20 Mbit/s，一批数据发送需 25.6 ms。为单独显示停等，图设接收端收齐一批后统一确认，从这批数据全部发出到收到整批确认再需 100 ms；每批完整周期为 125.6 ms。正文 W/R 是吞吐上界，图中的整批确认还增加了发送时间，因此实际周期更长。 |
| 12-17 | `figure-12-10-transport.svg` | 整体有序交付：音频第 3 s 已收齐，却因图片缺口继续等到第 7 s。灰色段表示数据已齐后的等待，圆点表示数据交给应用的时刻。 |
| 12-18 | `figure-12-transport-per-stream.svg` | 逐流有序交付：音频在第 3 s 收齐后立即交付，图片仍于第 7 s 交付。两图具有相同的发送、到达与恢复时刻；改变的是跨流等待依赖。 |
| 12-19 | `figure-12-11-wireless.svg` | 报文变短后，固定交换开销仍然存在，因此空口占用时间不会同比缩短。两种成功交换采用相同的 34 μs 接入等待、16 μs SIFS、44 μs MAC ACK，区别只在数据 PPDU：208 μs 或 40 μs。条件为 OFDM54／6、无聚合、无重传的教学参考模型。 |
| 12-20 | `figure-12-cancel-paths.svg` | 一次取消同时触发两条路径：本地清空待播音频，远端在控制消息到达后停止生成和发送。虚线表示控制流；本地停止播放与远端资源释放有各自的完成时刻。 |
| 12-21 | `figure-12-multipath-independent.svg` | 两条独立路径按带宽比例分配输入，分别发送 20 MB 和 10 MB，均需 8 s。任务等两部分都齐备后完成。 |
| 12-22 | `figure-12-12-multipath.svg` | 30 MB 图片按 20／10 MB 分到 20／10 Mbit/s 两条独立接入，各需 8 秒。两路再经过同一 24 Mbit/s 出口，全部数据通过该出口至少需 10 秒。箭头表示数据路径，不表示传播距离；容量取恒定值。 |
| 12-23 | `figure-12-13-queqiao.svg` | 新建连接时，354640-byte 固定音频的直接路径与 Queqiao 请求中位数分别为 1185.3、301.6 ms。计时从发起请求到收齐结果，输入文件相同，两条路径交替运行。 |
| 12-24 | `figure-12-queqiao-tuned.svg` | 保持连接并调优后，同一固定音频的直接路径与 Queqiao 中位数分别为 240.9、236.5 ms。与前图采用相同纵轴，两条路径均接近数据发送、必要往返和处理的总预算。 |
| 12-25 | `figure-12-experiment-design.svg` | 后续单因素实验的设计：保持相同文件、路径和其他设置，每次只改变一个因素，配对记录中间事件与总完成时间。此图列出实验步骤。 |
| 12-26 | `figure-12-14-budgets.svg` | 端侧：终端工作 6 s，模型计算 58 s，共 64 s。三图共用 45 s 期限线和同一横轴，颜色分别汇总二十轮同类工作的耗时。 |
| 12-27 | `figure-12-budgets-1.svg` | 附近工作站：准备 3 s，二十轮终端 6 s、模型 30 s、传播 0.4 s、上传 1.6 s，共 41 s，满足 45 s 期限。 |
| 12-28 | `figure-12-budgets-2.svg` | 云端：准备 1 s，二十轮终端 6 s、模型 16 s、传播 4 s、上传 20 s，共 47 s。计算更快而传输更久，总时间超过期限。 |
| 12-29 | `figure-12-15-deployment.svg` | 云路径上行改变完整任务的部署选择。剩余 20 轮每轮上传 0.8 MB，云端准备 1 秒，每轮其余工作合计 1.3 秒，因此总时间为 27+128/b 秒；附近工作站为 41 秒，期限为 45 秒。本例保持模型质量、处理时间、价格和其他网络参数不变，忽略排队与故障。约 7.1 Mbit/s 起云方案可满足期限，约 9.1 Mbit/s 以上才比附近工作站更快。 |
| 12-30 | `figure-12-16-recovery.svg` | 保留前九轮提交记录（绿色），只重做未确认的第十轮（橙色）。每轮 1.9 s，恢复连接 1 s，增加 2.9 s，总时间为 43.9 s。此例采用可安全重放的操作；已提交的外部操作则先查询其结果。 |
| 12-31 | `figure-12-recovery-all.svg` | 丢失十轮进度时，恢复连接后重做十轮，额外 1＋10×1.9＝20 s，总任务从 41 s 延至 61 s。 |
| 12-32 | `figure-12-task-counterfactual.svg` | 固定任务轨迹中的加速上限。模型从 8 秒缩短至 0.8 秒，其他串行阶段保持 2 秒；第三行表示模型时间趋近于零的理想下界。 |
