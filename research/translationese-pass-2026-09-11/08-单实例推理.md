# 第 8 章（推理优化）翻译腔清扫：修改对照

章节文件：`manuscripts/08-单实例推理.md`。共 12 处改动，全部为句式理顺，未改变任何技术内容、数字、公式、单位；代码块、数学式、图片路径、图注编号、脚注定义均未动。省略号表示只截取了改动附近的文字。

| 修改前 | 修改后 |
|---|---|
| 同一个模型使用相同的加速器，单个用户每秒可能只收到几十个 token，所有用户合计却能每秒收到上千个 token。 | 同一个模型跑在同一台加速器上，单个用户每秒可能只收到几十个 token，所有用户合计每秒却能收到上千个 token。 |
| …每轮 token 上限从 512 增至 8192，一个后到请求的引擎排队时间从约 149 ms 降到不足 0.1 ms… | …每轮 token 上限从 512 增至 8192，一个后到请求在引擎中排队的时间从约 149 ms 降到不足 0.1 ms… |
| …又不能影响 A。实现共享因此需要确定何时释放共享块，以及如何避免不同请求互相覆盖数据。 | …又不能影响 A。要实现共享，就需要确定何时释放共享块，以及如何避免不同请求互相覆盖数据。 |
| 短窗口重放得到的是近似恢复的状态，其误差来自截断了更早的局部依赖。 | 短窗口重放得到的是近似恢复的状态，误差来自更早的局部依赖被截断。 |
| …按实验 8-1 中 batch 1 prefill 达到的 BF16 峰值 62%（第 8.6.3 节）计，约需 307.9 ms。 | …按实验 8-1 中 batch 1 prefill 达到的 BF16 峰值的 62%（第 8.6.3 节）计，约需 307.9 ms。 |
| 一项在 H100 上对 Llama-3.1-8B 的早期评估用 … 和 … ms 拟合 BF16 与 FP8 的输出间隔。 | 一项早期评估在 H100 上运行 Llama-3.1-8B，分别用 … 和 … ms 拟合 BF16 与 FP8 的输出间隔。 |
| …即同时学习预测后续多个 token 的辅助模块，利用联合训练获得的预测能力生成草稿。 | …即同时学习预测后续多个 token 的辅助模块，靠联合训练学到的预测能力生成草稿。 |
| …在稳定服务中，平均系统内请求数满足 Little 定律 | …在稳定服务中，系统内的平均请求数满足 Little 定律 |
| …取消则在请求失去使用价值时停止未来迭代，在加速器操作完成后释放空间。 | …取消则在请求失去使用价值时停止安排后续迭代，并在加速器操作完成后释放空间。 |
| …草稿模型还要处理输入，实验 8-5 中约 2300 个 token 的输入，首 token 时间从 129.4 ms 增至 141.6 ms，多出 9.4%。 | …草稿模型还要处理输入：实验 8-5 中输入约 2300 个 token 时，首 token 时间从 129.4 ms 增至 141.6 ms，多出 9.4%。 |
| 推测采样的接受与修正规则奠定了保持目标分布的基础，后续… | 推测采样的接受与修正规则为保持目标分布奠定了基础，后续… |
| 前缀共享使原先放不下的 16 条长请求能够同时执行；但在 RTX PRO 6000 上… | 有了前缀共享，原先放不下的 16 条长请求就能同时执行；但在 RTX PRO 6000 上… |

## 说明

本章此前已经过多轮语言修订，整体译文质量较高；本次只动了确实不地道的句子，"被"字句、悬挂数量尾巴等典型问题在全章范围内很少出现，故改动总数少于第 4 章。排查中用正则检索了"被""使……能够""通过……来""对……进行""作为""基于""多层'的'字堆叠"等模式，命中的句子逐条人工判断，多数已属自然中文，未做无谓改动。

## 存疑句复核结论（2026-09-11，内容核对）

- 第 416 行交叉引用笔误已改："（第 8.3.3 节，峰值 1792 GB/s 的 65%）" → "（第 8.6.3 节，峰值 1792 GB/s 的 65%）"。依据：65% 的 decode 带宽效率出自 8.6.3 节实验 8-1 效率表（batch 64 行）；8.3.3 节是"对话与智能体的前缀复用"，与该数字无关。1792×0.65≈1165 GB/s≈1.16 TB/s，数字本身无误。
