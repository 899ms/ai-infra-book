--- a/manuscripts/12-端边云协同.md
+++ b/manuscripts/12-端边云协同.md
@@ -565,7 +565,7 @@
 
 这一案例的完整判断应回到同一质量目标。先记录输入和输出长度、状态命中与恢复条件，再沿设备、运行时、网络和服务核算时间；允许更长上下文或更多分支时，另列新的任务成功率与成本。V4.1 限制部分逐层表示和选择自由度，又保留更细历史粒度与独立查询，说明系统设计可以在多个维度重新分配资源。总体能力改善和局部近似可能有损，并不互相排斥。
 
-全书的三条原则由此连在一起：可编程性决定应用在哪里表达变化；模型与基础设施协同决定哪些结构和执行方式合算；改变约束的思想实验检验原有选择是否仍成立。先固定任务识别直接收益，再重新选择模型、运行时和部署，最后回到同质量条件下的完整任务测量，才完成了一次系统设计。
+从片上存储到端边云，设计选择始终依赖对完整执行过程的了解。注意力的归约关系使中间矩阵可以按块处理，重复的模型结构使执行计划可以复用，前缀与分支关系使上下文可以共享，工具等待则为状态换出和环境准备提供了时间。每一项改进都把模型或任务的信息用于其他层次，重新选择数据保存、执行边界或交接方式。固定任务计算直接收益，再根据新约束调整模型与部署，就能说明性能提升来自哪里，以及下一步应改变什么。
 
 ![图 12-32 固定任务轨迹中的加速上限。模型从 8 秒缩短至 0.8 秒，其他串行阶段保持 2 秒；第三行表示模型时间趋近于零的理想下界。](ch12/figure-12-task-counterfactual.svg)
 
@@ -710,3 +710,5 @@
 设计实验时，也可以沿用同一套时间模型。固定输入和计时的起止点，记录各阶段的等待，再逐项改变设置，检查总时间的变化能否由具体阶段的变化解释。Queqiao 的基线修正展示了这一过程：调优消除大部分额外等待后，进一步改善性能就需要缩短传播时间。
 
 最终选择先满足质量和期限，再比较成本。即使平均计算速度和传输速率足够，任务仍可能因慢速轮次或丢失进度超期；按时完成的概率与恢复后的重做量，因而是部署模型的组成部分。
+
+再看第一章提出的模型应用，开发者通过上下文表达任务，执行系统把它展开为计算、状态和数据传输。现在，保留一段历史、增加一个候选分支、提前准备工具环境或迁移一次会话，都有了可以追踪的资源代价。理解这些代价，也就能够识别哪些通用执行开销可以利用应用信息消除，并用具体的质量、时间和成本选择实现。这是学习 AI Infra 带来的系统设计能力。
--- a/manuscripts/11-资源调度与运行环境.md
+++ b/manuscripts/11-资源调度与运行环境.md
@@ -133,7 +133,7 @@
 
 E2B 是面向工具执行的环境平台，Firecracker 是亚马逊云服务（Amazon Web Services，AWS）为无服务器计算开发、于 2018 年开源的轻量虚拟机监控器；其 2020 年 NSDI 论文讨论了如何在大量短时任务之间兼顾硬件虚拟化隔离、启动速度和内存开销。[^firecracker-history]E2B 使用 Firecracker microVM 运行工具环境，模板可由容器镜像构建。镜像提供文件与依赖，microVM 提供运行时的客体内核和设备边界。创建时，平台从模板建立环境；运行时，工具通过客体内核读写文件和启动进程。容器镜像决定环境中有哪些文件和依赖，microVM 则决定这些程序运行时如何与宿主系统隔离。[^e2b]
 
-这里可以补全第 1 章的可编程性边界。模型根据上下文选择动作，工具接口把动作交给真实程序；文件、进程、网络连接和权限最终仍由操作系统及运行环境管理。同一个固定模型可以通过不同上下文完成多种任务，也可以调用不同工具，但模型输出不会自动赋予执行权限。AI Infra 因而同时承接模型生成输出所需的数值运算，以及文件读取、代码测试等工具程序。模型运算可以利用加速器执行，工具程序则在操作系统提供的运行环境中执行。
+模型根据上下文选择动作，执行器在已经配置好权限与资源的环境中运行工具。两条路径在同一个任务控制器处汇合：它知道模型调用何时开始、工具何时使用环境，以及哪些文件需要跨轮保存。这些信息让平台能够分别安排模型状态与工具状态的生命周期。模型计算期间可以准备下一轮环境，工具完成后可以保存文件并释放进程；后面的容量与时间预算将据此选择创建、驻留和恢复方式。
 
 ### 11.2.2 资源分配与环境创建
 
@@ -670,6 +670,8 @@
 
 章首平台平均使用 30 个 CPU 核，却保留 600 GiB 环境内存。原因是三轮任务中有 27 秒在等待模型，而工作状态始终存在。在模型调用期间准备环境，把每项任务的累计内存占用从 60 降到 18 GiB·秒，准备工作多用 0.3 秒 CPU 时间，就能释放大量内存。
 
+这项改进利用了完整任务的执行关系。环境管理掌握模型等待区间后，可以把创建提前，把进程寿命缩短到工具真正使用它的阶段；持久文件继续承接跨轮状态。模型服务和环境平台共同安排准备时机，使原来贯穿任务全程的资源占用变成按阶段分配。
+
 释放内存以后，任务仍需 30 秒，模型调用成为满足 24 秒期限的关键。将调用从 9 秒降到 6 秒，把三轮任务从 30 秒降到 21 秒，也将平均并发模型调用数从 270 次降到 180 次。结合每轮重建环境，现有 40 核、640 GiB 内存和最多 300 次并发模型调用的容量即可支持给定任务流。成本比较随后在满足质量和期限的方案之间完成。
 
 同样的方法也解释了 RL 的资源组织：成组作业先取得配套资源，临时 rollout 先完成权重传输和加载，样本生成后立即提交验证，可以让验证与后续生成同时进行；失败后重试，则需要累加各次尝试的工作量。分析资源需求，要计算任务总共做了多少工作；分析完成时间，还要考虑这些工作必须按什么顺序执行、哪些可以同时进行。结合这两方面，才能判断一项局部改进对整个系统是否有益。
--- a/manuscripts/02-模型架构.md
+++ b/manuscripts/02-模型架构.md
@@ -3,6 +3,8 @@
 第一章把一次模型执行分解为容量、计算与数据读取，并借助硬件的关键数字估算时间下界。这样的估算可以迅速排除一些方案，却还留下一个问题：所代入的计算量和数据量从哪里来？“一个 8B 模型”只说明参数数量的大致范围，没有说明每个 token 使用哪些参数、上下文保存在什么地方，也没有说明不同工作之间的先后关系。
 
 本章从一个 token 的数值表示开始，沿着一层网络追踪它如何变成下一个 token 的分数，再计算这条路径的容量 $M$、运算量 $F$ 和读写量 $R$。计算图用节点表示运算，用边表示运算之间传递的数据。先认识图上的对象，才能知道资源预算中的每个数字来自哪里。
+
+这张计算图也提供了优化实现所需的信息。哪些权重在不同请求间保持不变，哪些状态会被后续生成反复使用，哪些结果只交给紧接着的运算，都可以从模型结构中找到。后面的存储、编译和调度设计将利用这些区别，分别安排共享、驻留与临时缓冲。
 
 我们使用 Qwen3-8B 作为贯穿例子。这是一个约含 81.9 亿参数的语言模型，重复执行 36 层网络来处理文本。先完整理解这一种模型，再改变上下文的保存方式、每个 token 使用的参数集合和层间连接，比较 Qwen3.6-35B-A3B（下文简称 Qwen3.6）、DeepSeek V4-Flash 与 Kimi K3 的资源需求。
 
@@ -768,6 +770,8 @@
 
 上下文变为 16K 时，每条请求占 2.25 GiB，同一空间只能放两条；变为 4K 时，则能放九条。整数跳变来自剩余空间必须足以容纳完整的一条状态，而不是只够存放其中一部分。
 
+保留多少历史由此成为一个同时影响应用和系统的决定。应用多保留一段材料，模型就能继续参考它；服务需要为每条请求分配更多状态空间。前面的 GQA、MLA 和递推表示进一步改变了保存这些信息的方式。模型结构、上下文组织与并发容量可以放在同一份预算里比较，任务检查则确定哪些方案能够提供所需的回答质量。
+
 再看 DeepSeek-R1-Distill-Llama-70B，其参数量约为 705.5 亿。其 BF16 权重为 141.107 GB，每条 8192-token BF16 KV 为 2.5 GiB。在本例采用的分组量化方案中，保留嵌入、输出头和归一化等较高精度部分，并计入 scale 与打包尾部后，8-bit 方案约为 73.726 GB，4-bit 方案约为 39.500 GB。
 
 4-bit 方案的 39.500 GB 来自低位宽矩阵、高精度保留项、scale 与打包的共同贡献，比将全部参数一律按半字节计算更大。显存容量为 80 GB（十进制）、固定预留 2 GiB 时，8-bit 方案可放 1 条 8K 请求，4-bit 方案可放 14 条。上下文从 8K 增至 32K 时，每请求 KV 增至四倍，4-bit 方案可同时容纳的请求数降到 3 条。压缩权重腾出的空间可以保存更多请求的上下文，但每条上下文越长，能同时容纳的请求就越少。[^source-23]
--- a/manuscripts/06-超节点.md
+++ b/manuscripts/06-超节点.md
@@ -712,6 +712,8 @@
 
 八卡协作使单个会话的续写时间从约 132 ms 缩短到 34 ms，但四个会话排成一队，所有会话约 137 ms 才完成。采用四个两卡实例时，单个会话的续写时间较长，却能同时推进四个会话，约 69 ms 全部完成。八个单卡实例中，只有四个分配到了会话，多出来的空闲实例无法加速正在执行的请求。
 
+这里同时使用了模型内部与请求之间的并行关系。层内矩阵可以切分给多卡，独立会话可以分配给多个实例；两种并行共同竞争这八张卡。掌握这两层关系，才能根据完成目标选择协作组大小。服务调度给出同时推进的会话数，模型切分给出每步通信量，互联拓扑给出交接时间，三者共同决定表中的完成时刻。
+
 图中色块的横向长度表示单个会话的执行时间，纵向轨道数表示多少会话可以同时推进。完成期限决定了这两者怎样取舍。只有一个会话、要求 40 ms 完成，八卡实例满足目标；四个会话、要求 90 ms 全部完成，两卡和四卡部署方式都满足，两卡更早完成。较短的单会话完成期限需要更大协作组，足够多的独立会话则可以利用更多实例。
 
 连接改变后再沿原式计算。若八卡跨过 6.5.1 的服务器边界，每步约 6.32 ms，八步约 50.5 ms，原来的 40 ms 目标无法满足。此时解决问题要回到那项新增开销：改善服务器间连接、改变集合通信算法，或将八卡放进更紧密的互联域。
--- a/manuscripts/04-加速器架构.md
+++ b/manuscripts/04-加速器架构.md
@@ -592,9 +592,11 @@
 
 ### 4.6.3 固定数据流与固定权重
 
-专用化还可以向前推进：固定算子种类使指令和控制简化，固定数据流使通信提前安排，固定数值格式使存储器和运算单元可以按所需位宽设计，固定权重则让不可变参数进入专门的存储组织。条件确定得越早，越多通用工作可以被省去；模型变化时，需要重做的部分也越多。
+专用化利用执行中已经确定的条件重新分配硬件资源。固定算子种类使指令和控制简化，固定数据流使通信提前安排，固定数值格式使存储器和运算单元可以按所需位宽设计，固定权重则让不可变参数进入专门的存储组织。原来用于处理多种情况的控制和数据通路，可以相应简化。条件确定得越早，可省去的通用工作越多；模型变化时需要更新的部分，也由这些选择决定。
 
 设计中哪些部分允许重新配置，决定了模型改变后需要更新什么。SambaNova 的数据流组织与 现场可编程门阵列（FPGA）的可重配置通路，允许通过重新配置改变部分连接和执行顺序；固定权重设计则围绕特定参数组织存储与计算。[^special] 选择哪一种，取决于哪些部分长期稳定，以及省掉的工作原来占据多少时间。
+
+上层任务仍然可以通过输入变化。同一组模型权重既可以处理文档问答，也可以根据代码和测试结果提出修改；每次调用使用自己的上下文状态。因而，硬件可以针对共同的模型执行设计权重通路，同时用可写存储承接变化的请求。下面把权重与上下文分别计量，计算这种分工能够释放多少容量和带宽。
 
 作者的 OpenTallas 项目研究固定权重架构。下面以项目中的 Qwen3-8B 为例，分析“从独立只读存储器（ROM）读取权重、从 HBM 读写 KV”的设计怎样改变各接口的传输量。[^opentallas] 每批只读取一次本步计算所需的权重，约 $W=15.1$ GB；每条请求完整读取 8,192 位置 KV，约 $K=1.21$ GB。完整驻留权重约 16.4 GB，每步读取其中参与计算的部分。
 
--- a/manuscripts/03-推理与训练负载.md
+++ b/manuscripts/03-推理与训练负载.md
@@ -162,7 +162,7 @@
 
 用这一模型比较两种编辑方式：在末尾追加 100 个 token，只增加 100 行新输入；若在已有 2000 个 token 的第 501 个位置改写一个 token，变化会影响其后的表示，最长可直接复用的前缀只剩 500 个位置。缓存收益不仅取决于改了多少字，还取决于改动发生在哪里。
 
-从应用看，上下文是编排任务的接口；从系统看，它会展开为工作量与状态寿命。追加一次工具返回，会增加输入处理和后续每步读取；替换早期内容，可能使该位置之后的前缀缓存失效；分出多个候选，会让共享历史之后出现独立状态。因此，负载描述除了输入、输出长度，还应记录哪些位置不变、哪些位置被重写、分支何时合并，以及工具等待期间保留什么。第 8 章将比较三种具体的上下文更新方式。应用的编排选择由此直接进入基础设施预算。
+从应用看，上下文是编排任务的接口；从系统看，它会展开为工作量与状态寿命。追加工具返回、替换早期内容、分出多个候选，分别改变新增计算、可复用前缀和私有状态。把这些关系交给服务系统，就能按实际复用安排缓存，按分支共享历史，按工具等待安排状态换出。因此，负载记录还应包含稳定前缀、改写位置、分支依赖和复用间隔。第八章将使用这些信息选择上下文与缓存策略，第九章再加入状态位置和传输时间。
 
 ### 3.2.2 推理任务、验证与成功任务成本
 
--- a/manuscripts/10-训练系统.md
+++ b/manuscripts/10-训练系统.md
@@ -3,6 +3,8 @@
 一个模型能够在单张加速卡上生成文本，是否意味着它也能在这张卡上训练？全参数训练会更新模型的全部可训练权重；混合精度训练用较低精度执行主要矩阵运算，同时用较高精度保留更新所需的数值。这里 BF16 是占 2 字节的 16 位浮点格式，FP32 是占 4 字节的 32 位浮点格式。损失是衡量预测与训练目标差异的数值；优化器是根据损失梯度调整参数的算法。Adam 是一种利用梯度历史调整参数更新的优化器。Qwen3-8B 的 BF16 权重约为 16.4 GB，而采用混合精度 Adam 进行全参数训练时，训练除模型权重外，还保存梯度（损失对参数的导数）、主权重（用于高精度更新的参数副本），以及优化器跨步保留的梯度历史，合计约为 131 GB。后者约为前者的八倍。前向传播由输入得到预测，反向传播沿运算依赖求导；求导时还要读取前向保存的激活，即中间计算结果，执行期间还会出现参数聚合和转换缓冲。设计训练系统，首先要确定这些张量和缓冲区存放在哪里，以及需要保留多久。
 
 把训练状态分到更多设备上，可以减轻单卡的显存压力，但计算往往需要等待其他设备传来的数据。把状态移到主机内存，可以腾出显存，却又增加了主机与 GPU 之间的传输。训练持续数周时，等待输入、保存检查点和故障后的重复计算也会影响完成时间。理解这些取舍，需要把三个问题连起来：**哪些张量和缓冲区需要同时保留，哪些工作决定一步何时结束，已经完成的训练中，有多少能够在故障后恢复。**
+
+训练图给出了这些决定所需的生命周期信息。前向产生的激活在反向时使用，梯度在参数更新前汇集，优化器状态跨步保留。利用这些已知关系，运行时可以提前取回即将使用的参数、重算较便宜的激活，并让已就绪梯度的通信与后续反向计算重叠。模型求导、内存管理与设备调度由此共同决定一步训练的执行方式。
 
 本章围绕一个任务展开：使用 Qwen3-8B 处理 100B token，完成领域继续训练，期限为 30 天。我们先估算至少需要多少资源，再比较由 32 张和 48 张 RTX 4090 组成的两套方案。比较从显存需求开始，逐步加入计算、通信、数据准备和故障恢复的耗时，最后确定哪套方案能够按期完成。为了讲清各项机制，文中穿插较小的张量和流水线算例。强化学习（RL）部分则进一步分析生成、验证和学习如何组成反馈循环。
 
@@ -582,7 +584,7 @@
 
 训推一致性的目标是让概率差异有清楚的来源，使更新符合算法所用的分布假设；逐位相同是更强的要求，是否需要及其代价取决于具体实现。它是排查 RL 不稳定性的重要环节，但奖励噪声、全零优势、学习率和数据变化也会影响训练。前面的全零优势案例即使完全对齐两端计算，也不会凭空产生学习信号。因而应把一致性误差与奖励、梯度和固定任务评估一起观察，不能把所有训练波动都归因于训推不一致。
 
-RL 由此延续了全章的方法：先保证一次训练迭代使用正确的数据和概率，再比较每秒能完成多少次这样的迭代。阶段吞吐、过期样本筛选与重放成本共同决定反馈循环的有效训练进度。
+RL 把生成、环境验证和参数更新连接成同一个反馈应用。生成端提供采样概率与专家选择，训练端据此组织更新，资源调度再安排各阶段重叠。贯通这些信息，可以同时优化数据交接、执行一致性与资源利用。前面的异步周期和路由重放例子分别量化了省下的等待与增加的记录成本，最终用有效样本和训练进展评价整个反馈循环。
 
 ## 10.6 从系统方案到完成期限与硬件选择
 
--- a/manuscripts/07-数据中心网络.md
+++ b/manuscripts/07-数据中心网络.md
@@ -268,6 +268,8 @@
 
 这种变化可以用第 7.2 节的消息模型解释。一条 8 KiB 消息在 25 GB/s 链路上的数据传输时间约为 0.33 μs。若请求启动时间从 5 μs 缩短到 2 μs，总时间便从约 5.3 μs 降至 2.3 μs，节省超过一半。64 MiB 的载荷本身需要约 2.7 ms，同样省下的 3 μs 只占很小部分。因此，小消息应优先减少提交请求的开销，大消息应优先提高数据传输速度。
 
+主机统一组织通信，提供了供不同程序使用的控制路径。模型执行明确了数据的生产者、消费者和就绪条件后，可以把频繁的发起与完成处理放到设备侧。这里重新选择的是控制职责所在的层次：预先建立访问条件，随后让计算与通信沿同一条设备执行链推进。收益直接体现在上例的启动时间中。
+
 操作系统负责建立映射、权限和资源分配；设备负责已经授权的快速操作；完成通知把操作结果交回运行时。职责这样划分后，设备上的通信线程也会限制吞吐。要分析这种限制，先要分清一次请求等待多久、等待期间能否发起其他请求，再计算单位时间内能处理多少项请求。
 
 ### 7.3.3 指令与异步请求
@@ -429,6 +431,8 @@
 
 恢复时间若增至 200 μs，串行安排中的 C 要到 232 μs 才完成，独立安排中的 C 仍在 10 μs 完成。删除额外依赖，把恢复造成的停顿限制在需要这份数据的链上。对于等待 C 的下游任务，这种隔离比全组只提前 10 μs 更重要。[^ordering]
 
+全局顺序让上层容易描述操作的先后关系，也把独立工作放进了同一条等待链。计算图已经明确 A→B 的发布依赖后，接口和运行时可以围绕这条依赖提供顺序保证，让 C 独立推进。应用提供更准确的依赖信息，网络据此缩小排序范围，便能在保持发布语义的同时缩短等待。
+
 再假设所有操作共用一个处理单元：从 A 开始写入到 B 发布通知结束，这个单元一直被占用，C 只能随后执行。此时资源使用本身增加了发布链到 C 的顺序边，两种安排都到 112 μs 完成。程序依赖和资源依赖因此要画在同一张图里：删除前者后，后者仍会决定能够并行多少。
 
 > **设计背景：为什么全局顺序很有吸引力？**
--- a/manuscripts/09-分布式推理.md
+++ b/manuscripts/09-分布式推理.md
@@ -89,6 +89,8 @@
 
 PD 将 prefill 和 decode 分配给两个服务池，但两个池都要执行完整的前向计算。Qwen3-8B 的 P 处理 8192 个输入位置，D 随后调用 128 次；每次都经过 attention 和 FFN。于是 P、D 都需要相应权重，而上下文 KV 还要从 P 交给 D。独立批处理带来的收益，首先要补偿这次新增交接。
 
+完整模型请求向应用提供一次调用，内部执行则可以按阶段重新分工。模型的因果关系给出了切分位置，KV 给出了两阶段之间需要交接的状态，负载比例决定各池需要多少资源。利用这些信息，服务可以在统一调用接口之下，为输入处理和逐步生成分别选择批次与设备。接下来用传输量和处理能力计算这种分工的收益。
+
 ### 9.2.2 KV 传输与两端内存占用
 
 P 完成输入处理后，D 需要得到上下文 KV 才能继续生成。**分组查询注意力**（GQA）让多个查询头共享一组键和值。对保存逐位置上下文键和值的完整 GQA 状态，设层数为 $n$，上下文长度为 $L$，KV 头数为 $h_{kv}$，每头维度为 $d_h$，每元素为 $b$ bytes，则逻辑状态大小为
@@ -425,6 +427,8 @@
 
 B 要在 200 ms 内返回，扣除命中后 10 ms 计算，状态必须在 190 ms 内就绪。再扣除查找的 10 ms 与 host→GPU 的约 48.3 ms，远端读取只剩约 131.7 ms。用 1.125 GiB 除以这个预算，得到约 9.2 GB/s 的取回与重算耗时相等时的带宽。
 
+同一会话由此可以根据状态与资源的位置重新安排。应用保留连续的上下文，服务选择继续使用本地状态、迁移状态或重新计算；选择依据是这三条路径何时能让后续模型执行开始。第八章根据内容判断可复用的前缀，本节进一步把队列与链路纳入决定。
+
 把远端提高到 20 GB/s 后，单请求取回明显占优；但每秒 16 次取回需要约 19.3 GB/s，已用去约 97% 的链路能力。突发请求会增加传输排队时间。因此，按单请求算出所需带宽后，还要计算持续到达时的总流量。[^route]
 
 上面的比较假定缓存位置已知且数据可用。路由器通常依据缓存事件预测位置，事件延迟或对象淘汰会使预测失准。为了看清这种误差怎样影响响应时间，现在把 A 的队列改为 80 ms，且缓存只以概率 $p$ 真正可用。命中时为 90 ms，所有层次都失效时为 260 ms。其期望为
--- a/manuscripts/01-初识 AI Infra.md
+++ b/manuscripts/01-初识 AI Infra.md
@@ -30,7 +30,9 @@
 
 从模型一侧看，压缩保存的状态、改变注意力机制或只使用部分模型参数，可以改变硬件需要完成的工作；从硬件一侧看，存储容量、数据传输速度和设备连接方式，又会影响哪些模型结构和执行方法更合适。理解这种双向关系，才能同时讨论回答质量、响应时间和成本，并解释为什么某个应用的最佳方案在另一个应用中需要重新选择。
 
-**编程抽象从操作系统到模型上下文的迁移是计算机系统领域数十年来最重要的变化之一。**计算机系统领域的两大顶级会议 SOSP（操作系统原理研讨会）、OSDI（操作系统设计与实现）中都包括 “OS”（操作系统）。以往的操作系统最重要的职责是支撑多种多样的应用，而今天的 AI 系统中，操作系统之上最重要的应用就是模型的推理和训练。传统操作系统和编译器大量的工作是简化应用编程、保证应用之间的隔离性。这些工作在今天的 AI 系统中，重要性没有之前那么高了。相反，**跨层联合优化，提高 AI 推理和训练的性能变得越来越重要**。这就是本书的主题。
+**编程抽象从操作系统到模型上下文的迁移是计算机系统领域数十年来最重要的变化之一。** 计算机系统领域的两大顶级会议 SOSP（操作系统原理研讨会）、OSDI（操作系统设计与实现）中都包括“OS”（操作系统）。传统操作系统需要支撑多种多样的应用，编译器和硬件也要为事先未知的程序提供通用能力。各层通过接口分工，在简化编程、隔离执行和提高性能之间作出设计取舍。例如，独立算子通过内存交换结果，便于组合成不同程序，也增加了中间数据的读写；主机统一提交设备任务，便于控制执行，也增加了反复交接的时间。
+
+在以模型训练和推理为主要应用的系统中，各层可以围绕同一个完整的执行过程共同设计。模型结构给出计算和数据依赖，运行时掌握状态的产生与使用，硬件承担相应的运算和传输。利用这些信息，可以重新选择算子边界、存储表示、调度方式和设备交接路径，消除通用实现中的部分开销。同一模型又能通过不同上下文承接多种任务，使应用行为的多样性与底层执行的专用化同时成立。**上层可编程性的变化，为跨层联合优化打开了新的空间。** 本书用量化分析研究这些空间，解释原有设计取舍如何改变，以及新的实现怎样提高训练和推理的性能。
 
 ### 1.1.2 从任务到设备的六层分工
 
@@ -55,6 +57,8 @@
 六个层次说明了主要分工，但有些能力贯穿多个层次。资源调度、运行环境、观测和计费会同时作用于多个层次，需要贯穿各层来组织；工艺、封装、供电和散热构成物理条件，决定设备的部署条件。网络也并非只在最下方发挥作用：需要跨设备协作的模型、算子和服务，都要经过具体互联。
 
 沿这六层向下看，任务逐步变成具体的计算与数据访问；向上看，设备容量、算力与带宽决定能够承接多大的模型、多少并发和多快的响应。海思首席科学家廖恒博士提出的“十八层宝塔”，用来描述从应用、模型与软件系统，到芯片、制造工艺和底层物理的多个层次，强调这些层次之间的联系。本书据此组织全栈视角，并按教学需要将讨论内容归为图 1-2 的六层。[^panorama]
+
+这些层次也标出了可以共同优化的边界。模型改变状态表示，会改变存储和通信需求；编译器将相邻算子融合，会改变中间结果的保存位置；运行时让设备直接交接，会改变主机参与的次数。后续章节将从这些具体变化出发，计算省下的读写、提交和等待，再回到完整请求，判断哪一种组织方式更合适。
 
 AI Infra 继承了云数据中心的服务器、网络、存储和资源管理基础，但模型执行改变了资源组织的重心。同一个模型可以承接问答、代码生成等多种应用；一次模型调用又可能分布在许多加速器上，使网络通信直接进入计算依赖链。基础设施既要服务独立请求，也要组织设备之间的紧密协作。
 
@@ -476,7 +480,7 @@
 
 ## 本章小结
 
-应用可编程性的入口上移到模型与上下文，使许多任务共享同一类模型计算；操作系统继续负责工具程序与隔离。这一分工提供了专用化的机会，也要求我们检查模型和硬件的双向影响。后续各章先保持任务不变测算约束变化，再重新选择模型与系统方案。
+应用通过模型与上下文表达行为，使许多任务共享相近的底层计算。掌握模型的计算关系与状态寿命，就能把原来分属不同层次的决定放在一起：改变数据表示、合并执行步骤、复用准备工作，或缩短设备交接。后续各章将量化这些改动的收益，说明哪些设计取舍因此改变，再根据新的约束选择模型与系统方案。
 
 分析系统时，先检查数据能否放下，再用运算量和读写量估算耗时，最后根据执行顺序分析重叠与等待。在 70B 算例中，权重读取比矩阵计算慢约 148 倍，因此增加带宽、减少读取和提高复用更有助于缩短时间。批内共享可以提高吞吐，而每个请求的响应时间还要包括整批执行和排队。
 
--- a/manuscripts/08-单实例推理.md
+++ b/manuscripts/08-单实例推理.md
@@ -271,6 +271,8 @@
 
 独立保存需要 416 块，共享后减少约 69%。这项节省来自对公共前缀的去重，36 个私有尾块仍随分支数增长。[^pages]
 
+服务系统知道哪些位置属于共同前缀，哪些位置会在分支后独立写入，因此可以把模型的复用关系落实为物理块共享。页表负责地址映射，前缀关系决定共享范围，分支写入触发私有副本。原来按整条请求独立预留的空间，由此改为按实际产生与复用的状态分配。
+
 **回到章首的算例。** 16 条长请求共享前 6144 个输入 token，其 KV 共占 864 MiB。每条请求的私有部分包含 $8192-6144+255=2303$ 个位置。按每块 16 个位置分配，需要容纳 2304 个位置，即 324 MiB。因此，整组请求生成完毕时所需的 KV 空间为
 
 $$
@@ -316,6 +318,14 @@
 上下文编排还可以主动改变复用机会。假设已有 8K token 的上下文，下一轮追加 1K 工具结果，最多可复用原来的 8K 前缀，新增部分需处理；如果改写最前面的指令，后续 token 即使文字相同，产生 KV 时依赖的历史已经变化，不能继续把整段当作相同前缀。另一种方案是把历史总结为 2K，再追加 1K，后续状态会更短，却要支付总结与重新处理的成本，并检验信息损失。
 
 因此，“总 token 更少”与“本轮计算更少”并不等价。应用可以保留稳定指令和材料的位置，把变化较快的信息放在后面；需要总结时，则按未来还要生成多久，比较一次重算与后续每步节约。复用条件还包括模型版本、位置和其他影响计算的配置一致。
+
+用一组教学条件求出总结的收益边界。两种方案均通过相同任务检查，后续生成相同数量的 token。总结及重建缓存合计额外耗时 300 ms；在给定设备、批次和后续长度范围内，缩短上下文使每步 decode 净减少 1 ms。若后面还有 $N$ 步生成，总结方案相对保留历史节省的时间为
+
+$$
+\Delta T=N\times1\ \mathrm{ms}-300\ \mathrm{ms}.
+$$
+
+后续生成 100 步时，总结多花 200 ms；生成 1000 步时，总结节省 700 ms；超过 300 步后开始获得时间收益。总结把一次额外计算换成后续多次较短的状态访问，因而适合在还有足够后续工作时进行。把未来工具轮次也纳入比较，就按各轮的输入处理、生成与状态恢复逐项累计。应用决定何时总结，服务提供重建与读取代价，两者共同确定时机。
 
 ![图 8-20 同一历史的三种更新方式。蓝色表示可复用前缀，橙色表示需要重新处理的输入；总结方案还要额外执行总结并验证质量。长度以 K token 示意。](ch08/figure-8-context-edits.svg)
 
--- a/manuscripts/05-算子与运行时.md
+++ b/manuscripts/05-算子与运行时.md
@@ -285,6 +285,8 @@
 FlashAttention 由斯坦福大学的 Tri Dao 等研究者与纽约州立大学布法罗分校的合作者于 2022 年提出，针对长序列注意力中间矩阵过大、显存读写量高的问题，重新组织计算顺序与数据存储。[^fa]
 
 前面的流水在一块数据准备好后就开始计算，随后释放该块缓冲。逐元素运算很容易这样处理；注意力的输出却依赖整行分数，处理完当前块时，后面的分数还没有读到。要继续沿用逐块计算和复用缓冲的方式，就必须找到能够正确合并各块结果的归约方法。其中 Q 是查询向量，K 是供查询匹配的键向量，V 是最终按权重汇总的值向量。Softmax 将一行分数转换为总和为 1 的权重。标准注意力先计算 $S=QK^\mathsf{T}/\sqrt d$，再计算 $P=\operatorname{softmax}(S)$，最后计算 $O=PV$。
+
+把这三个运算分别交给通用算子，可以直接按公式组合程序，算子之间通过中间张量交换结果。理解完整注意力后，编译与执行设计获得了另一种选择：利用 Softmax 的归约关系，跨过独立算子的边界，在小块数据上连续计算。下面先算出原来物化中间张量的代价，再推导逐块计算所需保存的状态。
 
 取一个头，序列长度 $L=8192$，头维度 $d=128$，对全部位置计算注意力。Q、K、V、O 使用 BF16，各占 2 MiB；完整 S、P 使用 FP32，各占 256 MiB。Q、K、V 各读一次，O 写一次，共 8 MiB；S、P 各写一次、读一次，却要传输 1 GiB 数据。中间矩阵的大小随序列长度的平方增长，因而造成了大量读写。[^attention]
 
@@ -503,7 +505,9 @@
 
 在自回归生成中，能否提前准备，取决于所需的数据是否已经算好。下一步计算需要用到上一轮选出的 token；组建批次、更新语法状态和判断是否停止，也各自需要相应的结果。运行时可以提前执行不依赖这些结果的工作，减少两轮计算之间的等待。vLLM 等引擎的异步执行与调度演进围绕这一问题展开。[^runtime]
 
-如果专用设备把模型执行从 100 μs 缩短到 10 μs，而主机每轮准备仍需 20 μs，在可重叠、无其他依赖的稳定流水中，步间隔的下界就从 100 μs 变为 20 μs，收益只有 5 倍。设备越快，动态批次准备、地址更新和提交越值得重新测量。应用上下文不断变化，并不意味着每次都要重建算子程序：可以复用执行骨架，更新输入内容、长度和地址；当新形状或新的控制分支越出已有骨架时，才需要选择另一实现。上下文的可编程性与执行图的复用由不同层负责。
+如果专用设备把模型执行从 100 μs 缩短到 10 μs，而主机每轮准备仍需 20 μs，在可重叠、无其他依赖的稳定流水中，步间隔的下界就从 100 μs 变为 20 μs，收益为 5 倍。此时进一步缩短步间隔，需要把动态批次准备、地址更新与提交一起优化。
+
+两次调用可以分别要求模型解释代码和检查测试，输入内容不同，却仍经过同一组模型层。长度与批次落在已有执行计划支持的范围内时，运行时可以复用程序，更新 token、位置和状态地址；新的形状或控制路径则由另一份计划承接。应用通过上下文表达变化，运行时利用稳定的执行结构减少重复准备。下面的图重放与形状特化，将分别计算这两种复用的收益。
 
 ### 5.5.2 CUDA Graph：提交收益与输入复制开销
 
--- a/README.md
+++ b/README.md
@@ -1,6 +1,6 @@
 # 《深入理解 AI Infra：量化分析与系统设计》
 
-**数据搬移塑造了 AI Infra 的架构。** 这本书用作者的研究经历、具体模型和可复算的数字，解释芯片、网络、推理训练框架与模型算法怎样降低搬移代价，以及何时需要为容量、计算、正确性或质量付出更多搬移。
+**理解模型应用的执行过程，才能重新选择系统的设计取舍。** 应用通过模型与上下文表达行为，底层围绕共同的训练与推理计算开展专用化和跨层优化。这本书用作者的研究经历、具体模型和可复算的数字，解释模型、编译运行时、芯片与网络怎样共同减少读写、提交和等待。数据搬移贯穿这些分析，质量、完成时间与成本决定最终选择。
 
 每次设计先问：搬什么、搬多少、搬几次、经过哪里、谁必须等它。读者沿同一任务逐章修正判断，最终能提出候选、找到遗漏约束，并用最小实验决定值得改什么。
 
--- a/manuscripts/README.md
+++ b/manuscripts/README.md
@@ -1,6 +1,6 @@
 # 正文书稿
 
-十二章正文已系统补入三条主线：应用可编程性向模型与上下文上移、模型与基础设施的双向协同，以及改变约束后重新选择系统。此前新增九幅插图沿用全书统一的 Hands-On Large Language Models 风格，以 SVG、PNG、PDF 提供。
+十二章正文以模型训练与推理的完整执行过程连接跨层优化：应用通过模型与上下文表达行为，各层利用计算关系、状态寿命与任务依赖重新选择实现。算子融合、执行复用、设备交接、上下文共享和环境管理将这一主线融入具体推导。此前新增九幅插图沿用全书统一的 Hands-On Large Language Models 风格，以 SVG、PNG、PDF 提供。
 
 各章均以“本章小结”收尾，资料说明移出正文，来源保留为脚注。
 
--- a/outlines/structure.md
+++ b/outlines/structure.md
@@ -4,7 +4,7 @@
 
 ## 核心认识与设计方法
 
-**应用可编程性向模型与上下文上移，模型与基础设施相互塑造，约束变化要求重新选择系统。** 这是全书的三条主线。传统操作系统仍承担工具执行、权限与资源隔离；固定权重不等于固定应用。思想实验先固定任务、模型与质量目标，只改变一个条件，再允许模型与部署重新选择。
+**应用可编程性向模型与上下文上移，为底层专用化与跨层优化提供空间。** 模型训练与推理是可以贯穿多层共同分析的完整应用；模型结构、数据依赖与状态寿命，使各层原来为通用实现作出的部分设计取舍可以重新选择。正文通过具体机制量化省下的读写、提交与等待，再根据新约束选择模型和系统。传统操作系统仍承担工具执行、权限与资源隔离；固定权重不等于固定应用。思想实验先固定任务、模型与质量目标，只改变一个条件，再允许模型与部署重新选择。
 
 **数据搬移是检验这些主线的重要定量方法。** 本书从芯片体系结构、网络、推理训练框架和模型算法四个层次，解释怎样降低数据搬移的代价。每次先问：搬什么、搬多少、搬几次、经过哪里、谁必须等它；再比较改变表示、复用、放置、分工和执行顺序后的收益。
 
--- a/outlines/editorial-notes.md
+++ b/outlines/editorial-notes.md
@@ -28,6 +28,8 @@
 原图及改绘的出版使用状态随图登记；自绘图也要记录机制来源。实验的教学计算、简化程序、公开测量和本书实测分别标明。
 
 ## 全书的写作立场与层次
+
+作者澄清（2026-09-10）：第一章的中心是模型训练与推理作为完整应用，为重新选择各层原先的通用设计取舍提供条件。后文通过算子融合、执行计划复用、设备交接、必要顺序、KV 共享与任务生命周期具体推进。说明原实现的便利和代价、应用提供的信息、改变的机制与量化收益；将这些关系融入既有推导，保持自信、连贯的教材语气。章首与小结不反复复述主题，不用隔离重要性讨论替代这条主线。
 
 以资深芯片与系统设计者回顾行业的口吻写作：先交代当时要完成什么、手中有什么资源，再让读者跟着几笔计算发现旧方案的限制，由此理解新设计。经验体现在对关键约束、代价和未知量的判断上，不用自我标榜、口号或大量术语制造权威感。作者亲历可采用第一人称，其他人的设计动机以原件为据，推断明确作为推断。
 
