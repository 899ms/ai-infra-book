# 第八章写作前阅读与采用记录

2026-09-10。先阅读当前六节大纲、扩写资料及已有 calculations、survey、案例和实验，再写正文与画图。本轮没有重新运行推理服务，也没有宣称重新逐页精读所有论文。历史 survey 的旧章号不直接用作当前正文定位。

| 阅读材料 | 采用位置与判断 | 证据边界 |
|---|---|---|
| batch-reuse 的 2K、8K 计算及固定模型配置 | 8.1 容量、8.2 权重复用与两个交点 | 理想共享权重读取与完整驻留分别计量；未计工作区不证明服务可行 |
| iteration-batching 三种策略的 JSON／Markdown | 8.2 与图 8-1 | 直接按教学事件绘图；base、新位置、配对成本不是 GPU 拟合 |
| chunk-history-book | 8.2 的因果配对、完整矩阵与真实时间 | 11 次记录；事件区间与 logits／采样边界分别说明 |
| MLSys 2024 的 Punica／S-LoRA 阅读记录 | 8.3 多 adapter 旁栏 | 采用共享与状态身份原则；旧随机 adapter、模型与基线不转成当前性能 |
| MLSys 2025 的 Marconi 与 KV 压缩评估阅读记录 | 8.3 恢复边界、8.4 自然质量 | 仅采用先前已声明页段的设计与评价结论，未把摘要候选当正文证据 |
| OSDI 2024 的 Sarathi-Serve 阅读记录及分块笔记 | 8.2 连续接纳与分块的分工 | 不据抽取文本中的图轴差异估读性能；正文用本书计算和实测 |
| OSDI 2025 的 NanoFlow 阅读记录及资源共享笔记 | 8.2 切分可能重复读权重 | 高吞吐条件与 batch=1 分开；不复制论文加速比 |
| 混合状态、上下文工程、多 LoRA 笔记 | 8.3 共享身份、状态保存时间、检查点 | K3 为指定布局和并行下的教学载荷；Agent 输入回放不等于完成任务 |
| KV 格式、卸载、张量压缩与业务配置笔记 | 8.4 的真实文件、scale、预取、解压交点 | 每值位数与执行格式分开，24／384 GiB/s 为假设；大模型文件与小模型实测分开 |
| 推测执行、历史草稿笔记与有理数穷举结果 | 8.5 正确性、计数、两种草稿成本 | 保持目标分布不保证目标质量；EAGLE／DFlash 实现和公开条件不拼排名 |
| 既有 8-1 至 8-9 实验 README 和相关汇总 | 对应小节的实测对照 | 共享设备、固定顺序、单次或少量重复、窄题集、未知整机成本就地注明 |
| 12 轮 agent-prompts.json | 图 8-3 压缩前缀树与共同前缀 | 只读取 token ID 推导匹配长度，不以匹配推断实际缓存命中 |
| 8-9 analysis.json 的全部 21 个窗口 | 图 8-7 | 保留全部三个重复与有限窗口；组件能量非整机非独占 |

基础算法说明沿用仓库的 Orca、PagedAttention、推测采样原文入口及先前 survey 的声明范围。正文引用提供原件和核算入口；完整安装命令、模型版本、路径审计和未完成实验范围保留在原目录。

## 写作取舍

正文采用经典体系结构教材常用的论证方式：先定义观察对象与变量，再从容量、读取和依赖推导机制，用可复算算例解释转折，最后以实测和反例检验。章节按当前大纲的六节、22 个小节展开；保留九项练习。没有复写或仿写指定教材的具体段落。

图号和完整说明放在图外；图内只保留分面说明、变量、单位、图例和必要的教学／实测标记。所有图为本轮代码生成的 SVG／PNG／PDF，未使用位图生成模型。来源锁见 sources.json，具体图数据见 figure-data.json。

末轮补读 gguf-layout-q2k、gguf-inventory-8k／32k：逐张量格式与文件字节守恒已经有现成核算，补入 8.4.1，避免停留在仅有文件名的粗略估算。同期主大纲新增既有实验摘要；核对后更新其来源锁，正文仍以原计算和实验记录作为数值依据。

## 数字论证修订

2026-09-10 再次修订六节正文与八幅图。复用既有计算和实测，补读 Q2_K 逐张量结果与既有 MLSys 2025 阅读记录。新增的缓存期望收益、100 秒任务分解和等能量结果比较均为明确假设的教学例。没有新增硬件测量。图 8-1 的逐块样本、原能量窗口仍保留在 figure-data.json；展示聚焦后不再将不同问题并排。服务图改为 8-7，Amdahl 图改为 8-8。

## 连贯教学重写

2026-09-10：六节沿 32 GiB 设备、12 GiB 状态预算、2K／8K 输入、256 输出、3 秒期限的教学设计推进。补充完整请求页容量、前缀共享容量、缓存概率交点、推测边际产出及章末 A—D 配置选择。时间和费用是显式教学题设，实测仍来自原目录。

重新阅读 batch-reuse-h100-2k、权重卸载笔记、MLSys 2025 采用记录；检查三个调度 JSON 的请求交付事件与两组 KV 质量逐请求结果。同期 outline 增加实测摘要，重新阅读后确认六节与 22 小节标题相同；正文继续以原计算与实验为数字来源，仅更新该 outline 来源摘要。新增批量读取曲线，所有图按正文出现顺序编号为 8-1 至 8-9；质量图按任务、并发与重复对齐。练习分为计算、推导、数据分析与综合设计，保留原实验目录与核心练习编号。


## 段落衔接与机制图修订

逐段润色六节正文，补全主语、单位与因果关系；在公式、算例和实验之间说明下一步要回答的问题。新增请求时间线、分块注意力访问矩阵、缓存空间选择、KV 字节布局、草稿验证和配置比较六幅图，现有九幅图一并按正文顺序编号为 8-1 至 8-15。新增图采用正文教学条件；分块和格式说明重新核对既有计算及执行笔记，没有新增硬件测量。图号只放在外部图注，绘图输入保存到 figure-data.json。
