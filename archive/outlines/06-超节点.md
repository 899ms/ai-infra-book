# 第 6 章 超节点

> 写作大纲 · 多卡切分、通信实现与超节点规模选择 · 2026-09-10

第 5 章在芯片内组织数据复用，本章把计算和状态切到多张卡。由逐卡需求推出 TP、PP、DP、EP 及组合，再决定需要多大的紧密协作范围；拓扑与产品在具体限制出现时引入。

**本章判断：** 并行换来容量或处理能力，也增加交接与同步。总容量足够时仍要检查最忙设备；同一组卡应扩大协作还是增加推理实例数量，取决于负载和时限。

**前置与交付：** 依赖第 3、5 章；交付逐卡状态与跨边界流量，第 7 章沿实际路径继续计算。

**阅读安排：** 核心练习为实验 6-2、实验 6-3、实验 6-10；其余练习为延伸。每节的完整参数、实现版本与实验变体见对应[扩写资料](extensions/06-%E8%B6%85%E8%8A%82%E7%82%B9.md)。6.2、6.3 先形成并行候选，6.4、6.5 校正通信实现与物理路径代价，6.7 用实验 6-10 完成规模选择。章末更新[跨章设计决定](decision-record.md)。

## 6.1 从单卡到多卡

先比较独立推理实例和共同执行，再判断一次任务为何需要跨卡。总需求和局部需求分别记录。

### 6.1.1 八张卡的独立部署与协作

手上有八张卡，是运行八个独立推理实例，还是让多张卡共同执行一个请求？先固定质量、请求和完成目标，分别提出容量不足、吞吐不足和单请求延迟过长三种情形；本节建立预算，6.2 再用具体切分比较。

这里的“推理实例”指能够独立完成模型推理、承接请求的一组执行资源。每个实例持有完整模型，模型可以放在一张卡上，也可以通过 TP／PP 等方式分布在多张卡上。同样八张卡，可以部署八个单卡实例、两个各用四卡的实例，或一个使用八卡的实例；分别核算实例内协作与实例间请求分工。

复用第一章的资源地图和第四章的互联图，在图 6-1 的物理概览中标出单卡、服务器、超节点与跨超节点边界。超节点按紧密协作范围理解，服务器和机柜数量由具体实现决定；端口与拓扑细节在 6.5 展开。

从单卡、八卡服务器到更大超节点，固定 GPU 可用显存、有效矩阵性能、HBM 带宽、卡间单向带宽与小消息延迟；模型、精度、请求长度和并发共同构成实验输入。先用一组条件完成推算，再换硬件检验原选择是否仍然成立。

### 6.1.2 贯穿算例：Qwen3-8B 的容量与单步时间

**贯穿配置查阅：** 复用第 2、3 章配置：Qwen3-8B Dense；235B／22B 激活的 Qwen3 MoE 与 284B／13B 激活的 V4-Flash；约 1.6T／49B 激活的 V4-Pro 与 2.8T／104B 激活的 Kimi K3。DeepSeek-V3 的 671B 总参数作为历史过渡。它们代表不同规模与结构，不能写成参数量完全相同的模型。

配置表用于查阅，按推导阶段引入模型：先以 Qwen3-8B 讲清 Dense 切分，再用 Qwen3-235B 与 V4-Flash 比较专家放置，最后用 V4-Pro 与 Kimi K3 检查规模和结构改变后的选择。第 2 章已有的模型需求在这里作为输入，本章新增逐卡放置与跨卡交换。

逐卡检查 `权重＋KV／递推状态＋激活＋工作区 ≤ 可用内存`。分别数每步实际激活专家的权重读取、所有活跃序列的 KV 读取、算子 FLOPs，以及每条必要链路的字节和启动次数；总参数、激活参数、实际读取字节不能相互替代。

给出第一遍时间下界：本地计算 `F/P`、本地读取 `(W_read+KV_read)/B_mem`、路径传输 `V/B_link` 与必要同步；共享链路合并流量，串行依赖沿实际时序累加。以最忙的设备或链路定位瓶颈，再决定该减少哪项工作。

### 6.1.3 Prefill、Decode 与训练对资源的不同要求

同一模型分别代入一段 prefill、一次 decode 和一轮训练。低并发 decode 的小消息与逐层依赖，不能按大矩阵 prefill 的带宽效率估计；训练另有梯度、优化器和反向通信。增加推理实例数量主要改变吞吐，把一次计算分到多卡可能改变容量与单请求延迟，两者从计算结果比较。

> **实验 6-1 ★〔延伸〕：模型与硬件的资源收支**
>
> 先对 Qwen3-8B 完成逐卡容量和每 token 的计算、权重／KV 读取，再换为 Qwen3-235B 与 V4-Flash；固定一组 prefill／decode 输入，指出每个候选方案首先受容量、内存带宽、算力、互联带宽还是延迟限制。
>
> 条件：基础·手算／短脚本。

**计算结果（6-1）：** 同一组八卡上，六个场景的首要限制并不相同：Qwen3-8B prefill 始终受算力限制，decode batch=64 在单卡上先受容量限制、切开后转为内存带宽限制；Qwen3-235B 与 V4-Flash 在 TP≤4 时全部卡在容量上，只有 TP=8 才进入时间比较。互联在 Qwen3-8B prefill 的 TP=8 处成为首要限制（21.81 ms 超过算力的 16.88 ms）——切得越细，交接越贵。[逐候选收支表](../../experiments/ch06/06-01/README.md)。

> **图 6-1：从模型数据流到逐卡资源需求（配图计划）**
>
> 自绘 SVG；物理概览面板在 6.1.1 先标单卡、服务器、超节点和跨超节点边界，需求面板在第 5 章张量图上加入卡的边界，标出权重、状态与激活的放置；资源时间由实验 6-1 生成。

### 6.1.4 并行方式一览：切分哪个维度

从一层输入激活的三个维度（样本 B、序列位置 S、隐藏特征 H）与模型结构的两个维度（层 L、专家 E）引出六种并行方式各自的切分位置；汇总表列出每种方式每卡保存什么、卡间必须交换什么，后面各小节逐一算出交换量。贯穿区别：切样本、序列、层或专家时各卡得到不同数据或不同层的完整结果，切特征时往往只得到部分和、必须相加才能使用（呼应 5.2.4 的输出维与归约维，跨卡时相加经过卡间互联）。各方式不互斥：SP 通常与 TP 使用同一组卡，EP 可与注意力的 DP 共用同一批卡，PP 划分的是有先后依赖的计算图；组合与设备编号到 6.3 展开。

> 配图：图 6-3 五个维度与六种并行方式的切分位置一览（自绘 SVG）。

**扩写资料：** [本节完整计算、版本与实验变体](extensions/06-%E8%B6%85%E8%8A%82%E7%82%B9.md#detail-6.1)。

## 6.2 六种并行方式

按图 6-3 的顺序逐一介绍六种并行方式：每小节先看切在哪里、每张卡保存什么，再看卡之间要交换什么，最后代入贯穿算例算出容量或时间；先用两张卡说明，再推广到更多卡。通信的环／树实现与物理路径由 6.4、6.5 校正。

### 6.2.1 数据并行：复制模型，切分样本

从单卡可以驻留的请求开始，比较八个推理实例、少量 TP 的多个推理实例、以及把八张卡交给一个请求。训练 DP 为不同卡分配样本并同步梯度，推理实例不需要这项同步；把两者分别放回自己的工作流。容量充足并不自动说明需要模型并行。

### 6.2.2 张量并行：切分一层内的矩阵

沿 Qwen3-8B 的实际 QKV、输出投影和 SwiGLU，画列切分、行切分，以及必须归约或收集的张量。逐卡写矩阵尺寸，比较减少的权重／计算与新增通信；KV 头分片和复制按实际实现处理。用 TP=1、2、4、8 的 prefill 和 decode 推算解释适用条件，不能把 TP≤8 当成通用规则。

按“原矩阵与结果—每卡分片—局部结果—下一步缺少的数据—必要通信”的顺序推导。行切分得到部分和且各卡后续都需要完整结果时，用 all-reduce 求和并分发；各卡持有不同片段而后续需要完整张量时，用 all-gather 收集；归约后只需各卡保留对应分片时，用 reduce-scatter。操作由输入与输出布局决定，先画数据归属再命名，环／树实现与实际耗时到 6.4 校正。

先完成这一组基本切分，再在扩写资料中比较保留双模式状态与二维切分流水。Arctic 和 MeshSlice 分别提供这两种设计的具体对照，正文不在基础 TP 推导中同时展开多套调度与状态组织。

### 6.2.3 序列并行：逐 token 算子沿序列分片

TP 让每卡只保存矩阵的一部分，层内的 LayerNorm、Dropout、残差相加等逐 token 算子却仍在每卡保存完整激活。序列并行（本书采用 Megatron 的定义）让这些算子沿序列位置分片：进入列并行线性层前用 AllGather 收齐各卡位置，行并行线性层的部分和用 ReduceScatter 求和并重新按位置分片——AllReduce 本就等于 ReduceScatter 加 AllGather，SP 只是把两半放到不同的算子边界。[8192,4096] 的 BF16 激活四卡各保留 16 MiB 对完整复制的合计 256 MiB；节省只发生在保持序列分片的区间。

> 配图：图 6-9 SP 的分片位置与收集、分散归约的边界。

### 6.2.4 上下文并行：切分同一序列的注意力

SP 只处理逐 token 算子，注意力要让每个位置看到其他位置。上下文并行把同一长序列的位置分给多张卡，各保存本段的 Q、K、V 并计算本段的注意力输出。远端 K、V 可一次收齐，也可让 K、V 块环形传递、边到达边用 5.3.3 的在线 softmax 累计：Ring Attention 是调度与传输的实现方式，CP 是工作划分方式，两者不同义。连续切半在因果掩码下负载不均（八个位置共 36 对查询—键，前四位置 10 对、后四位置 26 对，差 2.6 倍），用前后位置交错搭配（如 1,2,7,8 对 3,4,5,6）配平，远端上下文不能当作 padding 丢掉。decode 沿历史位置切分后，各段先算 m_r、l_r、u_r，再跨卡合并为 m、l、u 与 o=u/l，不能各卡先各自 softmax 再平均。判断框架配置时看它切了哪些算子、张量与通信组，不只看缩写。

> 配图：图 6-10 两卡的 CP 分工、图 6-11 切分后仍然存在的注意力依赖。

### 6.2.5 流水线并行：按层划分阶段

前面几种方式都在一层内部分工，PP 改为按层分工：每个阶段保存自己那些层的权重，阶段之间只交接激活；训练时梯度沿同一边界反向传递。微批次让不同阶段同时处理不同输入：q 个等时阶段、b 个微批次时 T_PP=(q+b-1)t、利用率 η=b/(q+b-1)，四阶段四微批约 57%、16 微批约 84%；阶段耗时不均（1、1、2、1 ms）会在慢阶段前积压，按各层实际执行时间划分阶段可减小差距。自回归会话未来的 token 是先后依赖链，流水利用率直接取决于同时有多少微批可以开始。比较 TP×PP 与多个推理实例时，保留首 token、逐 token 延迟、吞吐和训练气泡的不同目标。

以 BF16 的 4096 维激活为例，一个逻辑张量每 token 为 8 KiB，8192-token prefill 对应 64 MiB；实际 PP 载荷还要按实现加上 residual 等交接状态；TP 的集合通信还要按算法、分片与轮次计算实际链路字节。改变消息规模后，同一网络延迟对两者的影响不同。

> **实验 6-2 ★〔核心〕：Qwen3-8B 的并行组合**
>
> 在同一组八卡资源上比较独立推理实例、TP=1／2／4／8 与若干 TP×PP 组合。逐卡算权重、KV、计算及每层通信，用实际或注明假设的 α、带宽修正；扫描 batch 和长度，给出各服务目标下的候选区间，再提出一项最值得补测的量。
>
> 延伸入口：四卡 SP×TP／完整 TP 的双模式切换放到第 9.6.2 节，届时沿用本题固定切分，再检查 KV 头身份、额外权重与切换准备。
>
> 条件：基础·手算／短脚本。

**计算结果（6-2）：** 八卡上八种组合的容量全部通过，取舍在两端：纯副本给出 64 路并发但 32K 历史时最忙卡驻留 53.3 GiB，TP=8 把逐卡驻留压到 5.03 GiB、逐卡矩阵工作压到 1/8，全局并发却只剩 8。TP 增大不改变每层激活载荷（8 KB），只把词表 logit 分片按 TP 均分；流水线组合把每步非空通信操作从 75 增加到 77–79。最值得补测的是 TP=8 每层 all-reduce 的实际启动开销 α。[逐卡收支、通信与候选区间](../../experiments/ch06/06-02/README.md)。

**部分实测：** [JAX实际TP/SP路径](../../experiments/ch06/06-02/jax-tp-sp/README.md)执行了Qwen3-8B形状的完整FFN子层，8个配置均通过FP64参考检查，配对TP/SP输出逐位相同。SP减少了每rank保留的输出，但需要聚集和分散归约；此处是单机CPU实现核验，不能代替八卡性能测量。

> **图 6-2：Qwen3-8B 的矩阵切分与执行时序（配图计划）**
>
> 自绘 SVG；同一层展示 TP 的矩阵分片与归约，同一模型展示 PP 的阶段与激活交接，各推理实例另画独立请求；数字来自实验 6-2。
>
> 第 9.6.2 节的切换变体沿用此图的 KV 头颜色；本图先画清固定切分的必要通信。

### 6.2.6 专家并行：切分专家集合

Dense 模型的每个 token 使用同一组 FFN 权重；MoE 准备多个专家，由路由器计算选择分数，每个 token 只执行被选中的专家，但整个专家集合都要驻留。专家并行把不同专家放到不同的卡。先解决专家权重放在哪里：固定 Qwen3-235B-A22B（94 层、4096 隐藏维、128 选 8、专家中间维 1536，每专家 BF16 36 MiB、单层 4.5 GiB、专家合计约 423 GiB）与 V4-Flash（43 层、256 选 6 加共享专家、专家维度 2048）；统一 BF16 第一轮估算约 470 对 568 GB，相对八张名义 80 GB 卡总容量看似够、留给 KV 与工作区的余量不同，再按实际混合精度与逐卡放置重算。GQA 与混合压缩注意力分别核算 KV，不能由接近的总参数量推断相近的状态与通信。

再跟踪 token 怎样到达专家：沿一个 token 画 dispatch 到目标专家、专家计算、combine 返回并按路由权重汇合（a₁y₁+a₆y₆）。同一目的卡承接多个被选专家时共用一份输入：按专家逐个发送 64 KiB、按目的卡只发 32 KiB，不能把 top-k 当成 k 个不同目的地。all-to-all 表示各参与者向不同对端交换各自的数据，MoE 的消息大小可不相等；先确认发送者、接收者与输出归属，实现代价到 6.4 比较。

> 配图：图 6-14 专家集合分两卡、图 6-15 token 派发与结果合并。

**扩写资料：** [本节完整计算、版本与实验变体](extensions/06-%E8%B6%85%E8%8A%82%E7%82%B9.md#detail-6.2)。

## 6.3 组合并行与模型规模

六种方式很少单独使用。本节先说明怎样把它们组合起来并给每张卡编号，再看 MoE 特有的批内复用与负载不均，最后检查模型继续增大时组合怎样变化。原大纲 6.3.1（专家放置）与 6.3.2（dispatch／combine）已并入 6.2.6 专家并行。

### 6.3.1 组合方案与设备坐标

TP×PP 组合时写清每个阶段输出哪些分片、下一阶段每张卡接收哪些分片：阶段内两卡各持完整 8 KiB 隐藏向量并分别下传，阶段间共发 16 KiB；传分片则由下一阶段按需拼合。组合记号 DP2×PP2×TP4 占 16 张卡，同一 TP 组内启用 SP 不再乘卡数；切分确定每份工作归谁，调度确定就绪、执行、通信与回收的顺序。专家并行可与专家内部 TP 组合成 TP2×EP4：每卡用专家组编号与组内 TP 编号两个坐标标识，每卡处理 768 中间维；四个专家组各自重复执行注意力并保存该请求的 KV（每卡权重约 64.8 GB、八卡 518.6 对参数独占的 470.2 GB，KV 为该请求实际量的四倍），换来专家输入就在本组、省去跨组派发。输出分两步汇合：组内两卡先相加，四个组之间再按编号相同的卡归约；每层注意力组内、专家组内、组间三次归约，按环形算法每卡每 token 发送 16+16+24=56 KiB。

> 配图：图 6-16 TP×EP 分组与归约方向、图 6-17 分两步汇合专家输出。

### 6.3.2 批内复用与负载不均

以 Qwen3-235B 的单专家 BF16 权重 36 MiB 为起点，区分总驻留、每 token 选中的专家和批内独立活跃专家。8 个输入 token 若都选同 8 个专家，理想一次读取涉及 288 MiB；均匀独立路由的期望则约为 1.815 GiB。再用真实路由重算逐卡读取与每专家矩阵：decode 的复用收益与 prefill 的计算长尾可能指向不同放置，不能仅按激活参数或平均 FLOPs 决定 EP。

实测（部分）：[完整 V4 的真实路由](../../experiments/ch06/06-03/README.md)在4个冻结输入上记录了43层、约131万次专家选择，答案与此前运行一致。不同专家的选中频率明显不同；decode每题仅4个输入，包含调度器额外执行的EOS前向，不能当作稳定分布。图中展示实际路由，跨卡放置与通信性能尚未实测。

### 6.3.3 从数千亿到万亿参数：并行组合怎样变化

V4-Pro 与 Kimi K3 分别沿自己的层数、注意力状态和专家结构推算。K3 的 896 选 16 路由专家在 3584 维潜空间工作，另有 2 个全宽共享专家，首个 dense 层和 MTP 单独计数；路由激活应按实际跨卡表示计量，不能直接套 7168 维全宽传输。总权重、实际激活矩阵、专家粒度、状态和同步次数一起决定放置。

先得到给定质量与服务目标下的可行组合，再在明确的候选范围内求预计时间最短或成本最低的切分，并检查其瓶颈；资料缺失或两方案差距小于估计误差时，输出候选范围与补测条件。以真实模型的不同规模比较为主，若统一总参数预算做反事实实验，明确它是教学控制变量，不改写产品规格。

> **实验 6-3 ★★〔核心〕：MoE 结构与并行选择**
>
> 对 Qwen3-235B／V4-Flash 和 V4-Pro／Kimi K3 两组案例，扫描 EP、TP、PP 与推理实例数量。逐项统计权重驻留、每 token 的专家／KV 读取、计算、dispatch／combine 与最忙设备；分别改变 prefill 长度、decode batch、互联带宽与延迟。说明每次选择改变的瓶颈，未知字段保留区间。
>
> 条件：进阶·配置复算／路由记录。

**计算结果（6-3）：** 纯 EP 的八卡物理权重比纯 TP 多 110 GB（582.1 对 471.7 GB），BF16 可并发请求只有 3 对 47——EP 省的是专家权重的重复，注意力与嵌入仍每 rank 一份。dispatch 的总发送字节与路由分布无关，最忙 rank 的接收量却完全取决于它：1,024 token/rank 时均匀路由每 rank 收 56 MiB，热点路由下有一个 rank 收 448 MiB，时间差 7 倍；换互联只改变斜率，治不了倾斜。真实采集的 1,313,220 个专家 assignment 覆盖全部 43 层，前 3 层用 HashTopK。[切分扫描、dispatch 与真实路由](../../experiments/ch06/06-03/README.md)。

> **图 6-3：不同 MoE 结构的分片与路由（配图计划）**
>
> 自绘 SVG；四个模型沿相同的权重、状态、计算和激活路径比较，明确 K3 的潜空间表示；实验 6-3 生成逐卡负载与瓶颈变化图。

**扩写资料：** [本节完整计算、版本与实验变体](extensions/06-%E8%B6%85%E8%8A%82%E7%82%B9.md#detail-6.3)。

## 6.4 集合通信的实现与执行代价

承接前两节已经推出的集合操作，依次核算算法轮次与字节、通信库的资源占用及专用卸载。逻辑消息到实际拓扑的映射在 6.5 继续校正。

### 6.4.1 根据模型切分方式确定通信需求

用一张通信需求表汇总 TP 的部分和／分片、训练 DP 的梯度以及 EP 的 token／专家结果，列出输入归属、输出归属、参与者、逻辑载荷、调用频次和必要先后关系。与 6.2、6.3 的候选逐项对应，再将同一逻辑操作交给不同算法与实现。

回到刚算过的 TP 与训练 DP，汇总 all-reduce、all-gather、reduce-scatter 分别补齐哪项数据。下一小节用四个参与者跟踪每轮字节，再代入前面真实模型的消息大小。

汇总前节从路由到专家的 all-to-all，列出通信对象、数据量与不均匀性。和梯度归约对照，让读者理解相同总字节并不意味着相同完成时间。

### 6.4.2 环、树与通信轮次

沿一次集合通信画出轮次和链路占用，由消息大小、带宽和启动延迟比较算法。再加入分块、流水与多通道，说明选择为什么随规模变化。

以 p 个参与者、每个输入 M 字节的简化 ring all-reduce 为例，写出 `T ≈ 2(p−1)α + 2(p−1)M/(pB)`，说明轮次项与传输项从哪里来。先用小例子比较消息大小改变后的选择，再由实际拓扑和归约成本修正；公式不替代所有集合算法。

> **实验 6-4 ★〔延伸〕：环与树的选择条件**
>
> 这条消息用环还是树更合适？ 手画四个参与者的环与树，算轮次和字节；用短脚本扫描消息大小，提交选择条件。
>
> 条件：基础·Python。

**计算结果（6-4）：** 树的轮次少（4 对 6）所以启动开销低，环的字节少（每 rank 趋近 2S 不再增长）。四参与者时 8 KiB 消息树快（16.7 对 24.2 μs），8 MiB 消息环快（275.7 对 687.1 μs）。交叉点在每 rank 128 KiB～512 KiB 之间，并随参与者增多右移（16 参与者时移到 512 KiB～2 MiB）。decode 每层载荷落在交叉点附近，prefill 稳在环的区间——同一模型的两个阶段需要不同算法。[轮次、字节与交叉点](../../experiments/ch06/06-04/README.md)。

> **图 6-4：集合通信做的事与环／树的过程（配图计划）**
>
> 自绘步骤图，实验 6-4 生成时间曲线。物理路径面板随 6.5.1 阅读：在同一物理环上画两种对端选择，按有向链路标字节，突出逻辑消息与物理搬运的差别；保留图 6-4 的面板编号与已有路径图。

### 6.4.3 通信库与计算的资源竞争

沿候选切分的一次 NCCL 调用追到 SM、显存和互联。用 AutoCCL（NSDI 2025）说明实现选择与资源分配为何要分开搜索，实际并发反馈怎样修正独占结果。给两个可重叠的 Qwen3 微批片段：通信独占耗时从 0.24 降到 0.18 ms，并发汇合却从 0.44 增到 0.62 ms；先找哪一段被挤慢，再算搜索成本能否摊薄。这是教学输入，论文基于 A40／NCCL 2.18.3 的结果另列条件；其通信优化目标不保证整步最优。[推算与版本范围](../../case-studies/communication-tuning.md)

以 Qwen3-8B 的 BF16 输出归约为例，1／4／16 个 token 分别产生 8／32／128 KiB 输入；用 vLLM 从 2024 的 IPC custom AR 到当前多后端分派，说明形状、图缓冲、P2P 和拓扑怎样决定实际路径。类中列出的参与者数不等于每种操作都支持，超节点内的 MNNVL 与普通跨服务器网络也要分别核实。在 8 卡、每轮 2 μs、有效单向 50 GB/s 的 ring 教学条件下，单 token 的 72 次逐层归约约 2.04 ms，带宽翻倍只省约 10 μs，由此引出小消息的启动与融合优化。

### 6.4.4 通信融合、加速器发起与专用卸载

比较软件归约、交换侧能力与昇腾 950 CCU 的组织，核算省下的处理与新增的调度要求。沿 NCCL 2025 的设备 API／CE 搬移到 2026 的网络零 CTA 路径，区分融合、设备发起与通信卸载。用 AllGather／AlltoAll 检查支持条件、对称窗口和注册成本；节点间仍可能靠 CPU proxy 推进，SM 省下来后，显存带宽和网络争用仍在。不能把这些条件下的搬移推广成任意 AllReduce 都无需计算资源。

> **实验 6-5 ★〔延伸〕：通信与计算的资源竞争**
>
> 通信会占掉多少计算资源？ 分析一组集合通信与计算并行记录，比较独立运行和共享运行。
>
> 条件：基础·数据分析。

**实测结果（6-5）：** 四个 CPU 进程的实际通信／矩阵乘对照中，64 MiB 消息并行运行时，通信可见区间中位数由 36.92 增至 45.21 ms，计算由 11.03 增至 12.88 ms。两者都变慢说明区间重叠不等于共享资源没有代价；GPU 通信与融合收益仍待验证。[记录与时序图](../../experiments/ch06/06-05/README.md)。

> **部分实测**：四个CPU进程的实际通信／矩阵乘对照中，64 MiB消息并行运行时，通信可见区间中位数由36.92增至45.21 ms，计算由11.03增至12.88 ms。区间重叠不代表共享资源没有代价；GPU通信和融合收益仍待验证。[记录与时序图](../../experiments/ch06/06-05/README.md)。

> **图 6-5：通信在 GPU、存储和专用单元上消耗什么（配图计划）**
>
> 自绘路径与实验 6-5 的资源时序。

**扩写资料：** [本节完整计算、版本与实验变体](extensions/06-%E8%B6%85%E8%8A%82%E7%82%B9.md#detail-6.4)。

## 6.5 超节点的物理组织

先用拓扑、端口、介质和功率约束得到可行组织，再比较 NVIDIA、TPU 与 UB 的系统实例。大型同步域与更多推理实例的综合选择在 6.7 完成。

### 6.5.1 直连、交换与分层拓扑

从端口和路径预算比较小规模直连、交换结构和更大型超节点。让同一组训练／推理通信经过不同拓扑，观察端点注入与共享链路如何限制性能。

**占位 D11-04：** 显式推算 radix、端口速率、端点多链路与交换容量怎样限制拓扑层级；区分芯片和整套 fabric 的端口预算、物理跳数与集合算法轮数，接到本节的拓扑实验。详见[端口与层级分析](../../research/draft11-outline-audit/report.md#radix)。

用 Swing 的物理路径案例补上公式里隐含的条件：沿 Qwen3-8B 的 BF16 `[1024,4096]` 激活取 8 MiB 归约输入，在 16 节点双向环上比较 reduce-scatter 的前三轮，两种算法每节点都发送 4、2、1 MiB，但最拥塞有向链路分别承担 4／4／4 和 4／2／2 MiB。让读者先数路径，再理解为什么相同算法字节与轮次不等于相同时间；多端口和拓扑形状也会改变结果。论文收益来自 torus 网络仿真，大消息下仍有其他算法胜出，不能直接当作 TPU 或 NCCL 实测。推算与采用范围见[集合通信与作业错峰](../../case-studies/network-planning-and-collectives.md)。

[OpenTallas 的通信跨度修正](../../case-studies/opentallas.md)提供一个先预测再揭示遗漏路径的变体：60 层、每层两次不可重叠通信，100 µs token 目标只留 30% 给通信，单次预算为 0.25 µs；15 hop、每 hop 100 ns 的传播下界却为 1.5 µs，全 token 为 180 µs。按一次局部 hop 计价会误判可行性，需重选拓扑、切分或时限。上述为教学输入；项目历史版本与当前修正分别保留。实验 6-6 在同一图上对照局部 hop 与完整跨度，不另设练习。

**计算结果（6.5.1）：** 把同一算法换成 Qwen3-8B 的真实 36 层重算，可以把"多少跳才不成立"解出来而不只是举一个失败例子：72 次集合通信、100 µs 预算的 30% 分给通信，单次可用 0.4167 µs，因此单次预算只容许 4 跳直径，整个 token 预算只容许 13 跳；14 跳起仅传播就超过整个 token 预算，案例的 15 跳网格落在失败侧（超出 1.080 倍）。权重按 64 MiB 片上容量摊到每层 6 个 tile 时直径只有 4 跳，说明失败与否取决于切分粒度而非"晶圆一定输"。列切分与行切分把同样的字节换个方向搬：前者每层扇出 49,152 B、扇入 8,192 B 且无需归约，后者正好相反并多出归约轮。[逐项跳数、扇出扇入与直径阈值](../../calculations/results/weight-resident-qwen8-8k.md)，宽网格变体见[15 跳的同一预算](../../calculations/results/weight-resident-qwen8-wide-mesh.md)。

再用四卡 PCIe 机器承接 Qwen3-8B 的 8 MiB AllReduce：逻辑发送量都是 48 MiB，把中转缓冲全放在一个 NUMA 节点或靠近发送 GPU，CPU 间互联的流量却不同。先逐段算字节，再以 TCCL 的作者材料和当前 NCCL 路径作对照，解释为什么拓扑、ring 次序与缓冲放置要一起考虑；具体假设与下界见[中转算例](../../case-studies/pcie-staging-and-numa.md)。

> **实验 6-6 ★〔延伸〕：拓扑与割集带宽**
>
> 增加交换层还是增加直连链路？ 给定端口数和流量矩阵，构造两种可行拓扑并比较割集与路径。
>
> 条件：基础·设计计算；设备选做·四卡 PCIe 机器。

**计算结果（6-6）：** 8 设备、每链路 50 GB/s、全对全每对 16 GB/s 时，穿过均衡二分的需求是 256 GB/s。单层交换与 4 端口直连网格的最小割同为 200 GB/s（都不够），但直连的最坏路径 0.6 μs 只有交换方案 1.4 μs 的四成。增加交换层不抬高割集，只把端口集中起来；两者争的是同一份端口预算。全互连（7 端口、350 GB/s、1 跳）是本规模下唯一满足流量的方案，但扩到 16 设备就要 15 个端口——不可扩展正是超节点需要交换层的原因。[两种拓扑、割集与限制](../../experiments/ch06/06-06/README.md)。

### 6.5.2 互联介质、供电与物理规模

从连接距离和线速进入介质选择，再加入封装、布线、模块功耗和维护。物理尺度影响组织，但不能仅由距离判断系统优劣。

**占位 D11-05：** 对照铜、可插拔光与 CPO 在距离、连接密度、功耗和维护上的取舍，结合实际端口与拓扑说明何时可能减少交换层或排队点。详见[互联介质与 CPO](../../research/draft11-outline-audit/report.md#cpo)。

> **实验 6-7 ★〔延伸〕：功率预算下的超节点规模**
>
> 同一功率预算下，怎样组成一个超节点？ 分别计入设备、网络与散热配套，求候选规模并比较模型服务能力。
>
> 条件：基础·表格／Python。

**计算结果（6-7）：** 计入网络 150 W／设备、主机分摊与 1.30 散热系数后，A100 与 B200 的 TDP 差 2.5 倍，完整功率份额只差 1.9 倍——配套按设备数分摊，稀释了低功耗设备的优势。500 kW 下 A100 能装 591 张（47.3 TB），B200 只能装 307 张（55.3 TB）却因带宽更高而吞吐领先；RTX PRO 6000 装得最多（452 张）吞吐却最低。按“能装多少卡”选设备会得出与按“能服务多少 token”相反的结论。[逐档规模与服务能力](../../experiments/ch06/06-07/README.md)。

> **图 6-6：拓扑、介质和机柜一起决定可行组织（配图计划）**
>
> 自绘三种候选系统图，配实验 6-6、实验 6-7。以局部放大图标出四卡 PCIe 机器的 NUMA 缓冲位置、ring 次序与有向数据量，让读者看见相同逻辑通信经过了哪些物理资源。

### 6.5.3 NVIDIA：从八卡服务器到机柜

三条系统演进线沿同一组问题阅读：支撑什么负载、设备怎样连接、高频通信经过哪里、扩展受什么约束。各代历史输入与本章固定模型分别计算，保留完整时间线，并用相同维度汇总系统差异；6.7 再作最终规模选择。

沿 DGX-1、DGX-2、DGX A100 到 NVL72 比较实际组织的变化。把当年的模型和并行软件放回去，讨论扩大和缩小各有什么收益，而不编造未公开产品决策。

### 6.5.4 TPU：拓扑与负载协同

结合多代 TPU 的芯片与系统论文解释同构部署、互联拓扑和重配置的选择；与 GPU 方案比较灵活性、利用率及系统成本。

### 6.5.5 Unified Bus 与 CloudMatrix

回到作者对总线和网络割裂的思考，结合正式材料解释资源连接、访问与系统规模。用同一负载比较 NVL72 与 CloudMatrix384 的可行配置，逐项看差异从哪里来。

> **实验 6-8 ★★〔延伸〕：超节点的历史设计选择**
>
> 回到某一代 DGX 或 TPU，你会采用同样的组织吗？ 用当年公开输入复算一组 TP／EP 方案；再将本章固定模型放到 NVIDIA、TPU 与 UB 的可用资源条件下，比较选择为何改变，保留实际采用与预测的差异。
>
> 条件：进阶·历史推演。

**推演结果（6-8）：** 三种资源条件下的并行选择完全相同，数值差别只有百分之几——阈值列给出原因：远端带宽只要超过 28 MB/s 双机就更好，这个门槛低到任何互联都远远越过；真正决定选择的是启动时间（阈值约 590 μs）。当年（2016–2018）公开模型远小于单机容量，按当年输入给不出扩大 scale-up 的充分理由；NVSwitch 与 TPU pod 都是在 GPT-3 之前按未来负载下注的。差异不在算术，而在为哪一类负载设计。[当年输入、三种条件与对照](../../experiments/ch06/06-08/README.md)。

> **图 6-7：NVIDIA、TPU 与 UB 系统怎样随需求变化（配图计划）**
>
> 自绘并排时间线，并按负载、设备连接、高频通信路径和扩展约束作同维度对照；少量官方整机图保留原图来源与版本。

**扩写资料：** [本节完整计算、版本与实验变体](extensions/06-%E8%B6%85%E8%8A%82%E7%82%B9.md#detail-6.5)。

## 6.6 内存池

从局部不足、全局空闲的实际问题进入内存池，再分别检查冷状态和活跃数据的搬移频次。

### 6.6.1 局部容量不足与全局空闲

以作者早期 UB 内存池的设想进入：一台机器的计算已经很忙，内存却有剩余；另一台机器先被容量卡住。先判断借用容量能解决什么，再跟到后来的 KV cache 应用。

用四个节点各有 64 GiB 可用内存、同时需求为 80／48／32／32 GiB 的教学例子：总需求 192 GiB 小于总容量 256 GiB，仍有一个节点短缺 16 GiB。比较增加本地内存、迁移任务与从池中借用，区分容量节省、复制减少与实际访问成本；不把这个假设配置当成某款 GPU 的远程寻址能力。

### 6.6.2 内存借用与访问边界

按 UB OS 参考设计的内存导入、导出、借用与共享机制，画本地内存、远端内存和地址映射的关系。统一地址、可远程访问和缓存一致性分别解释；哪一方保留访问权、何时归还以及故障时怎样处理，回到具体机制。将容量与带宽分别算，不能把各节点标称内存带宽相加当成借用者可用带宽。

### 6.6.3 共享容量与 KV 缓存

承接作者“有了内存池以后，什么应用最值得用”的问题，比较冷前缀、等待恢复的会话与正在逐 token 使用的 KV。先按每次读取的字节和频率判断远端链路能否承受，第 9.5 节再展开共享 KV 池、PD 交接和前缀复用。

> **实验 6-9 ★〔延伸〕：内存池的容量收益与访问代价**
>
> 四台机器的总内存够用，为什么仍有任务放不下？ 四个 64 GiB 节点分别承接 80、48、32、32 GiB 的工作集；比较迁移任务、增加本地容量、借用 16 GiB。对这份固定工作集只改变访问频次，求借用路径必须提供的能力；第 9.5 节再将它具体化为增长、失效和取回中的 KV，计算完整服务收益。
>
> 条件：基础·手算／短脚本。

**计算结果（6-9）：** 四个 64 GiB 节点总容量 256 GiB、总需求 192 GiB，富余 64 GiB，80 GiB 的任务却放不下——整任务迁移超过每一个节点，富余被锁在各自节点里，这正是池化要解决的 80 GiB 不可用余量。借用 16 GiB 后容量成立，代价转移到访问：同一条 10 GB/s／2 μs 链路，每分钟读一次负载 0.029 可行，每秒读一次变成 1.718 立刻积压。在途事务比延迟更关键——40 GB/s 下把在途从 128 提到 4096，服务时间从 10.486 s 降到 0.430 s，且此时 2 μs 与 20 μs 延迟结果相同。[容量三路径与 24 组访问条件](../../experiments/ch06/06-09/README.md)。

> **图 6-8：内存池的容量分布与访问路径（已生成容量／快照路径图）**
>
> 自绘 SVG；同一组节点画本地不足、借用容量，以及计算节点读取借用数据的路径。标注不变工作集的访问频次；配实验 6-9。增长 KV 与缓存取回的完整服务放在第 9.5 节。

**扩写资料：** [本节完整计算、版本与实验变体](extensions/06-%E8%B6%85%E8%8A%82%E7%82%B9.md#detail-6.6)。

## 6.7 从并行方案到超节点规模

将逐卡预算、集合通信、物理拓扑与共享容量的结果带回章首同一任务，比较扩大协作组和增加独立推理实例。6.2、6.3 形成的候选经过 6.4、6.5 校正后，在这里完成部署判断。

### 6.7.1 更大的协作组与更多推理实例

固定模型、精度、请求集合和完成目标，比较各方案的逐卡峰值、单请求时序、可并行服务的请求数与总占用。容量不足先排除；对剩余方案分别计算首 token、逐 token 和整组请求完成时间，保留共享容量引入的额外访问，不能用单请求最快替代整体最合适。

### 6.7.2 功率、成本与故障范围怎样改变选择

复用 6.5 的设备、互联与散热预算，比较同一资源预算下的完成能力和有效请求成本；再加入故障影响与恢复时间。物理超节点、并行通信组和故障域分别标记，故障范围由实际放置与依赖决定。

把本章的并行候选放回大型超节点与多个小型超节点，比较部署与故障影响。

> **实验 6-10 ★★〔核心〕：超节点规模与系统成本**
>
> 更大的超节点比更多的小型超节点划算吗？ 改变模型、并发、延迟目标和故障恢复成本，给出选择改变的条件。

**计算结果（6-10）：** 同一组八卡的三种组织中，tp2×4（多个小超节点）在基线、放宽延迟、更长输出和故障注入四种情形下每有效请求费用都最低（基线 0.320 对 tp8×1 的 0.496）；故障时 tp8×1 的单副本一挂就是 0 有效，tp2×4 只损失 1 个请求。选择在两处翻转：延迟目标收紧到 120 ms 时 tp2×4 的单请求 160 ms 全部超时，tp4×2 反而救回 2 个；换成 Qwen3-32B 时 tp2×4 容量放不下，只能退回 tp4×2。超节点该做多大由单请求延迟目标与模型放得下共同框定。[三种组织与翻转条件](../../experiments/ch06/06-10/README.md)。

>
> 本教学比较明确把每个 TP 组放在一个独立超节点：8 卡×1 个、4 卡×2 个、2 卡×4 个，总计均为 8 卡；每个超节点运行一个完整推理实例，组间没有请求执行依赖。物理卡 0 故障使所在超节点的推理实例不可用，其余超节点继续服务。这个放置和故障域是假设，不能仅由 TP 数值推断真实部署具有相同隔离性。
>
> 条件：进阶·综合计算。

> **图 6-9：超节点规模变大时，收益和代价怎样变化（已生成条件成本曲线）**
>
> 由实验 6-10 脚本生成多条件曲线，标出可行范围。

### 6.7.3 给定模型与硬件，怎样选择切分策略

为每个剩余候选列出逐卡状态与峰值、逐边界载荷与频次、集合轮次、最忙设备／链路、共享与故障范围，以及使选择翻转的条件。将逻辑载荷、算法发送量和物理链路字节分列，预测与实测分列；差距小于估计误差时保留候选范围，并提出最值得补测的量。

下一章继续处理跨超节点的训练集合通信，以及仅有八卡服务器时的大模型推理。当单个超节点的容量、计算能力或已有部署条件不足时，标出必须跨越其边界的数据与依赖。第 7 章沿主机、网卡和交换链路展开跨超节点路径、访问与完成语义及拥塞约束，继续修正这里的候选。

**扩写资料：** [本节完整计算、版本与实验变体](extensions/06-%E8%B6%85%E8%8A%82%E7%82%B9.md#detail-6.7)。

## 本章的设计决定

**V4／V4.1 会话案例：** V4.1 也不能只按 890 B/token 推断需要几张设备。该数把跨层共享状态只计一份；如果模型各层被放在不同设备上，后续层仍须访问拥有层产生的状态。可以把相关层放近、在目标设备复制状态，或建立远程访问路径。第一种限制放置灵活性，第二种增加容量与更新，第三种增加通信依赖，原来的单份逻辑容量并不能替代逐设备预算。

**三条主线的落实：**
沿用第 4 章的固定权重实验，多设备需求应按来源重新分开。为容纳权重而切分模型的需求可能减少；为保存更多 KV 而扩容的需求仍然存在；为缩短单请求计算而采用 TP，需要证明节省的计算超过新增同步；为提高总吞吐而复制实例，则仍由请求量决定。权重容量、状态容量、单请求时延和总吞吐分别给出四种扩容理由，不能只根据模型参数量推算超节点大小。

配图与定量推导以本章正文为准；新增图沿用统一浅色填充、矢量箭头和外置图注。先固定任务改变单一约束，再比较重新选择后的方案。

为同一任务保留“更大协作组”和“更多推理实例”两类候选，输出逐卡峰值、逐边界字节、集合轮次及最忙设备。总量相同而放置不同的结果并列，供第 7 章映射到实际网络。 将预测、证据和修改分别填入[跨章设计决定](decision-record.md)。

## 写作资料

- 并行切换与状态位置：[Shift Parallelism，物理页 1–12](../../references/proceedings/ASPLOS/2026/paper-004.pdf)、[Qwen3 四卡推算](../../case-studies/parallel-switching-and-state.md)；Arctic SP 的定义、矩阵驻留和实际发送分别计量。

- 6.4 的框架通信路径：[vLLM 2024／2025／当前与 SGLang 的固定源码](../../references/framework-history/2026-09-08/collective-paths/README.md)、[消息尺度、支持条件与实验推算](../../case-studies/collective-paths-and-diagnosis.md)。

- 批内专家复用与执行开销：[Demystifying the Mixture of Experts Serving Tax，MLSys 2026](../../references/proceedings/MLSys/2026/papers/mlsys2026-42a452cbafa9dd64e9ba4aa95cc1ef21.pdf)；[Qwen3 算例与论文边界](../../case-studies/moe-and-startup.md)。

- 本章的历史问题：[Unified Bus 背后的思考](../../references/files/documents/ub-reflection.md)；[计算机网络的新黄金时代（一）](../../references/files/documents/network-golden-1.md)；[A100/H100 太贵，何不用 4090？](../../references/files/documents/h100-vs-4090.md)。
- 6.2–6.4 的通信：[NCCL Collective Operations](../../references/files/documents/nccl-collectives.md)；[Optimization of Collective Communication Operations in MPICH](../../references/files/papers/collective-algorithms.pdf)；[NCCL User Guide](../../references/files/documents/nccl-guide.md)。
- 6.5 的系统演进：[NVIDIA NVLink and NVLink Switch Specifications](../../references/files/specs/nvidia-nvlink-spec.md)；[NVIDIA GB200 NVL72](../../references/files/specs/nvidia-gb200.md)；[Google's Training Supercomputers from TPU v2 to Ironwood: Architectural Stability, Scale, Resilience, Power Efficiency, and Sustainability Across Five Generations](../../references/files/papers/google-tpu-generations.pdf)；[TPU v4: An Optically Reconfigurable Supercomputer for Machine Learning with Hardware Support for Embeddings](../../references/files/papers/tpu-v4.pdf)。
- 6.5–6.6 的 UB 与昇腾实例：[Serving Large Language Models on Huawei CloudMatrix384, v2](../../references/files/papers/cloudmatrix384-v2.pdf)；[灵衢基础规范 2.0.1（中文版）](../../references/files/specs/UB-Base-Specification-2.0.1-zh-clean.pdf)；[昇腾 950 NPU 架构白皮书（官方下载原件）](../../references/files/specs/ascend-950-official.pdf)。
- 6.6 的内存池机制：[灵衢使能操作系统参考设计 2.0](../../references/files/specs/UB-Software-Reference-Design-for-OS-2.0-zh.pdf) §4；配合作者 UB 文章的“内存池的杀手级应用”段落，分别引用设计经历与规范行为。

- 6.1–6.3 的固定模型：[Qwen3-8B 配置](../../references/outline-checks/2026-09-07/scaling-history/qwen3-8b-config.json)；[Qwen3-235B-A22B 配置](../../references/outline-checks/2026-09-07/scaling-history/qwen3-235b-config.json)；[DeepSeek-V3](../../references/files/papers/deepseek-v3.pdf)；[DeepSeek-V4](../../references/files/papers/deepseek-v4.pdf)；[Kimi K3，表 1 与 §2.3](../../references/files/papers/kimi-k3.pdf)。
- 6.2–6.3 的并行执行：[Megatron-LM](../../references/files/papers/megatron.pdf)；具体模型与网络推算见[并行案例笔记](../../case-studies/model-parallelism.md)。

原文版本、参数差异与扩写时需补的材料见[编辑笔记](editorial-notes.md#ch-06)。
