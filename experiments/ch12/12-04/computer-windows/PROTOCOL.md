# Computer Use receive configuration

Use every round of the first successful structured Qwen3-VL-8B browser trace from ../computer-use:8 actual JSON screenshot/instruction uploads and action responses. Reproduce the measured per-round GPU service wait after each complete upload. No fresh model inference or browser actions occur during this network replay. Validate the exact action JSON and bytes for each round in the original order; source final browser state remains the independent task-quality evidence.

Compare default,64KiB,4MiB receive settings. TCP sets only SO_RCVBUF on listener before accept and client before connect; QUIC sets initial connection and stream credits on both endpoints, which can grow. Preserve TCP readbacks/TCP_INFO and QUIC parameters/MAX frames. Three trials use balanced mode order default/64k/4m,64k/4m/default,4m/default/64k. Each fresh container warms one step0 request per protocol and executes one complete8-round trace over one fresh reused connection per protocol.9×18=162 exchanges (144 formal18 warmups). Each trace has one fresh and7 reused requests; no concurrent model rounds.

Docker network-none/NET_ADMIN,2CPU/2GiB,netem80ms RTT/20Mbit/s/0.1% loss, common120s request deadline and20s handshake limit. Preserve hashes, full source instruction/image identity, action sequence, actual negotiated protocol, connection reuse and cleanup. Round medians pool different payload/service sizes; each condition receives the identical ordered trace. Trace completion excludes group connection cleanup and source browser screenshot/action execution. This is not a live end-to-end browser task under each network setting and does not establish safe action retries or acoustic behavior.
