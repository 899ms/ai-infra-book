# 写作前阅读与采用范围

2026-09-10。先读现行第 11 章 outline／扩写和既有计算、调研，再扩写正文。来源文件的当前 SHA-256 见 sources.json。本记录说明采用范围，不把 survey 已读原论文的范围升级为本轮逐页重读。

## 已有计算

| 资料 | 阅读及采用 | 保留的边界 |
| --- | --- | --- |
| environment-resources-book | 九组 36 进程、CPU CPU·秒、RSS 峰值与积分、有限窗恒等式；正文 11.1.2 | 不代表 PSS、物理内存、完整环境或生产稳态 |
| environment-lifecycle-default | 共享与私有容量、五路径声明字节下界、单调用预热期待、独立局部实测；正文 11.2 | 云端首工具与完整创建时间为空，图不伪造实测 |
| routing-cost-book / crossover | Haiku 4.5／Sonnet 5 实际计费用量、全尝试分子、79.52% 费用交点、91.84% 联合期限门槛；图 11-31 | 单价为 Claude 标准 API 价格；独立质量与缓存概率，时长为题设 |
| retry-paths-book | 六条有限终点、概率和费用、CPU·秒与驻留积分；图 11-8 | 期限只作判定，非停止策略；条件概率未实测 |
| C61–C65 计划与 C63–C66 审计 | 检查现有子账和完整任务缺口 | 不宣称整个计算工作包因写作而完成 |

正文的单位转换与已有关系的敏感性曲线在本章生成器中复算。新绘制的三轮时间线、成组放置和验证提交示意均明确为教学输入，不作为新增系统测量。

## 已有 survey 与案例

- 阅读 `research/2026-infra-survey/README.md` 中调度、验证、可抢占恢复和模型费用的相关记录，以及 RLBoost 阶段审计。原调查提供公开论文的固定版本与选读范围。
- `platform-routing.md`：ASI 分配比例不同于 MFU；SpecBox 为单机 Docker 预印本；E2B 模板、快照、连接分别定义；计费字段归一化。
- `preemptible-rollout-and-weight-readiness.md`：Qwen3-8B 16.38 GB×6 接收、共享 200 Gbit/s 出口与 50 Gbit/s 接收的 3.93 秒下界；TP=2 仍接收完整权重；批量恢复可能截短输出；历史价格仅用于条件算例。
- `reward-deadlines-and-feedback.md`：条件剩余时间、最迟启动、训练等待抵消验证节省，以及不同 verl 入口不自动等价于 DistRS。
- `routing-cost-and-completion.md`：同任务费用与按时质量联合门槛，避免以低 token 单价直接选择模型。

## 后续审计改变正文结论

单 Agent 24×7 serving 原比较已有优化审计。先读 COMPARISON-RESULTS，再读 OPTIMIZATION-AUDIT 和 AUDIT-RESULTS：原基线缺 MTP／DSpark、固定逐层开销及周期重建未经校准。正文不搬入“某卡最优／API 必然更省”的采购结论，采用固定并发下 25.317 ms → 17.358 ms 的条件推导，说明局部 decode 加速不能按比例降低全部成本。

## 已有实验

CPU 进程记录、E2B 文档契约、抢占恢复、验证反馈／释放、双批次、预热重放、思考预算、Collector 故障、退出语义和扩容决定分别用于其已经测量的边界。失败和反例继续保留：消除 96 个重复 token 没有更早完成；提前更多没有降低工具延迟中位；反馈改善的少量合格轨迹不能证明普遍质量提升；96.13% 模型等待不证明 GPU 饱和。

## 教材组织

每节从具体问题进入，定义变量与测量边界，给出逐步计算，再用反例或已有实测检查适用条件。详细版本与命令放在本配套和脚注。插图服务于依赖、状态、分配或数值关系，图内不重复图号和正文图注。沿用经典量化教材的问题、假设、推导、比较、局限与练习体例，文字和图片均为本书新写。

## 量化叙述修订后的呈现方式

2026-09-10 按作者建议，主文减少“已有结果、核验范围、任务完成状态”的报告式段落。配置与概率条件在推导前说明，详细证据继续在脚注与本配套。新添的暂停、成组完成时间、RL 串行阶段和扩容对照均明确为教学条件，并纳入 verify.py；未改动原始计算数据。

五幅主图重新聚焦：执行路径、预热等待、验证提交、带期限门槛的费用选择、恢复成本。早期阅读记录中的“六条终点”仍是数值来源，现图 11-8 改为单位完成成本比较，分支关系由正文表格解释。精确数据保留在 figure-data.json 和原始来源，显示数字按用途取舍。

## 全章连贯推导修订

正文以同一个代码修复平台贯穿需求、生命周期、模型选择和最终设计。新增[精确设计数据](platform-design.json)与[计算程序](platform_design.py)，构建时自动生成；事件扫描核对每阶段依赖、资源积分及峰值，费用比较包含四个方案和轮间等待交点。常驻环境循环复用，每轮重建环境按轮支付准备开销；条件在正文计算前给出。原始 calculations 和 survey 证据保持原有锁定。

修订前版本与论证调整见[本轮记录](../../research/ch11-coherent-revision-2026-09-10/README.md)。
