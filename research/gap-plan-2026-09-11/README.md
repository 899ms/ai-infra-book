# 概念覆盖补齐：计划与执行记录（2026-09-11）

作者审阅全书概念覆盖后确定七项补充，全部按“固定工作量 → 已有公式推导 → 翻转条件 → 练习”的体例写作，数字来自归档原件与 `calc.py` 主题。

| 序 | 内容 | 落点 | 计算主题 |
| --- | --- | --- | --- |
| 0 | 第 7 章贯穿配置改为两台 HGX 8 卡服务器（每卡一张 400G 网卡） | 7.1–7.5、7.6.4 | hierarchical_gradient 参数化、supernode-scaling |
| 1 | 交换网络：Clos 层数与半分带宽、多轨拓扑、ECMP 冲突与逐包喷洒、incast 与链路流控、在网归约 | 7.1.2、新 7.2.5、7.5.2、7.5.3、7.2.1 | clos-cut、hash-collision |
| 2 | 芯片物理与能耗：动态功耗、能耗层次、光罩与封装、功率封顶；机柜风冷上限；端侧续航 | 4.1.3 扩写、6.5.2、12.1.5 | energy-ledger |
| 3 | 训练稳定性与容错：interleaved 1F1B、zero-bubble、DualPipe；临界批量；FP8 训练与 loss scaling；掉队者；容量因子 | 10.3.2、10.1.1、10.1.2、新 10.4.5、10.3.4 | training_pipeline_schedule 扩展、critical-batch、straggler-max、moe-capacity |
| 4 | 端侧执行资源与本地部署（手机、M3 Ultra、RTX PRO 6000；llama.cpp、Ollama、MLX、Unsloth） | 新 12.1.5 | energy-ledger、capacity 复用 |
| 5 | 丢包是不是拥塞：Mathis、BBR、重传尾部、编码冗余 | 新 12.3.3 | wan-loss-model |
| 6 | GPU 核内执行模型：warp、占用率、延迟隐藏、MMA 指令、异步搬运与栅栏 | 新 5.1.5 | sm-occupancy |
| 7 | MLA 与 GQA 的 PD／AF 判断 | 9.2.2、9.2.3、9.3.4、9.5.2 | pd_pool、pd_af_handoff 的 kv-layout |

执行顺序：归档资料 → 第 7 章改配置 → 计算主题 → 逐章写作（7、4、10、5、9、12）→ 中文表达通读。

## 执行记录

（各步骤完成后在此追加：改动文件、生成结果、验证命令与结果。）

### 2026-09-11 归档资料（步骤 1）

- 改动文件：`references/sources.tsv`（新增 38 行）、`references/manifest.json`、`references/README.md`、`references/index.md`（由 fetch.py 重新生成后按现行 Markdown 快照约定转换）、`references/files/**`、`references/text/**`；`references/fetch.py` 章名改为从 `manuscripts/NN-*.md` 目录读取（原 `outlines/chapters.json` 已迁入 archive/，脚本此前无法运行）。另将同批登记的 `kv-direct` 按 `local:` 约定补齐 manifest 状态。
- 命令：`python3 references/fetch.py --only <38 个新 id>`；网页快照用 pandoc 转为 `.md` 并在 manifest 记录 `original_html_sha256`；`python3 references/fetch.py --reindex`。
- 已归档（36 项有正文）：能耗与芯片物理 `dally-hotchips2023`、`dally-aha2023`、`fgdram-micro17`、`nvidia-grace-hopper-blog`、`nvidia-h100-datasheet`、`nvidia-h200-datasheet`、`nvidia-hgx-h100-datasheet`、`nvidia-dgx-h200-datasheet`、`nvidia-connectx7-datasheet`、`micron-hbm3e-page`、`skhynix-hbm-page`、`ashrae-liquid-cooling`、`qualcomm-8elite-brief`、`qualcomm-8elite-gen5-brief`、`apple-iphone16-pro-specs`、`micron-lpddr5x-page`、`samsung-lpddr5x-page`、`melting-point`；网络 `dgx-superpod-h100-ra`、`sharp-docs-intro`、`sharp-paper`、`uec-spec-10`（v1.0.1）、`uec-overview`、`ieee-802-1qbb-entry`（landing_only）；训练 `pipedream`、`zero-bubble`、`large-batch-empirical`、`gshard`、`megascale`、`meta-cluster-reliability`、`mixed-precision`、`fp8-formats`；广域网 `bbr-ietf-draft`、`mathis-tcp-model`；GPU 执行模型 `cuda-compute-capabilities`、`cuda-writing-kernels`。此前已归档、直接复用：`deepseek-v2`、`deepseek-v3`、`switch-transformer`、`megatron-scale`、`llama3`、`nvidia-h100`、`nvidia-h100-spec`、`nvidia-nvlink-spec`、`nvidia-dgx-b200`、`nvidia-blackwell-brief`、`nvidia-blackwell-guide`、`dcqcn`。
- 未取得：`tsmc-cowos`（官网对所有请求返回 403，行保留为 failed）；`micron-hbm3e-brief`（micron.com 对脚本 UA 返回拒绝页，行保留为 failed，同样数值见 `micron-hbm3e-page`）；Horowitz ISSCC 2014 论文付费、公开幻灯片链接失效，以 Dally 两份公开幻灯片替代（表格注明取自 Horowitz）；Grace Hopper 白皮书阅读页需登录、镜像 PDF 无文本层，以官方技术博客替代（含 1.3 pJ/bit）；DGX H100 数据手册无官方直链，以 DGX H200 数据手册与 SuperPOD 参考架构覆盖；BBR 的 ACM Queue 原文 403，以 IETF 草案替代；ConnectX-7 数据手册不含功耗数值。
- 数值提取：`sources-extract.json`（42 个来源、237 条，其中 11 条 not stated；每条 quote 已按空白归一化与 `references/text/<id>.txt` 逐行核对，附行号与 PDF 页码）。

### 2026-09-11 第 7 章贯穿配置改为两台 HGX 8 卡服务器（步骤 0）

- 配置：两台 HGX 规格 H100 服务器，参与者 0–7 在 A、8–15 在 B；NVLink/NVSwitch 每卡 900 GB/s 双向合计（每方向 450 GB/s），每卡一张 ConnectX-7 400 Gb/s NIC（每方向 50 GB/s），编号相同的 NIC 构成一条 rail；例题 7.2 预算改为 3 ms（由 7.6.1 的 20 ms 计算、17 ms 就绪推出）。原“四卡、两张 25 GB/s NIC 共用 40 GB/s 出口、本地 200 GB/s”保留为对照配置（`-two-nic`），用于 7.2.1 讨论、7.2.3 共享入口、7.2.4 网卡数与实验 7-3。来源：`references/files/specs/nvidia-hgx-h100-datasheet.pdf`、`nvidia-h100-spec.md`、`nvidia-nvlink-spec.md`、`nvidia-connectx7-datasheet.pdf`、`nvidia-dgx-h200-datasheet.pdf`、`references/files/documents/dgx-superpod-h100-ra.pdf`、`experiments/ch07/07-03/README.md`（新增脚注 `[^hgx]`）。
- 计算：`calculations/src/infra_calc/topics/hierarchical_gradient.py` 参数化为 `ranks_per_server`（4/8）、`nics_per_server`（1…ranks）、`nic_bytes_per_second`、`local_bytes_per_second`、`shared_egress_bytes_per_second`；NIC 数等于 rank 数时每 rank 独占 NIC，否则按 NIC 条带化。`scenarios/book.json` 的 `hierarchical_gradient` 改为 12 行（fp32/bf16 × 三种组织 `-nic8`、`gradient-fp32-hierarchical-nic1`、对照 `-two-nic` 三种组织、`-one-nic`/`-three-nic` 连续环）；`periodic_queue` 四行改为两作业各 50 GB/s、`feedback_queue` 四行到达 100 GB/s（roomy 缓冲改 2 MiB）；三个 example 场景与 `supernode-scaling-example.json`（nic_Bps 25→50 GB/s）同步。测试 `tests/test_hierarchical_gradient.py` 重写（9 项通过）；`test_parallel_choice.py`、`test_periodic_queue.py`、`test_feedback_queue.py` 通过。
- 结果：`python3 calculations/calc.py reproduce` 在本仓库当前状态下无法运行（`calculations/sources/hardware/nvidia-a100-page.html` 等网页快照已被迁移为 .md，`hardware.json` 仍引用 .html；与本步骤无关），因此用与 `reproduce.save` 相同的 `report.markdown` 路径定向重生成 12 个 `gradient-*`、5 个 `periodic-queue-*`、4 个 `feedback-queue-*` 结果（json+md），删除旧 `gradient-*-nic1/nic2` 文件，手工同步 `results/README.md` 对应三段、`results/manifest.json` 的 artifact 条目（inputs 哈希未更新，待 reproduce 可运行后重跑）；`python3 calculations/supernode_scaling.py` 重生成 `supernode-scaling-book.{json,md}`。`calculations/README.md`、`src/infra_calc/outline.py`、`archive/outlines/extensions/07-数据中心网络.md`、`inventory/sources.json`（块 ::009 缓存文本）中的旧 id 一并更新。
- 关键数字：连续环 30 轮、跨服务器 720 MiB、只用一张 NIC，7.61 ms；交错环 5760 MiB、7.61 ms；分层归约 384 MiB、八张 NIC 同时工作，1.32 ms（本地 14 轮 0.78 ms + 跨服务器 2 轮 0.51 ms + 32 μs）；单 NIC 分层 4.84 ms；128 项在途限制下分层 2.35 ms。例题 7.1：360 MiB／50 GB/s ≈ 7.5 ms，交叉倍数 2.6。例题 7.5：两作业各 50 GB/s 共用 50 GB/s rail 上联，平均 40%，积压 1 GB、排空 20 ms、错峰 5 ms 积压 250 MB；7.5.2 反馈例：5.2 μs 填满、丢弃约 738 KB。例题 7.6：串行 29.6／23.3 ms，17 ms 就绪时连续环 26.6 ms、分层 22.0 ms（最晚就绪 18.7 ms），单 NIC 23.8 ms。7.6.4（NIC 50 GB/s）：出口扩展 8/64/128/256 卡步时间 2.530/2.319/2.304/2.296 s，出口封顶列不变，本地翻倍 128 卡 2.172 s。
- 正文：7.1.1 配置段与图 7-1、例题 7.1 与图 7-2、7.1.3、7.2.1（例题 7.2、图 7-3–7-6、讨论改为“本地不慢于一张 NIC 即分层更快”+对照配置翻转条件 80 GB/s）、7.2.3（一张 NIC 0.67 ms／八张 0.08 ms／对照共享入口 0.84 ms）、7.2.4（八 NIC 基线、对照配置一/二/三张 NIC、FuseLink 中继 50/450/100）、实验 7-2/7-3/7-5/7-8/7-10、7.3.1/7.3.2/7.4.1 rank 编号（卡 4→卡 8）、7.3.4 例题 7.3 路径带宽改 50 GB/s（391 槽位、5.12 ns）、7.4.4 目标 50 GB/s、7.5.1/7.5.2、7.6.2、7.6.4、7.6.5、小结；脚注 `[^gradient]`、`[^fuselink]` 改写，新增 `[^two-nic]`、`[^nic-count]`、`[^hgx]`。7.2.2、7.3.2、7.3.3、7.6.3 与 V4.1 迁移中“取一条 25 GB/s／40 GB/s 路径”的独立声明算例未改。
- 图：`manuscripts/ch07/build.py`、`visuals.inc.py` 中图 7-1/7-2/7-3/7-8–7-11 的旧式绘制改为只记录数据，最终图全部由 `teaching_revision.py` 经 `figure_style` 绘制（图 7-1 两台 8 卡服务器＋每卡 NIC＋rail 交换网络；图 7-3/7-4 十六个 rank 两行环；图 7-9 八张 NIC；图 7-11 中继 50/450/100；7-20 系列六条时间线换为连续环 17 ms 就绪与单 NIC）；`models.py`、`verify.py`、`sources.json`（149 项）同步。
- 命令与结果：`python3 -m unittest discover -s calculations/tests -p 'test_*gradient*.py' -v` → 9 项 OK；`test_parallel_choice`、`test_*queue*`、`test_coverage` OK；`.venv-site/bin/python manuscripts/ch07/build.py` → 171 个图文件、158 个公式、0 处文字出界；`.venv-site/bin/python manuscripts/check_teaching_reading.py 7` → 1440/390 两宽度 passed（57 图、158 公式）；`python3 manuscripts/ch07/verify.py` → 183 项中 181 通过，两项失败均为本步骤之前已存在：`outline section and subsection coverage`（正文比归档大纲多出 7.6.5；脚本已改为回退读取 `archive/outlines/`）、`book-size visible text`（9 幅 UB-EP 图的版式记录在 `ub-ep-layout-validation.json`，脚本只数 `teaching-layout-validation.json`）；`verify_teaching_book.py` 第 7 章仅剩同一项 `7: layout inventory`（HEAD 时另有两项 browser 错误已消除）。全量 `unittest discover`：119 处 ERROR 全部为缺失 `.html` 网页快照（与本步骤无关），`test_delivery` 余下 3 项失败来自其他步骤新增而未 reproduce 的 `clos-cut-*` 等场景与未写入文档的新子命令。
- 遗留：`grep -n '四张卡|两张网卡|40 GB/s|参与者 0 到 3|卡 4'` 剩余命中均为对照配置、反馈后 40 GB/s 发送速率与 7.3.3 独立算例。

### 2026-09-11 计算主题实现（步骤"计算主题"）

**改动文件。** 新模块 `calculations/src/infra_calc/topics/{clos_cut,hash_collision,sm_occupancy,energy_ledger,critical_batch,straggler_max,moe_capacity,wan_loss_model}.py`；扩展 `feedback_queue.py`（`incast()`）、`training_pipeline_schedule.py`（`interleaved_1f1b`、`zero_bubble`、`dualpipe`，`_work_groups`）、`kv_comparison.py`（`per_token_bytes`、`mla_compact_extra_flops_per_token`）、`pd_pool.py` 与 `pd_af_handoff.py`（`kv_layout=gqa|mla`）；新增 `src/infra_calc/declared.py`（声明输入来源校验）；`cli.py` 注册 `clos-cut`、`hash-collision`、`incast-feedback`、`in-network-reduce`、`sm-occupancy`、`energy-ledger`、`critical-batch`、`straggler-max`、`moe-capacity`、`wan-loss-model`；`reproduce.py` 增加 9 个场景循环与 results/README.md 的"概念覆盖补齐"段；`scenarios/book.json` 追加 9 个新键（22 行）并向 `training_pipeline_schedule`（6 行）、`pd_pool`（3 行）、`pd_af_handoff`（3 行）追加行；新增 `scenarios/{clos-cut,hash-collision,incast-feedback,sm-occupancy,energy-ledger,critical-batch,straggler-max,moe-capacity,wan-loss-model}-example.json`；`calculations/README.md` 追加用法段。每个新场景行的 `input_sources` 标明归档文件路径或 `book`。

**结果文件（由 `calc.py reproduce` 生成，各带 .json/.md）。** clos-cut-k64-nonblocking、clos-cut-k64-oversub3、clos-cut-rail-aligned、clos-cut-rail-shifted、clos-cut-in-network；hash-collision-8-rails、hash-collision-16-16、hash-collision-128-16、hash-collision-64-64；incast-feedback-book、incast-feedback-4mib；sm-occupancy-book-tile、sm-occupancy-register-accumulator、sm-occupancy-latency-1000ns；energy-ledger-book；critical-batch-book、critical-batch-noise-20m；straggler-max-sigma-2pct、straggler-max-sigma-5pct；moe-capacity-book；wan-loss-model-book、wan-loss-model-p036；training-pipeline-{interleaved,zero-bubble,dualpipe}-m{8,16}；pd-pool-mla、pd-pool-mla-50gbps、pd-pool-50gbps；pd-af-handoff-qwen8-mla、pd-af-handoff-qwen8-50gbps、pd-af-handoff-qwen8-mla-50gbps。

**来源。** 硬件与论文数值来自 `references/text/`（fat-tree、dcqcn、nvidia-hopper-tuning、cuda-compute-capabilities、nvidia-h100-spec、nvidia-h100、switch-transformer、gshard、scaling-laws、large-batch-empirical、megatron-scale、zero-bubble、pipedream、deepseek-v3、mathis-tcp-model、bbr-ietf-draft、logicfolding-energy）、`research/gap-plan-2026-09-11/sources-extract.json`（dally-hotchips2023、fgdram-micro17、nvidia-grace-hopper-blog、micron-lpddr5x-page、melting-point、ashrae-liquid-cooling）与鹊桥路径文档；各模块 docstring 逐行引用。无归档来源的量以 `declared_*` 命名：warp 宽度、HBM 延迟、每 warp 未完成加载数与加载宽度、MMA 指令形状（sm-occupancy）；B_noise（critical-batch）；每卡时间标准差、loss spike 回滚 MTBF（straggler-max）；一跳线缆长度与传播时延（incast）；手机 x16 通道数、风冷机柜上限（energy-ledger；ASHRAE 白皮书无明确机柜上限，寄存器与网卡每字节能耗未归档故留空）；热专家比例与倍数（moe-capacity）；FEC 符号大小与目标（wan-loss-model）；interleaved/zero-bubble/DualPipe 的槽位顺序（`declared_schedule_rule`，归档文本只给气泡公式与拆分思想）。

**验证。** 新增测试 `tests/test_{clos_cut,hash_collision,incast_feedback,sm_occupancy,energy_ledger,critical_batch,straggler_max,moe_capacity,wan_loss_model,training_pipeline_extended,pd_kv_layout}.py`（`_gap_common.py` 检查每个场景输入都有来源）全部通过；`python3 -m unittest discover -s calculations/tests`：1175 个测试，本次新增与相关旧测试（training_pipeline_schedule、feedback_queue、pd_pool、pd_af_handoff、v41_kv_comparison）通过；119 个 error 与 3 个 failure 均为本步之前已存在的缺失归档 HTML（sources/hardware/*.html、protocol-rfc 等 FileNotFoundError）和结果/场景配对检查（test_delivery 待 reproduce 生成新结果后通过；test_coverage 的缺失引用已由重生成的 gradient 结果消除）。`reproduce` 由协调者统一运行。

### 2026-09-11 第 5 章新增 5.1.5 GPU 核内执行模型（步骤 6）

- 改动文件：`manuscripts/05-算子与运行时.md`（新增 5.1.5“一个 kernel 在 SM 上怎样执行”，置于 5.1.4 之后；5.1.4 结尾改为交接到 5.1.5；5.2.2 增加一句回指在途字节；5.2.3 的 lane 改为“warp 中的位置”；5.3.2 的 2 μs／3 μs 改为“沿用第 5.1.5 节”；本章小结第一条增加一句；新脚注 `[^sm]`；图 5-4 及之后全部图号顺延 2，共 39 幅）；`manuscripts/ch05/structure_figures.py`（新增图 `figure-5-sm-residency`、`figure-5-warp-pipeline`，经 `figure_style` 的 420 pt 画布与 Exporter 导出 svg/png/pdf，并向 figure-data 写入 `sm_residency`、`warp_pipeline`）；`manuscripts/ch05/build.py`（把 data 传入 structure 图并把其版式检查并入 `teaching-layout-check.json`）；`manuscripts/ch05/verify.py`（大纲改为回退读取 `archive/outlines/` 并允许正文多出小节，HTML 改从 `build/legacy` 读取，适配现行 figure-index 结构；新增 SM 驻留、Little 定律、MMA 计数、累加器入寄存器、1000 ns 翻转、warp 流水栅栏与 64×64 练习的算术检查）；`manuscripts/ch05/sources.json`（新增三份 sm-occupancy 结果与四份 CUDA 文档的哈希锁）。
- 内容顺序：warp = 32 线程同发（256 线程 = 8 warp，对应 5.2.3 的 32 个 lane）→ 占用率（线程 2048/256 = 8、寄存器 64K/(256×128) = 2、共享内存 228 KB/(96 KiB + 1 KB) = 2、块上限 32 → 2 块、16 warp、25%，寄存器与共享内存同时限制；累加器入寄存器后共享内存放宽到 6 块、寄存器仍 2 块）→ 延迟隐藏（3.35 TB/s × 600 ns ÷ 132 ≈ 15.2 KB，16 warp × 4 × 512 B = 32 KiB，覆盖率 2.15，8 个 warp 即够；作为 5.2.2“大块少读却可能变慢”的定量形式）→ MMA 粒度（16×8×16，每块 512 条、每 warp 64 条，warpgroup 级指令一句并引 FlashAttention-3）→ 异步拷贝 + 计数栅栏 + warp specialization（4.2.3 三资源流水的软件形式；swizzle 与 padding 一句）→ 昇腾对照三行表（AIC/AIV 是核）→ 翻转条件（1000 ns、每 warp 2 条：需 25.4 KB，可用 16 KiB，覆盖率 0.65，至少 25 个 warp 即 4 块）→ 练习 5-1（64×64 tile 的限制项）。
- 数字来源：`calculations/results/sm-occupancy-{book-tile,register-accumulator,latency-1000ns}.{md,json}`；SM 限制 `references/text/cuda-compute-capabilities.txt` 227/235/242/274/294 行与 `nvidia-hopper-tuning.txt` 52–57、111 行；占用率定义 `cuda-writing-kernels.txt` 489 行；栅栏／warp 分工／swizzle `nvidia-async-copies.txt` 197、333–334、1179–1196 行；warpgroup 与生产者—消费者 `flashattention3.txt` 145、163–178、208–262 行；3.35 TB/s `nvidia-h100-spec.txt` 383 行，132 SM `nvidia-h100.txt` 644 行。声明输入（每线程 128 寄存器、600 ns、每 warp 4 条加载、每线程 16 bytes、16×8×16）在正文开头一次列出，脚注注明。未新增场景行。
- 命令与结果：`.venv-site/bin/python manuscripts/ch05/build.py` → 117 个图文件、129 个公式、0 处文字出界，`figure-index.json` 39 项（第 4、5 项为新图）；`.venv-site/bin/python manuscripts/ch05/verify.py` → passed，0 错误（改前基线 1 项失败“book-size revised labels and extents”，因 structure 图的版式记录未并入，本次已修）；`.venv-site/bin/python manuscripts/check_teaching_reading.py 5` → 1440/390 两宽度 passed（39 图、129 公式）；`verify_teaching_book.py` 第 5 章无错误项，其余章的哈希与 artifact 失败为此前已存在。禁用词 grep：5.1.5 无命中，唯一命中为 5.5.1 既有句“下一步计算需要用到上一轮选出的 token”。

### 2026-09-11 第 12 章新增 12.1.5 端侧执行资源与本地部署、12.3.3 丢包是不是拥塞（步骤 4、5）

- 改动文件：`manuscripts/12-端边云协同.md`（12.1.4 结尾改为交接；新增 12.1.5，置于 12.1.4 之后；新增 12.3.3，置于 12.3.2 之后；原 12.3.3 多流传输与优先级顺延为 12.3.4、原 12.3.4 Queqiao 案例顺延为 12.3.5；新增练习 12-9、12-10 与小结两句；新脚注 `[^phone]`、`[^phone-ledger]`、`[^melt]`、`[^mathis]`、`[^bbr]`、`[^wan-loss]`；图号 12-9 起全部顺延，12-9/12-18 为新图，共 32→34 幅，正文与练习内引用同步改号）；`manuscripts/ch12/teaching_revision.py`（新增 `draw_local_tiers`、`draw_loss_repair`，经 `figure_style` 的 420 pt 画布与 Exporter 导出 svg/png/pdf，并向 figure-data 写入 `12-local-tiers`、`12-loss-repair`）；`manuscripts/ch12/verify.py`（大纲改为回退读取 `archive/outlines/` 并允许正文多出小节、按 12.3.3→12.3.4→12.3.5 映射核对；HTML 改从 `build/legacy` 读取；练习改十项；修正两条此前已过时的机制句检查；新增手机带宽、decode 上限、量化容量、MELTing Point 能耗、三档设备下界与 Mathis／BBR／重传尾部／FEC 的算术检查及 12-9、12-10 答案键）；`manuscripts/ch12/sources.json`（刷新 6 个因 reproduce 重跑而变化的既有锁——差异仅为浮点末位与来源哈希行，正文引用数值不变；新增 16 项锁：energy-ledger-book、wan-loss-model-book/p036 各 json+md、qwen3-8b-decode-b1-s8192.json、七份归档规格／论文与 sources-extract.json）；`manuscripts/ch12/README.md`（小节 19→21、练习八项→十项、Queqiao 案例改指 12.3.5，阅读顺序图清单重排并补入两幅新图，32→34 幅）。
- 内容顺序 12.1.5：固定工作量（沿用 4.8.2 的 Qwen3-8B BF16 单请求 8K decode，每步权重 15.14 GB＋KV 1.21 GB）→ 手机带宽推导（LPDDR5X 每引脚 10.7 Gb/s、x16 通道 21.4 GB/s、四条通道声明输入合计 85.6 GB/s → 读一遍权重 0.177 s、上限约 5.6 token/s，含 KV 0.191 s、约 5.2 token/s；q4_0 后每步 5.47 GB、63.8 ms、约 15.7 token/s）→ 容量（2.6.2 公式；8/12/16 GB 三档、预留 4 GB 为声明输入；q4_0 权重 4.61 GB，12 GB 放两条 8K 或单条约 2.3 万 token，16 GB 放六条 8K 或约 5.0 万 token，q8_0 8.70 GB 需 16 GB 档）→ 能耗（MELTing Point 每 token 0.16–0.21 mWh 即 0.576–0.756 J，对照 4.1.3 的 0.534 J 下界；持续 13.8 W、瞬时 >18 W；14.8 token/s×0.576 J≈8.5 W 自洽核对；一次充电约 490–590 个提示）→ 三档设备（交叉引用 4.8.2 的 11 款设备表：M3 Ultra 19.96 ms/50.1 token/s、RTX PRO 6000 9.12 ms/109.6 token/s，不重复列表）→ 运行时各解决一层问题（llama.cpp＝GGUF 布局 8.4.1、Ollama＝准备与复用 12.2.3、MLX＝统一内存 4.6.3、Unsloth＝训练状态容量）→ 翻转条件（45 s/20 轮下手机 q4_0 每轮约 35 token；容量档位；能耗与续航），呼应 12.1.4 与 12.5。
- 内容顺序 12.3.3：传统 TCP 用丢包反推拥塞（Mathis 上界 MSS/(RTT·√p)，C=1.22）→ 鹊桥路径记录证明丢包可不含拥塞信息（1–300 Mbit/s 恒定约 14% 丢包、无排队、越过 333 Mbit/s 膝点才升至 44%）→ 代入得上界 0.155 Mbit/s，354,640 bytes 仅发送需约 18.3 s → BBR 直接测量 BBR.max_bw／BBR.min_rtt，BDP 8.325 MB、cwnd 16.65 MB（cwnd_gain 2），Startup 增益 2.77、ProbeBW_UP 1.25，理想 goodput 286 Mbit/s 约为 Mathis 上界的 1,850 倍 → 重传尾部（245 报文、期望丢 34.3 个，串行预算 238.5 ms，期望 0.756 s 约 3.2 倍、p99 1.24 s 对应 6 轮，模型为有利于 TCP 的下界）→ FEC（99.9% 需 63 个修复包、冗余 25.7%，完成约 241 ms 贴近串行预算；丢包 3.6% 时 20 个、8.2%，重传 p99 降为 0.84 s），为 12.3.5 鹊桥案例的前向纠错与发送调度给出定量动机；案例正文实测数字未改。
- 数字来源：`calculations/results/energy-ledger-book.{md,json}`（85.6 GB/s、0.534 J/token 与 decode 字节数）、`wan-loss-model-{book,p036}.{md,json}`（Mathis/BBR/重传/FEC 全部数值）、`references/text/` 的 qualcomm-8elite(-gen5)-brief、micron/samsung-lpddr5x-page、apple-iphone16-pro-specs、melting-point、mathis-tcp-model、bbr-ietf-draft（提取值见 sources-extract.json）、鹊桥路径文档（沿用既有 `[^queqiao]` 锁）。声明输入在正文一次注明：手机 x16 通道数 4、内存三档 8/12/16 GB、预留 4 GB、FEC 符号大小与 99.9% 目标。未新增场景行，calculations/ 未改。
- 图：图 12-9（`figure-12-local-tiers`，三档设备每步读取下界 191/63.8/19.96/9.12 ms 与 token/s 上限）、图 12-18（`figure-12-loss-repair`，对数轴完成时间：串行预算 0.239 s、FEC 0.241 s、重传期望 0.756 s、重传 p99 1.24 s、Mathis 上界下仅发送 18.3 s）。
- 命令与结果：`.venv-site/bin/python manuscripts/ch12/build.py` → 34 个图、123 个公式、0 处文字出界；`.venv-site/bin/python manuscripts/ch12/verify.py` → passed，513 项检查 0 错误（改前 verify.py 因 `outlines/` 迁移与 HTML 路径已无法运行，本次按第 5 章做法修复）；`.venv-site/bin/python manuscripts/check_teaching_reading.py 12` → 1440/390 两宽度 passed（34 图、123 公式）；`verify_teaching_book.py` 第 12 章无错误项，其余章的哈希、版式失败均为此前或其他步骤已存在。禁用词 grep（值得注意／不难／综上／或许／本节／笔者等）：新文本仅一处合理用法“重传仍可能再丢”。其他章无图 12-N 引用（已 grep 确认）。

### 2026-09-11 第 10 章训练稳定性与容错（步骤 3）

- 改动文件：`manuscripts/10-训练系统.md`（10.1.1 末尾新增临界批量段；10.1.2 末尾新增精度维度段；10.3.2 末尾新增流水线调度扩展段；10.3.4 在分派不均例后插入容量因子段；新增 10.4.5“掉队者与慢节点”，置于 10.4.4 之后；习题 10-2/10-3/10-4/10-6/10-9 各延伸一句；常见误区新增一条（批量随卡数放大）；小结第二段新增一句；新脚注 `[^critical-batch]`、`[^precision]`、`[^schedules]`、`[^capacity]`、`[^straggler]`；新增图 10-21 与图 10-24，原图 10-21…10-24 顺延为 10-22…10-25、原图 10-25…10-41 顺延为 10-26…10-43，共 41→43 幅，正文内引用与 `manuscripts/ch10/README.md` 图清单同步改号，其他章无图 10-N 引用（已 grep 确认））；`manuscripts/ch10/build.py`（新增 `pipeline_schedules`、`moe_capacity` 两组 figure-data，由六个 training-pipeline 扩展结果与 moe-capacity-book.json 计算，`new_numbers` 映射补 21、26 两项）；`manuscripts/ch10/teaching_revision.py`（新增图 `figure-10-pipeline-schedules`、`figure-10-moe-capacity`，经 `figure_style` 的 420 pt 画布与 Exporter 导出 svg/png/pdf）；`manuscripts/ch10/verify.py`（改前基线无法运行：`outlines/` 已迁入 archive/、HTML 已改写到 `build/legacy`，按第 5 章做法修复并允许正文多出小节；RL 标记“冻结同一份权重”在 03c54524 中已从正文移除而检查未更新，改为现行措辞“权重长期不变”；新增调度完成时间／峰值／消息数、气泡公式、临界批量天数与上限、掉队者阶次统计与驱逐代价、spike 修正的保存周期、容量因子丢弃与补零、FP8 字节等算术检查）；`manuscripts/ch10/sources.json`（29→52 项：新增十一份结果 json 与十二份归档文本／PDF 的哈希锁，另审阅并刷新三个既有锁——training-deadline-book、dense-training-scale-book 的差异仅为步骤 1 的 .html→.md 快照迁移，reading-asplos-2026 为调研步骤新增文件）；`manuscripts/ch10/README.md`（24 小节、43 图、261 公式，阅读顺序图清单插入两行）。
- 内容顺序：10.3.2 扩展段开头一次列出声明输入（三种调度的槽位顺序为 declared_schedule_rule，归档文本只给气泡公式与拆分思想；TW=TB/2 拆分比例）→ 交错式 1F1B（(1/v)(p−1)/m，Megatron-LM）→ 零气泡（反向拆 dX/dW，ZB-H1 (p−1)(TF+TB−TW)、ZB-H2 (p−1)(TF+TB−2TW)，zero-bubble 论文表 2）→ DualPipe（(PP/2−1)(F&B+B−3W)、参数 2×、激活 PP+1，DeepSeek-V3 表 2）→ 四方案对照表与图 10-21 → 翻转条件（m 增大收益减半而代价不缩；链路争用；容量先排除）。10.1.1：384 条序列的由来——B_noise=tr(HΣ)/GᵀHG（large-batch-empirical 式 2.8）、(S/S_min−1)(E/E_min−1)=1（scaling-laws 式 5.1），B_noise 二百万 token 为声明输入（归档区间一到二百万）→ 反推 S_min=19,434 步 → 弱/强扩展天数表 → 翻转条件（B_noise 两千万时上限 7.36×）。10.1.2：交叉引用 4.6.1 的 FP16/BF16 指数尾数（不重复）→ FP16 损失缩放（mixed-precision：8–32K 因子）→ BF16 免缩放 → DeepSeek-V3 FP8 分块缩放（1×128 激活、128×128 权重、每 128 元素提升 FP32 累加、相对损失误差 <0.25%）→ 三格式字节表 → 损失尖峰→回滚动机，接 10.4.5。10.4.5：同步步由最慢卡决定（E[max] 阶次统计）→ 三种响应（等待/重分配/驱逐）→ loss spike 回滚率并入一阶模型，修正 10.4.4。10.3.4：容量因子公式（Switch 式 (3)、GShard 残差直达）→ 96/32 例与 Qwen3-235B 例 → 图 10-24 → 翻转条件（c=1.5 起零丢弃、补零 1/3；均衡机制一句并与第 2.4 节“不丢弃 token”假设对上）。
- 关键数字：M=8 时 1F1B／交错 v=2／零气泡／DualPipe 完成 347/298/337/306 ms，每卡空闲 106/57/96/65 ms，阶段 0 峰值 921/1,329/1,610/1,228 MiB（DualPipe 阶段 1、2 为 1,612），交错式前向消息 24→56 次；M=16 为 599/538/577/558 ms。临界批量：S_min 19,434 vs 31,790 步，弱扩展上限 1.64×（B_noise 两千万时 7.36×），96/192/384/1536 卡弱扩展 16.8/14.8/13.8/13.0 天、强扩展 11.3/6.5/4.1/2.3 天（参考 20.9 天）。FP8：每参数约 1.0002 bytes，Qwen3-8B 权重 16.4→约 8.2 GB。掉队者：μ=52.2 s、σ=2%/5% 时 N=8/48/1024 的 E[max] 为 1.42/2.23/3.25 σ，每步 58.2/59.0/60.1 s 与 60.4/62.5/65.2 s（无波动 56.7 s）；驱逐代价 τ=600 s 时 420 s；spike MTBF 七天时 L(600) 0.0245→0.0252、最优周期 4,340→3,005 s、设计案例 1800 s 处 0.95%→1.12%（完成时间 +0.04 天，结论不变）。容量因子：c=1.0/1.25/1.5/2.0 时 96/32 例容量 64/80/96/128、丢弃 32/16/0/0；Qwen3-235B（E=128、k=8、8K token、一半专家 1.5 倍热为声明输入）丢弃占比 25%/12.5%/0/0、补零行占执行量 25%/30%/33%/50%。
- 命令与结果：`.venv-site/bin/python manuscripts/ch10/build.py` → 129 个图文件（43 图 × 3 格式）、261 个公式、0 处文字出界；`.venv-site/bin/python manuscripts/ch10/verify.py` → passed，24 小节、43 图、52 来源、0 错误；`.venv-site/bin/python manuscripts/check_teaching_reading.py 10` → 1440/390 两宽度 passed（43 图、261 公式）。禁用词 grep（我感觉／也许／可能吧／本节将／下一步我们将等）：新文本无命中，唯一命中为 10.5.3 既有句。两幅新图已经 ReadMediaFile 目检（图例与数值标签无重叠）。
- 遗留：calculations/ 未改；每卡时间标准差（2%/5%）、spike MTBF（7 天）、B_noise（二百万 token）、热专家分布（一半 1.5 倍）、调度槽位顺序与 TW=TB/2 均为声明输入，正文与脚注已注明需实测。

### 2026-09-11 第 4 章芯片物理与能耗（步骤 2，含 6.5.2 散热段）

- 改动文件：`manuscripts/04-加速器架构.md`（4.1.3 扩写为四段推导：动态功耗 $\alpha CV^2 f$ 与 LogicFolding（动态约占九成）；能耗层次表——KB 级 SRAM 1.25 pJ/B、MB 级 SRAM 12.5、NVLink-C2C 10.4、HBM2 31.76（内部分解 2.24+1.21+0.3 pJ/bit）、LPDDR 160、16 位乘加 0.75 pJ/FLOP，各行注明工艺与年份；Qwen3-8B 单请求 8K decode 一步分账 0.534 J（权重 0.481／KV 0.038／计算 0.015，权重占 90%），同字节来自各层的对照 0.020/0.204/0.170/0.519/2.62 J，batch 32 时约 0.068 J，翻转条件 $0.481/B<0.015$；光罩与封装——H100 814 mm²、A100 826 mm²、Blackwell 双 die 加 10 TB/s NV-HBI，HBM 每堆 1,024 引脚；功率封顶——H100 700 W／989.4 TFLOP/s 得 0.71 pJ/FLOP，HBM 满带宽 106 W 后计算预算 594 W（0.60 pJ/FLOP），峰值对应 742 W 不可持续，频率降到 0.80／0.93；4.8.4 引用该分账作为每 token 能耗下界；新增实验 4-8 与图 4-4～4-7，原图顺延）；`manuscripts/ch04/energy_physics.py`、`energy-figure-data.json`（新增，四幅能耗图经 `figure_style` 420 pt）；`manuscripts/ch04/teaching_revision.py`、`figure-index.json`（34→38）、`sources.json`（新增能耗分账结果与归档来源的锁）、`verify.py`（实验 7→8；后续修复：大纲回退读 `archive/outlines/`、允许正文 4.8 多出大纲、版式检查并入 `evolution-layout-validation.json`）、`README.md`；`manuscripts/06-超节点.md`（6.5.2 新增 ASHRAE 风冷上限段：40–50 kW 机柜需 5,000 cfm 而地板送风仅 1,900 cfm，代入式 (6-11) 风冷 40 kW 只装 23 张卡，64/72 卡须液冷，新脚注 `[^ashrae]`；6.6 新增指向 9.2.2 的交叉引用）。
- 关键数字来源：`calculations/results/energy-ledger-book.md`；`references/text/`（dally-hotchips2023 转引 Horowitz、fgdram-micro17、nvidia-grace-hopper-blog、logicfolding-energy、ashrae-liquid-cooling、dgx-superpod-h100-ra、nvidia-h100-datasheet）；提取值见 `sources-extract.json`。
- 命令与结果：`.venv-site/bin/python manuscripts/ch04/build.py` → 114 个图文件、0 处文字出界；`.venv-site/bin/python manuscripts/ch04/verify.py` → passed，981 项检查、0 错误；`.venv-site/bin/python manuscripts/check_teaching_reading.py 4` → 1440/390 两宽度 passed（38 图、282 公式）。

### 2026-09-11 第 7 章交换网络（步骤 1）

- 改动文件：`manuscripts/07-数据中心网络.md`（7.1.2 新增“Clos 网络的层数与半分带宽”：$k$ 端口、无阻塞 $d=u=k/2$、两层 $k$ 叶 $k^2/2$ 端点、三层 $k^3/4$；$k=64$ 时两层 2048 端点／96 台交换机／半分 51.2 TB/s，三层 65,536／5,120／1638.4 TB/s；超售比 3 时 3072 端点、半分降至 25.6 TB/s；图 7-3 与练习；7.2.1 新增第四种方案“在网归约”：两台服务器跨服务器总量同分层归约 384 MiB、一轮 0.505 vs 两轮 0.507 ms，$S$ 台服务器时环为 $2(S-1)$ 轮而在网归约保持一轮，32 台时只需环的 46%，SHARP 论文 128 台主机实测 8 B AllReduce 6.01→2.83 μs；图 7-8、实验 7-4；新增 7.2.5“多轨拓扑”：rail-optimized 组织、rank $i$ 与 $i+8$ 对齐配对只经一台叶交换机、错位配对 192 MiB 全过脊层、128 卡超节点八条 rail 各载 14 GB；图 7-14、实验 7-6；7.5.2 新增“多对一汇聚与链路流控”：incast 定义、$(N-2)\times50\ \mathrm{GB/s}\times20\ \mu s\le Q_{\mathrm{free}}$ 下 1 MiB 只容 $N\le3$、PFC 0.33 μs 一跳与 DCQCN 20 μs 端到端的分工；图 7-44；7.5.3 新增“多条路径上的哈希冲突”与“逐包喷洒”：ECMP 定义、$n$ 流 $m$ 上联最忙链路期望负载的精确分布、有效割集 $(n/m)/\mathrm{E}[L_{\max}]$（8→8 为 38.5%、16→16 为 32.5%、64→64 为 25.3%、128→16 为 59.9%）、8 MiB 拆 4 KiB 报文喷八条路径 29.0 μs vs 单路径 168.8 μs、峰值乱序缓存 1.39 MB、Ultra Ethernet 的熵值喷洒与选择性确认；图 7-48、实验 7-14；小结加一句；新脚注 `[^in-network]`、`[^rail]`、`[^incast]`、`[^ecmp]`）。
- 关键数字来源：`calculations/results/clos-cut-{k64-nonblocking,k64-oversub3,rail-aligned,rail-shifted,in-network}.md`、`hash-collision-{8-rails,16-16,64-64,128-16}.md`、`incast-feedback-{book,4mib}.md`；归档来源 `references/files/documents/dgx-superpod-h100-ra.pdf`（多轨）、`references/files/papers/sharp-paper.pdf` 与 `references/text/sharp-docs-intro.txt`（在网归约）、`references/files/standards/uec-spec-10.pdf` §3.6.5.2 与 `uec-overview.pdf`（喷洒与选择性确认）、`references/text/dcqcn.txt`（20 μs 反馈量级）。
- 命令与结果：`.venv-site/bin/python manuscripts/ch07/build.py` → 171 个图文件、0 处文字出界；`.venv-site/bin/python manuscripts/ch07/verify.py` → 仅剩步骤 0 已记录的两项预存失败（大纲比正文少 7.6.5；UB-EP 图版式记录在 `ub-ep-layout-validation.json`）；`.venv-site/bin/python manuscripts/check_teaching_reading.py 7` → 1440/390 两宽度 passed（62 图、261 公式）。

### 2026-09-11 第 9 章 MLA 与 GQA 的 PD／AF 判断（步骤 7，含 6.6／8.4.2 交叉引用）

- 改动文件：`manuscripts/09-分布式推理.md`（9.2.2 新增紧凑 MLA 状态并列：DeepSeek-V3 每 token 每层 $d_c+d_r=576$ 维、BF16 68.6 KiB/token、8K 上下文 549 MiB vs GQA 1.125 GiB，25/50 GB/s 下交接 23.0/11.5 ms vs 48.3/24.2 ms；展开保存 128 头为 4.77 MiB/token，是紧凑的 71 倍；图 9-6 双面板、双缓冲两端 1.07 GiB、每秒 8/16 次交接需 4.6/9.2 GB/s，同一条 11 GB/s 链路结论翻转；9.2.3 $\mu_{PD}$ 链路项 20.7/41.4 vs 43.4/86.9 请求/s、链路成约束的条件 $B_{\rm net}<\mu V_{KV}$；紧凑路径每步额外 2.05 GFLOPs（kv_b_proj 与表 2-3 一致）使 D 池 8→7.96 请求/s，转折批量提前；9.3.4 临界启动时间 GQA 680/340 μs vs MLA 324/162 μs（图 9-18），整条请求上 128 步 AF 累计 49.1 ms 是 MLA 一次 PD 交接 23.0 ms 的两倍多；9.5.2 共享池各算例并列（100 次远端读 4.83 vs 2.30 s、等待会话 11.25 vs 5.36 GiB·s、写加取回 96.6 vs 46.1 ms）；9.7 综合比较表加 MLA 行（12 GiB 驻留 8→21 份、64 GiB 池 56→119 份、通道能力翻倍）；新增练习 9-11、小结一句、脚注 `[^mla-handoff]`、`[^pool-mla]`）；`manuscripts/06-超节点.md` 6.6 与 `manuscripts/08-单实例推理.md` 8.4.2 各加一句指向 9.2.2；`manuscripts/ch09/` 的 build.py、teaching_revision.py、verify.py、sources.json、README.md 同步。
- 关键数字来源：`calculations/results/pd-af-handoff-qwen8-mla.md`（及 `-mla-50gbps`、`-50gbps` 对照）、`pd-pool-mla.md`（及 `-mla-50gbps`、`pd-pool-50gbps`）、`kv-comparison-n8192-b1.md` 的 deepseek-v3 行、`v3-forward-decode.md` 的 kv_b_proj 行；$d_c=512$、$d_r=64$ 取自 `references/files/papers/deepseek-v2.pdf` 第 12 页。
- 命令与结果：`.venv-site/bin/python manuscripts/ch09/build.py` → 111 个图文件、0 处文字出界；`.venv-site/bin/python manuscripts/ch09/verify.py` → passed，0 错误；`.venv-site/bin/python manuscripts/check_teaching_reading.py 9` → 1440/390 两宽度 passed（37 图、167 公式）。

### 2026-09-11 中文表达通读与各章验证收尾（最后一步）

- 通读范围：仅本轮新增／修改的段落（以各章 `git diff` 为界），六章并行润色（04、05、07、09、10、12），共约 60 处语言修订，全部为语序、搭配、量词、术语一致性问题；数字、公式、图号、脚注均未改动。无 hedging／editorial note 命中。每章润色后重跑 build.py、verify.py、check_teaching_reading.py 均通过。
- 顺带修正的内容问题：第 7 章 7.2.5 的“交错环（图 7-4）”改为图 7-5（改号时遗漏）；第 6 章 NCCL 段“不能据此复用”改为“并不意味着可以复用”（触发既有的防御性结尾检查）；第 6 章 ASHRAE 段“白皮书给出”改为“白皮书指出”。
- 第 4 章 verify.py 修复：大纲回退读 `archive/outlines/` 并允许正文与归档大纲的相互出入（正文有 4.8、大纲有已撤下的 4.7.4），版式检查并入 `evolution-layout-validation.json`（8 幅演进图此前漏并）。修复后 981 项检查 0 错误。
- 第 6 章 verify.py 修复（同上大纲回退）：版式检查并入 `parallel-layout-validation.json` 与 `ub-ep-layout-validation.json`；`figure-6-context-dependency` 的生成脚本未入库，其版式记录量自 SVG（420 pt 画布、字形轮廓缩放 0.11 即 11 pt）与 PNG 目检，以注释形式写入 verify.py。`sources.json` 更新 5 个因 reproduce 重跑产生的浮点表示差异锁（数值不变），补登 `ashrae-liquid-cooling.pdf` 与 `dgx-superpod-h100-ra.pdf`。修复后 152 项检查 0 失败，两宽度阅读检查通过。
- 第 8 章：正文仅一句交叉引用（8.4.2 指向 9.2.2）；`sources.json` 更新 4 个锁（仅为 .html→.md 快照迁移，无数值变化），重构建与两宽度阅读检查通过。
- 现存已知失败（均为本轮之前已存在，见各步骤记录）：第 7 章 verify 的“大纲覆盖”（正文多 7.6.5）与“book-size visible text”（UB-EP 图版式另存）；全量 unittest 的 119 处缺失归档 .html 快照错误。

### 2026-09-11 第 7 章审阅修订（交换网络内容的复核，不含 ECMP 哈希段）

- 改动文件：`manuscripts/07-数据中心网络.md`（7.1.1 网卡改为“经 PCIe 交换芯片与这张卡相连，不与其他卡共用”，20 ms／17 ms 输入的交叉引用改指例题 7.6（第 7.6.2 节）；7.1.2 超售句改为 3:1 叶交换机 48 下行／16 上联、两层 3072 端点／80 台交换机、64 叶共 1024 条上联、半分带宽 25.6 TB/s 为同样 3072 端点无阻塞值 76.8 TB/s 的三分之一、每端点份额 50→16.7 GB/s；第二张表后加一句说明表中为纯传输时间、7.6.4 的跨域项另含 $2(H-1)$ 轮各 5 μs 即 128 卡时 70 μs 启动，故两处分别为 17.5／17.6 与 52.5／52.6 ms，7.6.4 对应句加“（含 70 μs 启动）”；“讨论”段的 $f=1$ 加条件：配对两台服务器不在同一叶交换机（多轨拓扑中不在同一 32 台服务器组）下时 $f=1$，同组时 7.2.5 的对齐配对使 $f=0$；7.2.2、7.3.2、7.3.3、7.6.3 四处 25／40 GB/s 链路在首次出现处标明为该算例的给定输入；7.4.2 “都是教学假设”改为“是本节给定的取值”；7.5.1 补拓扑前提：两项作业各自的两台服务器分属不同 32 台服务器组、同一 rail 的流经上联走脊层并被哈希到同一条上联；7.5.2 incast 段改写：直接 ReduceScatter 一轮完成，$N$ 个参与者按同一顺序逐个目标全速发送时每个目标在自己的时段收到 $N-1$ 条流、$N=8$ 时为 350 GB/s 而非 50 GB/s，错开顺序则任一时刻只收一条流；7.6.2 单网卡句改为每台服务器八份分片经剩下的一张网卡、每轮每方向 96 MiB、两轮共 192 MiB；脚注 `[^ecmp]` 的 UEC 概述 Go-Back-N 页码 3→5（按换页符计）；7.5.3 “多条路径上的哈希冲突”段与图 7-48 未动，由另一路重推）；`manuscripts/ch07/teaching_revision.py`（图 7-14 下半图脊交换机加高至 0.07，两条代表路径 0→rail 0→脊→rail 1→9、5→rail 5→脊→rail 6→14 用 FancyArrowPatch 路径整段画出并在脊交换机框内标注“0 → 脊 → 9”“5 → 脊 → 14”，其余八对保留灰色上下箭头；420 pt 画布、标签 11 pt）。
- 数字来源：`calculations/results/clos-cut-k64-oversub3.md`（3072 端点、80 台、25.6 TB/s、1/3）、`clos-cut-k64-nonblocking.md`（51.2 TB/s、每端点 50 GB/s）、`supernode-scaling-book.md`（128 卡跨域 17.6 ms、112 GB、H=8）、`gradient-fp32-hierarchical-nic1.md`（跨服务器每方向 201,326,592 bytes 分两轮、cut 每方向 4.03 ms）、`incast-feedback-book.md`（$N-1$ 个发送方各 50 GB/s、350 GB/s）、`references/text/dgx-superpod-h100-ra.txt`（32 台一组同 rail 一跳、跨 rail 经脊层）、`references/text/uec-overview.txt`（Go-Back-N 在第 5 页）。76.8 TB/s 与 16.7 GB/s 由 3072/2×50 GB/s、25.6 TB/s÷1536 算出。归档的 HGX/DGX 文本只写到 NVSwitch 与 PCIe Gen5，未明写网卡经 PCIe 交换芯片接 GPU，7.1.1 该句沿用 `[^hgx]`。
- 命令与结果：`.venv-site/bin/python manuscripts/ch07/build.py` → 186 个图文件、269 个公式、0 处文字出界；`.venv-site/bin/python manuscripts/check_teaching_reading.py 7` → 1440/390 两宽度 passed（62 图、269 公式）；`manuscripts/ch07/verify.py` 直接运行时在 SHA256 检查处因 `calculations/results/hash-collision-{8-rails,16-16,64-64,128-16}.{json,md}` 缺失而中止（另一路正在重推 ECMP 场景），临时容忍缺失文件后 198 项检查仅剩“链接目标存在”“SHA256 一致”两项失败，均只涉及这八个缺失文件，待其重生成后需重锁 `sources.json` 并重跑。禁用词 grep（教学假设／仅作／不代表实测／待核／下一步／需要注意的是／本书认为／值得一提／需实测／声明输入）：正文无命中。

### 2026-09-11 第 4、5 章审阅修订（4.1.3 能耗与芯片物理、4.8.4、实验 4-8、5.1.5）

- 改动文件：`manuscripts/04-加速器架构.md`（4.1.3：静态功耗改为“与翻转无关的漏电”；LogicFolding 例补齐电压 0.42 × 频率 0.37 = 0.16 与论文 0.34 的差来自并行度（一大二小三核→四大核，论文 §V）；能耗表定义 pJ，MB 级 SRAM 行来源改为“Dally Hot Chips 2023 第 52 页，工艺未注明”；HBM2 分项 2.24+1.21+0.3 = 3.75 pJ/bit、含 ECC 3.92、正文总数 3.97 三个数并列；裸片（die）与中介层（interposer）在首次出现处定义，后文 4.1.3 内改用英文；分账场景改指第 4.8.3 节，合计句补 HBM 行为 28 nm HBM2 模型、计算行为 45 nm 逻辑工艺、结果为量级比较；HBM 表 H200 行改为“6 堆共 144 GB，产品启用 141 GB”，B200 八堆一句改为三代产品的堆数陈述，翻转条件同步去掉“周边能容纳”；功率封顶段改写为通用推导——(700 − 106) W ÷ 989.4 TFLOP/s = 0.60 pJ/FLOP 预算，45 nm 的 HFMA 0.75 pJ/FLOP 与 HMMA 110 pJ（Dally 第 12 页）说明矩阵指令与工艺进步的必要，持续频率比 = b/e（电压不变）或 (b/e)^(1/3)（电压随频率），删去 742 W、0.80／0.93 与把 4.8.3 实测差距归因于功率封顶的句子；4.3.2 共享内存改 164 KB／228 KB；全章 H100 带宽统一为数据手册的 3.35 TB/s（3,350 GB/s），脚注 `[^hbm-stack]` 注明白皮书 3352 GB/s 与 TSMC 4N；4.8.4 “下界”改“量级估计”；实验 4-8（c）改为先求预算 b、取 e = 1.5b 求持续频率比，再用一步能量（32 × 每 token 能耗）除以一步时间得平均功率；脚注 `[^energy-table]` 补 Dally 第 12 页各行）；`manuscripts/ch04/energy_physics.py`（图 4-7 重画为持续频率比随 e/b 的两条通用曲线，标 e/b = 1.5 处 0.67／0.87；HBM 带宽常量改 3350e9；`energy-figure-data.json` 去掉 peak_compute_w 与 H100 持续比，记录预算与示例点）；`manuscripts/ch04/README.md`（图 4-7 说明）；`manuscripts/05-算子与运行时.md`（5.1.5：228 KB 注明 233,472 bytes，预留 1 KiB、剩余 34 KiB、第三块 97 KiB 统一为二进制单位；MMA 计数改为“每个 warp 负责 16 个 16×8 输出片，沿 K 四步共发 64 条”；4.2.3 交叉引用改为矩阵单元、共享内存接口与指数单元三种资源，生产者 warp 驱动拷贝、消费者 warp 驱动矩阵与指数运算）；`manuscripts/ch05/structure_figures.py`（图 5-4 标签“剩 34 KiB”）。
- 数字来源：`calculations/results/energy-ledger-book.{md,json}`；`references/text/dally-hotchips2023.txt` 204 行（HFMA 1.5 pJ 2000%、HMMA 110 pJ 22%）、683–697 行；`fgdram-micro17.txt` 124、195–204 行；`logicfolding-energy.txt` 219–220、262–265 行；`nvidia-h100-spec.txt` 383 行（3.35 TB/s）；`nvidia-h100.txt` 233 行（TSMC 4N）；`micron-hbm3e-page.txt` 242、282 行；`nvidia-hopper-tuning.txt` 56 行（228 KB）；`calculations/results/sm-occupancy-book-tile.md`。
- 命令与结果：`.venv-site/bin/python manuscripts/ch04/build.py` → 114 个图文件、0 处文字出界；`manuscripts/ch05/build.py` → 117 个图文件、129 个公式、0 处版式警告；`manuscripts/ch04/verify.py` → 982 项检查、0 错误；`manuscripts/ch05/verify.py` → 0 错误；`check_teaching_reading.py 4 5` → 两章 1440／390 两宽度均 passed（38 图 298 公式；39 图 129 公式）。禁用词 grep：新增正文无命中，唯一命中为第 5 章既有脚注 `[^sm]` 的“声明输入”。
- 未改：第 12 章 12.1.5 仍称 0.534 J 为“下界”（与本次 4.8.4 改法不一致，待作者定夺是否同改）；4.5.1 及以后各节沿用“裸片”一词。

### 2026-09-11 第 12 章审校修订：BBR／Mathis 常数、丢包方向与 38 ms 模型时间、12.1.5 承诺句、数据驻留与电池、Unsloth 容量账

- 改动文件：`manuscripts/12-端边云协同.md`（12.1.5、12.3.3、12.5.1、12.5.2、练习 12-10(b)、脚注 `[^phone-ledger]`、`[^bbr]`、`[^wan-loss]`）；`calculations/scenarios/book.json`（`wan_loss_model` 两行的 `declared_model_seconds` 0.03→0.038，来源改指路径记录第 554 行“Transcribing a 355KB file takes the model 38ms”；未新增行）；`calculations/results/wan-loss-model-{book,p036}.{json,md}`（仅按 `reproduce.save` 的写法重生成这两个场景，未跑全量 reproduce，`results/manifest.json` 留待全量 reproduce 刷新）；`manuscripts/ch12/sources.json`（四个 wan-loss 锁刷新）；`manuscripts/ch12/teaching_revision.py`（图 12-18 五行数值）；`manuscripts/ch12/verify.py`（串行预算、期望／p99、FEC 完成、3.6% 尾部与 12-10 答案键改为 38 ms 口径，答案键加 `(1-p**n)**245` 轮数分布）。
- 12.3.3：BBR Startup 增益改为 $4\ln2\approx2.77$（草案 BBRStartupPacingGain=4·ln(2)），正文与 `[^bbr]` 同改；Mathis 段说明延迟确认时 $C<1$、论文式 (4) 给出 $C=1$ 的简化上界，0.155 Mbit/s 按式 (4) 计算（路径记录同法），并给出 $C=1.22$ 的 0.189 Mbit/s；擦除方向改为 Irvine→贵阳的下载方向，补“贵阳→Irvine 41,663 个报文零丢包，两个方向像两条路径”；模型时间 30→38 ms 后串行预算 238.5→246.5 ms、期望 0.756→0.764 s（3.2→3.1 倍）、p99 1.24→1.25 s（仍 6 轮）、FEC 完成 241→249 ms、3.6% 时 p99 0.84→0.85 s，图 12-18 数据与题注同步；`[^wan-loss]` 注明 38 ms 出自转写该文件的记录，固定文件一节的约数 30 ms 由 12.3.5 沿用，FEC 输入改为陈述句。
- 12.1.5：翻转条件段接上开头承诺——2.9 秒在 q4_0 的 15.7 token/s 下对应每轮约 45 个 token（BF16 约 15 个）；每轮预算改为 2.25 s 扣 0.3 s 终端工作剩 1.95 s、约 30 个 token（原 35），并指出 45 个已超预算即 12.5.2 端侧 64 s 超期的原因；“练习 12-9 复算三档设备”改为“手机一档”；三处“声明输入”改为“按四条 x16 通道计”／“手机内存取 8、12、16 GB 三档，系统与工作区预留 4 GB”，来源说明保留在 `[^phone]`、`[^phone-ledger]`；按协调方要求，4.1.3 的 0.534 J 改称“约 0.534 J 的量级估计”。Unsloth 新增一段一表：全参数混合精度 Adam 16 bytes／参数下 24 GB 约 1.5B、96 GB 约 6B 参数；BF16 权重＋rank-16 Q／V LoRA（Qwen3-8B adapter 14.6 MiB≈765 万参数，状态约 0.12 GB，合计约 16.5 GB）下 24 GB 约 12B、96 GB 约 48B 参数。
- 12.5.1 表加“截图去向”列（留在设备／传到附近工作站／传到云地域）并加数据驻留一段：不允许截图离开设备时先排除两条上传行再比较。12.5.2 加电池一段：20 轮×约 45 token≈900 token，按 0.576–0.756 J/token 约 520–680 J；归档 iPhone 16 Pro 规格页只有 27 小时视频播放、无瓦时数，故按 MELTing Point 一次充电 490–590 个提示折算，20 轮约占 3.4%–4.1%。练习 12-10(b) 去掉“查结果文件”，改为给出几何分布与 $(1-p^n)^{245}$ 的轮数分布及 p99 取法。
- 命令与结果：`.venv-site/bin/python manuscripts/ch12/build.py` → 34 图、131 公式、0 处文字出界；`manuscripts/ch12/verify.py` → passed，513 项检查 0 错误；`check_teaching_reading.py 12` → 1440/390 两宽度 passed。禁用词 grep：正文仅“判断下一步”“下一步优化的重点”两处普通用法，“声明输入”正文已清零。

### 2026-09-11 ECMP 哈希冲突场景改为 n 条流哈希到同一台叶的上联（7.5.3 收尾）

- 改动原因：7.2.5 错位配对的八条流从八台不同的叶交换机出发，不会在同一台叶的上联上冲突，旧的 n 对 n 场景（`hash-collision-{8-rails,16-16,64-64,128-16}`）对多轨拓扑不成立。新场景为 `hash-collision-{8-on-32,8-on-16,32-on-32,32-on-16,128-on-16}`：同一台叶交换机（一条 rail）上的 n 条跨组流哈希到该叶的 32 条上联（k=64 无阻塞叶，clos-cut-k64-nonblocking）或 16 条上联（3:1 超售叶，clos-cut-k64-oversub3）。场景与测试（`scenarios/book.json`、`tests/test_hash_collision.py`、`topics/hash_collision.py` 的 assumptions）此前已由另一路改好，本步骤补齐结果与正文。
- 结果：`python3 calculations/calc.py reproduce` 全量运行成功（退出码 0，2502 个 artifact；此前记录的 .html 快照问题已不存在），生成五个新 id 的 json+md 并自动同步 `results/README.md` 的“概念覆盖补齐”段与 `results/manifest.json`。关键数字（E[max]／p99／无冲突概率）：8-on-32 为 1.66／3／38.6%；8-on-16 为 2.06／4／12.1%；32-on-32 为 3.53／6／$1.8\times10^{-13}$；32-on-16 为 4.83／8／0；128-on-16 为 13.36／18／0。整组相对无冲突的速度 $\lceil n/m\rceil/\mathrm{E}[L_{\max}]$：60.1%、48.5%、28.3%、41.4%、59.9%（最后一行与旧文口径一致）。逐包喷洒参数未变，8-on-32 行的喷洒输出与旧文一致：28.97 μs（约 29.0）对单路径 168.8 μs，峰值乱序缓存 339 个报文、1.39 MB。
- 正文：`manuscripts/07-数据中心网络.md` 7.5.3 的“多条路径上的哈希冲突”段、数字表、翻转条件、图 7-48 题注、实验 7-14 第一问（改为求 16 条流分到 32 条上联：E[max] 2.39、相对无冲突的速度 41.8%，与表中同样 $n/m=1/2$ 的 8 条流分到 16 条上联的 48.5% 比较）、脚注 `[^ecmp]`（改引五个新结果文件）。表与题注的指标在收尾时由 $(n/m)/\mathrm{E}[L_{\max}]$ 改为 $\lceil n/m\rceil/\mathrm{E}[L_{\max}]$（相对无冲突的速度），避免 $n<m$ 时把未用满的上联计入分母。逐包喷洒段与超以太网段未动。
- 图：`manuscripts/ch07/visuals.inc.py` 的 `7-ecmp` 数据改读五个新 id（喷洒取自 8-on-32）；`teaching_revision.py` 的图 7-48 改为分组柱状图：x 轴为流数 8／32／128，两个系列为 32 条上联（无阻塞叶）与 16 条上联（3:1 超售叶），值为最忙上联流数／无冲突时（1.66/2.06、3.53/2.42、—/1.67），420 pt 画布、标签 11 pt，已目检。
- 锁与校验：`manuscripts/ch07/sources.json` 的八个旧 id 锁替换为十个新锁；`figure-data.json`、`validation.json` 由 build/verify 重生成。`verify.py` 的 ecmp 算术检查为场景无关写法，未改。归档大纲 `archive/outlines/07-数据中心网络.md` 的 7.5.3 不含旧数字，未改。本文件第 45、83–84、106 行的旧 id 为当时执行记录，保留不改动，以本条为准。
- 命令与结果：`python3 -m unittest calculations.tests.test_hash_collision -v` → 4 项 OK；`.venv-site/bin/python manuscripts/ch07/build.py` → 186 个图文件、268 个公式、0 处文字出界；`.venv-site/bin/python manuscripts/ch07/verify.py` → passed，198 项检查 0 失败（步骤 0 记录的两项预存失败此前已修，本次全绿）；`.venv-site/bin/python manuscripts/check_teaching_reading.py 7` → 1440/390 两宽度 passed（62 图、268 公式）。`test_coverage` OK；`test_delivery` 的 scenarios_without_results 失败为并行工作新增场景（rtxpro6000、qwen3-30b/32b 32K 等）尚未 reproduce，与 ECMP 无关，随协调方下一次全量 reproduce 消除。
- 遗留：`book/build/AI-Infra-Book/07-数据中心网络.md` 是旧的整书构建输出（未入库），仍引用旧 id 与旧页码，待下次整书构建刷新。

### 2026-09-11 第 10 章审阅修订：零气泡定义与数值、交错式层划分、FP16 下溢阈值、FP8 措辞、掉队者响应、三幅调度时间线

- 改动文件：`manuscripts/10-训练系统.md`、`manuscripts/ch10/build.py`（为交错式、零气泡、DualPipe 从结果 json 的 `events` 与 `partition` 取出逐阶段事件写入 figure-data `10-21`～`10-23`，不做重构或补造；每卡空闲改读 `stages[0].idle_during_training_seconds`；`new_numbers` 改为 21/22/23 时间线、24 柱状汇总、29 容量因子）、`manuscripts/ch10/teaching_revision.py`（新增三幅时间线，与图 10-17/10-18 同为 0—355 ms 刻度、蓝前向／橙反向或 dX／紫 dW／绿更新，第二层块或反方向加斜线）、`manuscripts/ch10/verify.py`（零气泡新数值、零传输空闲等于 ZB-H1 下界、`bubble_bound_zb_h1/h2_seconds`、阶段峰值、时间线事件种类与每阶段 240 ms 计算量、交错式第二块 64 个事件、DualPipe 反方向 48 个事件；容量因子图数据键 10-26→10-29）、`manuscripts/ch10/sources.json`（刷新六个重新生成的调度结果锁：interleaved/zero-bubble/dualpipe 的 m8、m16；交错式与 DualPipe 数值未变，1F1B 与填满排空未锁定且数值未变）、`manuscripts/ch10/README.md`（图清单 43→46 幅，插入 10-21～10-23，后续顺延 +3）。
- 零气泡（10.3.2）：T_B 改为只算 dX、T_W 为 dW，20 ms 反向拆为 T_B=T_W=10 ms；1F1B 气泡 (p−1)(T_F+T_B+T_W)=90 ms，ZB-H1 下界 (p−1)(T_F+T_B−T_W)=30 ms，ZB-H2 为 0（zero-bubble.txt 表 2）；删去“文献的构造假定 T_W<T_F<T_B”，改为论文手工调度按 T_F=T_B=T_W 构造、表 2 给出一般形式。事件模型改用论文图 3 上半部分的 ZB-H1 槽位顺序（另一代理重写，结果 18:19 落盘）：M=8 完成 283 ms、每卡空闲 42 ms（零传输反事实空闲恰为 30 ms，其余 12 ms 为传输暴露），阶段峰值 921/921/921/1,226 MiB（阶段 3 高于 1F1B 的 308 MiB，因模型把延后 dW 的激活按完整一份保留；论文表 2 的 ZB-H1 峰值与 1F1B 同为 pM_B），M=16 为 523 ms。据此改写：对照表行、“完成时间最短的是零气泡”、交错式段删去“时间收益最大”、16 微批次句 577→523、习题 10-4 的 337→283、脚注 `[^schedules]` 的槽位顺序说明；DualPipe 段注明 B 沿用 DeepSeek-V3 记号指完整反向。
- 其余修订：交错式层划分改为卡 0 执行第 0—4 层与第 20—23 层（前四块五层、后四块四层，依 interleaved-m8.json 的 partition）；FP16 改为 2⁻¹⁴≈6.1×10⁻⁵ 以下只剩次正规精度、2⁻²⁴≈6×10⁻⁸ 以下归零，并引混合精度论文“约 5% 的梯度指数小于 −24”；FP8 行与表头改为矩阵运算用的 FP8 操作数副本，主权重、梯度与优化器状态另存（DeepSeek-V3 §3.3），16 bytes/参数的常驻状态不降到 1；掉队者“等待”改为结果文件的 1.65σ（8 卡，1.72 s）、0.78σ（48 卡，0.81 s）、0（1024 卡），“重分配”注明 σ=2%，给出等待基线 59.0/58.2 s 以显示重分配更慢；损失尖峰改为“常见处理是回滚到上一份检查点”；正文删去“本章没有对它实测…声明输入”“两个标准差都是声明输入…需要实测”“槽位顺序为声明输入”（两处）“（声明输入…）”“（声明输入）”，来源说明只留脚注；10.1.3 结尾“下一步要判断”改为“接下来要判断”。
- 新图：`manuscripts/ch10/figure-10-pipeline-interleaved.{svg,png,pdf}`（图 10-21）、`figure-10-pipeline-zero-bubble.{svg,png,pdf}`（图 10-22）、`figure-10-pipeline-dualpipe.{svg,png,pdf}`（图 10-23）；柱状汇总 `figure-10-pipeline-schedules` 改为图 10-24；原图 10-22～10-43 顺延为 10-25～10-46，正文与小结内引用同步改号，其他章无图 10-N 引用。
- 命令与结果：`.venv-site/bin/python manuscripts/ch10/build.py` → 138 个图文件（46 图 × 3 格式）、293 个公式、0 处文字出界；`.venv-site/bin/python manuscripts/ch10/verify.py` → passed，0 错误；`.venv-site/bin/python manuscripts/check_teaching_reading.py 10` → 1440/390 两宽度 passed（46 图、293 公式）。禁用词 grep：正文唯一命中为 10.1.2“计算下一步权重”（字面义），其余“声明输入”只在脚注。三幅时间线与更新后的图 10-24 已目检。

### 2026-09-11 第 10、12 章新增段落的中文终审（语言通读）

- 执行记录：只改语言，不改事实；范围为第 10 章 10.1.1 临界批量段、10.1.2 精度段、10.3.2 新增调度段／对照表后文字／图 10-21～10-24 题注、10.3.4 容量因子段、10.4.5、改动过的习题与五条新脚注，第 12 章 12.1.5、12.3.3、12.5.1–12.5.2 新增段、练习 12-9／12-10 与新脚注。第 10 章 41 处、第 12 章 25 处修订：首次出现处补一句定义（临界批量／梯度噪声尺度、Hessian、掉队者 straggler、容量因子即平均分派量的 $c$ 倍、$PP$ 与 $M_B$、LPDDR5X、GGUF、adapter、Mathis 模型、MSS、BDP、cwnd、pacing、容量膝点、FEC 的符号即 MSS 大小报文、数据驻留）；FP16 下溢与损失缩放、零气泡的公式与本例数值各拆成两段；交错式层划分改写为“36 层切成八块、每卡两块合计九层”；段首补结论句（掉队者三种响应、调度两轴无一占优）；去掉“归档文献”“归档的 iPhone 16 Pro 规格页”等仓库用语，习题中的“教学参数／教学输入”改为“本章给定的参数／输入”，三条脚注的“声明输入”改为“本例取值”；12.3.3 的“同样 354,640 bytes 的数据”改指第 12.3.2 节那份约 355 KB 的语音请求；12.1.5 LoRA 状态“梯度、主权重与两份矩合计 16 bytes”补上 adapter 权重本身（2+2+4+4+4）。数字、公式、图号、脚注标记逐项比对未变。命令与结果：`.venv-site/bin/python manuscripts/ch10/build.py` → 138 个图文件、295 个公式、0 处出界；`manuscripts/ch10/verify.py` → 0 错误；`manuscripts/ch12/build.py` → 34 图、131 公式、0 处版式警告；`manuscripts/ch12/verify.py` → 0 错误；`check_teaching_reading.py 10 12` → 两章 1440／390 两宽度均 passed。留待作者定夺（未改）：10.3.2“16 个微批次时……继续拆细的绝对收益随之减半”与结果不符（交错式省下的时间 m=8 为 49 ms、m=16 为 61 ms，减半的是相对收益）；10.1.1 先说 1.57 倍“已经越过”步数减半区间、段末又说“已接近”收益开始衰减的位置，两处措辞不一；10.4.5 附加比例 0.0245／0.0252 与 0.95%／1.12% 两种写法并存；范围外仍有“教学条件”（12.5.1 第 537 行、练习引言第 655 行）与第 10 章旧脚注中的“教学输入”四处。

### 2026-09-11 第 4、5 章新增段落的中文终审（4.1.3 能耗与芯片物理、4.8.4 能耗句、实验 4-8、5.1.5 及脚注 `[^sm]`）

只改 `manuscripts/04-加速器架构.md` 与 `manuscripts/05-算子与运行时.md` 两个文件，共约 50 处，数字、公式、图号、脚注标记都没有动。改法分三类。一是给段落加主题句、拆分：搬运耗电段先说“能耗来自给连线充放电”；HBM2 分解段先给结论“能耗大部分花在 DRAM 内部”，删掉段末重复的那句；能耗分账段先说“能量几乎全部花在搬运上”，再讲工艺口径和时间；带宽段以“带宽只决定一步多快，不改变一步耗多少能量”开头；LogicFolding 的 DSP 例、5.1.5 的 MMA 两级发出方式各自拆成独立段落。二是在首次出现处补定义：TOPS、DSP、投影面积、功率密度、SRAM／DRAM／LPDDR、基底 die、行激活、中介层（移到首次出现处）、ECC、光罩与光罩上限、TSV、HFMA、$P_{\mathrm{mem}}$／$F_{\mathrm{peak}}$；第 5 章补了 warp（首段先给一句，下文正式定义）、驻留、线程槽、延迟隐藏、异步拷贝、张量描述符和覆盖率。三是理顺语句：功率密度的翻转条件统一用“面积比／功耗比”；“第 4.5.1 节计算过”“第 5.2.2／5.2.3 节”这类向后引用改成将来时；“从 45 nm 到这里”改为“从 45 nm 到 4N”；5,120 位接口一句补上“再除以 8”；4.8.4 的“这个积分”改为“一步 decode 耗电”；HBM2 分解中的“论文”写明是 MICRO 2017 论文，避免和 LogicFolding 混淆；5.1.5 首段的“均为本节的给定输入”改为直接陈述取值，`[^sm]` 中的“声明输入”改为“是本节选定的取值”。只报告、没有改动的问题：4.1.3 中 Amdahl 的 $f$ 和动态功耗的时钟频率 $f$ 同节重名；4.1.3 现在已经定义 die，4.5.1（原第 591 行）又定义了一次“裸片（die）”，4.5 节以后仍写“裸片”；能耗表导语说“三个来源”，表中实际列了 Horowitz、Dally、NVIDIA 与 MICRO 2017 四处；功率封顶段用 HBM2 的 31.76 pJ/byte 估算 H100（HBM3）的内存功率；5.1.5 首段说 FP32 累加器放在共享内存，MMA 段却说每个 warp 持有 16 片的累加器（mma.sync 的累加器在寄存器中）。命令与结果：`.venv-site/bin/python manuscripts/ch04/build.py` 得到 114 个图文件、0 处文字出界；`manuscripts/ch05/build.py` 得到 117 个图文件、129 个公式、0 处版式警告；`manuscripts/ch04/verify.py` 共 982 项检查、0 错误；`manuscripts/ch05/verify.py` 0 错误；`check_teaching_reading.py 4 5` 两章在 1440／390 两个宽度下都通过，第 4 章 38 图、300 公式（新加的 $P_{\mathrm{mem}}$、$F_{\mathrm{peak}}$ 使公式数比原来多 2 个），第 5 章 39 图、129 公式。禁用词 grep 在两章的范围内没有命中。

### 2026-09-11 计算修正：零气泡改为 ZB-H1 槽位顺序、ECMP 哈希场景改为同一叶交换机

- 零气泡（`calculations/src/infra_calc/topics/training_pipeline_schedule.py`，policy `zero_bubble`）：旧规则“微批 i 的 W 跟在微批 i+(p−1−g) 的 X 之后”把 W 全部推到尾部，M=8 时完成 337 ms、每卡空闲 96 ms，与未拆分的填充—排空相同，不是 ZB-H1。现改为 zero-bubble 论文图 3（上）的顺序：阶段 g（从 0 计）按 1F1B 排 F 与输入梯度反向 X（论文的 B），预热 p−1−g 个 F；微批 i 的权重梯度 W 紧跟在微批 i+g 的 X 之后，最后 g 个 W 放在最后一个 X 之后，每个阶段在途都是 p−1 个微批的待做工作。p=4、M=8 时四个阶段的 F/B/W 序列与论文图 3 逐槽一致（新测试逐项比对）。T_F=10 ms、T_B（dX）=T_W（dW）=10 ms（20 ms 反向按 declared_weight_backward_fraction=1/2 拆分）。
- 新数字：M=8 完成 283 ms（1F1B 347 ms），每卡空闲 42 ms，其中不计传输的反事实空闲 30 ms，恰为 ZB-H1 界 (p−1)(T_F+T_B−T_W)=30 ms；ZB-H2 界 0。M=16 完成 523 ms（1F1B 599 ms），空闲同为 42／30 ms。激活预留峰值（保存集按 F 开始到 W 结束整段预留）四个阶段为 921／921／921／1,225.8 MiB，阶段 0 与 1F1B 的 921 MiB 相同（论文 pM_B 的性质）；阶段 3 较高，因为本模块对延后的 W 仍预留完整保存集而不是论文中较小的 M_W。
- 所有扩展调度的 summary 新增 `bubble_bound_1f1b_seconds`（90 ms）、`bubble_bound_interleaved_seconds`／`_fraction`（v=2 时 45 ms、M=8 为 0.1875）、`bubble_bound_zb_h1_seconds`（30 ms）、`bubble_bound_zb_h2_seconds`（0）、`bubble_bound_dualpipe_seconds`（DeepSeek-V3 表 2，F&B=F+B、B 取完整反向：20 ms）、`bubble_bound_policy_seconds`、`idle_per_stage_seconds` 与 `idle_per_stage_zero_transfer_counterfactual_seconds`；`bubble_reference` 字段改名 `zb_h1_or_zb1p_bubble`、`zb_h2_bubble`。交错式（M=8 297.7 ms）与 DualPipe（M=8 306 ms、M=16 558 ms）调度未变。
- ECMP 哈希（`hash_collision.py` 与 `scenarios/book.json` 的 `hash_collision`）：旧的 `hash-collision-8-rails` 把“八条 rail”当成同一台叶交换机的 8 条上行，但多轨拓扑中错位配对的八条流从八台不同的叶交换机出发，彼此不会冲突。新场景都是同一台叶交换机（同一条 rail）上必须过脊层的 n 条流哈希到该叶的 m 条上行，m 取自 `results/clos-cut-k64-nonblocking.md`（32）与 `clos-cut-k64-oversub3.md`（16）。场景与结果：8-on-32 的 E[max]=1.665、p99=3、无冲突概率 0.386、有效割集 15.0%；8-on-16 为 2.063、4、0.121、24.2%；32-on-32 为 3.533、6、1.8×10⁻¹³、28.3%；32-on-16 为 4.830、8、0、41.4%；128-on-16 为 13.357、18、0、59.9%。逐包喷洒部分（8 MiB 分 4 KiB 报文喷到八条路径，完成 29.0 μs，单路径 168.8 μs，峰值乱序缓存 1.39 MB）挂在 8-on-32 行上，数值不变。旧的 8-rails、16-16、64-64、128-16 结果文件已删除；`scenarios/hash-collision-example.json` 同步改为 8-on-32；`tests/_gap_common.py` 允许 `results/` 开头的来源并检查所引文件存在。
- 生成：本次先启动的 reproduce 被中止（exit 143），另一会话的全量 reproduce（18:23 启动，已读取上述改动）写出了全部新结果与 `results/README.md`、`manifest.json`，未再重复运行。
- 命令与结果：`python3 -m unittest discover -s calculations/tests -p 'test_*pipeline*.py'` 19 项 OK；`-p 'test_hash_collision.py'` 4 项 OK。
- 正文未改。第 7 章 7.5.3、脚注 `[^ecmp]`、`manuscripts/ch07/visuals.inc.py`／`teaching_revision.py`／`sources.json` 仍引用旧的 hash-collision 场景名；第 10 章 `manuscripts/ch10/verify.py` 仍检查零气泡 337／577 ms 与阶段 0 峰值 1,610 MiB，需随正文一起改为 283／523 ms 与 921 MiB。

### 2026-09-11 审阅收尾（事实核对、逻辑与表达）

- 三组核对把第 4—7、9、10、12 章新增正文的数字逐项回溯到结果文件与归档原件。修正的主要事实问题：零气泡事件模型改为论文的 ZB-H1 顺序（283 ms，每阶段空闲 42 ms，无传输时恰为 30 ms 的界）；ECMP 场景改为同一台叶交换机上的流竞争该叶的上联，指标改为相对无冲突的速度 $\lceil n/m\rceil/\mathrm{E}[L_{\max}]$；功率封顶改为一般式 $b/e$、$(b/e)^{1/3}$，删去对 4.8.3 实测的归因；能耗分账注明 28 nm HBM2 与 45 nm 逻辑来源并改称量级估计；BBR 启动增益 $4\ln2$、Mathis 式按 $C=1$ 的简化上界、丢包方向（Irvine→贵阳）、ASR 模型 38 ms；FP16 下溢阈值 $2^{-24}$；交错式层块 0—4 与 20—23；超售半分带宽的“三分之一”口径；incast 到达速率的前提。
- 补齐计划遗漏：第 12.5.1 节数据驻留列、第 12.5.2 节电池能耗、Unsloth 单卡微调上限表；第 10.3.2 节三种调度的时间线图（图 10-21—10-23）；图 7-14 画出跨 rail 路径。
- 各章新增段落做了最后一轮中文通读（每段先给结论、术语首次出现即定义、删去编辑性措辞）。第 4、5、7、10、12 章 build、verify 与 1440/390 阅读检查全部通过。
- 与另一会话协调：第 4—7、9、10、12 章已交接给其做真实硬件参数替换（第 6.x 教学加速器、7.6.4 的 200 GB/s 与 100 TFLOP/s、10.6 的 4090 设计输入、12.5 三档设备与成本单位仍是旧输入）。
