--- before/06-超节点.md
+++ manuscripts/06-超节点.md
@@ -1,6 +1,6 @@
 # 第 6 章 超节点
 
-八张加速卡可以各自处理一个请求，也可以共同处理同一个请求。前一种部署方式让更多用户同时得到服务，后一种部署方式把一次推理的权重和计算分散到更多设备。多卡协作减少了每张卡承担的计算和内存访问，却增加了设备间的数据交换与同步等待。设计超节点时，需要比较分摊计算和内存访问节省的时间，以及通信和同步增加的时间。
+八张加速卡可以各自处理一个请求，也可以共同处理同一个请求。前一种部署方式让更多用户同时得到服务，后一种部署方式把一次推理的权重和计算分散到更多设备。多卡协作减少了每张卡承担的计算和内存访问，却增加了设备间的数据交换与同步等待。设计超节点时，需要比较分摊计算和内存访问所节省的时间，与通信和同步所增加的时间。
 
 上一章研究单个设备内的计算、数据复用与任务执行。本章继续研究数据在多个设备之间的传输：先把矩阵和状态分到多张卡，推导由此产生的通信，再把通信放到实际连接上，最后决定一个实例使用多少张卡、同时部署多少个实例。本章以 Qwen3-8B 贯穿整个推导过程；Qwen3-235B-A22B 用来解释专家分工，其他模型和系统则展示结构变化如何改变设计。
 
@@ -215,7 +215,7 @@
 >
 > （d）画出四阶段处理八个微批次的时序，分别考虑四个阶段均耗时 1 ms，以及耗时依次为 1、1、2、1 ms 的情况；比较两种情况下的完成时间，并指出微批次会在哪个阶段之前积压。
 >
-> （e）进阶：对同一个 FFN，比较输出完整张量和保留输出分片时，相邻算子之间需要传递的数据，或沿配套二维网格继续推导通信分块。
+> （e）进阶：对同一个 FFN，比较输出完整张量与保留输出分片两种方式下，相邻算子之间需要传递的数据；也可选择配套二维网格方案，进一步推导通信数据的分块方式。
 
 ## 6.3 MoE 的多卡执行
 
@@ -264,7 +264,7 @@
 
 省掉派发需要付出存储与计算代价。按这份布局，每卡权重约为 64.8 GB，八卡共 518.6 GB，比每个参数只保存一份时所需的 470.2 GB 多约 48.4 GB。8192 位置的每卡 KV 为 752 MiB，八卡共 5.875 GiB，是同一请求逻辑 KV 的四倍。每组重复执行 attention，换来专家输入在本组直接可用。[^ownership]
 
-**例题：注意力复制与专家切分的布局需要多少归约通信？** 归约采用 FP32，即每元素 4 字节的浮点格式，单 token 的完整隐藏张量为 $4096\times4=16$ KiB。普通 ring 在两卡中归约，每卡发送一个完整张量大小；四卡归约则发送 1.5 倍张量大小。每层有 attention TP、专家 TP、专家 EP 三次汇合，所以每卡发送
+**例题：注意力复制与专家切分的布局需要多少归约通信？** 归约采用 FP32，即每元素 4 字节的浮点格式，单 token 的完整隐藏张量为 $4096\times4=16$ KiB。普通 ring 在两卡间归约，每卡发送一个完整张量大小；四卡归约则发送 1.5 倍张量大小。每层有 attention TP、专家 TP、专家 EP 三次汇合，所以每卡发送
 
 $$
 16+16+24=56\ \mathrm{KiB}.
@@ -308,7 +308,7 @@
 
 取 $E=128,k=8,m=8$，期望约有 52 个活跃专家，理想读取约 1.8 GiB；八个 token 全部选择同样八个专家时仍只读 288 MiB。随着批次增长，被选中的专家数逐渐接近专家总数，继续增加输入行主要增加每份权重的复用。[^moe-tax]
 
-真实路由由输入与模型共同产生。已有 DeepSeek V4-Flash 的四个输入共记录约 131 万次专家选择，43 层中各专家被选择的频率各不相同。把每个专家接到的 token 数转换成矩阵行数，再按设备上的专家集合相加，就能从这份路由记录得到各设备的计算量分布。逐层热图保留在配套资料。[^routes]
+真实路由由输入与模型共同产生。已有的 DeepSeek V4-Flash 路由记录覆盖四个输入，共记录约 131 万次专家选择，43 层中各专家被选择的频率各不相同。把每个专家接到的 token 数转换成矩阵行数，再按设备上的专家集合相加，就能从这份路由记录得到各设备的计算量分布。逐层热图保留在配套资料。[^routes]
 
 ### 6.3.4 从数千亿到万亿参数：并行组合怎样变化
 
@@ -320,7 +320,7 @@
 
 模型的变化因此从两个方面影响系统设计：更多权重扩大所需的总存储容量，不同计算图改变需要交换的数据。前者确定至少要动用多少资源，后者决定这些资源应怎样连接。
 
-> **实验 6-3 · 核心：从专家选择到设备负载**
+> **实验 6-3 · 核心：专家分派与放置如何改变设备负载和通信量**
 >
 > （a）沿图 6-7 写出 token A 的输入发送、两次专家执行和加权合并顺序。
 >
@@ -414,7 +414,7 @@
 >
 > （b）求四卡和八卡 ring 的每卡发送量，再推导相同数据量下树形归约的耗时。
 >
-> （c）求八卡部署下两种归约算法耗时相等时的消息大小；将 $\alpha$ 减半后重算，解释交点移动方向。
+> （c）求八卡部署下两种归约算法耗时相等时的消息大小；将 $\alpha$ 减半后重算，解释两种算法耗时相等时所需的消息大小为何变化。
 >
 > （d）将树形算法归约 8 KiB 数据所需的时间代入本章 Qwen3-8B 算例，求八卡单步时间，比较算法选择与带宽翻倍的收益。
 
@@ -461,7 +461,7 @@
 >
 > （b）设采用优化方案需要额外花费 12 ms 准备，求累计节省的执行时间首次超过准备耗时所需的最少调用次数；改成 6 ms 后重算。
 >
-> （c）用两个二元素向量检验归约与 RMSNorm 的执行顺序。
+> （c）用两个各含两个元素的向量，比较先归约后执行 RMSNorm 与先分别执行 RMSNorm 后归约的结果，判断两者能否交换顺序。
 >
 > （d）设备选做：比较同一形状的独占与并发执行，记录就绪、开始和完成事件，说明共享后哪一段改变最多。
 
@@ -663,7 +663,7 @@
 
 *图 6-30：完整读取 16 GiB 的频率与平均带宽需求。路径带宽为 40 GB/s；每事务返回 256 bytes、往返 2 μs 时，128 个在途事务将有效带宽限制到约 16.4 GB/s。*
 
-设路径带宽为 40 GB/s，在途事务数量充足，启动开销为 5 μs，一次读取约用 0.43 s。每秒发起一次读取，有约 0.57 s 余量；每 0.05 s 发起一次，上一次尚未结束，新工作已经到来，队列不断增长。若最多只能同时发出 128 个事务，读取变为约 1.05 s，每秒一次也会逐渐积压。[^pool]
+设路径带宽为 40 GB/s，在途事务数量充足，启动开销为 5 μs，一次读取约用 0.43 s。每秒发起一次读取，两次读取之间有约 0.57 s 的空闲时间；每 0.05 s 发起一次，上一次尚未结束，新工作已经到来，队列不断增长。若最多只能同时发出 128 个事务，读取变为约 1.05 s，每秒一次也会逐渐积压。[^pool]
 
 KV 的使用方式决定了适合把它保存在何处。会话在等待用户下一轮输入时，可以长时间不读取 KV，远端内存主要用于保存状态，在会话恢复时再取回；活跃会话每步都需要上下文状态，反复远端扫描则持续消耗带宽。
 
@@ -718,9 +718,9 @@
 
 连接改变后再沿原式计算。若八卡跨过 6.5.1 的服务器边界，每步约 6.32 ms，八步约 50.5 ms，原来的 40 ms 目标无法满足。要使这一部署满足时限，需要降低新增的通信开销：改善服务器间连接、改变集合通信算法，或将八卡放进更紧密的互联域。
 
-第 4 章的固定权重架构会改变上述取舍。权重由独立 ROM 提供后，原来分摊给多卡的 HBM 权重读取减少，TP 的同步时间在单步耗时中占比上升。减少 TP 卡数能够省去一部分同步，腾出的设备可组成更多独立实例；每个实例则要容纳更大份额的请求状态。前面的逐卡容量表与会话时间表，可以继续用来比较这两项变化。
-
-计算 V4.1 所需的设备数时，除了 890 B/token 的逻辑状态容量，还要考虑状态在设备间的分布。这一数值对跨层共享状态只计一份；如果模型各层被放在不同设备上，后续层仍须访问负责生成这些状态的模型层所产生的结果。可以把相关层放近、在目标设备复制状态，或建立远程访问路径。第一种限制了模型层在设备上的分配方式，第二种增加存储占用和更新开销，第三种增加通信依赖，因此需要按各设备实际保存和访问的数据分别计算资源需求。[^v41-case]
+第 4 章的固定权重架构会改变上述取舍。权重由独立 ROM 提供后，原来分摊给多卡的 HBM 权重读取减少，TP 的同步时间在单步耗时中占比上升。减少 TP 卡数能够省去一部分同步，腾出的设备可组成更多独立实例；每张卡则要容纳更大份额的请求状态。前面的逐卡容量表与会话时间表，可以继续用来比较这两项变化。
+
+计算 V4.1 所需的设备数时，除了 890 B/token 的逻辑状态容量，还要考虑状态在设备间的分布。这一数值对跨层共享状态只计一份；如果模型各层被放在不同设备上，后续层仍须访问生成层保存的这些状态。可以把相关层部署在相近的设备上、在目标设备复制状态，或建立远程访问路径。第一种限制了模型层在设备上的分配方式，第二种增加存储占用和更新开销，第三种增加通信依赖，因此需要按各设备实际保存和访问的数据分别计算资源需求。[^v41-case]
 
 ### 6.7.2 功率、成本与故障范围怎样改变选择
 
