# 第四章 4.6 与 4.8 的整合

原 4.6.4 新增了同模型选型、有效速率和实测预览，与既有 4.8 的建模、Roofline、实测和成本内容发生重叠。本轮按论述作用重组，保留 4.6 → 4.7 → 4.8 的顺序：机制与演进、专用化、完整选型。原稿保存在 [before.md](before.md)。

| 内容 | 最终位置 | 处理 |
| --- | --- | --- |
| NVIDIA／昇腾／Apple 的代际机制与定量算例 | 4.6.1–4.6.3 | 保留；引言改为架构改动的收益，删去重复的通用公式与选型流程 |
| 同一 Qwen3-8B 的跨设备时间表与整模型工作量 | 4.8.2 | 从 4.6.4 整体迁移，图随文迁移；原 4.6.4 取消 |
| 共享资源求和、独立资源取最大值、阶段依赖 | 4.8.1 | 保留独有推导，删去重复的三槽流水复述 |
| Roofline 与 179 行转折点 | 4.8.1 | 合并原 4.8.2；保留公式、图和实验 4-5，删去重复的 batch 与资源翻倍说明 |
| 有效带宽／计算速率及 4090 利用率算例 | 4.8.3 | 从原 4.6.4 迁移，紧接同模型预算，随后用实测检验 |
| Mac／RTX 的实际投影计时 | 4.8.3 | 删除原 4.6.4 的重复预览，完整采集方法、数据与解释只保留一次 |
| MoE 容量和路由变化 | 4.6.3 | 保留完整演算；4.8.2 引用它作候选比较，不重复推导 |
| 任务成本、利用率、能耗与专用投入回本 | 4.8.4 | 保留，使用上一节实测时间进入新的成本计算 |

没有整节删除 4.8：它包含从局部资源预算到执行依赖、测量和经济性的独有推导。没有将 4.8 提到架构之前：前面 4.1–4.5 已足够支持代际算例，4.8 放在所有架构之后才能统一比较。完整流程为：理解改动 → 考察专用方案 → 建模 → 筛选设备 → 实测校准 → 成本与能耗。

整合后八节、二十六小节，保留 34 幅图、三道例题与七项实验。图号按新顺序重排，横向比较图从 4-29 改为 4-32；正文引用与图索引同步。数字与计算程序未改动。

## 体例与具体任务的修订

删除新增段落中重复提醒适用范围、核对参数和追加其他工作等收尾句，将计算条件放入算例开头。4.6 保留机制推导；4.8.2 以 Qwen3-8B 与 Qwen3-235B-A22B 的容量、时间和读取上限形成选型表；4.8.3 用给定有效带宽与串行时间演算 128 步生成任务，再接既有实测。新算例的 80%／90% 带宽与 2／1 ms 串行时间是教学输入，已有 Mac／RTX 实测保持不变。八请求 MoE 的两种路由直接换算为 265／86 token/s 读取预算。多卡并行引用后续章节。
