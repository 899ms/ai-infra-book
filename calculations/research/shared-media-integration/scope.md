# C68 共享媒体公共接入验收范围

将冻结 research/shared-media-transport（9d60611e）接入公共主题、可编辑输入、CLI、16固定场景、32结果、统一复算与第十二章。共享信用/教学恢复、业务依赖、版本与播放质量的合同保持原候选；不重新解释为实际TCP或QUIC。

本次逐字段对照只允许reference_sources/reference_source_root注册元数据差异；官方六原件的revision、URL、SHA及实际bytes单独核验。32次实际CLI覆盖全部16场景的JSON/Markdown，并核32冻结产物。新增10专项tests与全套tests分别记录。

图模块从7份实际公共结果生成四个选定教学面板：同wire HOL、同业务发送调度、同wire播放政策、另一个30MB混合场景首秒窗口。图data.json逐记录核来源，PNG必须目检。图12-4完整要求还包括协议握手与实际记录的恰当连接，不能把这四面板直接当全部要求完成。

公共reproduce完成之前，图agent正常render被旧manifest挡下；没有绕过验证器，待root统一复算之后再运行。原始研究候选保持冻结。

C68仍保留指定CUBIC/BBR和实际流控/恢复、剩余握手、匹配图片/语音/截图业务的协议对照与完整图12-4；相邻C69无线空口/多路径等未由此实现。全书目标保持原范围。

首次图渲染的PNG目检发现左侧长标签裁切、HOL图例遮住末行数值。只修改图模块标签与横轴布局，随后重新统一reproduce并重新绘图；计算topic、场景与32 CLI输出不变。全suite896项（874通过/22可选跳过）完成于该版式修正之前，最终图另以实际渲染、逐记录来源和manifest校验验收。
