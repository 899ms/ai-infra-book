# 第 12 章语言通读：逐句修改对照

本文件记录 2026-09-11 全书通读在本章留下的净改动。

## 全书通读（2026-09-11，按最终文本与通读前快照逐行对照）

共 35 行有改动；英文术语按“首次出现保留中文注释、之后用英文”的原则已回退，下表是回退之后的净改动。省略号表示只截取了改动附近的文字。

| 修改前 | 修改后 |
|---|---|
| 本例将 RTT 简化为往返传播所需的时间，排队和协议处理的耗时在 12.3 节单独讨论。连接已经建立，服务器收齐原图后开始处理，生成完整成片后再向用户发送，上下行以恒定速率传输有效载荷。设输入为 $S_u$ bytes，输出为 $S_d$ bytes，上下行速率为 $B_u,B_d$ bytes/s，往返时间（round-trip time，RTT）为 $R$，处理时间为 $T_c$。串行完成时间为 | 本例把往返时间（round-trip time，RTT）简化为往返传播所需的时间，排队和协议处理的耗时在第 12.3 节单独讨论。连接已经建立，服务器收齐原图后开始处理，生成完整成片后再向用户发送，上下行以恒定速率传输有效载荷。设输入为 $S_u$ bytes，输出为 $S_d$ bytes，上下行速率为 $B_u,B_d$ bytes/s，RTT 为 $R$，处理时间为 $T_c$。串行完成时间为 |
| 上传的最后一个字节离开发送端后，还要传播到服务器；成片返回也要传播一次。两次单向传播合计为 $R$。传播时间… | 上传的最后一个字节离开发送方后，还要传播到服务器；成片返回也要传播一次。两次单向传播合计为 $R$。传播时间… |
| …uter Use（计算机界面操作）让程序像用户一样通过截图、点击和输入完成任务。它在观察界面、模型判断、执行操作和再次观察之间循环。它与音频流水的关键区别是：只有上一轮操作完成、界面更新后，才能取得下一张有效截图。模型还没决… | …uter Use（计算机界面操作）让程序像用户一样通过截图、点击和输入完成任务。程序在观察界面、模型判断、执行操作和再次观察之间循环。这类任务与音频流水线的关键区别是：只有上一轮操作完成、界面更新后，才能取得下一张有效截图。模型还没决… |
| 以下以一个需要 30 轮操作的任务为例。使用原有服务时，每轮上传 0.8 MB 截图… | 下面以一个需要 30 轮操作的任务为例。使用原有服务时，每轮上传 0.8 MB 截图… |
| …旧截图作出的操作就可能出错，需要恢复界面并重新截图。把截图版本与动作编号绑定，让执行端在界面变化时重新截图，可以避免这类无效工作。[^author] | …旧截图作出的操作就可能出错，需要恢复界面并重新截图。把截图版本与动作编号绑定，让终端在界面变化时重新截图，可以避免这类无效工作。[^author] |
| …寸为 $640\times640$，图像块合并后得到 400 个视觉 token，即用来表示图片内容的 400 个特征向量；这里的视觉 token 是图像块合并后的特征向量，不是文本词元，也不是原图的单个像素。最终投影的每个特征向量有 256… | …寸为 $640\times640$，图像块合并后得到 400 个视觉 token；这里的视觉 token 是表示图片内容的特征向量，不是文本词元，也不是原图的单个像素。最终投影的每个特征向量有 256… |
| …重复出现的图片可以复用编码缓存，进一步减少编码次数。相同图片再次出现时，缓存命中省掉一次编码；界面内容改变后则重新编码。在一组使用相同尺寸图片的 CPU 实验中，再次输入同一张图片时可以命中缓存，修改图片内容后则需要重新编码。[^ec-test] 计算多轮任务的编码总时间时，可以用单次编码时间乘以缓存未… | …重复出现的图片可以复用编码缓存，进一步减少编码次数。相同图片再次出现时，缓存命中就省掉一次编码；界面内容改变后则要重新编码。一组使用相同尺寸图片的 CPU 实验观察到了同样的行为。[^ec-test] 计算多轮任务的编码总时间时，可以用单次编码时间乘以缓存未… |
| 会话迁移将缓存和任务进度传到另一台设备，使接收设备可以接着执行。目的端已有兼容权重时，需要传送的是编码缓存、语言 KV、任务记录和恢复信息。确认两端的… | 会话迁移将缓存和任务进度传到另一台设备，使接收设备可以接着执行。目的设备已有兼容权重时，需要传送的是编码缓存、语言 KV、任务记录和恢复信息。确认两端的… |
| …ken 位置索引；截图 Agent 的进度由已经执行的操作决定。一次点击已经改变界面，即使其应答丢失，也应恢复到点击后的状态。操作编号和提交记录使目的端可以查到既有结果，从下一轮继续。保存这些记录可以避免重复执行操作，就像复用缓存可… | …ken 位置索引；截图 Agent 的进度由已经执行的操作决定。一次点击已经改变了界面，即使它的应答丢失，也应恢复到点击后的状态。操作编号和提交记录让目的设备可以查到既有结果，从下一轮继续。保存这些记录可以避免重复执行操作，就像复用缓存可… |
| …每层矩阵分给两台设备）为例，考察每层注意力输出和前馈网络（FFN）输出的两次归约，归约将两台设备的局部结果相加，使后续计算得到完整输出。采用两阶段环算法时，每次归约有两次启动，每个阶段的启动耗时… | …每层矩阵分给两台设备）为例，考察每层注意力输出和前馈网络（FFN）输出的两次归约；归约把两台设备的局部结果相加，让后续计算得到完整输出。采用两阶段环算法时，每次归约有两次启动，每个阶段的启动耗时… |
| 流水线并行 PP 将层分成连续阶段，将通信集中到相邻阶段之间的激活传输。对单个请求，一个 token 仍需依次经过各个阶段；对多个独立请求，不同阶段可以同时处理不同请求。由此得到两种不同的收益：减少通信次数可以缩短单个请求的等待，让不同阶段同时处理请求可以提高总吞吐量。选择 TP 或 PP 时，先画出一个 token 的依赖，再安排多个请求占用各段资源，就能分别计算这两… | 流水线并行（PP）把层分成连续的阶段，通信集中在相邻阶段之间的激活传输。对单个请求，一个 token 仍需依次经过各个阶段；对多个独立请求，不同阶段可以同时处理不同请求。由此得到两种不同的收益：减少通信次数可以缩短单个请求的等待，让不同阶段同时处理请求可以提高总吞吐量。选择张量并行还是流水线并行时，先画出一个 token 的依赖，再安排多个请求占用各段资源，就能分别计算这两… |
| …即通信双方为认证与加密维护的密钥、身份和会话状态；随后取得发送额度，最后等待接收端把数据交给应用。将这些等待加入前面的时间模型，就能分析实际执行过程：数据虽然已经… | …即通信双方为认证与加密维护的密钥、身份和会话状态；随后取得发送额度，最后等待接收方把数据交给应用。将这些等待加入前面的时间模型，就能分析实际执行过程：数据虽然已经… |
| …建立以后，发送方也必须限制在途数据。拥塞窗口限制已经发送但尚未确认的数据量；接收端流量控制根据接收端的缓冲空间，限制发送方还能发送多少数据。确认消息（ACK）告知发送方哪些数据已收到。ACK 到达后，发送方可以继续发送；接收端从缓冲区读取数据后，也会通知发送方新增的可用空间。达到发送上限时，即使链路空闲，… | …建立以后，发送方也必须限制在途数据。拥塞窗口限制已经发送但尚未确认的数据量；接收方的流量控制根据接收方的缓冲空间，限制发送方还能发送多少数据。确认报文（ACK）告知发送方哪些数据已收到。ACK 到达后，发送方可以继续发送；接收方从缓冲区读取数据后，也会通知发送方新增的可用空间。达到发送上限时，即使链路空闲，… |
| 下面单独采用一个整批确认模型：接收端收齐 64 KB 后才确认，从这批发完到确认返回固定为 100 ms。因此完整周… | 下面单独采用一个整批确认模型：接收方收齐 64 KB 后才确认，从这批发完到确认返回固定为 100 ms。因此完整周… |
| …路 20 Mbit/s，一批数据发送需 25.6 ms。为单独显示停等，图设接收端收齐一批后统一确认，从这批数据全部发出到收到整批确认再需 100 ms；每批完整… | …路 20 Mbit/s，一批数据发送需 25.6 ms。为单独显示停等，图设接收方收齐一批后统一确认，从这批数据全部发出到收到整批确认再需 100 ms；每批完整… |
| …0 ms RTT，几轮反馈就是数百毫秒；短请求的大部分时间也可能花在等待确认上。12.3.4 将用这一机制解释一次跨地域语音调用的性能观察。 | …0 ms RTT，几轮反馈就是数百毫秒；短请求的大部分时间也可能花在等待确认上。第 12.3.4 节将用这一机制解释一次跨地域语音调用的性能观察。 |
| 图片、音频和控制消息共用出口时，存在两个可改变的顺序。发送调度决定谁先占用链路；接收端对数据顺序的要求决定哪些已到达的字节可以交给应用。前者决定链路先为谁发送数据，后者决定接收端何时把数据交给应用。 | 图片、音频和控制消息共用出口时，有两个顺序可以改变：发送调度决定链路先为谁发送数据，接收方对数据顺序的要求决定已到达的字节何时交给应用。 |
| …和“何时交给应用”。下面先通过一个跨地域语音案例，分析这些机制如何影响实际请求；12.4 节再结合音频的预定播放时段和截图版本，讨论数据交给应用时是否仍有价值。… | …和“何时交给应用”。下面先通过一个跨地域语音案例，分析这些机制如何影响实际请求；第 12.4 节再结合音频的预定播放时段和截图版本，讨论数据交给应用时是否仍有价值。… |
| …系统，在可控制的客户端与可信网关之间承载 TCP 和 UDP 流量。应用通过本地 SOCKS5 代理接入客户端，客户端跨广域网连接网关，网关再将请求转交目标服务。在这里的数据中心间… | …系统，在可控制的客户端与可信网关之间承载 TCP 和 UDP 流量。应用通过本地的 SOCKS5 代理（一种转发任意 TCP／UDP 连接的通用代理协议）接入客户端，客户端跨广域网连接网关，网关再将请求转交目标服务。在这里的数据中心间… |
| …频。README 中的 TTS 场景在模型完成后集中返回音频，属于一次突发传输；12.1 节的流式场景则在生成首块后开始传输，接收与播放随生成过程持续推进。对于… | …频。README 中的 TTS 场景在模型完成后集中返回音频，属于一次突发传输；第 12.1 节的流式场景则在生成首块后开始传输，接收与播放随生成过程持续推进。对于… |
| …输状态，复用连接，并根据路径情况安排发送和恢复；前向纠错通过增加冗余数据，让接收端有机会不等下一轮重传就恢复缺包。交互流与大文件共存时，还需要通过发送调度控制交互… | …输状态，复用连接，并根据路径情况安排发送和恢复；前向纠错通过增加冗余数据，让接收方有机会不等下一轮重传就恢复缺包。交互流与大文件共存时，还需要通过发送调度控制交互… |
| …改在附近执行所增加的准备和计算时间少于这 180 ms，它就优于原远端路径。这与 12.1 的比较方法相同：先算出更换服务器可以少等多久，再扣除新增的计算和准备时间。 | …改在附近执行所增加的准备和计算时间少于这 180 ms，它就优于原远端路径。这与第 12.1 节的比较方法相同：先算出更换服务器可以少等多久，再扣除新增的计算和准备时间。 |
| 音频块的生成、到达、进入播放缓冲和被播放回调读取，构成连续播放的时间线。12.1 节的递推式给出缓冲首次耗尽的时刻：此时下一块尚未生成，等待来自模型处理… | 音频块的生成、到达、进入播放缓冲和被播放回调读取，构成连续播放的时间线。第 12.1 节的递推式给出缓冲首次耗尽的时刻：此时下一块尚未生成，等待来自模型处理… |
| …间决定继续调优还是更换服务器。接下来把无线接入中的争用、波动与恢复加入分析，再在 12.5 节汇总完整部署方案。 | …间决定继续调优还是更换服务器。接下来把无线接入中的争用、波动与恢复加入分析，再在第 12.5 节汇总完整部署方案。 |
| …erface）**是设备通过无线信号通信的接口。无线网络还改变了发送本身的代价：数据和反馈要争用同一段空口时间。在共享无线介质上，上行数据和下行反馈需要轮流占用空口。每次交换除了发送字节，还要等待获得信道使用权、等待帧间间隔并接收本跳确认。于是… | …erface）**是设备通过无线信号通信的接口。无线网络还改变了发送本身的代价：上行数据和下行反馈要轮流占用同一段空口时间。每次交换除了发送字节，还要等待获得信道使用权、等待帧间间隔并接收本跳确认。于是… |
| …无线链路的发送，MAC ACK 确认本跳无线帧。承载端到端 ACK 的无线帧，也会进行自己的 MAC 确认。因此，即使端到端 ACK 很短，也需要等待信道空闲，并完成… | …无线链路的发送，MAC ACK 确认本跳无线帧。承载端到端 ACK 的无线帧，也要做自己的 MAC 确认。因此，即使端到端 ACK 很短，也需要等待信道空闲，并完成… |
| 12.1 的播放器等待缺块，以保持全部内容；另一种播放器按固定时钟播放，晚到的音频块不再用… | 第 12.1 节的播放器等待缺块，以保持全部内容；另一种播放器按固定时钟播放，晚到的音频块不再用… |
| 若两条路径汇入同一个 24 Mbit/s 出口，出口传完 30 MB 至少需 10 秒。两条接入路径可以同时发送，各需 8 秒，但所有数据都要经过同一个出口，在这里至少需 10 秒。此时瓶颈已经转移到共同出口。继续提高接入速率，只会让更多数据排队等待通过这个出口。 | 若两条路径汇入同一个 24 Mbit/s 的出口，两条接入路径仍可同时发送、各需 8 秒，但所有数据都要经过这个出口，出口传完 30 MB 至少需要 10 秒。瓶颈于是转移到了共同出口。继续提高接入速率，只会让更多数据排队等待通过这个出口。 |
| …频为例，一份为 5120 bytes，全量复制为 10,240 bytes。接收端用数据块编号去重，先到的完整副本进入播放器，后到副本被丢弃。多发送一份数据，就能… | …频为例，一份为 5120 bytes，全量复制为 10,240 bytes。接收方用数据块编号去重，先到的完整副本进入播放器，后到副本被丢弃。多发送一份数据，就能… |
| …影响。换成输入很小、前缀重复很多的文本任务，优化重点会转向计算复用。保留会话状态还可以减少重复计算。考虑一条文本 Agent 会话：逐轮重送前缀合计约 66 KB，保留状… | …影响。换成输入很小、前缀重复很多的文本任务，优化重点会转向计算复用。保留会话状态就能减少这些重复计算。考虑一条文本 Agent 会话：逐轮重送前缀合计约 66 KB，保留状… |
| 任务执行期间更换设备，还需要迁移会话状态。12.2 的 64 MiB 会话需要约 7.7 秒准备，每轮快 0.4 秒，20 轮只净省… | 任务执行期间更换设备，还需要迁移会话状态。第 12.2 节的 64 MiB 会话需要约 7.7 秒准备，每轮快 0.4 秒，20 轮只净省… |
| …算和读取时间判断加速器能否承载模型；本章进一步分析通信、执行顺序和恢复过程，判断它能否按时完成任务。选择部署方案时，需要依次回答：任务包含哪些工作，哪些步骤必须等… | …算和读取时间判断加速器能否承载模型；本章进一步分析通信、执行顺序和恢复过程，判断一个部署方案能否按时完成任务。选择部署方案时，需要依次回答：任务包含哪些工作，哪些步骤必须等… |
| V4 到 V4.1 展示了模型侧的同类调整。跨层共享减少全局 KV 的重复保存，使模型可以用更细的粒度保存历史信息；CED 减少输入阶段反复经过解码器的工作，缓存恢复则缩短多轮会话重新开始的路径。[^v4… | DeepSeek V4 到 V4.1 展示了模型侧的同类调整。跨层共享减少全局 KV 的重复保存，使模型可以用更细的粒度保存历史信息；因果编码器—解码器（CED）减少输入阶段反复经过解码器的工作，缓存恢复则缩短多轮会话重新开始的路径。[^v4… |
| ![图 12-32 固定任务轨迹中的加速上限。模型从 8 秒缩短至 0.8 秒，其他串行阶段保持 2… | ![固定任务轨迹中的加速上限。模型从 8 秒缩短至 0.8 秒，其他串行阶段保持 2… |
| 再看第一章提出的模型应用，开发者通过上下文表达任务，执行系统据此安排计算、保存状态并传输… | 再看第 1 章提出的模型应用，开发者通过上下文表达任务，执行系统据此安排计算、保存状态并传输… |

## 翻译腔审查（2026-09-11）

作者确认术语原则并统一“笔者”“N 个 token”“Queqiao”之后，再做一遍只针对翻译腔的审查（含图注里汉字数字改阿拉伯数字，以及复审后补改的几处）。共 32 处改动，按最终文本与审查前快照逐行对照。

| 修改前 | 修改后 |
|---|---|
| 图片精修需要保留阴影、高光和白平衡等信息。为后期编辑保留更多原始信息，通常也意味着需要传输更大的文件。本例上传一张 30 MB 原图，处理后返回一张 5 MB 成片，以用户收到完整成片为任务结束的标志。[^raw] | 图片精修需要保留阴影、高光和白平衡等信息。要为后期编辑保留更多原始信息，通常就要传输更大的文件。本例上传一张 30 MB 原图，处理后返回一张 5 MB 成片，以用户收到完整成片为任务结束的标志。[^raw] |
| 接下来考虑各步骤能否同时进行。假设原图分成三块后可以逐块独立处理，每块上传需 4 秒、处理需 0.1 秒；三块输出分别为 1、2、2 MB，回传依次需 0.08、0.16、0.16 秒。第一块到达服务器后，其处理和回传可以与第二块上传重叠；第二块同理。最后一块仍要等到第 12 秒才发完，之后再经过处理、回传和传播，完整成片约在 12.4 秒返回用户。与原来的 12.8 秒相比，前两块的处理与回传在后续上传期间就已完成，不再额外增加等待；总计 12 秒的上传时间仍然需要。[^stream] | 接下来考虑各步骤能否同时进行。假设原图分成三块后可以逐块独立处理，每块上传需 4 秒、处理需 0.1 秒；三块输出分别为 1、2、2 MB，回传依次需 0.08、0.16、0.16 秒。第一块到达服务器后，其处理和回传可以与第二块上传重叠；第二块同理。最后一块仍要等到第 12 秒才发完，之后再经过处理、回传和传播，完整成片约在 12.4 秒返回用户。与原来的 12.8 秒相比，前两块的处理与回传在后续上传期间就已完成，不再额外增加等待；但总计 12 秒的上传时间仍然省不掉。[^stream] |
| 其中，$c_i$、$u_i$、$r_i$ 分别表示处理完成、发送完成和到达的时刻。第一个最大值表示，处理本块既要等输入就绪，也要等处理器完成上一块；第二个最大值表示，发送本块既要等计算完成，也要等出口空闲。这两个等待来源分别是数据依赖和资源竞争。 | 其中，$c_i$、$u_i$、$r_i$ 分别表示处理完成、发送完成和到达的时刻。第一个最大值表示，处理本块既要等输入就绪，也要等处理器完成上一块；第二个最大值表示，发送本块既要等计算完成，也要等出口空闲。这两处等待分别来自数据依赖和资源竞争。 |
| 先用各步骤耗时与依赖关系判断远端执行能否更快。将整个任务交给远端服务时，省下的计算时间必须超过上传、下载、传播、排队和准备增加的时间。对于多轮任务，准备只需做一次，通信却每轮都会发生；对于连续播放的音频，还要保证数据及时到达。模型放在哪里，会同时改变计算、通信和准备时间。 | 先用各步骤耗时与依赖关系判断远端执行能否更快。将整个任务交给远端服务时，省下的计算时间必须超过上传、下载、传播、排队和准备增加的时间。多轮任务的准备只需做一次，通信却每轮都会发生；连续播放的音频还要保证数据及时到达。模型放在哪里，会同时改变计算、通信和准备时间。 |
| 对于截图 Agent，图片压缩可以用较少字节表示纹理和重复区域；视觉编码器却要把图片展开为每个视觉 token 的通道值，以供语言模型读取。前者追求紧凑存储，后者服务于后续计算。在端侧运行编码器，既改变了执行编码的设备，也把上传对象从图片换成了特征。 | 以截图 Agent 为例，图片压缩用较少的字节表示纹理和重复区域；视觉编码器却要把图片展开成每个视觉 token 的通道值，供语言模型读取。前者为了紧凑存储，后者为了后续计算。在端侧运行编码器，既改变了执行编码的设备，也把上传对象从图片换成了特征。 |
| 链路变快后，发送特征比发送图片多花的时间会减少。在 25 GB/s 的链路上，图片与特征的发送时间相差约 0.30 ms。因此，要让端侧编码方案更快，编码与排队合计节省的时间需要超过 0.30 ms；在原有的慢速上行链路上，则要省出超过 9 秒。所以，决定编码器放在哪里时，慢链路上首先要比较传输量，快链路上则更需要比较编码和排队时间。 | 链路变快后，发送特征比发送图片多花的时间会减少。在 25 GB/s 的链路上，图片与特征的发送时间相差约 0.30 ms。因此，要让端侧编码方案更快，编码与排队合计节省的时间需要超过 0.30 ms；在原有的慢速上行链路上，则要省出超过 9 秒。决定编码器放在哪里时，慢链路上先比传输量，快链路上则更要比编码和排队时间。 |
| HTTP 是组织请求和响应的应用层协议。HTTP/1.1 和 HTTP/2 通常使用 TCP（提供可靠有序字节流的传输协议）与 TLS（建立认证与加密上下文的安全协议），HTTP/3 使用 QUIC（在 UDP 数据报上提供可靠多流传输和连接管理的协议）。UDP 向应用提供独立的数据报，不自行负责重传与排序。QUIC 将传输握手与 TLS 1.3 结合，减少分别建立传输连接和安全上下文所需的往返次数。会话恢复时，客户端还可以根据保存的安全状态发送早期数据，即 0-RTT 数据。服务端接受后继续处理，拒绝后由客户端重发；服务端根据操作编号识别重试，并返回已保存的结果，从而避免重复执行同一操作。[^quic] | HTTP 是组织请求和响应的应用层协议。HTTP/1.1 和 HTTP/2 通常使用 TCP（提供可靠有序字节流的传输协议）与 TLS（建立认证与加密上下文的安全协议），HTTP/3 使用 QUIC（在 UDP 数据报上提供可靠多流传输和连接管理的协议）。UDP 向应用提供独立的数据报，不自行负责重传与排序。QUIC 将传输握手与 TLS 1.3 结合，减少分别建立传输连接和安全上下文所需的往返次数。会话恢复时，客户端还可以根据保存的安全状态发送早期数据，即 0-RTT 数据。服务端接受后继续处理，拒绝后由客户端重发；服务端根据操作编号识别重试，并返回已保存的结果，同一操作就不会重复执行。[^quic] |
| 式中 $B_{\mathrm{path}}$ 是路径可提供的载荷速率，$B_{\mathrm{useful}}$ 是实际有效吞吐。对于前述带宽为 20 Mbit/s、RTT 为 100 ms 的图片传输路径，要持续填满链路，需要约 250 KB 在途数据。若只有 64 KB 额度，速率上限为 $64000/0.1=640000$ bytes/s，约 5.1 Mbit/s，仅发送这 30 MB 数据就至少需要约 47 秒。链路每秒本可以传输更多数据，发送方却要等下一批确认到来才能继续发送；扩大可用窗口才会提高这时的实际传输速率。 | 式中 $B_{\mathrm{path}}$ 是路径可提供的载荷速率，$B_{\mathrm{useful}}$ 是实际有效吞吐。前面带宽 20 Mbit/s、RTT 100 ms 的图片传输路径，要持续填满链路，需要约 250 KB 在途数据。若只有 64 KB 额度，速率上限为 $64000/0.1=640000$ bytes/s，约 5.1 Mbit/s，仅发送这 30 MB 数据就至少需要约 47 秒。链路每秒本可以传输更多数据，发送方却要等下一批确认到来才能继续发送；扩大可用窗口才会提高这时的实际传输速率。 |
| 下面单独采用一个整批确认模型：接收方收齐 64 KB 后才确认，从这批发完到确认返回固定为 100 ms。因此完整周期包含 25.6 ms 发送和 100 ms 反馈，共 125.6 ms。图 12-16 将这段等待画出来。蓝色时段确实在发送，浅色时段链路虽然空闲，发送方却因为窗口已满而不能继续。物理带宽提高，只能缩短蓝色部分。 | 下面单独看一个整批确认的模型：接收方收齐 64 KB 后才确认，从这批发完到确认返回固定为 100 ms。因此完整周期包含 25.6 ms 发送和 100 ms 反馈，共 125.6 ms。图 12-16 画出了这段等待。蓝色时段确实在发送，浅色时段链路虽然空闲，发送方却因为窗口已满而不能继续。物理带宽提高，只能缩短蓝色部分。 |
| 上图把窗口固定在 64 KB，以便看清停等过程。新连接通常从更小的窗口开始，随着确认到达逐步增加；短请求可能在窗口充分增大以前就已结束。设初始窗口为十个 1460-byte 段，每轮反馈后翻倍。前四轮累计可发 $14600(1+2+4+8)=219000$ bytes，第五轮累计可达 452,600 bytes。约 355 KB 的语音请求因此要到第五轮才能发完。对于 200 ms RTT，几轮反馈就是数百毫秒；短请求的大部分时间也可能花在等待确认上。第 12.3.4 节将用这一机制解释一次跨地域语音调用的性能观察。 | 上图把窗口固定在 64 KB，以便看清停等过程。新连接通常从更小的窗口开始，随着确认到达逐步增加；短请求可能在窗口充分增大以前就已结束。设初始窗口为十个 1460-byte 段，每轮反馈后翻倍。前四轮累计可发 $14600(1+2+4+8)=219000$ bytes，第五轮累计可达 452,600 bytes。约 355 KB 的语音请求因此要到第五轮才能发完。RTT 为 200 ms 时，几轮反馈就是数百毫秒；短请求的大部分时间可能都花在等确认上。第 12.3.4 节将用这一机制解释一次跨地域语音调用的性能观察。 |
| HTTP/2 将多个流放入一个有序 TCP 字节流，前面的缺口会阻挡后续字节。QUIC 以流为单位保持有序，其他流已经连续到达的字节可独立交付。[^quic] 这与音频播放的递推关系采用相同的分析方法：将一个覆盖全部数据的等待条件，分解成各流自己的等待条件，已经满足条件的流就能向应用交付数据。 | HTTP/2 将多个流放入一个有序 TCP 字节流，前面的缺口会阻挡后续字节。QUIC 以流为单位保持有序，其他流已经连续到达的字节可独立交付。[^quic] 这和音频播放的递推关系是同一种分析方法：把一个覆盖全部数据的等待条件拆成各流自己的等待条件，已经满足条件的流就能向应用交付数据。 |
| 这两种机制分别回答“何时发送”和“何时交给应用”。下面先通过一个跨地域语音案例，分析这些机制如何影响实际请求；第 12.4 节再结合音频的预定播放时段和截图版本，讨论数据交给应用时是否仍有价值。[^http] | 这两种机制分别回答“何时发送”和“何时交给应用”。下面先用一个跨地域语音案例，看这些机制怎样影响实际请求；第 12.4 节再结合音频的预定播放时段和截图版本，讨论数据交给应用时是否仍有价值。[^http] |
| 前面几章介绍了模型服务的批处理与资源池化。把各地请求汇聚到少数数据中心，可以让更多请求共享模型副本与加速器，减少分散部署时各地分别预留却未充分使用的容量，也更容易形成批次。集中部署因此有机会提高加速器利用率、降低单位请求成本；与此同时，远方用户需要经过更长的网络路径。例如，将语音模型集中部署在美东，美西用户或接入服务就需要跨地域调用。资源池中的模型即使很快完成计算，用户仍要等待请求上传和结果返回。 | 前面几章介绍了模型服务的批处理与资源池化。把各地请求汇聚到少数数据中心，可以让更多请求共享模型副本与加速器，减少分散部署时各地分别预留却未充分使用的容量，也更容易形成批次。集中部署因此有机会提高加速器利用率、降低单位请求成本，代价是远方用户要经过更长的网络路径。例如，将语音模型集中部署在美东，美西用户或接入服务就需要跨地域调用。资源池中的模型即使很快完成计算，用户仍要等待请求上传和结果返回。 |
| **Queqiao 是什么，解决什么问题？** Queqiao（鹊桥）是笔者开发的开源、自托管广域网传输系统，在可控制的客户端与可信网关之间承载 TCP 和 UDP 流量。应用通过本地的 SOCKS5 代理（一种转发任意 TCP／UDP 连接的通用代理协议）接入客户端，客户端跨广域网连接网关，网关再将请求转交目标服务。在这里的数据中心间部署中，客户端位于调用方服务器，网关靠近远端推理服务；因此，需要优化的长距离链路集中在客户端与网关之间。应用仍调用原有的 ASR、TTS 接口，Queqiao 负责传送请求与结果。[^queqiao-readme] | **Queqiao 是什么，解决什么问题？** Queqiao（鹊桥）是笔者开发的开源、自托管广域网传输系统，在可控制的客户端与可信网关之间承载 TCP 和 UDP 流量。应用通过本地的 SOCKS5 代理（一种转发任意 TCP／UDP 连接的通用代理协议）接入客户端，客户端跨广域网连接网关，网关再将请求转交目标服务。在这里的数据中心间部署中，客户端在调用方的服务器上，网关靠近远端的推理服务，需要优化的长距离链路就集中在客户端与网关之间。应用仍调用原有的 ASR、TTS 接口，Queqiao 负责传送请求与结果。[^queqiao-readme] |
| 项目 README 描述了这一实际需求：模型运行在美国，客户端分布在不同地方。ASR 上传几百 KB 音频，返回一句识别文本；TTS 提交一段文本，返回几百 KB 音频。README 中的 TTS 场景在模型完成后集中返回音频，属于一次突发传输；第 12.1 节的流式场景则在生成首块后开始传输，接收与播放随生成过程持续推进。对于这类短请求，即使链路能在几毫秒内发送完全部字节，连接建立、窗口增长和丢包恢复仍可能各自引入长距离反馈等待。 | 项目 README 描述了这一实际需求：模型运行在美国，客户端分布在不同地方。ASR 上传几百 KB 音频，返回一句识别文本；TTS 提交一段文本，返回几百 KB 音频。README 中的 TTS 场景在模型完成后集中返回音频，属于一次突发传输；第 12.1 节的流式场景则在生成首块后开始传输，接收与播放随生成过程持续推进。这类短请求即使能在几毫秒内发完全部字节，连接建立、窗口增长和丢包恢复仍可能各自引入一次长距离的反馈等待。 |
| Queqiao 的设计目标是减少这些额外等待，使短请求尽量接近必要传播、数据发送和模型处理所需的时间，同时提高长流对可用带宽的利用。它让多条应用流共享客户端—网关路径的测量结果与传输状态，复用连接，并根据路径情况安排发送和恢复；前向纠错通过增加冗余数据，让接收方有机会不等下一轮重传就恢复缺包。交互流与大文件共存时，还需要通过发送调度控制交互等待。前面讨论的连接、窗口和多流机制，在这里共同服务于跨地域调用。传播距离本身仍然决定必须付出的时间，优化后的完整请求能否满足业务期限，决定了集中部署是否适合这项任务。 | Queqiao 的设计目标是减少这些额外等待，使短请求尽量接近必要传播、数据发送和模型处理所需的时间，同时提高长流对可用带宽的利用。它让多条应用流共享客户端—网关路径的测量结果与传输状态，复用连接，并根据路径情况安排发送和恢复；前向纠错则多发一些冗余数据，让接收方不必等下一轮重传就有机会恢复缺包。交互流与大文件共存时，还要靠发送调度控制交互流的等待。前面讨论的连接、窗口和多流机制，在这里共同服务于跨地域调用。传播距离本身仍然决定必须付出的时间，优化后的完整请求能否满足业务期限，决定了集中部署是否适合这项任务。 |
| 新连接条件相差约 0.89 秒，复用连接并调优后，两者均约为 0.24 秒。[^queqiao-calc] 原来的大部分差距随着基线配置改变而消失。这支持第二种解释：连接准备和发送方式在最初的差距中起了主要作用。即使使用同一种协议，配置不同，请求完成时间也可能相差很大。 | 新建连接时两条路径相差约 0.89 秒，复用连接并调优后都约为 0.24 秒。[^queqiao-calc] 原来的大部分差距随着基线配置的改变消失了。这支持第二种解释：连接准备和发送方式在最初的差距中起了主要作用。即使使用同一种协议，配置不同，请求完成时间也可能相差很大。 |
| **降低网络往返延迟与加速模型计算，哪个收益更大？** 调优对照方案后，下一步优化的重点也会改变。模型从 30 ms 加速到 3 ms，最多省 27 ms；将 RTT 从 200 ms 降至 20 ms，可省约 180 ms。只要改在附近执行所增加的准备和计算时间少于这 180 ms，它就优于原远端路径。这与第 12.1 节的比较方法相同：先算出更换服务器可以少等多久，再扣除新增的计算和准备时间。 | **降低网络往返延迟与加速模型计算，哪个收益更大？** 调优对照方案后，下一步优化的重点也会改变。模型从 30 ms 加速到 3 ms，最多省 27 ms；将 RTT 从 200 ms 降至 20 ms，可省约 180 ms。只要改在附近执行多出的准备和计算时间少于这 180 ms，就比原来的远端路径快。这与第 12.1 节的比较方法相同：先算出更换服务器可以少等多久，再扣除新增的计算和准备时间。 |
| 音频块的生成、到达、进入播放缓冲和被播放回调读取，构成连续播放的时间线。第 12.1 节的递推式给出缓冲首次耗尽的时刻：此时下一块尚未生成，等待来自模型处理；已经生成但未到达，等待位于传输阶段；已经到达却未被读取，等待位于本地缓冲与调度阶段。 | 音频块的生成、到达、进入播放缓冲和被播放回调读取，构成连续播放的时间线。第 12.1 节的递推式给出缓冲第一次耗尽的时刻：此时若下一块尚未生成，等待出在模型处理；若已生成但未到达，等待出在传输；若已到达却没被读取，等待出在本地缓冲与调度。 |
| 前一节已经区分了“何时发送”和“何时交给应用”。**空口（air interface）**是设备通过无线信号通信的接口。无线网络还改变了发送本身的代价：上行数据和下行反馈要轮流占用同一段空口时间。每次交换除了发送字节，还要等待获得信道使用权、等待帧间间隔并接收本跳确认。于是，应用字节数之外，还要计算交换次数。 | 前一节已经区分了“何时发送”和“何时交给应用”。**空口（air interface）**是设备通过无线信号通信的接口。无线网络还改变了发送本身的代价：上行数据和下行反馈要轮流占用同一段空口时间。每次交换除了发送字节，还要等信道空闲、等帧间间隔，并接收本跳的确认。于是除了应用字节数，还要数交换次数。 |
| 两种交换都包含 $34+16+44=94$ μs 固定开销。数据帧的发送时间从 208 μs 缩到 40 μs，缩短约八成，整次交换的耗时却只缩短约 56%。固定项所占比例越大，继续缩短载荷的收益越小；把多个小报文合并发送、减少交换次数，因而能更直接地降低开销。 | 两种交换都包含 $34+16+44=94$ μs 固定开销。数据帧的发送时间从 208 μs 缩到 40 μs，缩短约八成，整次交换的耗时却只缩短约 56%。固定项所占比例越大，继续缩短载荷的收益越小；把多个小报文合并发送、减少交换次数，降低开销更直接。 |
| TACK 是减少无线确认开销的一种反馈机制。它将常规的累积确认与需要即时反馈的事件分开：前者通过合并来减少交换次数，后者及时返回，使发送方尽快继续发送或重传丢失的数据。[^wireless-survey] 这种分工同时减少确认报文的发送开销和等待反馈的时间。 | TACK 是减少无线确认开销的一种反馈机制。它将常规的累积确认与需要即时反馈的事件分开：前者合并后发送以减少交换次数，后者立即返回，让发送方尽快继续发送或重传丢失的数据。[^wireless-survey] 这种分工同时减少确认报文的发送开销和等待反馈的时间。 |
| 例如，八块音频各长 20 ms，计划从第 0.4 秒开始连续播放，共 0.16 秒。让这些音频与大图共用链路时，FIFO 可使这些块最终全部到达，却全部错过各自预定的播放时刻；媒体优先则使八块都能按时到达。[^mixed] 两种情况下最终收到的字节数相同，按时可用的声音却分别为零与 0.16 秒。调度时统计按时播放的内容，才能反映用户实际听到了多少声音。 | 例如，八块音频各长 20 ms，计划从第 0.4 秒开始连续播放，共 0.16 秒。让这些音频与大图共用链路时，FIFO 让这些块最终全部到达，却都错过了各自预定的播放时刻；媒体优先则让八块都按时到达。[^mixed] 两种情况下最终收到的字节数相同，按时可用的声音却分别为零与 0.16 秒。调度时统计按时播放的内容，才能反映用户实际听到了多少声音。 |
| 如果一条路径先结束，可以把另一条上的部分字节移给它，使原本较晚完成的部分提前结束。最优比例因而使两条路径同时完成，即 $x=B_1/(B_1+B_2)$。快路传 20 MB、慢路传 10 MB，各需 8 秒，比只用快路的 12 秒少 4 秒。 | 如果一条路径先结束，可以把另一条上的部分字节移给它，使原本较晚完成的部分提前结束。最优比例因而是让两条路径同时完成，即 $x=B_1/(B_1+B_2)$。快路传 20 MB、慢路传 10 MB，各需 8 秒，比只用快路的 12 秒少 4 秒。 |
| 每轮速率在其上传期间恒定时，十轮 6 Mbit/s、十轮 10 Mbit/s 的平均速率为 8 Mbit/s，但任务约需 44.1 秒，比全程 8 Mbit/s 的 43 秒更慢。慢速增加的等待大于快速省下的等待，原因是发送时间与速率成反比。平台应根据整个任务的上传时间分布，判断是否满足 $P(T\le45\ \mathrm{s})\ge0.99$，还要考虑网络持续变慢时，相邻多轮会一起受到影响。 | 设每轮速率在其上传期间恒定，十轮 6 Mbit/s、十轮 10 Mbit/s 的平均速率也是 8 Mbit/s，任务却约需 44.1 秒，比全程 8 Mbit/s 的 43 秒慢。发送时间与速率成反比，慢的几轮多出的等待大于快的几轮省下的等待。平台应根据整个任务的上传时间分布，判断是否满足 $P(T\le45\ \mathrm{s})\ge0.99$，还要考虑网络持续变慢时，相邻多轮会一起受到影响。 |
| 保存状态既能减少正常执行中的重复计算，也能减少故障后的重做。下面以在附近工作站执行的截图任务为例，比较同一次断连如何因进度保存方式不同而产生不同开销。附近工作站完成一轮任务需要 1.9 秒。设第十轮已执行完，但确认尚未返回时连接中断，恢复连接需要 1 秒。若前九轮进度已经提交，只重做未确认的第十轮，额外花 2.9 秒，任务由 41 秒增至 43.9 秒；若十轮都要重做，额外花 20 秒，任务变为 61 秒。仍然使用同一台工作站，能否保留进度却决定了任务能否按时完成。 | 保存状态既能减少正常执行中的重复计算，也能减少故障后的重做。下面以在附近工作站执行的截图任务为例，比较同一次断连在两种进度保存方式下的开销。附近工作站完成一轮任务需要 1.9 秒。设第十轮已执行完，但确认尚未返回时连接中断，恢复连接需要 1 秒。若前九轮进度已经提交，只重做未确认的第十轮，额外花 2.9 秒，任务由 41 秒增至 43.9 秒；若十轮都要重做，额外花 20 秒，任务变为 61 秒。仍然使用同一台工作站，能否保留进度却决定了任务能否按时完成。 |
| 任务执行期间更换设备，还需要迁移会话状态。第 12.2 节的 64 MiB 会话需要约 7.7 秒准备，每轮快 0.4 秒，20 轮只净省约 0.3 秒。若带宽改善只持续十轮就恢复原状，这次迁移会净损失约 3.7 秒。因此，平台应将预测的持续收益与切换成本比较，只有预计节省的时间足以抵消迁移准备时间，并能留出余量应对额外开销时，才迁移状态。在一段时间内继续使用同一台设备，可以避免在临界带宽附近反复迁移状态。 | 任务执行期间更换设备，还需要迁移会话状态。第 12.2 节的 64 MiB 会话需要约 7.7 秒准备，每轮快 0.4 秒，20 轮只净省约 0.3 秒。若带宽改善只持续十轮就恢复原状，这次迁移会净损失约 3.7 秒。因此，平台应把预计的持续收益与切换成本放在一起比较：预计省下的时间足以抵消迁移准备，还留得出余量应对额外开销，才迁移状态。在一段时间内继续使用同一台设备，可以避免在临界带宽附近反复迁移状态。 |
| 下面将前文比较硬件与执行方式的方法用于完整任务。设一次固定轨迹中，模型执行合计 8 秒，工具与网络等不可重叠部分合计 2 秒。即使模型整体加速 10 倍，任务也只能从 10 秒降至 2.8 秒，加速比约为 3.57；[^core-calculation] 模型耗时趋近于零时，下界仍是 2 秒。 | 下面把前文比较硬件与执行方式的方法用到完整任务上。设一次固定轨迹中，模型执行合计 8 秒，工具与网络等不可重叠部分合计 2 秒。即使模型整体加速 10 倍，任务也只能从 10 秒降至 2.8 秒，加速比约为 3.57；[^core-calculation] 模型耗时趋近于零时，下界仍是 2 秒。 |
| (d) 将八块音频各复制一份，每块为 640 bytes，求新增的传输字节数，并说明接收端怎样避免重复播放。 | (d) 将八块音频各复制一份，每块为 640 bytes，求新增的传输字节数，并说明接收方怎样避免重复播放。 |
| 本小题仅假设远程服务发生断连，端侧方案仍正常执行。远程服务在断连期间保留运行状态，连接恢复后继续使用，因此无需重新初始化；任务进度则按上述两种情况分别恢复。恢复成本已包含在基础费率中，其余成本按实际执行轮次和发送字节数计算。采用原上传单价，重新选择满足期限且成本最低的方案。 | 本小题仅假设远程服务发生断连，端侧方案仍正常执行。远程服务在断连期间保留运行状态，连接恢复后继续使用，因此不必重新初始化；任务进度则按上述两种情况分别恢复。恢复成本已包含在基础费率中，其余成本按实际执行轮次和发送字节数计算。采用原上传单价，重新选择满足期限且成本最低的方案。 |
| [^author]: [作者材料与技术判断](../case-studies/author-context-and-design.md)。AOI 与 Latent Bridge 用于说明应观察哪些界面信息，以及设备之间应传递什么数据。 | [^author]: [笔者的材料与技术判断](../case-studies/author-context-and-design.md)。AOI 与 Latent Bridge 用于说明应观察哪些界面信息，以及设备之间应传递什么数据。 |
| [^media]: [FIFO](../calculations/results/shared-media-schedule-fifo.md)与[音频优先](../calculations/results/shared-media-schedule-priority.md)；固定发送记录的[整体有序](../calculations/results/shared-media-hol-connection.md)与[逐流交付](../calculations/results/shared-media-hol-per_stream.md)。分别考察发送调度和接收端交付依赖的影响。 | [^media]: [FIFO](../calculations/results/shared-media-schedule-fifo.md)与[音频优先](../calculations/results/shared-media-schedule-priority.md)；固定发送记录的[整体有序](../calculations/results/shared-media-hol-connection.md)与[逐流交付](../calculations/results/shared-media-hol-per_stream.md)。分别考察发送调度和接收方交付依赖的影响。 |
