# 观察后追加：一CTA一行分段读取控制

首轮65536列整行向量实现的编译n_spills=798，而拆分均0；该差异不能只解释为跨CTA并行。保留首轮成功原件，不修改或重跑。追加同一CTA循环64个1024列段、FP32累计部分和，再循环64段输出的实现；保持一CTA一行、无全局scratch，避免同时保留65536个元素。

只测N65536、M1/32/1024，读首轮封存输入，误差界相同。记录完整输出、5次重复、9×20 eager/graph计时、原生trace与编译资源。它在原观察后设计，计时不是同轮随机交错的三方对照；不能将跨批小差异归因实现优劣。目的为检查寄存器溢出混杂和固定一CTA一行的另一种实际路径，不作最优内核搜索。
