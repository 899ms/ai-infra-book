# 第 11 章《资源调度与运行环境》翻译腔清扫：修改对照

2026-09-11 对 `manuscripts/11-资源调度与运行环境.md` 的翻译腔通读。本章整体行文已比较自然，共 13 处改动，均为理顺句式，不改技术含义、数字、公式与单位。省略号表示只截取了改动附近的文字。

| 修改前 | 修改后 |
|---|---|
| …强化学习的验证也要区分这两种情况：答案是否正确用来计算学习反馈，执行故障则先由运行系统处理。 | …强化学习的验证也要区分这两种情况：答案是否正确，用于计算学习反馈；执行故障则先由运行系统处理。 |
| …中间有 9 秒模型调用。始终驻留需要 $2\times9=18$ GiB·秒。 | …中间有 9 秒模型调用。始终驻留要占用 $2\times9=18$ GiB·秒。 |
| …令两者相等，就能求出暂停开始节省成本时所需的空闲时间。 | …令两者相等，就能求出暂停开始节省成本所需的空闲时间。 |
| …平台增加少量环境创建工作，就省下 420 GiB 等待期间保留的状态… | …平台增加少量环境创建工作，就省下了原本要在等待期间保留的 420 GiB 状态… |
| …假设正确预测不会争用资源，错误预测可以立即取消，错误时再按需准备… | …假设正确预测不会争用资源，错误预测可以立即取消，猜错时再按需准备… |
| …对 2 GiB 环境，每项预测因此增加 $0.75\times2\times1=1.5$ GiB·秒的平均空闲内存占用。 | …对 2 GiB 环境，每次预测因此增加 $0.75\times2\times1=1.5$ GiB·秒的平均空闲内存占用。 |
| …利用临时资源扩展 RL 生成池的 PolyRL 系统，先把完整权重接收到 CPU 缓冲区… | …PolyRL 利用临时资源扩展 RL 生成池，它先把完整权重接收到 CPU 缓冲区… |
| …用 10.9 减去 10 会预测只剩 0.9 秒。 | …用 10.9 减去 10，就会得出只剩 0.9 秒的预测。 |
| …一项接下来要安排、执行时间不超过 100 秒的工作，最迟在第 20 秒启动，便可以利用前面的空隙而不推迟整批… | …接下来要安排一项执行时间不超过 100 秒的工作：它最迟在第 20 秒启动，便能利用前面的空隙而不推迟整批… |
| …主动终止子进程后，约 0.8 ms 后观察到进程退出。 | …主动终止子进程后，约 0.8 ms 就观察到进程退出。 |
| …分别以 $Np_{\mathrm{self}}$ 和 $Np_{\mathrm{api}}$ 为分母，便将比较转换为每个成功任务的成本。 | …分别以 $Np_{\mathrm{self}}$ 和 $Np_{\mathrm{api}}$ 为分母，比较的就变成每个成功任务的成本。 |
| …在模型调用期间准备环境，把每项任务的累计内存占用从 60 降到 18 GiB·秒，准备工作多用 0.3 秒 CPU 时间，就能释放大量内存。 | …在模型调用期间准备环境，每项任务的累计内存占用就从 60 降到 18 GiB·秒，能释放大量内存；代价只是准备工作多用 0.3 秒 CPU 时间。 |
| …模型服务和环境平台共同安排准备时机，使原来贯穿任务全程的资源占用变成按阶段分配。 | …模型服务和环境平台共同安排准备时机，原本贯穿任务全程的资源占用就变成了按阶段分配。 |

## 看过但未改的可疑句（供复查）

- "每个 token 的时间取自本书对这一部署的服务估算"（章首表格下方）："对这一部署的服务估算"略有定语堆叠，但意思清楚，改动收益小，未动。
- "48 个 CPU 核足以处理。"（11.3.2 节）：宾语省略，但紧承上句"每秒需要执行 30 次工具调用"，省略自然，未动。
- "等待缩短到 2 秒时，保留环境开始更省，准备也占满全部可重叠窗口。"（11.5.3 节）："准备也占满全部可重叠窗口"偏紧缩，但作为术语化表述可接受，未动。
- "对齐前缀长度后可以统一安排后续生成，代价是较长的回答超出共同前缀的部分要重新生成。"（11.3.3 节）：与上一句"较长那条的 3,000 个 token 要重新生成"语义重复，但属于行文冗余而非翻译腔，未动。
