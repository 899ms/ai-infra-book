# 第 6 章语言通读：逐句修改对照

共 166 处，按正文顺序分四批写入。左列为修改前，右列为修改后。

| 修改前 | 修改后 |
|---|---|
| 同一张分块图，由此变成了多卡并行方案。 | 同一种分块方式，由此变成了多卡并行方案。 |
| 本章会从算子的维度与依赖出发，逐步推导各种并行的通信，最后用给定模型、八卡硬件和会话期限筛选方案。读完后，应当能够为自己的配置列出候选、排除不可行项，并根据瓶颈和测量结果选择，而不只记住几个并行缩写。 | 本章从算子的维度与依赖出发，逐步推导各种并行方式产生的通信，最后用给定的模型、八卡硬件和会话期限筛选方案。读完本章，读者应当能够为自己的模型和硬件列出候选方案，排除不可行的方案，再根据瓶颈和测量结果做出选择，而不只是记住几个并行方式的缩写。 |
| 多卡并行延续同一个问题：沿哪些维度切分、每块放在哪里、需要哪些输入与结果汇合，以及按什么顺序执行。 | 多卡并行面对的仍是同一组问题：沿哪些维度切分，每一块放在哪里，各块需要哪些输入、结果在哪里汇合，以及按什么顺序执行。 |
| 服务器和机柜是安装与供电的单位，一个通信组可以位于同一台服务器内，也可以跨越多台服务器。 | 服务器和机柜是安装与供电的单位；一个实例的各张卡可以都在同一台服务器内，也可以分布在多台服务器上。 |
| 这三种需求分别指向容量、延迟和吞吐。 | 这三种需求分别对应容量、延迟和吞吐三个目标。 |
| 本章从一个具体任务开始：Qwen3-8B 已完成输入处理，缓存中有 8192 个上下文 token，现在继续生成 token。手上有八张卡，模型能放入一张卡。我们将依次求出单卡、两卡、四卡和八卡协作的时间，再比较多个请求到来时的完成顺序。 | 本章的贯穿算例是一个具体任务：Qwen3-8B 已经处理完输入，缓存里有 8192 个上下文 token，现在要继续生成。手上有八张卡，模型放得进一张卡。我们将依次求出单卡执行，以及两卡、四卡、八卡协作时每生成一个 token 的时间，再比较多个请求同时到来时的完成顺序。 |
| 它们在同一时刻占用同一份物理内存。 | 这四项在同一时刻共用这张卡的物理内存。 |
| 教学加速器的每卡可用内存为 24 GB | 本章的教学加速器每卡可用内存为 24 GB |
| 互联每轮的启动开销为 2 μs，有效单向带宽为 50 GB/s。 | 卡间互联每轮通信的启动开销为 2 μs，有效单向带宽为 50 GB/s。 |
| 权重与 KV 大体被分摊到各卡，工作区在每张卡各留一份，少量归一化参数也被复制。 | 权重与 KV 大体分摊到了各卡，工作区在每张卡各留一份，少量归一化参数在每张卡上都有一份副本。 |
| 单 token 调用需要使用这三个矩阵。 | 生成一个 token 时，这三个矩阵都要用一遍。 |
| 小消息的执行时间更容易受启动开销影响，大消息更依赖持续传输能力。 | 小数据量的传输更容易受启动开销影响，大数据量的传输更依赖持续带宽。 |
| 多个会话则可以同时提供就绪输入。 | 多个会话则可以同时提供已经就绪的输入。 |
| 多加速器上的训练分工由此增加梯度同步和状态存储。 | 多卡训练因此还要同步梯度、保存更多状态。 |
| 模型本身还有两个可以分的结构 | 模型本身还有两个可以切分的结构 |
| 这张表里有一个贯穿全章的区别。 | 表中隐含着一个贯穿全章的区别。 |
| 若单卡每秒处理 $r$ 个请求，八个始终有请求待处理的单卡实例就提供 $8r$ 的服务能力。 | 若一张卡每秒能处理 $r$ 个请求，八个单卡实例在始终有请求可处理时，每秒就能处理 $8r$ 个请求。 |
| 把八张卡合成一个实例后，设单请求的加速比为 $S(8)$，该实例的服务能力为 $S(8)r$。两种部署方式使用同样八张卡，吞吐之比是 $S(8)/8$。只有当单请求获得八倍加速时，两者吞吐才相同；出现通信和串行工作后，协作实例通常用一部分总吞吐换取更短的单请求时间。 | 把八张卡合成一个实例后，设单个请求的加速比为 $S(8)$，这个实例每秒能处理 $S(8)r$ 个请求。两种部署用的是同样八张卡，吞吐之比为 $S(8)/8$：只有单个请求恰好加速八倍，两者的吞吐才相同。一旦有通信和无法并行的工作，协作实例就是用一部分总吞吐换取更短的单请求时间。 |
| 推理实例之间没有这条参数更新依赖。 | 推理实例之间没有这种依赖。 |
| 两处切分都以“部分和”结束。 | 两处切分的最后一步都只得到部分和。 |
| 要理解为什么一处切完不需要通信、另一处却必须求和，先看一次最小的矩阵乘。要让两张卡共同完成它，先要决定各算什么：可以让它们分别算出不同的输出元素，也可以让它们各算同一个输出的一部分贡献。前一种结果需要拼接，后一种结果需要相加。 | 要理解为什么一种切法切完不需要通信、另一种却必须求和，先看一个最小的矩阵乘。两张卡要共同完成它，先要决定各算什么：可以让两张卡分别算出不同的输出元素，也可以让它们各算同一个输出的一部分。前一种结果需要拼接，后一种需要相加。 |
| 先用一次完整的矩阵乘法说明两种切分方式。 | 下面用一个完整的例子说明这两种切法。 |
| 按权重矩阵的列切分时，即把输出特征分给不同加速器，卡 0 保存第一列 | 按权重矩阵的列切分，也就是把输出特征分给不同的卡：卡 0 保存第一列 |
| 改为按权重矩阵的行切分，即把同一个 token 的输入特征分给不同加速器，卡 0 用输入 2 乘第一行 | 改为按权重矩阵的行切分，也就是把同一个 token 的输入特征分给不同的卡：卡 0 用输入 2 乘第一行 |
| 两卡都产生包含两个元素的向量，但每个向量只含一行权重的贡献。逐元素相加后，才得到同一个完整输出 $[8,23]$。 | 两张卡各得到一个含两个元素的向量，但每个向量只包含一行权重的贡献；对应位置相加后，才得到完整输出 $[8,23]$。 |
| 右侧两个乘积的形状都是 $m\times h$。它们分别包含来自中间维一半分量的贡献，求和后才是下一层要用的输入。 | 右侧两个乘积的形状都是 $m\times h$，各自只包含中间维一半分量的贡献，相加后才是下一层要用的输入。 |
| 若两张卡都需要完整结果，执行 **AllReduce**：先把贡献相加，再让每卡取得求和结果。若下一算子继续使用分片，执行 **ReduceScatter**，把求和后的不同片段留在不同卡。由分片恢复完整输入，则执行 **AllGather**，将各卡持有的片段拼起来。 | 若两张卡都需要完整结果，就执行前面定义的 AllReduce。若下一个算子可以继续使用分片，则执行 **ReduceScatter（归约后分散）**：先求和，再把求和结果的不同片段留在不同的卡上。要从分片恢复完整数据，则执行 **AllGather（全收集）**，把各卡持有的片段拼起来。 |
| 代价是汇合四个 8 KiB 输出部分和。 | 代价是要把四张卡各自的 8 KiB 输出部分和相加。 |
| Attention 也可沿头切分。Q、K、V 投影产生各卡负责的头，各卡独立计算这些头的注意力；输出投影把头结果映射回隐藏维，再将各卡的部分结果求和，得到完整输出。因此，Qwen3-8B 每层需要分别对 attention 输出和 FFN 输出进行归约，36 层共 72 次。Embedding、词表输出与采样归入贯穿算例的模型输入与输出处理时间。 | 注意力也可以按头切分：Q、K、V 投影只产生本卡负责的头，各卡独立计算这些头的注意力；输出投影把各头的结果映射回隐藏维，各卡得到的仍是部分和，相加后才是完整输出。因此 Qwen3-8B 每层要对注意力输出和 FFN 输出各做一次归约，36 层共 72 次。词嵌入、词表输出与采样计入贯穿算例的输入输出处理时间。 |
| 头是离散的分配单位。Qwen3-8B 有 8 个 KV 头，TP=8 时每卡一个；TP=16 时，使用同一 KV 头的查询头被分到两张卡，两卡各保存相应 KV。8192 token 时，每头占 144 MiB，八卡共 1.125 GiB，十六卡共 2.25 GiB。跨过头数这一边界，继续增加 TP 会增加 KV 的物理复制。 | 头不能再拆，是分配的最小单位。Qwen3-8B 有 8 个 KV 头，TP=8 时每卡一个；TP=16 时，共用同一个 KV 头的查询头被分到两张卡，两张卡都要保存这个 KV 头的状态。上下文为 8192 token 时，每个 KV 头的状态占 144 MiB，八卡合计 1.125 GiB，十六卡合计 2.25 GiB。TP 卡数一旦超过 KV 头数，继续增加就会复制 KV。 |
| 后续先求单实例时间 $T(p)$，再将它换算为整组请求的完成时间。 | 后面先求一个实例生成一个 token 的时间 $T(p)$，再换算成一组请求的完成时间。 |
| Attention 投影的 BF16 权重为 | 注意力投影的 BF16 权重为 |
| $s$ 为步前上下文长度。代入 8192，约为 16.34 GB。在本节的加速器参数下，每层 attention 和 FFN 的执行时间均受内存带宽限制，于是 TP=$p$ 的这部分时间为 $16.34/p$ ms。归一化、逐元素运算、embedding、词表投影结果的传递与采样统一取 0.20 ms 的输入输出及其他串行处理时间。计算与归约依次执行，得到后续各节使用的执行时间模型： | $s$ 为这一步开始前的上下文长度。代入 8192，约为 16.34 GB。按本章的加速器参数，每层注意力和 FFN 的执行时间都受内存带宽限制，TP 为 $p$ 时这部分时间为 $16.34/p$ ms。归一化、逐元素运算、词嵌入、词表投影结果的传递与采样统一记为 0.20 ms 的串行处理时间。计算与归约依次执行，就得到后面各节都会使用的执行时间模型： |
| 随着 $p$ 增大，右侧不断缩小。即使通信每次只增加同样一小段等待，也会逐渐抵消本地执行时间的缩短。 | $p$ 越大，右侧越小。即使每次归约只多花同样一小段时间，累积起来也会抵消本地执行时间的缩短。 |
| 加速器更多时，线性层还可以同时切分两个矩阵维度，将卡排成行、列两个方向的通信组。一次 $[8192,4096]\times[4096,12288]$ 投影，将输出分片保存在 16 张卡上，每卡原始输入和权重分别为 4 MiB、6 MiB。排成 4×4 网格，横向收集其他三片输入，发送 12 MiB；纵向收集其他三片权重，发送 18 MiB。改成 2×8 后两方向为 28、6 MiB，改成 8×2 后为 4、42 MiB。独立等速链路下，4×4 所需传输时间较短，因为两个方向中较大的传输量最小。MeshSlice 沿这一布局继续分块，使收集与矩阵乘流水执行；Arctic 则在不同执行阶段切换分工，其状态迁移在第 9 章展开。 | 卡更多时，线性层还可以同时切分两个矩阵维度，把卡排成行、列两个方向的通信组。以 $[8192,4096]\times[4096,12288]$ 的投影为例，把输出分成 16 片放在 16 张卡上，每张卡起初只有 4 MiB 的输入分片和 6 MiB 的权重分片。排成 4×4 网格时，每张卡要横向收集同一行另外三张卡的输入分片，发送 12 MiB；纵向收集同一列另外三张卡的权重分片，发送 18 MiB。改成 2×8 网格，两个方向分别为 28 MiB 和 6 MiB；改成 8×2 则为 4 MiB 和 42 MiB。两个方向的链路独立且等速时，传输时间由较大的一方决定，因此 4×4 最快。MeshSlice 沿这种布局继续分块，让收集与矩阵乘流水执行；Arctic 则在不同执行阶段切换分工，其状态迁移在第 9 章展开。 |
| 进入需要完整输入行的列并行线性层前，AllGather 收集序列分片；行并行线性层产生部分和后，用 ReduceScatter 一边求和、一边重新得到序列分片。这样把原先 AllReduce 的两部分放到不同的算子边界，不必让所有中间区间都复制完整激活。 | 列并行线性层需要完整的输入行，进入它之前先用 AllGather 收齐各卡的序列分片；行并行线性层产生部分和之后，用 ReduceScatter 一边求和、一边重新按位置分片。AllReduce 本来就等于 ReduceScatter 加 AllGather，序列并行只是把这两半放到了不同的算子边界上，中间的逐 token 算子就不必每张卡都保存一份完整激活。 |
| 这个节省只属于保持 SP 布局的区间，不代表模型所有激活与权重都缩小四倍。归约维和输出维的关系仍与图 6-8 相同，变化的是下一算子愿意接收何种布局。配套 JAX 实验展示了保持分片和重新收集这两条 FFN 路径。 | 这个节省只发生在保持序列分片的区间，不是模型的所有激活与权重都缩小四倍。归约维和输出维的关系仍与图 6-8 相同，变化的只是下一个算子接收哪种布局。JAX 是支持自动求导和编译执行的数值计算框架，配套的 JAX 实验展示了保持分片和重新收集两条 FFN 路径。 |
| 局部 Q 仍要看到 mask 允许的远端 K/V。可以一次收集全部 K/V，也可以让 K/V 块在各卡之间环形流转，并用第 5.3.3 节的在线 softmax 累计最大值、指数和及加权输出。环形 attention 是一种调度与传输实现，CP 是工作划分方式，两者不是同义词。 | 本卡的查询仍要用到掩码允许范围内的远端 K、V。可以一次收齐全部 K、V，也可以让 K、V 块在各卡之间环形传递，边到达边用第 5.3.3 节的在线 softmax 累计最大值、指数和与加权输出。环形注意力（Ring Attention）是一种调度和传输的实现方式，上下文并行是工作划分方式，两者不是同义词。 |
| 横轴为 key 位置、纵轴为 query 位置。实色格是必须计算的可见关系 | 横轴为键的位置、纵轴为查询的位置。实色格是必须计算的查询—键配对 |
| 连续切半还会产生计算不均。八个位置的因果 attention 含 $1+\cdots+8=36$ 个 query–key 对；前四个位置只有 10 对，后四个有 26 对，虽然两卡 token 数相同，工作量却相差 2.6 倍。若把早、晚位置配对，例如卡 0 持有位置 $1,2,7,8$，卡 1 持有 $3,4,5,6$，则两卡各承担 18 对。实际实现可采用分块交错或 zigzag 布局，但必须保留原位置和因果 mask，并重新安排通信；不能把远端上下文当作 padding 丢掉。 | 按位置连续切半还会造成计算不均。八个位置的因果注意力共有 $1+\cdots+8=36$ 个查询—键配对；前四个位置只有 10 对，后四个有 26 对，两张卡的 token 数相同，工作量却相差 2.6 倍。把靠前和靠后的位置搭配起来，例如卡 0 持有位置 $1,2,7,8$，卡 1 持有 $3,4,5,6$，两张卡就各承担 18 对。实际实现常用分块交错的“之”字形布局，但必须保留原始位置和因果掩码，并重新安排通信；远端上下文不能当作填充丢掉。 |
| Decode 时，新 token 的 Q 很少，而历史 K/V 很长。若沿历史位置切分，每卡可计算局部 attention 的最大值 $m_r$、指数和 $l_r$ 与未归一化加权向量 $u_r$，然后合并为 | Decode 时新 token 的查询很少，历史 K、V 却很长。沿历史位置切分后，每张卡先算出本段的最大值 $m_r$、指数和 $l_r$ 与未归一化的加权向量 $u_r$，再合并为 |
| 这与第 5 章跨 tile 的 softmax 合并相同，只是统计与向量跨卡传输。不能先分别做局部 softmax，再简单平均各卡输出。CP 减少每卡的上下文状态和局部 attention 工作，但会增加 K/V 交换或统计归约；生成的前后 token 依赖仍然存在。SP、CP 在不同框架中的命名有交叠，判断配置时应回到它切了哪些算子、哪些张量以及通信组，而不只看缩写。 | 这与第 5 章分块之间合并 softmax 的做法相同，只是统计量和向量要跨卡传输。不能各卡先分别做完 softmax，再简单平均各卡的输出。上下文并行减少了每张卡保存的上下文状态和本地的注意力计算，代价是 K、V 的交换或统计量的归约；生成时前后 token 之间的依赖并不因此消失。序列并行和上下文并行在不同框架里的名称有交叠，判断一个配置时，应看它切了哪些算子、哪些张量、用了哪个通信组，而不只看缩写。 |
| 前一阶段完成若干层，传递激活，后一阶段继续计算。各阶段保存所负责模型层的权重，阶段之间只传递中间结果。 | 前一个阶段算完自己的层，把激活传给下一个阶段继续算。每个阶段只保存自己负责的层的权重，阶段之间只传递中间结果。 |
| 使不同阶段同时处理不同组输入。设模型分成 $q$ 个阶段，每阶段的执行时间（含激活传输）为 $t$，输入由 $b$ 个已经就绪的独立微批次组成。先看四阶段、每阶段 1 ms 的情形。微批次 0 在 0 ms 进入阶段 0，经过四阶段，在 4 ms 完成。阶段 0 在 1 ms 已能接收微批次 1，后者在 5 ms 完成。其余微批次依次跟进，完成间隔为 1 ms。 | 使不同阶段同时处理不同的微批次。设模型分成 $q$ 个阶段，每个阶段的执行时间（含传递激活）为 $t$，输入是 $b$ 个已经就绪、互不依赖的微批次。先看四个阶段、每阶段 1 ms 的情形。微批次 0 在 0 ms 进入阶段 0，经过四个阶段，在 4 ms 完成；阶段 0 在 1 ms 就能接收微批次 1，它在 5 ms 完成。其余微批次依次跟进，每隔 1 ms 完成一个。 |
| 各阶段执行时间不均衡也会造成加速器空闲。若四阶段分别需 1、1、2、1 ms，第一个微批次在 5 ms 完成，之后最快每 2 ms 完成一个。2 ms 阶段前的输入到达速度高于它的处理速度，队列中的输入逐渐积压；缓冲用满后，上游必须暂停。按各层的执行时间分配模型，可以使各阶段更接近同时完成，减少阶段间积压。 | 各阶段执行时间不均也会让卡空闲。若四个阶段分别需要 1、1、2、1 ms，第一个微批次在 5 ms 完成，此后最快每 2 ms 完成一个。送到 2 ms 那个阶段的输入比它处理得快，队列逐渐积压；缓冲用满后，上游阶段只能暂停。按各层的实际执行时间划分阶段，可以让各阶段的耗时更接近，减少积压。 |
| 每个 token 只使用被选中专家的权重，加速器仍要保存可供后续选择的整个专家集合。 | 每个 token 只用到被选中的专家，但整个专家集合都要保存在加速器上，供后续 token 选择。 |
| 专家并行首先解决的是权重放置，然后才是当前 token 的执行。 | 专家并行首先要解决专家权重放在哪里，然后才是当前 token 怎样执行。 |
| 卡 0 保存专家 0、1，卡 1 保存 2、3，依次类推；卡 0 当前持有 token A，路由器为它选择专家 1 和 6。专家 1 在本地，专家 6 在卡 3。卡 0 既要执行本地专家，也要把 A 的输入送到卡 3，最后汇合两份输出，按路由权重合并。 | 卡 0 保存专家 0 和 1，卡 1 保存专家 2 和 3，依次类推。卡 0 当前持有 token A，路由器为它选中专家 1 和 6：专家 1 就在本卡，专家 6 在卡 3。卡 0 既要执行本卡的专家 1，也要把 A 的输入送到卡 3，最后收回两份输出，按路由权重合并。 |
| 专家输入派发称为 **dispatch**，输出返回与合并称为 **combine**。继续看 token A：若两个专家的输出分别为 $y_1,y_6$，路由权重为 $a_1,a_6$，完整 MoE 输出为 $a_1y_1+a_6y_6$。源卡保存 token 的编号和路由信息，目的卡执行专家，返回的结果再归到同一 token。 | 把专家输入送往专家所在卡的过程称为**派发**（dispatch），把输出送回原卡并合并的过程称为**合并**（combine）。继续看 token A：若两个专家的输出分别为 $y_1,y_6$，路由权重为 $a_1,a_6$，完整的 MoE 输出为 $a_1y_1+a_6y_6$。发送方的卡保存 token 的编号和路由信息，接收方的卡执行专家，返回的结果再归到同一个 token 名下。 |
| 专家选择数决定计算次数，跨加速器目的地决定输入派发路径。 | 每个 token 选中几个专家，就要算几次；专家在哪张卡，决定输入要送到哪里。 |
| 多个专家位于同一目的卡时，可以合并输入传输。若一个 token 选择八个专家，它们两两位于四张远端卡，4096 维 BF16 输入为 8 KiB。按专家发送八份需要 64 KiB，按目的卡发送四份只需 32 KiB，目的卡再让两个专家共享输入。这项优化利用了“同一卡上多个专家需要相同输入”这一事实。 | 多个专家在同一张卡上时，可以只发一份输入。若一个 token 选中的八个专家两两分布在四张远端卡上，4096 维的 BF16 输入为 8 KiB：按专家逐个发送要 64 KiB，按目的卡发送只要 32 KiB，同一张卡上的两个专家共用这份输入。 |
| 将多个 token 一起处理，各源卡向不同目的卡发出不同数量的数据，形成 **All-to-All** 交换：每个参与者把属于不同目的地的数据分别发给对应加速器。目的卡先按专家重排输入行，执行矩阵乘，再按 token 编号重排输出。接收输入最多的卡通常还需要完成更多的专家计算。 | 多个 token 一起处理时，每张卡要向不同的卡发送数量不等的数据。这种每张卡都向所有其他卡发送各自数据的交换称为 **All-to-All（全交换）**。接收方先按专家把收到的输入行重新排列，做矩阵乘，再按 token 编号把输出排回原序。收到输入最多的卡，通常也要做最多的专家计算。 |
| 此外还要保存注意力层、路由器及模型输入输出层的权重、KV 和工作区。专家集合可以分散，attention 的头和状态则按自己的分工保存。 | 此外每张卡还要保存注意力层、路由器和输入输出层的权重、KV 和工作区。专家集合可以分散到各卡，注意力的头和状态则按注意力自己的分工保存。 |
| TP 与 PP 可以组合：例如八卡分成四个流水阶段，每阶段两卡 TP。每层的两卡归约在阶段内部完成，相邻阶段之间传递激活。若两个 TP 进程各持有完整的 8 KiB 隐藏向量，并分别交给下一阶段对应进程，两个阶段之间总共发送的数据量就是 16 KiB；采用分片接口时，则由下一阶段根据计算需要重新组合分片。因此，采用 TP 与 PP 组合时，需要确定每个阶段输出的数据分片，以及下一阶段各进程接收的分片。 | 张量并行和流水线并行可以组合：例如把八张卡分成四个流水阶段，每个阶段内两张卡做张量并行。每层的两卡归约在阶段内完成，相邻阶段之间传递激活。若阶段内两张卡各自持有完整的 8 KiB 隐藏向量，并分别交给下一阶段对应的卡，两个阶段之间总共要发送 16 KiB；若阶段之间传递的是分片，则由下一阶段按计算需要重新拼合。因此组合两者时，要写清每个阶段输出的是哪些分片，下一阶段每张卡接收哪些分片。 |
| 分块确定工作归属，调度确定就绪、执行、通信与回收顺序，二者合起来才是完整并行方案。 | 切分确定每份工作归谁，调度确定就绪、执行、通信与回收的顺序，两者合起来才是完整的并行方案。 |
| EP 可以与专家内部 TP 组合。例如四个 EP 分组各拥有 32 个专家，每组再用两卡切分专家中间维，每卡处理 768 维。整个实例仍共八卡，可以用两个编号标识每张卡：EP 组编号确定它负责哪些专家，组内 TP 编号确定它负责这些专家的哪一半矩阵。后面将沿这份 TP2×EP4 布局推导通信。 | 专家并行也可以和专家内部的张量并行组合。例如四个专家组各拥有 32 个专家，每组再用两张卡切分专家的中间维，每张卡处理 768 维。整个实例仍是八张卡，每张卡用两个编号标识：专家组编号说明它负责哪些专家，组内张量并行编号说明它负责这些专家矩阵的哪一半。后面沿这份 TP2×EP4 布局推导通信。 |
| 现在改变输入的产生位置。在 TP2×EP4 方案中，让四个 EP 分组都对同一个请求执行 attention，并保存相同请求的 KV；attention 之后，每个分组便都有完整 MoE 输入。各组直接挑出本地专家需要的行，便无需跨 EP 组派发输入，结果则通过归约汇合。 | 再看专家输入从哪里来。让四个专家组都对同一个请求执行注意力，各自保存这个请求的 KV；注意力算完后，每个组手里都有完整的专家输入，直接挑出本组专家需要的行即可，不必跨组派发输入，各组的结果最后通过归约汇合。 |
| \| EP 分组 \| 加速器 \| 专家编号 \| 每卡专家中间维 \| 每卡 KV 头 \| | \| 专家组 \| 卡 \| 专家编号 \| 每卡专家中间维 \| 每卡 KV 头 \| |
| 先看清存放位置，再跟踪输出贡献。组内两卡各算专家的一半中间维，先沿行求和，得到本组的完整贡献；随后沿列汇合四个专家组。 | 看清数据放在哪里之后，再跟踪输出怎样汇合。组内两张卡各算专家中间维的一半，先在组内相加，得到本组专家的完整贡献；再把四个组的贡献相加。 |
| 横向箭头表示组内 TP 求和，纵向箭头表示四组 EP 求和。格内数字是行内求和后的一个输出元素，两列各自得到相同的完整结果 10。 | 横向箭头表示组内两卡求和，纵向箭头表示四个组求和。格内数字是某个输出元素经组内求和后的值，两列各自都得到相同的完整结果 10。 |
| 结果汇合分两步。先在每个 TP 对内求和，得到本 EP 分组的完整专家贡献；再沿同一 TP 坐标汇合四个 EP 分组。例如四组对某个输出元素的贡献为 1、2、3、4，偶数卡的一组归约得到 10，奇数卡的另一组也得到 10。此时每卡都持有相同的完整输出，继续计算下一层。 | 汇合分两步：先在每组的两张卡之间求和，得到本组专家的完整贡献；再在编号相同的卡之间汇合四个组。例如四个组对某个输出元素的贡献分别为 1、2、3、4，四张偶数卡一组归约得到 10，四张奇数卡另一组也得到 10。此时每张卡都持有相同的完整输出，可以继续算下一层。 |
| 省掉派发需要付出存储与计算代价。按这份布局，每卡权重约为 64.8 GB，八卡共 518.6 GB，比每个参数只保存一份时所需的 470.2 GB 多约 48.4 GB。8192 token 的每卡 KV 为 752 MiB，八卡共 5.875 GiB，是同一请求逻辑 KV 的四倍。每组重复执行 attention，换来专家输入在本组直接可用。 | 省掉派发要付出存储与计算的代价。按这份布局，每张卡的权重约 64.8 GB，八卡共 518.6 GB，比每个参数只保存一份的 470.2 GB 多约 48.4 GB。上下文 8192 token 时每卡 KV 为 752 MiB，八卡共 5.875 GiB，是这个请求实际 KV 的四倍。每个组都重复执行一遍注意力，换来的是专家输入就在本组。 |
| 归约采用 FP32，即每元素 4 字节的浮点格式，单 token 的完整隐藏张量为 $4096\times4=16$ KiB。普通 ring 在两卡间归约，每卡发送一个完整张量大小；四卡归约则发送 1.5 倍张量大小。每层有 attention TP、专家 TP、专家 EP 三次汇合，所以每卡发送 | 归约采用 FP32（每元素 4 字节），单个 token 的完整隐藏向量为 $4096\times4=16$ KiB。用第 6.4.2 节的环形算法在两张卡之间归约，每张卡发送一个完整向量的大小；四张卡归约则发送 1.5 倍。每层有注意力的组内归约、专家的组内归约、四个专家组之间的归约三次汇合，所以每张卡发送 |
| 八卡合计 448 KiB。8192-token prefill 采用相同分工，每卡发送 448 MiB。在这种布局下，每个 EP 组重复执行 attention，因此需要额外保存注意力权重和 KV，却省去了跨组派发专家输入的通信。各组得到的专家输出再通过归约合并。 | 八卡合计 448 KiB；8192 个 token 的 prefill 采用同样的分工，每张卡发送 448 MiB。这种布局让每个专家组重复执行注意力、额外保存注意力权重和 KV，换来的是不必跨组派发专家输入，各组的专家输出只需归约合并。 |
| 上一小节确定了专家由哪些加速器保存，还没有确定每一步哪些加速器最忙。这取决于当前批次的专家选择。一个 token 选八个专家，一个批次却可以覆盖整个专家集合。设 64 个 token 各选八个专家，共 512 次 token—专家分派。 | 上一小节确定了专家放在哪些卡上，还没有确定每一步哪些卡最忙，这取决于当前批次选中了哪些专家。一个 token 只选八个专家，一个批次却可能覆盖整个专家集合。设 64 个 token 各选八个专家，共 512 次 token 到专家的分派。 |
| 若八个活跃专家全部位于一个 EP 组，该组承担全部 512 次，计算量变为原来的四倍，其余三组等待它。 | 若八个活跃专家全都在一个专家组，这个组要承担全部 512 次，计算量是均匀时的四倍，其余三组只能等它。 |
| 真实路由由输入与模型共同产生。已有的 DeepSeek V4-Flash 路由记录覆盖四个输入，共记录约 131 万次专家选择，43 层中各专家被选择的频率各不相同。把每个专家接到的 token 数转换成矩阵行数，再按加速器上的专家集合相加，就能从这份路由记录得到各加速器的计算量分布。逐层热图保留在配套资料。 | 真实的路由结果由输入和模型共同决定。配套资料中的 DeepSeek V4-Flash 路由记录覆盖四个输入，共约 131 万次专家选择，43 层中各专家被选中的频率各不相同。把每个专家收到的 token 数换算成矩阵行数，再按每张卡上的专家集合相加，就能从这份记录得到各卡的计算量分布。逐层热图见配套资料。 |
| 专家输入的表示方式也会改变跨加速器通信量。Kimi K3 的路由专家在 3584 维潜空间工作，主干隐藏向量的完整维度为 7168。把投影放在发送之前，BF16 向量的大小从 14 KiB 降到 7 KiB；把投影放在接收之后，就要传输未经降维的输入。对于相同的专家计算，投影位置决定跨加速器传输的向量维度。 | 专家输入怎样表示，也会改变跨卡通信量。Kimi K3 的路由专家在 3584 维的潜空间里工作，主干隐藏向量则是 7168 维。先投影再发送，BF16 向量从 14 KiB 降到 7 KiB；先发送再投影，传的就是未降维的输入。同样的专家计算，投影放在发送前还是接收后，决定了跨卡传输的向量维度。 |
| Dense 与 MoE 还会需要拼接其他分片、把专家输入发给不同加速器；按开始与结束时各卡持有的数据，可以区分下面四种集合通信。 | 稠密模型和 MoE 还需要拼接分片、把专家输入发给不同的卡；按通信开始和结束时各卡持有什么数据，可以区分下面四种集合通信。 |
| Broadcast 把一个指定 rank 的数据广播给全组，Reduce 则只把归约结果交给指定 rank；它们与表中的全组持有结果不同。AllGather 按 rank 拼接数据，不执行求和；All-to-All 按目的地重新分配数据，也不自动执行专家输出的加权合并。MoE 的 combine 因而包括结果回传、恢复 token 顺序与按路由权重求和，不能只凭名字把它等同于 AllReduce。 | Broadcast（广播）把某一张指定卡的数据发给全组，Reduce（归约）只把归约结果交给指定的一张卡，两者都不像表中的操作那样让全组持有结果。AllGather 按卡的编号拼接数据，不做求和；All-to-All 按目的地重新分配数据，也不会自动按路由权重合并专家输出。MoE 的合并因此包括结果回传、恢复 token 顺序和按路由权重求和三步，不能只凭名字把它当成 AllReduce。 |
| 操作名称规定了计算方式和结果在各卡上的分布，具体算法则规定消息怎样分轮传递。 | 操作名称规定了计算什么、结果落在哪些卡上，具体算法则规定数据分几轮、怎样传递。 |
| 各卡开始通信的时刻也会影响等待时间。设四卡分别在 0、0、0、2 ms 发起一次集合通信，所有卡都发起通信后，再用 0.4 ms 完成交换。前三卡等待 2.4 ms，最后一卡等待 0.4 ms；把交换加快一倍只省 0.2 ms，消除最后一卡的计算延误却省 2 ms。前三张卡的调用时间较长，是因为它们还要等待最后一张卡完成前面的计算。 | 各卡发起通信的时刻也影响等待时间。设四张卡分别在 0、0、0、2 ms 发起同一次集合通信，最后一张卡发起之后，再用 0.4 ms 完成交换。前三张卡从发起到完成等了 2.4 ms，最后一张只等 0.4 ms；把交换加快一倍只省 0.2 ms，让最后一张卡不再迟到却能省 2 ms。前三张卡等得久，是因为它们在等最后一张卡算完前面的计算。 |
| 设参与加速器数为 $n$，每卡完整部分和为 $M$ bytes | 设参与的卡数为 $n$，每卡的完整部分和为 $M$ bytes |
| 第一项随参与加速器数增长，第二项逐渐接近 $2M/B$。环形算法把大消息分块，由各卡同时传输；对小消息而言，逐轮启动所需的时间则占比较大。 | 第一项随卡数增长，第二项逐渐接近 $2M/B$。环形算法把大块数据分成小块，让各卡同时传输；数据量小时，逐轮启动的时间反而占大头。 |
| 八卡、8 KiB 时约为 13.0 μs，比 ring 的 28.3 μs 短。消息增大到 64 MiB，ring 为约 2.38 ms，树为约 8.07 ms：减少轮次所省的时间，抵不过在关键路径上反复传递完整大张量所增加的时间。令两式相等，交点约为 184 KiB，消息大小跨过这个位置，执行时间较短的算法也随之改变。 | 八卡、8 KiB 时约为 13.0 μs，比环形算法的 28.3 μs 短。数据量增大到 64 MiB，环形算法约需 2.38 ms，树形约需 8.07 ms：减少轮次省下的时间，抵不过在关键路径上反复传递完整大张量多花的时间。令两式相等，交点约在 184 KiB；数据量跨过这个值，更快的算法就换成另一个。 |
| *图 6-25：八卡 ring 与未分段二项树的时间模型 | *图 6-25：八卡环形算法与未分段二项树的时间模型 |
| > **实验 6-4 · 延伸：消息多大时，环形归约比树形归约更快？** | > **实验 6-4 · 延伸：数据量多大时，环形归约比树形归约更快？** |
| > （b）求四卡和八卡 ring 的每卡发送量 | > （b）求四卡和八卡环形归约的每卡发送量 |
| 应用先建立通信组（communicator），为各参与者分配 rank，再提交带有缓冲区、元素数量、数据类型、归约运算和 CUDA stream 的操作。组内调用必须按接口要求匹配，包括调用顺序和对应的数据数量；一次调用返回通常表示操作已提交到 stream，不能据此复用仍在传输的缓冲区。消费者需要同一 stream 的顺序或显式事件依赖。集合通信也不等同于一个供任意主机代码使用的全局 barrier。 | 应用先建立通信组（communicator），给组内每张卡分配一个编号（rank），再提交操作，说明缓冲区、元素数量、数据类型、归约运算和所在的 CUDA 流。组内各卡的调用必须按接口要求相互匹配，包括调用顺序和数据数量；调用返回通常只表示操作已提交到流中，不能据此复用仍在传输的缓冲区。使用结果的一方要靠同一条流的顺序或显式的事件依赖来等待。集合通信也不是一个可供任意主机代码使用的全局同步点。 |
| 在 MoE 中，各对端之间的分派数量随批次变化。等长 All-to-All 不能直接表达所有这种不等长交换，运行时还要组织计数、偏移、打包和接收空间。NCCL 可用成组的 Send／Recv 表达对端间交换；每对发送与接收必须匹配，把需要共同推进的通信放进同一 group，才能避免串行等待未发起的对端操作。专用 EP 库进一步结合路由信息实现 dispatch 和 combine。选择哪条路径，应记录库版本与具体接口，不能把固定大小的 All-to-All 测试当成实际 MoE 通信。 | 在 MoE 中，每对卡之间派发的 token 数随批次变化。等长的 All-to-All 表达不了这种不等长的交换，运行时还要自己组织计数、偏移、打包和接收空间。NCCL 可以用成组的 Send／Recv 表达任意两卡之间的交换；每个发送必须有对应的接收，需要一起推进的通信要放进同一个组，否则会串行等待对方尚未发起的操作。专门的专家并行通信库进一步结合路由信息实现派发和合并。无论选哪种实现，都应记录库的版本与具体接口，不能把固定大小的 All-to-All 测试当作实际的 MoE 通信。 |
| 测量 NCCL 时，还要区分 **algbw** 与 **busbw**。前者是测试定义的数据量除以时间；后者按集合操作换算。 | 测量 NCCL 时，还要分清它报告的两种带宽：**algbw** 是测试定义的数据量除以时间，**busbw** 则按集合操作的实际传输量换算。 |
| busbw 是归一化指标，不能直接当作每张 NIC 的实际吞吐；分层通信、交换机内归约、链路复用仍要按第 7 章的物理路径逐项计数。小消息看延迟，大消息看持续带宽，模型执行还要看数据就绪到消费完成的区间。 | busbw 是归一化指标，不能直接当作每张网卡的实际吞吐；分层通信、交换机内归约、链路复用仍要按第 7 章的物理路径逐项计数。数据量小时看延迟，数据量大时看持续带宽，放到模型执行里还要看从数据就绪到用完的整段时间。 |
| 环和树的比较把一次通信作为独立操作来分析。实际执行中，通信还可能与其他微批次的计算重叠，这时两者会争用加速器资源。 | 前面比较环和树时，把一次通信当作独立操作。实际执行中，通信常与另一个微批次的计算重叠，两者会争用加速器资源。 |
| 则 66 次只省 11.88 ms，67 次省 12.06 ms，第一次超过准备成本。还剩 50 次调用时，搜索增加总时间；还剩 100 次时，净省 6 ms。 | 则 66 次只省 11.88 ms，67 次省 12.06 ms，才第一次超过搜索成本。若只剩 50 次调用，搜索反而增加总时间；剩 100 次时净省 6 ms。 |
| Gloo 是支持 CPU 等执行环境的集合通信库。CPU/Gloo 并发实验也展示了资源共享后的变化：64 MiB 通信耗时的中位数从约 36.9 ms 增至 45.2 ms，矩阵乘从约 11.0 ms 增至 12.9 ms。通信和计算的耗时都增加了，说明评价重叠执行的效果时，需要计入资源竞争造成的时间增加。 | Gloo 是支持 CPU 等执行环境的集合通信库。配套的 CPU 上 Gloo 并发实验也显示了资源共享的影响：64 MiB 通信耗时的中位数从约 36.9 ms 增至 45.2 ms，矩阵乘从约 11.0 ms 增至 12.9 ms。两者都变慢了，说明评价重叠执行的效果时，要把资源竞争造成的时间增加算进去。 |
| 归约必须先形成完整输入，归一化再使用它： | 归一化必须等归约得到完整输入之后再做： |
| **加速器发起**减少主机提交与加速器执行之间的往返。一个加速器内核产生输入后，可以直接推进后续通信，缩短计算完成到通信开始之间的等待。**专用卸载**则把搬运、归约或同步交给复制引擎、通信处理单元或交换侧逻辑，为通用计算单元释放资源，使其能够承担更多矩阵计算。 | **加速器发起**省去主机提交与加速器执行之间的往返：一个内核算出数据后，可以直接发起后续通信，缩短从计算完成到通信开始的等待。**专用卸载**把搬运、归约或同步交给复制引擎、通信处理单元或交换机内的逻辑，把通用计算单元腾出来做矩阵计算。 |
| 它们改变的是工作由谁执行。若图 6-26 中的计算受 SM 争用影响，释放 SM 可以缩短计算部分的执行时间；若瓶颈位于共同的网络出口，出口仍需传完相同字节。 | 它们改变的是由谁来做这些工作。若图 6-26 中的计算慢在与通信争用 SM，把 SM 让出来就能缩短计算；若瓶颈在共用的网络出口，出口要传的字节数并没有减少。 |
| 框架会根据消息大小和连接方式选择通信实现。Qwen3-8B 的 1、4、16 个 token 产生 8、32、128 KiB 的归约输入，vLLM 等框架可为这些形状选用不同的小消息通信实现。 | 框架会按数据量和连接方式选择通信实现。Qwen3-8B 一次处理 1、4、16 个 token 时，归约输入分别为 8、32、128 KiB，vLLM 等框架可以为这些小数据量选用专门的实现。 |
| 前面的算法和通信库决定怎样发消息，却还没有说明消息经过哪些物理连接。集合算法规定逻辑上的发送者和接收者，物理拓扑决定消息实际经过哪些设备与链路。两张卡可以直连，也可以经交换机、其他设备或主机内存中转。多个逻辑消息共用一个接口时，要排队使用同一份带宽。 | 前面的算法和通信库决定了每一轮谁发给谁、发多少，还没有说明数据经过哪些物理连接。集合算法规定逻辑上的发送方和接收方，物理拓扑决定数据实际经过哪些设备与链路。两张卡可以直连，也可以经交换机、其他卡或主机内存中转。多次传输共用一个接口时，要排队使用同一份带宽。 |
| 要让更多加速器高效地协同计算，需要提供足够端口和交换容量。一个端点若与其他 $N-1$ 个端点分别直连，就需要 $N-1$ 条连接。交换机将连接集中：端点先接交换层，再由交换层转发到目的地。交换芯片可提供的端口数称为 **radix**。 | 要让更多的卡高效协作，就要有足够的端口和交换容量。一张卡若与其他 $N-1$ 张卡都直连，就需要 $N-1$ 条连接。交换机把连接集中起来：卡先接到交换层，再由交换层转发到目的地。一颗交换芯片提供的端口数称为**基数**（radix）。 |
| 考虑一台叶交换机（leaf），它有 32 个等速端口，每端口的单向带宽为 50 GB/s。用 16 个端口下联加速器，另外 16 个端口上联脊交换机（spine），加速器侧总输入带宽和上联总带宽均为 800 GB/s。若改为 24 下联、8 上联，接入加速器增多，但它们向上最多仍只能送出 400 GB/s，只有全部加速器总输入带宽的三分之一。端口总数不变时，增加加速器接入端口，就会减少上联端口及其总带宽。 | 考虑一台**叶交换机**（leaf），它有 32 个等速端口，每个端口的单向带宽为 50 GB/s。用 16 个端口向下接卡，另外 16 个端口向上接**脊交换机**（spine），卡这一侧的总带宽和上联总带宽都是 800 GB/s。若改成 24 个下联、8 个上联，接入的卡多了，但它们一起向上最多只能送出 400 GB/s，只有下联总带宽的三分之一。端口总数不变时，多接卡就少了上联端口和上联带宽。 |
| 四台叶交换机各有 16 个下联端口，共连接 64 个端点。再配置 16 台脊交换机，让每台脊交换机各用一条链路连接这四台叶交换机，就能用满每台叶交换机的 16 条上联链路。对一侧必须送往另一侧的流量，所有跨界链路构成一个**割集**。若这一阶段有 $V_{A\to B}$ bytes 必须穿过有向带宽 $B_{A\to B}$ 的割集，传输至少需要 $V_{A\to B}/B_{A\to B}$。交换机的端口分配决定了这些跨组链路的总带宽。 | 四台叶交换机各有 16 个下联端口，共接 64 张卡。再配 16 台脊交换机，每台各用一条链路连到这四台叶交换机，正好用满每台叶交换机的 16 条上联。把系统分成两部分，所有连接这两部分的链路合起来称为一个**割集**。若某一阶段有 $V_{A\to B}$ bytes 必须从 A 侧穿过割集到 B 侧，而割集在这个方向的总带宽为 $B_{A\to B}$，传输至少需要 $V_{A\to B}/B_{A\to B}$。交换机的端口分配决定了割集的总带宽。 |
| 端口数量决定总共有多少链路，路由则决定流量集中到哪些链路。即使每个加速器发送的数据量相同，不同路径也会造成不同的瓶颈。将 16 个节点连成双向物理环，观察 8 MiB 输入的 ReduceScatter 前三轮。Swing 是为环面拓扑设计的集合通信算法，通过改变各轮通信对端分散链路负载。递归算法和 Swing 每节点每轮同样发送 4、2、1 MiB，但对端选择不同： | 端口数决定一共有多少链路，路由决定数据集中到哪些链路。即使每张卡发送的数据量相同，走不同的路径也会形成不同的瓶颈。把 16 张卡连成一个双向物理环，观察 8 MiB 输入的 ReduceScatter 前三轮。递归算法每轮把对端的距离翻倍；Swing 是为环面拓扑设计的集合通信算法，通过改变各轮的对端来分散链路负载。两种算法每张卡每轮同样发送 4、2、1 MiB，但对端不同： |
| \| 每消息经过的物理跳数 \| 1、2、4 \| 1、1、3 \| | \| 每次传输经过的物理跳数 \| 1、2、4 \| 1、1、3 \| |
| 第三轮递归消息走四跳，传输量最大的链路同时承载四条 1 MiB 消息；Swing 消息走三跳，传输量最大的链路承载两条。按每有向边 50 GB/s，前三轮的瓶颈传输时间分别约为 252 与 168 μs。传输时间减少三分之一，是因为共享链路上的数据量减少了。 | 第三轮，递归算法的数据要走四跳，最忙的链路同时承载四份 1 MiB 数据；Swing 的数据走三跳，最忙的链路只承载两份。按每条有向链路 50 GB/s 计，前三轮由瓶颈链路决定的传输时间分别约为 252 与 168 μs。传输时间少了三分之一，是因为共享链路上的数据少了。 |
| *图 6-29：递归算法第三轮从节点 0 发往节点 4。细线为 16 节点物理环，箭头标出经过的四条链路。* | *图 6-29：递归算法第三轮，卡 0 发往卡 4。细线为 16 张卡组成的物理环，箭头标出经过的四条链路。* |
| *图 6-30：同一物理环上，Swing 第三轮从节点 0 发往节点 3，经过三条链路。每节点该轮发送量仍为 1 MiB。* | *图 6-30：同一物理环上，Swing 第三轮卡 0 发往卡 3，经过三条链路。每张卡这一轮的发送量仍为 1 MiB。* |
| *图 6-31：把所有发送者的消息累加到各条有向链路上，取每轮最大值。 | *图 6-31：把所有发送方的数据累加到各条有向链路上，取每轮的最大值。 |
| 主机中转是同一问题的另一种形式。非统一内存访问（NUMA）结构把主机内存分到不同 CPU 附近，跨 CPU 读取要经过处理器间接口。G0、G1 接 NUMA 节点 A，G2、G3 接 B，四卡 ring 共发送 48 MiB。把全部中转缓冲放在 A，CPU 间每个方向承担 24 MiB；把缓冲移到发送卡附近，并让同 NUMA 的卡在环中相邻，每方向降到 12 MiB。若再将环改成交替跨 NUMA，每个方向的传输量又回到 24 MiB。若 CPU 间链路的单向带宽为 8 GB/s，三种方案的传输时间至少分别为约 3.15、1.57、3.15 ms。缓冲位置和卡的通信次序共同决定搬运路径。 | 经主机内存中转是同一个问题的另一种形式。非统一内存访问（NUMA）结构把主机内存分在不同 CPU 附近，跨 CPU 读取要经过处理器之间的接口。设卡 0、1 接在 NUMA 节点 A，卡 2、3 接在节点 B，四张卡做环形归约共发送 48 MiB。把全部中转缓冲放在 A，CPU 之间每个方向要承担 24 MiB；把缓冲放到发送卡附近，并让同一 NUMA 节点的卡在环中相邻，每个方向降到 12 MiB；若把环改成交替跨越 NUMA 节点，每个方向又回到 24 MiB。若 CPU 之间链路的单向带宽为 8 GB/s，三种方案的传输时间至少分别约为 3.15、1.57、3.15 ms。缓冲的位置和卡在环中的次序共同决定数据的搬运路径。 |
| 前面的例子里，消息要么走一条链路，要么经过一台交换机。 | 前面的例子里，数据要么直接走一条链路，要么经过一台交换机。 |
| 图 6-32 画出两种连接下一条消息经过的路径。 | 图 6-32 画出在这两种连接方式下，一次传输经过的路径。 |
| ![环面与交换网络中一条消息经过的路径](ch06/figure-6-topology-hops.svg) | ![环面与交换网络中一次传输经过的路径](ch06/figure-6-topology-hops.svg) |
| 橙色是本文比较的最远一对卡，粗线是消息经过的路径。 | 橙色是相距最远的一对卡，粗线是它们之间传输经过的路径。 |
| 一个 8 KiB 的 decode 消息在环面上最远要多花 0.4 μs。 | decode 时一次 8 KiB 的传输，在环面上最远的两张卡之间要多花 0.4 μs。 |
| 环面上一条消息平均要走三跳（每个维度平均一跳） | 环面上每份数据平均要走三跳（每个维度平均一跳） |
| 均匀 All-to-All 时环面上的消息平均要走三跳 | 均匀 All-to-All 时环面上的数据平均要走三跳 |
| 还是有大量任意目的地的小消息和 All-to-All | 还是有大量目的地任意的小数据传输和 All-to-All |
| > （c）根据图 6-29，累加第三轮经过同一有向链路的消息大小，说明消息跳数与链路传输量的区别。 | > （c）根据图 6-29，累加第三轮经过同一条有向链路的数据量，说明跳数与链路传输量的区别。 |
| > （d）在 NUMA 例中先移动缓冲，再改变 ring 次序，分别画出新增与消失的跨 CPU 访问。 | > （d）在 NUMA 例中先移动缓冲，再改变环的次序，分别画出新增与消失的跨 CPU 访问。 |
| 每张卡连同分摊的互联加速器功率为 $P_c$ | 每张卡连同分摊的互联设备功率为 $P_c$ |
| 固定配套加速器占用 12 kW | 固定配套设备占用 12 kW |
| 以第 6.5.1 节的八卡任务为例：在两台服务器之间，每轮启动耗时增加，使单步从约 4.28 ms 增到 6.32 ms；若所需八卡能放在同一个满足前述低延迟条件的互联域内，便可省去这部分跨服务器通信开销。更大的互联域也可以容纳多个两卡或四卡实例，各自完成自己的请求。 | 以第 6.5.1 节的八卡例题为例：八张卡分在两台服务器时，每轮通信的启动开销增加，单步从约 4.28 ms 增到 6.32 ms；若八张卡都放在同一个低延迟互联域内，这部分跨服务器的开销就省掉了。更大的互联域也可以容纳多个两卡或四卡实例，各自处理自己的请求。 |
| NVIDIA 的例子突出交换层如何扩大 GPU 的连接范围。Google 的张量处理器 TPU 则采用规则的互联形状，让模型切分、编译布局和物理通信彼此对应。二维环面网络（torus）沿两个维度连接加速器，每一维的首尾相连；三维环面网络再增加一个维度，使数据可以沿更多方向传输。 | NVIDIA 的例子突出交换层怎样扩大 GPU 的连接范围。Google 的张量处理器 TPU 则沿用第 6.5.1 节设计案例中的环面，让模型切分、编译器的布局和物理通信互相对应。二维环面沿两个维度连接芯片，每一维首尾相连；三维环面再加一个维度，数据可以沿更多方向传输。 |
| 将一个 $k\times k\times k$ 的三维 torus 沿某维等分 | 将一个 $k\times k\times k$ 的三维环面沿某一维等分 |
| 若每芯片都要向另一半发送等量数据，平均分得的割集带宽就减半。拓扑规模增长速度与跨组带宽增长速度之间的差别，决定了哪些并行布局更适合该形状。 | 若每颗芯片都要向另一半发送同样多的数据，平均每颗芯片分到的割集带宽就减半。芯片数与割集带宽增长速度的差别，决定了哪些并行布局适合这种形状。 |
| 固定拓扑决定了跨分区的带宽，空闲加速器的排列还决定作业能否取得所需的形状。OCS 在更大尺度上重连这些电互联单元，为不同作业分配形状合适的加速器子集。空闲资源的位置因此也很重要：一个 4×4 的加速器网格若有连续两行空闲，可以放下两个各需相邻 2×2 加速器的作业；同样八个 token 若呈棋盘式空闲，就找不到一组相邻的 2×2 空闲加速器。重构连接能够改变这种可分配形状。 | 固定拓扑决定了跨分区的带宽，空闲芯片的位置还决定一个作业能否拿到需要的形状。OCS 在更大尺度上重新连接这些电互联单元，为不同作业分配形状合适的芯片子集。以一个 4×4 的芯片网格为例：若有连续两行空闲，可以放下两个各需要相邻 2×2 芯片的作业；同样八颗空闲芯片若呈棋盘状分布，就找不出一组相邻的 2×2。重新连接可以改变可分配的形状。 |
| 重构连接所节省的执行时间需要超过重构本身的耗时。Morphlux 研究为任务重构光互联拓扑，使连接适合任务的通信需求。在配套的教学算例中，重构连接额外需要 100 ms，一次大消息交换由约 657 μs 降到 266 μs，单次省约 391 μs，重复 256 次所节省的时间才超过这 100 ms。作业持续得越久，建立更好连接的收益越容易积累。 | 重新连接省下的执行时间要超过重连本身的耗时才划算。Morphlux 研究按任务重构光互联拓扑，让连接适合任务的通信需求。在配套的教学算例中，重连额外需要 100 ms，一次大数据量交换由约 657 μs 降到 266 μs，每次省约 391 μs，重复 256 次省下的时间才超过这 100 ms。作业持续得越久，建立更合适的连接越划算。 |
| TP 组内频繁归约适合放在低延迟连接内，不同 EP 组交换专家结果时，则需要足够的跨组通信带宽。将算法通信组映射到物理交换层，才能确定每次汇合占用了哪些端口。 | 张量并行组内的频繁归约适合放在低延迟连接内，不同专家组之间交换专家结果时，则需要足够的跨组带宽。把算法上的通信组映射到物理交换层，才能确定每次汇合占用了哪些端口。 |
| 使用接口带宽计算传输时间时，需要区分单向带宽和双向带宽之和。例如昇腾 950 的 UB 双向线速汇总为 2016 GB/s，按 72 条串行传输通道（lane）、每条 112 Gbit/s 计算，单向为 1008 GB/s。UBoE 是在以太网上承载 UB 通信的方式，UB Link 是 UB 的链路连接方式。两者按预先配置的方式共用串行器／解串器（SerDes，将芯片内部并行数据与链路串行信号互相转换的电路），因此需要将同一组物理 lane 分配给这两种用途。端口分配方式决定任务实际可用的带宽。 | 用接口带宽计算传输时间时，要分清单向带宽和双向合计带宽。例如昇腾 950 的 UB 双向合计线速为 2016 GB/s，按 72 条串行通道（lane）、每条 112 Gbit/s 计，单向为 1008 GB/s。UBoE 是在以太网上承载 UB 通信的方式，UB Link 是 UB 自己的链路方式。两者按预先配置共用同一组串行器／解串器（SerDes，把芯片内的并行数据与链路上的串行信号互相转换的电路），所以要把同一组物理通道分配给两种用途。分配方式决定任务实际可用的带宽。 |
| > （b）复算 4³ 与 8³ torus 的芯片数和均衡二分链路数 | > （b）复算 4³ 与 8³ 环面的芯片数和均衡二分链路数 |
| > （c）将 TP2×EP4 放到两个各含四卡的主机，标出跨主机的 EP 归约边。 | > （c）将 TP2×EP4 放到两台各有四张卡的主机上，标出跨主机的专家组归约。 |
| 前面介绍的 TP、PP 和 EP 将计算与权重分配到不同加速器，以降低每张卡的内存需求。另一种方法是保持计算位置不变，只把一部分数据放到其他节点。这正是内存池的基本用途：将分散的空闲容量交给需要它的任务。 | 前面介绍的张量并行、流水线并行和专家并行把计算和权重分到不同的卡上，降低每张卡的内存需求。另一种办法是计算位置不变，只把一部分数据放到别的节点。这就是内存池的基本用途：把分散的空闲容量交给需要它的任务。 |
| 作者早期 UB 内存池构思关注的正是这种跨服务器不均衡，KV 保存与复用随后提供了具体应用。 | 作者早期关于 UB 内存池的构思，关注的正是这种服务器之间的不均衡；KV 的保存与复用后来成为它的具体用途。 |
| 地址标识回答“访问哪里” | 地址回答“访问哪里” |
| 访问权限和完成顺序保证数据正确，图中的等待过程则决定读取速度。请求发出后不能立即返回；要在等待期间保持链路忙碌，就需要同时发出多个请求。设每个读事务（一次读取请求）返回 $q$ bytes，从发出到完成需要 $L$，最多允许 $u$ 个事务已经发出但尚未完成，即同时在途。每 $L$ 秒最多返回 $uq$ bytes。同时考虑路径带宽和在途事务数量的限制，可得 | 权限和完成顺序保证数据正确，图中的等待过程则决定读取速度。请求发出后要过一段时间才有结果；要让链路在等待期间不闲着，就得同时发出多个请求。设每个读事务返回 $q$ bytes，从发出到完成需要 $L$，最多允许 $u$ 个事务同时在途（已发出、尚未完成），则每 $L$ 秒最多返回 $uq$ bytes。把路径带宽和在途事务数的限制放在一起，可得 |
| 增加线速而仍只允许 128 个事务同时在途，有效读取带宽仍受在途事务数量限制；增加到 4096 个事务，在途事务数量决定的带宽上限超过路径带宽，瓶颈便转到 40 GB/s 的传输路径。 | 只提高线速而仍只允许 128 个事务在途，有效读取带宽照样受在途事务数限制；放宽到 4096 个事务，在途事务数决定的上限超过路径带宽，瓶颈就转到 40 GB/s 的路径本身。 |
| KV 的使用方式决定了适合把它保存在何处。会话在等待用户下一轮输入时，可以长时间不读取 KV，远端内存主要用于保存状态，在会话恢复时再取回；活跃会话每步都需要上下文状态，反复远端扫描则持续消耗带宽。 | KV 怎样使用，决定了它适合放在哪里。会话等待用户下一轮输入时，可以很久不读 KV，这时远端内存主要用来保存状态，会话恢复时再取回；活跃会话每一步都要读上下文状态，反复从远端扫描会持续占用带宽。 |
| 采用 6.4 的低延迟八卡连接，全部加速器一直分配给这组任务，直到四个会话都完成。 | 采用第 6.4 节的低延迟八卡连接，八张卡一直分配给这组任务，直到四个会话都完成。 |
| 这里同时利用了模型内部的并行计算与不同请求之间的并发执行。层内矩阵可以切分给多卡，独立会话可以分配给多个实例；两种并行共同竞争这八张卡。掌握这两层关系，才能根据请求的完成期限选择协作组大小。 | 这里同时用到了两种并行：模型内部的并行计算，和不同请求之间的并发执行。层内矩阵可以切给多张卡，独立会话可以分给多个实例，两者争的是同一批八张卡。分清这两层关系，才能按请求的完成期限选择协作组的大小。 |
| 连接改变后再沿原式计算。若八卡跨过 6.5.1 的服务器边界，每步约 6.32 ms，八步约 50.5 ms，原来的 40 ms 目标无法满足。要使这一部署满足时限，需要降低新增的通信开销：改善服务器间连接、改变集合通信算法，或将八卡放进更紧密的互联域。 | 连接方式改变后，仍沿原式计算。若八张卡像第 6.5.1 节那样分在两台服务器上，每步约 6.32 ms，八步约 50.5 ms，40 ms 的目标就达不到了。要满足时限，就得降低新增的通信开销：改善服务器之间的连接、改变集合通信算法，或者把八张卡放进更紧密的互联域。 |
| 权重由独立 ROM 提供后 | 权重由独立的只读存储器（ROM）提供后 |
| 计算 V4.1 所需的加速器数时，除了 890 B/token 的逻辑状态容量，还要考虑状态在加速器间的分布。这一数值对跨层共享状态只计一份；如果模型各层被放在不同加速器上，后续层仍须访问生成层保存的这些状态。可以把相关层部署在相近的加速器上、在目标加速器复制状态，或建立远程访问路径。第一种限制了模型层在加速器上的分配方式，第二种增加存储占用和更新开销，第三种增加通信依赖，因此需要按各加速器实际保存和访问的数据分别计算资源需求。 | 计算 DeepSeek V4.1 需要多少张卡时，除了每 token 890 B 的逻辑状态容量，还要看状态怎样分布在各卡上。这个数值对跨层共享的状态只计一份；如果各层放在不同的卡上，后面的层仍要访问生成这些状态的那一层所保存的数据。可以把相关的层放在相近的卡上，在目标卡上复制状态，或者建立远程访问路径。第一种限制了层的放置，第二种增加存储和更新开销，第三种增加通信依赖，所以要按每张卡实际保存和访问的数据分别计算资源需求。 |
| 占用成本覆盖全部工作的持续时间，分母只计在期限内完成的会话。分子还包括失败、重新执行和超时请求所产生的成本，因此这些开销也计入按时完成的会话的平均成本。 | 占用成本按全部工作持续的时间计，分母只数期限内完成的会话。失败、重做和超时请求的成本也在分子里，所以它们都摊到了按时完成的会话上。 |
| 故障隔离使另外三个会话仍能按时完成，延长的加速器占用时间和额外恢复成本仍然显著抬高成本。 | 故障被隔离在一个实例内，另外三个会话仍能按时完成；但占用时间延长和恢复费用仍把成本抬高了不少。 |
| 在 6.5.2 的线性功率模型中 | 按第 6.5.2 节的线性功率模型 |
| 回到章首的问题，现在可以把切分选择写成一项有输入、有约束的优化任务。令候选 $\pi$ 包括并行度、物理放置、通信算法、微批次和局部算子实现；先确定目标 $J$，再寻找 | 回到章首的问题。现在可以把切分策略的选择写成一个有输入、有约束的优化问题：候选方案 $\pi$ 包括并行度、物理放置、通信算法、微批次划分和局部算子实现，先确定目标 $J$，再寻找 |
| $\mathcal P_{\mathrm{feasible}}$ 中的每个候选都必须满足逐卡容量、模型分片与数据依赖、内核支持和服务期限。$J$ 可以是单请求延迟，也可以是本节的每个按时完成会话的成本；若目标是最大吞吐，就在相同 SLO 下比较可持续接纳的请求率。没有固定负载与目标，“最优并行度”就没有唯一含义。 | 可行集 $\mathcal P_{\mathrm{feasible}}$ 里的每个候选都要满足每卡容量、模型切分的数据依赖、内核支持和服务期限。目标 $J$ 可以是单个请求的延迟，也可以是上一节的每个按时完成会话的成本；若目标是吞吐，就在相同的服务目标（SLO）下比较能持续接纳的请求率。负载和目标不固定，“最优并行度”就没有唯一的答案。 |
| 模型侧需要层数、矩阵维度、注意力头与 KV 头、专家数和 top-k、精度、共享权重及跨层状态。 | 模型侧需要层数、矩阵维度、注意力头与 KV 头、专家数和每 token 选中的专家数、精度、共享权重及跨层状态。 |
| 第 4 章给供给参数，第 5 章给实际形状与局部执行时间，不能把集群总带宽直接平均给每个通信组。 | 第 4 章给出硬件参数，第 5 章给出实际形状与局部执行时间；不能把集群的总带宽平均分给每个通信组。 |
| **第二步，按瓶颈形成候选，而不是先选一个缩写。** 下表给出增加候选的理由；它用于缩小搜索方向，最终仍要比较完成时间。 | **第二步，按瓶颈形成候选，而不是先选一个缩写。** 下表列出各种瓶颈下应当加入哪些候选；它只用来缩小搜索范围，最终仍要比较完成时间。 |
| \| 长上下文的 KV／attention 占主导 \| CP 与 TP 的组合 \| K/V 交换、统计归约、因果负载不均 \| | \| 长上下文的 KV 与注意力占主导 \| CP 与 TP 的组合 \| K、V 交换、统计量归约、因果注意力的负载不均 \| |
| \| 多层权重容量大，跨节点归约昂贵 \| 节点内 TP＋跨节点 PP \| 微批次是否足够，最慢阶段和气泡 \| | \| 多层权重容量大，跨节点归约昂贵 \| 节点内 TP＋跨节点 PP \| 微批次是否足够、最慢阶段与流水空档 \| |
| \| 专家集合大、路由热点突出 \| EP、专家副本、专家分离 \| dispatch／combine 两方向、最忙卡与池队列 \| | \| 专家集合大、路由热点突出 \| EP、专家副本、专家分离 \| 派发与合并两个方向的流量、最忙的卡与专家池的队列 \| |
| **第三步，逐个候选建立所有权和时间表。** 给每张卡标出它保存哪些层、头、专家、激活和 KV；峰值内存还要加入集合通信缓冲、双缓冲、padding、在途微批次和副本迁移空间。随后为每个算子求本地计算／访存时间，为每次跨卡操作求轮数、字节与共享路径，将两者排到依赖时间线上。单个 token 的前后层不能因 PP 而同时完成，同一 token 选择的专家结果不能因网络空闲而提前就绪。MoE 要把最忙组而非平均组代入，第 9.4 节给出了具体偏斜模型。 | **第三步，为每个候选写出数据归属和时间表。** 给每张卡标出它保存哪些层、头、专家、激活和 KV；算峰值内存时还要加上集合通信缓冲、双缓冲、填充、在途微批次和副本迁移的空间。然后为每个算子求本地计算与访存时间，为每次跨卡操作求轮数、字节数与共享路径，把两者按依赖关系排到时间线上。同一个 token 的前后层不会因为流水线并行而同时完成，同一个 token 选中的专家结果也不会因为网络空闲而提前就绪。MoE 要代入最忙的专家组而不是平均值，第 9.4 节给出具体的偏斜模型。 |
| 本章的八卡例子固定为稠密 Qwen3-8B、每实例一次只处理一个会话、当前状态已能放下，以 TP1／2／4／8 和剩余卡组成的独立实例作为首轮候选。EP 不适用于该稠密模型；SP 主要改变激活区间，在这个单 token、固定工作区的模型中不计额外收益；PP 若不交错多个会话，仍要依次经过全部阶段，因此没有把它当作缩短单会话的首选。CP、跨会话流水和连续批处理可以进入扩展搜索，但必须分别补上上下文归约、阶段排程与批量变化的时间模型，不能套用 TP 的 $1/p$ 缩放。 | 本章的八卡例子固定为稠密的 Qwen3-8B，每个实例一次只处理一个会话，当前状态放得下；首轮候选是 TP 取 1、2、4、8，其余卡组成独立实例。专家并行不适用于稠密模型；序列并行只改变一段激活的存储，在这个逐 token、工作区固定的算例里没有额外收益；流水线并行若不交错多个会话，单个会话仍要依次经过全部阶段，所以没有把它当作缩短单会话时间的首选。上下文并行、跨会话流水和连续批处理可以进入扩展搜索，但要分别补上上下文归约、阶段排程和批量变化的时间模型，不能套用张量并行的 $1/p$ 缩放。 |
| \| 两台四卡、跨机参数改为 4 μs 与 25 GB/s，单会话 40 ms \| | \| 两台四卡服务器、跨机每轮 4 μs 与 25 GB/s，单会话 40 ms \| |
| **第五步，用测量修正，而不是把估算排名当成证明。** 对排名靠前的候选使用相同请求、路由与质量条件，记录峰值内存、每阶段就绪／完成、各端口收发和服务尾延迟。若实际矩阵效率、通信争用或排队与估算不同，替换对应参数后重排；如果两个候选的差值小于测量波动，就保留两者并扩大代表负载验证。最终交付应包括选择、被排除的方案、使结论翻转的阈值和实测范围。配套脚本可复算表中四组条件，枚举的是声明过的候选；它不是能覆盖任意模型与框架的全局最优规划器。 | **第五步，用测量修正估算，不把估算的排名当作结论。** 对排在前面的候选，用相同的请求、路由和质量条件做测量，记录峰值内存、各阶段的就绪与完成时刻、各端口的收发量和服务的尾延迟。若实际的矩阵效率、通信争用或排队与估算不同，换上实测参数后重新排序；若两个候选的差距小于测量波动，就都保留，再用更有代表性的负载验证。最后交付的应包括选定的方案、被排除的方案、会让结论翻转的阈值和实测范围。配套脚本可以复算表中的四组条件，但它只枚举声明过的候选，不是覆盖任意模型和框架的全局规划器。 |
| 在八卡小消息算例中，带宽翻倍只省约 10 μs | 在八卡、小数据量的算例中，带宽翻倍只省约 10 μs |
| 下一章将沿着本章分析的消息传输路径和依赖关系 | 下一章沿着本章分析的数据传输路径和依赖关系 |
| 求八卡部署下两种归约算法耗时相等时的消息大小 | 求八卡部署下两种归约算法耗时相等时的数据量 |
| 八卡 ring 按 0→1→…→7→0 排列 | 八张卡按 0→1→…→7→0 排成环 |

## 第二遍通读（2026-09-11）

共 14 处。

| 修改前 | 修改后 |
|---|---|
| 后一个算子等待前一个算子的输出，因此，依次执行的各段时间需要相加。 | 后一个算子要等前一个算子的输出，因此依次执行的各段时间要相加。 |
| 将上例从单 token 改为 8192-token prefill，矩阵运算量增长为约 2.47 TFLOPs | 把上例从单 token 改为 8192 个 token 的 prefill，矩阵运算量增长到约 2.47 TFLOPs |
| 训练在前向之后继续反向传播，并用梯度更新参数。训练还要计算输入梯度和权重梯度，前向激活要保留到反向计算用完。 | 训练在前向之后还要反向传播，计算输入梯度和权重梯度，再用梯度更新参数；前向激活要一直保留到反向用完为止。 |
| 这里采用第二章的 **SwiGLU** | 这里采用第 2 章介绍的 **SwiGLU** |
| 要理解为什么一种切法切完不需要通信、另一种却必须求和，先看一个最小的矩阵乘。 | 要理解为什么一种切法只需拼接、另一种却必须求和，先看一个最小的矩阵乘。 |
| 前一个阶段算完自己的层，把激活传给下一个阶段继续算。每个阶段只保存自己负责的层的权重，阶段之间只传递中间结果。 | 前一个阶段算完自己负责的层，把激活传给下一个阶段接着算。每个阶段只保存自己那些层的权重，阶段之间只传递中间结果。 |
| 送到 2 ms 那个阶段的输入比它处理得快，队列逐渐积压； | 送到耗时 2 ms 那个阶段的输入来得比它处理得快，队列逐渐积压； |
| 汇合分两步：先在每组的两张卡之间求和，得到本组专家的完整贡献；再在编号相同的卡之间汇合四个组。例如四个组对某个输出元素的贡献分别为 1、2、3、4， | 第二步的求和发生在编号相同的卡之间。例如四个组对某个输出元素的贡献分别为 1、2、3、4， |
| 下面将继续跟踪其中一块，看看它怎样沿环传递。 | 下一小节继续跟踪其中一块，看它怎样沿环传递。 |
| 若图 6-26 中的计算慢在与通信争用 SM，把 SM 让出来就能缩短计算 | 若图 6-27 中的计算慢在与通信争用 SM，把 SM 让出来就能缩短计算 |
| （a）复算图 6-26 两种配置的完成时间和相对差异 | （a）复算图 6-26 与图 6-27 两种配置的完成时间和相对差异 |
| 单看一次并不多，但 decode 每层要做两次归约，60 层就是 120 次；若每次都要等最远的卡，环面共多等 48 μs。对每 token 100 μs 的时间预算来说，这已接近一半。 | 单看一次并不多，但 decode 每层要做两次归约，Qwen3-8B 的 36 层就是 72 次；若每次都要等最远的卡，环面共多等约 29 μs。对每 token 100 μs 的时间预算来说，这已超过四分之一。 |
| 如果一个交换单元只能连接 64 个端点，第 65 张卡就可能要求增加一整套交换设备。卡数逐步增加时，配套交换设备的成本会在需要新增整套交换设备时突然上升。 | 如果一个交换单元只能连接 64 个端点，第 65 张卡就可能要求增加一整套交换设备。卡数逐步增加时，配套交换设备的成本不是平滑上升，而是在跨过这样的整组边界时突然跳升。 |
| 如何影响后续计算。 UB 的算例说明 | 如何影响后续计算。UB 的算例说明 |


## 全书通读（2026-09-11，按最终文本与通读前快照逐行对照）

共 29 行有改动；英文术语按“首次出现保留中文注释、之后用英文”的原则已回退，下表是回退之后的净改动。省略号表示只截取了改动附近的文字。

| 修改前 | 修改后 |
|---|---|
| …哪些输入、结果在哪里汇合，以及按什么顺序执行。本章把单卡算子的分块与调度扩展到多个加速器：先把矩阵和状态分到多张卡，推导由此产生的通信，再把通信放到实际连接上，最后决定… | …哪些输入、结果在哪里汇合，以及按什么顺序执行。本章把单卡算子的分块与调度扩展到多张卡：先把矩阵和状态分到多张卡，推导由此产生的通信，再把通信放到实际连接上，最后决定… |
| …“之”字形布局，但必须保留原始位置和因果掩码，并重新安排通信；远端上下文不能当作填充丢掉。 | …“之”字形布局，但必须保留原始位置和因果掩码，并重新安排通信；远端上下文不能当作 padding 丢掉。 |
| 这与第 5 章分块之间合并 softmax 的做法相同，只是统计量和向量要跨卡传输。不能各卡先分别… | 这与第 5 章 tile 之间合并 softmax 的做法相同，只是统计量和向量要跨卡传输。不能各卡先分别… |
| 分子是所有阶段实际工作的时间总和，分母是加速器数乘以处理全部微批次所需的时间。四阶段、四微批次的利用率为 $4/7$，约 57… | 分子是所有阶段实际工作的时间总和，分母是阶段数乘以处理全部微批次所需的时间。四阶段、四微批次的利用率为 $4/7$，约 57… |
| …**专家并行**（expert parallelism，EP）把不同专家放到不同加速器，让它们处理被分派来的输入。图 6-14 把八个专家分给两张卡。 | …**专家并行**（expert parallelism，EP）把不同专家放到不同的卡上，由各卡处理分派给自己的输入。图 6-14 把八个专家分给两张卡。 |
| …b）对 TP=1、2、4、8，计算贯穿算例的本地内存访问时间，暂不计通信开销；求加速器数每次翻倍所节省的时间。 | …b）对 TP=1、2、4、8，计算贯穿算例的本地内存访问时间，暂不计通信开销；求卡数每次翻倍所节省的时间。 |
| ![专家复用与加速器负载分布](ch06/figure-6-8-expert-load.svg) | ![专家复用与各卡负载分布](ch06/figure-6-8-expert-load.svg) |
| 图中后两种布局读取同样多的权重，完成时间却可能不同，因为参与计算的加速器数不同。因此，批内复用应与专家在加速器上的分布一起分析。 | 图中后两种布局读取同样多的权重，完成时间却可能不同，因为参与计算的卡数不同。因此，批内复用应与专家在各卡上的分布一起分析。 |
| …这些模型的存储需求已经超过八张 80 GB 卡的总容量。增加 EP 分组数，可以将专家分配到更多加速器；增加专家内部的 TP 卡数，可以减小每卡保存的专家矩阵；增加 PP 阶段数，则… | …这些模型的存储需求已经超过八张 80 GB 卡的总容量。增加 EP 分组数，可以把专家分到更多的卡上；增加专家内部的 TP 卡数，可以减小每卡保存的专家矩阵；增加 PP 阶段数，则… |
| > **实验 6-3 · 核心：专家分派与放置如何改变加速器负载和通信量** | > **实验 6-3 · 核心：专家分派与放置如何改变各卡负载和通信量** |
| …交给指定的一张卡，两者都不像表中的操作那样让全组持有结果。AllGather 按卡的编号拼接数据，不做求和；All-to-All 按目的地重新分配数据，也不会自动按路由权重合并专家输出。MoE 的合并因此包括结果回传、恢复 token 顺序和按路由权重求和三步，不能只凭名字把它当… | …交给指定的一张卡，两者都不像表中的操作那样让全组持有结果。AllGather 按 rank 拼接数据，不做求和；All-to-All 按目的地重新分配数据，也不会自动按路由权重合并专家输出。MoE 的 combine 因此包括结果回传、恢复 token 顺序和按路由权重求和三步，不能只凭名字把它当… |
| **环形 AllReduce** 将参与者排成一个有向环，每轮每卡向下一卡发送一块，并接收上一卡发来的块。设参与的卡数为 … | **环形 AllReduce** 把参与的卡排成一个有向环，每轮每卡向下一卡发送一块，并接收上一卡发来的块。设参与的卡数为 … |
| ![消息大小与环树选择](ch06/figure-6-11-collectives.svg) | ![数据量与环树算法的选择](ch06/figure-6-11-collectives.svg) |
| …算法耗时相等时的数据量；将 $\alpha$ 减半后重算，解释两种算法耗时相等时所需的消息大小为何变化。 | …算法耗时相等时的数据量；将 $\alpha$ 减半后重算，解释两种算法耗时相等时的数据量为何变化。 |
| …nk），再提交操作，说明缓冲区、元素数量、数据类型、归约运算和所在的 CUDA 流。组内各卡的调用必须按接口要求相互匹配，包括调用顺序和数据数量；调用返回通常只表示操作已提交到流中，不能据此复用仍在传输的缓冲区。使用结果的一方要靠同一条流的顺序或显式的事件依赖来等待。集合通信也不是一个可供任意主机代码使用的全局同步点。 | …nk），再提交操作，说明缓冲区、元素数量、数据类型、归约运算和所在的 CUDA stream。组内各卡的调用必须按接口要求相互匹配，包括调用顺序和数据数量；调用返回通常只表示操作已提交到 stream 中，不能据此复用仍在传输的缓冲区。使用结果的一方要靠同一 stream 的顺序或显式的 event 依赖来等待。集合通信也不是一个可供任意主机代码使用的全局同步点。 |
| …组，否则会串行等待对方尚未发起的操作。专门的专家并行通信库进一步结合路由信息实现派发和合并。无论选哪种实现，都应记录库的版本与具体接口，不能把固定大小的 All-to-A… | …组，否则会串行等待对方尚未发起的操作。专门的专家并行通信库进一步结合路由信息实现 dispatch 和 combine。无论选哪种实现，都应记录库的版本与具体接口，不能把固定大小的 All-to-A… |
| …CIe 三次，每次都是几百纳秒；一个 CPU 线程每秒能处理的请求也有限，大批小消息会在这里排队。**GPU 的 SM**：GPU 直接在自己的内存里构造请求项并敲… | …CIe 三次，每次都是几百纳秒；一个 CPU 线程每秒能处理的请求也有限，大批小请求会在这里排队。**GPU 的 SM**：GPU 直接在自己的内存里构造请求项并敲… |
| …个维度各接两个邻居，每一维首尾相连，这就是**三维环面**（torus）。环面是高性能计算的超级计算机长期使用的一类拓扑，TPU 沿用了它。另一条是让每张卡只接交换芯片，由… | …个维度各接两个邻居，每一维首尾相连，这就是**三维环面**（torus）。环面是超级计算机长期使用的一类拓扑，TPU 沿用了它。另一条是让每张卡只接交换芯片，由… |
| …交换机，比较下行／上行端口分别为 16／16 与 24／8 的两种分配，计算所有加速器同时向上发送时的平均可用带宽。 | …交换机，比较下行／上行端口分别为 16／16 与 24／8 的两种分配，计算所有卡同时向上发送时的平均可用带宽。 |
| 供电能力进一步限制可安装的加速器数量。设系统预算为 $P_b$，固定配套功率为 $P_0$，每张卡连同分摊的互联设备… | 供电能力进一步限制可以安装的卡数。设系统预算为 $P_b$，固定配套功率为 $P_0$，每张卡连同分摊的互联设备… |
| > （c）沿用（b）的功率预算和额外交换设备条件，假设加速器只能按八卡托盘整组增加，求最多可以安装多少张卡，并解释（a）至（c）的上限为何不… | > （c）沿用（b）的功率预算和额外交换设备条件，假设只能按八卡托盘整组增加，求最多可以安装多少张卡，并解释（a）至（c）的上限为何不… |
| ![加速器与二分链路的增长](ch06/figure-6-torus-growth.svg… | ![芯片数与二分链路的增长](ch06/figure-6-torus-growth.svg… |
| *图 6-36：k 从 4 增到 8 时，加速器数从 64 增到 512，二分链路从 32 增到 128。两种增长均以 k = … | *图 6-36：k 从 4 增到 8 时，芯片数从 64 增到 512，二分链路从 32 增到 128。两种增长均以 k = … |
| …混淆）维护报文序号、确认、重传和拥塞控制状态。多个应用端点访问同一远端时，可以共享适用的底层传输通道，而不用让每条应用关系都携带一份完整的可靠传输状态。图 6-37 展示了这一分离。传统 RDMA 的可靠连… | …混淆）维护报文序号、确认、重传和拥塞控制状态。多个应用端点访问同一远端时，可以共用底层的传输通道，不必让每条应用关系都各带一份完整的可靠传输状态。图 6-37 展示了这一分离。传统 RDMA 的可靠连… |
| …TP 的同步时间在单步耗时中占比上升。减少 TP 卡数能够省去一部分同步，腾出的加速器可组成更多独立实例；每张卡则要容纳更大份额的请求状态。前面的逐卡容量表与会话时间表… | …TP 的同步时间在单步耗时中占比上升。减少 TP 卡数能够省去一部分同步，腾出的卡可以组成更多独立实例；每张卡则要容纳更大份额的请求状态。前面的逐卡容量表与会话时间表… |
| 以上比较假定加速器一直正常运行。若一张卡发生故障，包含这张卡的实例会暂停，分配给该实例的后续会话也… | 以上比较假定各卡一直正常运行。若一张卡发生故障，包含这张卡的实例会暂停，分配给该实例的后续会话也… |
| …约 1.50 kJ；两个四卡实例运行约 82.7 ms，消耗约 1.79 kJ。使用相同加速器、保持相同功率时，越早完成任务，消耗的电能就越少。 | …约 1.50 kJ；两个四卡实例运行约 82.7 ms，消耗约 1.79 kJ。硬件相同、功率相同时，越早完成任务，消耗的电能就越少。 |
| \| 专家集合大、路由热点突出 \| EP、专家副本、专家分离 \| 派发与合并两个方向的流量、最忙的卡与专家池的队列 \| | \| 专家集合大、路由热点突出 \| EP、专家副本、专家分离 \| dispatch／combine 两个方向的流量、最忙的卡与专家池的队列 \| |
| …它保存哪些层、头、专家、激活和 KV；算峰值内存时还要加上集合通信缓冲、双缓冲、填充、在途微批次和副本迁移的空间。然后为每个算子求本地计算与访存时间，为每次跨卡操作… | …它保存哪些层、头、专家、激活和 KV；算峰值内存时还要加上集合通信缓冲、双缓冲、padding、在途微批次和副本迁移的空间。然后为每个算子求本地计算与访存时间，为每次跨卡操作… |

## 翻译腔审查（2026-09-11）

作者确认术语原则并统一“笔者”“N 个 token”“Queqiao”之后，再做一遍只针对翻译腔的审查（含图注里汉字数字改阿拉伯数字）。共 14 行有改动，按最终文本与审查前快照逐行对照。

| 修改前 | 修改后 |
|---|---|
| 使用更多卡通常由三种需求推动。模型权重超过单卡内存时，需要分散存储；一个请求执行过慢时，可以将计算分配给多张卡；同时到来的请求过多时，需要增加能够独立处理请求的实例。这三种需求分别对应容量、延迟和吞吐三个目标。 | 用更多的卡，通常出于三种需求：模型权重超过单卡内存，要分散存储；一个请求执行太慢，要把计算分给多张卡；同时到来的请求太多，要增加能独立处理请求的实例。这三种需求分别对应容量、延迟和吞吐三个目标。 |
| 当算子的计算与内存访问以流水方式重叠执行时，可以用下式估计执行时间： | 算子的计算与内存访问重叠执行时，执行时间可以用下式估计： |
| …色格是必须计算的查询—键配对；前四行归卡 0，后四行归卡 1。虚线只是设备边界，不能删除左下区域的远端依赖。* | …色格是必须计算的查询—键配对；前四行归卡 0，后四行归卡 1。虚线只是设备边界，左下区域的远端依赖并不因此消失。* |
| …后，每个组手里都有完整的专家输入，直接挑出本组专家需要的行即可，不必跨组派发输入，各组的结果最后通过归约汇合。 | …后，每个组手里都有完整的专家输入，直接挑出本组专家需要的行即可，不必跨组派发输入；各组的结果最后再归约汇合。 |
| …速度限制，prefill 的大矩阵运算则更容易受负载最重的专家组的计算时间限制。将热点专家分散到不同 EP 组，可以保留批内复用，又减轻单个专家组的计算负担。 | …速度限制，prefill 的大矩阵运算则更容易受负载最重的专家组的计算时间限制。把热点专家分散到不同的 EP 组，既保留了批内复用，又减轻了单个专家组的计算负担。 |
| …式约为 568 GB。八张 80 GB 卡共有 640 GB，留给状态和工作区的剩余空间已经较少。DeepSeek V4-Flash 每层有 256 个路由专家，每个 toke… | …式约为 568 GB。八张 80 GB 卡共有 640 GB，留给状态和工作区的空间已经不多。DeepSeek V4-Flash 每层有 256 个路由专家，每个 toke… |
| …为单步执行时间。本地项包括权重与 KV 读取，归约项是卡间集合通信，其余串行项不能由增加 TP 卡数消除。* | …为单步执行时间。本地项包括权重与 KV 读取，归约项是卡间集合通信，其余串行项不会因为增加 TP 卡数而减少。* |
| 端口、距离和功率共同限制了系统规模。NVIDIA 的多代系统展示了这些条件下，互联如何从机箱内直连发展到机柜级交换网络。DGX-1 用八张 GPU 组成 NVLink 混合立方体网格，部分 GPU 直接相连，其他 GPU 之间的通信需要中转。此时将频繁通信的计算任务分配到相邻 GPU，就能减少中转与共享链路压力。 | 端口、距离和功率共同限制了系统规模。NVIDIA 的几代系统展示了在这些条件下，互联怎样从机箱内直连发展到机柜级交换网络。DGX-1 用八张 GPU 组成 NVLink 混合立方体网格，部分 GPU 直接相连，其他 GPU 之间的通信需要中转。把频繁通信的计算任务放到相邻的 GPU 上，就能减少中转和共享链路的压力。 |
| 机柜级互联使更多 GPU 能够参与同一个实例。对于单卡即可容纳的小模型，可以根据延迟和吞吐要求选择实例数；对于权重已超过八卡总容量的大模型，则可以使用更多 GPU 保存权重，并通过机柜内互联完成各层通信。 | 机柜级互联让更多 GPU 能参与同一个实例。单卡放得下的小模型，可以按延迟和吞吐要求选择实例数；权重超过八卡总容量的大模型，则可以用更多 GPU 保存权重，各层的通信走机柜内互联。 |
| …借用期间，使用权交给借用者，原持有者等待归还后再使用；共享访问则允许多个使用者按相应的一致性与同步规则访问。[^ub] | …借用期间，使用权交给借用者，原持有者等待归还后再使用；共享访问则允许多个使用者按约定的一致性与同步规则访问。[^ub] |
| …能访问”，操作完成与内存释放的顺序回答“什么时候可以使用”。地址、权限和访问顺序共同保证远端内存能够被正确使用。 | …能访问”，操作完成与内存释放的顺序回答“什么时候可以使用”。地址、权限和访问顺序三者合在一起，远端内存才能用得正确。 |
| …提供的读取带宽。能否用远端内存替代本地内存，还要把这一带宽与应用的读取需求比较。对于同样的 16 GiB 远端数据，读取频率决定所需的平均带宽。每分钟完整读取一次，平均约… | …提供的读取带宽。能否用远端内存替代本地内存，还要把这一带宽与应用的读取需求比较。同样是 16 GiB 远端数据，读取频率决定所需的平均带宽。每分钟完整读取一次，平均约… |
| … 本地内存空间，节省了约 3.7 s 的访问时间。因此，对需要反复读取的数据，还应比较远端读取与本地缓存的时间，决定何时将数据取回本地。 | … 本地内存空间，节省了约 3.7 s 的访问时间。因此，对需要反复读取的数据，还要比较远端读取与本地缓存的时间，决定什么时候把数据取回本地。 |
| 这里同时用到了两种并行：模型内部的并行计算，和不同请求之间的并发执行。层内矩阵可以切给多张卡，独立会话可以分给多个实例，两者… | 这里同时用到了两种并行：模型内部的并行计算和不同请求之间的并发执行。层内矩阵可以切给多张卡，独立会话可以分给多个实例，两者… |
