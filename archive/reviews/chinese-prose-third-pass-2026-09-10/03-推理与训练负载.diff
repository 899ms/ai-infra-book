--- before/03-推理与训练负载.md
+++ manuscripts/03-推理与训练负载.md
@@ -4,7 +4,7 @@
 
 两种服务的平均请求率和平均输入、输出长度可以完全相同：一种始终均匀接收请求，另一种前一分钟集中处理长文档，后一分钟集中生成长回答。前者各阶段的资源需求较为稳定，后者则在不同时段分别需要更多输入处理能力或生成能力。智能体（Agent）是通过模型决定下一步操作、调用工具并根据结果继续工作的程序，它在模型与工具之间往返，GPU 暂停生成时，上下文状态仍需保留，供后续调用使用。训练中的序列长度、有效标签和更新次数，也会影响运算量和状态需要保存的时间。
 
-第二章给出了单次调用的容量、运算量和读取量。本章再加入三个问题：工作何时到达，哪些步骤必须等待，什么结果才算任务完成。由此依次建立排队、状态寿命、关键路径和成功任务成本的模型，再综合计算训练与长期服务的成本。
+第二章给出了单次调用的容量、运算量和读取量。本章再加入三个问题：工作何时到达，哪些步骤必须等待，什么结果才算任务完成。由此依次建立排队、状态保存时间、关键路径和成功任务成本的模型，再综合计算训练和长期服务的成本。
 
 ## 3.1 从单次请求到持续到达的负载
 
@@ -35,7 +35,7 @@
 \overline T_{\mathrm{token}}=\frac{t_G-t_1}{G-1}\quad(G>1).
 $$
 
-TTFT 是 time to first token，即从请求到达到首个 token 返回的时间。首响应衡量多久开始输出，完整请求延迟衡量多久生成完，平均 token 间隔衡量输出节奏。把某一段时间缩短，只会直接改变包含这段时间的指标。例如，减少请求开始前的排队会改善 TTFT，却不会自动缩短已经开始生成后的 token 间隔。
+TTFT 是 time to first token，即从请求到达到首个 token 返回的时间。首响应衡量多久开始输出，完整请求延迟衡量多久生成完，平均 token 间隔衡量输出节奏。把某一段时间缩短，只会直接改变包含这段时间的指标。例如，减少请求开始执行前的排队会改善 TTFT，却不会自动缩短已经开始生成后的 token 间隔。
 
 ![请求到达、首输出与末输出决定三个计时区间。首响应包含开始生成前的等待，输出间隔描述生成过程，完整请求时间从到达累计到结束。](ch03/figure-3-request-clocks.svg)
 
@@ -52,9 +52,9 @@
 | 多久把事情做完？ | 完整请求时间、完整任务时间 |
 | 每秒完成多少合格任务？ | 在质量与时限要求下的有效吞吐 |
 | 得到一个成功结果花多少？ | 全部尝试的模型、工具和环境成本／成功任务数 |
-| 等待期间还占着什么？ | 状态所属的请求与模型版本、大小、驻留位置和寿命 |
-
-p95 是第 95 百分位数，用来描述所选指标中较慢的一部分观测。本章采用最近秩法：把 $n$ 个观测从小到大排列，取第 $\lceil0.95n\rceil$ 个。四条请求的 p95 是最大值，480 条请求的 p95 是第 456 个。将请求完成、排队与首响应的耗时分别排序，就能看出长等待发生在哪一段；全程 p95 则直接对每条请求的全程时间排序得到。
+| 等待期间还占着什么？ | 状态所属的请求与模型版本、大小、存放位置和保存时间 |
+
+p95 是第 95 百分位数，用来描述所选指标中较慢的一部分观测。本章采用最近秩法：把 $n$ 个观测从小到大排列，取第 $\lceil0.95n\rceil$ 个。四条请求的 p95 是最大值，480 条请求的 p95 是第 456 个。将请求完成、排队与首响应的耗时分别排序，就能看出长等待发生在哪一段；完整请求的 p95 则直接由各请求的全程耗时排序得到。
 
 状态保存得越久，同样的容量能同时服务的任务就越少。令任务状态大小为 $M(t)$，状态占用空间对时间的积分为
 
@@ -64,9 +64,9 @@
 
 将各时间段占用的空间乘以持续时间再相加，就得到占用空间随时间变化的曲线下面积。它的单位是 byte·s：1 GiB 状态持续占用空间 10 秒，对应 10 GiB·s。状态没有变大，但这段时间内无法将空间用于其他请求。若任务以平均每秒 $\lambda$ 个的速度持续到达，每个任务都占用 $M_0$ 字节、平均等待 $\tau$ 秒，稳定情况下仅这些等待任务的平均占用就约为 $\lambda M_0\tau$。工具等待由此也会成为容量问题。
 
-![1 GiB 状态持续占用空间十秒，对应面积为 10 GiB·s。横轴为状态存活时间，纵轴为占用空间；面积描述这段等待期间的累计存储占用。](ch03/figure-3-state-time-area.svg)
-
-*图 3-3　1 GiB 状态持续占用空间十秒，对应面积为 10 GiB·s。横轴为状态存活时间，纵轴为占用空间；面积描述这段等待期间的累计存储占用。*
+![1 GiB 状态持续占用空间十秒，对应面积为 10 GiB·s。横轴为状态保存时间，纵轴为占用空间；面积描述这段等待期间的累计存储占用。](ch03/figure-3-state-time-area.svg)
+
+*图 3-3　1 GiB 状态持续占用空间十秒，对应面积为 10 GiB·s。横轴为状态保存时间，纵轴为占用空间；面积描述这段等待期间的累计存储占用。*
 
 成功任务成本把所有尝试的成本相加，再除以成功任务数。这样，失败生成、工具测试和环境运行都进入分子，分母对应用户得到的合格结果。一个批次零成功时，记录其总消耗与失败原因；取得成功结果后，再用全部尝试的总成本除以成功任务数。[^cost]
 
@@ -80,7 +80,7 @@
 
 **例题 3-1：平均处理能力足够，为什么仍会积压？** 判断每秒可完成 6144 个后续 decode 步的资源，能否持续处理章首的负载。
 
-解： 定义两类请求：长输入类输入 8192、输出 256 个 token；长输出类输入 1024、输出 2048 个 token。两分钟内各到达 240 条，总计平均每秒四条。一种负载中，两类请求始终各占一半；另一种前一分钟两类占比为 $9:1$，后一分钟为 $1:9$。每条请求的首输出由 prefill 产生，后续 decode 步数为输出数减一，于是有：
+解： 定义两类请求：长输入类输入 8192、输出 256 个 token；长输出类输入 1024、输出 2048 个 token。两分钟内各到达 240 条，合计平均每秒四条。一种负载中，两类请求始终各占一半；另一种前一分钟两类占比为 $9:1$，后一分钟为 $1:9$。每条请求的首输出由 prefill 产生，后续 decode 步数为输出数减一，于是有：
 
 | 时间窗 | 新输入 token/s | 后续 decode 步/s |
 | --- | ---: | ---: |
@@ -134,11 +134,11 @@
 
 请求进入执行还需要足够的状态空间。状态池有余量时，新请求可以较早开始；状态池填满后，即使部分计算单元空闲，也要等待已有请求释放空间。排队因此既可能来自处理速度不足，也可能来自已有请求的状态尚未释放。[^serve-replay]
 
-这个递推还能直接预测新的突发条件。若工作到达速率 $\lambda_w$ 连续 $\tau$ 秒高于处理率 $\mu$，初始队列为空，则积压为 $(\lambda_w-\mu)\tau$；停止到达后的排空时间为 $(\lambda_w-\mu)\tau/\mu$。突发持续时间加倍，积压与排空时间都加倍。平均值相同并不能抵消这段已经发生的等待。
+这个递推式还能预测其他突发条件下的积压。若工作到达速率 $\lambda_w$ 连续 $\tau$ 秒高于处理率 $\mu$，初始队列为空，则积压为 $(\lambda_w-\mu)\tau$；停止到达后的排空时间为 $(\lambda_w-\mu)\tau/\mu$。突发持续时间加倍，积压与排空时间都加倍。平均值相同并不能抵消这段已经发生的等待。
 
 > **练习 3-2〔核心〕：突发请求与工具等待如何增加积压和状态占用**
 >
-> 采用例题 3-1 后一分钟的负载强度，即每秒新增 7471.2 步 decode 工作。将这一突发负载的持续时间改为 30 秒，分别考虑处理率为 6000、8000 步/秒的情况。初始队列为空，求积压与停止到达后的排空时间。随后考虑另一种负载：每秒有两个任务进入工具等待阶段，每个任务等待 10 秒，期间状态占用 1 GiB 空间。求稳定状态下的平均等待任务数与状态占用；将等待延长到 30 秒后重新计算。
+> 采用例题 3-1 中第二分钟的负载强度，即每秒新增 7471.2 步 decode 工作。将这一突发负载的持续时间改为 30 秒，分别考虑处理率为 6000、8000 步/秒的情况。初始队列为空，求这 30 秒内积压量随时间的变化，以及请求停止到达后清空积压所需的时间。随后考虑另一种负载：每秒有两个任务进入工具等待阶段，每个任务等待 10 秒，期间状态占用 1 GiB 空间。求稳定状态下的平均等待任务数与状态占用；将等待延长到 30 秒后重新计算。
 
 ## 3.2 多轮交互与 Agent 任务
 
@@ -192,7 +192,7 @@
 
 ### 3.2.3 智能体工作负载
 
-反馈还可以改变后续计算。代码 Agent 先生成修改方案，工具写入代码并运行测试，模型再读取测试结果决定下一步。每个循环既有模型计算，也有工具执行；工具返回的日志又成为下一轮输入。因此，减少无关日志能降低下一轮的 prefill 计算量和上下文状态占用，减少无效修改则能直接减少循环次数。
+反馈还可以改变后续的计算过程。代码 Agent 先生成修改方案，工具写入代码并运行测试，模型再读取测试结果决定下一步。每个循环既有模型计算，也有工具执行；工具返回的日志又成为下一轮输入。因此，减少无关日志能降低下一轮的 prefill 计算量和上下文状态占用，减少无效修改则能直接减少循环次数。
 
 本章的实测任务是修复一个区间合并函数，检查返回结果是否正确、输入是否保持不变。模型负责决定修改并调用工具，工具负责写入文件和运行测试。下面按四轮成功完成原定检查的轨迹，分开统计模型时间、工具时间和等待期间保存的状态。工具调用明细见章末[代码任务轨迹](#agent-trace-detail)。[^agent]
 
@@ -221,7 +221,7 @@
 
 **例题 3-2：加速首轮模型计算能缩短多少代码任务时间？** 用第一章的 Amdahl 分析，假设后续行为与质量不变。
 
-解： 保持同一条执行轨迹，只改变一个条件：只把首轮耗时 36.375 秒的模型计算缩短一半，其余阶段、输出、工具行为和质量都不变，总时间便从 76.510 秒降到 58.323 秒。此时 $f=36.375/76.510$，$s=2$，总加速比约为 1.31；首轮速度提高到两倍的收益受其原有时间占比限制。首轮占总时间约 47.5%，将这一段减半节省约 23.8% 的总时间；其余阶段仍合计耗时约 40.1 秒。优化收益由被缩短阶段的原有占比决定。[^agent-speedup]
+解： 以同一条执行轨迹为基准，将首轮耗时 36.375 秒的模型计算缩短一半，其余阶段、输出、工具行为和质量都不变，总时间便从 76.510 秒降到 58.323 秒。此时 $f=36.375/76.510$，$s=2$，总加速比约为 1.31；首轮速度提高到两倍的收益受其原有时间占比限制。首轮占总时间约 47.5%，将这一段减半节省约 23.8% 的总时间；其余阶段仍合计耗时约 40.1 秒。优化收益由被缩短阶段的原有占比决定。[^agent-speedup]
 
 设模型先计算 2 秒，随后调用两个工具，耗时分别为 6 秒和 10 秒，最后再计算 3 秒。若第二个工具依赖第一个的结果，总时间为 $2+6+10+3=21$ 秒；若工具相互独立，总时间为 $2+\max(6,10)+3=15$ 秒。并行减少了 6 秒，但两个工具的总执行工作仍为 16 秒。
 
@@ -251,7 +251,7 @@
 
 先比较专家矩阵计算这部分工作，以各 token 经过的专家层数之和衡量工作量。设一次从空状态开始的输入为 $P$ 个 token，则普通 40 层路径为 $40P$，论文 CED 调度为 $20P+20\min(P,128)$。当 $P=8192$ 时，两者为 327,680 与 166,400，后者约为前者的 50.8%；当 $P\leq128$ 时，这个子项没有减少。
 
-已有前缀时，输入工作由追加长度与恢复路径共同决定。若编码器 SWA 命中缓存，编码器主体便可以继续处理追加的输入；编码器 SWA 缺失，则先重放前缀末尾窗口。两条路径随后都要构建本轮的解码器 SWA。第八章将用 8K 与 128K 上下文的教学算例，比较保存状态和重新执行的成本。
+已有前缀时，输入处理的工作量由追加长度与恢复方式共同决定。若编码器 SWA 命中缓存，编码器主体便可以继续处理追加的输入；编码器 SWA 缺失，则先重放前缀末尾窗口。两条路径随后都要构建本轮的解码器 SWA。第八章将用 8K 与 128K 上下文的教学算例，比较保存状态和重新执行的成本。
 
 ![同一批 8K 输入中，各 token 经过的专家层数之和。普通全层路径执行 40 层；CED 路径执行 20 层编码器，并为最近 128 个 token 重放 20 层解码器。灰色说明项仍需另外计算；生成阶段执行完整主干。](ch03/figure-3-v41-ced.svg)
 
@@ -297,7 +297,7 @@
 
 三种数据表示对应三个不同的交接位置。发送原图，接收端还要执行视觉编码；发送 EC，接收端从语言 prefill 开始；发送 KV，则交接已经完成的语言上下文。第八章安排同卡执行，第九章比较阶段交接，第十二章把相同数据流放到端、边、云链路上。
 
-保持 patch 与合并方式不变，将图片两条边都加倍，位置数变为四倍。因此 EC 和视觉位置的语言 KV 都变为四倍；视觉编码内部的位置交互则还要按注意力结构计算。特征拼接增加的是宽度，图片变大增加的是位置数，二者在语言主干中产生不同后果。
+保持 patch 与合并方式不变，将图片的高度和宽度都加倍，位置数变为四倍。因此 EC 和视觉位置的语言 KV 都变为四倍；视觉编码内部的位置交互则还要按注意力结构计算。特征拼接增加的是宽度，图片变大增加的是位置数，二者在语言主干中产生不同后果。
 
 ### 3.3.2 音频与图像生成的计算过程
 
@@ -311,13 +311,13 @@
 
 ### 3.3.3 连续感知、首段响应与打断
 
-工作总量决定平均处理需求，实时播放还要求每块数据按时到达。令 $A(t)$ 表示截至时刻 $t$ 已到达的可播放音频时长，$P(t)$ 表示已播放的音频时长，缓冲中剩余音频为
+工作总量决定平均处理需求，实时播放还要求每块数据都按时到达。令 $A(t)$ 表示截至时刻 $t$ 已到达的可播放音频时长，$P(t)$ 表示已播放的音频时长，缓冲中剩余音频为
 
 $$
 B_{\mathrm{audio}}(t)=A(t)-P(t).
 $$
 
-播放开始后，每经过一秒，已播放音频时长 $P(t)$ 就增加一秒，$A(t)$ 则随数据块到达跳跃增加。两条曲线之间的距离就是缓冲余量；余量降到零而下一块尚未到达，声音就会中断。预缓冲增加初始余量，同时推迟开始播放。
+连续播放期间，每经过一秒，已播放音频时长 $P(t)$ 就增加一秒，$A(t)$ 则随数据块到达跳跃增加。两条曲线之间的距离就是缓冲余量；余量降到零而下一块尚未到达，声音就会中断。预缓冲增加初始余量，同时推迟开始播放。
 
 时间要求同时作用于输入和输出。先看输入端：截图 Agent 如果只在动作结束后观察屏幕，就可能漏掉中途出现又消失的弹窗。AOI 是研究持续观察与交互的工作，它将屏幕观测与动作执行分开：持续收集图像、音频和事件，再筛选出供模型使用的记录。增加观测频率可以发现更多短暂事件，但保存更多关键帧也会占用上下文；过多关键帧还可能挤占重要信息，使任务效果下降。[^aoi] 因此，持续观测还要配合筛选，决定哪些内容值得保留并交给模型处理。
 
@@ -331,7 +331,7 @@
 24{,}000\times0.020\times1\times2=960\ \mathrm{bytes}.
 $$
 
-每块采集结束后，模型处理 12 ms、发送 1 ms，通常再传播 5 ms。第一块在 38 ms 到达，在抖动缓冲区中等待 40 ms 后，于采集开始后的第 78 ms 播放。第三块的传播延迟被设为 50 ms，直到 123 ms 才到，错过 118 ms 的播放计划，造成 5 ms 新增停顿。后续块虽已到达，也要按声音顺序等待。
+每块采集结束后，模型处理耗时 12 ms、发送耗时 1 ms，通常再传播 5 ms。第一块在 38 ms 到达，在抖动缓冲区中等待 40 ms 后，于采集开始后的第 78 ms 播放。第三块的传播延迟被设为 50 ms，直到 123 ms 才到，错过 118 ms 的播放计划，造成额外 5 ms 的停顿。后续块虽已到达，也要按声音顺序等待。
 
 将缓冲改为 60 ms，这次停顿消失，首播却推迟到 98 ms。增加缓冲可以避免这次到达时间波动造成播放中断，代价是更晚开始；若音频的平均到达速度长期低于播放速度，有限缓冲终究会耗尽。[^audio]
 
@@ -353,7 +353,7 @@
 
 > **练习 3-5〔延伸〕：图像分辨率与音频缓冲如何影响资源需求**
 >
-> 将 Qwen3-VL-4B 图片从 $640\times640$ 改为 $1280\times1280$，保持图像块大小、合并方式和特征精度不变，求视觉位置数、完整编码缓存 EC 的大小，以及这些视觉位置对应的 KV 大小。再根据本节音频时间线，比较初始缓冲为 40 ms 和 60 ms 时的首次播放时刻与停顿情况。如果音频长期每秒只到达 0.9 秒可播放内容，初始缓冲为 0.3 秒，求连续播放多久后耗尽，并解释增加有限缓冲能解决什么问题。
+> 将 Qwen3-VL-4B 图片从 $640\times640$ 改为 $1280\times1280$，保持图像块大小、合并方式和特征精度不变，求视觉位置数、完整编码缓存 EC 的大小，以及这些视觉位置对应的 KV 大小。再根据本节音频时间线，比较初始缓冲为 40 ms 和 60 ms 时的首次播放时刻与停顿情况。如果每秒收到的音频只能播放 0.9 秒，开始播放时已缓存 0.3 秒音频，求连续播放多久后会耗尽缓冲，并解释增加有限缓冲能解决什么问题。
 
 ## 3.4 训练的计算与状态需求
 
@@ -379,17 +379,17 @@
 
 这一推导也说明怎样处理结构变化。冻结某个权重矩阵后，可省去它的权重梯度计算；若更早的层仍需要梯度，则仍要计算输入梯度。MoE 按选中专家累计两类梯度，注意力则按位置对累计。沿反向依赖逐项累加，就得到各训练方法需要执行的计算量。
 
-反向传播需要使用前向计算留下的信息。例如，计算 $\mathrm dW$ 要使用 $X$，对非线性运算求梯度也需要相应的中间结果。参数梯度在反向传播中产生，再由优化器用来计算参数更新量。优化器的状态记录多次更新积累的信息，因此需要保留到下一次更新。它们的寿命不同：某层激活在该层反向结束后可以释放，优化器动量通常一直留到下一次更新。前向产生的激活要等待相应反向才能释放，因此反向传播把中间数据的寿命从相邻算子之间延长到了前后两次遍历之间。
-
-![一层激活从前向完成后保留到相应反向用完。横轴按事件排列，间距表示过程顺序；反向需要的前向输入决定这段存活区间。](ch03/figure-3-activation-lifetime.svg)
-
-*图 3-20　一层激活从前向完成后保留到相应反向用完。横轴按事件排列，间距表示过程顺序；反向需要的前向输入决定这段存活区间。*
+反向传播需要使用前向计算留下的信息。例如，计算 $\mathrm dW$ 要使用 $X$，对非线性运算求梯度也需要相应的中间结果。参数梯度在反向传播中产生，再由优化器用来计算参数更新量。优化器的状态记录多次更新积累的信息，因此需要保留到下一次更新。不同数据的保留期限取决于最后一次使用它的操作：某层激活通常在该层反向计算用完后释放，优化器动量则需要跨越多次更新持续保留。
+
+![一层激活从前向完成后保留到相应反向用完。横轴按事件排列，间距表示过程顺序；反向需要的前向输入决定这段保存时间。](ch03/figure-3-activation-lifetime.svg)
+
+*图 3-20　一层激活从前向完成后保留到相应反向用完。横轴按事件排列，间距表示过程顺序；反向需要的前向输入决定这段保存时间。*
 
 **例题 3-3：梯度与优化器状态为何使训练内存需求成倍增加？**
 
 解： 以 Qwen3-8B 的 8,190,735,360 个参数为例，保存 BF16 权重、FP32 梯度、一份独立的 FP32 主权重（master weight），以及 Adam 的两个 FP32 矩估计。Adam 是根据梯度及梯度平方的移动平均调节更新幅度的优化器，两个矩估计分别保存这两种平均量。不做分片时，每参数需要 $2+4+4+4+4=18$ bytes，共 147.433 GB，约 137.308 GiB。BF16 权重用于前向和反向计算，梯度用于计算更新量，FP32 主权重用于高精度更新，Adam 的两个矩估计则累计梯度及其平方的统计量。这些数据合计占用的空间，是推理 BF16 权重的九倍。[^train-matrix]
 
-微批次是一次前向和反向实际处理的数据子集；完整训练批可以由多个微批次组成。一个微批次与一次参数更新也要区分。显存放不下完整训练批时，可以分多个微批次累计梯度，最后再更新一次参数。保持目标、有效标签和归一化一致时，将同一训练批拆成更多微批次，会改变执行顺序与激活寿命，而总训练数据量保持不变。第十章将据此讨论分片和流水。
+微批次是一次前向和反向实际处理的数据子集；完整训练批可以由多个微批次组成。一个微批次与一次参数更新也要区分。显存放不下完整训练批时，可以分多个微批次累计梯度，最后再更新一次参数。保持目标、有效标签和归一化一致时，将同一训练批拆成更多微批次，会改变执行顺序与激活的保存时间，而总训练数据量保持不变。第十章将据此讨论分片和流水。
 
 ![训练沿前向依赖计算输出，再沿反向依赖传递梯度。当前层既向前层传输入梯度，也计算自己的权重梯度，供优化器更新。](ch03/figure-3-5-training.svg)
 
@@ -407,7 +407,7 @@
 
 Qwen3 的训练过程体现了这些变化：通用阶段使用长度为 4096 的序列训练超过 30 万亿 token，随后增加科学、技术、工程与数学（STEM）、代码、推理与合成数据，再训练约 5 万亿 token；长上下文阶段将长度扩至 32768。[^qwen] 数据组成改变学习内容，序列长度还直接改变每个位置可以访问多少前文，因此需要分别观察。
 
-系统需求随长度改变。假设两组独立文档都含 8,192 个 token：一组为 $4096+4096$，另一组为 $7168+1024$。参数投影的行数相同，因果注意力的有效位置对却分别为 16,781,312 和 26,218,496。在 Qwen3-8B 一层上，$QK^{\mathsf T}$ 与 $AV$ 前向工作约从 0.275 增到 0.430 TFLOPs，增加约 56%。较长文档中的位置可以看到更多前文，所以在总行数不变时，上下文交互增加；投影与 FFN 仍处理同样的 8192 个 token 表示。整层增加量由这笔新增交互占原有工作的比例决定。[^training]
+系统需求随长度改变。假设两组独立文档都含 8,192 个 token：一组为 $4096+4096$，另一组为 $7168+1024$。参数投影的行数相同，因果注意力的有效位置对却分别为 16,781,312 和 26,218,496。在 Qwen3-8B 一层上，$QK^{\mathsf T}$ 与 $AV$ 前向工作约从 0.275 增到 0.430 TFLOPs，增加约 56%。较长文档中的位置可以看到更多前文，所以在总行数不变时，上下文交互增加；投影与 FFN 仍处理同样的 8192 个 token 表示。整层运算量的增幅取决于这部分新增交互占原有工作量的比例。[^training]
 
 SFT 中，模型读取的位置与参与损失计算的位置往往不同。一条样本可以包含系统提示、用户问题、工具结果和回答，但训练只对回答部分计算损失。其余位置仍要经过主干网络，为回答提供上下文。实现还可以只对受监督位置计算词表投影；如果只在计算损失时屏蔽无监督位置，却没有跳过这些位置的词表投影，之前的矩阵乘法仍会照常执行。
 
@@ -443,9 +443,9 @@
 
 因此，序列长度变化时可以先判断各项开销的变化趋势：参数投影随输入行数增长，注意力随位置对数增长，优化器更新随参数量和更新次数增长。若通过重计算减少保存的激活，容量下降，但被重做的前向运算还要加回 $F_{\mathrm{train}}$。这些变化都能沿同一张前向—反向图逐项解释。
 
-> **练习 3-6〔延伸〕：计算一次参数更新的运算量、状态与监督规模**
->
-> 从 $Y=XW$ 推导输入梯度和权重梯度的尺寸，说明三次矩阵乘为何有相同运算量。对 Qwen3-8B，按每参数 18 字节计算长期训练状态的总容量，再将梯度改为 BF16，求节省的容量。设每个微批次含 2 条序列，每条有 4096 个输入位置，其中 1024 个位置参与监督。累计 8 个微批次后更新一次参数，求每次更新处理的输入 token 总数和有效监督 token 总数。解释仅屏蔽无监督位置的损失，与只对受监督位置执行词表投影，在计算量上有何区别。
+> **练习 3-6〔延伸〕：一次参数更新需要多少计算、训练状态和监督 token**
+>
+> 从 $Y=XW$ 推导输入梯度和权重梯度的尺寸，说明三次矩阵乘法为何具有相同的运算量。对 Qwen3-8B，按每参数 18 字节计算长期训练状态的总容量，再将梯度改为 BF16，求节省的容量。设每个微批次含 2 条序列，每条有 4096 个输入位置，其中 1024 个位置参与监督。累计 8 个微批次后更新一次参数，求每次更新处理的输入 token 总数和有效监督 token 总数。解释仅屏蔽无监督位置的损失，与只对受监督位置执行词表投影，在计算量上有何区别。
 
 ## 3.5 强化学习（RL）
 
@@ -516,13 +516,13 @@
 
 *图 3-25　保持十六条有效样本目标，生成数由三十二增到六十四。生成时的输入处理、后续解码和参考模型评分随回答总数增加，策略更新处理的样本数保持相同。*
 
-更新完成后，生成副本需要获得新权重。设有 $r$ 个副本，逐个独立发送大小为 $M_W$ 的完整权重，则传输量为 $rM_W$。Qwen3-8B BF16 的一份权重约为 16.381 GB，四个副本共需约 65.526 GB。优化器状态留在学习器，用于下一次更新；生成副本只需要执行前向所需的权重。广播或分层分发则通过复用中间传输减少重复发送。
+更新完成后，生成副本需要获得新权重。设有 $r$ 个副本，逐个独立发送大小为 $M_W$ 的完整权重，则传输量为 $rM_W$。Qwen3-8B BF16 的一份权重约为 16.381 GB，四个副本共需约 65.526 GB。优化器状态留在学习器，用于下一次更新；生成副本只需要执行前向所需的权重。广播或分层分发则可以让多个副本共用部分传输路径，减少重复发送。
 
 为各阶段分配资源时，既要看运算量，也要看计算方式。生成阶段的 decode 每步通常只处理各请求的一个新 token，在小批次下同时处理的 token 很少，评分和训练则能一起处理已知回答中的多个位置，工具验证还需要 CPU 和运行环境。分别估算各阶段的耗时，再按生成、反馈、更新和权重同步的顺序排列，才能得到完整周期。第四至第十章将逐步介绍相应硬件和调度方法。
 
-> **练习 3-7〔核心〕：固定更新目标下的 RL 成本**
->
-> 保持一次更新使用 16 条等长样本。利用例题 3-4 中分别生成 32 条和 64 条回答时的周期总运算量，求每多生成一条回答所增加的生成与参考模型运算量，以及一次更新所需的固定运算量。据此预测生成 48 条回答时的周期总运算量。再用结构相同的教师模型对全部 48 条回答各执行一次前向，求新增的运算量。若将更新后的 BF16 权重分别发送给四个副本，每个副本独立接收一份，求总传输量。最后画出生成、反馈、更新与同步的依赖，指出哪些阶段可以在不同批次之间重叠。
+> **练习 3-7〔核心〕：更新样本数不变时，多生成回答会增加多少 RL 开销**
+>
+> 保持一次更新使用 16 条等长样本。利用例题 3-4 中分别生成 32 条和 64 条回答时的周期总运算量，求每多生成一条回答所增加的生成与参考模型运算量，以及一次更新所需的固定运算量。保持更新样本数不变，据此预测生成 48 条回答时的周期总运算量。再用结构相同的教师模型对全部 48 条回答各执行一次前向，求新增的运算量。若将更新后的 BF16 权重分别发送给四个副本，每个副本独立接收一份，求总传输量。最后画出生成、反馈、更新与同步的依赖，指出哪些阶段可以在不同批次之间重叠。
 
 ## 3.6 从负载需求到训练与服务预算
 
@@ -570,7 +570,7 @@
 
 训练本身也存在波动。同样的数据与模型规模，改变随机初始化就可能得到不同的学习曲线；较大模型在训练尚未充分时，也未必优于较小模型。使用多个随机种子重复训练，可以区分规模变化带来的趋势与单次训练的偶然波动。[^local-train]
 
-长期服务会把每次调用的成本累积到训练预算之上。较小模型可以通过更长训练达到目标质量，前期多付成本，之后每次调用读取更少权重、执行更少工作。Beyond Chinchilla-Optimal 将这种推理需求纳入分析，47 个模型覆盖 150M–6B；其中 150M 模型测试到 10,000 token／参数，较大模型测试到 1,000。[^beyond] 训练预算与服务次数共同决定较小模型何时能收回额外训练的成本。
+长期服务会把每次调用的成本累积到训练预算之上。较小模型可以通过更长训练达到目标质量，前期多付成本，之后每次调用读取更少权重、执行更少工作。Beyond Chinchilla-Optimal 将这种推理需求纳入分析，47 个模型覆盖 150M–6B；其中 150M 模型的训练量最高达到每个参数 10,000 个 token，较大模型最高达到 1,000 个。[^beyond] 训练预算与服务次数共同决定较小模型何时能收回额外训练的成本。
 
 **固定目标损失下的训练投入与累计服务成本。** 固定拟合目标损失为 2.9，以矩阵运算量和给定单价估算训练与服务成本：请求取 $P=512,G=128$，训练运算量为 $6ND$，每个完整请求的运算量为 $2N[P+(G-1)]$，每 FLOP 按 $10^{-18}$ 个成本单位计价。0.1B 模型需要约 298.6B 训练 token，超过拟合所用数据量的上界，因此成本图用虚线标注这条外推曲线。按这条外推曲线计算，调用约 2.048 亿次时，0.1B 与 0.5B 模型的总成本相等。在此之前，0.1B 模型多付的训练成本尚未收回；在此之后，每次服务省下的成本累计超过了前期投入。[^lifecycle]
 
@@ -620,7 +620,7 @@
 
 > **练习 3-9〔延伸〕：增加训练数据或专家数量会改变哪些资源需求**
 >
-> 根据 Llama／Qwen 表重算 $D/N$ 与 $6ND$。若参数量保持不变、训练数据增加四倍，预测训练计算与上线后的纯权重容量分别如何变化。对 MoE，说明总专家数加倍、每 token 选中数不变时，参数容量与专家训练计算能否都按两倍估算。
+> 根据 Llama／Qwen 表重算 $D/N$ 与 $6ND$。若参数量保持不变、训练数据量增至四倍，预测训练计算与上线后的纯权重容量分别如何变化。对 MoE，说明总专家数加倍、每 token 选中数不变时，参数容量与专家训练计算能否都按两倍估算。
 
 GPU 小时衡量设备数量与使用时间的乘积。若持续使用 $n_{\mathrm{GPU}}$ 张卡，总 GPU 小时为 $H_{\mathrm{GPU}}$，实际历时为
 
@@ -668,21 +668,21 @@
 
 > **练习 3-10〔延伸〕：训练完成时间与累计服务成本**
 >
-> 将 1,022,362 GPU 小时分别分配给 2048 与 1024 张同类设备，求训练从开始到完成所需的实际天数。再比较两个满足相同质量与时限的模型：小模型多投入 100 万美元训练，每个成功任务节省 0.002 美元。预计服务两亿次和十亿次时分别选哪个？计算总成本差，并说明若实际任务量发生变化，选择会在何处反转。
+> 将 1,022,362 GPU 小时分别分配给 2048 与 1024 张同类设备，求训练从开始到完成所需的实际天数。再比较两个满足相同质量与时限的模型：小模型多投入 100 万美元训练，每个成功任务节省 0.002 美元。预计完成两亿项和十亿项成功任务时，分别应选择哪个模型？计算总成本差，并说明若实际任务量发生变化，选择会在何处反转。
 
 后续章节将使用本章整理出的三类负载信息。下表列出各类负载需要记录的内容，以及这些信息将用于哪些系统设计问题。
 
 | 负载 | 必须保留的字段 | 后续使用 |
 | --- | --- | --- |
 | Chat／Agent | 任务检查、模型与版本、每轮输入／命中／输出、到达、工具依赖、分支与复用间隔 | 第 8 章安排批处理与状态；第 9 章交接与路由；第 11 章管理工具环境 |
-| 视觉／实时 | 原图字节、$\mathrm E/\mathrm P/\mathrm D$、EC/KV、帧或块到达、首次响应时间／连续播放要求、打断与过期工作 | 第 8、9 章安排编码和交接；第 12 章加入链路与执行位置 |
+| 视觉／实时 | 原图字节、$\mathrm E/\mathrm P/\mathrm D$、EC/KV、帧或块到达、首次响应时间／连续播放要求、打断后不再需要的计算 | 第 8、9 章安排编码和交接；第 12 章加入链路与执行位置 |
 | 训练／RL | 数据与质量、长度和标签、微批次／更新、生成样本数／保留样本数、奖励计算与模型调用、权重版本、完成期限 | 第 4–7 章分析算力、存储与通信需求；第 10 章安排学习；第 11 章配置环境与资源池 |
 
-这三类例子都说明，描述负载需要记录具体的执行过程。两分钟请求中，各时段的输入输出比例决定积压何时出现；代码 Agent 中，工具依赖决定等待时间和状态保存多久；RL 中，筛选前后的样本数决定生成与更新阶段各处理多少数据。掌握这些信息，才能为各阶段安排资源。
+这三类例子都说明，描述负载需要记录具体的执行过程。两分钟请求中，各时段的输入输出比例决定积压何时出现；代码 Agent 中，工具依赖决定等待时间，以及状态需要保存多久；RL 中，筛选前后的样本数决定生成与更新阶段各处理多少数据。掌握这些信息，才能为各阶段安排资源。
 
 第四章将结合芯片的算力、存储容量与带宽，分析这些负载如何在设备上执行。本章算出的矩阵尺寸、状态大小和访问次数，将用于判断计算需要多久、数据能否放下，以及读写能否及时完成。各阶段的依赖关系则决定哪些工作可以重叠，哪些必须等待。
 
-训练预算下的最优规模与长期服务的最优规模，也可能不同。固定训练计算量时，参数与训练 token 之间存在折中；若模型还要服务大量请求，多训练一个较小模型，可能用一次性训练投入换取持续较低的推理成本。Llama 3 报告说明，小模型会通过更长时间的训练提高能力，以满足推理成本要求。计算生命周期成本时，应将训练投入、预期服务量、每次任务成本和模型有效寿命分别列出。专用硬件若需要较长部署周期，模型版本能使用多久也会成为这一选择的条件。
+训练预算下的最优规模与长期服务的最优规模，也可能不同。固定训练计算量时，参数与训练 token 之间存在折中；若模型还要服务大量请求，对一个较小模型进行更充分的训练，可能用一次性训练投入换取持续较低的推理成本。Llama 3 报告说明，小模型会通过更长时间的训练提高能力，以满足推理成本要求。计算整个生命周期的成本时，应将训练投入、预期服务量、每次任务成本和模型有效寿命分别列出。专用硬件若需要较长部署周期，模型版本能使用多久也会成为这一选择的条件。
 
 ## 谬误与陷阱
 
@@ -716,7 +716,7 @@
 
 [^reasoning]: [练习 3-3：1,024 预算](../experiments/ch03/03-03/README.md)与[4,096 预算](../experiments/ch03/03-03/wide-budget/README.md)；理论背景见[Test-Time Compute](../references/files/papers/test-time-compute.pdf)。
 
-[^reasoning-off]: [关闭 thinking 的诊断](../experiments/ch03/03-03/no-thinking/README.md)，严格预先规定的评分标准、后验提取和并发进程观测分开。
+[^reasoning-off]: [关闭 thinking 的诊断](../experiments/ch03/03-03/no-thinking/README.md)，分别记录按预定标准评分的结果、事后从输出中提取答案的结果，以及并发进程的观测数据。
 
 [^agent]: [练习 3-4：两条代码 Agent 轨迹](../experiments/ch03/03-04/README.md)，含独立检查与额外别名条件。
 
@@ -738,7 +738,7 @@
 
 [^audio]: [40 ms 缓冲教学时序](../calculations/results/audio-timing-base.md)与[60 ms 缓冲](../calculations/results/audio-timing-large-buffer.md)。
 
-[^audio-interrupt]: [打断与静音的教学投影](../calculations/results/audio-timing-interrupt.md)。
+[^audio-interrupt]: [打断与静音的教学算例](../calculations/results/audio-timing-interrupt.md)。
 
 [^training]: [训练计算量笔记](../case-studies/training-compute.md)，线性层前反向、序列分布与 DeepSeek V4-Flash 专家分项计算。
 
@@ -758,7 +758,7 @@
 
 [^verl]: [实验 10-8：固定 verl 最小训练流程](../experiments/ch10/10-08/README.md)。本章用这一实验区分任务质量与参数更新，完整系统的组织方式见第十章。
 
-[^verl-loss]: [固定 verl 配方的损失归一与更新路径](../research/2026-infra-survey/qa/verl-recipe-loss-closure.md)，绑定原配置、源码与导出张量。
+[^verl-loss]: [固定 verl 训练配置的损失归一化与参数更新过程](../research/2026-infra-survey/qa/verl-recipe-loss-closure.md)，绑定原配置、源码与导出张量。
 
 [^rl]: [Qwen 教学 RL 批次](../calculations/results/rl-qwen8-base.md)。
 
