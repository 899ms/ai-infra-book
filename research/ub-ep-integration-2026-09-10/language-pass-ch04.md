# 第 4 章语言通读：逐句修改对照

本文件记录 2026-09-11 全书通读在本章留下的净改动。

## 全书通读（2026-09-11，按最终文本与通读前快照逐行对照）

共 47 行有改动；英文术语按“首次出现保留中文注释、之后用英文”的原则已回退，下表是回退之后的净改动。省略号表示只截取了改动附近的文字。

| 修改前 | 修改后 |
|---|---|
| 第二、三章介绍了模型的计算图，以及请求在不同阶段的计算需求。本章以 Qwen3-8B 的… | 第 2、3 章介绍了模型的计算图，以及请求在不同阶段的计算需求。本章以 Qwen3-8B 的… |
| …也是 $W$ 的列数；本例 $K=N=4096$。这组大写字母描述矩阵形状，与第一章容量记号 $M$ 的语境不同。若一次处理 $B$ 条等长请求、每条有 $P$ … | …也是 $W$ 的列数；本例 $K=N=4096$。这组大写字母描述矩阵形状，与第 1 章容量记号 $M$ 的语境不同。若一次处理 $B$ 条等长请求、每条有 $P$ … |
| ![图 4-3 模型与硬件跨代协同。实线沿时间向下：现有加速器影响模型选择，软件运行暴露长期瓶颈… | ![模型与硬件跨代协同。实线沿时间向下：现有加速器影响模型选择，软件运行暴露长期瓶颈… |
| …MiB 的统一数据缓冲（Unified Buffer）和 4 MiB 累加存储。论文给出的芯片面积分布中，数据缓冲约占 37%，计算单元约占 30%，控制电路约… | …MiB 的统一数据缓冲（Unified Buffer）和 4 MiB 累加存储。TPU v1 论文给出的芯片面积分布中，数据缓冲约占 37%，计算单元约占 30%，控制电路约… |
| …的硬件。它支持的矩阵指令形状是有限的，程序需要把大任务组织成相应的小步；一张加速器上的多个计算单元可以同时处理不同块。先看其中一块怎样计算，再看形状不合适时会发生… | …的硬件。它支持的矩阵指令形状是有限的，程序需要把大任务组织成相应的小步；一张加速卡上的多个计算单元可以同时处理不同块。先看其中一块怎样计算，再看形状不合适时会发生… |
| …传递，部分和留在本地更新。一条指令描述整块工作，控制成本也由大量运算共同分摊。第五章将进一步说明，软件怎样组织块大小和遍历顺序，让这些小步持续取得所需数据。 | …传递，部分和留在本地更新。一条指令描述整块工作，控制成本也由大量运算共同分摊。第 5 章将进一步说明，软件怎样组织块大小和遍历顺序，让这些小步持续取得所需数据。 |
| …矩阵乘法，Transformer 层还包含归一化、位置编码和激活函数；专家模型还需要进行路由。这些步骤包含逐元素运算，也包含多个元素之间的依赖。 | …矩阵乘法，Transformer 层还包含归一化、位置编码和激活函数；专家模型还要做路由。这些步骤包含逐元素运算，也包含多个元素之间的依赖。 |
| …元处理指数等操作，控制逻辑安排循环、地址、分支和同步。它们一起决定矩阵结果何时能够被后续计算使用。昇腾的 Cube、Vector、Scalar 命名直观地体现了这种分工；950 白皮书中的一个 AI 子系统由一个 Cube Core 和两个 Vect… | …元处理指数等操作，控制逻辑安排循环、地址、分支和同步。它们一起决定矩阵结果何时能交给后续计算使用。昇腾的 Cube、Vector、Scalar 命名直观地体现了这种分工；昇腾 950 白皮书中的一个 AI 子系统由一个 Cube Core 和两个 Vect… |
| …QK 与 PV 合计约 8.39 MFLOPs，指数结果有 16,384 个。按矩阵速率 8,192 FLOPs/SM/cycle、指数速率 16 results/SM/cycle，两者各需 1,024 周期。矩阵操作数的共享内存读取为 96 KiB，按 128 bytes/SM/cycle，需要 768 周期。[^fa4] | …QK 与 PV 合计约 8.39 MFLOPs，指数结果有 16,384 个。按每个 SM 每周期 8,192 FLOPs 的矩阵速率和每周期 16 个结果的指数速率，两者各需 1,024 周期。矩阵操作数的共享内存读取为 96 KiB，按每周期 128 bytes，需要 768 周期。[^fa4] |
| 降低 KV 位宽也能释放容量。第二章的 DeepSeek V4.1-Flash 同时使用全局历史与滑动窗口注意力（… | 降低 KV 位宽也能释放容量。第 2 章的 DeepSeek V4.1-Flash 同时使用全局历史与滑动窗口注意力（… |
| 训练还使用梯度、优化器状态和为反向传播保存的激活。按照第三章介绍的张量生命周期计算这些状态的占用：在某一时刻仍需保存的所有张量共同决定内存… | 训练还使用梯度、优化器状态和为反向传播保存的激活。按照第 3 章介绍的张量生命周期计算这些状态的占用：在某一时刻仍需保存的所有张量共同决定内存… |
| …，但第三块到 512 tick 才就绪，第二块在 448 tick 已结束，留下六十四 tick 空闲。](ch04/figure-4-pipeline-two.sv… | …，但第三块到 512 tick 才就绪，第二块在 448 tick 已结束，留下 64 tick 空闲。](ch04/figure-4-pipeline-two.sv… |
| …，但第三块到 512 tick 才就绪，第二块在 448 tick 已结束，留下六十四 tick 空闲。横轴 tick 为离散时间单位；各行对应一个数据块，灰色表示输… | …，但第三块到 512 tick 才就绪，第二块在 448 tick 已结束，留下 64 tick 空闲。横轴 tick 为离散时间单位；各行对应一个数据块，灰色表示输… |
| …产品与昇腾 910C 都采用双裸片设计：各裸片提供本地计算能力和存储接口，封装内链路交换跨 die 数据。[^package] 扩大的容量使更多模型能够驻留，计算放在哪一侧则决定多少访… | …产品与昇腾 910C 都采用双裸片设计：各裸片提供本地计算能力和存储接口，封装内的链路负责跨 die 的数据交换。[^package] 扩大的容量使更多模型能够驻留，计算放在哪一侧则决定多少访… |
| …放到权重所在裸片后，跨连接交换输入与结果，共 64 MiB。在相同链路上，传输约六十一微秒；权重由各裸片本地读取。](ch04/figure-4-locality-com… | …放到权重所在裸片后，跨连接交换输入与结果，共 64 MiB。在相同链路上，传输约 61 μs；权重由各裸片本地读取。](ch04/figure-4-locality-com… |
| …放到权重所在裸片后，跨连接交换输入与结果，共 64 MiB。在相同链路上，传输约六十一微秒；权重由各裸片本地读取。* | …放到权重所在裸片后，跨连接交换输入与结果，共 64 MiB。在相同链路上，传输约 61 μs；权重由各裸片本地读取。* |
| …等两侧都完成，由模型的计算图决定；如果下一步要合并部分和，就在合并点形成同步。第六章会从具体并行切分推导这些交接。 | …等两侧都完成，由模型的计算图决定；如果下一步要合并部分和，就在合并点形成同步。第 6 章会从具体的并行切分推导这些交接。 |
| 主机上传的例子强调了数据驻留和复用。加速器之间还会频繁交换激活、专家输入或部分结果，这些消息有大有小，除了传多少字节，还要考虑每次传输的固定开销。一次传输既要提交请求、通知… | 主机上传的例子强调了数据驻留和复用。卡之间还会频繁交换激活、专家输入或部分结果，每次传的数据有大有小，除了传多少字节，还要考虑每次传输的固定开销。一次传输既要提交请求、通知… |
| 把带宽翻倍，小消息时间从 2.08 降至 2.04 μs，大消息从约 23 降至 12.5 μs。若改为把固定开销减半，小消息约减半，大消息仅省约 4%。两种优化针对的是公式中不同的主导项。 | 把带宽翻倍，8 KiB 传输的时间从 2.08 降至 2.04 μs，2 MiB 传输从约 23 降至 12.5 μs。若改为把固定开销减半，小数据量的传输时间约减半，大数据量的传输仅省约 4%。两种优化针对的是公式中不同的主导项。 |
| ![8 KiB 小消息的启动与传输。固定启动两微秒，带宽从 100 增至 200 GB/s 只缩短蓝色传输项。](ch04/fig… | ![一次 8 KiB 传输的启动与传输时间。固定启动开销 2 μs，带宽从 100 增至 200 GB/s 只缩短蓝色传输项。](ch04/fig… |
| *图 4-20　8 KiB 小消息的启动与传输。固定启动两微秒，带宽从 100 增至 200 GB/s 只缩短蓝色传输项。* | *图 4-20　一次 8 KiB 传输的启动与传输时间。固定启动开销 2 μs，带宽从 100 增至 200 GB/s 只缩短蓝色传输项。* |
| ![2 MiB 消息在相同启动条件下的传输时间。较大的蓝色传输项使带宽翻倍带来更显著的收益；本图纵轴范围与小消息图分别标注。](ch04/figure-4-large-message.svg) | ![一次 2 MiB 传输在相同启动条件下的时间。较大的蓝色传输项使带宽翻倍带来更显著的收益；本图纵轴范围与上一图分别标注。](ch04/figure-4-large-message.svg) |
| *图 4-21　2 MiB 消息在相同启动条件下的传输时间。较大的蓝色传输项使带宽翻倍带来更显著的收益；本图纵轴范围与小消息图分别标注。* | *图 4-21　一次 2 MiB 传输在相同启动条件下的时间。较大的蓝色传输项使带宽翻倍带来更显著的收益；本图纵轴范围与上一图分别标注。* |
| 令两项相等，就得到消息大小的转折点： | 令两项相等，就得到数据量的转折点： |
| …approx24.4$，即从 25 行起，传输时间超过固定开销。小于这一规模时，合并消息能降低每条消息分摊的启动开销；大于这一规模时，减少字节或提高带宽的收益更明显。把多个已就绪的消息合并，会减少启动次数；等待尚未就绪的消息来凑大包，则会增加排队时间。消息粒度因此也涉及吞吐与响应时间的取舍。 | …approx24.4$，即从 25 行起，传输时间超过固定开销。小于这一规模时，把多次传输合并成一次，能降低每份数据分摊的启动开销；大于这一规模时，减少字节或提高带宽的收益更明显。把多份已就绪的数据合并发送，会减少启动次数；等待尚未就绪的数据来凑大包，则会增加排队时间。传输的粒度因此也涉及吞吐与响应时间的取舍。 |
| 若一次执行有 100 次必须串行的小消息交换，每次 2 μs，仅固定开销就有 200 μs。此时，即使减少一些传输数据，总时间也很难明显缩短，而减少交换轮数可以直接缩短依赖链。全归约（AllReduce）将各加速器的局部贡献合并，并让每个加速器取得完整结果；它将多次传输与归约连接起来，第六章会分别计算轮数、每条链路的传输字节数，以及传输与计算能够重叠的时间。 | 若一次执行有 100 次必须串行的小数据量交换，每次 2 μs，仅固定开销就有 200 μs。此时，即使减少一些传输数据，总时间也很难明显缩短，而减少交换轮数可以直接缩短依赖链。全归约（AllReduce）把各卡的局部贡献合并，并让每张卡取得完整结果；它把多次传输与归约连接起来，第 6 章会分别计算轮数、每条链路的传输字节数，以及传输与计算能够重叠的时间。 |
| NVLink、昇腾 Unified Bus 等互联提供设备间的传输路径，完成通知让接收方在数据到达… | NVLink、华为 Unified Bus 等互联提供设备间的传输路径，完成通知让接收方在数据到达… |
| > **思考：合并消息如何改变完成时间与首条响应？** 四条 8 KiB 消息依次在 0、1、2、3 μs 就绪。仍取 100 GB/s、每次启动 2 μs，在同一链路上比较就绪后分别发送与等齐后合并发送：所有消息何时送达？第一条消息又何时可供接收方使用？ | > **思考：合并发送如何改变完成时间与首份数据的响应？** 四份 8 KiB 数据依次在 0、1、2、3 μs 就绪。仍取 100 GB/s、每次启动 2 μs，在同一链路上比较就绪后分别发送与等齐后合并发送：所有数据何时送达？第一份数据又何时可供接收方使用？ |
| 沿用第 4.2.4 节每 32 个值共享一字节 scale 的分块格式；MX 表示这种微缩放表示。MXFP8 需要 $16+0.5=16.5$ MiB，MXFP4 需要 $8+… | 沿用第 4.2.4 节每 32 个值共享一字节 scale 的分块格式；MX 是这种微缩放（microscaling）格式的名称。MXFP8 需要 $16+0.5=16.5$ MiB，MXFP4 需要 $8+… |
| …090、5090 没有 NVLink；RTX 3090 支持双卡 NVLink。后续并行计算章节将用这些链路参数计算张量切分、梯度交换与专家分派的时间。[^nvidia-prod… | …090、5090 没有 NVLink；RTX 3090 支持双卡 NVLink。第 6、7 章会用这些链路参数计算张量切分、梯度交换与专家分派的时间。[^nvidia-prod… |
| 早期昇腾 910、即后续代际比较中称作 910A 的一代，设计背景是 ResNet 等 CNN 大量占据训练负载。作者的设计回顾记录了这一背景；DaVinci 论文进一步给出 Scal… | 早期昇腾 910（后面的代际比较中称作 910A）设计时，ResNet 等卷积神经网络（CNN）占据了大部分训练负载。作者的设计回顾记录了这一背景；DaVinci 论文进一步给出 Scal… |
| … $56\times56\times64$，输出通道数为 64，步长为 1，通过 padding 保持空间尺寸。输入以 FP16 保存，占 $56\times56\times64… | … $56\times56\times64$，输出通道数为 64，步长为 1，通过填充（padding）保持空间尺寸。输入以 FP16 保存，占 $56\times56\times64… |
| …36\times576\times64\approx231.2$ MFLOPs。论文给出的 Ascend-Max Cube 为每周期 8,192 FLOPs，因… | …36\times576\times64\approx231.2$ MFLOPs。DaVinci 论文给出的 Ascend-Max Cube 为每周期 8,192 FLOPs，因… |
| …力块只占 1,024 周期；向量工作却从简单 ReLU 变成指数、归约与归一化。早期论文的 BERT 分析中，多数层由 Cube 时间主导。把注意力单独拆开后，可以… | …力块只占 1,024 周期；向量工作却从简单 ReLU 变成指数、归约与归一化。DaVinci 论文的 BERT 分析中，多数层由 Cube 时间主导。把注意力单独拆开后，可以… |
| …外层接口承担，要在 1.024 μs 内完成，该接口需要约 256 GB/s。把早期论文中每核分配的 94 GB/s 末级缓存（LLC）带宽代入，这笔工作需要约 2… | …外层接口承担，要在 1.024 μs 内完成，该接口需要约 256 GB/s。把 DaVinci 论文中每核分配的 94 GB/s 末级缓存（LLC）带宽代入，这笔工作需要约 2… |
| 950 在 Cube 的一级缓冲 L1 与 Vector 的统一缓冲 UB 之间增加 CV（Cube–Vector）直接通路。两次各 64 KiB 的中间张… | 950 在 Cube 的一级缓冲 L1 与 Vector 的统一缓冲（Unified Buffer，昇腾文档简写为 UB，与第 6 章的互联 Unified Bus 不是一回事）之间增加 CV（Cube–Vector）直接通路。两次各 64 KiB 的中间张… |
| …0 同时将单个 Vector Core 的 FP16／FP32 吞吐提高一倍，在 UB 与向量算术逻辑单元（ALU）之间增加寄存器文件，并优化 Softmax、GELU… | …0 同时将单个 Vector Core 的 FP16／FP32 吞吐提高一倍，在统一缓冲与向量算术逻辑单元（ALU）之间增加寄存器文件，并优化 Softmax、GELU… |
| …的请求，其 BF16 KV 是 1.125 GiB。设一种片上方案放完权重后，每个加速器剩 256 MiB 用于 KV，状态可均匀切分。一条请求需要 $\lceil1152/256\rceil=5$ 个加速器；八条请求共 9 GiB，需要 36 个；若八条请求的上下文都增至 32,768 token，需要 144 个。 | …的请求，其 BF16 KV 是 1.125 GiB。设一种片上方案放完权重后，每颗芯片剩 256 MiB 用于 KV，状态可均匀切分。一条请求需要 $\lceil1152/256\rceil=5$ 颗芯片；八条请求共 9 GiB，需要 36 颗；若八条请求的上下文都增至 32,768 token，需要 144 颗。 |
| …权重并未增加。片上保存权重减少了一类读取，但持续服务还需要为增长的状态提供容量。增加加速器后，分布式 KV 又带来跨加速器数据汇集；容量扩展与通信需求由同一工作负载同时产生。 | …权重并未增加。片上保存权重减少了一类读取，但持续服务还需要为增长的状态提供容量。芯片增多后，分布在各芯片上的 KV 又要跨芯片汇集；容量扩展与通信需求由同一工作负载同时产生。 |
| …位置。分配计算任务时，既要让数据在本地复用，又要避免少数单元过忙而其余单元空闲，原理与多 die 放置一致。 | …位置。分配计算任务时，既要让数据在本地复用，又要避免少数单元过忙而其余单元空闲，与多 die 放置的原理一致。 |
| …权重的同时，各请求仍独立读取 KV。题设每请求保留 8K 上下文，batch 从十三起 KV 读取超过共享权重读取。](ch04/figure-4-12-speci… | …权重的同时，各请求仍独立读取 KV。题设每请求保留 8K 上下文，batch 从 13 起 KV 读取超过共享权重读取。](ch04/figure-4-12-speci… |
| …权重的同时，各请求仍独立读取 KV。题设每请求保留 8K 上下文，batch 从十三起 KV 读取超过共享权重读取。* | …权重的同时，各请求仍独立读取 KV。题设每请求保留 8K 上下文，batch 从 13 起 KV 读取超过共享权重读取。* |
| ![图 4-30 独立只读权重改变两条存储路径。上方权重与 KV 争用 HBM；下方 ROM 提供… | ![独立只读权重改变两条存储路径。上方权重与 KV 争用 HBM；下方 ROM 提供… |
| 同一个 100 μs 时限也可以用来求计算单元数量。沿用项目历史执行产物中的一组张量工作，共 15,134,641,792 次操作。设每条运算通道（la… | 同一个 100 μs 时限也可以用来求计算单元数量。沿用该项目历史执行记录中的一组张量工作，共 15,134,641,792 次操作。设每条运算通道（la… |
| 多卡运行的张量切分与专家分派在第六章展开。下面用运行记录把单设备的资源预算换算为任务时间。 | 多卡运行的张量切分与专家分派在第 6 章展开。下面用运行记录把单设备的资源预算换算为任务时间。 |
| …了相同规模的片外权重，单行的时间差需要沿提交、执行与等待进一步定位。256 行则呈现另一种情况：此前重复使用同一份权重时只读 256 bytes，此前轮换使用不同权重副本时约读 32.1 MiB，但常规计时都在 33 μs 左右。缓存改变了片… | …了相同规模的片外权重，单行的时间差需要沿提交、执行与等待进一步定位。256 行则是另一种情况：复用同一份权重时只读 256 bytes，轮换不同的权重副本时约读 32.1 MiB，但常规计时都在 33 μs 左右。缓存改变了片… |
| …怎样改变资源配比。访问计数、内核记录与计时将这些预算落实为运行速度和任务成本。第五章继续研究布局、融合与调度，说明软件怎样用好这些硬件资源。 | …怎样改变资源配比。访问计数、内核记录与计时将这些预算落实为运行速度和任务成本。第 5 章继续研究布局、融合与调度，说明软件怎样用好这些硬件资源。 |

## 翻译腔审查（2026-09-11）

作者确认术语原则并统一“笔者”“N 个 token”“Queqiao”之后，再做一遍只针对翻译腔的审查（含图注里汉字数字改阿拉伯数字）。共 24 行有改动，按最终文本与审查前快照逐行对照。

| 修改前 | 修改后 |
|---|---|
| …加，部分和仍留在累加存储中，下一块继续更新它。如果每次更新都写回片外，中间结果就会被反复写入和读出；把部分和留在片上，便把这些访问缩减为最终的一次写回。数据重复使用的次数越多，将它留在片上就越有价值。 | …加，部分和仍留在累加存储中，下一块继续更新它。如果每次更新都写回片外，中间结果就要反复写出、读入；把部分和留在片上，这些访问就只剩最后一次写回。数据重复使用的次数越多，将它留在片上就越有价值。 |
| …它增加专用能力。新一代芯片投入使用后，不同模型的运行成本又会随之改变。设计和制造存在周期，因此硬件响应的是此前已经观察到、并预计会延续的需求。第 4.6 节将沿三家架构的代际变化，具体展开图 4-3 中的… | …它增加专用能力。新一代芯片投入使用后，不同模型的运行成本又会随之改变。设计和制造都要花时间，所以硬件回应的是此前已经观察到、并且预计会延续的需求。第 4.6 节将沿三家架构的代际变化，具体展开图 4-3 中的… |
| 大矩阵要拆成许多小块计算，矩阵单元正是执行这些小块的硬件。它支持的矩阵指令形状是有限的，程序需要把大任务组织成相应的小步；一张加速卡上的多个计算单元可以同时处理不同块。先看其… | 大矩阵要拆成许多小块计算，矩阵单元正是执行这些小块的硬件。它支持的矩阵指令形状有限，程序要把大任务组织成相应的小步；一张加速卡上的多个计算单元可以同时处理不同块。先看其… |
| …imes16^3=8192$ 次浮点操作。每个输入元素在块内参与 16 次乘法。相比逐个输出独立取得全部操作数，这种计算方式减少了重复搬运和指令开销。 | …imes16^3=8192$ 次浮点操作。每个输入元素在块内参与 16 次乘法。与每个输出各自取一遍全部操作数相比，这种方式减少了重复搬运和指令开销。 |
| …多个块时，矩阵单元、共享内存接口和指数单元可以构成流水线，处理每个块时，三者分别被占用 1,024、768 和 1,024 周期。若新块到达的速度超过其中任一单元… | …多个块时，矩阵单元、共享内存接口和指数单元可以构成流水线，处理每个块时，三者分别占用 1,024、768 和 1,024 周期。若新块到达的速度超过其中任一单元… |
| …P4 保存和读取，减少了传输字节数；转换后，矩阵单元按 FP8 格式计算。若展开结果被多次使用，一次转换可以分摊到多次调用；若每次调用都重新展开，转换时间就成为每次任务的固定部分。低精度能省下多少时间，要将减少的读取时间与增加的转换时间相比较。[^precision] | …P4 保存和读取，减少了传输字节数；转换后，矩阵单元按 FP8 格式计算。若展开后的权重要用很多次，一次转换的时间就能分摊到多次调用；若每次调用都重新展开，转换时间就成为每次任务的固定部分。低精度能省下多少时间，要拿省下的读取时间和多出的转换时间相比。[^precision] |
| …00 的容量缺口接近 58 GB，H200 则有约 3.1 GB 余量。容量增加首先使这些数据能够共同驻留。[^storage] | …00 的容量缺口接近 58 GB，H200 则有约 3.1 GB 余量。容量增加，首先是让这些数据能够同时驻留。[^storage] |
| > **在途事务不足使接口带宽翻倍只带来有限收益。** 升级到 2 TB/s 后，4,096 个事务仍只能支持约 $4096\times128/(500\ \mathrm{ns})=1.05$ TB/s，时间约为 1.15 ms。接口带宽增加一倍，而未完成事务数的限制使吞吐率只能提高约 5%。 | > **在途事务不足时，接口带宽翻倍收益有限。** 升级到 2 TB/s 后，4,096 个事务仍只能支持约 $4096\times128/(500\ \mathrm{ns})=1.05$ TB/s，时间约为 1.15 ms。接口带宽增加了一倍，吞吐率却受未完成事务数限制，只提高约 5%。 |
| 数据布局通过两种方式影响访问开销：步长影响地址计算，地址的连续性影响多个访问能否合并为一次事务。相同… | 数据布局从两方面影响访问开销：步长影响地址计算，地址的连续性影响多个访问能否合并为一次事务。相同… |
| 异步搬运使计算线程在发起加载后继续执行其他工作，到使用数据前再等待完成事件。搬运单元向空槽写入数据，完成事件标记数据就绪；计算结束后再释放槽… | 有了异步搬运，计算线程发起加载后可以继续做其他工作，到要用数据时再等待完成事件。搬运单元向空槽写入数据，完成事件标记数据就绪；计算结束后再释放槽… |
| …算能力和存储接口，封装内的链路负责跨 die 的数据交换。[^package] 扩大的容量使更多模型能够驻留，计算放在哪一侧则决定多少访问要经过链路。 | …算能力和存储接口，封装内的链路负责跨 die 的数据交换。[^package] 容量扩大后，更多模型能够驻留；计算放在哪一侧，则决定多少访问要经过链路。 |
| …间的数据交接：CPU 准备的数据需要传给 GPU 使用。CPU 准备的输入通常先位于主机内存，独立显存 GPU 通过 PCIe 或平台专用连接取得数据。主机到设备称为 H2D，设备到主机称为 D2H。复制引擎通过直接内存访问（… | …间的数据交接：CPU 准备的数据需要传给 GPU 使用。CPU 准备的输入通常先放在主机内存里，带独立显存的 GPU 要经 PCIe 或平台专用连接取得这些数据。主机到设备称为 H2D，设备到主机称为 D2H。复制引擎通过直接内存访问（… |
| …重连同输入输出的传输却需要约 37.3 μs。于是，两条路径都先受数据读取限制。相同的 Tensor Core 改进，对大批矩阵带来显著收益，对这次单行调用则仍需改善权重读取。 | …重连同输入输出的传输却需要约 37.3 μs。于是，两条路径都先受数据读取限制。同一项 Tensor Core 改进，对大批量矩阵收益显著，对这次单行调用则帮助不大，要加快的仍是权重读取。 |
| …重读取。RTX 5090 的容量提高到 32 GB，带宽再提高约 1.78 倍；其新增 FP4 路径还让能够采用低精度的模型改变执行方法。这三代产品分别改变了矩阵处理、权重读取和模型驻留的资源预算。 | …重读取。RTX 5090 的容量提高到 32 GB，带宽再提高约 1.78 倍；新增的 FP4 路径还让能用低精度的模型有了另一种执行方法。这三代产品分别改变了矩阵处理、权重读取和模型驻留的资源预算。 |
| …ple 的 CPU、GPU 和独立 Neural Engine 共享统一内存。它减少了不同处理器间复制数据的需要，也让本地模型使用较大的内存池。下面先算统一内存能够装下的模型，再算生成一个 token 需要… | …ple 的 CPU、GPU 和独立 Neural Engine 共享统一内存。它省去了处理器之间复制数据的需要，也让本地运行的模型能使用较大的内存池。下面先算统一内存能够装下的模型，再算生成一个 token 需要… |
| 局部存储不均衡可以通过重新分配任务缓解，但还有一个随服务规模增长的问题：KV 的总量会增加。沿用 Qwen3-8B，每条包含 8,192 个 toke… | 局部存储的不均衡可以靠重新分配任务来缓解，但还有一个问题会随服务规模增长：KV 的总量会增加。沿用 Qwen3-8B，每条包含 8,192 个 toke… |
| 专用化利用执行中已经确定的条件重新分配硬件资源。固定算子种类使指令和控制简化，固定数据流使通信能够提前安排，固定数值格式使存储器和运算单元可以按所需位宽设计，固定权重则可以为不再变化的参数设计专门的存储结构。原来用于处理多种情况的控制和数据通路，… | 专用化利用执行中已经确定的条件重新分配硬件资源。固定算子种类，指令和控制就能简化；固定数据流，通信就能提前安排；固定数值格式，存储器和运算单元就能按所需位宽设计；固定权重，则可以为不再变化的参数设计专门的存储结构。原来用于处理多种情况的控制和数据通路，… |
| 模型改变后，可重配置设计通过更新连接与调度适应新的计算。SambaNova 的数据流组织与现场可编程门阵列（FPGA）的可重配置通路，允许通过重新配置改变部分连接和执行顺序；固定权重设计则围绕特定参数组织存储与计算。[^special… | 模型改变后，可重配置设计靠更新连接与调度来适应新的计算。SambaNova 的数据流组织与现场可编程门阵列（FPGA）的可重配置通路，都允许重新配置部分连接和执行顺序；固定权重设计则围绕特定参数组织存储与计算。[^special… |
| 独立 ROM 接口使权重与 KV 可以同时读取。设两条接口的带宽分别为 $\beta_{\mathrm{ROM}}… | 有了独立的 ROM 接口，权重与 KV 就可以同时读取。设两条接口的带宽分别为 $\beta_{\mathrm{ROM}}… |
| …，还可以求出负载变化时的瓶颈转折点。对本章的 Q 投影，Roofline 模型把这一关系写成算术强度与吞吐的关系。 | …，还可以求出负载变化时的瓶颈转折点。对本章的 Q 投影，Roofline 模型把它写成算术强度与吞吐之间的关系。 |
| …应约 59.8 与 350.4 token/s 的读取上限。Ultra 的大内存使这一个 235B MoE 能在本地整机上运行，H200 的高带宽则将同一步读取缩短到约六分之一。 | …应约 59.8 与 350.4 token/s 的读取上限。Ultra 的大内存让这个 235B 的 MoE 能在一台本地整机上运行，H200 的高带宽则把同一步读取缩短到约六分之一。 |
| 先看 RTX 单行：轮换比复用约慢 21%。一个自然解释是重复使用同一份权重使其留在缓存中，减少了 DRAM 读取。这个解释可以直接检验：若少读权重是差别来源，复用权重时… | 先看 RTX 单行：轮换比复用约慢 21%。一个自然的解释是：反复使用同一份权重，权重就留在了缓存里，减少了 DRAM 读取。这个解释可以直接检验：若少读权重是差别来源，复用权重时… |
| …有约 147 MiB L2 请求，远大于近乎为零的那次 DRAM 读取。缓存命中意味着由片上提供数据，计算单元的读取请求和局部传输仍然存在。源程序中的同一权重，在分块执行后可以被多个计算块反复请求。 | …有约 147 MiB L2 请求，远大于近乎为零的那次 DRAM 读取。缓存命中只是数据改由片上提供，计算单元的读取请求和局部传输仍然存在。源程序中的同一份权重，分块执行后会由多个计算块反复请求。 |
| …算交给多个计算块，先形成部分输出，再由归约合并。这样能让更多计算块并行执行，但也需要保存和读取部分结果，并在最后进行归约。因而第 4.1 节的一次数学乘法，在这里展开成多个阶段。 | …算交给多个计算块，先形成部分输出，再由归约合并。这样能让更多计算块并行执行，但也要保存和读取部分结果，最后再做一次归约。因而第 4.1 节的一次数学乘法，在这里展开成多个阶段。 |
