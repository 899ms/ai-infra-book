# 第 2 章语言通读：逐句修改对照

本文件记录 2026-09-11 全书通读在本章留下的净改动。

## 全书通读（2026-09-11，按最终文本与通读前快照逐行对照）

共 48 行有改动；英文术语按“首次出现保留中文注释、之后用英文”的原则已回退，下表是回退之后的净改动。省略号表示只截取了改动附近的文字。

| 修改前 | 修改后 |
|---|---|
| 第一章从存储容量、运算量和数据读取量三个方面分析了一次模型执行，并借助硬件的关键数字… | 第 1 章从存储容量、运算量和数据读取量三个方面分析了一次模型执行，并借助硬件的关键数字… |
| …给因果编码器—解码器（CED），并结合跨层 KV 共享、分层索引与 Engram，为理解模型架构如何改变系统需求提供了新的案例。 | …给因果编码器—解码器（CED），并结合跨层 KV 共享、分层索引与 Engram（按 n-gram 查表取得附加表示的模块），为理解模型架构如何改变系统需求提供了新的案例。 |
| …，只说明后续运算需要这份数据，不表示它一定要在 CPU 与 GPU 之间复制。第四章说明数据放在哪里，第五章再解释程序怎样提交、数据怎样传入传出。 | …，只说明后续运算需要这份数据，不表示它一定要在 CPU 与 GPU 之间复制。第 4 章说明数据放在哪里，第 5 章再解释程序怎样提交、数据怎样传入传出。 |
| … 是当前输入，$h_t$ 是处理到当前 token 位置时保存的状态，$f$ 是使用模型权重进行的变换。当前结果依赖前一 token 位置，所以同一层按位置顺序执行。即使四个输… | … 是当前输入，$h_t$ 是处理到当前 token 位置时保存的状态，$f$ 是用模型权重做的变换。当前结果依赖前一 token 位置，所以同一层按位置顺序执行。即使四个输… |
| … network，FFN）对每个 token 分别做特征变换；**注意力**则在token 之间选择并汇总信息。这两类运算交替组成网络的一层。 | … network，FFN）对每个 token 分别做特征变换；**注意力**则在 token 之间选择并汇总信息。这两类运算交替组成网络的一层。 |
| …里有 32 个查询头和 8 组供查询使用的键值头（KV 头），每头 128 维；2.1.3 将沿查询、键和值的路径解释它们。词表包含 151936 个 token，词嵌入是… | …里有 32 个查询头和 8 组供查询使用的键值头（KV 头），每头 128 维；第 2.1.3 节将沿查询、键和值的路径解释它们。词表包含 151936 个 token，词嵌入是… |
| \| $m_{\mathrm{out}}$ \| 实际进行词表投影的 token 数；只用每条请求的末尾 token 预测下一个 tok… | \| $m_{\mathrm{out}}$ \| 实际执行词表投影的 token 数；只用每条请求的末尾 token 预测下一个 tok… |
| ![同一个 token 的输入经不同投影产生查询、键和值。注意力先用查询与键计算token 之间的关系，再用这些关系汇总值。](ch02/figure-2-qk… | ![同一个 token 的输入经不同投影产生查询、键和值。注意力先用查询与键计算 token 之间的关系，再用这些关系汇总值。](ch02/figure-2-qk… |
| …3　同一个 token 的输入经不同投影产生查询、键和值。注意力先用查询与键计算token 之间的关系，再用这些关系汇总值。* | …3　同一个 token 的输入经不同投影产生查询、键和值。注意力先用查询与键计算 token 之间的关系，再用这些关系汇总值。* |
| 对于每一对查询与上下文 token，一个 $Q$ 头的 $QK^{\mathsf T… | 对每一对查询与上下文 token，一个 $Q$ 头的 $QK^{\mathsf T… |
| …。分块还可以把评分、归一化和 $V$ 汇总接在一起，让分数在片上被使用后释放。第五章将沿这条数据流解释如何减少中间结果的显存读写。 | …。分块还可以把评分、归一化和 $V$ 汇总接在一起，让分数在片上被使用后释放。第 5 章将沿这条数据流解释如何减少中间结果的显存读写。 |
| …保存的是 $K$ 和 $V$。旧查询已经完成了自己的任务，下一步使用新的查询。第八章将进一步讨论如何在不同请求之间共享这些状态。 | …保存的是 $K$ 和 $V$。旧查询已经完成了自己的任务，下一步使用新的查询。第 8 章将进一步讨论如何在不同请求之间共享这些状态。 |
| …就追加 144 KiB。层数或 KV 头数减半，增长速度也减半；batch 增加则为每个独立请求增加一份上下文。[^source-13] | …就追加 144 KiB。层数或 KV 头数减半，增长速度也减半；batch 增加时，每条独立请求各增加一份上下文。[^source-13] |
| 对于 $[m,d]\times[d,f]$，每一行都使用同一个权重矩阵。将权重块读入片上存储后，可以依次用于多行输入，让一次读取的数据参与更多乘加运算。处理行数 $m$ 增加，计算量随之增加，整份矩阵的容量保持不变。第五章将进一步解释片上容量与分块怎样决定一份权重能够复用多少次。 | 对 $[m,d]\times[d,f]$ 的矩阵乘，输入的每一行都使用同一个权重矩阵。将权重块读入片上存储后，可以依次用于多行输入，让一次读取的数据参与更多乘加运算。处理行数 $m$ 增加，计算量随之增加，整份矩阵的容量保持不变。第 5 章将进一步解释片上容量与分块怎样决定一份权重能够复用多少次。 |
| …此，计算读写量时要数清每次访问，计算内存峰值时则要找出同一时刻仍需保存的张量。第五章将结合具体程序分析。 | …此，计算读写量时要数清每次访问，计算内存峰值时则要找出同一时刻仍需保存的张量。第 5 章将结合具体程序分析。 |
| 沿第一章的数据路径看，加载把权重从存储送到 GPU 显存，执行再把所需权重送到计算单元。KV 和层间激活也可在加速器内部保存与使用。相同的逻辑访问因此可以留在片上、经过 HBM，或跨加速器传输；第四章将分别分析这些路径的存储容量与传输带宽。 | 沿第 1 章的数据路径看，加载把权重从存储送到 GPU 显存，执行再把所需权重送到计算单元。KV 和层间激活也可在加速器内部保存与使用。相同的逻辑访问因此可以留在片上、经过 HBM，或跨加速器传输；第 4 章将分别分析这些路径的存储容量与传输带宽。 |
| 多头注意力让不同的查询头分别学习上下文中的关系。在多头注意力（MHA）中，每个 $Q$ 头对应自己的 $K$、$V$ 头；多查询注意力（MQA）让所有 $Q$ 头共用一组 $K$、$V$；分组查询注意力（GQA）则把 $Q$ 头分组，每组共享一组 $K$、$V$。共享后，每个上下文 … | 多头注意力让不同的查询头分别学习上下文中的关系。在多头注意力（Multi-Head Attention，MHA）中，每个 $Q$ 头对应自己的 $K$、$V$ 头；多查询注意力（Multi-Query Attention，MQA）让所有 $Q$ 头共用一组 $K$、$V$；分组查询注意力（Grouped-Query Attention，GQA）则把 $Q$ 头分组，每组共享一组 $K$、$V$。共享后，每个上下文 … |
| …的贡献合入已有状态，当前查询直接使用更新后的状态；下一步继续改写这份状态。这对应 2.3 节开头不断改写定长摘要的图景。下面这类线性注意力通过重新组织查询、键和… | …的贡献合入已有状态，当前查询直接使用更新后的状态；下一步继续改写这份状态。这对应第 2.3 节开头不断改写定长摘要的图景。下面这类线性注意力通过重新组织查询、键和… |
| …，因此仅凭最终递推状态的大小，还无法预测 prefill 期间的内存占用峰值。第五章根据缓冲区从分配到释放的过程分析这一问题。[^source-15] | …，因此仅凭最终递推状态的大小，还无法预测 prefill 期间的内存占用峰值。第 5 章根据缓冲区从分配到释放的过程分析这一问题。[^source-15] |
| …token，递推矩阵通过更新保留上下文关系，压缩索引则在较少条目中寻找相关内容。2.6.3 将结合具体检索任务比较资源需求。[^source-17] | …token，递推矩阵通过更新保留上下文关系，压缩索引则在较少条目中寻找相关内容。第 2.6.3 节将结合具体检索任务比较资源需求。[^source-17] |
| …量的影响：表中的紧凑路径每个 token 增加 27,648 B，本书采用的 HF 参考实现将 K/V 展开，每个 token 增加 1,474,560 B。[跨… | …量的影响：表中的紧凑路径每个 token 增加 27,648 B，本书采用的 Hugging Face 参考实现将 K/V 展开，每个 token 增加 1,474,560 B。[跨… |
| … token 生成后，新 token 依次经过编码器和解码器的全部 40 层。第三章再计算省下了哪些输入工作，第八章区分编码器和解码器的两种重放。 | … token 生成后，新 token 依次经过编码器和解码器的全部 40 层。第 3 章再计算省下了哪些输入工作，第 8 章区分编码器和解码器的两种重放。 |
| …层共享这些缓存。主记录的 512 个值采用 FP4，每 16 个值另有一个字节的 scale，因此每条记录占 $512/2+512/16=288$ 字节；索引记录占 $12… | …层共享这些缓存。主记录的 512 个值采用 FP4，每 16 个值另有一个字节的缩放因子（scale），因此每条记录占 $512/2+512/16=288$ 字节；索引记录占 $12… |
| …rse Attention 2，CSA2）**把层分成三种模式。**Full** 层完整执行“生成全局 KV、索引、选取和注意力”的流程。**Reindex** 层共享 KV，用自己的索引查询重新选取 Top-K；**Reuse** 层连同所选缓存条目一起复用。V4.1 的 38 个全局注意力层中，有 4 个 F… | …rse Attention 2，CSA2）**把层分成三种模式。**Full**（完整）层完整执行“生成全局 KV、索引、选取和注意力”的流程。**Reindex**（重索引）层共享 KV，用自己的索引查询重新选取 Top-K；**Reuse**（复用）层连同所选缓存条目一起复用。V4.1 的 38 个全局注意力层中，有 4 个 F… |
| …次查询的后续 Reindex 层便无法再选入它。模型训练需要适应这一搜索范围，第十章将说明 V4.1 怎样把候选限制纳入后训练。 | …次查询的后续 Reindex 层便无法再选入它。模型训练需要适应这一搜索范围，第 10 章将说明 V4.1 怎样把候选限制纳入后训练。 |
| …参与的分支。专家本身通常仍由 gate、up、down 三个矩阵构成，因此可以按 2.1.4 的 SwiGLU 计算逐个展开。 | …参与的分支。专家本身通常仍由 gate、up、down 三个矩阵构成，因此可以按第 2.1.4 节的 SwiGLU 计算逐个展开。 |
| …=B=64$。每个 token 选择 8 个专家，总共有 512 次 token–expert 分派。若均匀分到 256 个专家，每个专家平均接收 2 个 token，当前批次… | …=B=64$。每个 token 选择 8 个专家，总共有 512 次 token 到专家的分派。若均匀分到 256 个专家，每个专家平均接收 2 个 token，当前批次… |
| 如果专家分布在不同加速器上，就要把输入 token 发送到对应加速器，专家完成计算后再合并结果。通信量因此取决于专家放在哪里、输入保存在何处。第六章讨论多卡划分，第九章介绍 CPU／GPU 放置，以及将注意力（attention）与前馈网络（FF… | 如果专家分布在不同的卡上，就要把输入 token 发送到专家所在的卡，专家算完后再把结果合并。通信量因此取决于专家放在哪里、输入保存在哪里。第 6 章讨论多卡划分，第 9 章介绍 CPU／GPU 放置，以及将注意力（attention）与前馈网络（FF… |
| …KV 头按完整头分工时只能形成四份，八卡执行就需要复制部分 KV 或调整分组。第五章从矩阵分块分析执行效率，第六章将进一步把这些矩阵和状态分配到加速器。 | …KV 头按完整头分工时只能形成四份，八卡执行就需要复制部分 KV 或调整分组。第 5 章从矩阵分块分析执行效率，第 6 章将进一步把这些矩阵和状态分配到各张卡。 |
| …前层的输入。这种注意力沿网络深度选择信息，前面的序列注意力则在不同 token token 之间选择信息。为减少保存和读取所有层输出的开销，块式实现把若干层组织成一块，保留… | …前层的输入。这种注意力沿网络深度选择信息，前面的序列注意力则在不同 token 之间选择信息。为减少保存和读取所有层输出的开销，块式实现把若干层组织成一块，保留… |
| …效果取决于草稿生成、目标模型验证各需多久，以及每轮有多少 token 被接受。第八章将计算这三者的关系。 | …效果取决于草稿生成、目标模型验证各需多久，以及每轮有多少 token 被接受。第 8 章将计算这三者的关系。 |
| 辅助预测使一次前向执行能够提供多个候选，经验证和采样后形成有效输出。第三章将把编码、生成、迭代及实时输出组织成完整负载；第八章再计算生成草稿与验证所需的时间，以及由此节省的生成时间。 | 辅助预测使一次前向执行能够提供多个候选，经验证和采样后形成有效输出。第 3 章将把编码、生成、迭代及实时输出组织成完整负载；第 8 章再计算生成草稿与验证所需的时间，以及由此节省的生成时间。 |
| … 查询后的投影、Single-Pass mHC 和分层索引。MTP、DSpark 与视觉模块按本次执行范围单独加入。 | … 查询后的投影、Single-Pass mHC 和分层索引。MTP、DSpark（V4.1 的推测解码模块）与视觉模块按本次执行范围单独加入。 |
| 其他包含 router、潜空间进出投影、归一化、卷积和连接等参数；注意力投影包括 DeepSeek V… | 其他包含路由器、潜空间进出投影、归一化、卷积和连接等参数；注意力投影包括 DeepSeek V… |
| …ram 构造跨调用 n-gram；生产 FP4 全局缓存与 FP8 窗口的容量见 2.3.5。最后一列将一个 token 在各层选中的路由专家权重相加，再按 BF16 换算… | …ram 构造跨调用 n-gram；生产 FP4 全局缓存与 FP8 窗口的容量见第 2.3.6 节。最后一列将一个 token 在各层选中的路由专家权重相加，再按 BF16 换算… |
| …精度的参数以及格式元数据共同决定。模型加载后，还需要为格式转换和计算分配空间。第五章将分析存储格式与运行时实现如何影响显存占用。 | …精度的参数以及格式元数据共同决定。模型加载后，还需要为格式转换和计算分配空间。第 5 章将分析存储格式与运行时实现如何影响显存占用。 |
| …给出了模型权重和每条请求状态的大小。现在把它们放进同一张显存容量表，就可以判断一个加速器能同时容纳多少条请求，以及量化权重能腾出多少空间。 | …给出了模型权重和每条请求状态的大小。现在把它们放进同一张显存容量表，就可以判断一张卡能同时容纳多少条请求，以及量化权重能腾出多少空间。 |
| 对一个加速器，先从总容量中扣除权重、工作区和其他固定预留空间，剩余空间才能放请求状态。在请求… | 对一张卡，先从总容量中扣除权重、工作区和其他固定预留空间，剩余空间才能放请求状态。在请求… |
| …卡合计为 640 GB。后者为状态、工作区与复制留下约 170 GB 总余量；第六章将根据模型在各卡上的分工，计算每张卡需要为状态、工作区和副本预留多少空间。 | …卡合计为 640 GB。后者为状态、工作区与复制留下约 170 GB 总余量；第 6 章将根据模型在各卡上的分工，计算每张卡需要为状态、工作区和副本预留多少空间。 |
| 容量决定一个实例至少需要多少加速器，请求长度与并发数则决定实例需要处理多少工作。更低位宽释放空间，较短上下文允许更多请求同时驻留，批内复用则改变这些请求的执行成本。因此，选择部署方案时需要依次考虑容量、访问量与运算量，第三章将进一步加入请求的到达与时限。 | 容量决定一个实例至少需要多少张卡，请求长度与并发数则决定实例要处理多少工作。更低位宽释放空间，较短上下文允许更多请求同时驻留，批内复用则改变这些请求的执行成本。因此，选择部署方案时需要依次考虑容量、访问量与运算量，第 3 章将进一步加入请求的到达与时限。 |
| …起变化。增加层数或 KV 头数会增大每请求的 $K$，留给权重的空间随之减少；跨加速器部署则扩大总容量，同时增加通信。Llama 3 报告中的 405B BF16 推理使用两台、共 16 张 H100，其所需容量超过一台服务器所能提供的显存。[^codesign] 模型规模、状态结构与加速器数量因此需要共同选择。 | …起变化。增加层数或 KV 头数会增大每请求的 $K$，留给权重的空间随之减少；跨卡部署则扩大总容量，同时增加通信。Llama 3 报告中的 405B BF16 推理使用两台、共 16 张 H100，其所需容量超过一台服务器所能提供的显存。[^codesign] 模型规模、状态结构与卡数因此需要一起选择。 |
| ![图 2-34 从加速器反推权重预算。24 GB 中先为 4 条 8K 请求的 KV 和 2 G… | ![从加速器反推权重预算。24 GB 中先为 4 条 8K 请求的 KV 和 2 G… |
| 最后把容量、调用链和任务质量合在一起。先根据加速器容量排除无法放下的配置，再沿每个模型的实际计算方式累计请求工作，最后检查模型能否完成目标任务。 | 最后把容量、调用链和任务质量合在一起。先根据每张卡的容量排除放不下的配置，再沿每个模型的实际计算方式累计请求工作，最后检查模型能否完成目标任务。 |
| …并多次读取上下文。长输入、短输出与短输入、长输出由此对各类资源提出不同的需求，第三章将分析这两类请求持续到达时的系统负载。 | …并多次读取上下文。长输入、短输出与短输入、长输出由此对各类资源提出不同的需求，第 3 章将分析这两类请求持续到达时的系统负载。 |
| …独立完成预测与推导，再用配套计算核对。计算命令在仓库根目录运行，完整变体参见[第二章扩写资料](../archive/outlines/extensions/02… | …独立完成预测与推导，再用配套计算核对。计算命令在仓库根目录运行，完整变体参见[第 2 章扩写资料](../archive/outlines/extensions/02… |
| …；路由专家还要按实际接收的输入行数累计。表 2-1 的 Qwen3-8B 基线见 2.1.4。 | …；路由专家还要按实际接收的输入行数累计。表 2-1 的 Qwen3-8B 基线见第 2.1.4 节。 |
| 本章把第一章的三个资源量展开为可计算的对象。矩阵尺寸决定权重容量与线性运算量，查询与上下文… | 本章把第 1 章的三个资源量展开为可计算的对象。矩阵尺寸决定权重容量与线性运算量，查询与上下文… |
| …ode 和完整生成各自的资源需求，并解释长度或 batch 改变时哪些项增长。第三章将进一步考虑请求的到达过程与工具调用的依赖关系，使这些需求成为随时间变化的负载… | …ode 和完整生成各自的资源需求，并解释长度或 batch 改变时哪些项增长。第 3 章将进一步考虑请求的到达过程与工具调用的依赖关系，使这些需求成为随时间变化的负载… |

## 翻译腔审查（2026-09-11）

作者确认术语原则并统一“笔者”“N 个 token”“Queqiao”之后，再做一遍只针对翻译腔的审查（含图注里汉字数字改阿拉伯数字）。共 35 行有改动，按最终文本与审查前快照逐行对照。

| 修改前 | 修改后 |
|---|---|
| 这张计算图也提供了优化实现所需的信息。哪些权重在不同请求间保持不变，哪些状态会被后续生成反复使用，哪些结果只供紧接着的运算使用，都可以从模型结构中找到。后面的存储、编译和调度设… | 这张计算图也提供了优化实现所需的信息。哪些权重在不同请求间保持不变，哪些状态后续生成会反复用到，哪些结果只供紧接着的运算使用，都可以从模型结构中找到。后面的存储、编译和调度设… |
| …四个 token、三层网络，横轴是 token 在序列中的位置索引，纵轴是层数。位置索引用来区分同一个词元在序列中的不同出现。循环神经网络（RNN）把前一 token 位置的状态传给后一 token 位置… | …四个 token、三层网络，横轴是 token 在序列中的位置索引，纵轴是层数。同一个词元在序列中出现多次时，位置索引用来区分它们。循环神经网络（RNN）把前一 token 位置的状态传给后一 token 位置… |
| …维向量，作为输入矩阵的一行；$m$ 统计本次实际参与计算的 token 数。同一词元出现在两个 token 时计作两个 token。若有 $B$ 条等长请求、每条本次处理 $P$ 个 toke… | …维向量，作为输入矩阵的一行；$m$ 统计本次实际参与计算的 token 数。同一个词元出现两次，就算作两个 token。若有 $B$ 条等长请求、每条本次处理 $P$ 个 toke… |
| …oftmax 将分数转成加权系数，再用这些系数汇总值向量。Qwen3 在点积前还进行 **查询与键归一化（QK Norm）**和**旋转位置编码（Rotary Pos… | …oftmax 将分数转成加权系数，再用这些系数汇总值向量。Qwen3 在点积前还要做**查询与键归一化（QK Norm）**和**旋转位置编码（Rotary Pos… |
| …分块则跳过上三角。分块还可以把评分、归一化和 $V$ 汇总接在一起，让分数在片上被使用后释放。第 5 章将沿这条数据流解释如何减少中间结果的显存读写。 | …分块则跳过上三角。分块还可以把评分、归一化和 $V$ 汇总接在一起，让分数在片上用完即释放。第 5 章将沿这条数据流解释如何减少中间结果的显存读写。 |
| …Qwen3-8B 使用这种结构：输入分别经过 gate 与 up 两条升维投影，其中 gate 分支应用 SiLU，再与 up 分支逐元素相乘，最后通过 down 投影降回隐藏维度： | …Qwen3-8B 使用这种结构：输入分别经过 gate 与 up 两条升维投影，gate 分支经过 SiLU 后，再与 up 分支逐元素相乘，最后通过 down 投影降回隐藏维度： |
| …算生成完整回答所需的资源，还需要知道模型调用多少次，以及状态保存多久。同一份权重可能在一个请求中被使用数百次，KV 则一边读取一边增长。最终保存的字节数与累计访问的字节数因此可以相差… | …算生成完整回答所需的资源，还需要知道模型调用多少次，以及状态保存多久。同一份权重在一个请求里可能要用到数百次，KV 则一边读取一边增长。最终保存的字节数与累计访问的字节数因此可以相差… |
| KV 缓存通过保存已经算出的结果，减少后续生成中的重复计算。为了分析节省的计算与增加的存储开销，我们按用途和生命周期，… | KV 缓存保存已经算出的结果，省去后续生成中的重复计算。为了分析节省的计算与增加的存储开销，我们按用途和生命周期，… |
| …tch 为 13；$H=2048$ 时为 51。上下文长度缩短到原来的四分之一，使上下文读取量超过权重读取量所需的请求数约增至四倍。上下文越长，批内权重复用的收益就越早受到限制，因为每个新增请求都会增… | …tch 为 13；$H=2048$ 时为 51。上下文长度缩短到原来的四分之一，上下文读取量超过权重读取量所需的请求数就约增至四倍。上下文越长，批内权重复用的收益就越早受到限制，因为每个新增请求都会增… |
| 上一节比较了一步执行中的读取量。一个完整回答会重复执行许多步，同一份上下文也随之被读取许多次。现在固定一个普通串行生成请求：$B=1$，已恢复前缀为 $S$，本次输入… | 上一节比较了一步执行中的读取量。一个完整回答会重复执行许多步，同一份上下文也随之要读许多次。现在固定一个普通串行生成请求：$B=1$，已恢复前缀为 $S$，本次输入… |
| …共保存约 1.27 GiB。累计读取量是最终存储量的近千倍，因为较早生成的上下文被后续查询反复使用。 | …共保存约 1.27 GiB。累计读取量是最终存储量的近千倍，因为较早生成的上下文，后面的每次查询都要再读一遍。 |
| …相加，批内权重则共同使用。长度不同时，逐请求代入各自的 $H$ 与 $n_d$：长输出让已有上下文被更多次读取，输入越长，第一次 decode 需要访问的上下文就越多。对实际对话，可先根据每轮输入长度、缓存命中情况和返回的 t… | …相加，批内权重则共同使用。长度不同时，逐请求代入各自的 $H$ 与 $n_d$：输出越长，已有上下文被读取的次数就越多；输入越长，第一次 decode 要访问的上下文就越多。对实际对话，可先根据每轮输入长度、缓存命中情况和返回的 t… |
| …f T}$ 与 $AV$ 交互仍按查询头数累计。一次读入的 $K$、$V$ 还可服务同组多个查询头，从而在实际计算中复用同一份数据。 | …f T}$ 与 $AV$ 交互仍按查询头数累计。一次读入的 $K$、$V$ 还可以供同组的多个查询头使用，实际计算中就复用了同一份数据。 |
| GQA、MQA 通过减少 KV 组数节省空间，多头潜变量注意力（Multi-head Latent Attention，MLA）则通过降低上下文表示的维度节省空间。可以先把输入投影为低维潜变量 $c$，再通过上投影… | GQA、MQA 靠减少 KV 组数节省空间，多头潜变量注意力（Multi-head Latent Attention，MLA）则靠降低上下文表示的维度节省空间。可以先把输入投影为低维潜变量 $c$，再通过上投影… |
| …imi K3 的 **Gated MLA（门控多头潜变量注意力）**作实例；门控通过学习得到的系数调节注意力输出。紧凑路径保存 512 维潜变量和参与 $QK^{\… | …imi K3 的 **Gated MLA（门控多头潜变量注意力）**作实例；门控用学习得到的系数调节注意力输出。紧凑路径保存 512 维潜变量和参与 $QK^{\… |
| …四个新 token 生成一条记录，但采用相邻块重叠：除首块的边界处理外，一个条目通过两组不同投影汇总相邻两块、共八个 token 的信息；每个 token 还使用学习得到的… | …四个新 token 生成一条记录，但采用相邻块重叠：除首块的边界处理外，一个条目用两组不同的投影汇总相邻两块、共八个 token 的信息；每个 token 还使用学习得到的… |
| …改写这份状态。这对应第 2.3 节开头不断改写定长摘要的图景。下面这类线性注意力通过重新组织查询、键和值的计算，避免显式计算所有查询—键配对的注意力分数。在特征维度固定时，状态矩阵大小不变，处理整段序列的… | …改写这份状态。这对应第 2.3 节开头不断改写定长摘要的图景。下面这类线性注意力重新组织查询、键和值的计算次序，不再显式算出所有查询—键配对的注意力分数。在特征维度固定时，状态矩阵大小不变，处理整段序列的… |
| …，而不是矩阵大小。逐 token 的上下文列表因此变成了一组累积的键值关联；查询通过这组关联得到输出。 | …，而不是矩阵大小。逐 token 的上下文列表因此变成了一组累积的键值关联；查询从这组关联中取得输出。 |
| …它使用门控与 delta 更新；delta 在这里指依据当前预测误差写入修正量。状态更新不仅写入新信息，还根据当前键对既有状态的预测形成修正，并通过衰减控制旧信息的保留程度。直观上，它更新的是一张固定大小的关联矩阵，而不是不断向逐 token 保存的列表追加条目。旧信息以汇总后的关联形式保存，当前键控制修正的位置，衰减门决定保留多少旧信息。 | …它使用门控与 delta 更新；delta 在这里指依据当前预测误差写入修正量。每次更新不只写入新信息，还用当前键检验既有状态的预测，据此写入修正；旧信息以汇总后的关联形式保存，当前键决定修正落在哪里，衰减门决定保留多少旧信息。它更新的是一张固定大小的关联矩阵，而不是向逐 token 保存的列表不断追加条目。 |
| …与 decode 的实现方式也不同。decode 使用单步递推；一段已知输入可以通过块式算法并行计算，再在块之间传递状态。块内中间量和块间状态会占用临时空间，因此仅… | …与 decode 的实现方式也不同。decode 使用单步递推；一段已知输入可以用块式算法并行计算，再在块之间传递状态。块内中间量和块间状态会占用临时空间，因此仅… |
| …个 token 的上下文时，三项合计 221.9 MiB。这些状态来自注意力分支；接下来的混合专家（Mixture of Experts，MoE）分支准备多个前馈子网络，由路由器为每个 token 选择其中一部分执行，决定同一主干中实际使用哪些权重，两类机制共同组成完整模型。 | …个 token 的上下文时，三项合计 221.9 MiB。这些状态来自注意力分支。接下来的混合专家（Mixture of Experts，MoE）分支准备多个前馈子网络，由路由器为每个 token 选择其中一部分执行，决定同一主干中实际使用哪些权重；两类机制共同组成完整模型。 |
| …状态支持不同的信息访问方式：全局 KV 允许查询直接选择旧 token，递推矩阵通过更新保留上下文关系，压缩索引则在较少条目中寻找相关内容。第 2.6.3 节将结合… | …状态支持不同的信息访问方式：全局 KV 允许查询直接选择旧 token，递推矩阵靠逐步更新保留上下文关系，压缩索引则在较少条目中寻找相关内容。第 2.6.3 节将结合… |
| …V4.1 Flash 对这一分工作出重要调整，将上下文表示的构建与后续查询分开，使输入处理能够使用较少的主干计算。技术报告将这一设计称为因果编码器—解码器，并说明它受到 YoCo… | …V4.1 Flash 对这一分工作出重要调整，将上下文表示的构建与后续查询分开，让输入处理只需较少的主干计算。技术报告将这一设计称为因果编码器—解码器，并说明它受到 YoCo… |
| …中可以双向访问整个输入的编码器；生成新 token 时也仍然需要编码器参与。这种非对称性体现在信息的产生位置和各阶段需要执行的层，而非把输入与输出交给两个互不参与对方计算的模型。 | …中可以双向访问整个输入的编码器；生成新 token 时也仍然需要编码器参与。这种不对称体现在信息在哪里产生、各阶段要执行哪些层，而不是把输入与输出交给两个互不参与对方计算的模型。 |
| 读取差额来自每份权重服务的输入行数：均匀分派时每个专家处理两个 token，集中分派时每个专家处理 64 个 to… | 读取量的差别来自每份权重供多少输入行使用：均匀分派时每个专家处理两个 token，集中分派时每个专家处理 64 个 to… |
| 稀疏激活让总参数量与每个 token 实际使用的参数量可以分别调整，使模型可以用较少计算访问较大的参数集合。服务时，路由结果决定每份专家权重供多少 token 复用：少量 token 分散到很多专家，会产生许多小矩阵，每次读入的权重也很少得到复用；集中到同一组专家，则能组成更大的矩阵。例题 2-4 展示的正是这两种访问方式的… | 稀疏激活让总参数量与每个 token 实际使用的参数量可以分别调整，模型就能用较少的计算访问较大的参数集合。服务时，路由结果决定每份专家权重供多少 token 复用：少量 token 分散到很多专家，会产生许多小矩阵，每次读入的权重也用不了几次；集中到同一组专家，则能组成更大的矩阵。例题 2-4 展示的正是这两种访问方式的… |
| …行这些工作时使用多大的矩阵、按什么顺序执行。参数量相近的模型也可以有不同的计算图，甚至相近的矩阵 FLOPs 也可能伴随不同的缓冲区占用时间和串行步骤。 | …行这些工作时使用多大的矩阵、按什么顺序执行。参数量相近的模型也可以有不同的计算图；矩阵 FLOPs 相近的模型，缓冲区的占用时间和串行步骤也可能不同。 |
| 除了层数和宽度，还要看各层怎样连接。残差连接把子层输入直接加到变换结果上，使原始信息和梯度具有跨层直通路径。普通残差计算 $x_{\ell+1}=x_\ell+f_\ell(x_\e… | 除了层数和宽度，还要看各层怎样连接。残差连接把子层输入直接加到变换结果上，让原始信息和梯度有一条跨层直通的路径。普通残差计算 $x_{\ell+1}=x_\ell+f_\ell(x_\e… |
| …）**。它把一条残差通路扩展成多条，并约束通路之间的混合系数；这里的“流形约束”具体表现为将残差混合矩阵的元素限制为非负、各行与各列的和接近 1。[^architecture-motivation] | …）**。它把一条残差通路扩展成多条，并约束通路之间的混合系数；这里的“流形约束”，具体是把残差混合矩阵的元素限制为非负，且各行与各列的和接近 1。[^architecture-motivation] |
| …因此，加速效果取决于草稿生成、目标模型验证各需多久，以及每轮有多少 token 被接受。第 8 章将计算这三者的关系。 | …因此，加速效果取决于草稿生成、目标模型验证各需多久，以及每轮有多少 token 通过验证。第 8 章将计算这三者的关系。 |
| … ms；若只能得到一个，则需要 30 ms。多预测几个位置会增加单轮工作量，只有足够多的输出被采用，才能抵消新增的计算开销。 | … ms；若只能得到一个，则需要 30 ms。多预测几个位置会增加单轮工作量，只有采用的输出足够多，才能抵消新增的计算开销。 |
| …专家、Engram、其余组件。将各类参数相加，就得到模型总参数量。这种分解能直观地展示参数分布，也为解释下一张计算与状态表提供依据。 | …专家、Engram、其余组件。将各类参数相加，就得到模型总参数量。这种分解能直观展示参数的分布，也是解释下一张计算与状态表的依据。 |
| …，每条请求占 2.25 GiB，同一空间只能放两条；变为 4K 时，则能放九条。整数跳变来自剩余空间必须足以容纳一条请求的完整状态，而不是只够存放其中一部分。 | …，每条请求占 2.25 GiB，同一空间只能放两条；变为 4K 时，则能放九条。请求数按整数跳变，是因为剩余空间必须放得下一条请求的完整状态，只够放一部分不算数。 |
| …了保存这些信息的方式。模型结构、上下文组织与并发容量可以放在同一份预算里比较，再通过实际任务测试，确定哪些方案能够达到所需的回答质量。 | …了保存这些信息的方式。模型结构、上下文组织与并发容量可以放在同一份预算里比较，再用实际任务测试，确定哪些方案能达到所需的回答质量。 |
| …FFN，后 58 层每 token 选择 8 个路由专家并执行一个共享专家。下面沿展开 MLA 计算：每个 token 的潜变量经上投影形成各头的 $K$、$V$，64 维位置表示… | …FFN，后 58 层每 token 选择 8 个路由专家并执行一个共享专家。下面按展开路径计算 MLA：每个 token 的潜变量经上投影形成各头的 $K$、$V$，64 维位置表示… |
