--- before/manuscripts/01-初识 AI Infra.md
+++ manuscripts/01-初识 AI Infra.md
@@ -18,7 +18,7 @@
 
 如果要让这个服务检查代码，可以把代码、测试要求和可以调用的工具一同提供给模型，由模型提出修改方案或发起工具调用；应用程序执行测试，把结果加入下一次调用的上下文，再由模型继续判断。工具是供应用调用的程序功能，例如读取文件或运行测试。模型负责选择下一步做什么，宿主程序负责检查权限、执行操作和保存结果。完成一次任务可能需要多次模型调用和工具执行。
 
-**可编程性**是开发者通过某种接口改变系统行为的能力。传统应用主要用程序代码表达行为，编译器将代码转换为可执行指令，操作系统管理程序运行所需的资源。模型驱动的应用增加了另一个重要入口：通过选择模型、组织上下文和提供工具来表达任务。一部分原来需要逐条编写的行为规则，现在由模型根据上下文决定；代码继续组织调用流程，工具程序继续通过操作系统访问文件、网络和设备。
+**可编程性**是开发者通过某种接口改变系统行为的能力。传统应用主要用程序代码表达行为，编译器将代码转换为可执行指令，操作系统管理程序运行所需的资源。模型驱动的应用增加了另一个重要入口：通过选择模型、组织上下文和提供工具来表达任务。一部分原来需要逐条编写的行为规则，现在由模型根据上下文决定；代码继续组织调用流程，工具程序继续通过操作系统访问文件、网络和设备。[^abstraction]
 
 图 1-1 对照了这两种组织方式。右侧的 Agent 负责组织上下文、调用模型，并执行模型选择的工具。右侧的**模型接口**是应用向模型提交输入、取得输出的调用方式。接口背后的运行系统计算模型输出，并保存后续步骤需要复用的上下文状态；加速器是适合并行执行大量模型运算的处理器，内存保存参数与运行状态，互联在设备之间传递数据。开发者从上层改变任务，底层要把这些变化落实为具体运算与数据访问。
 
@@ -58,7 +58,7 @@
 
 沿这六层向下看，任务逐步变成具体的计算与数据访问；向上看，设备容量、算力与带宽决定系统能运行多大的模型、同时处理多少请求，以及多快作出响应。海思首席科学家廖恒博士提出的“十八层宝塔”，用来描述从应用、模型与软件系统，到芯片、制造工艺和底层物理的多个层次，强调这些层次之间的联系。本书据此组织全栈视角，并按教学需要将讨论内容归为图 1-2 的六层。[^panorama]
 
-这些层次也标出了可以共同优化的边界。模型改变状态表示，会改变存储和通信需求；编译器将相邻算子融合，会改变中间结果的保存位置；运行时让设备直接交接，会改变主机参与的次数。后续章节将从这些具体变化出发，计算省下的读写、提交和等待，再回到完整请求，判断哪一种组织方式更合适。
+这些层次也标出了可以共同优化的边界。模型改变状态表示，会改变存储和通信需求；编译器将相邻算子融合，会改变中间结果的保存位置；运行时让设备直接交接，会改变主机参与的次数。后续章节将从这些具体变化出发，计算省下的读写、提交和等待，再比较完整请求的耗时，判断哪一种组织方式更合适。
 
 AI Infra 建立在云数据中心已有的服务器、网络、存储和资源管理技术之上，但模型执行的需求改变了资源配置的重点。同一个模型可以承接问答、代码生成等多种应用；一次模型调用又可能分布在许多加速器上，使网络通信直接进入计算依赖链。基础设施既要服务独立请求，也要组织设备之间的紧密协作。
 
@@ -66,7 +66,7 @@
 
 ### 1.1.3 一次生成请求的处理过程
 
-服务实例是能够独立完成模型请求的一组执行资源，可以由一张或多张设备组成。一次请求由其中一个实例承接。图 1-3 展示了请求分配与实例内部执行的分工：服务路由器是选择请求执行位置的软件，选定实例后，由实例内部安排设备执行模型。先分清谁负责执行，才能知道一份权重应放在哪里、一次中间结果应传给谁。
+服务实例是能够独立完成模型请求的一组执行资源，可以由一个或多个设备组成。每次请求交由其中一个实例处理。图 1-3 展示了请求分配与实例内部执行的分工：服务路由器是选择请求执行位置的软件，选定实例后，由实例内部安排设备执行模型。先分清谁负责执行，才能知道一份权重应放在哪里、一次中间结果应传给谁。
 
 ![一次请求先由路由器选择模型实例，再由实例内部的调度器安排执行。灰色外框圈出同一个实例，实线表示请求和工作提交的方向。](ch01/figure-1-2-request.svg)
 
@@ -74,7 +74,7 @@
 
 上下文（context）是模型当前可使用的输入信息，包括用户输入、已有对话、检索结果和此前生成的内容。应用将这些信息组织成请求，交给模型服务入口。入口核验访问权限、检查配额和请求内容后，路由器依据目标模型、实例负载等条件选择副本。这里的副本指一个能够独立完成请求的模型实例，可以运行在一张卡上，也可以分布在多张卡上。路由选定实例之后，实例内部再安排这些设备协作。
 
-文本随后被转换成 token 序列。词元（token）是模型处理文本的基本单位，可能对应一个字、词的一部分或其他片段，因此 token 数与字数并不总是相同。文本转换可以由实例或共享前端完成。实例调度器将请求放入队列，将请求编入批次，并为其状态分配空间。CPU 上的运行时再向 GPU 提交计算任务，GPU 执行相应算子。[^request]
+文本随后被转换成 token 序列。词元（token）是模型处理文本的基本单位，可能对应一个字、词的一部分或其他片段，具体文本的 token 数由分词结果确定。文本转换可以由实例或共享前端完成。实例调度器将请求放入队列，将请求编入批次，并为其状态分配空间。CPU 上的运行时再向 GPU 提交计算任务，GPU 执行相应算子。[^request]
 
 模型先处理输入，这一阶段称为预填充（prefill）；随后逐步生成新 token，这一阶段称为解码（decode）。后续步骤会复用此前保存的状态，其中的 KV 缓存（KV cache）保存上下文位置产生的键和值两类向量，供后续位置计算注意力时复用。键和值是注意力机制使用的数据表示，第 2 章会从具体运算说明它们怎样产生。生成循环由实例内部的调度器持续推进，前一步输出成为下一步输入。产生的 token 经输出处理转换为文本，沿连接流式返回；如果输出要求调用工具，应用运行工具，把结果加入上下文，再发起后续调用。
 
@@ -84,7 +84,7 @@
 
 *图 1-4　权重加载一次，生成时逐步读取。蓝框表示显存中持续保存的同一份权重，三个绿框表示先后发生的计算；箭头表示读取或结果依赖，步骤间距不代表耗时。*
 
-这些软件功能最终要在机房中的设备上运行，图 1-5 展示了它们对应的硬件。数据中心包含入口与 CPU 服务、共享存储，以及由多个超节点构成的加速器资源池。这里把超节点理解为通过高带宽互联紧密协作的一组设备；它可能包含多个服务器或计算托盘，并不天然等同于一台服务器或一个机柜。
+这些软件功能最终要在机房中的设备上运行，图 1-5 展示了它们对应的硬件。数据中心包含入口与 CPU 服务、共享存储，以及由多个超节点构成的加速器资源池。超节点是通过高带宽互联紧密协作的一组设备，可以跨多个服务器或计算托盘组织。
 
 ![物理连接的两层视图。上方用数据中心网络连接服务与超节点，下方放大一个超节点，显示主机、网卡、GPU 与显存；线条展示数据路径，设备数量用于示意。](ch01/figure-1-3-datacenter.svg)
 
@@ -102,7 +102,7 @@
 
 一次模型执行既受单卡能力影响，也受设备连接方式影响。单卡的算力和存储带宽决定本地计算与读写的耗时，多卡协作则增加数据交换和等待。下面先估算单卡执行，再将同样的方法用于后续章节的超节点和网络分析。
 
-DeepSeek V4 到 V4.1 Flash 的演进提供了一条可以沿全书追踪的线索。模型团队从长上下文 Agent 的输入处理、KV 保存与迁移成本出发，改变了上下文表示、层间共享和缓存恢复方式。[^v41-case] 我们将围绕同一类会话展开讨论：Agent 等待工具返回，恢复此前上下文，处理新增输入，再继续生成。第 2 章计算状态的存储需求，第 3 章分解负载，第 4 至第 7 章追踪设备执行和数据路径，第 8、9 章讨论怎样缓存状态、分配请求，最后回到完整任务。Qwen3 的基础算例继续用于建立计算方法，V4／V4.1 用于检验模型与系统怎样共同改变。
+DeepSeek V4 到 V4.1 Flash 的演进提供了一条可以沿全书追踪的线索。模型团队从长上下文 Agent 的输入处理、KV 保存与迁移成本出发，改变了上下文表示、层间共享和缓存恢复方式。[^v41-case] 我们将围绕同一类会话展开讨论：Agent 等待工具返回，恢复此前上下文，处理新增输入，再继续生成。第 2 章计算状态的存储需求，第 3 章分解负载，第 4 至第 7 章追踪设备执行和数据路径，第 8、9 章讨论怎样缓存状态、分配请求，最后比较完整任务的执行效果。Qwen3 的基础算例继续用于建立计算方法，V4／V4.1 用于检验模型与系统怎样共同改变。
 
 ## 1.2 系统设计的关键数字
 
@@ -169,7 +169,7 @@
 
 **计算吞吐**回答单位时间内能完成多少运算。常见的 FLOP 表示一次浮点运算，FLOPs 表示运算总次数，FLOP/s 表示每秒的浮点运算数。GFLOPs、TFLOPs 分别表示十亿、万亿次浮点运算，GFLOP/s、TFLOP/s 则表示相应的每秒速率。矩阵计算中一次乘法加一次加法通常计为两次运算。比较峰值时，还必须说明输入精度、累加精度以及是否使用稀疏条件。稠密计算按完整矩阵执行；稀疏计算利用零元素或规定的稀疏结构跳过部分运算。估算时间时，所用算力必须与计算条件一致。
 
-**带宽**回答单位时间内能传送多少数据。显存带宽描述显存与芯片之间的数据搬运能力，卡间链路和网络带宽描述其他路径的能力。它们对应的接口不同，即使都使用 GB/s，也不能随意替换。
+**带宽**回答单位时间内能传送多少数据。显存带宽描述显存与芯片之间的数据搬运能力，卡间链路和网络带宽描述其他路径的能力。估算哪一段传输，就使用该段接口的带宽。
 
 **延迟**回答发起一次操作以后需要等多久。高带宽接口也可能有不可忽略的启动或往返时间。传输很大的数据块时，耗时往往主要取决于数据量与带宽的比值；请求很小、又必须一次接一次等待时，启动和往返时间就可能占据主要部分。
 
@@ -208,7 +208,7 @@
 
 *图 1-7　DeepSeek-R1-Distill-Llama-70B：BF16 权重共 141.11 GB，两卡均分后每卡约 70.55 GB；分组 8 比特量化后共 73.73 GB。各条使用相同尺度，虚线表示一张 H100 SXM 的名义 80 GB 容量。柱长只计权重及相应量化附加数据。*
 
-**把运行状态加入容量预算。** 生成文本时，显存还要保存上下文 token 的键和值，称为 **KV 缓存（key–value cache）**，以便后续计算直接复用；算子执行还需要临时工作区。因此，容量检查应写成
+**KV 缓存如何增加单请求的显存需求** 生成文本时，显存还要保存上下文 token 的键和值，称为 **KV 缓存（key–value cache）**，以便后续计算直接复用；算子执行还需要临时工作区。因此，容量检查应写成
 
 $$
 M_W+M_{\mathrm{state}}+M_{\mathrm{work}}\le M_{\mathrm{cap}}.
@@ -222,7 +222,7 @@
 
 > **练习 1-2〔核心〕：容量、位宽与逐卡放置**
 >
-> 使用上述真实模型的 BF16 权重 141.11 GB 和 8 比特量化权重 73.73 GB，每张卡容量为 80 GB。为每张卡预留 5 GB 状态与工作区，判断单卡、两卡均分能否容纳模型。再将两卡容量分别改为 48 GB 与 80 GB，判断两种精度各是否可行，并为可行情况给出一种权重分配。最后将 400 Gb/s 换成 GB/s，求传输 1 GB 的理想时间。
+> DeepSeek-R1-Distill-Llama-70B 的 BF16 权重占 141.11 GB，8 比特量化权重占 73.73 GB。假设每张卡有 80 GB 显存。为每张卡预留 5 GB 状态与工作区，分别判断单卡部署和将权重均分到两张卡的方案能否容纳模型。再将两卡容量分别改为 48 GB 与 80 GB，分别判断两种精度下能否完成部署，并为可行的方案给出一种权重分配方式。最后将 400 Gb/s 换成 GB/s，求传输 1 GB 的理想时间。
 
 只做几步乘除，我们就判断了权重能否放下，并算出了数据传输需要的时间。下面将容量、计算和读取放在一起，估算生成一个 token 的耗时。
 
@@ -230,13 +230,13 @@
 
 ### 1.3.1 生成一个 token，先检查什么
 
-**例题 1-1：为单步生成选择优化方向。** 给定一张 H100 SXM，能否让上一节的 DeepSeek-R1-Distill-Llama-70B 在 10 ms 内生成一个新 token？先考虑每参数一字节存储，再比较算力翻倍、带宽翻倍和批内权重复用。
+**例题 1-1：70B 模型的单步生成如何接近 10 ms 目标？** 给定一张 H100 SXM，能否让上一节的 DeepSeek-R1-Distill-Llama-70B 在 10 ms 内生成一个新 token？先考虑每参数一字节存储，再比较算力翻倍、带宽翻倍和批内权重复用。
 
 **解：先检查显存容量，再计算运算量和读取量。**
 
 为便于手算，将该模型的参数量近似取为 $N=70\times10^9$，先按每参数一字节估算主要权重数据，读入后转换为 BF16 参加矩阵运算。一个请求每步处理一个新 token，每步从显存读取一遍这些权重。
 
-**本例的估算条件。** 容量采用上一节计入量化附加数据的 73.73 GB 结果；本节先用 70 GB 近似主要权重的读取量。时间模型只计主要权重矩阵的乘加和一次完整权重读取，采用资源表中的 BF16 矩阵峰值与 HBM 带宽；读取和计算按充分重叠估算。上下文状态访问、格式转换和程序启动在后续模型中逐项加入。这一题设用来识别当前最紧的资源约束。
+**估算条件：权重读取与矩阵计算充分重叠** 容量采用上一节计入量化附加数据的 73.73 GB 结果；本节先用 70 GB 近似主要权重的读取量。时间模型只计主要权重矩阵的乘加和一次完整权重读取，采用资源表中的 BF16 矩阵峰值与 HBM 带宽；读取和计算按充分重叠估算。上下文状态访问、格式转换和程序启动在后续模型中逐项加入。这一题设用来识别当前最紧的资源约束。
 
 上一节已经检查过单请求的容量预算。现在看生成速度：权重加载到显存之后，每一步仍需将参与计算的权重送到计算单元。按上述近似，每步读取约 70 GB，下面先计算读取这些数据需要多久。
 
@@ -280,9 +280,9 @@
 T_{\mathrm{step}}\ge T_{\mathrm{lower}}=\max\!\left(\frac{F}{\Pi},\frac{R_W}{\beta}\right).
 $$
 
-进入稳定阶段后，每块数据都要经过读取和计算，较慢的一项决定处理速度。本例中，读取权重远慢于矩阵计算，因此优化应首先针对这 20.90 ms 的读取时间。后文分析实际程序时，还会计入启动、流水填充和逐层等待。
-
-**讨论：改变资源或执行组织。** 仅权重读取就至少需要 20.90 ms，已经超过 10 ms 的目标。虽然显存能容纳这些数据，读取速度仍然达不到要求。现在做两个最简单的改动。
+进入稳定阶段后，每块数据都要经过读取和计算，较慢的一项决定处理速度。本例中，读取权重远慢于矩阵计算，因此优化应首先针对这 20.90 ms 的读取时间。
+
+**讨论：算力、带宽与批内复用分别能缩短多少生成时间？** 仅权重读取就至少需要 20.90 ms，已经超过 10 ms 的目标。虽然显存能容纳这些数据，读取速度仍然达不到要求。现在做两个最简单的改动。
 
 把计算能力翻倍，矩阵计算项从约 0.1415 ms 变为 0.0707 ms，纯权重读取仍需要约 20.90 ms，决定执行时间的读取下界保持不变。把 HBM 带宽翻倍，读取项才会降为约 10.45 ms，当前下界随之下降。两种改动都使一项资源更强，却没有相同的任务收益。
 
@@ -322,7 +322,7 @@
 
 > **练习 1-3〔核心〕：改变资源能否满足时限**
 >
-> 沿例题 1-1，将有效带宽设为标称值的 70%，计算吞吐设为峰值的 50%。分别取 $B=1,16,64$，计算整批时间下界、每输出分摊时间与理想吞吐。目标是每请求每步不超过 10 ms：哪些 batch 可被下界直接排除？再把权重读取量减半、运算量保持不变，重新判断。求计算与读取相等时的 $B_*$，并解释为什么增大 batch 不能无限降低每请求延迟。
+> 采用例题 1-1 的模型与设备，将有效带宽设为标称值的 70%，有效计算吞吐率设为峰值的 50%。分别取 $B=1,16,64$，计算整批执行时间的下界、平均每个输出 token 分摊的执行时间，以及理想吞吐率。目标是每请求每步不超过 10 ms：哪些批量设置仅根据时间下界就可以判定无法满足目标？再把权重读取量减半、运算量保持不变，重新判断。求计算时间与读取时间相等时的批量 $B_*$，并解释为什么增大 batch 不能无限降低每请求延迟。
 
 ### 1.3.3 用实测检验估算
 
@@ -378,7 +378,7 @@
 
 微软 Azure 在 2015 年设计新一代 40 Gb/s 云服务器时，面临另一类增长：网络带宽提高，虚拟网络策略也越来越复杂。**SmartNIC（智能网卡）**是能够在网卡上执行部分数据处理的设备。微软从 2015 年末起在新部署的 Azure 服务器中安装基于 FPGA 的 SmartNIC，2016 年向客户提供 Accelerated Networking（加速网络）服务。FPGA 是可通过编程配置硬件逻辑的芯片，使网络处理能够随策略更新而改变。[^azure]
 
-这项设计要同时满足两个要求：把更多 CPU 核留给租户的应用，又能快速更新网络隔离、转发和访问控制规则。云服务器上的网络处理就是一个例子：CPU 除了运行应用，还要处理收发的数据包。虚拟机是在一台物理主机上划分出的独立计算环境。多个虚拟机共用物理网络时，平台需要识别数据的归属、查找转发规则、完成封装，并维持隔离。网络中的字节抵达服务器，并不意味着这些工作已经完成。
+这项设计要同时满足两个要求：把更多 CPU 核留给租户的应用，又能快速更新网络隔离、转发和访问控制规则。云服务器上的网络处理就是一个例子：CPU 除了运行应用，还要处理收发的数据包。虚拟机是在一台物理主机上划分出的独立计算环境。多个虚拟机共用物理网络时，平台需要识别数据的归属、查找转发规则、完成封装，并维持隔离。
 
 先估算这部分工作占用多少 CPU。40 Gb/s 链路在最小帧条件下每秒约需处理六千万个包。若一个核每秒处理一千万至两千万个包，持续转发所需核数约为
 
@@ -414,7 +414,7 @@
 
 再看开篇的六层图，三个案例分别从不同问题出发，却都会改变计算与数据移动的组织。应用需求推动资源专用化，处理位置改变主机与网卡之间的工作分配，更大的模型则要求重新安排设备协作。后面的章节会沿这些具体关系展开本书的主线：**数据搬运塑造了 AI Infra 的架构。**
 
-回到两卡模型：权重均分可以满足逐卡容量约束，但每个阶段还要等待输入数据和前序结果。增加设备改变了 $M_{\mathrm{cap}}$ 和可用算力，也引入新的通信量与依赖。因此，第一步先检查每台设备能否放下所需数据，第二步计算各设备的运算量与读写量，第三步根据执行依赖计算总时间。模型再大、设备再多，这个分析顺序仍然成立。
+以本章的两卡模型部署为例：权重均分可以满足逐卡容量约束，但每个阶段还要等待输入数据和前序结果。增加设备改变了 $M_{\mathrm{cap}}$ 和可用算力，也引入新的通信量与依赖。因此，第一步先检查每台设备能否放下所需数据，第二步计算各设备的运算量与读写量，第三步根据执行依赖计算总时间。模型再大、设备再多，这个分析顺序仍然成立。
 
 ## 谬误与陷阱
 
@@ -438,11 +438,11 @@
 | 一步 decode 的主要权重读取 | 15.14 GB | 权重读一次；嵌入层只读取当前 token 对应的一行 |
 | 一步 decode 的旧 KV 读取 | 0.302 GB | 每份上下文 KV 读一次时的字节数 |
 
-**将需求换算为时间。** 先单独计算矩阵运算和数据读取：按 H100 的 989.4 TFLOP/s 矩阵峰值换算，prefill 的矩阵运算约需 30.0 ms，decode 约需 0.0165 ms；按 3.35 TB/s 显存带宽，一步 decode 的主要权重与旧 KV 读取约需 4.61 ms。这里分别给出资源时间下界，完整执行还要沿算子顺序加入其他运算、实际访存和启动时间。第二至第五章将逐项建立这些计算。
+**用计算量与读取量估算执行时间下界** 先单独计算矩阵运算和数据读取：按 H100 的 989.4 TFLOP/s 矩阵峰值换算，prefill 的矩阵运算约需 30.0 ms，decode 约需 0.0165 ms；按 3.35 TB/s 显存带宽，一步 decode 的主要权重与旧 KV 读取约需 4.61 ms。这里分别估算计算与读取所需的最短时间，完整执行还要沿算子顺序加入其他运算、实际访存和启动时间。第二至第五章将逐项建立这些计算。
 
 这组数字解释了两阶段的差别：prefill 在一次调用中处理大量输入，矩阵工作量较大；单请求 decode 每次只处理一个位置，却仍要读取大量权重和上下文。提高计算吞吐、提高带宽和增加批内复用，分别作用于不同的时间项。
 
-需要查实测响应时，回到图 1-13、1-14：那里使用 RTX PRO 6000 Blackwell、vLLM 0.23 和明确的请求长度，显示吞吐与客户端输出间隔。GPU 规格表提供硬件性能数据，模型表用于估算工作量，实测图则展示系统实际运行的表现。
+实际响应表现可参见图 1-13、1-14：那里使用 RTX PRO 6000 Blackwell、vLLM 0.23 和明确的请求长度，显示吞吐与客户端输出间隔。GPU 规格表提供硬件性能数据，模型表用于估算工作量，实测图则展示系统实际运行的表现。
 
 [^panorama]: “十八层宝塔”的提出者为海思首席科学家廖恒博士。全栈讨论的背景资料见[《Unified Bus 背后的思考》本地原件](../references/files/documents/ub-reflection.md)及[系统抽象边界取材记录](../research/system-abstraction-boundary/report.md)。六层图为本书自绘的教学分组。
 
--- before/manuscripts/02-模型架构.md
+++ manuscripts/02-模型架构.md
@@ -36,7 +36,7 @@
 
 > **练习 2-1〔延伸〕：从依赖图判断并行**
 >
-> 画出四个已知位置、三层 RNN 与因果 Transformer 的依赖。再追加一个未知输出，指出等待来自哪个节点。设每层每位置的计算耗时为一个单位、并行资源充足，分别求完成已知输入的最长依赖链长度；解释增加计算单元可以缩短哪些等待、不能缩短哪些等待。
+> 对于含四个已知输入位置的序列，分别画出三层 RNN 和三层因果 Transformer 的计算依赖图。再增加一个待生成的输出位置，指出计算该位置之前必须等待哪个节点完成。设每层每位置的计算耗时为一个单位、并行资源充足，分别求完成已知输入的最长依赖链长度；解释增加计算单元可以缩短哪些等待、不能缩短哪些等待。
 
 ### 2.1.2 模型配置与张量结构
 
@@ -75,7 +75,7 @@
 
 **例题 2-1：从矩阵尺寸求一层的参数量与运算量。** Qwen3-8B 的查询和输出投影宽度为 4096，键和值投影宽度为 1024，FFN 中间维度为 12288。求四个注意力投影与三个 FFN 矩阵的参数量，以及为本次输入的 $m$ 个 token 完成这些投影和 FFN 计算所需的运算量。每个 token 在这一层都有一个 4096 维向量，作为输入矩阵的一行；$m$ 统计本次实际参与计算的 token 位置数。同一词元出现在两个位置时计作两个 token。若有 $B$ 条等长请求、每条本次处理 $P$ 个 token，则 $m=BP$。例如，一条请求处理 128 个输入 token 时 $m=128$；八条请求各推进一个 token 时 $m=8$。
 
-**解：** 查询与输出各使用一个 $4096\times4096$ 矩阵，键和值各使用一个 $4096\times1024$ 矩阵，三个 FFN 矩阵各有 $4096\times12288$ 个参数。因此，
+解： 查询与输出各使用一个 $4096\times4096$ 矩阵，键和值各使用一个 $4096\times1024$ 矩阵，三个 FFN 矩阵各有 $4096\times12288$ 个参数。因此，
 
 $$
 \begin{aligned}
@@ -207,7 +207,7 @@
 
 > **练习 2-2〔核心〕：输入行数相同，注意力为何不同**
 >
-> 使用表 2-1，取 $B=4,S=4096,P=1024$，计算投影与 FFN、有效因果注意力和末位置词表头的 FLOPs。保持 $BP=4096$，改为 $B=1,S=4096,P=4096$，先预测哪些项相同，再计算差异。最后比较完整 8K prefill 与恢复 6144 个前缀后只处理 2048 个新位置，求矩阵运算节省比例。
+> 使用表 2-1，取 $B=4,S=4096,P=1024$，分别计算投影与 FFN、有效因果注意力，以及最后一个位置的词表输出层所需的 FLOPs。保持 $BP=4096$，改为 $B=1,S=4096,P=4096$，先预测哪些项相同，再计算差异。最后比较两种输入处理方式：完整处理 8K 输入，或复用前 6144 个位置的 KV、只处理新增的 2048 个位置。求后一种方式节省的矩阵运算比例。
 
 ## 2.2 自回归生成的资源消耗计算
 
@@ -249,7 +249,7 @@
 
 **例题 2-2：增加 batch 后，上下文读取量何时超过权重读取量？** Qwen3-8B 每批将所需共享权重读取一遍，数据量约为 15.14 GB，嵌入按请求查行另计。每请求的 8K BF16 上下文为 1.125 GiB，假定各请求独立、旧上下文各读一次。
 
-**解：** 每批共享权重读取量记为 $R_W$，各请求的上下文独立，则上下文读取量为 $Bc_{\mathrm{KV}}H$。两者相等时
+解： 每批共享权重读取量记为 $R_W$，各请求的上下文独立，则上下文读取量为 $Bc_{\mathrm{KV}}H$。两者相等时
 
 $$
 B_* = \frac{R_W}{c_{\mathrm{KV}}H}.
@@ -279,9 +279,9 @@
 
 decode 新增的状态大小为 $c_{\mathrm{KV}}n_d$；若保留整个上下文，最终有效 KV 为 $c_{\mathrm{KV}}(H+n_d)$。本次请求的全部新增 KV 还应包含 prefill 写入的 $c_{\mathrm{KV}}P$。已有前缀的恢复与传输单独核算。求和中的每一项都对应一次 decode 开始时已有的上下文：早出现的位置参与更多后续查询，晚出现的位置参与较少查询。
 
-**例题 2-3：一份上下文为什么会被读取一千多 GiB？** 求请求结束时保存的 KV 大小，以及生成期间读取已有 KV 的总量。
-
-**解：** 取 $S=0$、$P=8192$、$G=1025$，则 $n_d=1024$，累计访问 8,912,384 个上下文位置。每个位置占 144 KiB，因此累计读取约 1224 GiB。生成期间只新增 144 MiB KV，结束时共保存约 1.27 GiB。累计读取量是最终保存量的近千倍，因为较早生成的上下文被后续查询反复使用。
+**例题 2-3：逐步生成如何累积出千 GiB 级的 KV 读取量？** 求请求结束时保存的 KV 大小，以及生成期间读取已有 KV 的总量。
+
+解： 取 $S=0$、$P=8192$、$G=1025$，则 $n_d=1024$，累计访问 8,912,384 个上下文位置。每个位置占 144 KiB，因此累计读取约 1224 GiB。生成期间只新增 144 MiB KV，结束时共保存约 1.27 GiB。累计读取量是最终保存量的近千倍，因为较早生成的上下文被后续查询反复使用。
 
 这组调用也决定了总运算量。线性投影和 FFN 在每次 decode 中处理一行，上下文交互则随 $H+j$ 增长。设每步固定的矩阵运算量为 $F_0$、与每个上下文位置交互所需的运算量为 $a$，便有
 
@@ -309,7 +309,7 @@
 
 *图 2-9　固定四个查询头，分别为每头、每组和全部查询保存 KV。连线表示使用关系；共享上下文后，各查询仍分别产生自己的评分与输出。*
 
-回到 Qwen3-8B：它让每四个查询头共用一组 KV，总计 32 个查询头、8 个 KV 头。如果给每个查询头单独保存 KV，容量会是当前配置的四倍；如果全部查询共享一组，则会是当前配置的八分之一。实际 GQA 配置保存 8192 个位置需要约 1.13 GiB。本节末的容量图固定上下文长度与头宽，只改变 KV 组数，因此柱高之比直接呈现共享比例。
+Qwen3-8B 采用这种共享方式，让每四个查询头共用一组 KV，总计 32 个查询头、8 个 KV 头。如果给每个查询头单独保存 KV，容量会是当前配置的四倍；如果全部查询共享一组，则会是当前配置的八分之一。实际 GQA 配置保存 8192 个位置需要约 1.13 GiB。本节末的容量图固定上下文长度与头宽，只改变 KV 组数，因此柱高之比直接呈现共享比例。
 
 共享 $K$、$V$ 改变了被查询的数据，32 个 $Q$ 头仍分别计算分数和加权输出。因此，KV 投影和保存量按组数减少，$QK^{\mathsf T}$ 与 $AV$ 交互仍按查询头数累计。一次读入的 $K$、$V$ 还可服务同组多个查询头，从而在实际计算中复用同一份数据。
 
@@ -319,7 +319,7 @@
 
 *图 2-10　固定 Qwen 层数、头宽、8192 个上下文位置与 BF16，只改变 KV 组数。实际模型采用 GQA；其余两柱是用于分析共享比例的结构变体。*
 
-KV 共享也展示了基础设施的限制如何影响模型设计。MQA 的原论文直接把增量推理中的内存带宽开销作为动机；GQA 则在 MHA 的质量与 MQA 的速度之间寻找折中。[^codesign] 减少 KV 头数既改变模型可表达的注意力关系，也减少服务端必须保存和读取的状态。因此，比较结构不能只看参数和精度，还应在目标上下文长度、并发和设备上比较实际服务能力。
+KV 共享也展示了基础设施的限制如何影响模型设计。MQA 的原论文直接把增量推理中的内存带宽开销作为动机；GQA 则在 MHA 的质量与 MQA 的速度之间寻找折中。[^codesign] 减少 KV 头数既改变模型可表达的注意力关系，也减少服务端必须保存和读取的状态。
 
 ### 2.3.2 潜变量压缩与 MLA 执行路径
 
@@ -357,9 +357,9 @@
 
 先把这里的三个对象分开。**局部滑动窗口注意力（Sliding-Window Attention，SWA）**直接读取最近的一段位置；窗口长 128 时，较早位置退出这一局部窗口。**全局主 KV**是供主注意力读取的长期表示，可能已将多个位置合并；“全局”说明它覆盖较长历史，不表示每次都读取全部条目。**索引 K**是用于检索的另一份较小表示：索引器先用它给条目打分，再让主注意力读取选中的主 KV。索引分数用于选位置，主注意力另用自己的查询计算最终汇总权重。
 
-这里的压缩是模型学出来的信息汇总，不是可还原原文的无损文件压缩。一条 512 维主 KV 记录可以兼作注意力中的键和值，并由多个查询头共享；因此，统计这种共享表示时不能再乘上独立 K、V 两份或查询头数。SWA 与全局分支把近期细节和较早信息一起提供给当前查询。
-
-DeepSeek V4-Flash 有 43 个主干层、4096 隐藏维度、64 个注意力头，每头维度 512。$Q$ 先从 4096 维投影到 1024 维，再得到 $64\times 512$ 维查询；共享 KV 从 4096 维投影到 512 维；输出采用分 8 组的低秩投影。投影宽度并不必然等于隐藏维度，各投影的输出维度决定了矩阵的第二维，不能一律写成 $d\times d$。
+模型通过训练学会把一段输入汇总为共享表示。一条 512 维主 KV 记录兼作注意力中的键和值，供多个查询头共享，容量按一份记录计算。SWA 保存近期细节，全局分支提供较早信息，两者共同供当前查询使用。
+
+DeepSeek V4-Flash 有 43 个主干层、4096 隐藏维度、64 个注意力头，每头维度 512。$Q$ 先从 4096 维投影到 1024 维，再得到 $64\times 512$ 维查询；共享 KV 从 4096 维投影到 512 维；输出采用分 8 组的低秩投影。各投影的输入、输出维度分别确定矩阵形状。
 
 DeepSeek V4-Flash 使用两种压缩注意力：**压缩稀疏注意力（Compressed Sparse Attention，CSA）**先压缩上下文，再通过索引选取相关条目；**高压缩注意力（Heavily Compressed Attention，HCA）**把更多位置汇成较少的粗粒度条目，供查询访问。两者都用于减少长上下文的存储与访问量，区别在于压缩程度和是否进一步筛选条目。其主干包含 2 个纯窗口层、21 个 CSA 层和 20 个 HCA 层，窗口长度为 128。CSA 按压缩比 4 保存上下文，并维护索引；HCA 按压缩比 128 保存更粗的上下文。用 BF16 参考表示，CSA 每个完成的压缩条目含 512 维主状态，另有 128 维索引条目。已有 $S$ 个位置时，已生成的条目数按 $\lfloor S/4\rfloor$ 计算；尚未完成的压缩块还需独立缓冲。
 
@@ -385,7 +385,7 @@
 
 > **练习 2-4〔延伸〕：共享、压缩与实际访问**
 >
-> 先把 Qwen3-8B 的 KV 头数从 8 改为 4，保持 32 个查询头不变，分别求 KV 容量、KV 投影参数与查询—上下文运算的变化。再对 DeepSeek V4-Flash 的 CSA 层，把上下文从 8192 增至 16384，按压缩比 4、每次最多选择 512 个压缩条目，计算保存条目数、索引扫描条目数和选中条目上限。解释三者为何不按同一比例增长。
+> 先把 Qwen3-8B 的 KV 头数从 8 改为 4，保持 32 个查询头不变，分别计算 KV 容量、KV 投影参数量，以及查询与上下文之间的运算量如何变化。再对 DeepSeek V4-Flash 的 CSA 层，把上下文从 8192 增至 16384，按压缩比 4、每次最多选择 512 个压缩条目，计算保存条目数、索引扫描条目数和选中条目上限。解释三者为何不按同一比例增长。
 
 ### 2.3.4 线性注意力与有限状态递推
 
@@ -443,13 +443,13 @@
 
 > **练习 2-5〔核心〕：固定状态与增长状态的交点**
 >
-> 对 Qwen3.6，取固定状态 61.875 MiB、每个上下文位置 20 KiB；对 Kimi K3 紧凑表示，取递推 414 MiB、卷积 20,348,928 字节、每位置 27 KiB。分别求固定项与增长项相等的上下文长度，以及 32K、128K 上下文的总状态。若只有 256 MiB 状态预算，两个模型各最多保存多少上下文？说明预算不足固定项时，缩短上下文为什么不能解决问题。
+> 对 Qwen3.6，取固定状态 61.875 MiB、每个上下文位置 20 KiB；对 Kimi K3 的紧凑表示，递推状态占 414 MiB，卷积状态占 20,348,928 字节，每个上下文位置另占 27 KiB。分别求固定项与增长项相等的上下文长度，以及 32K、128K 上下文的总状态。若只有 256 MiB 状态预算，两个模型各自最多能保存多少个上下文位置？说明容量预算小于固定状态大小时，为什么缩短上下文仍无法容纳模型状态。
 
 ### 2.3.6 每个 token 存多少，每次 decode 读多少
 
 比较模型的上下文成本，需要同时给出三个量：全局历史每增加一个 token 的平均存储增量、长度为 $N$ 时实际保存的状态，以及一次 decode 查询需要读取的历史。全局 KV 会随上下文增长；滑动窗口只保留最近一段；递推状态的大小固定，却仍要在每一步读写。把三者都除以上下文长度，会掩盖它们不同的增长规律。
 
-下面考虑一条请求，可访问的上下文长度为 $N=8192$。读取量指当前查询在各层需要访问的逻辑数据量，每份选中的 K/V 或共享潜变量在一层内理想读取一次，并供各查询头及 QK/PV 复用；索引扫描另计。它不等于实测 HBM 流量，实际实现还可能重读，也可能通过片上缓存和跨层复用减少外存读取。可见历史包含当前查询位置；下一次追加的容量增量单独计算。表中注意力 KV 读取包含局部 SWA 窗口，不含递推矩阵、卷积与压缩器的更新。
+下面考虑一条请求，当前查询可访问的上下文共 $N=8192$ 个位置，包含查询位置自身。表中按各行指定的精度和缓存路径计算全局历史容量，以及一次查询的逻辑读取量：每层选中的 K/V 或共享潜变量读取一次，供各查询头的 QK 与 PV 计算复用，再加上局部 SWA 窗口与索引扫描。递推矩阵、卷积和压缩器的状态更新在表后分别讨论。
 
 | 模型与缓存路径 | 全局历史平均增长（B/token） | 8K 全局历史（MiB） | 每次 decode 注意力 KV＋索引读（MiB） |
 | --- | ---: | ---: | ---: |
@@ -463,11 +463,11 @@
 | DeepSeek V4 Flash，生产混合格式 | 3,514.25 | 27.455 | 12.556 |
 | DeepSeek V4.1 Flash，生产混合格式 | 890 | 6.953 | 11.375 |
 
-表中结果按各行指定的精度和执行方式计算。Kimi K3 的紧凑 MLA 是显式采用的潜空间执行路径，本书采用的 HF 参考实现版本使用展开的 K/V；展开路径的全局增长为 1,474,560 B/token。完整对照表同时保留这两种路径、V4 的 BF16 参考路径、V4 Pro、DeepSeek V3 和 70B 蒸馏模型，不能将本表视为相同质量条件下的性能排名。[跨模型缓存计算](../calculations/results/kv-comparison-n8192-b1.md)给出精确字节及各项假设。
-
-混合注意力还要加固定状态。在本章 FP32 递推、BF16 短卷积的条件下，Qwen3.5 的递推矩阵与卷积槽合计 184.219 MiB，Qwen3.6 为 61.875 MiB，Kimi K3 为 433.406 MiB。它们不计入表中的“全局历史增长”，但计入当前容量。一次 decode 还要读写递推矩阵并更新卷积状态，所以表中主历史读取较少并不表示其全部状态访问同样少。
-
-理解 V4.1 时，可以先把“生成全局表示”和“查询全局表示”分开。它把 40 层文本主干按顺序分成前 20 层**因果编码器**和后 20 层**解码器**，称为因果编码器—解码器（Causal Encoder-Decoder，CED）。这里的编码器仍只看当前位置及其之前的信息，不是可以读取未来位置的双向编码器；两部分属于同一个自回归模型，也不天然对应两台机器。
+Kimi K3 的两种 MLA 表示展示了缓存路径对容量的影响：表中的紧凑路径每个位置增加 27,648 B，本书采用的 HF 参考实现将 K/V 展开，每个位置增加 1,474,560 B。[跨模型缓存计算](../calculations/results/kv-comparison-n8192-b1.md)列出了两种路径的精确字节数。
+
+混合注意力还要加固定状态。在本章 FP32 递推、BF16 短卷积的条件下，Qwen3.5 的递推矩阵与卷积槽合计 184.219 MiB，Qwen3.6 为 61.875 MiB，Kimi K3 为 433.406 MiB。这些固定状态与表中的历史缓存共同构成当前容量；一次 decode 还要读写递推矩阵并更新卷积槽。
+
+理解 V4.1 时，可以先把“生成全局表示”和“查询全局表示”分开。它把 40 层文本主干按顺序分成前 20 层**因果编码器**和后 20 层**解码器**，称为因果编码器—解码器（Causal Encoder-Decoder，CED）。两部分属于同一个自回归模型，编码器中的每个位置只访问当前位置及其之前的信息。
 
 编码器先处理输入。解码器需要的全局 KV 由编码器末层表示经过投影生成，而不必先运行每个输入位置的全部解码器层。解码器每层的局部 SWA 则仍依赖该层自己的输入表示，因此要把提示末尾最多 128 个位置的编码器输出送入解码器，构建局部状态。开始逐 token 生成后，新位置依次经过编码器和解码器的全部 40 层。第三章再计算省下了哪些输入工作，第八章区分编码器和解码器的两种重放。
 
@@ -495,17 +495,17 @@
 21\frac{584+68}{4}+20\frac{584}{128}=3514.25\ \mathrm{B/token}.
 $$
 
-这复现了官方图中取整后的 3,514 与 890，约为 3.95 倍的存储差距。2.3.3 的旧例子采用 BF16 参考表示并加入压缩器缓冲，因此与这里的生产量化全局 KV 数值不同。V4.1 还需另外保存局部窗口：每层最多 128 条、每条 528 B 的 FP8+scale 记录，40 层合计 2.578 MiB；压缩器及运行时工作区仍需另算。因而 890 B/token 不能当成整个请求的显存需求。
-
-平均增长也不等于每次追加的写入。对 V4.1，全局缓存从偶数长度增长到下一个奇数长度只增加 356 B；到下一个偶数长度时，三个 2:1 压缩器同时完成，增加 1,424 B。窗口满后继续覆盖旧槽，容量不再增长，但仍然产生写入：40 层 SWA 每步合计写入 $40\times528=21{,}120$ B，压缩器更新另计。356／1,424 B 只表示新增全局记录，不能代替全部缓存写入。另一方面，共享同一份全局缓存的各层仍需读取数据、部分层还会重新索引；8K 场景下，两代 Flash 的注意力 KV（含局部 SWA）与索引逻辑读取量约为 12.56 和 11.38 MiB，并没有随全局存储等比例下降。
-
-CED 的阶段差异也反映在官方约 8B／16B 的输入／输出激活参数标签中；它们不能代替完整阶段 FLOPs，也不能套用旧 V4 Flash 的对称全层前向。新版本还包含 Engram（按 token 组合查表的条件记忆）、视觉编码器与 DSpark（先提出多个草稿 token 再验证的推测解码模块）。这些分支有各自的参数与执行成本，因此本书保留已核对的 V4 算例，把 V4.1 作为独立版本扩展。[固定配置与版本比较](../calculations/research/deepseek-v41-flash/README.md)记录了计算来源和覆盖范围。
-
-缓存复用与位置选择还可以分别安排。**压缩稀疏注意力第二版（Compressed Sparse Attention 2，CSA2）**把层分成三种模式。这里的 **Full** 表示完整执行“生成全局 KV、索引、选取和注意力”的流程，并不表示读取全部历史的稠密注意力。CSA2 的 Full 层生成全局 KV 并选择位置；Reindex 层共享 KV，但用自己的索引查询重新选取 Top-K；Reuse 层连同所选位置一起复用。V4.1 的 38 个全局注意力层中，有 4 个 Full、4 个 Reindex 和 30 个 Reuse；另外两个层仅使用 SWA。解码器的首个 Full 层扫描全局，并选出最多 $2048\times8=16{,}384$ 个候选位置；技术报告第 2.3.2 节将这个集合称为**候选池（candidate pool）**，其中的候选是可供注意力检索的位置。后续 Reindex 层只在池内各自选取最多 512 个条目。[^v41-candidate]后续层的搜索量因此有了上限，初始全局扫描仍随上下文增长。
-
-例如，某次查询面对 131,072 个全局位置，将它们按每块 8 个分成 16,384 块。首个解码器 Full 层先为所有位置评分，以块内最高分作为块分数，再选出 2,048 块，形成 16,384 个候选位置。它自己仍从全局分数选取 Top-512；后续 Reindex 层在候选内用新的索引查询重算分数，各自选出 512 个位置。候选池是允许搜索的范围，Top-512 才是该层实际选中的条目，二者不是同一个集合。“块”在这里用于筛选位置，也不等同于服务端的 KV 分页块。
-
-这一安排也提出了质量问题：如果首个 Full 层没有把相关信息所在块纳入候选池，后续 Reindex 无法在本次查询中重新选入那个位置；局部注意力和其他信息通路仍可能提供帮助。训练需要在这样的选择约束下学习，而不能假定所有未读信息都不重要。共享、稀疏选择、量化和近似恢复分别限制不同自由度；两代模型的规模、数据与训练同时变化，发布成绩不能单独测出每项限制的损失。报告也提出了进一步检验稀疏选择和恢复机制适用范围的方向。第 8 章将把这一问题落实为缓存恢复的对照实验。
+两者的全局 KV 平均增量相差约 3.95 倍，对应官方图中取整后的 3,514 与 890 B/token。全局历史之外，V4.1 的局部窗口每层最多保存 128 条记录，每条 FP8+scale 记录占 528 B，40 层合计 2.578 MiB。全局历史随上下文增长，局部窗口则在填满后保持固定容量。
+
+沿生成过程逐步观察，还能看出全局缓存的增长节奏。对 V4.1，全局缓存从偶数长度增长到下一个奇数长度只增加 356 B；到下一个偶数长度时，三个 2:1 压缩器同时完成，增加 1,424 B。窗口满后继续覆盖旧槽，容量不再增长，但仍然产生写入：40 层 SWA 每步合计写入 $40\times528=21{,}120$ B，压缩器更新另计。
+
+共享同一份全局缓存的各层仍需读取数据、部分层还会重新索引；8K 场景下，两代 Flash 的注意力 KV（含局部 SWA）与索引逻辑读取量约为 12.56 和 11.38 MiB，跨层共享主要减少了重复保存的空间。
+
+缓存复用与位置选择还可以分别安排。**压缩稀疏注意力第二版（Compressed Sparse Attention 2，CSA2）**把层分成三种模式。**Full** 层完整执行“生成全局 KV、索引、选取和注意力”的流程。**Reindex** 层共享 KV，用自己的索引查询重新选取 Top-K；**Reuse** 层连同所选位置一起复用。V4.1 的 38 个全局注意力层中，有 4 个 Full、4 个 Reindex 和 30 个 Reuse；另外两个层仅使用 SWA。解码器的首个 Full 层扫描全局，并选出最多 $2048\times8=16{,}384$ 个候选位置；技术报告第 2.3.2 节将这个集合称为**候选池（candidate pool）**，其中的候选是可供注意力检索的位置。后续 Reindex 层只在池内各自选取最多 512 个条目。[^v41-candidate]后续层的搜索量因此有了上限，初始全局扫描仍随上下文增长。
+
+例如，某次查询面对 131,072 个全局位置，将它们按每块 8 个分成 16,384 块。首个解码器 Full 层先为所有位置评分，以块内最高分作为块分数，再选出 2,048 块，形成 16,384 个候选位置。它自己仍从全局分数选取 Top-512；后续 Reindex 层在候选内用新的索引查询重算分数，各自选出 512 个位置。候选池限定后续层的搜索范围，各层的 Top-512 决定本层最终读取哪些条目。
+
+候选池让后续层的搜索量有了上限，也使首轮筛选影响后续选择：某个位置一旦落在候选池之外，本次查询的后续 Reindex 层便无法再选入它。模型训练需要适应这一搜索范围，第十章将说明 V4.1 怎样把候选限制纳入后训练。
 
 ![V4.1 解码器的层级位置选择。首个 Full 层先扫描全局，构造最多 16,384 个候选；后续 Reindex 在候选内重新选 512 个，Reuse 使用已有选择。示意位置数量不按比例；各层独立 Q 与局部 SWA 未画出。](ch02/figure-2-v41-selection.svg)
 
@@ -531,7 +531,7 @@
 
 **例题 2-4：相同专家计算量，权重读取为何相差 32 倍？** 对 Qwen3.6 的同一层，比较均匀分派和集中分派。
 
-**解：** 即使激活参数相同，batch 内的权重读取也可能不同。假设同时为 $B=64$ 条请求各执行一步 decode，每条请求本次输入一个 token，因此 $m=B=64$。每个 token 选择 8 个专家，总共有 512 次 token–expert 分派。若均匀分到 256 个专家，每个专家平均接收 2 个 token，当前批次会访问全部路由专家；若所有 token 都集中到同 8 个专家，则每个专家接收 64 个 token，当前批次只访问 8 份专家权重。
+解： 即使激活参数相同，batch 内的权重读取也可能不同。假设同时为 $B=64$ 条请求各执行一步 decode，每条请求本次输入一个 token，因此 $m=B=64$。每个 token 选择 8 个专家，总共有 512 次 token–expert 分派。若均匀分到 256 个专家，每个专家平均接收 2 个 token，当前批次会访问全部路由专家；若所有 token 都集中到同 8 个专家，则每个专家接收 64 个 token，当前批次只访问 8 份专家权重。
 
 设本批实际访问的不同专家数为 $U$。没有丢弃的路由满足 $\sum_e t_e=Bk_{\mathrm{top}}$，因此
 
@@ -554,7 +554,7 @@
 
 Qwen3.6 在已有 8192-token 上下文的 $B=1$ decode 中约需 7.33 GFLOPs，小于 Qwen3-8B 的约 20.0 GFLOPs。整个模型的计算量差异来自几项结构上的变化：2048 维主干缩小投影，30 个线性层使用递推状态，MoE 每次只执行选中的专家及共享分支。专家数量增加使总参数量增大，而每次调用的工作量取决于实际执行了哪些运算。
 
-稀疏激活把总参数容量与每 token 的激活计算分开，使模型可以用较少计算访问较大的参数集合。不过，服务效率取决于专家怎样被访问：少量 token 分散到很多专家，会产生许多小矩阵和低复用读取；跨设备专家还要传递激活。稀疏结构提供了一种系统折中，不能仅凭激活参数较少就断言其部署更便宜。设计者需要同时选择专家数量、每次选中的专家数、专家粒度和路由约束，再用质量实验与目标硬件上的执行结果筛选。
+稀疏激活把总参数容量与每 token 的激活计算分开，使模型可以用较少计算访问较大的参数集合。服务时，路由结果决定每份专家权重供多少 token 复用：少量 token 分散到很多专家，会产生许多小矩阵和低复用读取；集中到同一组专家，则能组成更大的矩阵。下面用一个批次比较这两种访问方式。
 
 ### 2.4.3 共享专家与潜空间计算
 
@@ -570,7 +570,7 @@
 
 > **练习 2-6〔延伸〕：专家总量、激活与复用**
 >
-> 对 Qwen3.6 的一层，取 $B=64$、256 个路由专家、每 token 选 8 个。分别按均匀分派和全部集中到相同 8 个专家构造 $t_e$，计算 FLOPs、不同专家数与 BF16 权重读取量。再将专家数减半、专家中间宽度加倍、每 token 选中数减半，判断总专家参数与激活矩阵运算是否变化。最后说明共享专家与潜空间进出投影应加在哪一项。
+> 对 Qwen3.6 的一层，取 $B=64$、256 个路由专家、每 token 选 8 个。分别考虑均匀分派和集中分派到同一组 8 个专家的情况，写出各专家接收的 token 数 $t_e$，再计算 FLOPs、被访问的专家数量与 BF16 权重读取量。再将专家数减半、专家中间宽度加倍、每 token 选中数减半，判断总专家参数与激活矩阵运算是否变化。最后说明共享专家与潜空间进出投影应加在哪一项。
 
 ## 2.5 网络形状与计算组织
 
@@ -578,7 +578,7 @@
 
 ### 2.5.1 深度、宽度与专家粒度
 
-先忽略词表和小向量参数。若 FFN 宽度与隐藏维度保持固定比例，稠密主干的参数量大致正比于 $Ld^2$。把隐藏维度减半、层数增至四倍，可以保持这部分参数量相近，却把逐层依赖加长了四倍。每个矩阵更小，但必须经过更多层；每层还要保存相应的上下文状态。参数总量相近并不能说明执行形状相近。
+先忽略词表和小向量参数。若 FFN 宽度与隐藏维度保持固定比例，稠密主干的参数量大致正比于 $Ld^2$。把隐藏维度减半、层数增至四倍，可以保持这部分参数量相近，却把逐层依赖加长了四倍。每个矩阵更小，但必须经过更多层；每层还要保存相应的上下文状态。层数与宽度由此在相近参数预算下改变了计算粒度和依赖深度。
 
 专家也有类似的在参数量和运算量不变时调整结构的方法。单层路由专家的总参数为 $3Edf$，单 token 专家运算量为 $6k_{\mathrm{top}}df$。将专家数 $E$ 减半、中间维度 $f$ 加倍、选中数 $k_{\mathrm{top}}$ 减半，这两个量都不变。Qwen3-235B-A22B 的 128 个宽度 1536 的专家、每 token 选 8 个，可以与 64 个宽度 3072 的专家、每 token 选 4 个作这一对照。路由输出维度与每专家接收行数仍会改变，从而改变分组矩阵乘法的形状。
 
@@ -628,7 +628,7 @@
 
 ### 2.6.1 各组件的资源需求与全模型汇总
 
-**四个模型的主要结构参数。** 本章选择一个稠密基线和三个不同规模的 MoE 模型。现在已经认识注意力、专家和层间连接，可以用相同的矩阵规则比较它们的资源需求。
+**四种模型的注意力结构与专家配置** 本章选择一个稠密基线和三个不同规模的 MoE 模型。现在已经认识注意力、专家和层间连接，可以用相同的矩阵规则比较它们的资源需求。
 
 **表 2-A　四个模型的总体架构（文本主干）**
 
@@ -675,17 +675,17 @@
 
 阅读时可以按数据流走一遍：输入进入哪类注意力层，随后经过哪类前馈网络，最终如何得到词表分数。每张表按注意力、前馈与连接、输出接口分组；“层／调用数”给出整模型累计倍数，“矩阵 FLOPs”仍是该行对应操作执行一次的浮点运算量。先确定这一层采用的注意力、前馈网络与连接结构，再累加该层的注意力、前馈与连接工作，最后加入嵌入查行、末端归一化和词表头。
 
-同样的累加规则用于不同模型时，需要注意各自的分支。对 Qwen3-8B，表中包含嵌入、36 层注意力与 FFN、残差和归一化、最终输出头；对 Qwen3.6，还需分别计算线性注意力层与完整注意力层，并列路由与共享专家。对 DeepSeek V4-Flash，还要加入窗口、压缩、索引和 mHC；对 Kimi K3，则包括 KDA、MLA、潜空间投影、共享专家与 AttnRes。MTP 与视觉模块按本次执行范围单独加入。
+将同样的累加规则用于其他模型，可以逐项列出它们的计算分支。对 Qwen3-8B，表中包含嵌入、36 层注意力与 FFN、残差和归一化、最终输出头；对 Qwen3.6，还需分别计算线性注意力层与完整注意力层，并列路由与共享专家。对 DeepSeek V4-Flash，还要加入窗口、压缩、索引和 mHC；对 Kimi K3，则包括 KDA、MLA、潜空间投影、共享专家与 AttnRes。MTP 与视觉模块按本次执行范围单独加入。
 
 | 模型分支 | 上下文状态 | 权重与附加计算 | 合计时最容易遗漏的部分 |
 | --- | --- | --- | --- |
 | Qwen3-8B | GQA KV | 稠密 SwiGLU、输出头 | 非矩阵算术、输出头位置范围 |
-| Qwen3.6 | 全局 KV＋递推与卷积状态 | 路由／共享专家 | 线性层不能按完整上下文 KV 计量 |
+| Qwen3.6 | 全局 KV＋递推与卷积状态 | 路由／共享专家 | 线性层保存固定大小的递推状态 |
 | DeepSeek-V3 | MLA 潜变量或展开 KV，随执行路径变化 | 前三层稠密 FFN、后续 MoE 与共享专家 | 上投影执行位置、两种缓存路径的区别 |
 | DeepSeek V4-Flash | 窗口＋压缩＋索引＋缓冲 | 专家、压缩器、mHC | 索引扫描、块边界更新、共享分支 |
 | Kimi K3 | MLA＋KDA＋卷积 | 潜空间专家、AttnRes | 展开路径、跨层临时状态、首层差异 |
 
-先连续比较完整权重、单步计算和上下文状态，再回到各模型的分项表。Qwen3.6 的权重比 Qwen3-8B 多，单步计算和上下文状态却更少；DeepSeek V4-Flash 的专家权重继续增加，上下文压缩又使 8K 状态进一步减小。后面的表将说明这些差异分别来自哪些矩阵和状态。
+先连续比较完整权重、单步计算和上下文状态，再通过各模型的分项表解释差异。Qwen3.6 的权重比 Qwen3-8B 多，单步计算和上下文状态却更少；DeepSeek V4-Flash 的专家权重继续增加，上下文压缩又使 8K 状态进一步减小。后面的表将说明这些差异分别来自哪些矩阵和状态。
 
 ![四模型完整权重统一用 BF16 表示时的容量，使用同一线性尺度。这里累计全部参数，包括每次仅选中部分的路由专家。](ch02/figure-2-resources.svg)
 
@@ -733,19 +733,21 @@
 
 各模型按自己的执行路径累计：Qwen3-8B 为有效因果注意力，Qwen3.6 为逐项执行的矩形注意力实现（eager）与块式线性分支，DeepSeek V4-Flash 为有效主注意力及参考索引，Kimi K3 为紧凑 MLA 与块式 KDA。Qwen3.6 保存的权重约为 Qwen3-8B 的四倍，单步矩阵运算量却更少；DeepSeek V4-Flash 权重继续扩大，8K 状态反而因压缩与选择缩小；Kimi K3 拥有更多专家，隐藏维度更大、层数也更多，因此需要保存更多权重，完成更多运算。这些差异说明，总参数量、单次运算量和上下文状态大小需要根据各自对应的结构分别计算。
 
-**根据状态大小计算访问量。** Qwen3-8B 每步使用 1152 MiB 旧 KV；DeepSeek V4-Flash 的主注意力读取与索引扫描合计 27.625 MiB，并更新压缩器。Kimi K3 紧凑 MLA 使用 216 MiB 上下文状态，414 MiB 递推矩阵还要一读一写，读写量合计 828 MiB；Qwen3.6 对应 160 MiB 全局注意力的 KV 缓存和 120 MiB 递推矩阵读写。递推状态每步都要读出并更新，上下文表示则要供当前查询使用。因此，即使状态大小固定，每步也仍有相应的读写开销。
-
-**存储格式怎样影响权重大小。** 统一按 BF16 计算时，每个参数占 2 字节，权重大小可以直接反映参数量的差异。实际发布的权重文件（checkpoint，即模型参数的保存快照）使用各自的存储格式，DeepSeek V4-Flash 主干约为 156.02 GB，Kimi K3 文本约为 1559.97 GB。文件大小由低位宽矩阵、保留较高精度的参数以及格式元数据共同决定。模型加载后，还需要为格式转换和计算分配空间。第五章将分析存储格式与运行时实现如何影响显存占用。
+**由上下文状态大小推算单步读取量** Qwen3-8B 每步使用 1152 MiB 旧 KV；DeepSeek V4-Flash 的主注意力读取与索引扫描合计 27.625 MiB，并更新压缩器。Kimi K3 紧凑 MLA 使用 216 MiB 上下文状态，414 MiB 递推矩阵还要一读一写，读写量合计 828 MiB；Qwen3.6 对应 160 MiB 全局注意力的 KV 缓存和 120 MiB 递推矩阵读写。递推状态每步都要读出并更新，上下文表示则要供当前查询使用。因此，即使状态大小固定，每步也仍有相应的读写开销。
+
+**量化编码与元数据共同决定权重容量** 统一按 BF16 计算时，每个参数占 2 字节，权重大小可以直接反映参数量的差异。实际发布的权重文件（checkpoint，即模型参数的保存快照）使用各自的存储格式，DeepSeek V4-Flash 主干约为 156.02 GB，Kimi K3 文本约为 1559.97 GB。文件大小由低位宽矩阵、保留较高精度的参数以及格式元数据共同决定。模型加载后，还需要为格式转换和计算分配空间。第五章将分析存储格式与运行时实现如何影响显存占用。
 
 表 2-B 的参数组成解释了权重容量，表 2-C 则展示这些参数怎样参与一次调用。二者结合起来，可以区分“模型能容纳多少参数”与“处理一个 token 要使用多少资源”。[^comparison-data]
 
 <!-- MODEL-COMPARISON:END -->
 
-**把上下文从 8K 拉长到 200K。** 长文档问答和多轮任务往往需要保留远多于 8K 的上下文。下面保持单请求、一次新增一个 token 和末位置词表头不变，只把调用前的上下文从 8192 增至 204800 个位置；本例的 K 均指 1024，所以上下文长度增至 25 倍。这里计算已有上下文后的单步 decode，构建这段上下文的 prefill 工作需另计。
+**上下文从 8K 增至 200K 时的运算量与状态增长** 长文档问答和多轮任务往往需要保留远多于 8K 的上下文。下面保持单请求、一次新增一个 token 和末位置词表头不变，只把调用前的上下文从 8192 增至 204800 个位置；本例的 K 均指 1024，所以上下文长度增至 25 倍。
 
 ![四模型在 8K 与 200K 上下文下的单步矩阵运算量，横轴为对数刻度；Qwen3-8B 的 200K 数值为结构公式外推。](ch02/figure-2-long-context-compute.svg)
 
 *图 2-31　已有 8K／200K 上下文后再处理一个 token 的矩阵运算量。蓝、橙分别对应两种上下文长度，横轴为对数刻度，柱端标出 GFLOPs。沿用表 2-C 的 decode 路径，Kimi K3 的 KDA 使用递推、MLA 使用紧凑表示。星号表示 Qwen3-8B 的 200K 条件超过本章固定配置的未缩放上下文上限，仅按原结构外推。*
+
+表 2-D 将上下文交互从整模型运算中提取出来，累计当前查询的评分与值汇总，DeepSeek V4-Flash 再加索引点积扫描。状态列包含调用前的上下文与固定状态，精度沿用表 2-C。把交互项与图 2-31 的整模型矩阵运算对照，就能看出上下文增长带来的新增工作。[^long-context-data]
 
 **表 2-D　长上下文下的交互运算与状态容量**
 
@@ -756,15 +758,13 @@
 | DeepSeek V4-Flash | 3.00 | 23.94 | 0.069 | 1.329 |
 | Kimi K3（紧凑 MLA） | 41.08 | 1026.77 | 0.634 | 5.697 |
 
-上下文交互列只累计当前查询对上下文及自身的评分与值汇总，DeepSeek V4-Flash 还计入索引点积扫描；它不包含当前 token 的投影、专家及固定状态更新。图中的整模型矩阵运算则保留原计算口径，归一化、激活、递推中的普通算术与特殊函数仍单列。状态列累计调用前的上下文和固定状态，精度与表 2-C 相同。表中各值根据模型结构计算，用于比较资源需求；执行时间与回答质量需分别测量。[^long-context-data]
-
 DeepSeek V4-Flash 的收益可以从条目数直接看出。200K 上下文下，一个 CSA 层保存 51200 个压缩条目，索引扫描这些条目后，主注意力最多使用其中 512 个，再加 128 个窗口位置；一个 HCA 层使用 1600 个粗粒度条目，再加窗口。与 8K 相比，CSA 的主注意力访问上限保持不变，HCA 和索引扫描继续增长，因此整模型单步矩阵运算只从 29.54 增至 50.48 GFLOPs，约为 1.71 倍。
 
-若固定 DeepSeek V4-Flash 的 43 层、64 个查询头和 512 维头宽，假设每层对全部 204801 个位置执行完整评分与汇总，则这项矩阵工作为 $43\times4\times64\times512\times204801$，约 1154.28 GFLOPs；实际窗口、压缩上下文及索引扫描合计约 23.94 GFLOPs，相差约 48.2 倍。这个对照用来单独分析上下文组织方式的影响，不包含替换架构后的投影、训练或质量变化，不能作为整模型加速比。
-
-Kimi K3 则通过两种机制降低长上下文的负担。69 个 KDA 层把过去输入汇入固定大小的状态，只有 24 个 MLA 层继续逐位置访问上下文。若仅比较相同头数和紧凑宽度下的上下文交互项，24 层需要的工作是 93 层全部采用这类 MLA 时的 $24/93$，约 25.8%；KDA 自身仍需要投影与递推更新。与此同时，24 个 MLA 层在 200K 下保存的紧凑上下文表示约为 5.273 GiB，若保存展开后的各头 K/V，则需 281.25 GiB，相差约 53.3 倍。这是表示压缩带来的容量节省；紧凑路径的潜空间点积更宽，容量比例不等于计算节省比例。
-
-因此，Kimi K3 的整模型单步矩阵运算仍从 249.44 增至 1235.13 GFLOPs：长上下文最终会放大剩余 MLA 层的工作。评估上下文压缩时，应同时看保存了多少状态、每步访问多少上下文，以及为压缩和更新状态付出多少运算；不同规模模型的总 FLOPs 排名无法单独说明压缩机制的价值。
+为单独比较上下文交互的矩阵工作，固定 DeepSeek V4-Flash 的 43 层、64 个查询头和 512 维头宽，假设每层对全部 204801 个位置执行完整评分与汇总。这项矩阵工作为 $43\times4\times64\times512\times204801$，约 1154.28 GFLOPs；实际窗口、压缩上下文及索引扫描合计约 23.94 GFLOPs，相差约 48.2 倍。窗口、压缩和索引选择共同减少了参与评分与汇总的位置数。
+
+Kimi K3 则通过两种机制降低长上下文的负担。69 个 KDA 层把过去输入汇入固定大小的状态，只有 24 个 MLA 层继续逐位置访问上下文。若仅比较相同头数和紧凑宽度下的上下文交互项，24 层需要的工作是 93 层全部采用这类 MLA 时的 $24/93$，约 25.8%；KDA 自身仍需要投影与递推更新。与此同时，24 个 MLA 层在 200K 下保存的紧凑上下文表示约为 5.273 GiB，若保存展开后的各头 K/V，则需 281.25 GiB，相差约 53.3 倍。紧凑表示减少了保存的数据，而潜空间点积的宽度决定每次交互的运算量。
+
+因此，Kimi K3 的整模型单步矩阵运算仍从 249.44 增至 1235.13 GFLOPs：长上下文最终会放大剩余 MLA 层的工作。评估上下文压缩时，应同时看保存了多少状态、每步访问多少上下文，以及为压缩和更新状态付出多少运算。
 
 ### 2.6.2 参数规模、数值精度与容量约束
 
@@ -806,7 +806,7 @@
 
 > **练习 2-7〔核心〕：由显存容量决定可容纳请求数**
 >
-> 显存为 24 GB，工作区预留 2 GiB，Qwen3-8B BF16 权重采用表中的精确值。分别求 4K、8K、16K 上下文下最多可同时容纳多少条请求；在 8K 情形下，将工作区预留增至 3 GiB，重新计算。再使用表中的 70B 4-bit 权重 39.500 GB，将可用容量改为 48 GB、预留仍为 2 GiB，分别求 8K 与 32K 上下文的请求数。先判断哪一项主导，再做除法。
+> 显存为 24 GB，工作区预留 2 GiB，Qwen3-8B BF16 权重采用表中的精确值。分别求 4K、8K、16K 上下文下最多可同时容纳多少条请求；在 8K 情形下，将工作区预留增至 3 GiB，重新计算。再使用表中的 70B 4-bit 权重 39.500 GB，将可用容量改为 48 GB、预留仍为 2 GiB，分别求 8K 与 32K 上下文的请求数。先比较权重、工作区与 KV 的容量需求，再用剩余显存计算可容纳的请求数。
 
 还可以反过来问：给定设备和服务目标，留给模型多少空间？设每卡可用容量为 $C$，固定预留工作区为 $M_0$，每条请求的 KV 为 $K$，并发为 $B$，每参数占 $b_w$ 字节。忽略其他驻留对象，单卡容量给出的参数上界为
 
@@ -814,9 +814,9 @@
 N_{\max}=\frac{C-M_0-BK}{b_w}.
 $$
 
-取 24 GB 容量、2 GiB 工作区、4 条各 8192 位置的请求；仅为隔离变量，固定采用 Qwen3-8B 的每请求 KV，即 1.125 GiB。BF16 每参数 2 字节，剩余空间最多容纳约 85.1 亿参数。[^core-calculation]图中先扣除服务状态，再划出权重预算。这个数是固定状态预算下的容量边界；实际增大层数、宽度或 KV 头数会同时改变 $K$，必须重新核算。
-
-**能填满显存的大小，只是候选上界；最优模型还要同时满足质量、时延、吞吐与生命周期成本。** 单卡和整组设备的容量会产生离散的部署门槛，促使模型设计者考虑合适的大小、精度和状态结构，却不能据此反推某个已发布模型的历史动机。例如 Llama 3 报告中的 405B BF16 推理使用两台、共 16 张 H100，说明模型能力目标也可能推动系统跨越原有单机边界。[^codesign]
+先固定服务状态，计算留给权重的空间。取 24 GB 容量、2 GiB 工作区、4 条各 8192 位置的请求，每请求 KV 沿用 Qwen3-8B 的 1.125 GiB。BF16 每参数 2 字节，剩余空间最多容纳约 85.1 亿参数。[^core-calculation]图中先扣除服务状态，再划出权重预算。
+
+改变模型结构时，权重预算与状态预算会一起变化。增加层数或 KV 头数会增大每请求的 $K$，留给权重的空间随之减少；跨设备部署则扩大总容量，同时增加通信。Llama 3 报告中的 405B BF16 推理使用两台、共 16 张 H100，便跨越了单机容量边界。[^codesign]模型规模、状态结构与设备数量因此需要共同选择。
 
 ![图 2-34 从设备反推权重预算。24 GB 中先保留 4 条 8K 请求的 KV 和 2 GiB 工作区，余量给出 BF16 参数上界；图中容量均为十进制 GB。](ch02/figure-2-reverse-budget.svg)
 
@@ -824,7 +824,7 @@
 
 ### 2.6.3 不同请求条件下的模型比较
 
-最后把容量、调用链和任务质量合在一起。先根据设备容量排除无法放下的配置，再沿每个模型的实际计算方式累计请求工作，最后检查模型能否完成目标任务。相同参数量不保证相同计算量，相同 token 数也不保证相同文本内容；应根据比较目的选择输入。
+最后把容量、调用链和任务质量合在一起。先根据设备容量排除无法放下的配置，再沿每个模型的实际计算方式累计请求工作，最后检查模型能否完成目标任务。
 
 比较完整模型时，可以从两种输入出发。固定 $S,P,G,B$，得到相同调用长度下的资源需求，便于观察结构差异；固定文本和任务，则先经各模型的分词器得到输入长度，再检查输出。这两种对照分别回答“同样多的位置怎样计算”和“同一个任务需要怎样执行”。
 
@@ -843,7 +843,7 @@
 
 > **练习 2-8〔延伸〕：从多轮输入重建调用**
 >
-> 一轮输入总长 1443 个 token，其中前 1392 个命中缓存，返回 128 个 token。求 $S,P,G,n_d$、请求结束时的上下文长度，以及本轮新增 KV。比较完全重算与复用缓存时，投影行数、有效因果位置对数分别减少多少。若在第 501 个输入位置修改了内容，重新求最长可复用前缀与新输入长度。
+> 一轮输入总长 1443 个 token，其中前 1392 个命中缓存，返回 128 个 token。求 $S,P,G,n_d$ 的值、请求结束时的上下文长度，以及本轮新增 KV 的大小。比较完全重算与复用缓存时，投影行数、有效因果位置对数分别减少多少。若在第 501 个输入位置修改了内容，重新求最长可复用前缀与新输入长度。
 
 固定文本时，不同分词器会产生不同的输入长度。例如，在八道短语检索题中，Qwen3-8B 与 DeepSeek V4-Flash 都正确找到了目标短语，但短文在 Qwen 中对应 523 或 524 个 token，在 DeepSeek V4-Flash 中对应 501 个；较长文本分别对应 2121 或 2122 个和 2036 个。相同文本保持了任务内容，模型各自的 token 数则决定了实际输入形状。
 
@@ -857,9 +857,9 @@
 
 *图 2-36　同一 128 输入、四输出请求的矩阵运算量。模型执行范围与缓存路径见本节题设；这是逐调用累计的计算量。*
 
-> **练习 2-9〔延伸〕：完成一次模型选择**
+> **练习 2-9〔延伸〕：根据容量、计算需求与任务质量选择模型**
 >
-> 使用四模型资源表，为每个模型分别列出完整权重、8K 状态、单步 decode 与 8K prefill 的需求。先以 80 GB 容量、2 GiB 工作区、BF16 权重判断哪些模型能完整放入单卡；再把问题改为多卡部署，说明还需从后续章节获得哪些时间与通信参数。将短请求的 $G$ 改为 1，检验调用次数。最后为一个短语检索任务写出质量标准，说明同 token 数比较与同文本比较分别回答什么问题。
+> 使用四模型资源表，为每个模型分别列出完整权重、8K 状态、单步 decode 与 8K prefill 的需求。设单卡显存为 80 GB，预留 2 GiB 工作区，并采用 BF16 权重。先判断哪些模型能完整放入单卡；再把问题改为多卡部署，说明还需从后续章节获得哪些时间与通信参数。将短请求的 $G$ 改为 1，检验调用次数。最后为一个短语检索任务写出质量标准，说明同 token 数比较与同文本比较分别回答什么问题。
 
 本章从计算图出发，逐步算出了完整请求的资源需求。注意力决定如何保存和访问上下文，MoE 决定每次选择哪些权重，网络深度与连接方式决定计算顺序和临时存储。下一章将加入请求长度分布、到达时间、工具调用、训练与 RL，分析这些计算如何随时间累积成系统负载。
 
@@ -896,7 +896,7 @@
 
 这组表用于从整模型合计追溯到具体矩阵。查阅时先确定模型与执行分支，再用单次矩阵 FLOPs 乘层数；路由专家还要按实际接收的输入行数累计。表 2-1 的 Qwen3-8B 基线保留在 2.1.4。
 
-**表 2-2　Qwen3.6-35B-A3B：混合注意力与 MoE 的模块核算**
+**表 2-2　Qwen3.6-35B-A3B：混合注意力与 MoE 各模块的资源消耗计算**
 
 主干共 40 层：10 层选完整注意力分支，30 层选线性注意力分支；两类层随后都执行 MoE。每 token 从 256 个路由专家中选 8 个，另有一个共享专家。线性分支的 QKV 输出拆为 2048、2048、4096 维；其 $Q$、$K$ 会按值头的分组关系复用。
 
--- before/manuscripts/03-推理与训练负载.md
+++ manuscripts/03-推理与训练负载.md
@@ -22,7 +22,7 @@
 
 > **练习 3-1〔延伸〕：相同输出总量的不同生成过程**
 >
-> 分别考虑一条请求输出 1024 个 token，以及八条请求各输出 128 个 token。取每条输入均为 1024 个 token，求两种情况的 prefill 次数、后续 decode 总步数和各请求最终上下文。假设八条请求同步生成，比较每批共享一次权重时的读取轮数，再解释总输出相同为什么不足以判断完成时间。
+> 分别考虑一条请求输出 1024 个 token，以及八条请求各输出 128 个 token。每条请求的输入均为 1024 个 token。求两种情况下的 prefill 次数、后续 decode 总步数，以及各请求结束时的上下文长度。假设八条请求同步生成，比较每批共享一次权重时的读取轮数，再解释总输出相同为什么不足以判断完成时间。
 
 ### 3.1.2 任务目标与评价指标
 
@@ -43,7 +43,7 @@
 
 首个 token 到达客户端之前，请求要依次经过排队、输入准备、设备计算和结果返回等阶段。输入原来在主机还是设备，决定数据传输发生在哪里；计算完成后，结果还要经过主机处理和连接发送。记录同一请求在各阶段的开始和结束时间，就能看出 TTFT 主要花在哪些环节。第五章将展开设备提交、传输与完成的具体时序。
 
-客户端有时按块接收多个 token，块间隔不能直接当作逐 token 间隔。reasoning（推理）模型在给出可见回答前生成中间推理内容，这些内容称为思考 token。这类模型又增加了一个观察点：首个内部 token 出现时，用户未必已看到答案。因此，内部生成、用户可见输出和任务完成分别对应不同的时间点。语音的首次播放还要再经过解码与设备缓冲，稍后讨论。
+客户端按块接收多个 token 时，可以直接记录每块的到达时间与 token 数，再统计输出节奏。reasoning（推理）模型在给出可见回答前生成中间推理内容，这些内容称为思考 token。这类模型又增加了一个观察点：首个内部 token 出现时，用户未必已看到答案。因此，内部生成、用户可见输出和任务完成分别对应不同的时间点。语音的首次播放还要再经过解码与设备缓冲，稍后讨论。
 
 | 要回答的问题 | 需要的指标或记录 |
 | --- | --- |
@@ -80,7 +80,7 @@
 
 **例题 3-1：平均处理能力足够，为什么仍会积压？** 判断每秒可完成 6144 个后续 decode 步的资源，能否持续承接章首负载。
 
-**解：** 定义两类请求：长输入类输入 8192、输出 256 个 token；长输出类输入 1024、输出 2048 个 token。两分钟内各到达 240 条，总计平均每秒四条。一种负载始终各占一半；另一种前一分钟两类占比为 $9:1$，后一分钟为 $1:9$。每条请求的首输出由 prefill 产生，后续 decode 步数为输出数减一，于是有：
+解： 定义两类请求：长输入类输入 8192、输出 256 个 token；长输出类输入 1024、输出 2048 个 token。两分钟内各到达 240 条，总计平均每秒四条。一种负载始终各占一半；另一种前一分钟两类占比为 $9:1$，后一分钟为 $1:9$。每条请求的首输出由 prefill 产生，后续 decode 步数为输出数减一，于是有：
 
 | 时间窗 | 新输入 token/s | 后续 decode 步/s |
 | --- | ---: | ---: |
@@ -96,7 +96,7 @@
 
 *图 3-5　三个时间窗中的请求组成对应的输入与后续生成需求。输入数按 token 计，后续生成按每请求的一次 decode 步计；每请求首输出已计入 prefill。*
 
-**本例题设。** 使用流体队列模型，把离散的生成工作近似为连续流量；每个窗口内均匀到达，处理率固定，初始队列为空。单位是一条请求的一次后续 decode 步，prefill 能力单独核算。
+**队列假设：窗口内均匀到达、处理率固定** 使用流体队列模型，把离散的生成工作近似为连续流量；每个窗口内均匀到达，处理率固定，初始队列为空。单位是一条请求的一次后续 decode 步，prefill 能力单独核算。
 
 ![工作先进入队列，再由处理资源完成。到来快于处理时差额留在队列中；处理快于到来时，资源逐步消化已有积压。](ch03/figure-3-queue-mechanism.svg)
 
@@ -116,7 +116,7 @@
 
 *图 3-7　每秒完成 6144 个 decode 步的流体模型。后一分钟积压至 79,632 步，120 秒后停止到达，再经过约 12.96 秒排空。*
 
-**队列与显存容量的共同作用。** 在 Qwen3-8B／vLLM 0.23 上运行这两种到达序列，可以观察到上述差异怎样影响用户等待。设备为 RTX PRO 6000，KV 池是专门留给请求键值上下文的显存区域，本次容量为 24 GiB，同时最多执行 64 条序列；前缀缓存指跨请求复用相同开头的已有 KV，本次关闭这一功能，每条请求生成指定数量的 token。两组负载都在 120 秒内到达 480 条请求，全部完成后的结果如下。[^arrival]
+**请求积压如何增加 KV 驻留需求** 在 Qwen3-8B／vLLM 0.23 上运行这两种到达序列，可以观察到上述差异怎样影响用户等待。设备为 RTX PRO 6000，KV 池是专门留给请求键值上下文的显存区域，本次容量为 24 GiB，同时最多执行 64 条序列；前缀缓存指跨请求复用相同开头的已有 KV，本次关闭这一功能，每条请求生成指定数量的 token。两组负载都在 120 秒内到达 480 条请求，全部完成后的结果如下。[^arrival]
 
 | 实际观察 | 均匀混合 | 前 9:1、后 1:9 |
 | --- | ---: | ---: |
@@ -138,7 +138,7 @@
 
 > **练习 3-2〔核心〕：突发、积压与容量**
 >
-> 沿例题 3-1，保持后一分钟每秒 7471.2 步的到达需求，先把突发持续时间改为 30 秒，再分别取处理率 6000、8000 步/秒。初始队列为空，求积压与停止到达后的排空时间。随后设每秒到达两个工具等待任务，每个保留 1 GiB 状态、等待 10 秒，求稳定状态下的平均等待任务数与状态占用；将等待延长到 30 秒后重新计算。
+> 采用例题 3-1 后一分钟的负载强度，即每秒新增 7471.2 步 decode 工作。将这一突发负载的持续时间改为 30 秒，分别考虑处理率为 6000、8000 步/秒的情况。初始队列为空，求积压与停止到达后的排空时间。随后考虑另一种负载：每秒有两个任务进入工具等待阶段，每个任务等待 10 秒，期间保留 1 GiB 状态。求稳定状态下的平均等待任务数与状态占用；将等待延长到 30 秒后重新计算。
 
 ## 3.2 多轮交互与 Agent 任务
 
@@ -180,7 +180,7 @@
 
 答案检查应区分接口格式和内容正确性。格式正确的回答可能算错，数值正确的回答也可能缺少任务要求的字段。把两类错误分开，才能判断应调整生成方式、输出约束还是解题过程。[^reasoning-off]
 
-单 token 变便宜也不能替代这项检查。用一个简单假设说明：新系统 token 单价降到原来的 1/10，每任务用量却增加到 20 倍，成功率由 50% 提高到 80%。只计模型成本，并假定重复尝试的分布稳定，每个成功任务的成本比为 $2\times\frac{0.5}{0.8}=1.25$。价格下降、质量提高和成功任务成本上升可以同时发生。对带上下文的 Agent，可以沿实际轨迹累加各次尝试，再以通过检查的任务数作分母。[^cost]
+token 单价降低后，仍需计算完成任务的总成本。用一个简单假设说明：新系统 token 单价降到原来的 1/10，每任务用量却增加到 20 倍，成功率由 50% 提高到 80%。只计模型成本，并假定重复尝试的分布稳定，每个成功任务的成本比为 $2\times\frac{0.5}{0.8}=1.25$。价格下降、质量提高和成功任务成本上升可以同时发生。对带上下文的 Agent，可以沿实际轨迹累加各次尝试，再以通过检查的任务数作分母。[^cost]
 
 ![各尝试 100 次的教学比较。所有尝试的成本都进入分子，通过检查的成功任务数进入分母；两种策略的成功任务成本分别为 2 和 2.5 单位。](ch03/figure-3-success-cost.svg)
 
@@ -188,7 +188,7 @@
 
 > **练习 3-3〔延伸〕：思考预算与成功成本**
 >
-> 旧方法每次尝试成本为 1 单位、成功率为 50%；新方法成本为 2 单位、成功率为 80%。计算每成功任务成本并判断新方法是否更省。若新方法成本降到 1.5 单位，成功率至少应达到多少才能不高于旧方法？对多次采样，再分别写出生成、验证和失败尝试进入成本的方式。
+> 旧方法每次尝试成本为 1 单位、成功率为 50%；新方法成本为 2 单位、成功率为 80%。计算平均每完成一个成功任务所需的成本，并判断新方法是否更省。若新方法成本降到 1.5 单位，成功率至少应达到多少，才能使每个成功任务的平均成本不高于旧方法？对于需要多次采样的任务，说明如何将生成、验证和失败尝试的开销计入总成本。
 
 ### 3.2.3 智能体工作负载
 
@@ -221,7 +221,7 @@
 
 **例题 3-2：加速首轮能节省多少任务时间？** 用第一章的 Amdahl 分析，假设后续行为与质量不变。
 
-**解：** 保持同一条执行轨迹，只改变一个条件：只把首轮 36.375 秒的模型段缩短一半，其余阶段、输出、工具行为和质量都不变，总时间便从 76.510 秒降到 58.323 秒。此时 $f=36.375/76.510$，$s=2$，总加速比约为 1.31；首轮速度提高到两倍的收益受其原有时间占比限制。首轮占总时间约 47.5%，将这一段减半节省约 23.8% 的总时间；其余阶段继续形成约 40.1 秒的固定部分。优化收益由被缩短阶段的原有占比决定。[^agent-speedup]
+解： 保持同一条执行轨迹，只改变一个条件：只把首轮 36.375 秒的模型段缩短一半，其余阶段、输出、工具行为和质量都不变，总时间便从 76.510 秒降到 58.323 秒。此时 $f=36.375/76.510$，$s=2$，总加速比约为 1.31；首轮速度提高到两倍的收益受其原有时间占比限制。首轮占总时间约 47.5%，将这一段减半节省约 23.8% 的总时间；其余阶段继续形成约 40.1 秒的固定部分。优化收益由被缩短阶段的原有占比决定。[^agent-speedup]
 
 设模型先计算 2 秒，随后调用两个工具，耗时分别为 6 秒和 10 秒，最后再计算 3 秒。若第二个工具依赖第一个的结果，总时间为 $2+6+10+3=21$ 秒；若工具相互独立，总时间为 $2+\max(6,10)+3=15$ 秒。并行减少了 6 秒，但两个工具的总执行工作仍为 16 秒。
 
@@ -243,13 +243,15 @@
 
 > **练习 3-4〔核心〕：关键路径与状态寿命**
 >
-> 模型先执行 2 秒，再调用耗时 6 秒、10 秒的两个工具，最后执行 3 秒。分别求工具串行与并行时的总任务时间，以及仅工具等待期间保留 1 GiB 状态产生的 GiB·s。回到代码任务的 76.510 秒轨迹，首轮为 36.375 秒，工具合计 0.078 秒：将首轮加速四倍或工具全部加速两倍，各能节省多少？解释为什么最快的局部加速未必是最有效的优化。
+> 一次任务先由模型计算 2 秒，然后调用两个工具，两个工具分别耗时 6 秒和 10 秒。两个工具都完成后，模型再计算 3 秒，任务结束。分别计算两个工具依次执行和同时执行时的任务总耗时。假设仅在等待工具返回期间保留 1 GiB 状态，求两种执行方式下状态占用量与保留时间的乘积，单位为 GiB·s。
+>
+> 本节的代码任务实测记录显示，任务总耗时为 76.510 秒，其中首轮模型计算耗时 36.375 秒，所有工具执行合计耗时 0.078 秒。保持其余阶段不变，分别计算两种优化能节省多少任务时间：将首轮模型计算速度提高到原来的四倍；将所有工具的执行速度提高到原来的两倍。根据计算结果，解释为什么局部加速倍数不能单独决定一项优化对完整任务的收益。
 
 把同一条会话换成 V4.1，可以看到负载需求如何推动模型结构的变化。设 Agent 已有一段公共上下文，工具随后返回新材料；如果前缀未命中，服务要重新处理较多输入，而本轮输出可能很短。为这样的输入密集场景降低 Prefill 成本，比只优化单步生成更有价值。V4.1 因而使用 20 层因果编码器和 20 层解码器：大部分输入只执行编码器主体，解码器全局 KV 从编码器末层表示投影得到；解码器局部 SWA 则通过提示末尾最多 128 个 token 的编码器输出进行有界重放，近似构建。输出 token 继续经过全部 40 层。[^v41-case]
 
-先只统计各个 token 经过的专家层数，再将它们相加。设一次从空状态开始的输入为 $P$ 个 token，则普通 40 层路径为 $40P$，论文 CED 调度为 $20P+20\min(P,128)$。当 $P=8192$ 时，两者为 327,680 与 166,400，后者约为前者的 50.8%；当 $P\leq128$ 时，这个子项没有减少。全局 KV 投影、注意力、索引、Engram 等工作还要另外计入，不能把 50.8% 当作总时延比例。
-
-后续章节沿这条会话以 8K 上下文为基准计算状态大小，再比较 128K 上下文的情况，并分别检查全局 KV 与编码器 SWA 均命中、仅全局 KV 命中和全局 KV 未命中三种情况。它们是固定模型下的教学条件，不是一次已有实测轨迹。编码器 SWA 命中时，编码器主体可从追加输入继续；若它缺失，还要重放已缓存前缀的末尾窗口。无论这两种情况中的哪一种，每次 Prefill 仍要构建解码器 SWA，不能把缓存命中理解成完全没有旧位置重放。上述从空状态的 $P$ 不能直接替换成总会话长度。新增输入长度与恢复条件共同决定本轮工作量，第 8 章再计算缓存策略。
+先计算专家矩阵这一子项，用各 token 经过的专家层数之和表示工作量。设一次从空状态开始的输入为 $P$ 个 token，则普通 40 层路径为 $40P$，论文 CED 调度为 $20P+20\min(P,128)$。当 $P=8192$ 时，两者为 327,680 与 166,400，后者约为前者的 50.8%；当 $P\leq128$ 时，这个子项没有减少。
+
+已有前缀时，输入工作由追加长度与恢复路径共同决定。编码器 SWA 命中，编码器主体便从追加输入继续；编码器 SWA 缺失，则先重放前缀末尾窗口。两条路径随后都要构建本轮的解码器 SWA。第八章将用 8K 与 128K 上下文的教学算例，比较保存状态和重新执行的成本。
 
 ![同一批 8K 输入的专家矩阵 token 层数。普通全层路径执行 40 层；CED 路径执行 20 层编码器，并为最近 128 个 token 重放 20 层解码器。灰色说明项仍需另外计算；生成阶段执行完整主干。](ch03/figure-3-v41-ced.svg)
 
@@ -289,7 +291,7 @@
 | 完整编码结果 EC | 7.8125 MiB | 图片、预处理、编码器与输出格式一致 |
 | 视觉位置的语言 KV | 56.25 MiB | 还依赖先前上下文、顺序、位置与模型状态 |
 
-同一图片回答不同问题时，EC 可能复用；语言 KV 则还要检查问题放在图片前还是后、此前上下文是否改变。**输入状态预算。** 下面只累计四张图片与 400 个文本位置的有效张量；特殊 token、生成的新位置、分页未用空间和运行工作区在实例容量预算中分别加入。四张相同规格图片的 EC 为 31.25 MiB，视觉位置 KV 为 225 MiB；再加 400 个文本位置，输入逻辑 KV 为 281.25 MiB。这一容量是输入有效位置的逻辑张量大小。
+同一图片回答不同问题时，EC 可能复用；语言 KV 则还要检查问题放在图片前还是后、此前上下文是否改变。**多模态输入的编码状态与 KV 容量预算** 下面只累计四张图片与 400 个文本位置的有效张量；特殊 token、生成的新位置、分页未用空间和运行工作区在实例容量预算中分别加入。四张相同规格图片的 EC 为 31.25 MiB，视觉位置 KV 为 225 MiB；再加 400 个文本位置，输入逻辑 KV 为 281.25 MiB。这一容量是输入有效位置的逻辑张量大小。
 
 三种数据表示对应三个不同的交接位置。发送原图，接收端还要执行视觉编码；发送 EC，接收端从语言 prefill 开始；发送 KV，则交接已经完成的语言上下文。第八章安排同卡执行，第九章比较阶段交接，第十二章把相同数据流放到端、边、云链路上。
 
@@ -297,7 +299,7 @@
 
 ### 3.3.2 音频与图像生成的计算过程
 
-视觉问答仍通过语言模型逐 token 生成文本。若输出本身是声音或图像，就需要重新确定一次计算更新什么、循环多少次，不能直接把文本的生成步数套用过去。
+视觉问答仍通过语言模型逐 token 生成文本。若输出本身是声音或图像，一次迭代处理的数据和迭代次数也会改变。下面分别看声音与图像的生成过程。
 
 音频输出还有帧内循环。以 Fish Audio 的语音生成模型 S2 Pro 为例，slow（慢速）路径每次推进一个音频帧，fast（快速）路径在帧内补齐多个码本的编号。码本是离散声音表示的候选向量集合，codec（编解码器）将这些编号还原为波形。每帧先用一次 fast 前向建立状态，再用九次预测补齐码本，因此生成一帧需要十次 fast 前向计算。语言 token/s、声学帧/s 与每秒生成多少秒声音因而是不同指标。[^source-1][^media]
 
@@ -351,7 +353,7 @@
 
 > **练习 3-5〔延伸〕：视觉尺寸与实时播放**
 >
-> 将 Qwen3-VL-4B 图片从 $640\times640$ 改为 $1280\times1280$，保持 patch、合并与特征精度不变，求视觉位置数、完整 EC 和视觉位置 KV。再按音频图比较 40 ms、60 ms 预缓冲的首播和停顿。如果音频长期每秒只到达 0.9 秒可播放内容，初始缓冲为 0.3 秒，求连续播放多久后耗尽，并解释增加有限缓冲能解决什么问题。
+> 将 Qwen3-VL-4B 图片从 $640\times640$ 改为 $1280\times1280$，保持图像块大小、合并方式和特征精度不变，求视觉位置数、完整编码缓存 EC 的大小，以及这些视觉位置对应的 KV 大小。再根据本节音频时间线，比较初始缓冲为 40 ms 和 60 ms 时的首次播放时刻与停顿情况。如果音频长期每秒只到达 0.9 秒可播放内容，初始缓冲为 0.3 秒，求连续播放多久后耗尽，并解释增加有限缓冲能解决什么问题。
 
 ## 3.4 训练的计算与状态需求
 
@@ -373,7 +375,7 @@
 
 输入梯度把误差信号传给前一层，权重梯度决定本层参数的调整。观察三组矩阵尺寸：虽然乘法顺序不同，三次运算都含有相同的 $mkn$ 个乘加，因此合计约为 $6mkn$ FLOPs。若所有训练 token 都经过同一组主要参数矩阵，累计便得到常见近似 $F_{\mathrm{train}}\approx6ND$，其中 $D$ 为训练 token 总数。[^training]
 
-这一推导也说明怎样处理结构变化。冻结某个权重矩阵后，可省去它的权重梯度计算；若更早的层仍需要梯度，则仍要计算输入梯度。MoE 按选中专家累计两类梯度，注意力则按位置对累计。训练计算取决于反向图中实际需要的运算，不能只看参数是否存放在设备中。
+这一推导也说明怎样处理结构变化。冻结某个权重矩阵后，可省去它的权重梯度计算；若更早的层仍需要梯度，则仍要计算输入梯度。MoE 按选中专家累计两类梯度，注意力则按位置对累计。沿反向依赖逐项累加，就得到各训练方法需要执行的计算量。
 
 反向需要前向留下的信息。计算 $\mathrm dW$ 要使用 $X$，对非线性运算求梯度时，也需要前向计算留下的中间结果，参数梯度在反向中产生，优化器是根据梯度计算参数更新的方法，其状态记录多次更新积累的信息，因此需要保留到下一次更新。它们的寿命不同：某层激活在该层反向结束后可以释放，优化器动量通常一直留到下一次更新。前向产生的激活要等待相应反向才能释放，因此反向传播把中间数据的寿命从相邻算子之间延长到了前后两次遍历之间。
 
@@ -383,17 +385,17 @@
 
 **例题 3-3：训练状态为什么超过推理权重数倍？**
 
-**解：** 以 Qwen3-8B 的 8,190,735,360 个参数为例，保存 BF16 权重、FP32 梯度、一份独立的 FP32 主权重（master weight），以及 Adam 的两个 FP32 矩估计。Adam 是根据梯度及梯度平方的移动平均调节更新幅度的优化器，两个矩估计分别保存这两种平均量。不做分片时，每参数需要 $2+4+4+4+4=18$ bytes，共 147.433 GB，约 137.308 GiB。BF16 权重用于前向和反向计算，梯度用于计算更新量，FP32 主权重用于高精度更新，Adam 的两个矩估计则累计梯度及其平方的统计量。这些数据合计占用的空间，是推理 BF16 权重的九倍。[^train-matrix]
-
-微批次是一次前向和反向实际处理的数据子集；完整训练批可以由多个微批次组成。一个微批次与一次参数更新也要区分。显存放不下完整训练批时，可以分多个微批次累计梯度，最后再更新一次参数。只要目标、有效标签和归一化保持一致，微批次数增加并不意味着相同数据被训练了更多轮；它首先改变的是执行顺序与激活寿命。第十章将据此讨论分片和流水。
+解： 以 Qwen3-8B 的 8,190,735,360 个参数为例，保存 BF16 权重、FP32 梯度、一份独立的 FP32 主权重（master weight），以及 Adam 的两个 FP32 矩估计。Adam 是根据梯度及梯度平方的移动平均调节更新幅度的优化器，两个矩估计分别保存这两种平均量。不做分片时，每参数需要 $2+4+4+4+4=18$ bytes，共 147.433 GB，约 137.308 GiB。BF16 权重用于前向和反向计算，梯度用于计算更新量，FP32 主权重用于高精度更新，Adam 的两个矩估计则累计梯度及其平方的统计量。这些数据合计占用的空间，是推理 BF16 权重的九倍。[^train-matrix]
+
+微批次是一次前向和反向实际处理的数据子集；完整训练批可以由多个微批次组成。一个微批次与一次参数更新也要区分。显存放不下完整训练批时，可以分多个微批次累计梯度，最后再更新一次参数。保持目标、有效标签和归一化一致时，将同一训练批拆成更多微批次，会改变执行顺序与激活寿命，而总训练数据量保持不变。第十章将据此讨论分片和流水。
 
 ![训练沿前向依赖计算输出，再沿反向依赖传递梯度。当前层既向前层传输入梯度，也计算自己的权重梯度，供优化器更新。](ch03/figure-3-5-training.svg)
 
 *图 3-21　训练沿前向依赖计算输出，再沿反向依赖传递梯度。当前层既向前层传输入梯度，也计算自己的权重梯度，供优化器更新。*
 
-![Qwen3-8B 全参数训练的参数相关状态。每参数包括两字节计算权重和四组四字节状态，共十八字节；激活和工作区由各自寿命另行核算。](ch03/figure-3-training-states.svg)
-
-*图 3-22　Qwen3-8B 全参数训练的参数相关状态。每参数包括两字节计算权重和四组四字节状态，共十八字节；激活和工作区由各自寿命另行核算。*
+![Qwen3-8B 全参数训练的参数相关状态。每参数包括两字节计算权重和四组四字节状态，共十八字节；激活和工作区的容量需求按各自的保存时间单独计算。](ch03/figure-3-training-states.svg)
+
+*图 3-22　Qwen3-8B 全参数训练的参数相关状态。每参数包括两字节计算权重和四组四字节状态，共十八字节；激活和工作区的容量需求按各自的保存时间单独计算。*
 
 训练过程中还要不断准备下一批输入。CPU 读取、解码、分词并整理批次，再把张量送到设备。双缓冲可以让当前批计算时准备下一批：一个缓冲供计算读取，另一个接收新数据，交替使用。计算耗尽已准备数据时会等待输入，取回设备结果时又会等待相应计算完成。第五章将结合前向、反向和参数更新的执行顺序，分析数据准备与计算如何重叠。
 
@@ -441,7 +443,7 @@
 
 > **练习 3-6〔延伸〕：训练矩阵、标签与更新**
 >
-> 从 $Y=XW$ 推导输入梯度和权重梯度的尺寸，说明三次矩阵乘为何有相同运算量。对 Qwen3-8B，计算每参数 18 字节的参数状态，再把梯度改为 BF16 求节省量。取每微批次 2 条、每条 4096 输入位置、1024 个监督位置，累计 8 个微批次更新一次，求输入与有效监督 token 数。解释只屏蔽损失与只计算受监督位置词表头的区别。
+> 从 $Y=XW$ 推导输入梯度和权重梯度的尺寸，说明三次矩阵乘为何有相同运算量。对 Qwen3-8B，按每参数 18 字节计算长期训练状态的总容量，再将梯度改为 BF16，求节省的容量。设每个微批次含 2 条序列，每条有 4096 个输入位置，其中 1024 个位置参与监督。累计 8 个微批次后更新一次参数，求每次更新处理的输入 token 总数和有效监督 token 总数。解释只屏蔽损失与只计算受监督位置词表头的区别。
 
 ## 3.5 强化学习（RL）
 
@@ -481,7 +483,7 @@
 
 **例题 3-4：固定有效样本目标，样本保留比例降低需要多少额外工作？**
 
-**解：** 以 Qwen3-8B 为例，设有八道题，每题生成四份回答，$P=1024,\ G=256$，共保留 16 条样本用于一次策略更新。参考模型（reference model）与策略模型结构相同，分别保存权重；参考模型对全部 32 份回答各执行一次前向，本例不调用教师模型。各样本长度相同，生成时不提前停止，也不共享前缀。[^rl]
+解： 以 Qwen3-8B 为例，设有八道题，每题生成四份回答，$P=1024,\ G=256$，共保留 16 条样本用于一次策略更新。参考模型（reference model）与策略模型结构相同，分别保存权重；参考模型对全部 32 份回答各执行一次前向，本例不调用教师模型。各样本长度相同，生成时不提前停止，也不共享前缀。[^rl]
 
 生成时每条先做一次 prefill，再做 255 次 decode。训练与评分时，轨迹已经已知，可以采用教师强制（teacher forcing）执行前向：每个位置使用轨迹中已经确定的前一个 token，而不是等待模型重新生成它。因此，这些已知位置可以一同计算。输入长度 $P+G-1=1279$，256 个输出标签位于相应位置。16 条保留样本每轮更新的输入位置共 20,464，监督输出共 4,096。输入位置决定上下文计算，监督位置决定损失作用于哪些输出，二者分别进入训练预算。
 
@@ -518,7 +520,7 @@
 
 > **练习 3-7〔核心〕：固定更新目标下的 RL 成本**
 >
-> 保持一次更新使用 16 条等长样本。利用 32 条与 64 条生成的周期运算量，求每新增一条回答的生成加参考模型运算，以及固定更新运算量；进而预测生成 48 条时的总量。再增加一次对全部 48 条回答的同结构教师前向，求新增运算量。若更新权重向四个副本各独立发送一次，求 BF16 权重传输量。最后画出生成、反馈、更新与同步的依赖，指出哪些阶段可以在不同批次之间重叠。
+> 保持一次更新使用 16 条等长样本。利用例题 3-4 中分别生成 32 条和 64 条回答时的周期总运算量，求每多生成一条回答所增加的生成与参考模型运算量，以及一次更新所需的固定运算量。据此预测生成 48 条回答时的周期总运算量。再用结构相同的教师模型对全部 48 条回答各执行一次前向，求新增的运算量。若将更新后的 BF16 权重分别发送给四个副本，每个副本独立接收一份，求总传输量。最后画出生成、反馈、更新与同步的依赖，指出哪些阶段可以在不同批次之间重叠。
 
 ## 3.6 从负载需求到训练与服务预算
 
@@ -548,9 +550,9 @@
 
 Kaplan 等人的早期研究给出约 $N\propto C^{0.73},\ D\propto C^{0.27}$ 的分配关系；Chinchilla 研究用不同实验和拟合，得到模型规模与数据量应随预算更接近等比例增长的结果。前一种分配更快增加模型参数，后一种把更多新增预算分给训练数据；曲线及其拟合指数因此直接改变参数与数据的选择。[^scaling]
 
-**例题 3-5：怎样检验一条 Scaling Law？** 从公开训练记录中固定六个较小模型的观测用于拟合，另将两个较大模型的观测留出。先选定拟合方法，再比较留出模型的预测值与实际观测值。
-
-**解：** datablations 是研究模型与数据规模变化的公开实验记录，C4 是这些记录使用的一套文本语料。以其中八个模型为例，用六个较小模型拟合曲线，将 $N\ge2\times10^9$ 的两个模型留作检验。两个大模型的实际损失分别约为 2.574、2.337，拟合曲线预测为 2.583、2.363。预测略高于实际值，均方根误差约为 0.0193 nats/token。nat 是使用自然对数时的信息量单位，nats/token 表示每个 token 的平均损失。图 3-26 用不同标记区分拟合点与检验点。[^fit]
+**例题 3-5：训练规模拟合关系能否预测更大模型的损失？** 从公开训练记录中固定六个较小模型的观测用于拟合，另将两个较大模型的观测留出。先选定拟合方法，再比较留出模型的预测值与实际观测值。
+
+解： datablations 是研究模型与数据规模变化的公开实验记录，C4 是这些记录使用的一套文本语料。以其中八个模型为例，用六个较小模型拟合曲线，将 $N\ge2\times10^9$ 的两个模型留作检验。两个大模型的实际损失分别约为 2.574、2.337，拟合曲线预测为 2.583、2.363。预测略高于实际值，均方根误差约为 0.0193 nats/token。nat 是使用自然对数时的信息量单位，nats/token 表示每个 token 的平均损失。图 3-26 用不同标记区分拟合点与检验点。[^fit]
 
 ![八个公开 C4 观测的预测与实际损失。六点用于拟合，两点预先留出作检验；对角线表示预测等于观测，点到线的偏差反映误差。](ch03/figure-3-7-scaling.svg)
 
@@ -568,7 +570,7 @@
 
 长期服务会把每次调用的成本累积到训练预算之上。较小模型可以通过更长训练达到目标质量，前期多付成本，之后每次调用读取更少权重、执行更少工作。Beyond Chinchilla-Optimal 将这种推理需求纳入分析，47 个模型覆盖 150M–6B；其中 150M 模型测试到 10,000 token／参数，较大模型测试到 1,000。[^beyond] 训练预算与服务次数共同决定较小模型的额外训练何时收回成本。
 
-**生命周期算例的题设。** 固定拟合目标损失为 2.9，以矩阵运算量和给定单价估算训练与服务成本：请求取 $P=512,G=128$，训练运算量为 $6ND$，每次调用为 $2N[P+(G-1)]$，每 FLOP 按 $10^{-18}$ 个成本单位计价。0.1B 模型需要约 298.6B 训练 token，超过拟合所用数据量的上界，因此成本图用虚线标注这条外推曲线。按这条外推曲线计算，调用约 2.048 亿次时，0.1B 与 0.5B 模型的总成本相等。在此之前，0.1B 模型多付的训练成本尚未收回；在此之后，每次服务省下的成本累计超过了前期投入。[^lifecycle]
+**固定目标损失下的训练投入与累计服务成本** 固定拟合目标损失为 2.9，以矩阵运算量和给定单价估算训练与服务成本：请求取 $P=512,G=128$，训练运算量为 $6ND$，每次调用为 $2N[P+(G-1)]$，每 FLOP 按 $10^{-18}$ 个成本单位计价。0.1B 模型需要约 298.6B 训练 token，超过拟合所用数据量的上界，因此成本图用虚线标注这条外推曲线。按这条外推曲线计算，调用约 2.048 亿次时，0.1B 与 0.5B 模型的总成本相等。在此之前，0.1B 模型多付的训练成本尚未收回；在此之后，每次服务省下的成本累计超过了前期投入。[^lifecycle]
 
 ![训练与服务累计成本的题设比较。截距是训练投入，斜率是单次调用成本；虚线标出超出拟合参数或数据范围的方案，竖线为约 2.048 亿次的成本交点。](ch03/figure-3-lifecycle-cost.svg)
 
@@ -578,11 +580,11 @@
 
 > **练习 3-8〔延伸〕：从拟合关系预测预算分配**
 >
-> 在 $L=E+A/N^\alpha+B/D^\beta$ 与 $C=6ND$ 下，推导最优 $N$ 与 $D$ 随 $C$ 的指数关系。取 $\alpha=\beta$，预算增至四倍与九倍时分别怎样分配？再用八个 C4 点的六点拟合、两点检验，计算两个检验点的预测误差，解释为什么不能用同一批拟合误差代替检验误差。
+> 在 $L=E+A/N^\alpha+B/D^\beta$ 与 $C=6ND$ 下，推导最优 $N$ 与 $D$ 随 $C$ 的指数关系。取 $\alpha=\beta$，预算增至四倍与九倍时分别怎样分配？再使用例题 3-5 的八个 C4 观测点，用其中六个拟合、另外两个检验，计算两个检验点的预测误差，并解释为什么拟合点上的误差不能替代留出检验的误差。
 
 ### 3.6.2 从 Llama 到 Qwen 的训练投入
 
-前面的拟合说明了模型规模与训练数据之间可以怎样取舍。下面回到公开模型，观察开发者实际选择了怎样的训练投入：先比较每个参数对应的训练 token 数，再将算法运算量与具体硬件上的训练时间区分开。
+前面的拟合说明了模型规模与训练数据之间可以怎样取舍。下面用公开模型的训练记录，分析开发者实际采用的训练投入：先比较每个参数对应的训练 token 数，再将算法运算量与具体硬件上的训练时间区分开。
 
 从各报告披露的阶段和规模，可以看到一个趋势：相近的 7–8B 参数规模，训练 token 数持续增加。[^history]
 
@@ -662,9 +664,9 @@
 
 额外投入 100 万美元、每任务节省 0.002 美元时，$Q_*=5\times10^8$，即五亿次任务。预期需求低于这个量，额外训练成本尚未收回；高于这个量，较小模型累计节省更多。容量、延迟、质量和调用量共同决定选择，单看模型大小或 token 单价无法完成这一判断。
 
-> **练习 3-10〔延伸〕：日历时间与长期成本选择**
->
-> 将 1,022,362 GPU 小时分别分配给 2048 与 1024 张同类设备，求日历时间。再比较两个满足相同质量与时限的模型：小模型多投入 100 万美元训练，每个成功任务节省 0.002 美元。预计服务两亿次和十亿次时分别选哪个？计算总成本差，并说明若实际任务量发生变化，选择会在何处反转。
+> **练习 3-10〔延伸〕：训练完成时间与累计服务成本**
+>
+> 将 1,022,362 GPU 小时分别分配给 2048 与 1024 张同类设备，求训练从开始到完成所需的实际天数。再比较两个满足相同质量与时限的模型：小模型多投入 100 万美元训练，每个成功任务节省 0.002 美元。预计服务两亿次和十亿次时分别选哪个？计算总成本差，并说明若实际任务量发生变化，选择会在何处反转。
 
 后续章节将使用本章整理出的三类负载信息。下表列出各类负载需要记录的内容，以及这些信息将用于哪些系统设计问题。
 
@@ -680,8 +682,6 @@
 
 训练预算下的最优规模与长期服务的最优规模，也可能不同。固定训练计算量时，参数与训练 token 之间存在折中；若模型还要服务大量请求，多训练一个较小模型，可能用一次性训练投入换取持续较低的推理成本。Llama 3 报告说明，小模型会通过更长时间的训练提高能力，以满足推理成本要求。计算生命周期成本时，应将训练投入、预期服务量、每次任务成本和模型有效寿命分别列出。专用硬件若需要较长部署周期，模型版本能使用多久也会成为这一选择的条件。
 
-做第二轮思想实验时，可以重新选择模型规模和训练量，但必须重新验证任务质量。硬件加速后采用更长推理、更多分支，是另一种负载方案；应与保持原任务轨迹的方案并列，不能用未来可能增加的需求替代当前加速收益。
-
 ## 谬误与陷阱
 
 **均值相同就是同一负载。** 少量超长请求、输入与输出长度的组合、请求到达顺序，都会影响短时间内的资源需求。例题 3-1 中，处理能力足以应付平均到达量，后一分钟却仍会积压。
--- before/manuscripts/04-加速器架构.md
+++ manuscripts/04-加速器架构.md
@@ -26,7 +26,7 @@
 F=2MKN=2M\times4096^2=33\,554\,432M\ \mathrm{FLOPs}.
 $$
 
-再看数据。**本例的读写条件。** 输入、权重与输出均为 BF16，即每元素两字节的浮点表示；计算从零开始累加，FP32 是四字节浮点表示，部分和使用这一格式保存在芯片内部。假定输入与权重各从片外读取一次，最终结果写回一次，则总读写量为
+再看数据。**读写假设：输入和权重各读一次，结果写回一次** 输入、权重与输出均为 BF16，即每元素两字节的浮点表示；计算从零开始累加，FP32 是四字节浮点表示，部分和使用这一格式保存在芯片内部。假定输入与权重各从片外读取一次，最终结果写回一次，则总读写量为
 
 $$
 V=2MK+2KN+2MN=33\,554\,432+16\,384M\ \mathrm{bytes}.
@@ -78,7 +78,7 @@
 
 模型与芯片在一代代设计中相互影响。现有设备的能力影响模型的状态大小、矩阵形状和通信方式，软件通过压缩、融合和并行提高执行效率；当某种负载长期占据较大比重，芯片设计者便会考虑为它增加专用能力。新一代芯片投入使用后，不同模型的运行成本又会随之改变。设计和制造存在周期，因此硬件响应的是此前已经观察到、并预计会延续的需求。
 
-DeepSeek V3 报告给出了模型团队向硬件提出需求的具体例子：H800 上部分 SM 用于通信，报告据此讨论通信卸载、跨互联的一致操作，以及低精度累加和量化支持。[^feedback] 这些建议来自模型在现有设备上遇到的具体瓶颈。结合芯片的设计时间线，可以进一步研究这些需求如何影响后续硬件的设计。
+DeepSeek V3 报告针对 H800 上的执行瓶颈，提出通信卸载、跨互联的一致操作、低精度累加和量化支持等硬件需求；其中，通信卸载源于部分 SM 被通信占用。[^feedback] 结合芯片设计时间线，可以研究这些需求如何影响后续设计。
 
 ![图 4-3 模型与硬件跨代协同。实线沿时间向下：现有设备影响模型选择，软件运行暴露长期瓶颈，硬件设计回应这些需求，新设备使更多模型方案成为可能。](ch04/figure-4-codesign-loop.svg)
 
@@ -88,7 +88,7 @@
 
 计算单元、存储器和接口都占用芯片面积，也都消耗电能。分给某一部分的资源越多，留给其他部分的预算就越少。因此，评价一项局部改进，要看它能让整个任务缩短多少时间。
 
-回到开篇的串行任务：矩阵计算占 60 μs，其余工作占 40 μs。矩阵速度翻倍后，两部分分别用 30 μs 和 40 μs，合计 70 μs。若以 $f$ 表示可加速部分的时间比例，$s$ 表示其加速倍数，根据 Amdahl 定律，整体加速比为：
+本章开篇的串行任务中，矩阵计算占 60 μs，其余工作占 40 μs。矩阵速度翻倍后，两部分分别用 30 μs 和 40 μs，合计 70 μs。若以 $f$ 表示可加速部分的时间比例，$s$ 表示其加速倍数，根据 Amdahl 定律，整体加速比为：
 
 $$
 S=\frac{1}{(1-f)+f/s}=\frac{1}{0.4+0.6/2}\approx1.43.
@@ -100,7 +100,7 @@
 
 假设为了增加乘加器，设计者缩小了缓冲容量。原来能保留的输入块被提前逐出，下次使用时要再次加载；重复读取增加的时间，可能抵消新增乘加器带来的收益。反过来，为很少重复使用的数据配置过大的缓冲，也会占用本可用于计算的面积。比较设计方案时，要计算同一任务在改动前后的总时间，把计算加快与重复读取增加的影响一起考虑。
 
-> **思考。** 在上述 60 μs 加 40 μs 的任务中，把矩阵速度从两倍提高到四倍，与把其余工作从 40 μs 降至 25 μs，哪种改动节省的时间更多？如果两者面积代价不同，应怎样选择？
+> **思考：矩阵加速与其余工作加速的收益比较** 在上述 60 μs 加 40 μs 的任务中，把矩阵速度从两倍提高到四倍，与把其余工作从 40 μs 降至 25 μs，哪种改动节省的时间更多？如果两者面积代价不同，应怎样选择？
 
 下一节先分析计算单元怎样利用矩阵运算的规律，再引入那些无法由矩阵乘法完成的步骤。这样才能确定公式中的“可加速部分”究竟包含什么。
 
@@ -126,7 +126,7 @@
 
 若行方向最小为 16，真实矩阵只有一行，其余维度均对齐，则 16 行中只有一行包含有效输入。把矩阵单元数量翻倍，会同时加快有效计算和补零计算；改用适合单行的矩阵向量乘法（GEMV）实现，则可以减少补零造成的无用计算。第 4.7 节的实测将展示库如何选择不同实现。
 
-专家模型让行数问题更加突出。设 64 个 token 各选择八个专家，共有 512 次分派；各专家矩阵尺寸相同。如果分派恰好均匀覆盖 256 个专家，每个专家只处理两个 token 的特征向量；如果全部 token 选择同样的八个专家，每个专家处理 64 个 token 的特征向量。两种情况下，64 个原始 token 都产生 512 次专家分派，在各专家输入矩阵中合计占 512 个有效行。内核为满足计算块大小而补入的零行另行计数，不代表新的 token。
+专家模型让行数问题更加突出。设 64 个 token 各选择八个专家，共有 512 次分派；各专家矩阵尺寸相同。如果分派恰好均匀覆盖 256 个专家，每个专家只处理两个 token 的特征向量；如果全部 token 选择同样的八个专家，每个专家处理 64 个 token 的特征向量。两种情况下，64 个原始 token 都产生 512 次专家分派，在各专家输入矩阵中合计占 512 个有效行。
 
 继续使用行方向按 16 补齐的实现。前一种分派执行 $256\times16=4096$ 行，其中 512 行有效；后一种执行 $8\times64=512$ 行，全部有效。两种分派的有效计算相同，补齐后的工作却相差八倍。一批 token 选中了哪些专家，既决定需要读取哪些权重，也决定每个专家处理多少行，从而影响矩阵单元的利用率。
 
@@ -190,15 +190,15 @@
 
 *图 4-7　同一注意力块在四种资源配置下的服务周期。矩阵、共享存储和指数三项分别比较，单独增加一种能力后，其他资源可能成为较长的一项。*
 
-> **例 4-1：只增加矩阵能力是否值得？**
->
-> **问题。** 多个块在独立资源间形成流水。只把矩阵吞吐率提高到原来的两倍，然后同时把指数吞吐率提高到原来的两倍，流水线稳定运行时，相邻两个块最短隔多久完成？
->
-> **推导。** 原配置为 $\max(1024,768,1024)=1024$ 周期。只提高矩阵能力，得到 $\max(512,768,1024)=1024$ 周期；再提高指数能力，得到 $\max(512,768,512)=768$ 周期。
->
-> **结果解释。** 第一项改动使矩阵单元更早完成，但指数运算的吞吐率保持不变，完成时间间隔的下界不变。第二项改动消除了指数的限制，共享内存成为耗时最长的单元，对应的理想吞吐提高到原来的 $1024/768=4/3$ 倍。
->
-> **改变条件。** 若共享内存速度也翻倍，三项变为 512、384、512 周期，下界才降到 512。新的均衡点由矩阵与指数共同决定。
+> **例 4-1：注意力流水的吞吐为何受指数运算与共享内存限制？**
+>
+> **比较矩阵与指数吞吐提高后的块完成间隔** 多个块在独立资源间形成流水。只把矩阵吞吐率提高到原来的两倍，然后同时把指数吞吐率提高到原来的两倍，流水线稳定运行时，相邻两个块最短隔多久完成？
+>
+> **由最慢资源求注意力流水的服务周期** 原配置为 $\max(1024,768,1024)=1024$ 周期。只提高矩阵能力，得到 $\max(512,768,1024)=1024$ 周期；再提高指数能力，得到 $\max(512,768,512)=768$ 周期。
+>
+> **矩阵加速后，瓶颈向指数运算与共享内存转移** 第一项改动使矩阵单元更早完成，但指数运算的吞吐率保持不变，完成时间间隔的下界不变。第二项改动消除了指数的限制，共享内存成为耗时最长的单元，对应的理想吞吐提高到原来的 $1024/768=4/3$ 倍。
+>
+> **共享内存速度也翻倍后的新服务周期** 若共享内存速度也翻倍，三项变为 512、384、512 周期，下界才降到 512。新的均衡点由矩阵与指数共同决定。
 
 提高一种资源的处理能力，可以减少等待，直到另一种资源成为瓶颈。对单个块，QK、Softmax、PV 仍有先后关系；要达到上述完成时间间隔，需要同时保存不同进度的多个块。第 4.4 节将把所需缓冲与时序一起算出来。
 
@@ -242,17 +242,17 @@
 
 > **实验 4-2 · 延伸：低精度执行成本**
 >
-> 从上述分组点积式出发，列出两种方法需要保存的权重、scale、展开副本、部分和与输出。计算加载时展开一次、每次调用展开、直接分组计算三种策略在重复使用 $R$ 次后的总时间。再用实际输入的实验记录比较误差与显存占用峰值，解释何时值得用较大副本换取较少调用。
+> 从上述分组点积式出发，列出两种方法各自需要保存的权重、量化尺度、展开后的权重副本、部分和与输出。计算加载时展开一次、每次调用展开、直接分组计算三种策略在重复使用 $R$ 次后的总时间。再用实际输入的实验记录比较误差与显存占用峰值，解释何时值得多占用内存来保存展开后的权重，以减少重复展开的开销。
 
 ## 4.3 存储层次
 
-现在回到 Q 投影的数据读写。上一节说明了数值格式怎样改变数据大小，本节进一步区分三个问题：这些数据能否放下，重复使用时从哪一级读取，独立访问是否足以维持目标带宽。
+接下来分析 Q 投影的数据读写。上一节说明了数值格式怎样改变数据大小，本节进一步区分三个问题：这些数据能否放下，重复使用时从哪一级读取，独立访问是否足以维持目标带宽。
 
 继续分析 V4.1 处理会话时的数据读写，可以看出降低位宽首先减少了 KV 的存储和传输量。主 KV 的 512 个值及分组 scale 共占 288 B；局部 SWA 采用 FP8 加 scale，每条为 528 B。主 KV 在注意力计算前先反量化，因此 FP4 主要用于缓存的存储和传输，注意力乘法使用的是反量化后的数据。主通路降低位宽、局部通路保留更高精度，是按数据敏感性分别选择。[^v41-case]
 
 硬件条件也影响量化格式和量化位置的选择。索引采用 OCP MXFP4，以覆盖更多硬件平台，尽管实验中其他格式的精度更高；主 KV 选择在 RoPE 后量化，因为 RoPE 前量化虽略有精度收益，却增加 decode 开销。报告还测试了去掉第二级全局 scale 的方案，在该实验中未测出精度下降。
 
-若一种新设备提高了 HBM 带宽，第一轮保持这些格式不变，检查节省的读取时间；第二轮才比较提高主 KV 精度是否合算。需要同时重测任务质量、反量化成本和缓存容量，而不是根据算力表中的低精度峰值直接选择格式。
+若一种新设备提高了 HBM 带宽，先保持这些格式不变，计算节省的读取时间，再比较提高主 KV 精度后的收益与开销。格式选择需要结合任务质量、反量化成本和缓存容量进行评估。
 
 ### 4.3.1 显存与统一内存
 
@@ -290,7 +290,7 @@
 
 高带宽内存（HBM）通过堆叠存储裸片和宽接口提供带宽；图形双倍数据速率内存（GDDR）通常以多颗存储芯片配合高速接口提供显存。两者都是内存器件与接口技术，统一内存是处理器共享内存的组织方式。Apple 的 CPU 与 GPU 访问共享物理内存，两者可以先后使用同一个缓冲区，减少独立副本；相应地，CPU、GPU 与其他应用也共同消耗这份容量与带宽。[^apple] 第 4.5 节会沿主机到设备的路径分析这种组织对传输时间的影响。
 
-> **思考。** 若每条请求 KV 需求从 1.21 GB 降低 10%，上述设备能否容纳第五条请求？先求五条请求的总需求，再比较剩余空间。
+> **思考：KV 压缩能否容纳第五条请求？** 若每条请求 KV 需求从 1.21 GB 降低 10%，上述设备能否容纳第五条请求？先求五条请求的总需求，再比较剩余空间。
 
 ### 4.3.2 缓存、片上缓冲与寄存器
 
@@ -336,13 +336,13 @@
 
 > **例 4-2：把接口带宽翻倍，读取会快一倍吗？**
 >
-> **问题。** 读取一条请求的 8,192 位置 KV，共 1.125 GiB。接口带宽为 1 TB/s，每事务 128 bytes，完成延迟为 500 ns。比较 128 和 4,096 个在途事务，再把接口升级到 2 TB/s。
->
-> **推导。** 维持 1 TB/s 需要 $\lceil10^{12}\times500\times10^{-9}/128\rceil=3907$ 个事务。128 个事务只能提供约 32.8 GB/s，读取时间至少为 36.9 ms；4,096 个事务足以维持 1 TB/s，时间降为约 1.21 ms。
->
-> **结果解释。** 升级到 2 TB/s 后，4,096 个事务仍只能支持约 $4096\times128/(500\ \mathrm{ns})=1.05$ TB/s，时间约为 1.15 ms。接口增加一倍带宽，而未完成事务数的限制使吞吐率只能提高约 5%。
->
-> **改变条件。** 同时把在途事务数提高到至少 7,813 个，才能维持新的 2 TB/s；若延迟增至 800 ns，这个要求又增至 12,500 个。
+> **比较在途事务数与接口带宽对 KV 读取的限制** 读取一条请求的 8,192 位置 KV，共 1.125 GiB。接口带宽为 1 TB/s，每事务 128 bytes，完成延迟为 500 ns。比较 128 和 4,096 个在途事务，再把接口升级到 2 TB/s。
+>
+> **由在途字节数和访问延迟求有效读取带宽** 维持 1 TB/s 需要 $\lceil10^{12}\times500\times10^{-9}/128\rceil=3907$ 个事务。128 个事务只能提供约 32.8 GB/s，读取时间至少为 36.9 ms；4,096 个事务足以维持 1 TB/s，时间降为约 1.21 ms。
+>
+> **在途事务不足使接口带宽翻倍只带来有限收益** 升级到 2 TB/s 后，4,096 个事务仍只能支持约 $4096\times128/(500\ \mathrm{ns})=1.05$ TB/s，时间约为 1.15 ms。接口增加一倍带宽，而未完成事务数的限制使吞吐率只能提高约 5%。
+>
+> **带宽或访问延迟增加时所需的在途事务数** 同时把在途事务数提高到至少 7,813 个，才能维持新的 2 TB/s；若延迟增至 800 ns，这个要求又增至 12,500 个。
 
 ![在每事务 128 bytes、返回延迟 500 ns 的题设下，增加在途请求数提高带宽上界，直到碰到接口本身的速率上限。](ch04/figure-4-7-memory.svg)
 
@@ -445,7 +445,7 @@
 
 硬件机制提供执行这种安排的手段：NVIDIA Ampere 架构（A100 所属架构）的异步拷贝可以省去部分加载过程中的寄存器中转，Hopper TMA 用描述符发起较大的张量搬运，昇腾 MTE／NDDMA 配合同步事件交接数据，Metal 则按照任务之间以及缓冲区读写之间的依赖安排执行。[^transfer] 在每一种实现中，搬运单元向空槽写入数据，计算单元用完数据后释放该槽。槽位既要保存尚未用完的输入，又要容纳提前取得的数据，因此决定了计算能否正确进行，以及搬运和计算能重叠多少。
 
-> **思考。** 保持原来的传输和计算速率，把输入完成的额外延迟从 128 增至 256 tick。三个槽能否继续实现无间隙计算？分别写出第四块的就绪时刻与所需时刻。
+> **思考：传输延迟翻倍后三个缓冲槽是否够用？** 保持原来的传输和计算速率，把输入完成的额外延迟从 128 增至 256 tick。三个槽能否继续实现无间隙计算？分别写出第四块的就绪时刻与所需时刻。
 
 ### 4.4.3 单元间的数据传递
 
@@ -475,7 +475,7 @@
 
 > **实验 4-4 · 核心：片上缓冲与数据复用**
 >
-> 重建输入流水的一至四槽时间线，用数据到达、计算结束和缓冲区释放时刻证明最少槽数；再将矩阵速率翻倍，解释最少槽数的变化。随后重建行组交接表，区分分组、增加槽位和直接传递各自省下的时间。最后在固定片上容量下，比较每组缓冲与同时驻留组数的取舍。
+> 分别画出输入流水使用一至四个缓冲槽时的时间线，用数据到达、计算结束和缓冲区释放时刻证明最少槽数；再将矩阵速率翻倍，解释最少槽数的变化。随后重建行组交接表，区分分组、增加槽位和直接传递各自省下的时间。最后在固定片上容量下，分析每组缓冲的大小如何限制能够同时驻留的组数。
 
 至此，计算单元之间的协作已经可以通过各步骤的计算量、使用的硬件单元和数据依赖来分析。跨芯片执行会增加更长的数据路径，但仍然遵循同样的结果生成、传递和使用顺序。
 
@@ -507,7 +507,7 @@
 
 ### 4.5.2 CPU 与加速器之间的数据访问
 
-封装内的数据放置决定跨裸片传输；设备外也有类似问题。回到图 4-2，CPU 准备的数据需要交给 GPU 使用。CPU 准备的输入通常先位于主机内存，独立显存 GPU 通过 PCIe 或平台专用连接取得数据。主机到设备称为 H2D，设备到主机称为 D2H。复制引擎通过直接内存访问（DMA）按准备好的地址搬运，CPU 提交复制命令后可以处理其他工作。
+封装内的数据放置决定跨裸片传输；设备外也有类似问题。图 4-2 展示了设备之间的数据交接：CPU 准备的数据需要传给 GPU 使用。CPU 准备的输入通常先位于主机内存，独立显存 GPU 通过 PCIe 或平台专用连接取得数据。主机到设备称为 H2D，设备到主机称为 D2H。复制引擎通过直接内存访问（DMA）按准备好的地址搬运，CPU 提交复制命令后可以处理其他工作。
 
 同一个张量先经主机链路上传，再由 GPU 从显存读取，需要分别计算传输和读取的时间。设输入为 64 MiB，主机链路有效带宽为 32 GB/s，GPU 内存读取为 1 TB/s，先忽略启动开销，则
 
@@ -558,7 +558,7 @@
 
 NVLink、昇腾 Unified Bus 等互联提供带宽与可达路径，完成通知让接收方及时开始计算。[^package] 分析它们时，仍是从每次传输的数据量、固定开销，以及接收方需要等待哪些数据出发；本节的转折点让我们能够判断应优先减少消息次数，还是增加持续传输能力。
 
-> **思考。** 对一条 8 KiB 消息，将带宽由 100 增至 200 GB/s，与将固定开销由 2 降至 1 μs，各节省多少时间？对 2 MiB 消息重复计算，解释选择为何变化。
+> **思考：消息大小如何改变带宽与启动延迟的优化收益？** 对一条 8 KiB 消息，将带宽由 100 增至 200 GB/s，与将固定开销由 2 降至 1 μs，各节省多少时间？对 2 MiB 消息重复计算，解释选择为何变化。
 
 ## 4.6 专用架构
 
@@ -598,7 +598,7 @@
 
 改变输入，就能让同一个模型执行不同的上层任务。同一组模型权重既可以处理文档问答，也可以根据代码和测试结果提出修改；每次调用使用自己的上下文状态。因而，硬件可以针对共同的模型执行设计权重通路，同时用可写存储保存各条请求的上下文。下面把权重与上下文分别计量，计算这种分工能够释放多少容量和带宽。
 
-作者的 OpenTallas 项目研究固定权重架构。下面以项目中的 Qwen3-8B 为例，分析“从独立只读存储器（ROM）读取权重、从 HBM 读写 KV”的设计怎样改变各接口的传输量。[^opentallas] 每批只读取一次本步计算所需的权重，约 $W=15.1$ GB；每条请求完整读取 8,192 位置 KV，约 $K=1.21$ GB。完整驻留权重约 16.4 GB，每步读取其中参与计算的部分。
+作者的 OpenTallas 项目研究将不可变权重放入掩模 ROM 的架构。下面以项目中的 Qwen3-8B 为例，通过解析估算说明“从独立只读存储器（ROM）读取权重、从 HBM 读写 KV”的设计怎样改变各接口的传输量。[^opentallas] 每批只读取一次本步计算所需的权重，约 $W=15.1$ GB；每条请求完整读取 8,192 位置 KV，约 $K=1.21$ GB。完整驻留权重约 16.4 GB，每步读取其中参与计算的部分。
 
 先看同一接口原来承担多少读取。权重与 KV 共用 HBM 时，每步读取为 $W+BK$。把权重移到独立 ROM 后，HBM 只承担 $BK$。两种 HBM 读取量之比为
 
@@ -626,17 +626,17 @@
 
 因此，评价专用架构需要回答两个问题：它省去了哪些操作，省去之后又由什么限制速度；模型被替换之前，它能完成多少任务，节省的成本能否收回投入。第 4.7 节将结合性能与能耗计算这些收益。
 
-分别考察上述实验的假设，可以明确模型更新受到哪些限制。独立存储的容量固定，只限制能放多少权重；内容不可修改，才限制权重更新；计算图固定，则进一步限制算子和形状。OpenTallas 的掩模 ROM 设想涉及不可变权重，不能把它等同于普通 RAM 容量不变。这里引用的是项目设计与解析估算，并非目标芯片的实测吞吐。[^opentallas]
-
-第一轮保持 Qwen3-8B、BF16、上下文和 batch 不变。两条独立路径理想重叠时，存储服务时间下界变为
+先保持 Qwen3-8B、BF16、上下文和 batch 不变。两条独立路径理想重叠时，完成存储访问所需的最短时间变为
 
 $$
 t_{\mathrm{memory}}\geq\max\left(\frac{W}{\beta_{\mathrm{ROM}}},\frac{BK}{\beta_{\mathrm{HBM}}}\right).
 $$
 
-只有 ROM 足够快、计算和其他依赖都不占主导时，前面的 HBM 流量比才能接近单步加速比。即使权重读取耗时趋近于零，KV、计算和通信仍然存在。第二轮则释放原来的权重容量：24 GB HBM 扣除 2 GiB 工作区后，按每条 1.125 GiB 的静态 KV 预算，可容纳 18 条 8K 请求；原来还需存放完整 BF16 权重时只能容纳 4 条。[^core-calculation]这是某一时刻的状态容量，随着生成继续、KV 增长，需要重新判断能否接收新请求。
-
-容量允许扩大 batch，速度却未必允许。8K 时 $BK=W$ 的交点约为 $B=12.53$；32K 时交点降至约 3.13。更长上下文使 KV 更早主导。于是模型设计仍有压缩 KV 的动机，服务系统仍要管理动态状态，而原来为了放下权重而增加设备的理由可能减弱。后续章节将继续检查并行、批处理和版本更新是否还值得采用。
+提高 ROM 带宽会缩短第一项，直到 KV 读取成为较慢的路径。
+
+独立 ROM 还释放了原来存放权重的 HBM 容量。固定 8K 上下文，24 GB HBM 扣除 2 GiB 工作区后，按每条 1.125 GiB 的静态 KV 预算，可容纳 18 条 8K 请求；原来还需存放完整 BF16 权重时只能容纳 4 条。[^core-calculation]
+
+扩大 batch 后，新增请求的 KV 读取会继续增加。8K 时 $BK=W$ 的交点约为 $B=12.53$；32K 时交点降至约 3.13。更长上下文使 KV 更早主导。于是模型设计仍有压缩 KV 的动机，服务系统仍要管理动态状态，而原来为了放下权重而增加设备的理由可能减弱。后续章节将继续检查并行、批处理和版本更新是否还值得采用。
 
 ![图 4-23 独立只读权重改变两条存储路径。上方权重与 KV 争用 HBM；下方 ROM 提供权重，HBM 保留可写状态。箭头表示读取，KV 还需写入新状态。](ch04/figure-4-rom-paths.svg)
 
@@ -707,7 +707,7 @@
 
 > **实验 4-5 · 核心：batch 与架构性能**
 >
-> 推导并画出 Q 投影从 1 到 256 行的算术强度，求矩阵能力、内存带宽分别翻倍后的转折行数。随后把注意力与专家矩阵加入资源表，解释上下文和专家行数分布怎样改变耗时最长的单元。选择两个具体设备，将相同任务放入各自资源模型比较。
+> 推导并画出 Q 投影从 1 到 256 行的算术强度，分别将矩阵计算吞吐率、内存带宽提高到原来的两倍，求计算时间与读取时间相等时的输入行数。随后把注意力与专家矩阵加入资源表，解释上下文和专家行数分布怎样改变耗时最长的单元。选择两个具体设备，将相同任务放入各自资源模型比较。
 
 这个模型已经明确指出了复用的作用。下一节考察运行库实际执行的计算，观察同一权重的复用究竟发生在片外、缓存还是局部计算之中。
 
@@ -752,7 +752,7 @@
 
 > **实验 4-6 · 延伸：Mac 与 RTX 的预测和测量**
 >
-> 先用投影的实测总耗时提出两个时间差解释，再读取 DRAM、L2 与内核记录，为每个解释列出预期现象和实际现象。随后画出 256 行调用的矩阵、部分结果与归约路径，指出需要哪些阶段时间才能区分计算单元工作时的执行效率与等待时间。将所得结果与假设输入和权重均从片外读取的模型对照。
+> 根据投影在两种设备上的实测总耗时，提出两个能够解释耗时差异的原因，再读取 DRAM、L2 与内核记录，为每个解释列出预期现象和实际现象。随后画出 256 行调用的矩阵、部分结果与归约路径，指出需要哪些阶段时间才能区分计算单元工作时的执行效率与等待时间。将所得结果与假设输入和权重均从片外读取的模型对照。
 
 ### 4.7.4 在容量、速度、功耗与成本之间做选择
 
@@ -786,13 +786,13 @@
 
 > **例 4-3：专用化能否在模型寿命内回本？**
 >
-> **问题。** 增量固定成本为 2,000 万美元，每百万合格输出 token 节省 0.50 美元。部署 100 台，每台处理请求时产出 10,000 token/s，利用率 50%，模型经济寿命一年，每年按 365 天计算。
->
-> **推导。** 回本量为 $20\,000\,000/(0.50/10^6)=4\times10^{13}$ token，即 40 万亿。一年产出为 $100\times10\,000\times0.5\times31\,536\,000\approx1.58\times10^{13}$ token，即约 15.8 万亿。
->
-> **结果解释。** 一年产出约为回本量的 39%。在单机速率与利用率保持不变时，至少需要 254 台按上述速率和利用率运行一年，并有足够多的请求使用这些设备。
->
-> **改变条件。** 模型半年更换，每台可用时间减半，设备与需求规模要增至至少 508 台；若利用率从 50% 降至 25%，也产生相同结果。
+> **给定固定投入、单次节省与模型可用时间** 增量固定成本为 2,000 万美元，每百万合格输出 token 节省 0.50 美元。部署 100 台，每台处理请求时产出 10,000 token/s，利用率 50%，模型经济寿命一年，每年按 365 天计算。
+>
+> **比较回本所需输出量与一年可完成的输出量** 回本量为 $20\,000\,000/(0.50/10^6)=4\times10^{13}$ token，即 40 万亿。一年产出为 $100\times10\,000\times0.5\times31\,536\,000\approx1.58\times10^{13}$ token，即约 15.8 万亿。
+>
+> **满足回本目标需要多少设备与请求需求？** 一年产出约为回本量的 39%。在单机速率与利用率保持不变时，至少需要 254 台按上述速率和利用率运行一年，并有足够多的请求使用这些设备。
+>
+> **模型寿命减半或利用率下降后的回本规模** 模型半年更换，每台可用时间减半，设备与需求规模要增至至少 508 台；若利用率从 50% 降至 25%，也产生相同结果。
 
 这个算例说明，能否回本既取决于每个任务能省多少钱，也取决于模型被替换之前能完成多少任务。只有模型使用时间足够长、实际需求足够多，才能积累足够的节省来收回固定投入。[^opentallas] 这与芯片内部的资源取舍遵循相同原则：比较增加资源以后，目标任务实际减少的代价。
 
--- before/manuscripts/05-算子与运行时.md
+++ manuscripts/05-算子与运行时.md
@@ -46,7 +46,7 @@
 
 运行完整模型时，并非每次调用都要重新传入所有数据。权重加载后可以一直保留在显存中，层间激活可以由下一个内核直接读取，KV 也可以在后续 decode 步中继续使用。如果采样在 GPU 上完成，CPU 只需接收少量 token ID，即输出词元在词表中的整数编号。因此，主机与设备之间的复制次数，与设备内部的读取次数并不相同：一份权重可以只传入一次，却在设备上读取很多次。
 
-**例 5-1：传输一批输入。** 一个 $[8192,4096]$ 的 BF16 张量占 $8192\times4096\times2=64$ MiB。取 H2D 有效单向带宽为 24 GiB/s，传输时间为：
+**例 5-1：主机到 GPU 的输入传输需要多久？** 一个 $[8192,4096]$ 的 BF16 张量占 $8192\times4096\times2=64$ MiB。取 H2D 有效单向带宽为 24 GiB/s，传输时间为：
 
 $$
 T_{\mathrm{H2D}}=\frac{64\ \mathrm{MiB}}{24\ \mathrm{GiB/s}}\approx2.6\ \mathrm{ms}.
@@ -77,7 +77,7 @@
 
 把复制、计算和缓冲使用的顺序连起来，才能确定结果何时可用。图 5-1 将这一顺序画成时间线。
 
-**例 5-2：同一次执行的三种计时结果。** 输入已在主机准备好。CPU 在 0–3 μs 内提交全部任务，第一项设备工作从 3 μs 开始；H2D、kernel、D2H 分别用 8、20、4 μs，设备按依赖连续执行。
+**例 5-2：主机提交、设备执行与结果可用为何有不同耗时？** 输入已在主机准备好。CPU 在 0–3 μs 内提交全部任务，第一项设备工作从 3 μs 开始；H2D、kernel、D2H 分别用 8、20、4 μs，设备按依赖连续执行。
 
 ![图 5-1 提交、执行与结果可用的时间](ch05/figure-5-1-execution.svg)
 
@@ -107,7 +107,7 @@
 
 如果这行 A 在两次使用之间仍留在局部存储或缓存中，就能直接复用；若已被替换，就需要再次向下一层请求。转到下一行输出时，W 中的元素也会再次使用。数学上需要同一份数据多次参与计算，程序如何安排这些使用，决定了它要跨越存储接口多少次。
 
-因此，本节先计数“工作缓冲与下一层存储之间的访问”。这不直接等于显存流量：如果下一层是 L2（二级缓存），多个线程块的重复请求可能由 L2 中的同一份副本提供，只有未命中时才继续请求显存。后面的约 3 GiB 也统计这一接口上的访问字节数。[^tiles]
+本节以工作缓冲和下一层存储之间的接口为计数位置，累计程序每次读入与写出的字节数。后面约 3 GiB 的访问量，也按这一接口计算。[^tiles]
 
 把图中的逐元素计算写成循环，就是：
 
@@ -132,7 +132,7 @@
 V_{\mathrm{once}}=2(MK+KN+MN)=(8+96+24)\ \mathrm{MiB}=128\ \mathrm{MiB}.
 $$
 
-其中 MiB 为 $2^{20}$ bytes。这个基准只计入各输入的一次读取和最终输出的一次写出，并不保证给定容量下的程序能够做到。
+其中 MiB 为 $2^{20}$ bytes。以各输入读一次、输出写一次为基准，后面就能计算分块增加了多少重复访问。
 
 循环顺序还决定每次访问的地址。W 按行连续存储时，上面的内层循环依次读取 W[k,j]、W[k+1,j]，地址相差 N 个元素。若改成 i、k、j 的顺序，内层便依次读取 W[k,j]、W[k,j+1]，同时将 A[i,k] 用于多个输出。这样一次连续传输取得的数据能立即参与计算。
 
@@ -148,7 +148,7 @@
 
 A 块中的一个元素可以用于 n 列输出，W 块中的一个元素可以用于 m 行输出。扩大输出块，就能让一次读入的数据参与更多乘加；同时也要保存更多输入和输出部分和。
 
-**例 5-3：扩大输出块能减少多少读取。** 图中的三个对象分别占 $2mk$、$2kn$ 和 $4mn$ bytes。只使用一组输入缓冲时，局部存储需求为：
+**例 5-3：扩大矩阵输出块能减少多少输入读取？** 图中的三个对象分别占 $2mk$、$2kn$ 和 $4mn$ bytes。只使用一组输入缓冲时，局部存储需求为：
 
 $$
 S=\underbrace{2mk}_{A\text{ 块}}+\underbrace{2kn}_{W\text{ 块}}+\underbrace{4mn}_{\text{累加器}}\quad\mathrm{bytes}.
@@ -178,11 +178,11 @@
 | 64×64 | 24 KiB | 3096 MiB | 31.7 FLOP/byte |
 | 128×128 | 80 KiB | 1560 MiB | 63.0 FLOP/byte |
 
-访问量接近减半，时间是否也会减半？还要看设备能同时安排多少工作。先用一个简化的容量模型：每个线程块（一组协作执行、可共享局部数据的 GPU 线程）负责一个输出块，所有工作集共用 96 KiB 的局部存储预算。
+访问量接近减半，时间是否也会减半？还要看设备能同时安排多少工作。先用一个简化的容量模型：每个线程块（一组协作执行、可共享局部数据的 GPU 线程）负责一个输出块，把输入缓冲与累加器合并计算，所有工作集共用 96 KiB 的局部存储预算。
 
 ![图 5-5 同一局部存储预算容纳的工作集](ch05/figure-5-tile-residency.svg)
 
-*图 5-5：条形总长均表示 96 KiB。24 KiB 的工作集能放四份，80 KiB 的只能放一份。这里把输入缓冲和累加器合并计入本例的容量上限；实际 GPU 的共享内存、寄存器分别受限，真实驻留数还取决于线程数等约束。*
+*图 5-5：条形总长均表示 96 KiB。24 KiB 的工作集能放四份，80 KiB 的只能放一份。*
 
 同时驻留的线程块可以交错发起访存：一些块等待数据时，另一些块继续工作。工作集从四份减为一份，可能降低隐藏访存等待的能力。因此，大块虽然少读数据，实际获得的带宽却可能下降。
 
@@ -194,6 +194,8 @@
 
 若带宽相同，时间接近减半；若大块只能获得原来四成的带宽，时间比约为 $0.504/0.4=1.26$，反而慢约 26%。可以先按容量排除放不下的方案，再按访问量缩小比较范围，最后测量实际执行效率。5.4 节将把这一过程交给编译器。
 
+需要注意的是，上述驻留比较将输入缓冲与累加器合并为一个容量预算。硬件会分别限制这些存储，例如 GPU 的共享内存与寄存器各有上限，块形状要同时满足各自的容量条件。确定能驻留多少线程块后，下一步便是检查这些线程的访问能否同时得到服务。
+
 ### 5.2.3 数据布局与归约方式对并行执行的影响
 
 上一小节中，大块虽然少读数据，却可能因并行度下降而变慢。并行度不仅取决于驻留多少线程块，也取决于这些线程发出的读取请求能否同时得到服务。先看局部存储的布局。考虑一个 32×32 的 FP32 暂存块，分成 32 个可独立服务读取请求的存储分体，称为 bank，每个 bank 每轮提供一个 32-bit 字，bank 号为字地址对 32 取模。这里将同时执行的一组线程中的各个位置称为 lane；32 个 lane 各读一行中的一个字时，落在 32 个 bank；读一列时，行跨度为 32 个字，所有地址落到同一 bank，需要 32 轮。
@@ -234,7 +236,7 @@
 
 *图 5-8：上半图的完整 T 经过一次写出与一次读回；下半图中局部片段 t 直接传给乘法。两种方式仍读取 G、U 并写出 Z，图中省略这些共同的输入输出边。融合保持原来的中间舍入语义。*
 
-**例 5-4：省去中间结果的写回和重读。** 分开执行时，先计算 $T=\operatorname{SiLU}(G)$，读取 G、写出 T，共 48 MiB；再计算 $Z=T\odot U$，读取 T、U，写出 Z，共 72 MiB。总访问为 120 MiB。将两步融合，每次读取 G、U 的一片，在局部完成 SiLU 和乘法，写出 Z，只需 72 MiB。
+**例 5-4：激活算子融合如何减少中间张量读写？** 分开执行时，先计算 $T=\operatorname{SiLU}(G)$，读取 G、写出 T，共 48 MiB；再计算 $Z=T\odot U$，读取 T、U，写出 Z，共 72 MiB。总访问为 120 MiB。将两步融合，每次读取 G、U 的一片，在局部完成 SiLU 和乘法，写出 Z，只需 72 MiB。
 
 省下的 48 MiB 正是 T 的一次写回和一次读取。沿用上一节的带宽模型，取两种程序的有效带宽相同，时间比为 $72/120=0.6$，加速约 1.7 倍。实际激活链对照从约 14.2 μs 降到 7.7 μs，约快 1.8 倍。理论计算与实测结果都说明了同一个收益来源：省去完整 T 的写回和重读。[^exp2]
 
@@ -260,7 +262,7 @@
 
 *图 5-10：块 0 在 2–5 μs 使用槽 A，所以块 2 到 5 μs 才能开始写入 A。图中抽取三个时段；4–5 μs 的块 1 已在槽 B 中等待，尚未开始计算。颜色固定表示槽 A、B，文字说明当前读写的是哪个块。*
 
-**例 5-5：四块数据的双缓冲。** 每块搬运 2 μs，计算 3 μs。搬运器和计算单元独立工作，事件开销记为零。串行执行四块需要 $4\times(2+3)=20$ μs。
+**例 5-5：双缓冲如何重叠数据搬运与计算？** 每块搬运 2 μs，计算 3 μs。搬运器和计算单元独立工作，事件开销记为零。串行执行四块需要 $4\times(2+3)=20$ μs。
 
 采用两个槽后，0–2 μs 搬入块 0，2–5 μs 计算块 0；块 1 在 2–4 μs 搬入，5 μs 时已经就绪。此后每隔 3 μs 完成一块。块 2 在 5 μs 才开始写槽 A，因为此前计算仍在读块 0；块 3 同理在 8 μs 开始写槽 B。第四块于 14 μs 完成，时间减少 30%。
 
@@ -298,7 +300,7 @@
 
 要丢弃已经处理过的分数，需要保留三个量：最大分数 m，以 m 为基准的指数和 $\ell$，以及相同指数权重下的值之和 u。这里 u 通常是向量，先用标量值的小例子看清更新过程。
 
-**例 5-6：用两个块完成一行 Softmax。** 分数为 $[0,\ln2]$，对应值为 $[1,3]$，每块只含一个元素。第一块得到 $m=0,\ell=1,u=1$。处理第二块后，最大值增至 $\ln2$，将原来的 $\ell$ 和 u 都乘以 $r=1/2$；新元素的指数为 1，于是 $\ell'=1/2+1=1.5$，$u'=1/2+3=3.5$，结果为 $7/3$。一次处理整行时，两个权重正比于 1 和 2，结果同样为 $(1+2\times3)/3=7/3$。
+**例 5-6：在线 Softmax 如何合并分块结果并保持归一化？** 分数为 $[0,\ln2]$，对应值为 $[1,3]$，每块只含一个元素。第一块得到 $m=0,\ell=1,u=1$。处理第二块后，最大值增至 $\ln2$，将原来的 $\ell$ 和 u 都乘以 $r=1/2$；新元素的指数为 1，于是 $\ell'=1/2+1=1.5$，$u'=1/2+3=3.5$，结果为 $7/3$。一次处理整行时，两个权重正比于 1 和 2，结果同样为 $(1+2\times3)/3=7/3$。
 
 ![图 5-13 在线 Softmax 如何合并两个块](ch05/figure-5-7-online-softmax.svg)
 
@@ -346,11 +348,13 @@
 
 为了检验这些机制在实际程序中的效果，配套实验比较了 PyTorch 中同一个注意力接口的两种执行实现。这里的**后端（backend）**，指框架在接口内部实际选用的计算程序：调用者给出相同的 Q、K、V，框架可以用不同程序完成注意力计算。
 
-**数学后端（`SDPBackend.MATH`）**按注意力公式组合矩阵乘、Softmax 等通用运算，保存完整的分数和概率中间矩阵。**FlashAttention 后端（`SDPBackend.FLASH_ATTENTION`）**采用前面介绍的分块与在线归约方法，避免保存完整的中间矩阵。实验强制选择其中一种实现，以确保比较的是指定的两条执行路径；这里的“数学”是 PyTorch 的实现名称，两种实现计算的都是注意力。
-
-实验在 RTX PRO 6000 Blackwell 上进行，输入输出为 BF16，一次处理一个序列、一个注意力头，头维度为 128。序列长为 8192 token，其中 token 是模型处理的一个离散输入位置；采用因果注意力，即每个位置只使用自身及此前的位置。这里的因果条件与前面无掩码的容量算例不同。预热后，使用 CUDA Graph 将一组 GPU 操作预先记录并重复执行，以减少逐次由主机提交的干扰。按每次注意力计算统计，保存完整中间矩阵的数学实现约需 2.6 ms，采用分块与在线归约的 FlashAttention 实现约需 80 μs，后者约快 33 倍。[^exp3]
-
-另外，在输入 Q、K、V 已驻留显存的条件下，测量一次普通调用新分配的显存峰值，包含输出与临时工作区：数学实现约为 848 MiB，FlashAttention 实现约为 22 MiB。这不是整个模型或进程的显存占用。两条执行路径的中间精度和任务组织也有差别，33 倍是这组条件下完整实现的对比结果，不能全部归因于少写回某一份矩阵。这个例子展示了分块、在线归约和融合如何配合：分块让当前数据放得进快速存储，在线归约使 Softmax 可以逐块计算，融合省去块内中间结果的写回。
+**数学后端（`SDPBackend.MATH`）**按注意力公式组合矩阵乘、Softmax 等通用运算，保存完整的分数和概率中间矩阵。**FlashAttention 后端（`SDPBackend.FLASH_ATTENTION`）**采用前面介绍的分块与在线归约方法，避免保存完整的中间矩阵。实验分别指定这两个后端，比较它们完成同一次注意力计算的时间与新增显存。
+
+实验在 RTX PRO 6000 Blackwell 上进行，输入输出为 BF16，一次处理一个序列、一个注意力头，头维度为 128。序列长为 8192 token，其中 token 是模型处理的一个离散输入位置；采用因果注意力，即每个位置只使用自身及此前的位置。预热后，使用 CUDA Graph 将一组 GPU 操作预先记录并重复执行，以减少逐次由主机提交的干扰。按每次注意力计算统计，保存完整中间矩阵的数学实现约需 2.6 ms，采用分块与在线归约的 FlashAttention 实现约需 80 μs，后者约快 33 倍。[^exp3]
+
+再比较一次普通调用新增的显存峰值。输入 Q、K、V 预先驻留显存，计量范围为输出与临时工作区：数学实现约为 848 MiB，FlashAttention 实现约为 22 MiB。
+
+这组完整实现的对照展示了分块、在线归约和融合如何配合：分块让当前数据放得进快速存储，在线归约使 Softmax 可以逐块计算，融合省去块内中间结果的写回。三者共同改变了执行时间与中间存储需求。
 
 ## 5.4 编译器：表达、变换与选择
 
@@ -427,7 +431,7 @@
 
 浮点舍入进一步限制了变换。原程序将 FP32 累加结果舍入为 BF16，再激活，融合后的程序也要在激活之前执行 `round_to_bf16`。中间值可以从寄存器直接传给激活，这次舍入仍然存在。数据保存位置与数值格式是两项独立选择。
 
-**例 5-7：量化尺度何时确定。** 一行 256 项分成两个 128 项块，两个块的首项分别为 1 和 10，其余为零。量化使用 E4M3FN 格式：一种含符号位、四位指数和三位尾数的八位浮点表示，最大有限值为 448。尺度由整行最大绝对值决定；随后做点积，权重仅第一项为 1。因此结果完全取决于第一项如何量化。
+**例 5-7：整行量化尺度如何约束分块计算顺序？** 一行 256 项分成两个 128 项块，两个块的首项分别为 1 和 10，其余为零。量化使用 E4M3FN 格式：一种含符号位、四位指数和三位尾数的八位浮点表示，最大有限值为 448。尺度由整行最大绝对值决定；随后做点积，权重仅第一项为 1。因此结果完全取决于第一项如何量化。
 
 ![图 5-17 整行尺度与第一次舍入](ch05/figure-5-quantization-scale.svg)
 
@@ -449,7 +453,7 @@
 
 将计算和存储一起分析，也能解释融合为什么会增加访存。前面的逐元素链通过融合少读了中间值；若中间结果使用更少的位数表示，又需要被后续计算反复读取，保留它反而可以减少读取。
 
-**例 5-8：保存量化结果，还是在矩阵乘中重新量化。** 取 $[4096,4096]$ 的 FP16 输入 A，占 32 MiB，量化为 FP8 后占 16 MiB；FP8 权重为 $[4096,1536]$，占 6 MiB，FP16 输出占 12 MiB。输出按 128×128 分块，共有 32 个行块、12 个列块。各输出块独立读取所需数据，量化尺度来自整行最大值。
+**例 5-8：保存量化结果与融合量化，哪种方式读写更少？** 取 $[4096,4096]$ 的 FP16 输入 A，占 32 MiB，量化为 FP8 后占 16 MiB；FP8 权重为 $[4096,1536]$，占 6 MiB，FP16 输出占 12 MiB。输出按 128×128 分块，共有 32 个行块、12 个列块。各输出块独立读取所需数据，量化尺度来自整行最大值。
 
 先看单独执行量化的做法。每处理一行，就用 8 KiB 局部空间保存该行输入，求出尺度后再量化。全部输入共读入 32 MiB，量化结果共写出 16 MiB。随后，12 个输出列块各读取一遍 FP8 输入，共 192 MiB。因此，与输入有关的读写量为 $32+16+192=240$ MiB。
 
@@ -473,7 +477,7 @@
 
 TVM 的自动调优根据硬件实测结果搜索分块、循环顺序等调度方案；让能够调用编译和测试工具的智能体（Agent）修改调度或内核代码，还可以尝试原有调度模板之外的实现。无论采用哪种方法，都需要生成代码、编译、检查数值结果并测量时间。源码中分开表达的算子，还可能被编译器融合为一个内核，因此应比较编译后实际执行的程序。[^exp4] 搜索方法还要解决另一个问题：用什么负载评价这些实现。
 
-**例 5-9：调用比例如何影响实现的选择。** 某个算子有 A、B 两种输入形状，原实现处理它们都需要 10 μs。新实现处理 A 需要 5 μs，处理 B 需要 20 μs：A 的耗时减半，B 的耗时翻倍。若两种形状各出现一次，总时间从 20 μs 增至 25 μs，新实现慢 25%；若 A 调用 100 次、B 调用一次，总时间从 1010 μs 降至 520 μs，新实现约快 1.9 倍。
+**例 5-9：输入形状的调用比例如何改变算子实现的选择？** 某个算子有 A、B 两种输入形状，原实现处理它们都需要 10 μs。新实现处理 A 需要 5 μs，处理 B 需要 20 μs：A 的耗时减半，B 的耗时翻倍。若两种形状各出现一次，总时间从 20 μs 增至 25 μs，新实现慢 25%；若 A 调用 100 次、B 调用一次，总时间从 1010 μs 降至 520 μs，新实现约快 1.9 倍。
 
 令输入形状为 A 的调用占比为 p。原实现的平均执行时间为 10 μs，新实现的平均执行时间为：
 
@@ -641,15 +645,17 @@
 
 共享资源还会改变分支自身的时间。设 A 的新实现大量占用带宽，使并行 B 延长到 90 μs，请求就变成 $10+\max(15,90)+10=110$ μs。B 增加的等待时间超过了 A 节省的计算时间，导致整个请求反而变慢。比较实现时，需要测量两条分支同时运行时各自的耗时，才能看出 A 的改动对 B 的影响。[^dag]
 
-第二章的 V4.1 会话说明，保存同一份数据所需的容量，与执行期间对它的累计访问量可以明显不同。8K 条件下，V4 Flash 全局 KV 为 27.455 MiB，V4.1 为 6.953 MiB；但计入局部窗口与索引的逐层逻辑读取为 12.556 与 11.375 MiB。共享状态不必为每层复制一份容量，各使用层仍需要访问它。若能在片上缓存中保留共享条目，实际 HBM 读取可以进一步减少；若分块使条目被重复装载，实际流量又可能增加。[^v41-case]
+第二章的 V4.1 会话说明，保存同一份数据所需的容量，与执行期间对它的累计访问量可以明显不同。8K 条件下，V4 Flash 全局 KV 为 27.455 MiB，V4.1 为 6.953 MiB；但计入局部窗口与索引的逐层逻辑读取为 12.556 与 11.375 MiB。共享状态不必为每层复制一份容量，各使用层仍需要访问它。[^v41-case]
 
 上下文增长后，仍需执行的索引扫描会产生更大的开销。128K 时，两代主历史与索引逻辑读量分别约为 65.085 与 33.422 MiB。V4.1 后续解码索引器只访问候选池，初始索引仍需遍历全局。因此，即使深层索引的访问范围固定，整个 decode 的开销仍会随上下文长度增长。
 
-分析性能时，还需要确认程序实际执行的步骤与估算中的假设一致。公开参考实现先对完整索引缓存执行点积，再屏蔽候选池之外的位置；论文生产路径则减少后续索引实际扫描的条目。两者可实现相同选择规则，却有不同的访存与运算。验证时先确认实现，再分别测 kernel 时间、HBM 流量、缓存命中与主机提交，才能判断共享和稀疏化的数学收益是否兑现。
-
-![图 5-30 全局驻留与逐层逻辑读取分开比较。上方仅计全局 KV 容量，下方读取包含全局条目、局部窗口和索引；两栏各自采用相同横轴尺度。数值为生产布局的逻辑载荷，不是实测 HBM 流量。](ch05/figure-5-v41-traffic.svg)
-
-*图 5-30：全局驻留与逐层逻辑读取分开比较。上方仅计全局 KV 容量，下方读取包含全局条目、局部窗口和索引；两栏各自采用相同横轴尺度。数值为生产布局的逻辑载荷，不是实测 HBM 流量。*
+需要注意的是，逻辑读取量与 HBM 流量对应不同的计数位置。共享条目若留在片上缓存中，后续层可直接复用；分块执行若反复装载同一条目，则会增加片外读取。第四章的缓存计数实验给出了区分这两种情况的方法。
+
+除了数据从哪里取得，程序是否真正省去了计算也会改变开销。候选池通过缩小后续索引的搜索范围来节省工作。公开参考实现先对完整索引缓存执行点积，再屏蔽候选池之外的位置；论文生产路径直接减少后续索引扫描的条目。前者已经完成了池外点积，后者在发起计算时就省去这部分工作。同一选择规则由此对应两种执行成本。
+
+![图 5-30 全局驻留与逐层逻辑读取分开比较。上方仅计全局 KV 容量，下方读取包含全局条目、局部窗口和索引；两栏各自采用相同横轴尺度。数值按生产布局计算。](ch05/figure-5-v41-traffic.svg)
+
+*图 5-30：全局驻留与逐层逻辑读取分开比较。上方仅计全局 KV 容量，下方读取包含全局条目、局部窗口和索引；两栏各自采用相同横轴尺度。数值按生产布局计算。*
 
 ### 5.6.3 综合案例：一次 Qwen3 请求的优化选择
 
@@ -692,15 +698,15 @@
 
 > **实验 5-1 · 延伸：矩阵分块与访存计数**
 >
-> 将例 5-3 的输出块改为 64×128，归约块仍为 32，求局部存储需求、A/W 各自的读取次数与总访问量。它要保持 64×64 方案多少比例的有效带宽，才能在访存时间上领先？再比较两种循环次序的执行记录，解释连续访问如何影响处理率。[^exp1]
+> 将例 5-3 的输出块改为 64×128，归约块仍为 32，求局部存储需求、A/W 各自的读取次数与总访问量。与 64×64 方案相比，64×128 方案的有效带宽至少要达到前者的多少，才能使访存时间更短？再比较两种循环次序的执行记录，解释连续访问如何影响处理率。[^exp1]
 
 > **实验 5-2 · 核心：融合的流量与资源占用**
 >
-> 对 G、U 各 24 MiB 的激活链，画出三步独立与完全融合的分配、使用和释放顺序，推导 156 MiB、60 MiB 的访问量，以及两种方案的峰值容量。若融合实现每次增加 5 μs 固定开销，接口带宽为 2 TB/s，输入行数至少达到多少才值得？再用配套结果解释访问节省和时间节省的关系。[^exp2]
+> 对 G、U 各 24 MiB 的激活链，分别画出三个算子独立执行和完全融合时，各张量的分配、使用与释放顺序，推导 156 MiB、60 MiB 的访问量，以及两种方案的峰值容量。若融合实现每次增加 5 μs 固定开销，接口带宽为 2 TB/s，输入行数至少达到多少才值得？再结合配套实验结果，解释访问量减少的比例与耗时缩短的比例之间的关系。[^exp2]
 
 > **实验 5-3 · 延伸：分块 Softmax 与注意力 IO**
 >
-> 给例 5-6 再加入分数 $\ln4$、值 5，更新 m、$\ell$、u，并与一次处理三个元素比较。再给 128 KiB 注意力缓冲增加一份 b×d 的 BF16 预取槽，取 b=64，求新的最大 Q 行数、扫描次数与访问量。解释预取为何会改变重读。[^exp3]
+> 给例 5-6 再加入分数 $\ln4$、值 5，更新 m、$\ell$、u，并与一次处理三个元素比较。再给 128 KiB 注意力缓冲增加一份 b×d 的 BF16 预取槽，取 b=64，求新的最大 Q 行数、扫描次数与访问量。解释预取缓冲为何会改变数据的重复读取次数。[^exp3]
 
 > **实验 5-4 · 延伸：融合位置与数值语义**
 >
@@ -712,19 +718,19 @@
 
 > **实验 5-6 · 延伸：按负载选择调优目标**
 >
-> 沿例 5-9，令 A 占比分别为 1/2、2/3、9/10，计算原实现和新实现的平均时间。若根据形状选择实现，每次多花 1 μs，对 A 使用新实现、对 B 使用原实现，何时优于始终使用原实现？再从配套搜索记录计算总搜索成本和实际产生的稳态节省。[^exp6]
+> 采用例 5-9 的两种输入形状，令形状 A 的调用占比分别为 1/2、2/3、9/10，计算原实现和新实现的平均时间。若根据形状选择实现，每次多花 1 μs，对 A 使用新实现、对 B 使用原实现，何时优于始终使用原实现？再从配套搜索记录计算总搜索成本和实际产生的稳态节省。[^exp6]
 
 > **实验 5-7 · 延伸：特化需要重复执行多少次才有收益**
 >
-> 重画例 5-12 的准备加执行时间线。将小形状的频数从 8 次改为 4 次，其他两种形状仍各一次，求三种方案总时间相等时的重复次数。再假设两个分桶对应的程序已编译并缓存，说明准备时间改变后，各方案分别在多少次重复执行时最快。[^specialization]
+> 画出例 5-12 中准备与执行的完整时间线。将每组中小形状的调用次数从 8 次改为 4 次，其他两种形状仍各调用一次。对三种方案两两比较，求各对方案总耗时相等时的重复次数。再假设两个分桶对应的程序已编译并缓存，说明准备时间改变后，各方案分别在多少次重复执行时最快。[^specialization]
 
 > **实验 5-8 · 核心：图重放与算子融合**
 >
-> 阅读图 5-20，分别计数四种方式的主机提交与设备 kernel。将例 5-11 的复制带宽改为 1 TB/s，求图输入数据量的临界值；再让上游直接写入图缓冲，重新比较 2 MiB、16 MiB 两种输入的完成时间。[^exp8]
+> 阅读图 5-20，分别统计四种方式下的主机提交次数与设备 kernel 数量。将例 5-11 的复制带宽改为 1 TB/s，求输入数据增大到多少时，复制开销恰好抵消图重放节省的时间；再让上游直接写入图缓冲，重新比较 2 MiB、16 MiB 两种输入的完成时间。[^exp8]
 
 > **实验 5-9 · 核心：内核加速对不同请求的影响**
 >
-> 用配套 11 对请求计算配对时间差的中位数，并解释它与两个请求中位数之差的区别。沿综合案例，保持 prefill 不变，把 decode 步数从 31 增至 127，假设每步节省的时间不变，估算激活计算总共能节省多少时间。再将图 5-29 中 A 的执行时间逐步缩短，求完整请求时间不再随之缩短时 A 的执行时间。[^exp9]
+> 根据配套的 11 对请求记录，先计算每对请求的耗时差，再取这些差值的中位数。解释它与分别取两组请求耗时的中位数后再相减有何区别。采用本章综合案例的条件，保持 prefill 不变，把 decode 步数从 31 增至 127，假设每步节省的时间不变，估算激活计算总共能节省多少时间。再将图 5-29 中 A 的执行时间逐步缩短，求完整请求时间不再随之缩短时 A 的执行时间。[^exp9]
 
 ## 历史与进一步阅读
 
--- before/manuscripts/06-超节点.md
+++ manuscripts/06-超节点.md
@@ -30,7 +30,7 @@
 
 这里 $C_i$ 是该卡可供任务使用的内存。权重随模型确定，KV 随会话上下文增长，激活和工作区随执行方式变化。它们在同一时刻占用同一份物理内存。
 
-**贯穿算例的参数。** KV 是注意力层为上下文位置保存的键（K）和值（V），后续生成会读取这些状态。模型采用每元素占 2 字节的 BF16 浮点格式保存权重与 KV，隐藏维为 4096，共 36 层；每层有 32 个查询头、8 个 KV 头，头维为 128，前馈网络（FFN）的中间维为 12288。教学设备的每卡可用内存为 24 GB，高带宽内存（HBM）的有效带宽为 1 TB/s，矩阵运算性能为 100 TFLOP/s。互联每轮的启动开销为 2 μs，有效单向带宽为 50 GB/s。GB、TB 使用十进制，MiB、GiB 使用二进制。[^continuous]
+**Qwen3-8B 的模型规模与八卡部署资源** KV 是注意力层为上下文位置保存的键（K）和值（V），后续生成会读取这些状态。模型采用每元素占 2 字节的 BF16 浮点格式保存权重与 KV，隐藏维为 4096，共 36 层；每层有 32 个查询头、8 个 KV 头，头维为 128，前馈网络（FFN）的中间维为 12288。教学设备的每卡可用内存为 24 GB，高带宽内存（HBM）的有效带宽为 1 TB/s，矩阵运算性能为 100 TFLOP/s。互联每轮的启动开销为 2 μs，有效单向带宽为 50 GB/s。GB、TB 使用十进制，MiB、GiB 使用二进制。[^continuous]
 
 先求容量。全模型 BF16 权重约为 16.4 GB。一个上下文位置需要保存每层的 K 和 V，因此全模型每位置的 KV 为
 
@@ -140,7 +140,7 @@
 
 **例题：四卡切分减少多少权重读取，又增加多少通信？** 取 Qwen3-8B 的 $h=4096,f=12288$，将中间维均分到四卡。
 
-**解答。** 每卡上投影为 $4096\times3072$，下投影为 $3072\times4096$。三个矩阵共占 72 MiB，是原来 288 MiB 的四分之一。单 token 的理想读取时间从约 302 μs 降到 75.5 μs，每卡少读 216 MiB，省约 226 μs。代价是汇合四个 8 KiB 输出部分和。第 6.4 节将用具体算法求出这次汇合的时间。
+解答： 每卡上投影为 $4096\times3072$，下投影为 $3072\times4096$。三个矩阵共占 72 MiB，是原来 288 MiB 的四分之一。单 token 的理想读取时间从约 302 μs 降到 75.5 μs，每卡少读 216 MiB，省约 226 μs。代价是汇合四个 8 KiB 输出部分和。第 6.4 节将用具体算法求出这次汇合的时间。
 
 Attention 也可沿头切分。Q、K、V 投影产生各卡负责的头，各卡独立计算这些头的注意力；输出投影把头结果映射回隐藏维，再将各卡的部分结果求和，得到完整输出。因此，Qwen3-8B 每层需要分别对 attention 输出和 FFN 输出进行归约，36 层共 72 次。Embedding、词表输出与采样归入贯穿算例的模型输入与输出处理时间。[^dense]
 
@@ -176,7 +176,7 @@
 
 随着 $p$ 增大，右侧不断缩小。即使通信每次只增加同样一小段等待，也会逐渐抵消本地执行时间的缩短。
 
-**进一步的切分。** ReduceScatter 可以把一个 64 MiB 输出分到四卡，每卡只留 16 MiB；若下一算子保持分片，总输出占用便从四份完整张量的 256 MiB 降到 64 MiB。相邻算子需要完整输入时，AllGather 再把四片收集起来。只要后续算子继续使用分片，就能维持较低的内存占用。JAX 是支持数组计算、自动求导和编译执行的数值计算框架。配套 JAX 实验给出这两条 FFN 路径的具体执行。[^tp-experiment]
+**保留输出分片如何减少重复存储** ReduceScatter 可以把一个 64 MiB 输出分到四卡，每卡只留 16 MiB；若下一算子保持分片，总输出占用便从四份完整张量的 256 MiB 降到 64 MiB。相邻算子需要完整输入时，AllGather 再把四片收集起来。只要后续算子继续使用分片，就能维持较低的内存占用。JAX 是支持数组计算、自动求导和编译执行的数值计算框架。配套 JAX 实验给出这两条 FFN 路径的具体执行。[^tp-experiment]
 
 上面的切分只沿一个矩阵维度分配设备。设备更多时，还可以同时切分两个维度，将卡排成行、列两个方向的通信组。一次 $[8192,4096]\times[4096,12288]$ 投影，将输出分片保存在 16 张卡上，每卡原始输入和权重分别为 4 MiB、6 MiB。排成 4×4 网格，横向收集其他三片输入，发送 12 MiB；纵向收集其他三片权重，发送 18 MiB。改成 2×8 后两方向为 28、6 MiB，改成 8×2 后为 4、42 MiB。独立等速链路下，4×4 所需传输时间较短，因为两个方向中较大的传输量最小。MeshSlice 沿这一布局继续分块，使收集与矩阵乘流水执行；Arctic 则在不同执行阶段切换分工，其状态迁移在第 9 章展开。[^variants]
 
@@ -199,7 +199,7 @@
 
 分子是所有阶段实际工作的时间总和，分母是设备数乘以处理全部微批次所需的时间。四阶段、四微批次的利用率为 $4/7$，约 57%；增加到 16 微批次时为 $16/19$，约 84%。独立微批次越多，流水填充与排空造成的空闲时间占比就越小。
 
-各阶段执行时间不均衡也会造成设备空闲。若四阶段分别需 1、1、2、1 ms，第一个微批次在 5 ms 完成，之后最快每 2 ms 完成一个。2 ms 阶段前的输入到达速度高于它的处理速度，队列逐渐积累；缓冲用满后，上游必须暂停。分配模型层时，应使各阶段的执行时间接近；层数相同并不保证执行时间相同。[^pipeline]
+各阶段执行时间不均衡也会造成设备空闲。若四阶段分别需 1、1、2、1 ms，第一个微批次在 5 ms 完成，之后最快每 2 ms 完成一个。2 ms 阶段前的输入到达速度高于它的处理速度，队列逐渐积累；缓冲用满后，上游必须暂停。按各层的执行时间分配模型，可以使各阶段更接近同时完成，减少阶段间积压。[^pipeline]
 
 自回归会话的下一 token 在当前输出后才就绪。四个独立会话可以提供四个微批次，同一会话未来的四个 token 则是一条先后依赖链。流水利用率因此直接取决于同时有多少微批次可以开始执行。
 
@@ -207,13 +207,13 @@
 
 > **实验 6-2 · 核心：切分、通信与流水**
 >
-> （a）写出两卡 SwiGLU 的六个权重分片和两个输出部分和的形状，用式（6-3）说明输出为何求和。
->
-> （b）对 TP=1、2、4、8，计算贯穿算例的本地内存访问时间，暂把通信置零；求每次加倍省下的时间。
->
-> （c）读完 6.4 后将归约时间填回式（6-5），重新计算加速比，解释差距从哪里产生。
->
-> （d）画出四阶段处理八个微批次的时序，分别采用等时阶段和 1、1、2、1 ms 阶段；比较完成时间与积压位置。
+> （a）写出两卡 SwiGLU 的六个权重分片和两个输出部分和的形状，用式（6-3）说明为什么需要将两张卡的输出部分和相加。
+>
+> （b）对 TP=1、2、4、8，计算贯穿算例的本地内存访问时间，暂不计通信开销；求设备数每次翻倍所节省的时间。
+>
+> （c）采用第 6.4 节的归约时间，将其代入式（6-5），重新计算加速比，解释差距从哪里产生。
+>
+> （d）画出四阶段处理八个微批次的时序，分别考虑四个阶段耗时相同，以及耗时依次为 1、1、2、1 ms 的情况；比较两种情况下的完成时间，并指出微批次会在哪个阶段之前积压。
 >
 > （e）进阶：用同一个 FFN 比较完整输出与分片输出的相邻算子之间的数据传递，或沿配套二维网格继续推导通信分块。
 
@@ -264,7 +264,7 @@
 
 省掉派发需要付出存储与计算代价。按这份布局，每卡权重约为 64.8 GB，八卡共 518.6 GB，比去除重复存储后的权重的 470.2 GB 多约 48.4 GB。8192 位置的每卡 KV 为 752 MiB，八卡共 5.875 GiB，是同一请求逻辑 KV 的四倍。每组重复执行 attention，换来专家输入在本组直接可用。[^ownership]
 
-**例题：这份布局每层发送多少数据？** 归约采用 FP32，即每元素 4 字节的浮点格式，单 token 的完整隐藏张量为 $4096\times4=16$ KiB。普通 ring 在两卡中归约，每卡发送一个完整张量大小；四卡归约则发送 1.5 倍张量大小。每层有 attention TP、专家 TP、专家 EP 三次汇合，所以每卡发送
+**例题：注意力复制与专家切分的布局需要多少归约通信？** 归约采用 FP32，即每元素 4 字节的浮点格式，单 token 的完整隐藏张量为 $4096\times4=16$ KiB。普通 ring 在两卡中归约，每卡发送一个完整张量大小；四卡归约则发送 1.5 倍张量大小。每层有 attention TP、专家 TP、专家 EP 三次汇合，所以每卡发送
 
 $$
 16+16+24=56\ \mathrm{KiB}.
@@ -379,7 +379,7 @@
 
 **例题：将通信开销计入 Qwen3-8B 的单步时间。** 使用贯穿算例的 $M=8$ KiB、$\alpha=2$ μs、$B=50$ GB/s，求 TP=2、4、8 的单次归约以及式（6-5）的时间。
 
-**解答。** TP=8 共十四轮，启动开销合计为 28 μs，每卡发送 14 KiB，传输约 0.29 μs，单次约 28.3 μs。其余规模同样代入式（6-9）：
+解答： TP=8 共十四轮，启动开销合计为 28 μs，每卡发送 14 KiB，传输约 0.29 μs，单次约 28.3 μs。其余规模同样代入式（6-9）：
 
 | TP 卡数 | 本地内存访问 | 72 次归约 | 输入输出等串行处理 | 单步时间 |
 |---|---:|---:|---:|---:|
@@ -410,13 +410,13 @@
 
 > **实验 6-4 · 延伸：逐轮归约与算法交点**
 >
-> （a）按图 6-14 的发送规则，跟踪块 1 的三轮归约，写出每轮持有者及已累计贡献。
->
-> （b）求四卡和八卡 ring 的每卡发送量，再推导对应树形时间。
->
-> （c）求八卡两种算法的交点；将 $\alpha$ 减半后重算，解释交点移动方向。
->
-> （d）将树形 8 KiB 时间代入贯穿算例，求八卡单步时间，比较算法选择与带宽翻倍的收益。
+> （a）按图 6-14 的发送规则，跟踪块 1 的三轮归约，写出每轮由哪张卡持有该块，以及其中已经累加了哪些卡的贡献。
+>
+> （b）求四卡和八卡 ring 的每卡发送量，再推导相同数据量下树形归约的耗时。
+>
+> （c）求八卡部署下两种归约算法耗时相等时的消息大小；将 $\alpha$ 减半后重算，解释交点移动方向。
+>
+> （d）将树形算法归约 8 KiB 数据所需的时间代入本章 Qwen3-8B 算例，求八卡单步时间，比较算法选择与带宽翻倍的收益。
 
 ### 6.4.3 通信库与计算的资源竞争
 
@@ -459,7 +459,7 @@
 >
 > （a）复算图 6-18 两种配置的完成时间和相对差异，指出每种配置中是计算还是通信决定了整体完成时间。
 >
-> （b）求准备成本为 12 ms 时的节省时间首次超过优化开销所需的最少调用次数；改成 6 ms 后重算。
+> （b）设优化前的准备耗时为 12 ms，求累计节省的执行时间首次超过准备耗时所需的最少调用次数；改成 6 ms 后重算。
 >
 > （c）用两个二元素向量检验归约与 RMSNorm 的执行顺序。
 >
@@ -471,9 +471,9 @@
 
 前面的算法和通信库决定怎样发消息，却还没有说明消息经过哪些物理连接。集合算法规定逻辑上的发送者和接收者，物理拓扑决定消息实际经过哪些设备与链路。两张卡可以直连，也可以经交换机、其他设备或主机内存中转。多个逻辑消息共用一个接口时，要排队使用同一份带宽。
 
-**例题：贯穿算例的八卡分处两台服务器。** 每台放四卡，服务器内仍采用每轮 2 μs、50 GB/s；服务器间链路采用每轮 4 μs、25 GB/s。八卡 ring 按 0→1→…→7→0 排列，3→4 与 7→0 跨过服务器边界，每轮同步等待最慢链路上的传输完成。
-
-**解答。** 每轮服务器间链路发送 $8/8=1$ KiB，十四轮形成一次归约。归约时间为
+**例题：跨服务器通信为何使八卡推理慢于单机四卡？** 每台放四卡，服务器内仍采用每轮 2 μs、50 GB/s；服务器间链路采用每轮 4 μs、25 GB/s。八卡 ring 按 0→1→…→7→0 排列，3→4 与 7→0 跨过服务器边界，每轮同步等待最慢链路上的传输完成。
+
+解答： 每轮服务器间链路发送 $8/8=1$ KiB，十四轮形成一次归约。归约时间为
 
 $$
 14\times4\ \mu\mathrm{s}+\frac{14\ \mathrm{KiB}}{25\ \mathrm{GB/s}}
@@ -522,7 +522,7 @@
 >
 > （a）复算两台四卡服务器上的八卡时间，再求服务器间通信的每轮固定开销达到多少时，八卡与四卡的单步时间恰好相同。
 >
-> （b）对 32 端口叶交换机，比较 16／16 与 24／8 分配，计算所有设备同时向上发送时的平均可用带宽。
+> （b）对 32 端口叶交换机，比较下行／上行端口分别为 16／16 与 24／8 的两种分配，计算所有设备同时向上发送时的平均可用带宽。
 >
 > （c）根据图 6-21，累加第三轮经过同一有向链路的消息大小，说明消息跳数与链路传输量的区别。
 >
@@ -546,7 +546,7 @@
 
 > **实验 6-7 · 延伸：功率与端口共同决定规模**
 >
-> （a）复算 64、72、96 卡功率，求预算增加到 150 kW 时的线性上限。
+> （a）分别复算安装 64、72、96 张卡时的总功率，求预算增加到 150 kW 时的线性上限。
 >
 > （b）假定超过 64 卡需额外增加 6 kW 交换设备，求 120 kW 下的新上限。
 >
@@ -560,7 +560,7 @@
 
 NVLink 是 NVIDIA 的设备互联，NVSwitch 是组织 NVLink 交换网络的交换芯片。DGX-2 使用 16 张 V100 和 NVSwitch。交换结构把“某对 GPU 是否具有直连”转化为“交换层能同时服务多少流量”，扩大了可用协作范围。DGX A100 又以八卡服务器为基本单元，配备 NVLink／NVSwitch。这些系统采用不同的 GPU 数量与互联结构，以适应芯片接口、机箱空间和应用需求。[^nvidia]
 
-GB200 NVL72 将 72 张 GPU 和 36 个 Grace CPU 放进机柜级 NVLink 系统。机柜内的更多 GPU 因而能够通过 NVLink 频繁交换数据。回到 6.5.1 的八卡任务：在两台服务器之间，每轮启动耗时增加，使单步从约 4.28 ms 增到 6.32 ms；若所需八卡能放在同一个满足前述低延迟条件的互联域内，便可省去这部分跨服务器通信开销。更大的域也可以容纳多个两卡或四卡实例，各自完成自己的请求。[^nvl]
+GB200 NVL72 将 72 张 GPU 和 36 个 Grace CPU 放进机柜级 NVLink 系统。机柜内的更多 GPU 因而能够通过 NVLink 频繁交换数据。以第 6.5.1 节的八卡任务为例：在两台服务器之间，每轮启动耗时增加，使单步从约 4.28 ms 增到 6.32 ms；若所需八卡能放在同一个满足前述低延迟条件的互联域内，便可省去这部分跨服务器通信开销。更大的域也可以容纳多个两卡或四卡实例，各自完成自己的请求。[^nvl]
 
 机柜级互联使更多 GPU 能够参与同一个实例。对于单卡即可容纳的小模型，可以根据延迟和吞吐要求选择实例数；对于权重已超过八卡总容量的大模型，则可以使用更多 GPU 保存权重，并通过机柜内互联完成各层通信。
 
@@ -675,7 +675,7 @@
 >
 > （b）求 40 GB/s、256-byte 事务在 2 μs 和 4 μs 往返下所需的最少在途事务数。
 >
-> （c）比较 128 与 4096 个在途事务时的单次读取时间，求严格周期读取的最大频率。
+> （c）比较 128 与 4096 个在途事务时的单次读取时间，求每次读取都完成后才发起下一次读取时，周期性读取所能达到的最大频率。
 >
 > （d）采用本节远端与本地带宽，求重复读多少次时，先取回本地的总访问时间更少；同时说明所需本地容量。
 
@@ -683,7 +683,7 @@
 
 ### 6.7.1 更大的协作组与更多推理实例
 
-至此，已经知道数据能否存下、每步如何执行，以及连接和内存位置怎样改变执行时间。现在回到章首的八张卡，用这些结果安排多个会话。四个会话都已完成 prefill，各自保存了 8192 个上下文位置的 KV。每个会话还有一个已经生成、尚未写入 KV 的 token，用作下一步输入。现在，四个会话同时请求再生成八个 token。一个实例一次服务一个会话，完成这八步后再接下一个会话；请求按实例轮转分配。采用 6.4 的低延迟八卡连接，全部设备保留到四个会话都完成。
+至此，已经知道数据能否存下、每步如何执行，以及连接和内存位置怎样改变执行时间。下面利用这些结果，为章首给定的八张卡安排多个会话。四个会话都已完成 prefill，各自保存了 8192 个上下文位置的 KV。每个会话还有一个已经生成、尚未写入 KV 的 token，用作下一步输入。现在，四个会话同时请求再生成八个 token。一个实例一次服务一个会话，完成这八步后再接下一个会话；请求按实例轮转分配。采用 6.4 的低延迟八卡连接，全部设备保留到四个会话都完成。
 
 每步时间来自式（6-5）。续写时，式（6-4）的上下文长度依次为 8192 到 8199，KV 访问量逐步增加。将八步相加，就得到会话的服务时间。等待中的会话只保存各自的 KV，同一实例中的会话共享模型权重；四种部署方式的每卡内存需求均不超过 24 GB。
 
@@ -716,15 +716,11 @@
 
 图中色块的横向长度表示单个会话的执行时间，纵向轨道数表示多少会话可以同时推进。完成期限决定了这两者怎样取舍。只有一个会话、要求 40 ms 完成，八卡实例满足目标；四个会话、要求 90 ms 全部完成，两卡和四卡部署方式都满足，两卡更早完成。较短的单会话完成期限需要更大协作组，足够多的独立会话则可以利用更多实例。
 
-连接改变后再沿原式计算。若八卡跨过 6.5.1 的服务器边界，每步约 6.32 ms，八步约 50.5 ms，原来的 40 ms 目标无法满足。此时解决问题要回到那项新增开销：改善服务器间连接、改变集合通信算法，或将八卡放进更紧密的互联域。
-
-沿用第 4 章的固定权重实验，需要分别分析使用多个设备的原因。为容纳权重而切分模型的需求可能减少；为保存更多 KV 而扩容的需求仍然存在；为缩短单请求计算而采用 TP，需要确认节省的计算时间超过新增的同步时间；为提高总吞吐而复制实例，则仍由请求量决定。权重容量、状态容量、单请求时延和总吞吐分别给出四种扩容理由，不能只根据模型参数量推算超节点大小。
-
-先保持并行方案不变，观察权重读取加快后通信在单步耗时中的占比；再比较减少 TP 卡数、保留状态分片和增加独立实例等方案。第 6.5.1 节的通信直径估算说明，即使权重靠近计算单元，结果依赖仍必须跨越物理路径。更大协作组是否合适，要重新用每 token 的时间预算判断。
+连接改变后再沿原式计算。若八卡跨过 6.5.1 的服务器边界，每步约 6.32 ms，八步约 50.5 ms，原来的 40 ms 目标无法满足。要使这一部署满足时限，需要降低新增的通信开销：改善服务器间连接、改变集合通信算法，或将八卡放进更紧密的互联域。
+
+第 4 章的固定权重架构会改变上述取舍。权重由独立 ROM 提供后，原来分摊给多卡的 HBM 权重读取减少，TP 的同步时间在单步耗时中占比上升。减少 TP 卡数能够省去一部分同步，腾出的设备可组成更多独立实例；每个实例则要容纳更大份额的请求状态。前面的逐卡容量表与会话时间表，可以继续用来比较这两项变化。
 
 计算 V4.1 所需的设备数时，除了 890 B/token 的逻辑状态容量，还要考虑状态在设备间的分布。这一数值对跨层共享状态只计一份；如果模型各层被放在不同设备上，后续层仍须访问负责生成这些状态的模型层所产生的结果。可以把相关层放近、在目标设备复制状态，或建立远程访问路径。第一种限制放置灵活性，第二种增加存储容量和更新开销，第三种增加通信依赖，因此需要按各设备实际保存和访问的数据分别计算资源需求。[^v41-case]
-
-同时，两代 Flash 的权重并不相同。V4.1 主干约 552B，还加入 Engram 等组件，需要重新计算权重存储量和部署所需卡数。对同一会话，先分别列出权重、全局 KV、局部 SWA 与临时缓冲的位置，再比较状态减少后能否部署更多实例或处理更多并发请求；参数规模变大带来的需求要独立计入。
 
 ### 6.7.2 功率、成本与故障范围怎样改变选择
 
@@ -762,7 +758,7 @@
 
 模型的内存需求会限制可用的部署方式。配套 Qwen3-32B 案例中，两卡实例的每卡内存需求超过 24 GB，因此需要增加每个实例的卡数才能容纳权重和状态。先找出内存容量足够的部署方案，再比较这些方案的执行时间和成本。[^cost]
 
-> **实验 6-10 · 核心：由执行模型完成部署选择**
+> **实验 6-10 · 核心：按会话完成期限与成本选择并行配置**
 >
 > （a）从式（6-4）、（6-5）逐步求四种 TP 配置的八步续写时间，再计算四个会话各自的完成时刻。
 >
--- before/manuscripts/07-数据中心网络.md
+++ manuscripts/07-数据中心网络.md
@@ -48,7 +48,7 @@
 
 **例题 7.1：增加设备何时不再缩短完成时间？** 先采用两服务器算例中的环形归约：每台服务器向另一台发送 336 MiB。共享出口为 40 GB/s，原设备规模下的计算时间为 20 ms。增加设备时，计算均匀分摊，跨服务器传输量保持不变。求完全串行和完全重叠两种安排的完成时间。
 
-**解答。** 一个方向的传输下界为
+解答： 一个方向的传输下界为
 
 $$
 T_{\mathcal C}=\frac{336\times2^{20}}{40\times10^9}
@@ -67,7 +67,7 @@
 
 > **实验 7-2 · 延伸：超节点之间的扩展效率**
 >
-> 沿用例题 7.1，分别将每方向流量减半、出口增加一倍，求两条完成时间曲线及计算与通信相等的位置。再令流量随设备倍数线性增长，求完全重叠安排下使完成时间最短的设备倍数。
+> 沿用例题 7.1，分别考虑每个方向的流量减半，以及出口带宽翻倍这两种改动，求两条完成时间曲线及计算与通信相等的位置。再令流量随设备倍数线性增长，求完全重叠安排下使完成时间最短的设备倍数。
 
 ### 7.1.3 统一互联
 
@@ -202,7 +202,7 @@
 
 ### 7.2.4 多网卡
 
-专家分派说明了多入口并行的收益和共享入口的限制。同样的分析也适用于发送端：增加网卡，只有在它提供了额外可用带宽时才会缩短传输。回到前述算例中的两张 25 GB/s NIC。如果只使用一张，连续环十四轮共需约 14.1 ms；将每次传送拆成两个不重叠区间交给两张 NIC，吞吐升至共享出口允许的 40 GB/s，时间降至约 8.8 ms。第三张 NIC 加入同一个 40 GB/s 接口，无法再缩短这段传输时间。共享接口带宽耗尽后，增加网卡便不再提高吞吐率。
+专家分派说明了多入口并行的收益和共享入口的限制。同样的分析也适用于发送端：增加网卡，只有在它提供了额外可用带宽时才会缩短传输。以前述两张 25 GB/s NIC 的配置为例。如果只使用一张，连续环十四轮共需约 14.1 ms；将每次传送拆成两个不重叠区间交给两张 NIC，吞吐升至共享出口允许的 40 GB/s，时间降至约 8.8 ms。第三张 NIC 加入同一个 40 GB/s 接口，无法再缩短这段传输时间。共享接口带宽耗尽后，增加网卡便不再提高吞吐率。
 
 另一种情况是与某张 GPU 直接连接的 NIC 忙碌，相邻 GPU 的 NIC 空闲。借用相邻出口要先把数据送到相邻 GPU，因此又引入一段本地传输。FuseLink 利用这种路径，通过机内 GPU 互联和网络缓冲重映射使用空闲 NIC。[^fuselink]
 
@@ -224,7 +224,7 @@
 
 > **实验 7-3 · 核心：并行放置与分层集合通信**
 >
-> 完整复算例题 7.2。将本地带宽从 200 GB/s 降至 50 GB/s，求连续环和分层归约的时间，解释为什么更快的算法发生了变化。再将每服务器 NIC 数从一张增为两张、三张，保持共享出口 40 GB/s 不变，画出增加网卡不再缩短时间的位置。
+> 完整复算例题 7.2。将本地带宽从 200 GB/s 降至 50 GB/s，求连续环和分层归约的时间，解释本地带宽下降后，为什么应选择另一种归约算法。再将每服务器 NIC 数从一张增为两张、三张，保持共享出口 40 GB/s 不变，画出增加网卡不再缩短时间的位置。
 
 ## 7.3 远程访问的路径与并发
 
@@ -242,7 +242,7 @@
 
 > **实验 7-4 · 延伸：远程调用的时间分解**
 >
-> 从配套记录选取同一载荷的成对调用，画出客户端编码、发送、等待、解码与服务端执行的位置。解释为什么服务端执行包含在客户端等待内。计算省去 base64 的字节收益，以及编码开销占比变化如何改变总调用的收益。
+> 从配套记录选取同一载荷的成对调用，画出客户端编码、发送、等待、解码与服务端执行的位置。解释为什么服务端的执行时间已经包含在客户端的等待时间中。计算省去 base64 的字节收益，以及编码开销占比变化如何改变总调用的收益。
 
 ### 7.3.2 由产生数据的设备直接发起通信
 
@@ -309,7 +309,7 @@
 
 ### 7.3.4 并发与吞吐模型
 
-前一小节用复用减少远端读取次数。对于仍然需要通过网络的请求，接下来要看它们能否连续利用链路。现在回到算例中服务器的 40 GB/s 出口。**请求槽位**是保存一项未完成请求的地址、长度和状态等信息的记录空间。提交时分配，完成状态被处理后释放；释放前不能将同一记录用于新请求。假设每次远程访问传输 256 B，从发起请求到释放请求槽位需要 2 μs。链路在这 2 μs 内可以传送 80,000 B，相当于 312.5 次访问的数据量。为了让等待期间始终有数据可传，至少要维持 313 个在途事务，即已发出但尚未处理完的请求。
+前一小节用复用减少远端读取次数。对于仍然需要通过网络的请求，接下来要看它们能否连续利用链路。下面分析本章算例中服务器的 40 GB/s 出口需要多少个并发请求。**请求槽位**是保存一项未完成请求的地址、长度和状态等信息的记录空间。提交时分配，完成状态被处理后释放；释放前不能将同一记录用于新请求。假设每次远程访问传输 256 B，从发起请求到释放请求槽位需要 2 μs。链路在这 2 μs 内可以传送 80,000 B，相当于 312.5 次访问的数据量。为了让等待期间始终有数据可传，至少要维持 313 个在途事务，即已发出但尚未处理完的请求。
 
 ![请求槽位的生命周期](ch07/figure-7-slot-lifetime.svg)
 
@@ -325,7 +325,7 @@
 
 **例题 7.3：128 个槽位为何达不到出口带宽？** 仍取 256 B、2 μs 和 40 GB/s。求 128 个活跃槽位的吞吐；再假设请求处理单元每 100 ns 最多启动一项事务，求新的吞吐上界。
 
-**解答。** 128 个槽位每 2 μs 周转一次，提供的载荷为 $128\times256$ B，因此最多约为 16.4 GB/s。若只让一个事务在途，则为 0.128 GB/s。分配了多少槽位与实际同时使用多少槽位，是两个不同的数量。
+解答： 128 个槽位每 2 μs 周转一次，提供的载荷为 $128\times256$ B，因此最多约为 16.4 GB/s。若只让一个事务在途，则为 0.128 GB/s。分配了多少槽位与实际同时使用多少槽位，是两个不同的数量。
 
 ![请求槽位不足造成的链路空闲](ch07/figure-7-9-window.svg)
 
@@ -344,7 +344,7 @@
 
 其中，$T$ 是一项请求占用槽位的时间，$\delta$ 是连续两项请求之间的最短启动间隔。前者决定槽位多久可以复用，后者决定每秒最多能发起多少请求。[^window]
 
-**讨论：怎样才能达到 40 GB/s？** 保持 256 B 事务，启动间隔需缩至 $256/(40\times10^9)=6.4$ ns，并至少维持 313 项在途工作。另一条路是合并相邻访问：每次传输改为 4 KiB 后，100 ns 的启动间隔对应 40.96 GB/s；在等待时间为 2 μs 时，只需 20 个槽位就能支持 40 GB/s。前者提高事务率，后者让每次请求传输更多数据，降低单位字节的提交开销。
+**讨论：远程读取需要多少并发才能达到 40 GB/s？** 保持 256 B 事务，启动间隔需缩至 $256/(40\times10^9)=6.4$ ns，并至少维持 313 项在途工作。另一条路是合并相邻访问：每次传输改为 4 KiB 后，100 ns 的启动间隔对应 40.96 GB/s；在等待时间为 2 μs 时，只需 20 个槽位就能支持 40 GB/s。前者提高事务率，后者让每次请求传输更多数据，降低单位字节的提交开销。
 
 笔者在 KV-Direct 中研究 PCIe 小访问时，关心的正是怎样让多个独立请求同时在途，使 NIC 在等待一个结果时继续处理其他请求。将这些处理交给 NIC，可以在网卡内部以流水方式完成地址计算、访存和结果处理。PCIe 性能测试工具 rPCIeBench 后来对共享 PCIe 路径的研究也显示，在途工作量与入口竞争会影响带宽分配。[^pcie] 可以用上式判断瓶颈：是链路带宽已用尽，是请求槽位尚未释放，还是请求发起速率太低。
 
@@ -352,7 +352,7 @@
 
 > **实验 7-5 · 延伸：远程读取的在途并发**
 >
-> 将例题 7.3 的等待从 2 μs 增为 4 μs，分别求 256 B 与 4 KiB 事务所需的窗口。固定 100 ns 启动间隔，求达到 40 GB/s 所需的最小事务大小。最后将独立地址改成指针链，解释其时间如何随链长增长。
+> 将例题 7.3 的等待从 2 μs 增为 4 μs，分别求每次传输 256 B 和 4 KiB 时，维持目标带宽所需的在途事务数。固定 100 ns 启动间隔，求达到 40 GB/s 所需的最小事务大小。最后将独立地址改成指针链，解释其时间如何随链长增长。
 
 ## 7.4 数据交接、顺序与状态管理
 
@@ -411,15 +411,15 @@
 
 > **实验 7-6 · 延伸：连接与传输状态的规模**
 >
-> 保持 64 个端点，只让一个远端目标活跃，求独占与共享的总状态。再恢复 128 个目标，将可用存储空间提高到 2 MiB，求最多容纳多少个完整的隔离组。比较增加端点数和增加目标数对固定状态与传输状态的影响。
+> 保持 64 个端点，只让一个远端目标活跃，分别计算独占与共享传输状态时所需的总存储空间。再将活跃目标数量改为 128 个，将可用存储空间提高到 2 MiB，求最多容纳多少个完整的隔离组。比较增加端点数和增加目标数对固定状态与传输状态的影响。
 
 ### 7.4.3 操作依赖与故障隔离
 
-上一小节通过独立传输组减少不同业务之间的干扰。同一组操作内部也可能出现不必要的等待，这取决于系统强制哪些操作依次完成。回到卡 0 发布分片的过程。操作 A 写数据 D，操作 B 发布“D 已就绪”的通知，操作 C 传送另一块无关数据。要正确使用 D，必须保持 A→B 的顺序，C 可以使用另一条独立传输通路。
+上一小节通过独立传输组减少不同业务之间的干扰。同一组操作内部也可能出现不必要的等待，这取决于系统强制哪些操作依次完成。以卡 0 发布分片的过程为例。操作 A 写数据 D，操作 B 发布“D 已就绪”的通知，操作 C 传送另一块无关数据。要正确使用 D，必须保持 A→B 的顺序，C 可以使用另一条独立传输通路。
 
 **例题 7.4：减少顺序约束能让哪些操作更早完成？** A 写入需 20 μs，随后恢复需 80 μs，恢复结束后 D 可见；B 需 2 μs；C 需 10 μs。比较两种安排：A、B、C 依次完成，以及仅要求 A 完成后才能执行 B。
 
-**解答。** 发布链的时间是 $20+80+2=102$ μs。串行安排让 C 等到 102 μs 才开始，全组在 112 μs 完成。保留必要依赖时，C 从起点开始，10 μs 完成；发布链仍到 102 μs 才结束。全组提前 10 μs，C 却提前 102 μs。
+解答： 发布链的时间是 $20+80+2=102$ μs。串行安排让 C 等到 102 μs 才开始，全组在 112 μs 完成。保留必要依赖时，C 从起点开始，10 μs 完成；发布链仍到 102 μs 才结束。全组提前 10 μs，C 却提前 102 μs。
 
 ![必要依赖与独立操作的完成时间](ch07/figure-7-12-ordering.svg)
 
@@ -471,7 +471,7 @@
 
 > **实验 7-7 · 核心：操作顺序与依赖等待**
 >
-> 将例题 7.4 的独立操作改为 50 μs，求两种安排的整组操作和独立操作的完成时间。再加入消费者在通知后处理 30 μs，画出目的缓冲最早可覆盖的时刻。对旧值反例分别画出等待后读取、提前读取后重读两条执行路径。
+> 将例题 7.4 的独立操作改为 50 μs，求两种安排的整组操作和独立操作的完成时间。再假设消费者收到通知后需要 30 μs 处理数据，标出目的缓冲区最早可以写入新数据的时刻。对旧值反例分别画出等待后读取、提前读取后重读两条执行路径。
 
 ## 7.5 共享网络中的拥塞与可靠性
 
@@ -481,7 +481,7 @@
 
 **例题 7.5：平均只用 32% 的链路，为什么仍然排队？** 两项作业每 100 ms 各有 20 ms 的通信高峰，高峰速率均为 40 GB/s，其他时间不发送。假设队列初始为空，作业按上述时间表发送数据，缓冲区足够大，可以保存所有积压。
 
-**解答。** 两项作业的平均总需求为
+解答： 两项作业的平均总需求为
 
 $$
 \bar\lambda=2\times40\times\frac{20}{100}=16\ \mathrm{GB/s}.
@@ -503,7 +503,7 @@
 
 *图 7-31：将到达与服务速率的差额随时间累积，得到 600、150、0 MB 三种峰值。此算例使用无限缓冲计算需求积压。*
 
-**讨论：可以容忍多少相位漂移？** 重叠时间为 $h$ 时，新增积压为 $(80-50)h$。重叠 5 ms 就积压 150 MB。若只给这次突发留出 512 KiB 缓冲，从空队列出发，允许的重叠时间约为 $512\ \mathrm{KiB}/30\ \mathrm{GB/s}=17.5$ μs。毫秒级的高峰漂移，远大于这份缓冲能够吸收的时间。
+**讨论：发送时刻偏移多少会使共享出口产生积压？** 重叠时间为 $h$ 时，新增积压为 $(80-50)h$。重叠 5 ms 就积压 150 MB。若只给这次突发留出 512 KiB 缓冲，从空队列出发，允许的重叠时间约为 $512\ \mathrm{KiB}/30\ \mathrm{GB/s}=17.5$ μs。毫秒级的高峰漂移，远大于这份缓冲能够吸收的时间。
 
 CASSINI 利用训练的周期通信，通过安排作业的部署位置和通信时刻，让通信高峰错开，并监测漂移、重新调整。[^cassini] 本书两个 CPU 分布式数据并行（DDP）训练作业的一次性错峰对照也显示，初始延迟 50 ms 后，相位在运行中继续漂移，三轮测量中，整组作业的完成时间反而增加约 2%–3%。[^phase] 周期调度需要持续维护相位；拥塞控制则根据实时反馈，调整偏离预期的发送速率。
 
@@ -543,7 +543,7 @@
 
 > **实验 7-8 · 延伸：割集带宽与反馈队列**
 >
-> 沿用 80／50 GB/s 和初始 256 KiB 积压，求总缓冲为 512 KiB、1 MiB 时可以容忍的反馈时延。将降速后的到达分别设为 50、45、40 GB/s，求排空时间，并解释其中一个条件为何无法排空。
+> 设到达速率为 80 GB/s，出口速率为 50 GB/s，初始积压为 256 KiB，求总缓冲为 512 KiB、1 MiB 时可以容忍的反馈时延。将降速后的到达速率分别设为 50、45、40 GB/s，求排空时间，并解释其中一个条件为何无法排空。
 
 ### 7.5.3 多路径与重传
 
@@ -573,7 +573,7 @@
 
 > **实验 7-9 · 延伸：多路径与重传成本**
 >
-> 将第二条路径的传播时延从 1 μs 扫描到 9 μs，求均分双路径与单路径的交点。再令两条路径分别承担五个和三个报文，求 1／9 μs 条件下的完成时间。保持首包丢失，把恢复等待从 20 μs 增为 40 μs，计算首次有序交付与缓存的数据量。
+> 将第二条路径的传播时延从 1 μs 扫描到 9 μs，求均分双路径与单路径的交点。再令两条路径分别承担五个和三个报文，求 1／9 μs 条件下的完成时间。保持首包丢失，把恢复等待从 20 μs 增为 40 μs，计算首次能够按序交付数据的时刻，以及此前需要缓存的数据量。
 
 ### 7.5.4 死锁
 
@@ -621,7 +621,7 @@
 
 ### 7.6.2 分层网络的通信耗时计算
 
-**例题 7.6：为一次归约选择改进顺序。** 沿用前述算例中的 192 MiB 梯度。梯度在计算开始后 20 ms 就绪，归约后执行 2 ms 更新。比较连续环与分层归约，再考察并发不足、提前通信和链路退化。
+**例题 7.6：归约算法、执行顺序与链路带宽应先优化哪一项？** 沿用前述算例中的 192 MiB 梯度。梯度在计算开始后 20 ms 就绪，归约后执行 2 ms 更新。比较连续环与分层归约，再考察并发不足、提前通信和链路退化。
 
 **解答：先确定执行顺序。** 计算→归约→更新形成一条串行链。使用第 7.2 节的传输模型，连续环总时间约为 $20+8.8+2=30.8$ ms，分层归约约为 $20+6.6+2=28.6$ ms。通信时间减少约 26%，整步只减少约 7%。20 ms 计算和 2 ms 更新占据了其余时间。
 
@@ -667,7 +667,7 @@
 
 连续环约为 27.8 ms，分层归约约为 25.6 ms。若数据更早在 12 ms 就绪，分层通信约在 18.6 ms 结束，早于 20 ms 的计算结束时刻，整步只需 22 ms。继续缩短通信不再改变这条关键路径，此时需要缩短计算或更新，才能进一步减少总时间。
 
-**讨论：链路退化会把瓶颈移回哪里？** 将共享出口从 40 GB/s 降至 20 GB/s，分层方案的远端传输时间增加一倍，通信约为 11.6 ms。数据在 17 ms 就绪时，整步约为 30.6 ms。原本可以与计算重叠的通信，现在延迟到了计算结束之后。流量模型计算传输时间，并发模型判断实际吞吐能否达到链路带宽，依赖模型据此计算训练步耗时。
+**讨论：出口带宽下降后，归约是否重新成为训练步的瓶颈？** 将共享出口从 40 GB/s 降至 20 GB/s，分层方案的远端传输时间增加一倍，通信约为 11.6 ms。数据在 17 ms 就绪时，整步约为 30.6 ms。原本可以与计算重叠的通信，现在延迟到了计算结束之后。流量模型计算传输时间，并发模型判断实际吞吐能否达到链路带宽，依赖模型据此计算训练步耗时。
 
 再按顺序比较这些时间线：减少通信量会缩短蓝色条带，并发不足会把它拉长，数据提早就绪则让它向左移动。绿色更新要等计算和通信都结束后才能开始。这三个模型也给出了清楚的改进顺序。先通过分层归约减少跨服务器传输，再通过并发提交和处理请求，充分利用链路带宽；达到带宽上限之后，寻找可以提早开始的通信；通信被计算完全覆盖后，优化此时决定完成时间的计算或更新操作。
 
@@ -699,11 +699,9 @@
 
 先保持通信图不变，比较带宽和启动开销的影响；再调整并行方案与专家分布，比较减少交接次数、由设备直接发起通信或使用卸载单元的收益。MoE 的路由、分发与合并会提出对网络操作的要求；网络能否高效完成这些操作，又会影响专家粒度与跨节点部署。若模型计算因专用硬件变快，通信时间本身未变，它在关键路径中的份额却会增加。
 
-继续分析同一个 V4.1 会话，设请求被迁移到没有其全局 KV 的实例。只为比较传输下界，假设搬运一份 128K 全局历史、有效带宽为 25 GB/s，忽略分页元数据、复制与其他状态。V4 Flash 的 439.281 MiB 需要约 18.425 ms，V4.1 的 111.250 MiB 需要约 4.666 ms。状态压缩使这条路径的传输时间减少了约 13.759 ms。[^v41-case]
-
-但请求恢复还可能等待连接、缓存查询、队列和局部 SWA 重建。若用一个串行教学模型，$T_{\mathrm{resume}}=T_{\mathrm{lookup}}+T_{\mathrm{transfer}}+T_{\mathrm{replay}}+T_{\mathrm{queue}}$，全局 KV 的 3.95 倍压缩只直接作用于传输项。采用预取和重叠执行后，应根据新的依赖图计算恢复时间。
-
-先保持迁移策略不变，计算传输量减少带来的收益；再比较留在原实例等待、迁移全局 KV 后重放、以及从输入重新计算。更高带宽也可能使原先不合算的远端缓存值得使用，但链路启动、并发争用和恢复计算仍需检验。
+继续分析同一个 V4.1 会话，设请求被迁移到没有其全局 KV 的实例。先计算全局历史这一段载荷的传输时间：上下文为 128K，有效带宽为 25 GB/s。V4 Flash 的 439.281 MiB 需要约 18.425 ms，V4.1 的 111.250 MiB 需要约 4.666 ms。状态压缩使这条路径的传输时间减少了约 13.759 ms。[^v41-case]
+
+这 13.759 ms 可以作为迁移方案节省的传输预算。若请求依次经历排队、缓存查询、传输和局部 SWA 重建，恢复时间就是四段之和；其中传输缩短多少，总时间便缩短多少。第九章将把这条恢复路径与留在原实例等待、从输入重新计算放在一起比较，决定请求应去哪里执行。
 
 ### 7.6.4 部署与常见误区
 
@@ -727,7 +725,7 @@
 
 > **实验 7-10 · 核心：网络优化的模型级收益**
 >
-> 复算例题 7.6，画出连续环、分层归约、在途请求数不足、提前通信与出口退化的整步时间线。求分层通信恰好被 20 ms 计算覆盖时的数据最晚就绪时刻。再换成 36 层、每层两次 8 KiB 归约，分别将带宽增加三倍、启动减至 2 μs，解释两类负载的改进顺序为何不同。
+> 复算例题 7.6，画出连续环、分层归约、在途请求数不足、提前通信与出口退化的整步时间线。求分层通信恰好被 20 ms 计算覆盖时的数据最晚就绪时刻。再换成 36 层、每层两次 8 KiB 归约，分别考虑将带宽提高到原来的三倍，以及将每次启动开销降至 2 μs，解释两类负载的改进顺序为何不同。
 
 [^capacity]: 模型规模与状态大小的计算方法见[模型资源计算](../case-studies/model-resource-accounting.md)及[第七章扩写资料](../outlines/extensions/07-数据中心网络.md#detail-7.1)。16 bytes／参数是本章声明的混合精度 Adam 教学配置；万亿参数推理例子按每参数 0.5 byte 计算权重容量。
 
--- before/manuscripts/08-单实例推理.md
+++ manuscripts/08-单实例推理.md
@@ -45,9 +45,9 @@
 
 请求完成后，调度器不再为它安排计算，状态管理器则决定如何处理留下的 KV。该请求独有的 KV 所占空间可以释放，公共前缀的 KV 可以保留，供后续请求复用。因此，独有状态所占空间随请求结束而释放，可复用状态由缓存策略继续管理。第 8.3 节将进一步说明这些缓存何时共享、何时释放。
 
-### 8.1.2 权重、KV 与运行时缓冲的容量预算
-
-时间线说明了请求要经历哪些阶段。要让多条请求沿着各自的时间线向前执行，实例必须同时保存权重、请求状态和计算中间结果。本节的算例使用单张设备，因此下面列出这张设备上的内存占用。多卡实例则要按各卡保存的权重分片、KV 和缓冲区分别列式：某张卡上的空闲显存，只有通过改变数据放置才能用于另一张卡的计算。
+### 8.1.2 权重、KV 与运行时缓冲的存储容量需求
+
+时间线说明了请求要经历哪些阶段。要让多条请求沿着各自的时间线向前执行，实例必须同时保存权重、请求状态和计算中间结果。本节的算例使用单个设备，因此下面列出这张设备上的内存占用。多卡实例则要按各卡保存的权重分片、KV 和缓冲区分别列式：某张卡上的空闲显存，只有通过改变数据放置才能用于另一张卡的计算。
 
 用 $M_w$ 表示常驻权重的容量，$M_{\mathrm{KV}}$ 表示去除重复引用后的物理 KV 块总容量，$M_a$ 表示激活与普通工作区的容量，$M_u$ 表示计算图重放等其他缓冲区的容量。这些内存占用之和必须小于或等于设备可用容量 $C$：
 
@@ -97,7 +97,7 @@
 
 ## 8.2 批处理与请求调度
 
-### 8.2.1 权重复用与批量收益
+### 8.2.1 权重复用如何提高批处理效率
 
 第 8.1 节算出了内存能够容纳多少请求。接下来要解释：把这些请求一起计算，为什么能提高吞吐率？考虑矩阵乘法 $Y=XW$，输入矩阵 $X$ 有 $b$ 行。同一个权重元素参与所有行的计算，因此将多行一起处理，可以在读取一次权重后完成更多乘加。假设每个权重元素在批内只读取一次，每次乘加计为两次运算，权重元素占 $s_w$ bytes，则**算术强度**是每读取一个字节完成的运算数。只计权重读取时，它为
 
@@ -140,15 +140,15 @@
 
 上述模型解释了合批如何减少重复读取。落实到请求时间上，还要看更大的批次是否延迟了首个 token，以及相邻输出之间是否等得更久。批量实验同时记录了这两项变化。Qwen3-8B 输入 2K、输出 256 个 token 时，batch 从 1 增至 64，总吞吐率从约 37 增至 1165 token/s；但首个 token 的返回时间从约 96 ms 延后到 3.29 秒，平均输出间隔从约 27 增至 40 ms。每轮输出虽然更多，处理更多输入和运行更大批次也花费了更多时间。该实验同时提交每批请求；在线服务若额外等待请求凑齐批次，这段时间还要计入首 token 延迟。[^batch-measure]
 
-> **练习 8-1 · 计算：批量的转折。** 取 $D_w=15,136,811,008$ bytes、$k=144$ KiB，计算 2K 与 8K 上下文在 batch=1、4、16、64 时的每个输出 token 的读取量，并求两个整数交点。再用 12 GiB KV 可用内存、每块 16 个位置和 256 个输出，求两类独立请求各能接纳多少条。比较容量边界与读取交点的先后顺序。
-
-权重读取变得便宜后，批处理的收益需要重新计算。沿用第 4 章的 8K 示例，传统 HBM 每输出 token 摊到的读取为 $W/B+K$；权重由足够快的独立 ROM 提供时，HBM 每输出仍需读取 $K$。[^core-calculation]图中两条曲线的距离随 batch 缩小，说明原先依靠 batch 摊薄权重读取的收益正在消失。
-
-这不表示应当取消批处理。大矩阵可能更有效地使用计算单元，连续批处理仍能利用不同请求释放的执行位置。应重新测量计算效率、KV 带宽和排队时间，再选择满足时延要求的 batch；不能直接沿用传统设备上测得的最优值。静态 KV 放得下 18 条请求，也不代表同时推进 18 条的时延一定合格。
-
-![图 8-4 每输出 token 分摊的 HBM 读取量。固定 8K 上下文，传统路径为 W/B+K，独立快速 ROM 路径为 K；曲线只表示读取量，不是实测执行时间。](ch08/figure-8-batch-counterfactual.svg)
-
-*图 8-4　每输出 token 分摊的 HBM 读取量。固定 8K 上下文，传统路径为 W/B+K，独立快速 ROM 路径为 K；曲线只表示读取量，不是实测执行时间。*
+> **练习 8-1 · 计算：批量增大时的读取量变化。** 取 $D_w=15,136,811,008$ bytes、$k=144$ KiB，计算 2K 与 8K 上下文在 batch=1、4、16、64 时的每个输出 token 的读取量，并分别求两种上下文长度下，KV 读取量首次达到或超过分摊权重读取量的整数批量。再用 12 GiB KV 可用内存、每块 16 个位置和 256 个输出，分别求两种上下文长度下最多可以接纳多少条独立请求。比较容量边界与读取交点的先后顺序。
+
+权重读取耗时减少后，批处理的收益需要重新计算。沿用第 4 章的 8K 示例，传统 HBM 每输出 token 摊到的读取为 $W/B+K$；权重由足够快的独立 ROM 提供时，HBM 每输出仍需读取 $K$。[^core-calculation]图中两条曲线的距离随 batch 增大而缩小，说明原先依靠 batch 摊薄权重读取的收益正在消失。
+
+独立 ROM 省去了合批中的权重摊销收益，大矩阵对计算单元的利用和批次内的 KV 读取则继续随 batch 改变。将新的读取量代入前面的时间模型，就能求出计算与 KV 读取的交点，再按首 token 延迟和输出间隔选择批量。
+
+![图 8-4 每输出 token 分摊的 HBM 读取量。固定 8K 上下文，传统路径为 W/B+K，独立快速 ROM 路径为 K；纵轴按上述公式计算读取量。](ch08/figure-8-batch-counterfactual.svg)
+
+*图 8-4　每输出 token 分摊的 HBM 读取量。固定 8K 上下文，传统路径为 W/B+K，独立快速 ROM 路径为 K；纵轴按上述公式计算读取量。*
 
 ### 8.2.2 从固定批次到连续批处理
 
@@ -156,7 +156,7 @@
 
 新请求更早进入，也意味着它的输入计算更早占用设备。若新请求带有很长的提示，prefill 就会延长已有请求两次 decode 之间的等待。调度器因此要在两件事之间分配设备时间：处理新请求的输入，以及让已有请求继续生成。[^schedule-survey]
 
-**算例：四条请求的接纳顺序。** 最多同时运行两条请求。r0、r1 在零时刻到达，各有 16 个输入，分别生成 8、2 个输出；r2 在 20 μs 到达，有 128 个输入、2 个输出；r3 在 30 μs 到达，有 16 个输入、4 个输出。每步成本取 10 μs 启动、每个新位置 1 μs、每个有效因果配对 1 ns。[^schedule]
+**算例：连续批处理为何缩短总时间，却拉长输出间隔？** 最多同时运行两条请求。r0、r1 在零时刻到达，各有 16 个输入，分别生成 8、2 个输出；r2 在 20 μs 到达，有 128 个输入、2 个输出；r3 在 30 μs 到达，有 16 个输入、4 个输出。每步成本取 10 μs 启动、每个新位置 1 μs、每个有效因果配对 1 ns。[^schedule]
 
 固定批次让 r2、r3 等待 r0、r1 整组结束，总时间约 317 μs。连续批处理在 r1 结束后接纳 r2，使总时间降到约 277 μs；与此同时，r0 的下一次输出等待 r2 的长 prefill，最大输出间隔从约 12 增至 147 μs。总时间节省约 13%，一次输出停顿却增长了一个数量级。
 
@@ -174,7 +174,7 @@
 
 *图 8-7：每次先安排已有请求 decode，再处理一段新输入。最大间隔降至约 29 μs。三张调度图使用相同的时间尺度。*
 
-### 8.2.3 长 Prefill 干扰与分块执行
+### 8.2.3 长 Prefill 对生成的干扰与分块执行
 
 一次处理的 prefill 块越大，新请求所需的输入处理轮数越少；块越小，已有请求就越早得到下一次执行机会。调度器通常先安排正在 decode 的请求，再用本轮剩余的 token 配额处理 prefill。这样，长输入就被拆到多个输出间隔中计算。
 
@@ -211,7 +211,7 @@
 
 因此，可以分两步确定每轮处理多少 token。先根据允许的输出间隔确定一轮最多执行多久，再结合上下文长度和预设执行形状，换算为可处理的新位置数。在相同时间内，上下文较长的请求能处理的新位置较少，上下文较短的请求则可以使用更大的块。确定执行安排后，还要为这些请求分配 KV 空间；下一节将讨论如何分配和复用这部分内存。
 
-> **练习 8-2 · 核心 · 分析：分块预算。** 用第 8.2.2 节的四请求和成本模型，手算 r0、r1 的初次 prefill、下一步 decode，以及连续批处理接纳 r2 的那一轮。解释最长输出间隔的来源。再计算 $c=512$、$h=0$ 与 $h=7680$ 时的注意力配对数。最后读取配套六请求回放，在“最长间隔小于 50 ms”和“后到请求尽快接纳”两种目标下分别选择每轮 token 上限，并写出决定选择的时间项。
+> **练习 8-2 · 核心 · 分析：由输出间隔限制确定预填充分块大小** 采用第 8.2.2 节的四条请求与执行时间模型，手算 r0、r1 的初次 prefill、下一步 decode，以及连续批处理接纳 r2 的那一轮。解释最长输出间隔的来源。再计算 $c=512$、$h=0$ 与 $h=7680$ 时的注意力配对数。最后分析配套的六条请求回放记录，在“最长间隔小于 50 ms”和“后到请求尽快接纳”两种目标下分别选择每轮 token 上限，并指出哪一项耗时决定了选择。
 
 ## 8.3 KV 缓存的分配、复用与释放
 
@@ -233,7 +233,7 @@
 
 最多 $p-1$ 个位置。块越小，这一上限越低，块表项和分配次数也越多。2023 年，伯克利等机构的研究者在 vLLM 中提出 PagedAttention，将操作系统分页管理的思路用于 KV 缓存，减少预留空间与内存碎片对服务并发的限制。PagedAttention 将这种映射带入注意力执行；FlashAttention 则在算子内部组织分块计算与中间结果，两者分别作用于状态分配和计算访存。
 
-**算例：同一组序列怎样少占空间？** 设 A、B、C、D 当前长度分别为 9、13、5、15，每条最大允许 16 个位置。整段预留共占 64 个位置；取 $p=4$，分页分别分配 12、16、8、16，共 52 个位置。其中有效位置仍是 42 个，未用尾槽从 22 减到 10 个。
+**算例：KV 分页分配能减少多少预留空间？** 设 A、B、C、D 当前长度分别为 9、13、5、15，每条最大允许 16 个位置。整段预留共占 64 个位置；取 $p=4$，分页分别分配 12、16、8、16，共 52 个位置。其中有效位置仍是 42 个，未用尾槽从 22 减到 10 个。
 
 ![整段预留分页与前缀共享的物理位置数](ch08/figure-8-5-pages.svg)
 
@@ -251,7 +251,7 @@
 
 ### 8.3.2 分支共享、写时复制与内存释放
 
-前缀共享图中，B 与 A 引用相同的两个物理块。如果 A 先结束，这两个块仍要留给 B；如果 B 要修改其中的内容，又不能影响 A。实现共享因此需要确定何时释放共享块，以及如何避免不同请求互相覆盖数据。每张块表中指向该物理块的记录称为一个**引用**。**引用计数**记录还有多少使用者需要它。共享块由多个块表引用。每当一条请求获得引用，引用数加一；请求结束时释放引用。最后一个引用被释放后，块才满足释放的引用条件。这样，同一份共同上下文既能服务多个分支，也能在其中一条分支结束后继续服务其余分支。
+前缀共享图中，B 与 A 引用相同的两个物理块。如果 A 先结束，这两个块仍要留给 B；如果 B 要修改其中的内容，又不能影响 A。实现共享因此需要确定何时释放共享块，以及如何避免不同请求互相覆盖数据。每张块表中指向该物理块的记录称为一个**引用**。**引用计数**记录还有多少使用者需要它。共享块由多个块表引用。每当一条请求获得引用，引用数加一；请求结束时释放引用。最后一个引用被释放后，才满足释放该块所需的引用计数条件。这样，同一份共同上下文既能服务多个分支，也能在其中一条分支结束后继续服务其余分支。
 
 ![根据引用计数释放共享块](ch08/figure-8-reference-release.svg)
 
@@ -273,7 +273,7 @@
 
 服务系统知道哪些位置属于共同前缀，哪些位置会在分支后独立写入，因此可以把模型的复用关系落实为物理块共享。页表负责地址映射，前缀关系决定共享范围，分支写入触发私有副本。原来按整条请求独立预留的空间，由此改为按实际产生与复用的状态分配。
 
-**回到章首的算例。** 16 条长请求共享前 6144 个输入 token，其 KV 共占 864 MiB。每条请求的私有部分包含 $8192-6144+255=2303$ 个位置。按每块 16 个位置分配，需要容纳 2304 个位置，即 324 MiB。因此，整组请求生成完毕时所需的 KV 空间为
+**公共前缀共享后的整批 KV 容量** 16 条长请求共享前 6144 个输入 token，其 KV 共占 864 MiB。每条请求的私有部分包含 $8192-6144+255=2303$ 个位置。按每块 16 个位置分配，需要容纳 2304 个位置，即 324 MiB。因此，整组请求生成完毕时所需的 KV 空间为
 
 $$
 M_{\mathrm{KV}}=864+16\times324=6048\ \mathrm{MiB}.
@@ -289,7 +289,7 @@
 
 内存不足时，引擎可以暂时停止一条请求，释放它的 KV，之后再根据已保存的输入和输出重新计算状态，这个过程称为抢占。同一组实验使用 1 GiB KV 池时发生了一次抢占，比正常执行多调度 1805 个位置；使用 2 GiB 池时则没有这次重算。抢占暂时把内存让给其他请求，代价是恢复执行时需要重复计算。[^pages]
 
-> **练习 8-3 · 计算：块容量与共享。** 将四条长度 9、13、5、15 的序列分别按 2、4、8 个位置分页，计算尾槽数与块表项数。再按章首算例计算 8、16、32 条长请求在独立与共享状态下的完整容量。说明公共前缀增长一个完整块时，$b$ 条请求合计减少多少物理块。
+> **练习 8-3 · 计算：KV 块大小与前缀共享如何影响容量** 将四条长度 9、13、5、15 的序列分别按 2、4、8 个位置分页，计算最后一个块中未使用的位置总数与块表项数。再按章首算例计算 8、16、32 条长请求在独立与共享状态下的完整容量。说明公共前缀增长一个完整块时，$b$ 条请求合计减少多少物理块。
 
 ### 8.3.3 对话与智能体的前缀复用
 
@@ -309,7 +309,7 @@
 
 每轮在末尾追加内容，可以保留已有的公共前缀，但上下文越长，KV 占用和后续读取量也越大。用较短的新内容替换旧内容，可以缩短输入，却需要重新计算替换点之后的状态。生成摘要同样要先花时间得到摘要，再对摘要后的输入执行 prefill。因此，组织上下文时，需要同时考虑内存占用、重算时间和后续读取量。
 
-**扩展：混合状态的恢复位置。** 全注意力保留逐位置 KV，递推模型通常只保留当前状态。设文本匹配到第 10,752 个位置，递推快照保存于 4096 和 8192；系统从 8192 恢复，再计算到 10,752，重算 2560 个位置。增加快照可以缩短这段距离，同时增加持久状态。KDA 是第二章介绍的 Kimi Delta Attention，通过固定形状的递推状态汇总上下文信息。一种 KDA 实现将层内张量分到八张卡（张量并行 TP=8），此时每卡每份快照约 53.6 MiB，保存 2 份约 107 MiB，保存 32 份约 1714 MiB。保存快照的位置越密，重算距离越短，保留的状态也越多。[^hybrid]
+**扩展：混合注意力模型如何确定可恢复的前缀位置** 全注意力保留逐位置 KV，递推模型通常只保留当前状态。设文本匹配到第 10,752 个位置，递推快照保存于 4096 和 8192；系统从 8192 恢复，再计算到 10,752，重算 2560 个位置。增加快照可以减少重算的位置数，同时需要保存更多状态。KDA 是第二章介绍的 Kimi Delta Attention，通过固定形状的递推状态汇总上下文信息。一种 KDA 实现将层内张量分到八张卡（张量并行 TP=8），此时每卡每份快照约 53.6 MiB，保存 2 份约 107 MiB，保存 32 份约 1714 MiB。保存快照的位置越密，重算距离越短，保留的状态也越多。[^hybrid]
 
 ![从最近递推快照恢复](ch08/figure-8-prefix-restore.svg)
 
@@ -317,7 +317,7 @@
 
 上下文编排还可以主动改变复用机会。假设已有 8K token 的上下文，下一轮追加 1K 工具结果，最多可复用原来的 8K 前缀，新增部分需处理；如果改写最前面的指令，后续 token 即使文字相同，产生 KV 时依赖的历史已经变化，不能继续把整段当作相同前缀。另一种方案是把历史总结为 2K，再追加 1K，后续状态会更短，却要支付总结与重新处理的成本，并检验信息损失。
 
-因此，“总 token 更少”与“本轮计算更少”并不等价。应用可以保留稳定指令和材料的位置，把变化较快的信息放在后面；需要总结时，则按未来还要生成多久，比较一次重算与后续每步节约。复用条件还包括模型版本、位置和其他影响计算的配置一致。
+应用可以保留稳定指令和材料的位置，把变化较快的信息放在后面，使后续请求继续复用同一前缀。需要总结时，则按未来还要生成多久，比较一次重算与后续每步节约。
 
 用一组教学条件求出总结的收益边界。两种方案均通过相同任务检查，后续生成相同数量的 token。总结及重建缓存合计额外耗时 300 ms；在给定设备、批次和后续长度范围内，缩短上下文使每步 decode 净减少 1 ms。若后面还有 $N$ 步生成，总结方案相对保留历史节省的时间为
 
@@ -325,23 +325,21 @@
 \Delta T=N\times1\ \mathrm{ms}-300\ \mathrm{ms}.
 $$
 
-后续生成 100 步时，总结多花 200 ms；生成 1000 步时，总结节省 700 ms；超过 300 步后开始获得时间收益。总结把一次额外计算换成后续多次较短的状态访问，因而适合在还有足够后续工作时进行。把未来工具轮次也纳入比较，就按各轮的输入处理、生成与状态恢复逐项累计。应用决定何时总结，服务提供重建与读取代价，两者共同确定时机。
+后续生成 100 步时，总结多花 200 ms；生成 1000 步时，总结节省 700 ms；超过 300 步后开始获得时间收益。总结把一次额外计算换成后续多次较短的状态访问，因而适合在还有足够后续工作时进行。把未来工具轮次也纳入比较，就需要逐轮累加输入处理、生成和状态恢复的耗时。应用决定何时总结，服务系统提供重建缓存与读取状态所需的时间，两者共同确定时机。
 
 ![图 8-20 同一历史的三种更新方式。蓝色表示可复用前缀，橙色表示需要重新处理的输入；总结方案还要额外执行总结并验证质量。长度以 K token 示意。](ch08/figure-8-context-edits.svg)
 
 *图 8-20　同一历史的三种更新方式。蓝色表示可复用前缀，橙色表示需要重新处理的输入；总结方案还要额外执行总结并验证质量。长度以 K token 示意。*
 
-V4.1 让“复用一个前缀”变成两类局部状态的不同处理。第二章的 CED 包含编码器和解码器：**编码器 SWA**可以在主机 DRAM 池中短期保留，帮助下一轮从前缀继续；**解码器 SWA**只为本轮后续生成使用，论文部署不把它作为前缀缓存保存。全局 KV 则进入可较长期复用的缓存。不能把一次正在生成时的 40 层 SWA 驻留量，直接当作下一轮前缀缓存的保存量。[^v41-case]
-
-工具返回后，先判断编码器怎样继续。全局 KV 和编码器 SWA 都命中时，编码器直接处理追加输入；仅全局 KV 命中时，编码器重放已缓存前缀的最近最多 128 个 token，并与未缓存后缀一起执行。重放段只重建编码器 SWA，复用既有全局 KV，不重算或覆盖它；新增后缀才产生新的全局 KV 和 SWA。全局 KV 也未命中时，则重算缺失前缀。这些路径都要求恢复所需的 token 或输入表示可获得，模型版本、位置及输入一致。
-
-随后，每次 Prefill 都有共同的一步：取完整提示末尾最多 128 个位置的编码器输出，经过 20 层解码器，近似构建解码器 SWA，为第一步 decode 准备局部状态。因此，“编码器缓存都命中”只省去了编码器的前缀恢复，不能跳过解码器窗口重放。
-
-为什么短窗口重放是近似的？即使每层只访问最近 128 个位置，多层叠加后，一个局部状态也可能通过前一层间接依赖更早输入。有界重放从某个位置开始，并把局部注意力限制在重放片段内，截断了片段之前的局部依赖。论文第 3.2.2 节特别指出：编码器重建状态是近似的，随后新增后缀的全局 KV 和 SWA 因而也可能随缓存命中位置变化，不再数学上完全一致。内容和版本相同仍不足以保证不同恢复路径产生逐位相同的状态。
-
-报告中的持久化缓存约缩为 1/8，来自同一工作负载下的两项变化：V4 原部署中 SWA 约占一半长期缓存；V4.1 不再长期持久化这一部分，再将保留的全局 KV 压到约 1/4。因此近似为 $1/2\times1/4$，它依赖会话长度和缓存策略，并不是每个请求都必然减少八倍。短期编码器 SWA 仍占用 DRAM，解码器 SWA 仍占用本轮生成期间的设备内存。
-
-检验这种取舍时，固定解码器恢复方式，比较编码器直接命中、完整精确恢复和有界恢复；再固定编码器状态，单独比较解码器完整前向与窗口重放。分别记录输出分布、任务成功率、恢复时间和传输量，把关键证据移到窗口之外、改变命中位置或增加多跳依赖。报告称已测质量影响很小，并在后训练中模拟解码器重放以适应这种执行；这仍不证明所有边界条件下都与完整前向等价。
+V4.1 在复用前缀时，对两类局部状态分别处理。第二章的 CED 包含编码器和解码器：**编码器 SWA**可以在主机 DRAM 池中短期保留，帮助下一轮从前缀继续；**解码器 SWA**只为本轮后续生成使用，论文部署不把它作为前缀缓存保存。全局 KV 则进入可较长期复用的缓存。因此，生成期间驻留的 40 层 SWA 与跨轮次保留的前缀缓存，需要分别计算容量。[^v41-case]
+
+设工具返回后，恢复所需的 token 或输入表示仍可取得，模型版本、位置与输入保持一致。全局 KV 和编码器 SWA 都命中时，编码器直接处理追加输入；仅全局 KV 命中时，编码器重放已缓存前缀的最近最多 128 个 token，并与未缓存后缀一起执行。重放段重建编码器 SWA，继续使用既有全局 KV；新增后缀产生新的全局 KV 和 SWA。全局 KV 也未命中时，则重算缺失前缀。
+
+随后，每次 Prefill 都有共同的一步：取完整提示末尾最多 128 个位置的编码器输出，经过 20 层解码器，近似构建解码器 SWA，为第一步 decode 准备局部状态。因此，编码器缓存全部命中时，可以省去编码器的前缀恢复，解码器仍需执行窗口重放。
+
+需要注意的是，短窗口重放会截断更早的局部依赖。每层虽然只访问最近 128 个位置，多层叠加后，局部状态仍可通过前一层间接依赖更早的输入。从窗口边界重新执行，会改变这部分历史信息。论文第 3.2.2 节因此将编码器重建状态定义为近似状态；它又参与后续计算，使新增后缀的全局 KV 和 SWA 随恢复起点变化。
+
+在报告采用的工作负载与缓存策略下，V4 的 SWA 约占长期缓存的一半。V4.1 将这部分移出持久化缓存，再将全局 KV 压到约四分之一，长期保存量因此约为原来的 $1/2\times1/4=1/8$。短期保留的编码器 SWA 放在 DRAM 中，解码器 SWA 则在本轮生成时驻留设备。缓存按使用时段分层保存，进一步降低了长期容量需求。
 
 ![编码器的三条前缀恢复路径与共同的解码器重放。缓存命中状态中的 SWA 专指编码器；所有路径在 Prefill 中仍构建解码器 SWA，再开始生成。箭头表示执行先后，框大小不代表耗时。](ch08/figure-8-v41-recovery.svg)
 
@@ -383,9 +381,9 @@
 
 前缀共享通过去除重复的 KV 节省空间。即使请求之间没有相同前缀，也可以通过量化减少权重和 KV 的存储量。量化通常将数值分组编码，每组保存较低位宽的编码值，以及缩放系数（scale）等元数据；计算时再根据这些信息恢复近似数值。
 
-**Q2_K** 是 张量计算库 GGML 系列实现中的一种分组量化格式，主编码使用 2 bit，并为组内缩放保存额外信息。以它为例，每组有 256 个值，2-bit 编码占 $256\times2/8=64$ bytes，缩放系数和最小值等元数据另占 20 bytes。因此，一组共需 84 bytes，平均每值为 $84\times8/256=2.625$ bit。位宽降低后，元数据在总容量中的比重相应提高，在这个格式中约占 24%。[^gguf]
-
-**扩展算例：接近内存边界的大模型。** Qwen3-235B-A22B 的一个 Q2_K 文件变体采用以 2 比特为主的分组量化，并混合使用多个位宽，量化编码与未量化的浮点数据约 64.43 GiB、量化元数据约 15.37 GiB，加上文件头与填充，共约 79.81 GiB。另一个 `UD-Q2_K_XL` 变体约 81.97 GiB。取 96 GB 十进制内存、给系统与工作区预留 8 GiB，剩余约 81.41 GiB。[^files]
+**Q2_K** 是张量计算库 GGML 系列实现中的一种分组量化格式，主编码使用 2 bit，并为组内缩放保存额外信息。以它为例，每组有 256 个值，2-bit 编码占 $256\times2/8=64$ bytes，缩放系数和最小值等元数据另占 20 bytes。因此，一组共需 84 bytes，平均每值为 $84\times8/256=2.625$ bit。位宽降低后，元数据在总容量中的比重相应提高，在这个格式中约占 24%。[^gguf]
+
+**扩展算例：量化权重装入内存后，还能容纳多长的上下文？** Qwen3-235B-A22B 的一个 Q2_K 文件变体采用以 2 比特为主的分组量化，并混合使用多个位宽，量化编码与未量化的浮点数据约 64.43 GiB、量化元数据约 15.37 GiB，加上文件头与填充，共约 79.81 GiB。另一个 `UD-Q2_K_XL` 变体约 81.97 GiB。取 96 GB 十进制内存、给系统与工作区预留 8 GiB，剩余约 81.41 GiB。[^files]
 
 装入前一个文件后，还剩 1.60 GiB；后一个文件则已经超出可用空间约 0.56 GiB。该模型一条 8K 上下文的 BF16 KV 约占 1.47 GiB，因此前一种方案还能处理一条请求，并剩余约 0.13 GiB。若上下文长度增至 32K，KV 就需要约 5.88 GiB，必须增加内存或改变权重的存放方式。两个文件只相差 2.16 GiB，已经足以决定能否容纳请求。
 
@@ -423,9 +421,7 @@
 
 上下文越长，减少读取所节省的时间越多。一个 H100／Llama-3.1-8B 的历史评估用 $6.44+4.37\times10^{-5}L$ 和 $6.58+2.37\times10^{-5}L$ ms 拟合 BF16 与 FP8 的输出间隔。FP8 固定项多 0.14 ms，每个上下文位置的增长项少 $2\times10^{-5}$ ms。两者相除，交点约为 7000 token；在 2K 时 BF16 约 6.53 ms、FP8 约 6.63 ms，在 16K 时则分别约 7.16 和 6.97 ms。较长上下文使读取所节省的时间超过了新增的固定开销。[^kv-format]
 
-回到 8K 基线，V4.1 全局历史 6.953 MiB 加上有效 SWA 2.578 MiB，共 9.531 MiB；对应 V4 为 30.521 MiB，约相差 3.20 倍。短上下文下局部固定项的占比，使总状态的比例小于全局增长的 3.95 倍。压缩器、分配器和运行时空间尚未计入，更不能把整个实例显存除以四来推断可承接并发。
-
-模型训练时已适应 FP4 主 KV，SWA 则保留 FP8。改变这些位宽时，先保持调度和任务不变测质量与访问，再重新选择 batch 和准入；应与第 8.3 节的命中／恢复条件交叉比较。高命中时全局迁移量很小，长时间暂停后的恢复可能更受重放限制，相同压缩率未必改善相同的服务阶段。
+在 8K 上下文基线下，将全局历史与有效 SWA 的容量相加。V4.1 两项分别为 6.953 MiB 与 2.578 MiB，共 9.531 MiB；V4 的对应两项合计为 30.521 MiB，约相差 3.20 倍。局部窗口占固定空间，因此短上下文下的合计容量差距小于全局历史的 3.95 倍。
 
 ### 8.4.3 权重卸载、预取与每步搬运
 
@@ -459,7 +455,7 @@
 
 这一节的卸载始终由 GPU 执行模型计算，主存负责保存暂不使用的权重。第 9.3 节将进一步改变计算位置：既然专家权重已经在主存，能否让 CPU 直接计算，只向 GPU 传回较小的激活？届时需要比较每个专家的输入行数、CPU 计算时间与权重传输时间。
 
-> **练习 8-5 · 计算：容量换搬运。** 对九份 288 MiB FFN，分别计算一组、两组预取缓冲的净容量，以及能增加多少条 1152 MiB 输入状态、1188 MiB 完整请求状态。再求 24、384 GiB/s 下每步与 255 步复制下界。若留给所有复制的总时间为 1 秒，求所需最低有效带宽。
+> **练习 8-5 · 计算：权重卸载节省的容量与增加的搬运。** 设卸载九份 FFN 权重，每份占 288 MiB。分别计算使用一组和两组预取缓冲时净节省的显存，并求这些空间能多容纳多少份 1152 MiB 的输入状态，或多少份 1188 MiB 的完整请求状态。再分别取 24、384 GiB/s 的复制带宽，求每步复制以及累计 255 步复制的时间下界。若留给所有复制的总时间为 1 秒，求所需最低有效带宽。
 
 ### 8.4.4 质量约束下的容量与速度选择
 
@@ -485,9 +481,9 @@
 
 错误还会增加完成任务的时间。第一次回答失败后重试，得到正确答案的总时间应包括首次生成、检查答案和再次生成。已有实验中，两次重试修正了错误，但额外增加了约 2.0 秒生成时间和 92 个 token。第一次回答更快，如果同时更容易出错，完成整个任务反而可能更慢。[^retry]
 
-回到容量选择，16 条独立长请求采用 q8_0 后约占 9.86 GiB，共享 BF16 前缀后约占 5.91 GiB。接下来比较这两种方案：将转换、生成和重试的时间加入请求的总耗时。第 8.6 节的综合例题将完成这一步。
-
-> **练习 8-6 · 数据分析：压缩后完成任务的成本。** 先用每组 32 值、2-byte scale 复算 8K 与完整长请求的 q8_0、q4_0 容量。再读取配套三种 KV／Q 精度配置的逐题结果，列出 BF16 KV 与“FP8 KV＋BF16 Q”中正确性改变的题目。最后读取重试记录，对每个成功任务合计首答与重试时间，并与只计入最后一次成功生成时间的结果比较。
+比较两种容量方案：16 条独立长请求采用 q8_0 后约占 9.86 GiB，共享 BF16 前缀后约占 5.91 GiB。接下来比较这两种方案：将转换、生成和重试的时间加入请求的总耗时。第 8.6 节的综合例题将完成这一步。
+
+> **练习 8-6 · 数据分析：压缩后完成任务的成本。** 设每组包含 32 个值，另用 2 字节保存量化尺度，复算 8K 输入和完整长请求在 q8_0、q4_0 格式下的 KV 容量。再读取配套三种 KV／Q 精度配置的逐题结果，列出 BF16 KV 与“FP8 KV＋BF16 Q”中正确性改变的题目。最后读取重试记录，对每个成功任务合计首答与重试时间，并与只计入最后一次成功生成时间的结果比较。
 
 ## 8.5 推测解码
 
@@ -545,11 +541,11 @@
 
 ![草稿查询时间与平均每个输出 token 的耗时](ch08/figure-8-12-speculation.svg)
 
-*图 8-32：两符号目标分布，每轮生成四个草稿 token。每轮验证 1.4 ms，查询时间沿横轴变化；每个输出 token 的耗时用一轮成本除以平均输出数。普通 decode 为 1 ms/token。没有提前停止，额外 token 计入输出数。点标出查询为 0.1 ms 的算例，BBBB 与普通执行在查询约 1.65 ms 处相交。*
+*图 8-32：两符号目标分布，每轮生成四个草稿 token。每轮验证 1.4 ms，查询时间沿横轴变化；每个输出 token 的耗时用一轮耗时除以平均输出数。普通 decode 为 1 ms/token。没有提前停止，额外 token 计入输出数。点标出查询为 0.1 ms 的算例，BBBB 与普通执行在查询约 1.65 ms 处相交。*
 
 使用草稿前的准备工作也要计入总时间。若建立上下文索引需要 2 秒，而每个输出 token 能节省约 $1-0.49=0.51$ ms，就要生成约 3900 个 token 才能抵消这 2 秒开销。对于只输出 256 个 token 的请求，准备索引占用的时间过多；如果索引可以提前建立并由多个请求共用，这项开销便能由更多输出分摊。
 
-### 8.5.3 草稿来源与串行、并行起草
+### 8.5.3 草稿来源与串行、并行生成方式
 
 四个草稿 token 能节省多少时间，既取决于它们被接受多少，也取决于得到这段草稿要花多久。实际系统可以从上下文中查找草稿，也可以调用额外的网络生成草稿；两种选择会带来不同的准备时间与内存占用。
 
@@ -557,9 +553,9 @@
 
 将每轮分为生成草稿、准备验证、目标验证和确定输出四个阶段，就能逐项比较这些方法。假设小模型串行生成四个草稿 token，每个花 0.2 ms，共 0.8 ms；并行生成整段草稿只需 0.4 ms。若目标验证都需 1.4 ms，且最终每轮都平均输出 3 个 token，那么串行方式每个 token 约需 $(0.8+1.4)/3=0.73$ ms，并行方式约需 0.60 ms。此时，加速来自草稿生成时间的缩短。
 
-再设并行生成的草稿接受率较低，平均输出数降为 2 个，平均每个输出 token 的耗时升至 0.90 ms，反而慢于前者。较快的起草省下了 0.4 ms，但每轮输出减少，使每个输出 token 分摊的时间增加；整个请求因而变慢。
-
-### 8.5.4 块长、动态预算与并发竞争
+再设并行生成的草稿接受率较低，平均输出数降为 2 个，平均每个输出 token 的耗时升至 0.90 ms，反而慢于前者。更快的草稿生成省下了 0.4 ms，但每轮输出减少，使每个输出 token 分摊的时间增加；整个请求因而变慢。
+
+### 8.5.4 草稿长度的动态选择与并发竞争
 
 确定草稿来源之后，还需要决定每轮生成多长。草稿越长，一轮能够输出的 token 上限越高。但后面的 token 只有在前面全部被接受时，才有机会进入最终输出。设前面的草稿 token 已被接受时，第 $j$ 个 token 的条件接受概率为 $a_j$，则长度为 $m$ 的草稿对应的平均输出数为
 
@@ -575,7 +571,7 @@
 
 也就是说，为新增输出付出的平均时间，必须低于原来的平均每 token 耗时。由于接受概率需要逐项相乘，越靠后的草稿 token 被保留的概率越低，增加它们就越难获得足够的时间收益。
 
-预设执行形状也会影响新增时间 $\Delta T$。如果本次验证四个或五个 token，都把各 token 的特征向量补齐为八行矩阵，使用预先准备的八行计算图，增加一个位置所需的额外计算较少；若待验证的 token 从八个增至九个，需要补齐为十六行矩阵并改用相应计算图，计算量便会突然增加。补入的行只用于凑齐执行形状，不代表额外的候选 token。并发请求数也影响推测解码的收益：低负载时，多行验证可以利用闲置算力；高负载时，普通批处理已经充分共享权重，草稿生成和验证还要与其他请求争用设备和内存。
+预设执行形状也会影响新增时间 $\Delta T$。如果本次验证四个或五个 token，都把各 token 的特征向量补齐为八行矩阵，使用预先准备的八行计算图，增加一个位置所需的额外计算较少；若待验证的 token 从八个增至九个，需要补齐为十六行矩阵并改用相应计算图，计算量便会突然增加。并发请求数也影响推测解码的收益：低负载时，多行验证可以利用闲置算力；高负载时，普通批处理已经充分共享权重，草稿生成和验证还要与其他请求争用设备和内存。
 
 K7 和 K15 分别表示每轮生成 7 个和 15 个草稿 token 的配置。Qwen3-8B／DFlash 的短提取任务在并发 1 时，普通 decode、K7、K15 的完整耗时中位数约 90.0、30.6、29.4 ms。普通执行改为 K7 节省约 59 ms，从 K7 改为 K15 只再节省约 1.2 ms。逐轮记录中还出现两种块长都只接受一个草稿 token 的轮次：K7 丢弃六个草稿 token，K15 丢弃十四个，最终输出长度相同。更大的验证块增加了工作，而两种配置在该轮最终输出的 token 数相同。[^spec-measure]
 
@@ -601,7 +597,7 @@
 
 **准入控制**决定是否开始处理新请求。接收请求之前，先检查可用的 KV 空间和距离截止时间还剩多久。对第 8.1 节的短请求，0.4 秒首输出加 2.55 秒生成已用掉 2.95 秒；额外排队 0.1 秒就会迟到。提前识别这种请求，可以拒绝、转交其他实例，或使用更快的执行配置。取消则在请求失去使用价值时停止未来迭代，在设备操作完成后释放空间。
 
-这些优化会相互影响。KV 压缩让实例能同时接收更多请求，较大的批次又让普通 decode 更有效地共享权重，从而改变推测解码的相对收益。因此，组合使用多种优化时，应重新计算组合后的执行时间和内存占用，单项加速比不能直接相乘。
+这些优化会相互影响。KV 压缩让实例能同时接收更多请求，较大的批次又让普通 decode 更有效地共享权重，从而改变推测解码的相对收益。下面将这些变化放进同一个请求，计算组合后的执行时间和内存占用。
 
 ### 8.6.2 质量与 SLO 约束下的有效吞吐
 
@@ -625,13 +621,13 @@
 
 到达率为 1/s 时，两种在线策略的有效吞吐均约 0.90 req/s。每批工作较少，逐个接纳也能及时处理大部分正确请求。到达率升至 4/s，逐个接纳的完成吞吐约 1.22 req/s，有效吞吐却降至 0.31 req/s；队列增长使许多正确输出越过期限。连续批处理把完成吞吐提高到约 2.33 req/s，有效吞吐达到约 1.02 req/s，更多工作在期限内结束。
 
-再把连续模式的到达率推至 16/s，完成吞吐只增至约 2.38 req/s，有效吞吐反而降到约 0.60 req/s，减少约 41%。模型的完成吞吐率已经接近不变，新增到达主要增加等待。对这组窗口，4/s 比 16/s 按时返回了更多正确答案。服务选择因而要寻找有效吞吐开始下降的位置，再通过限制新接收的请求数，避免排队时间过长。
+再把连续模式的到达率推至 16/s，完成吞吐只增至约 2.38 req/s，有效吞吐反而降到约 0.60 req/s，减少约 41%。模型的完成吞吐率已基本不再增长，更多请求到达主要增加了排队时间。对这组窗口，4/s 比 16/s 按时返回了更多正确答案。服务选择因而要寻找有效吞吐开始下降的位置，再通过限制新接收的请求数，避免排队时间过长。
 
 逐个接收请求时，其他请求虽然没有进入引擎，却仍在应用队列中等待。因此，应从请求原定的到达时刻开始计时，把引擎外的等待也算进去。由此可见，设备每秒输出多少 token、单条请求等待多久，以及每秒有多少请求正确且按期完成，需要分别计算。
 
 ### 8.6.3 配置、设备与完整任务成本
 
-**综合算例：为 16 条长请求选择配置。** 有效吞吐给出了服务的评价标准，容量和时间分析则帮助我们在运行之前排除不合适的配置。回到章首的实例。设备内存为 32 GiB，扣除权重和基本工作区后，留给 KV 及辅助缓冲区的空间为 12 GiB。16 条长请求同时到达，每条输入 8192 个 token、输出 256 个 token，前 6144 个输入 token 相同，完成时限为 3 秒。下面比较四种方案，假设它们都能给出正确答案。共享方案在请求到达前已经缓存了公共前缀。推测方案额外需要 2 GiB 空间，用于草稿权重、草稿 KV 和验证临时数据，并在请求到达后花 0.2 秒准备。表中给出整批 16 条请求完成各阶段所需的教学时间参数。
+**综合算例：在显存与时限约束下选择前缀共享、压缩和推测配置** 有效吞吐给出了服务的评价标准，容量和时间分析则帮助我们在运行之前排除不合适的配置。采用章首给定的推理实例：设备内存为 32 GiB，扣除权重和基本工作区后，留给 KV 及辅助缓冲区的空间为 12 GiB。16 条长请求同时到达，每条输入 8192 个 token、输出 256 个 token，前 6144 个输入 token 相同，完成时限为 3 秒。下面比较四种方案，假设它们都能给出正确答案。共享方案在请求到达前已经缓存了公共前缀。推测方案额外需要 2 GiB 空间，用于草稿权重、草稿 KV 和验证临时数据，并在请求到达后花 0.2 秒准备。表中给出整批 16 条请求完成各阶段所需的教学时间参数。
 
 | 方案 | 状态组织 | 首 token 返回前的时间 | 首 token 返回后的执行 |
 |---|---|---:|---|
@@ -658,7 +654,7 @@
 
 最后比较成本。假设 B 每运行一秒收费 1 个单位，D 收费 1.2 个单位，从整批请求到达到全部完成计费。B 每个合格结果的成本为 $2.95/16\approx0.184$，D 为 $1.875\times1.2/16\approx0.141$。虽然 D 每秒收费更高，但完成得更快，每个合格结果的成本反而低约 24%，因此选择 D。
 
-**改变条件，重新选择。** 如果 KV 和辅助缓冲区的可用内存从 12 GiB 减至 6 GiB，B 所需的约 5.91 GiB 仍能放入，D 所需的约 7.91 GiB 则放不下，应改选 B。如果内存不变，只把输出缩短到 16 个 token，B 需要 $0.4+15\times0.010=0.55$ 秒，D 需要 $0.2+0.4+5\times0.015=0.675$ 秒。此时，推测解码省下的生成时间还不足以抵消准备时间，B 又变得更快、更便宜。
+**内存减少或输出缩短时，推测解码方案为何不再合算？** 如果 KV 和辅助缓冲区的可用内存从 12 GiB 减至 6 GiB，B 所需的约 5.91 GiB 仍能放入，D 所需的约 7.91 GiB 则放不下，应改选 B。如果内存不变，只把输出缩短到 16 个 token，B 需要 $0.4+15\times0.010=0.55$ 秒，D 需要 $0.2+0.4+5\times0.015=0.675$ 秒。此时，推测解码省下的生成时间还不足以抵消准备时间，B 又变得更快、更便宜。
 
 计算完整 Agent 任务的耗时，还要加上工具执行和外部等待。假设一次任务耗时 100 秒，其中 decode 占 50 秒；将 decode 加速四倍后，总耗时变为 $50+50/4=62.5$ 秒，整体加速 1.6 倍。如果 decode 原本只占 20 秒，总耗时则变为 $80+20/4=85$ 秒，整体只加速约 1.18 倍。一般地，设 decode 占原总时间的比例为 $f$，该阶段加速 $s$ 倍，其他阶段耗时不变，则由 Amdahl 定律得到
 
@@ -672,11 +668,9 @@
 
 图 8-36 中，decode 占比越低，曲线越早变平：即使生成阶段继续加速，工具调用和其他等待仍占据原来的时间。比较设备成本和能耗时，也要覆盖这些阶段，并按最终得到的合格请求数计算。设观察期间的总成本为 $C_{\mathrm{obs}}$，总能耗为 $E_{\mathrm{obs}}$，合格结果数为 $N_g$，则每个合格结果的平均成本和能耗分别为 $C_{\mathrm{obs}}/N_g$ 和 $E_{\mathrm{obs}}/N_g$。假设两个方案各消耗 10 kJ，分别完成 20 条和 16 条请求，其中合格的有 10 条和 14 条。前者每完成一条请求只用 500 J，但每个合格结果实际需要 1000 J，高于后者的约 714 J。即使答案错误或返回太晚，已经消耗的能量也必须计入总成本。
 
-> **练习 8-8 · 计算：局部与完整任务。** 一个 100 秒任务由 prefill 20 秒、decode 50 秒、工具及等待 30 秒组成。分别将三个部分单独加速两倍，求完整时间。再将 decode 加速四倍、增加 8 秒草稿准备，求净加速比，并计算准备时间达到多少时这项改动不再节省时间。
-
-> **练习 8-9 · 核心 · 综合设计。** 沿用综合算例的容量、时间和成本，以 B 为基础构造方案 E：共享 BF16 前缀、额外 1 GiB 状态、0.1 秒准备，首输出前另需 0.4 秒，每请求后续 51 轮、每轮输出 5 个 token，整批每轮 25 ms，实例每运行一秒的成本 1.1 单位。计算 E 的内存占用、总耗时和每个合格结果的成本，与 B、D 比较；再把输出长度改为 16 个 token，重新选择。开放实验部分使用相同题集和到达轨迹运行两种配置，记录全部请求的到达、首输出、结束、正确性与状态峰值，按本节公式计算有效吞吐。
-
-固定权重实验也会改变推测解码的比较。若目标模型普通解码已经很快，原本合算的草稿生成和验证轮次可能不再节约时间。第二轮应重新代入普通解码时间、每轮草稿与验证时间，以及有效接受数量，并与新的 batch 竞争关系一起比较。量化、批处理、推测解码各自的旧加速比不能相乘：它们可能在重复消除同一段权重读取时间。
+> **练习 8-8 · 计算：阶段加速与草稿准备如何改变任务总时间** 一个 100 秒任务由 prefill 20 秒、decode 50 秒、工具及等待 30 秒组成。分别将三个部分单独加速两倍，求完整时间。再将 decode 加速四倍、增加 8 秒草稿准备，求净加速比，并计算准备时间达到多少时这项改动不再节省时间。
+
+> **练习 8-9 · 核心 · 综合设计：在容量与时限内降低合格请求成本** 沿用综合算例的容量、时间和成本，以 B 为基础构造方案 E：共享 BF16 前缀、额外 1 GiB 状态、0.1 秒准备，首输出前另需 0.4 秒，每请求后续 51 轮、每轮输出 5 个 token，整批每轮 25 ms，实例每运行一秒的成本 1.1 单位。计算 E 的内存占用、总耗时和每个合格结果的成本，与 B、D 比较；再把输出长度改为 16 个 token，重新选择。开放实验部分使用相同题集和到达轨迹运行两种配置，记录全部请求的到达、首输出、结束、正确性与状态峰值，按本节公式计算有效吞吐。
 
 ## 历史与延伸阅读
 
@@ -746,7 +740,7 @@
 
 ## 本章小结
 
-服务配置依赖底层约束：权重供给变快后，应重新比较 batch 与推测解码；上下文的追加、改写和总结则会改变缓存复用与未来状态开销。应用编排和服务优化需要在同一任务质量目标下比较。
+服务配置取决于底层资源的性能。权重读取加快后，需要重新比较不同 batch 和推测解码方案；上下文的追加、改写和总结则会改变缓存复用、后续状态存储与读取的开销。比较上下文组织方式与服务优化方案时，应采用相同的任务质量要求。
 
 本章从单条请求的执行过程出发，逐步分析了内存容量、计算安排和服务效果之间的关系。不同请求共用权重，KV 则随各自的上下文增长。合批能减少每个输出 token 分摊的权重读取，但每条请求仍要读取自己的上下文。连续批处理及时使用短请求释放的槽位，分块 prefill 则减少长输入对已有请求生成过程的干扰。
 
@@ -754,6 +748,6 @@
 
 章末的综合算例先检查内存是否足够，再判断能否按期完成，最后比较每个合格结果的成本。前缀共享使原先放不下的 16 条长请求能够同时执行，推测解码进一步缩短了生成时间。当可用内存减少或输出长度缩短时，普通共享方案又更合适。选择改变的原因，都能从前面推导的数据量、执行时间和输出数量中找到。
 
-完成本章的配置比较后，应记录三类结果：在给定设备、精度和请求长度下能保存多少并发请求，prefill 与 decode 分别以多快的速度处理工作，以及请求延迟怎样随批量和到达率变化。这些结果已经包含调度、缓存和内核效率的影响。
-
-下一章以这些结果为基础，决定各类设备承担什么工作、需要多少执行单元，以及 KV 怎样传输和共享。多张设备既可以共同执行一个实例，也可以组成多个完整副本或分别调度的阶段服务池。批处理决定已有资源怎样处理请求，部署方式决定这些资源如何分工，两者共同影响完整服务的性能。
+完成本章的配置比较后，应记录三类结果：在给定设备、精度和请求长度下能同时保存多少条请求的状态，prefill 与 decode 分别以多快的速度处理工作，以及请求延迟怎样随批量和到达率变化。这些结果已经包含调度、缓存和内核效率的影响。
+
+下一章以这些结果为基础，决定各类设备承担什么工作、需要多少执行单元，以及 KV 怎样传输和共享。多个设备既可以共同执行一个实例，也可以组成多个完整副本或分别调度的阶段服务池。批处理决定已有资源怎样处理请求，部署方式决定这些资源如何分工，两者共同影响完整服务的性能。
--- before/manuscripts/09-分布式推理.md
+++ manuscripts/09-分布式推理.md
@@ -6,7 +6,7 @@
 
 增加完整副本，可以让不同资源组分别处理请求。将 prefill 与 decode 分开，可以为两个阶段分别配置设备；将 attention 与专家计算分开，可以利用不同设备的计算和存储能力；共享 KV 则让请求使用其他执行位置留下的状态。这些组织方式可能发生在一个推理实例内部，也可能连接多个独立调度的服务池。下面直接分析执行位置、调度关系和数据流向。
 
-本章从一个具体设计问题出发：四个擅长 prefill 的 A 类执行单元（worker）和四个擅长 decode 的 B 类 worker，怎样承接每秒四个请求？每个请求输入 8192 个位置、输出 129 个 token，模型采用 Qwen3-8B，KV 使用每元素 2 字节的 BF16 浮点格式。一个 worker 可以是一张卡，也可以是协同执行的一组卡。八个 worker 都有足以运行完整模型的内存容量。我们将依次决定计算怎样分工、上下文在哪里交接、缓存何时值得保存，以及启动后的积压多久能够消退。第 9.7 节回到这组资源，确定部署方案。
+本章从一个具体设计问题出发：四个擅长 prefill 的 A 类执行单元（worker）和四个擅长 decode 的 B 类 worker，怎样处理每秒到达的四个请求？每个请求输入 8192 个位置、输出 129 个 token，模型采用 Qwen3-8B，KV 使用每元素 2 字节的 BF16 浮点格式。一个 worker 可以是一张卡，也可以是协同执行的一组卡。八个 worker 都有足以运行完整模型的内存容量。我们将依次决定计算怎样分工、上下文在哪里交接、缓存何时值得保存，以及启动后的积压多久能够消退。第 9.7 节将综合这些结果，为这组资源确定部署方案。
 
 贯穿第 9.3、9.4 节的另一个算例采用 Qwen3-235B-A22B，研究专家权重放在哪里。同样是 512 次专家分派，分派结果不同时，可能需要读取许多专家的权重，也可能反复使用少数热点专家的权重。我们将据此判断专家计算应放在 CPU 还是 GPU 上，再考察热点集中到一张卡后，为什么需要专家副本。两个算例采用相同的分析方法：先确定每个请求需要的工作和状态，再计算设备处理这些工作、存储系统提供所需数据的速度。
 
@@ -34,7 +34,7 @@
 
 *图 9-4：注意力与 FFN／专家分别执行，隐状态作为激活在两侧往返。每层都需要这次交接。*
 
-建立比较时，先使用第八章得到的运行配置和处理速度，再改变设备之间的分工。这里将 P、D 由同一组执行资源处理的方式称为共置。考虑一个已经调优的共置实例。请求的输入长度为 $L$，总输出长度为 $G$，prefill 计算最后一个输入位置的 **logits**，即转换成 token 概率之前的词表分数，并由此采样首个输出。因此，在本章采用的生成约定下，后续 decode 调用为 $G-1$ 次。最后一个返回 token 尚未送入模型，因此输出数比后续 decode 调用数多一。
+比较这些部署方式时，先使用第八章得到的运行配置和处理速度，再改变设备之间的分工。这里将 P、D 由同一组执行资源处理的方式称为共置。考虑一个已经调优的共置实例。请求的输入长度为 $L$，总输出长度为 $G$，prefill 计算最后一个输入位置的 **logits**，即转换成 token 概率之前的词表分数，并由此采样首个输出。因此，在本章采用的生成约定下，后续 decode 调用为 $G-1$ 次。最后一个返回 token 尚未送入模型，因此输出数比后续 decode 调用数多一。
 
 对给定请求分布，记每个请求在资源 $r$ 上消耗的平均服务时间为 $d_r$，同类资源共有 $n_r$ 个可独立工作的单元。到达率与资源占用时间需要满足
 
@@ -52,7 +52,7 @@
 
 对开篇的 A 类 worker，每个请求的 P、D 阶段分别占用 0.5 秒和 2 秒；B 类恰好相反，分别为 2 和 0.5。每个完整副本都要承担两个阶段，所以两类副本均需 2.5 资源秒/请求，每秒完成 $1/2.5=0.4$ 个请求。八个合计 3.2 请求/s，低于每秒四个请求的到达率，每秒积压 0.8 个请求。问题由此变得明确：同样八个 worker，能否通过重新分工消除这 0.8 请求/s 的缺口？
 
-这里的副本可以由多张卡共同构成。例如，Qwen3-235B-A22B 的一种 TP2×EP4 放置（张量并行 TP 将专家矩阵二分，专家并行 EP 将专家分成四组）用八个 rank 处理同一批请求：每两个 rank 分担专家矩阵，四个 EP 组分别持有不同专家。attention 和 KV 在四个 EP 组间复制，因此一条请求的 8K 上下文在整个组内占 5.875 GiB，是单份逻辑 KV 的四倍。这八个 rank 共同组成一个推理实例；要增加独立请求副本，还需另外安排完整的权重与状态。[^ownership]
+这里的副本可以由多张卡共同构成。例如，Qwen3-235B-A22B 的一种 TP2×EP4 放置（张量并行 TP 将专家矩阵二分，专家并行 EP 将专家分成四组）用八个 rank 处理同一批请求：每两个 rank 分担专家矩阵，四个 EP 组分别持有不同专家。attention 和 KV 在四个 EP 组间复制，因此一条请求的 8K 上下文在整个组内占 5.875 GiB，是单份逻辑 KV 的四倍。这八个 rank 共同组成一个推理实例；要增加独立处理请求的副本，还需另外安排完整的权重与状态。[^ownership]
 
 ### 9.1.2 计算分工与状态共享
 
@@ -67,7 +67,7 @@
 
 部署选择从当前负载的资源需求开始。如果两个阶段的资源需求相近，完整副本已经能够均衡服务，分离可能只增加交接。如果许多请求共享长前缀，跨实例缓存可能比进一步拆分计算更有价值。分别计算资源占用和交接时间，就能判断当前应增加副本、分离阶段还是共享状态。
 
-### 9.1.3 同一请求的执行路径与状态图
+### 9.1.3 同一请求的执行路径与状态保存时间
 
 图 9-5 把一次多轮请求的计算阶段与状态寿命放在同一时间线上。带视觉输入时，**编码阶段 E** 将图像转换为模型使用的特征；保存下来供复用的编码结果称为**编码缓存 EC**（encoder cache）。P 处理这些编码结果与文本，D 逐步生成输出；工具返回后，新一轮 P 处理新增信息。权重通常跨请求保留，激活主要跨相邻算子传递，KV 随已处理的位置增长。工具等待期间计算可以暂停，但上下文状态仍可能占据内存。
 
@@ -75,21 +75,21 @@
 
 *图 9-5：计算暂停期间，状态仍需保留。示意同一请求从 prefill、decode 到工具等待和下一轮的状态寿命；横轴按阶段排列，不表示相等时长，EC 仅在有视觉输入时出现。*
 
-以本章的 Qwen3-8B 为例，一份包含 8192 个位置的 BF16 上下文 KV占 1.125 GiB。工具执行 10 秒时，GPU 可以处理其他请求，但这份上下文 KV 仍需占用 1.125 GiB 内存，容量与保留时间的乘积为 11.25 GiB·s；十个这样的会话同时等待，就占 11.25 GiB。计算暂停后，状态仍需保留，这正是共享缓存和换出策略要解决的问题。
+以本章的 Qwen3-8B 为例，一份包含 8192 个位置的 BF16 上下文 KV 占 1.125 GiB。工具执行 10 秒时，GPU 可以处理其他请求，但这份上下文 KV 仍需占用 1.125 GiB 内存，容量与保留时间的乘积为 11.25 GiB·s；十个这样的会话同时等待，就占 11.25 GiB。计算暂停后，状态仍需保留，这正是共享缓存和换出策略要解决的问题。
 
 这条请求若随后返回 129 个 token，P 先产生第一个输出，128 次 decode 再处理前 128 个生成位置。此时连续 KV 覆盖 $8192+128=8320$ 个位置，最后一个返回 token 尚未送回模型。下一轮在这 8320 个位置之后继续，先处理这个尾 token 和新增输入。因此，已返回的输出序列比 KV 所覆盖的序列多一个 token；恢复生成时，需要从这个位置继续计算。
 
 ## 9.2 Prefill–Decode 分离与资源配比
 
-### 9.2.1 Prefill 与 Decode 的干扰及分离动机
+### 9.2.1 Prefill 与 Decode 为什么需要分离
 
 Prefill 可以在一次调用中处理许多新位置，较大的矩阵往往有较高的权重复用。在 decode 阶段，每个请求每步通常只处理一个新位置，需要反复读取权重与上下文状态。两者混合执行时，一个较长的 prefill 可能推迟已有请求的下一个输出；过度优先 decode 又会延长新请求的等待。
 
-第 8.2 节已经用分块 prefill 缩短了长输入对 decode 的阻塞。本节保持相同模型和请求，进一步改变执行位置。PD 分离进一步把两个阶段放入不同的服务池，使它们拥有独立的队列、批处理和资源配比。DistServe 与 Splitwise 是研究 P、D 分离部署的推理系统，展示了这一设计方向，但资源池的分离并不要求设备异构。相同设备也可能因调度隔离受益；异构设备的收益来自两个阶段在不同设备上的资源需求差异。[^pd]
+第 8.2 节已经用分块 prefill 缩短了长输入对 decode 的阻塞。本节保持相同模型和请求，进一步改变执行位置。PD 分离进一步把两个阶段放入不同的服务池，使它们拥有独立的队列、批处理和资源配比。DistServe 与 Splitwise 是研究 P、D 分离部署的推理系统。同构设备可通过独立队列减少阶段间的调度干扰，异构设备还可以利用各自更擅长的阶段。[^pd]
 
 PD 将 prefill 和 decode 分配给两个服务池，但两个池都要执行完整的前向计算。Qwen3-8B 的 P 处理 8192 个输入位置，D 随后调用 128 次；每次都经过 attention 和 FFN。于是 P、D 都需要相应权重，而上下文 KV 还要从 P 交给 D。独立批处理带来的收益，首先要补偿这次新增交接。
 
-完整模型请求向应用提供一次调用，内部执行则可以按阶段重新分工。模型的因果关系给出了切分位置，KV 给出了两阶段之间需要交接的状态，负载比例决定各池需要多少资源。利用这些信息，服务可以在统一调用接口之下，为输入处理和逐步生成分别选择批次与设备。接下来用传输量和处理能力计算这种分工的收益。
+应用通过一次调用请求完整的模型推理，服务内部则可以按阶段安排不同资源。模型的数据依赖确定了阶段之间的切分位置，KV 给出了两阶段之间需要交接的状态，负载比例决定各池需要多少资源。利用这些信息，服务可以在统一调用接口之下，为输入处理和逐步生成分别选择批次与设备。接下来用传输量和处理能力计算这种分工的收益。
 
 ### 9.2.2 KV 传输与两端内存占用
 
@@ -105,7 +105,7 @@
 V_{KV}=8192\times144\ \mathrm{KiB}=1.125\ \mathrm{GiB}.
 $$
 
-**例 9.1　一次完整 KV 交接。** 假设有效单向载荷带宽为 25 GB/s，采用整份状态直接交接。纯载荷传输时间为
+**例 9.1　预填充与解码之间的 KV 交接需要多久？** 假设有效单向载荷带宽为 25 GB/s，采用整份状态直接交接。纯载荷传输时间为
 
 $$
 T_{\mathrm{payload}}=\frac{1.125\times2^{30}}{25\times10^9}
@@ -140,7 +140,7 @@
 
 沿用第 8.6 节的方法，在确定批处理、精度和缓存设置后，用完成的工作量除以资源占用时间，得到各阶段的实际处理速度。以一个假设的计时实验为例：若 A 连续处理 20 份互不复用的 8192-token 输入，总共占用一个 worker 10 秒，则 $r_P=20\times8192/10=16384$ 新位置/s，每请求消耗 $10/20=0.5$ 资源秒。若同一批请求的 2560 次 decode 调用共占用 40 秒，则 $r_D=64$ 调用/s，每请求消耗 2 资源秒。批内请求共享一次调用时，应累计所有请求完成的解码步，但计算资源占用时间时，同一次批执行只计一次。这样计算的资源占用时间可以相加，输入 token/s 与输出 token/s 则先要换算成同一请求的需求。
 
-**例 9.2　固定八个 worker 的配比。** 使用 8192 个输入位置、129 个输出 token。两类 worker 各四个，假设它们采用已选定的运行配置，各阶段的处理速度如下表所示；后续改变请求长度时，保持这组有效能力不变。
+**例 9.2　预填充与解码的资源配比如何决定吞吐上限？** 使用 8192 个输入位置、129 个输出 token。两类 worker 各四个，假设它们采用已选定的运行配置，各阶段的处理速度如下表所示；后续改变请求长度时，保持这组有效能力不变。
 
 | worker 类型 | P 能力，新位置/s | D 能力，调用/s | 每请求 P 资源秒 | 每请求 D 资源秒 |
 | --- | ---: | ---: | ---: | ---: |
@@ -167,10 +167,6 @@
 
 图 9-10 还给出了偏离最优配比后的吞吐率变化。把一个 A 从 P 调到 D，P 从 8 降为 6 请求/s，D 从 8 增为 8.5 请求/s，结果受 P 限制而降至 6。把一个 B 从 D 调到 P，P 增为 8.5，D 却降为 6，同样变慢。最优配比使两池都达到八请求/s。改变配比后，一个池即使有富余能力，也无法替另一个池完成工作。
 
-对第 3 章的 V4.1 会话，P 与 D 处理能力必须分别测量。CED 让大部分输入跳过解码器主体，但生成仍执行完整主干；8B／16B 激活参数标签描述不同阶段，不能据此断言 P 快两倍或 P/D 实例数应为 1:2。新增输入长度、前缀命中与 SWA 恢复概率改变 P 的工作分布，输出长度决定 D 的持续占用。[^v41-case]
-
-第一轮保持既有实例配比，找出请求积压在哪一段；第二轮才按新的每实例服务率调整配比，并计入 KV 交接。论文的 EPD 分离还包含视觉编码阶段：它可独立扩展，但本书这条文本会话不额外计入视觉编码工作。执行边界由模型定义，资源数量仍由实际负载和时间约束决定。
-
 ### 9.2.4 不同请求与负载下的收益边界
 
 现在只改变前缀命中。P 已经拥有 6144 个位置，只需处理剩余 2048 个位置，即原来的四分之一。一个 A 的 P 能力于是从 2 增至 8 请求/s；若仍给 P 四个 A，它们合计能处理 32 请求/s，D 却仍只能处理 8 请求/s。
@@ -209,7 +205,7 @@
 
 对 Qwen3-235B-A22B，一个专家包含三个矩阵，参数数量为 $3\times4096\times1536=18874368$。BF16 权重为 36 MiB。
 
-**例 9.3　复用使执行位置发生变化。** 设有效 CPU 算力为 2 TFLOP/s、DRAM 带宽为 200 GB/s，GPU 算力为 100 TFLOP/s、HBM 带宽为 1000 GB/s，交接带宽为 25 GB/s，每次启动为 5 μs，BF16 激活宽度为 4096。八个专家依次执行，总时间为各专家耗时之和，权重每批读取一次。每个专家只收到一个 token 的特征向量时，CPU 可以直接读取本地主存中的权重，避免了较慢链路上的 36 MiB 搬运；随着每个专家的输入行数增加，一次权重传输的开销由更多行计算分摊，而 CPU 更早进入计算受限区。
+**例 9.3　专家权重复用如何改变 CPU 与 GPU 的执行选择？** 设有效 CPU 算力为 2 TFLOP/s、DRAM 带宽为 200 GB/s，GPU 算力为 100 TFLOP/s、HBM 带宽为 1000 GB/s，交接带宽为 25 GB/s，每次启动为 5 μs，BF16 激活宽度为 4096。八个专家依次执行，总时间为各专家耗时之和，权重每批读取一次。每个专家只收到一个 token 的特征向量时，CPU 可以直接读取本地主存中的权重，避免了较慢链路上的 36 MiB 搬运；随着每个专家的输入行数增加，一次权重传输的开销由更多行计算分摊，而 CPU 更早进入计算受限区。
 
 先看八个热点专家各处理一个 token：权重合计 288 MiB。在 200 GB/s 主存上读取约 1.5 ms，加激活交接后 CPU 路径约 1.6 ms；搬到 GPU 的路径仅传权重就约 12.1 ms，连同启动与 GPU 计算约 12.4 ms。低复用时，避免大块权重搬运比提高矩阵算力更有价值。
 
@@ -235,7 +231,7 @@
 
 *图 9-14：专家复用改变执行位置的选择。八个专家各有 36 MiB BF16 权重，有效 CPU／GPU 算力为 2／100 TFLOP/s，DRAM／HBM 带宽为 200／1000 GB/s，交接为 25 GB/s、每次启动 5 μs。曲线按例 9.3 计算单层专家路径，未含 NUMA 和格式转换。*
 
-### 9.3.3 容量、并发与专家复用的服务边界
+### 9.3.3 容量、并发与专家复用对服务能力的影响
 
 能否装下权重和能否及时完成请求，是两个不同的问题。Qwen3-235B-A22B 每层 128 个 BF16 专家共占 4.5 GiB。若每层固定 32 个专家驻留 GPU，单层为 1.125 GiB，94 层合计约 106 GiB。即使一张卡能腾出 80 GiB，也装不下这组常驻专家；还需在多卡间切分，或减少每层驻留数量。把每层驻留数减到 16，可将这部分权重减到约 53 GiB，同时也把更多专家执行交给主存一侧。容量选择由此直接改变请求的执行路径。
 
@@ -271,19 +267,19 @@
 
 例如，attention 与专家阶段每微批次分别需要 2 ms、3 ms。四个微批次串行执行共需 20 ms；理想双阶段流水只需 $2+3+3\times3=14$ ms，省下 6 ms。若拆小微批次后，每阶段的实际服务时间变长，或无法与计算重叠的往返通信超过 6 ms，这项收益就会消失。因此，通信和计算效率下降引入的额外耗时合计必须小于 6 ms，流水执行才会更快。[^af]
 
-**例 9.4　字节与交接频次。** 在 Qwen3-8B 的示例部署中，把 36 层稠密前馈网络 放在另一侧，每层发送一个完整 BF16 隐状态向量，并返回同样大小的结果。单向传输一次的数据量为 $4096\times2=8192$ bytes；一步 decode 合计 72 次、576 KiB。25 GB/s 下纯载荷约 23.6 μs，72 次 5 μs 启动合计 360 μs，串行交接约 0.384 ms。
+**例 9.4　逐层注意力与前馈分离如何累积通信开销？** 在 Qwen3-8B 的示例部署中，把 36 层稠密前馈网络放在另一侧，每层发送一个完整 BF16 隐状态向量，并返回同样大小的结果。单向传输一次的数据量为 $4096\times2=8192$ bytes；一步 decode 合计 72 次、576 KiB。25 GB/s 下纯载荷约 23.6 μs，72 次 5 μs 启动合计 360 μs，串行交接约 0.384 ms。
 
 这段时间短于例 9.1 中的一次 PD 交接，但两者对应不同工作范围。若生成 129 个输出，后续 128 次 decode 的 AF 串行交接累计约 49.1 ms；8192 个输入位置的 prefill 若也按整批 dense AF 交接，激活载荷为 4.5 GiB，约需 193 ms 纯传输，再加启动。一次 PD 搬运与逐层 AF 往返的区别在此显现：前者随上下文长度增长，后者还随生成步数反复发生。
 
 ![消息数量与启动开销](ch09/figure-9-8-handoff.svg)
 
-*图 9-17：相同总载荷下，消息数放大启动开销。两条曲线总载荷均为 1.125 GiB、有效带宽均为 25 GB/s，分别串行发送 1 次和 72 次；差值为 $71\alpha$，纵轴从48.2 ms起以显示差异。这样保持总字节数不变，只比较消息数量的影响，实际一步 AF 仅传 576 KiB。*
+*图 9-17：相同总载荷下，消息数放大启动开销。两条曲线总载荷均为 1.125 GiB、有效带宽均为 25 GB/s，分别串行发送 1 次和 72 次；差值为 $71\alpha$，纵轴从 48.2 ms 起以显示差异。这样保持总字节数不变，只比较消息数量的影响，实际一步 AF 仅传 576 KiB。*
 
 保持总字节数相同，更容易看清传输次数的影响：把同一份 1.125 GiB 载荷分成 72 次串行交接，载荷时间不变，启动开销比一次交接多 $71\alpha$，在本例中约为 0.36 ms。PD 往往先受大块 KV 搬运影响，AF 则更容易对小消息延迟和依赖敏感。下一节考察 MoE 中的激活传输：一个输入可能发往多个执行位置，完成时还要等待它所选择的专家汇合。
 
 ## 9.4 专家放置、复制与负载均衡
 
-### 9.4.1 路由倾斜与最忙设备
+### 9.4.1 路由不均衡与设备负载
 
 上一节中，集中复用减少了权重读取。但这些热点专家如果都放在同一张卡上，其他卡就帮不上忙。本节继续跟踪同样的 512 次分派，考察各张卡何时完成计算。即使全系统的总计算量不变，最忙的设备也可能使整层推迟完成。对设备 $r$，记矩阵计算量为 $F_r$、需要读取的权重字节数为 $W_r$，有效算力和带宽分别为 $C_r$、$B_r$。该设备计算与权重读取所需时间至少为 $\max(F_r/C_r,W_r/B_r)$；合并各设备的结果时，必须等耗时最长的设备完成。
 
@@ -309,24 +305,24 @@
 
 专家副本占用显存，也会挤占 KV 和缓冲。每卡每层增加一个 36 MiB 专家，94 层共需 3384 MiB，约 3.30 GiB；若只为八个最拥挤的层各增加一个副本，则只需 288 MiB。两者相差近 12 倍。CRAFT 是研究专家副本与显存分配的服务系统。它要解决的各层显存分配问题由此产生：应把空间花在最能缩短等待的层，而不是机械地给每层同样多的副本。[^moe]
 
-**例 9.5　副本需要多久才能回本？** 在八个 rank 间增加七份专家副本，共 252 MiB；七张接收卡各增加 36 MiB，均放得进 64 MiB 的额外可用空间。假设这次复制准备需要约 10.6 ms，此后每批从约 0.92 ms 缩短到 0.52 ms，节省约 0.40 ms。按未经舍入的执行时间，节省时间超过复制时间所需的最少批数满足
+**例 9.5　热点专家副本需要多少批次才能收回复制成本？** 在八个 rank 间增加七份专家副本，共 252 MiB；七张接收卡各增加 36 MiB，均放得进 64 MiB 的额外可用空间。假设这次复制准备需要约 10.6 ms，此后每批从约 0.92 ms 缩短到 0.52 ms，节省约 0.40 ms。按未经舍入的执行时间，节省时间超过复制时间所需的最少批数满足
 
 $$
 N\Delta t>T_{copy},\qquad
 N_{min}=\left\lfloor\frac{T_{copy}}{\Delta t}\right\rfloor+1=27.
 $$
 
-若热点持续 16 个批次，节省约 6.4 ms，还不足以偿还 10.6 ms 的准备；持续 64 批时，节省约 25.8 ms，扣除准备后净省约 15.2 ms。图 9-20 将一次投入与逐批收益画在同一条线上。若接收卡只有 32 MiB 空间，连一份 36 MiB 专家也放不下，此时无论热点持续多久，都应先改变放置或腾出容量。[^replica]
+若热点持续 16 个批次，节省约 6.4 ms，还不足以抵消 10.6 ms 的准备时间；持续 64 批时，节省约 25.8 ms，扣除准备后净省约 15.2 ms。图 9-20 将一次投入与逐批收益画在同一条线上。若接收卡只有 32 MiB 空间，连一份 36 MiB 专家也放不下，此时无论热点持续多久，都应先改变放置或腾出容量。[^replica]
 
 ![专家副本的准备与回本](ch09/figure-9-10-experts.svg)
 
 *图 9-20：热点持续多久，专家复制才值得。一次准备约 10.6 ms，每批节省约 0.40 ms；曲线使用例 9.5 未舍入时间计算，第 27 批开始净获益。七张接收卡各需额外 36 MiB，假设容量足够且热点不变。*
 
-副本策略需要预测的是热点还会持续多久。若统计窗口结束时热点只剩 16 批，复制将多花约 4.2 ms；若还剩 64 批，则净省约 15.2 ms。每次重新复制都重新支付 10.6 ms，所以频繁追逐热点会反复推迟回本。将容量优先分给持续拥挤的层，才能让缩短的等待累计超过准备成本。
+副本策略需要预测的是热点还会持续多久。若统计窗口结束时热点只剩 16 批，复制将多花约 4.2 ms；若还剩 64 批，则净省约 15.2 ms。每次重新复制都要再花费 10.6 ms，所以频繁追逐热点会反复推迟回本。将容量优先分给持续拥挤的层，才能让缩短的等待累计超过准备成本。
 
 ### 9.4.3 输入分发、计算与结果合并的重叠
 
-分组通用矩阵乘法（Grouped GEMM）把多个专家矩阵组织成一个执行批次，但不保证所有补齐已经消除。例如八个本地专家收到 $[32,16,8,4,2,1,1,0]$ 个 token，合计 64 次 token 到专家的分派，对应 64 个有效输入矩阵行。若每个非空专家补齐到 32 行，需要 224 行；若八个专家都按最大值补齐，需要 256 行。实际执行工作分别为有效行数的 3.5 倍和 4 倍。这解释了为什么有效任务数相同，矩阵时间仍可能不同：硬件执行的是补齐后的矩阵，模型所需的专家计算只对应其中 64 个有效行；补齐的零行没有对应的真实 token。
+分组通用矩阵乘法（Grouped GEMM）把多个专家矩阵组织成一个执行批次，内核按照分块要求补齐各矩阵的行数。例如八个本地专家收到 $[32,16,8,4,2,1,1,0]$ 个 token，合计 64 次 token 到专家的分派，对应 64 个有效输入矩阵行。若每个非空专家补齐到 32 行，需要 224 行；若八个专家都按最大值补齐，需要 256 行。实际执行工作分别为有效行数的 3.5 倍和 4 倍。这解释了为什么有效任务数相同，矩阵时间仍可能不同：硬件执行的是补齐后的矩阵，模型所需的专家计算只对应其中 64 个有效行；补齐的零行没有对应的真实 token。
 
 假设 dispatch、专家计算与 combine 分别需要 0.2、0.6、0.2 ms，完全串行为 1.0 ms。若可均分成两个微批次，各阶段时间减半，理想三阶段流水为 $0.1+0.3+0.1+0.3=0.8$ ms，只省下 0.2 ms。若共享 HBM 使计算阶段每微批次从 0.3 增至 0.4 ms，同一流水就回到 $0.1+0.4+0.1+0.4=1.0$ ms。通信确实重叠了，整批却没有变快。
 
@@ -338,7 +334,7 @@
 
 *图 9-22：每微批次各阶段时间减半，三条轨道使用独立资源。第一微批次计算时可以分派第二微批次，总时间降到 0.8 ms。*
 
-沿图 9-21 的下半图看，微批次 1 计算时，可以分派微批次 2；微批次 1 合并时，微批次 2 已开始计算。首批的分派和末批的合并仍留在总时间内，因而把通信画到计算旁边，并不等于消除了全部通信时间。
+沿图 9-22 的时间线看，第一个微批次计算时，可以分派第二个微批次；第一个微批次合并时，第二个微批次已开始计算。首批分派负责填充流水，末批合并负责收尾，两段共同构成流水之外的串行时间。
 
 专家执行存在两项相互作用的成本：补齐扩大矩阵计算量，并发传输又可能减慢这些工作的执行。上述流水通过重叠节省了 0.2 ms，一旦计算累计多花同样的时间，重叠收益便被完全耗尽。[^expert-exp]
 
@@ -358,9 +354,9 @@
 
 ### 9.5.2 多级存储
 
-第 8.3.4 节比较了本地缓存保留、换出和重算的代价。现在把同一份状态放到远端：除了对象大小，还要考虑经过哪些链路，以及每次生成是否重复读取。共享 KV 的三种典型路径是本地重新 prefill、从远端取回一次并在本地驻留、每一步都从远端读取数据。它们可能使用同样的上下文内容，却有完全不同的频次。
-
-沿用 1.125 GiB 前缀和 25 GB/s 链路，一次载荷取回约 48.3 ms。若 100 次 decode 调用都重新远程读取这份不变前缀，累计占用链路约 4.83 s，增长中的 KV 尚未计入。若这 100 次读取分布在一秒内，仅这份上下文就要求约 121 GB/s，是给定链路能力的 4.8 倍。将读取与计算并行安排，也无法使 25 GB/s 链路在一秒内送完这些字节。
+第 8.3.4 节比较了本地缓存保留、换出和重算的代价。现在把同一份状态放到远端：除了对象大小，还要考虑经过哪些链路，以及每次生成是否重复读取。共享 KV 的三种典型路径是本地重新 prefill、从远端取回一次并在本地驻留、每一步都从远端读取数据。它们可能使用同样的上下文内容，却传输频率却完全不同。
+
+沿用 1.125 GiB 前缀和 25 GB/s 链路，一次载荷取回约 48.3 ms。若 100 次 decode 调用都重新远程读取这份不变前缀，累计占用链路约 4.83 s。若这 100 次读取分布在一秒内，仅这份上下文就要求约 121 GB/s，是给定链路能力的 4.8 倍。将读取与计算并行安排，也无法使 25 GB/s 链路在一秒内送完这些字节。
 
 一次取回并驻留将频次从“每步”降到“每次复用”，代价是占用本地 HBM。设一份状态占 $V$ bytes，保留 $\tau$ 秒，其内存占用量可用 $V\tau$ 个 byte-seconds 表示。保留时间相同时，大对象占用更多容量；大小相同时，等待越久，占用越大。因此可把不活跃会话放到远端，在会话恢复时取回，并在连续 decode 期间保留在本地。这样，数据传输主要发生在会话恢复和暂停时。
 
@@ -398,7 +394,7 @@
 
 如果取回必须等到 GPU 排队结束后才开始，排队时间与取回时间就需要相加；如果逐层流水，则需要更细的执行图。例如 $Q=80$ ms、取回耗时 60 ms、剩余计算 10 ms，并行就绪时共需 90 ms；等 GPU 空闲后再启动取回则需 150 ms。相同计算和字节，通过改变依赖关系便相差 60 ms。
 
-**例 9.6　有缓存的机器还是空闲机器？** 对同一 8K 前缀，A 有 HBM 命中，但需等 250 ms；B 等 20 ms，完整重算需 180 ms；命中后的剩余工作为 10 ms。远端读取固定开销 10 ms，整份先到 host，再通过 25 GB/s 搬到 GPU。
+**例 9.6　缓存命中与排队等待如何共同决定请求路由？** 对同一 8K 前缀，A 有 HBM 命中，但需等 250 ms；B 等 20 ms，完整重算需 180 ms；命中后的剩余工作为 10 ms。远端读取固定开销 10 ms，整份先到 host，再通过 25 GB/s 搬到 GPU。
 
 | 路径 | 首 token 时间，教学计算 |
 | --- | ---: |
@@ -427,7 +423,7 @@
 
 B 要在 200 ms 内返回，扣除命中后 10 ms 计算，状态必须在 190 ms 内就绪。再扣除查找的 10 ms 与 host→GPU 的约 48.3 ms，远端读取只剩约 131.7 ms。用 1.125 GiB 除以这个预算，得到约 9.2 GB/s 的取回与重算耗时相等时的带宽。
 
-同一会话由此可以根据状态与资源的位置重新安排。应用保留连续的上下文，服务选择继续使用本地状态、迁移状态或重新计算；选择依据是这三条路径何时能让后续模型执行开始。第八章根据内容判断可复用的前缀，本节进一步把队列与链路纳入决定。
+同一会话由此可以根据状态与资源的位置重新安排。应用保留连续的上下文，服务选择继续使用本地状态、迁移状态或重新计算；选择依据是这三条路径何时能让后续模型执行开始。第八章根据内容判断可复用的前缀，本节进一步将排队时间与链路带宽纳入路由决策。
 
 把远端提高到 20 GB/s 后，单请求取回明显占优；但每秒 16 次取回需要约 19.3 GB/s，已用去约 97% 的链路能力。突发请求会增加传输排队时间。因此，按单请求算出所需带宽后，还要计算持续到达时的总流量。[^route]
 
@@ -439,17 +435,17 @@
 
 平均优于 B 的 200 ms 需要 $p>6/17$。取 $p=0.9$，平均仅 107 ms，但 10% 的请求仍为 260 ms，因此该两点分布的 p99 为 260 ms，达不到 220 ms 的目标。为达到 220 ms 的 p99 目标，这组两点时间模型要求命中概率至少达到 99%；提高到 90% 已经显著改善均值，却仍让十分之一请求走上 260 ms 的慢路径。[^events]
 
-同时运行两个任务的压力实验，更直观地显示了这种取舍。缓存优先时，目标请求约 1.38 秒完成，两个任务也在此时全部结束；把目标移到空闲实例后，它缩短到约 0.33 秒，两个任务却约 1.54 秒才全部完成。目标快了约 1.05 秒，整体完成却晚了约 0.16 秒。路由决策之前必须确定优化的是目标请求响应，还是所有任务排空。[^pressure]
+同时运行两个任务的压力实验，更直观地显示了这种取舍。缓存优先时，目标请求约 1.38 秒完成，两个任务也在此时全部结束；把目标移到空闲实例后，它缩短到约 0.33 秒，两个任务却约 1.54 秒才全部完成。目标快了约 1.05 秒，全部任务完成的时刻却推迟了约 0.16 秒。路由决策之前必须确定优化的是目标请求响应，还是全部任务的完成时间。[^pressure]
 
 ## 9.6 服务启动、扩缩容与故障恢复
 
-把第 8 章的恢复路径接到路由选择，就不能只比较“哪个实例空闲”。设实例 A 保留全局 KV 与编码器 SWA 但有排队，实例 B 可立即执行却需要取回全局 KV 并恢复编码器 SWA。A 的优势来自状态复用，B 的优势来自执行位置；路由器需要比较它们的预计完成时间，而不是只看单个缓存命中标志。
-
-只取两实例不同的准备成本，并假定后续新增输入处理、解码器窗口重放与生成耗时相同，作为共同项略去：若 A 排队 10 ms，B 的串行全局传输按第 7 章为 4.666 ms、编码器 SWA 恢复另假设为 8 ms，则 A 更早开始有效处理；若 A 排队变成 20 ms，B 才占优。8 ms 是教学假设，必须由部署测量替换。若状态压缩改变的是全局传输，而局部恢复占据大头，继续减少全局字节的路由收益就会减小。
-
-![缓存亲和性与空闲执行位置的比较。A 保留全局 KV 与编码器 SWA；B 需要 4.666 ms 全局传输和假设的 8 ms 编码器恢复。两端共同的解码器重放等后续工作略去，只比较不同的串行准备时间；A 的队列从 10 ms 增到 20 ms 时，选择翻转。](ch09/figure-9-v41-routing.svg)
-
-*图 9-29　缓存亲和性与空闲执行位置的比较。A 保留全局 KV 与编码器 SWA；B 需要 4.666 ms 全局传输和假设的 8 ms 编码器恢复。两端共同的解码器重放等后续工作略去，只比较不同的串行准备时间；A 的队列从 10 ms 增到 20 ms 时，选择翻转。*
+将第 8 章的状态恢复过程计入路由决策后，需要同时比较实例的空闲时间与状态准备时间。设实例 A 保留全局 KV 与编码器 SWA 但有排队，实例 B 可立即执行却需要取回全局 KV 并恢复编码器 SWA。A 的优势是能够复用状态，B 的优势是设备可以立即开始工作；路由器需要比较它们的预计完成时间，而不是只看单个缓存命中标志。
+
+设两端后续的新增输入处理、解码器窗口重放与生成耗时相同，准备工作串行执行。B 的全局传输按第七章为 4.666 ms，编码器 SWA 恢复假设为 8 ms，共需 12.666 ms。A 保留了这两份状态，准备时间就是排队时间：排队 10 ms 时，A 更早开始处理新增输入；排队增至 20 ms 时，B 更早。12.666 ms 由此成为本例切换路由选择的排队阈值。
+
+![缓存亲和性与空闲执行位置的比较。A 保留全局 KV 与编码器 SWA；B 需要 4.666 ms 全局传输和假设的 8 ms 编码器恢复。两端共同的解码器重放等后续工作略去，只比较不同的串行准备时间；A 的队列从 10 ms 增到 20 ms 时，更快的方案由 A 变为 B。](ch09/figure-9-v41-routing.svg)
+
+*图 9-29　缓存亲和性与空闲执行位置的比较。A 保留全局 KV 与编码器 SWA；B 需要 4.666 ms 全局传输和假设的 8 ms 编码器恢复。两端共同的解码器重放等后续工作略去，只比较不同的串行准备时间；A 的队列从 10 ms 增到 20 ms 时，更快的方案由 A 变为 B。*
 
 ### 9.6.1 启动与预热开销
 
@@ -471,7 +467,7 @@
 
 并发预取还可能改变谁先开始执行。一个 1024 位置的请求在单独运行时复用 1008 个位置；八个请求同时到达时，前四个各登记 1024 位置预取，占用达到 4096，后续请求因超过容量限额而未登记。首个完成的请求恰来自后者，以零命中开始 prefill。它没有等缓存，因而更早进入执行队列；因此，即使缓存对象存在，请求也要先获得预取所需的空间，才能使用这份缓存。预取空间用尽后，后续请求会直接重算，而已经开始预取的请求仍在等待数据。[^branch]
 
-权重固定在不可修改介质上时，增加该型号设备仍能扩展同版本服务，但发布新权重不能再简单地解释为从存储加载新文件。路由器需要识别设备支持的模型版本，容量规划要计入新旧版本并存与替换周期。上下文和工具仍可更新，权重升级则取决于实际可重配置能力。固定权重设备因而更适合在预计使用期内稳定且需求足够大的版本；这一判断需要服务量与版本寿命的证据。
+权重固定在不可修改介质上时，增加同型号设备可以扩展同版本服务；部署新权重则要配置支持新版本的设备。路由器按模型版本分派请求，容量规划为新旧版本并存和设备替换预留资源。固定版本服务持续得越久、需求越大，部署投入便能由更多请求分担。
 
 ### 9.6.2 运行中重配置与状态交接
 
@@ -529,7 +525,7 @@
 
 ### 9.7.2 同质量、同资源约束下的方案比较
 
-**例 9.7　用八个 worker 完成一次部署选择。** 回到开篇的问题，四个 A、四个 B，每秒到达四个独立请求，每请求输入 8192 个位置、输出 129 个 token。有效能力沿用例 9.2，两阶段在同一 worker 上串行占用资源，表中有效能力对应下述驻留限制。链路为共享的 25 GB/s 载荷通道，各次搬运占用同一份链路带宽。比较窗口内模型与生成设置相同，请求需要的前缀互不复用。服务从停止状态启动，三种部署方案都在 10 秒后就绪；就绪后的队列按连续流量模型处理。设计目标是持续处理新到达的请求，并在开始启动后的 25 秒内消除启动积压。
+**例 9.7　哪种推理部署能持续服务并按期清空启动积压？** 采用章首的资源与负载：四个 A 类 worker、四个 B 类 worker，每秒到达四个独立请求，每个请求输入 8192 个位置、输出 129 个 token。有效能力沿用例 9.2，两阶段在同一 worker 上串行占用资源，表中有效能力对应下述驻留限制。链路为共享的 25 GB/s 载荷通道，各次搬运占用同一份链路带宽。比较窗口内模型与生成设置相同，请求需要的前缀互不复用。服务从停止状态启动，三种部署方案都在 10 秒后就绪；就绪后的队列按连续流量模型处理。设计目标是持续处理新到达的请求，并在开始启动后的 25 秒内消除启动积压。
 
 容量条件也一并给定：各 worker 装入模型后，留出 12 GiB 专用于本例的 KV 与交接缓冲，调度器最多允许八份活动请求状态同时驻留；使用共享池的方案另有 64 GiB 状态容量。每个请求经过 128 次 decode 后，其 KV 覆盖 8320 个位置，占约 1.14 GiB。八份活动状态共约 9.14 GiB，再预留一份 1.125 GiB 接收缓冲，共约 10.3 GiB，能放入每个 worker 的 12 GiB 可用空间。尚未执行的请求只保留输入，分配到缓存空间后再开始计算。
 
@@ -539,11 +535,11 @@
 | --- | --- | ---: | ---: | ---: | ---: |
 | 八个完整副本 | 每个 worker 都执行 P、D | 0 | 3.2 | — | 3.2 |
 | 直接 PD | 四个 A 做 P，四个 B 做 D | 1.125 GiB | 8 | 20.7 | 8 |
-| PD 加共享池中转 | 相同的 P、D 实例的数量比例，P→池→D | 2.25 GiB | 8 | 10.3 | 8 |
+| PD 加共享池中转 | 相同的 P、D 实例配比，P→池→D | 2.25 GiB | 8 | 10.3 | 8 |
 
 完整副本每秒只能完成 3.2 个请求，持续少于到达的四个，因此首先被排除。两种 PD 方案的通道能力都高于八请求/s，均由计算池限制。在每秒四个请求的到达负载下，直传消耗约 4.8 GB/s，中转消耗约 9.7 GB/s；在八请求/s 的排空阶段，两者分别需要约 9.7 和 19.3 GB/s。
 
-两种方案的稳态吞吐率相同，但直接 PD 更适合这组请求。这批请求的前缀不会被后续请求复用，中转多消耗一倍通道载荷，每请求还多等待约 48.3 ms。64 GiB 共享池至多容纳 56 份完整的 1.125 GiB 前缀，保留下来的每一份在本次工作负载中都不会再次使用。直接交接完成相同工作，保留更多链路余量，并缩短交接等待。因此本例选择四 A 做 P、四 B 做 D、P→D 直传。
+两种方案的稳态吞吐率相同，但直接 PD 更适合这组请求。这批请求的前缀不会被后续请求复用，中转使通道传输量增加一倍，每请求还多等待约 48.3 ms。64 GiB 共享池至多容纳 56 份完整的 1.125 GiB 前缀，保留下来的每一份在本次工作负载中都不会再次使用。直接交接完成相同工作，保留更多链路余量，并缩短交接等待。因此本例选择四 A 做 P、四 B 做 D、P→D 直传。
 
 接着检查这一方案能否按期处理完启动期间积压的请求。10 秒后积压 40 个请求；直接 PD 就绪后每秒完成八个，同时每秒又来四个，净减少四个，10 秒后排空。从开始启动算起共 20 秒，满足 25 秒目标。完整副本在就绪后仍每秒增加 0.8 个请求，到第 25 秒时积压已从 40 增至 52 个。
 
@@ -568,7 +564,7 @@
 
 ### 9.7.3 负载变化后的部署调整
 
-现在改变例 9.7 中“前缀不再复用”这一条件。设每次留下的 1.125 GiB 上下文 KV 缓存还会被另一个实例复用一次，重算耗时为 180 ms，取回耗时为 48.3 ms。先前为中转多付出的 48.3 ms，加后续取回的 48.3 ms，共约 96.6 ms，低于后续重算的 180 ms，一次后续复用就净省约 83 ms。共享池由此获得了具体用途：把过去的计算替换为更短的读取。
+现在改变例 9.7 中“前缀不再复用”这一条件。设每次留下的 1.125 GiB 上下文 KV 缓存还会被另一个实例复用一次，重算耗时为 180 ms，取回耗时为 48.3 ms。先前为中转多付出的 48.3 ms，加后续取回的 48.3 ms，共约 96.6 ms，低于后续重算的 180 ms，一次后续复用就净省约 83 ms。共享池由此获得了具体用途：用更短的读取代替重复计算。
 
 这项收益同时增加通道需求。每次原始交接写一次、读一次，后续复用再读一次，共搬 3.375 GiB。在每秒四组这样的请求对下，共需约 14.5 GB/s；通道最多供给约 6.9 组请求对/s。与只传输一次相比，后续复用增加了数据读取量，占用了原有的带宽余量，因此下一步应根据后续请求新增的 prefill 与 decode 工作量，重新确定 P、D 实例的数量比例。
 
@@ -578,37 +574,35 @@
 
 这也给出了把本章推导用于新系统的顺序。先把每请求工作换算成各类资源需求；再按整数资源分配找出最忙的池或设备；按执行顺序计入状态传输时间与内存占用；最后用服务率减去到达率得到的剩余处理能力求出突发或启动积压的持续时间。若限制项是 D，增加 P 不会缩短队列；若限制项是共享通道，增加计算副本只会让更多请求等候取回。
 
-PD 的八 worker 案例和 MoE 的专家案例在这里相接。前者将不同阶段交给更擅长处理该阶段的设备，后者通过批内复用减少权重读取，再通过专家副本分散最忙设备上的工作。共享 KV 则把已完成的计算保留到下一次请求，启动与恢复决定这些能力何时开始供给。一次部署选择的依据，是这些部署方式共同改变了多少计算量和等待时间，以及消除积压所需的时间。
-
-对第 4 章的实验，还应做一次阶段配比复算。设每个 P 实例每秒能处理 20 条请求，每个 D 实例能完成 5 条相同请求，忽略传输与排队，1P 配 4D 才能平衡。若只有 D 阶段加速 4 倍，第一轮保留设备数量，系统仍受 P 的每秒 20 条限制；第二轮重新分配资源，1P 配 1D 已能达到这一上限。该例只用于说明配比，不能把权重通路改善假定为所有阶段等比例加速。
-
-AF 分离也应重新比较：独立权重通路改变 FFN 读取，Attention 仍处理增长的状态，两者之间还要传递激活。只有重新计入阶段时间和传输后，才能判断分离、合并或异构执行更合适。整个服务的最优组织会随瓶颈迁移，而不是由一种分离方式永久决定。
+PD 的八 worker 案例和 MoE 的专家案例在这里相接。前者将不同阶段交给更擅长处理该阶段的设备，后者通过批内复用减少权重读取，再通过专家副本分散最忙设备上的工作。共享 KV 则把已完成的计算保留到下一次请求，启动与恢复决定这些设备何时能够提供服务。一次部署选择的依据，是这些部署方式共同改变了多少计算量和等待时间，以及消除积压所需的时间。
+
+单个阶段变快也会改变实例配比。设链路充足，每个 P 实例每秒处理 20 条请求，每个 D 实例每秒完成 5 条相同请求，1P 配 4D 时两池能力相等。若 D 阶段加速四倍，保留原配比会使 D 池能力增至 80 请求/s，而 P 池仍为 20；改为 1P 配 1D，两池都提供 20 请求/s，并释放三个 D 实例。加速一个阶段的收益由此体现为完成相同工作所需的资源减少。
 
 ## 练习
 
 练习按复算、改变条件和独立设计展开。前两题建立共同的请求单位，第三至九题分别改变计算、通信与状态条件，第十题完成新工作负载的设计。带“核心”的题目贯穿多个资源层次。
 
-**9-1　服务基线。** 对输入 8192、输出 129 的请求，画出完整副本、PD 和共享 KV 三种部署方式，分别列出每请求调用数、KV 产生位置和跨实例交接。将输出改为 1，指出哪些工作消失。延伸到一次工具调用后继续生成，标出尚无 KV 的最后返回位置。
-
-**9-2　PD 配比。** 复算例 9.2 的 25 种整数分配方案，并把有效链路设为恰好每秒传一份 1.125 GiB 快照（约 1.21 GB/s）。求新的总体吞吐率上限；到达率恰等于该上界时，再一次性加入十个请求，求积压随时间的变化。随后复核高前缀命中与长输出两个情景。
-
-**9-3　CPU 专家。** 用例 9.3 的同一专家形状，推导 CPU 从读取受限转为计算受限的位置，再将 DRAM 带宽减半。分别评价单 token 和 128 token/专家；将权重改为每元素一字节、有效 CPU 算力保持不变，重新求解读取与计算的交点，并解释它的移动方向。
-
-**9-4　PD 与 AF 交接〔核心〕。** 固定 Qwen3-8B、25 GB/s，分别取 1、5、20 μs 的启动开销，比较一次 1.125 GiB 交接与总字节数相同的 72 次交接。再恢复实际 AF 载荷，累计 8192 输入、129 输出的完整请求交接。若四个微批次两侧计算分别为 2、3 ms，求允许增加的通信时间和计算时间。
-
-**9-5　异构服务器的服务范围。** 从已保存的 DeepSeek V4-Flash 或 Kimi K2 配置还原权重存放、GPU 缓冲和主存需求。增加并发时分别预测不同专家数、任务数和最忙 NUMA 节点。再构造一组总权重可以容纳、但到达率超过 CPU 计算能力的请求条件，推导积压增长率。
-
-**9-6　专家副本。** 对 $[32,16,8,4,2,1,1,0]$ 比较有效行、非空专家补齐和全部补齐。随后复算例 9.5 的严格回本批数，把每卡额外容量改为 32 MiB，解释为什么应先拒绝不可行副本方案。对动态热点给出观察窗口与迁移成本的比较方法。
-
-**9-7　共享 KV〔核心〕。** 给定一份 1.125 GiB 前缀，比较重算、一次取回并驻留、100 次逐步远程读取。增加写入时间与两次请求的复用间隔，推导保存的收益条件；设计一个改变 HBM 可用容量后需要将状态从 HBM 换出的例子。结合已有双引擎记录，解释共享 CPU 池从 4 GiB 增至 8 GiB 时，哪些复用机会得以保留。
-
-**9-8　持久化恢复。** 解释读取 1024 个位置但只复用 1008 个位置的计量差异。对缺页、截断页、版本不兼容分别提出继续等待、部分重算与放弃缓存的判断条件。把“请求完成”“数据修复”“输出一致”列为三个分别检查的问题。
-
-**9-9　缓存路由。** 推导例 9.6 的远端取回与本地重算耗时相等时的带宽，并计算 $p=0.5,0.9,0.99$ 时 A 的均值和按逆累积分布定义的 p99。再让 CPU 副本仍可用、取回与 80 ms 排队重叠，检查二点分布模型是否仍适用。
-
-**9-10　完整部署〔核心〕。** 沿用例 9.7，每秒四个请求中一半输出 129 个 token，另一半输出 1025 个 token，均输入 8192 个位置，无前缀命中；池内按长期平均工作量分配能力。先求每请求平均 D 调用数，再枚举将八个 worker 分配给 P、D 两阶段的所有数量组合。允许复制相同的八 worker 组合，求处理持续到达的请求所需的最少组数，以及 10 秒启动、第 25 秒排空所需的最少组数。最后将长输出请求集中到每分钟的前十秒到达，画出队列变化，并说明平均需求不变时，哪一段时间决定容量选择。
-
-**解题示范：练习 9-3 的第一问。** 单专家 CPU 路径的读取项与计算项相等时，
+**9-1　三种推理部署的调用次数与状态交接** 对于输入为 8192 个 token、输出为 129 个 token 的请求，画出完整副本、PD 和共享 KV 三种部署方式，分别列出每请求调用数、KV 产生位置和跨实例交接。再将输出长度改为 1 个 token，指出哪些工作不再需要执行。最后考虑调用一次工具后继续生成的情况，标出最后一个已返回、但尚未生成对应 KV 的 token 位置。
+
+**9-2　链路瓶颈与请求组成如何改变 PD 配比** 复算例 9.2 的 25 种整数分配方案，并把有效链路设为恰好每秒传一份 1.125 GiB 快照（约 1.21 GB/s）。求新的总体吞吐率上限；到达率恰等于该上界时，再一次性加入十个请求，求积压随时间的变化。随后复核高前缀命中与长输出两个情景。
+
+**9-3　专家批量与权重位宽如何改变 CPU 执行瓶颈** 用例 9.3 的同一专家形状，求 CPU 执行从读取受限转为计算受限时，每个专家接收的 token 数，再将 DRAM 带宽减半。分别比较每个专家接收 1 个和 128 个 token 时的执行时间；将权重改为每元素一字节、有效 CPU 算力保持不变，重新求解读取与计算的交点，并解释它的移动方向。
+
+**9-4　PD 与 AF 的传输量、启动次数与通信重叠〔核心〕** 采用 Qwen3-8B，设有效交接带宽为 25 GB/s，分别取 1、5、20 μs 的启动开销，比较一次 1.125 GiB 交接与总字节数相同的 72 次交接。再采用例 9.4 中 AF 的实际载荷大小，计算输入 8192 个 token、输出 129 个 token 的完整请求所需的累计交接时间。若有四个微批次，每个微批次在两侧分别计算 2 ms 和 3 ms，求在分离方案仍能节省总时间的条件下，最多可以增加多少通信与计算耗时。
+
+**9-5　异构推理的权重容量与 CPU 处理能力约束** 从已保存的 DeepSeek V4-Flash 或 Kimi K2 配置还原权重存放、GPU 缓冲和主存需求。增加请求并发后，分别预测被访问的专家数量、各专家的任务数，以及负载最重的 NUMA 节点。再构造一组总权重可以容纳、但到达率超过 CPU 计算能力的请求条件，推导积压增长率。
+
+**9-6　专家副本的容量约束与复制成本回收** 设八个专家分别接收 $[32,16,8,4,2,1,1,0]$ 行输入，比较只计算有效行、只对非空专家补齐，以及对全部专家补齐时的计算量。随后复算例 9.5 的严格回本批数，把每卡额外容量改为 32 MiB，解释为什么应先拒绝不可行副本方案。对动态热点给出观察窗口与迁移成本的比较方法。
+
+**9-7　KV 保存、取回与重算的收益条件〔核心〕** 给定一份占 1.125 GiB 的前缀 KV，比较重算、一次取回并驻留、100 次逐步远程读取。进一步计入写入耗时，以及两次请求之间保留缓存的时间，推导何时保存 KV 比重算更合算；设计一个改变 HBM 可用容量后需要将状态从 HBM 换出的例子。结合已有双引擎记录，解释共享 CPU 池从 4 GiB 增至 8 GiB 时，哪些复用机会得以保留。
+
+**9-8　KV 缺页与版本不兼容时的恢复选择** 解释读取 1024 个位置但只复用 1008 个位置的计量差异。对缺页、截断页、版本不兼容分别提出继续等待、部分重算与放弃缓存的判断条件。把“请求完成”“数据修复”“输出一致”列为三个分别检查的问题。
+
+**9-9　缓存路由的带宽交点与首 token 尾延迟** 推导例 9.6 的远端取回与本地重算耗时相等时的带宽，并计算 $p=0.5,0.9,0.99$ 时，选择 A 路径的首 token 时间均值，以及按逆累积分布定义的 p99。再假设 HBM 副本不可用时，CPU 副本仍可用，且取回过程可以与 80 ms 的排队时间重叠。检查原来的二点分布模型是否仍然适用。
+
+**9-10　长短输出混合负载下的 PD 扩容与排队〔核心〕** 沿用例 9.7，每秒四个请求中一半输出 129 个 token，另一半输出 1025 个 token，均输入 8192 个位置，无前缀命中；池内按长期平均工作量分配能力。先求每请求平均 D 调用数，再枚举将八个 worker 分配给 P、D 两阶段的所有数量组合。允许复制相同的八 worker 组合，分别求两种目标下所需的最少组数：持续处理新到达的请求；启动耗时 10 秒，并在开始启动后的第 25 秒前清空积压。最后将长输出请求集中到每分钟的前十秒到达，画出队列变化，并说明平均需求不变时，哪一段时间决定容量选择。
+
+**解题示范：CPU 专家从读取受限转为计算受限的批量** 单专家 CPU 路径的读取项与计算项相等时，
 
 $$
 \frac{2mP_e}{C_C}=\frac{2P_e}{B_D},
@@ -675,4 +669,4 @@
 
 分布式推理把一条请求的计算与状态分配到多个位置。PD、AF、专家放置和共享 KV 都需要比较局部执行收益与新增交接，容量、服务率、启动和恢复共同决定部署是否合适。路由不仅选择空闲设备，也决定能够复用哪些状态。
 
-硬件变化后，先固定请求与部署，找出新的瓶颈，再重新分配各阶段资源。权重读取加速不等于 Prefill、Decode 和 Attention 同比例加速；固定版本设备还要求把模型寿命与更新方式纳入容量规划。下一章将转向训练，讨论持续更新的参数、状态和有效进展。
+硬件变化后，先固定请求与部署，找出新的瓶颈，再重新分配各阶段资源。下一章将转向训练，讨论参数与训练状态的持续更新，以及训练进度如何推进。
--- before/manuscripts/10-训练系统.md
+++ manuscripts/10-训练系统.md
@@ -1,16 +1,16 @@
 # 第 10 章 训练系统
 
-一个模型能够在单张加速卡上生成文本，是否意味着它也能在这张卡上训练？全参数训练会更新模型的全部可训练权重；混合精度训练用较低精度执行主要矩阵运算，同时用较高精度保留更新所需的数值。这里 BF16 是占 2 字节的 16 位浮点格式，FP32 是占 4 字节的 32 位浮点格式。损失是衡量预测与训练目标差异的数值；优化器是根据损失梯度调整参数的算法。Adam 是一种利用梯度历史调整参数更新的优化器。Qwen3-8B 的 BF16 权重约为 16.4 GB，而采用混合精度 Adam 进行全参数训练时，训练除模型权重外，还保存梯度（损失对参数的导数）、主权重（用于高精度更新的参数副本），以及优化器跨步保留的梯度历史，合计约为 131 GB。后者约为前者的八倍。前向传播由输入得到预测，反向传播沿运算依赖求导；求导时还要读取前向保存的激活，即中间计算结果，执行期间还会出现参数聚合和转换缓冲。设计训练系统，首先要确定这些张量和缓冲区存放在哪里，以及需要保留多久。
+一个模型能够在单张加速卡上生成文本，是否意味着它也能在这张卡上训练？全参数训练会更新模型的全部可训练权重；混合精度训练用较低精度执行主要矩阵运算，同时用较高精度保留更新所需的数值。这里 BF16 是占 2 字节的 16 位浮点格式，FP32 是占 4 字节的 32 位浮点格式。损失是衡量预测与训练目标差异的数值；优化器是根据损失梯度调整参数的算法。Adam 是一种利用梯度历史调整参数更新的优化器。Qwen3-8B 的 BF16 权重约为 16.4 GB，而采用混合精度 Adam 进行全参数训练时，系统除模型权重外，还需保存梯度（损失对参数的导数）、主权重（用于高精度更新的参数副本），以及优化器跨步保留的梯度历史，合计约为 131 GB。后者约为前者的八倍。前向传播由输入得到预测，反向传播沿运算依赖求导；求导时还要读取前向保存的激活，即中间计算结果，执行期间还会出现参数聚合和转换缓冲。设计训练系统，首先要确定这些张量和缓冲区存放在哪里，以及需要保留多久。
 
 把训练状态分到更多设备上，可以减轻单卡的显存压力，但计算往往需要等待其他设备传来的数据。把状态移到主机内存，可以腾出显存，却又增加了主机与 GPU 之间的传输。训练持续数周时，等待输入、保存检查点和故障后的重复计算也会影响完成时间。理解这些取舍，需要把三个问题连起来：**哪些张量和缓冲区需要同时保留，哪些工作决定一步何时结束，已经完成的训练中，有多少能够在故障后恢复。**
 
-训练图给出了这些决定所需的生命周期信息。前向产生的激活在反向时使用，梯度在参数更新前汇集，优化器状态跨步保留。利用这些已知关系，运行时可以提前取回即将使用的参数、重算较便宜的激活，并让已就绪梯度的通信与后续反向计算重叠。模型求导、内存管理与设备调度由此共同决定一步训练的执行方式。
+训练计算图明确了各类数据的产生和使用时机，为这些设计提供了依据。前向产生的激活在反向时使用，梯度在参数更新前汇集，优化器状态跨步保留。利用这些已知关系，运行时可以提前取回即将使用的参数、重新计算开销较小的激活，并让已就绪梯度的通信与后续反向计算重叠。模型求导、内存管理与设备调度由此共同决定一步训练的执行方式。
 
 本章围绕一个任务展开：使用 Qwen3-8B 处理 100B token，完成领域继续训练，期限为 30 天。我们先估算至少需要多少资源，再比较由 32 张和 48 张 RTX 4090 组成的两套方案。比较从显存需求开始，逐步加入计算、通信、数据准备和故障恢复的耗时，最后确定哪套方案能够按期完成。为了讲清各项机制，文中穿插较小的张量和流水线算例。强化学习（RL）部分则进一步分析生成、验证和学习如何组成反馈循环。
 
 GB、TB 表示十进制容量，GiB、MiB 表示二进制容量。教学方案的处理时间和容量预留在首次使用时给定；论文及实验结果标明来源，完整配置见章末资料。
 
-## 10.1 从训练任务到容量与时间预算
+## 10.1 根据训练任务估算容量与时间需求
 
 ### 10.1.1 训练任务、可训练参数与完成目标
 
@@ -20,7 +20,7 @@
 
 从时间上看，一次训练迭代包括前向传播、反向传播、梯度归约和参数更新。持续训练由多次迭代组成，其间还需要准备输入数据、保存检查点。启动、准备资源和维护也要计入从开始到结束的总时间。本章的设计案例从 30 天中预留 5 天给启动、评估和计划性停顿，留下 25 天用于训练、检查点与故障恢复。这使平均有效吞吐要求从约 38,600 token/s 提高到约 46,300 token/s。
 
-完成整个任务需要 $\lceil10^{11}/3\,145\,728\rceil=31\,790$ 次训练迭代。25 天分摊到每次迭代，只有约 67.9 s。这是贯穿全章的时间预算：每步计算、通信等待、输入等待，以及分摊到这一步的保存与恢复，共同消耗这 67.9 s。
+完成整个任务需要 $\lceil10^{11}/3\,145\,728\rceil=31\,790$ 次训练迭代。25 天分摊到每次迭代，只有约 67.9 s。这是贯穿全章的时间预算：每步计算、通信等待、输入等待，以及分摊到这一步的检查点保存与故障恢复，共同消耗这 67.9 s。
 
 ### 10.1.2 参数、梯度、优化器与激活
 
@@ -160,7 +160,7 @@
 
 参数梯度需要前向输入 $X$。保存 $X$ 会使它从前向一直保留到反向传播时；释放 $X$ 则要求反向使用前重新生成它。**激活重计算**只保留部分中间结果。反向计算需要其他结果时，再从已保留的结果出发，重新执行相应的前向计算。这样增加了计算量，却减少了需要长期保存的数据。
 
-**例：保存乘积，还是保存它的来源？** Qwen3-8B 的一个多层感知机（MLP，即本例的前馈子网络）先得到门控结果 $a$ 和上投影结果 $u$，再形成 $h=a\odot u$，最后计算 $Y=hW_{\mathrm{down}}$。下投影的参数梯度需要 $h$。如果已为非线性算子的反向计算保留了 $a,u$，可以在下投影反向前重新相乘，就不必一直保存 $h$。
+**例：反向传播前重算门控乘积能节省多少激活存储？** Qwen3-8B 的一个多层感知机（MLP，即本例的前馈子网络）先得到门控结果 $a$ 和上投影结果 $u$，再形成 $h=a\odot u$，最后计算 $Y=hW_{\mathrm{down}}$。下投影的参数梯度需要 $h$。如果已为非线性算子的反向计算保留了 $a,u$，可以在下投影反向前重新相乘，就不必一直保存 $h$。
 
 ![保存乘积与反向前重建乘积的时间对比](ch10/figure-10-4-recompute.svg)
 
@@ -174,11 +174,11 @@
 
 通用矩阵乘法记作 GEMM。原方案为这些 GEMM 保存的输入每层共占 15 MiB，其中上述三个乘积占 10 MiB，其余 Q、K、V 和注意力输出占 5 MiB。九层阶段因此从 135 MiB 降到 45 MiB，即约 142 MB 降到 47 MB。重建按层依次发生，最大的乘积工作区为 6 MiB；上一层用完后，下一层复用这块空间。[^pipeline]
 
-若有四个微批次同时等待反向计算，需要长期保存的数据就能减少 $4\times90=360$ MiB，约 377 MB，反向时则逐个微批次执行重建计算。这里的收益来自生命周期的差别：等待反向计算的微批次越多，需要长期保存的数据越多；逐层重建时，却只需反复使用同一块工作区。流水调度若让反向更早开始，等待反向计算的微批次数量又会下降。因此，重计算与流水调度共同决定应保存多少，而新增计算是否延长步骤，由其在执行时间轴上的位置决定。
+若有四个微批次同时等待反向计算，需要长期保存的数据就能减少 $4\times90=360$ MiB，约 377 MB，反向时则逐个微批次执行重建计算。这里的收益来自生命周期的差别：等待反向计算的微批次越多，需要长期保存的数据越多；逐层重建时，却只需反复使用同一块工作区。流水调度若让反向更早开始，等待反向计算的微批次数量又会下降。因此，重计算与流水调度共同决定需要保存多少数据，新增计算是否延长训练步，则取决于它在执行时间线上的位置。
 
 ### 10.2.3 CPU／GPU 卸载与缓冲区的使用时间
 
-重计算适合从已保存结果中较便宜地重建数据。另一种办法是保留数据本身，把它移到容量更大的主机内存，使用前再传回 GPU，这就是**卸载**。选择卸载以后，原先的显存问题就变成了传输能否及时完成的问题。假设一份状态大小为 $V$，链路有效带宽为 $B$，从开始传输到算子需要这些数据之间有 $W$ 秒可用，则未能在这段时间内完成的传输
+当利用已保存结果重建数据的开销较小时，适合采用重计算。另一种办法是保留数据本身，把它移到容量更大的主机内存，使用前再传回 GPU，这就是**卸载**。选择卸载以后，原先的显存问题就变成了传输能否及时完成的问题。假设一份状态大小为 $V$，链路有效带宽为 $B$，从开始传输到算子需要这些数据之间有 $W$ 秒可用，则未能在这段时间内完成的传输
 
 $$
 T_{\mathrm{wait}}=\max(0,V/B-W)
@@ -186,7 +186,7 @@
 
 会使相应算子等待。传输和计算能重叠多少，取决于数据何时可发、链路何时空闲、相应算子何时需要这些数据。直接内存访问（DMA）由设备发起数据搬运，传输期间源数据必须保留。源缓冲在 DMA 结束前保持有效，目标缓冲从接收开始占用容量；卸载同时改变两端缓冲区的使用时间。
 
-**例：梯度在哪一端转换？** 门控投影（gate）决定前馈子网络各位置的通过程度。一个 gate 梯度形状为 $[12288,4096]$，BF16 大小 $G=96$ MiB，FP32 为 192 MiB。CPUAdam 是在 CPU 上执行 Adam 更新的实现，它使用 FP32 梯度，可以先传 BF16 再在 CPU 转换，也可以先在 GPU 转换再传 FP32。转换都要读 $G$、写 $2G$，共访问 $3G$ bytes。设 CPU、GPU 转换吞吐分别为 100、1500 GB/s，串行路径时间为
+**例：梯度在 CPU 还是 GPU 转换能缩短卸载时间？** 门控投影（gate）决定前馈子网络各位置的通过程度。一个 gate 梯度形状为 $[12288,4096]$，BF16 大小 $G=96$ MiB，FP32 为 192 MiB。CPUAdam 是在 CPU 上执行 Adam 更新的实现，它使用 FP32 梯度，可以先传 BF16 再在 CPU 转换，也可以先在 GPU 转换再传 FP32。转换都要读 $G$、写 $2G$，共访问 $3G$ bytes。设 CPU、GPU 转换吞吐分别为 100、1500 GB/s，串行路径时间为
 
 $$
 T_c=G/B+3G/C_c,\qquad T_g=3G/C_g+2G/B.
@@ -212,9 +212,9 @@
 
 这个阈值把两种相反作用放在同一尺度上：GPU 转换更快，节省了格式转换时间；FP32 数据更大，增加了传输时间。链路越快，后一项代价越小。
 
-容量又给出第二道条件。GPU 转换期间原始 96 MiB 与输出 192 MiB 共存，峰值为 288 MiB；先传 BF16 的路径在这次转换的数据范围内只保留 96 MiB GPU 梯度。选择 GPU 转换需要额外 192 MiB 缓冲区。SuperOffload 将转换位置与主机优化器执行共同安排，利用的正是转换速度、传输数据量和缓冲区的使用时间之间的取舍。整步收益则由最后一桶梯度、CPU 上的参数更新和权重回传何时完成决定。
-
-### 10.2.4 并行组合下的每张卡的状态分配与互联拓扑
+存储容量还带来另一项限制。GPU 转换期间原始 96 MiB 与输出 192 MiB 共存，峰值为 288 MiB；先传 BF16 的路径在这次转换的数据范围内只保留 96 MiB GPU 梯度。选择 GPU 转换需要额外 192 MiB 缓冲区。SuperOffload 将转换位置与主机优化器执行共同安排，利用的正是转换速度、传输数据量和缓冲区的使用时间之间的取舍。整步收益则由最后一桶梯度、CPU 上的参数更新和权重回传何时完成决定。
+
+### 10.2.4 组合并行方案中的每卡状态分配与互联拓扑
 
 分片、重计算和卸载分别改变保存量、计算量和传输量。把这些办法组合成完整系统时，还要确定它们作用于哪些设备和通信组。第 6、7 章的并行方法在训练中划分不同的数据和计算：张量并行（TP）切分层内矩阵，流水线并行（PP）划分层，数据并行（DP）划分样本，专家并行（EP）划分专家。先确定每份参数由谁更新，再沿前后向依赖安排收集、交换与归约，就能同时得到每卡容量和各接口流量。
 
@@ -226,15 +226,15 @@
 | 激活重计算 | 前向到反向之间保存的中间值 | 重建前向值 | 重建是否延迟关键路径上的反向计算 |
 | 状态卸载 | 设备上暂时不用的状态 | 主机与设备间传输 | 数据能否在使用前传回 |
 
-**例：增加设备还是减少激活？** 取每卡 24 GiB 可用显存，Qwen3-8B ZeRO-3 在八卡上每卡保存约 15.3 GiB，留给其他张量和缓冲区约 8.7 GiB。如果激活与临时量同时需要 10 GiB，总量约 25.3 GiB，超出约 1.3 GiB。此时可以通过重计算减少这 1.3 GiB，也可以增至十六卡，将长期保存的训练状态降到约 7.6 GiB，总量降至约 17.6 GiB。
+**例：显存不足时，应增加分片设备还是重计算激活？** 取每卡 24 GiB 可用显存，Qwen3-8B ZeRO-3 在八卡上每卡保存约 15.3 GiB，留给其他张量和缓冲区约 8.7 GiB。如果激活与临时量同时需要 10 GiB，总量约 25.3 GiB，超出约 1.3 GiB。此时可以通过重计算减少这 1.3 GiB，也可以增至十六卡，将长期保存的训练状态降到约 7.6 GiB，总量降至约 17.6 GiB。
 
 ![分片参与者数与每卡容量](ch10/figure-10-6-candidates.svg)
 
-*图 10-16：给定附加存储需求时，分片数决定容量可行区域。曲线为 $16N/d+10$ GiB，水平线为 24 GiB 教学算例中的可用显存。曲线低于预算的区域可以容纳这些训练状态和缓冲区；纵向距离给出容量余量。*
+*图 10-16：给定附加存储需求时，分片数决定哪些方案满足容量要求。曲线为 $16N/d+10$ GiB，水平线为 24 GiB 教学算例中的可用显存。曲线低于预算的区域可以容纳这些训练状态和缓冲区；纵向距离给出容量余量。*
 
 这两个选择对应不同的代价：八卡方案增加重建计算，十六卡方案增加设备并改变通信组。可以先从图中找出满足显存要求的方案，再比较这些方案的每步耗时。
 
-回到本章的训练任务，32 卡与 48 卡均采用全组 ZeRO-3，TP=PP=1，每卡微批次为一条序列。为设备运行时预留空间后，教学方案给每卡 22 GiB 可用显存，并为同时保留的激活、完整模块参数和工作区安排 10 GiB 上限。两套方案长期保存的训练状态分别约为 3.8、2.5 GiB，总量约为 13.8、12.5 GiB，容量余量约为 8.2、9.5 GiB。两套方案的显存都足够，接下来主要比较完成时间。[^design]
+针对本章的训练任务，32 卡与 48 卡方案均采用全组 ZeRO-3，TP=PP=1，每卡微批次为一条序列。为设备运行时预留空间后，教学方案给每卡 22 GiB 可用显存，并为同时保留的激活、完整模块参数和工作区安排 10 GiB 上限。两套方案长期保存的训练状态分别约为 3.8、2.5 GiB，总量约为 13.8、12.5 GiB，容量余量约为 8.2、9.5 GiB。两套方案的显存都足够，接下来主要比较完成时间。[^design]
 
 ## 10.3 训练步的流水调度与通信重叠
 
@@ -250,7 +250,7 @@
 
 求和与求导可以交换顺序，因此可以分别对各个微批次执行反向传播；计算每个微批次的损失时，都用其损失总和除以同一个 $L$。所有微批次的梯度累积完成后，执行一次梯度裁剪和一次参数更新。这样划分微批次只改变执行顺序，不改变各个 token 在损失函数中的权重。
 
-**例：平均顺序为什么会改变梯度？** 八条回答中，四条各含两个有效 token，四条各含三个，共 20 个。设短回答每 token 的损失为 $\theta/5$，长回答为 $2\theta/5$，则
+**例：按微批次还是按 token 求平均，为何会改变梯度？** 八条回答中，四条各含两个有效 token，四条各含三个，共 20 个。设短回答每 token 的损失为 $\theta/5$，长回答为 $2\theta/5$，则
 
 $$
 \mathcal L=\frac{8\theta/5+12(2\theta/5)}{20}=\frac{8\theta}{25},\qquad
@@ -263,7 +263,7 @@
 
 ### 10.3.2 流水线调度、气泡与激活的保存时间
 
-微批次的划分保持了训练目标，却改变了计算的先后顺序。对流水线并行而言，顺序还决定每个阶段什么时候有活可做，以及多少份激活需要等待反向。流水线并行把模型分成多个阶段，每个微批次依次前向，再沿反方向传播梯度。**填满排空**先完成所有微批次前向，再执行反向。**1F1B（one forward, one backward）**在流水线预热后交替执行一次前向和一次反向，让较早微批次更快释放激活。
+微批次的划分保持了训练目标，却改变了计算的先后顺序。对流水线并行而言，顺序还决定每个阶段什么时候可以开始计算，以及多少份激活需要等待反向。流水线并行把模型分成多个阶段，每个微批次依次前向，再沿反方向传播梯度。**填满排空**先完成所有微批次前向，再执行反向。**1F1B（one forward, one backward）**在流水线预热后交替执行一次前向和一次反向，让较早微批次更快释放激活。
 
 先设 $p$ 个阶段完全平衡，阶段前向为 $t_f$、反向为 $t_b$，传输和参数更新耗时取零。$m$ 个微批次填满排空需要
 
@@ -271,9 +271,9 @@
 T=(m+p-1)(t_f+t_b),\qquad u=\frac{m}{m+p-1}.
 $$
 
-式中的 $m$ 项是有用微批次工作，$p-1$ 项来自填充和排空。设备时间利用率 $u$ 因而随微批次数量增加而提高。取 $p=4,m=8$，得到 $u=8/11\approx72.7\%$。若保持全局批次，增加微批次数量意味着缩小每个微批次；由此获得的气泡收益，需要与更小矩阵的执行时间一起比较。
-
-**例：提前反向怎样改变整条流水？** 将 Qwen3-8B 的 36 层分为四个九层阶段，八个微批次各含一条 128-token 序列。每阶段前向 10 ms、反向 20 ms，边界传输每次 1 ms，正反方向链路独立，参数更新耗时 1 ms。先看填满排空：无传输时为 $(8+4-1)\times30=330$ ms；前向填充经过三条边界增加 3 ms，反向排空再增加 3 ms，参数更新增加 1 ms，总计 337 ms。
+式中的 $m$ 项是有用微批次工作，$p-1$ 项来自填充和排空。设备时间利用率 $u$ 因而随微批次数量增加而提高。取 $p=4,m=8$，得到 $u=8/11\approx72.7\%$。若保持全局批次，增加微批次数量意味着缩小每个微批次；由此减少的流水线空闲时间，需要与更小矩阵的执行时间一起比较。
+
+**例：1F1B 如何改变激活驻留与流水完成时间？** 将 Qwen3-8B 的 36 层分为四个九层阶段，八个微批次各含一条 128-token 序列。每阶段前向 10 ms、反向 20 ms，边界传输每次 1 ms，正反方向链路独立，参数更新耗时 1 ms。先看填满排空：无传输时为 $(8+4-1)\times30=330$ ms；前向填充经过三条边界增加 3 ms，反向排空再增加 3 ms，参数更新增加 1 ms，总计 337 ms。
 
 1F1B 更早开始反向，也使后续前向等待反向释放计算资源。在阶段 0，前四个微批次于 40 ms 完成前向，第一份传回的梯度于 106 ms 到达。随后反向执行到 126 ms，第五个微批次才开始前向。这种交错逐级影响下游到达时间。阶段 3 在 93 ms 完成第二个微批次反向，但第三个前向输入到 95 ms 才到，产生 2 ms 空隙；后续相似的等待继续进入关键路径。按这些依赖关系排定执行顺序后，所有梯度在 346 ms 时计算完毕，加上参数更新共为 347 ms。[^pipeline]
 
@@ -293,7 +293,7 @@
 
 *图 10-20：两种调度中各阶段的激活与收发缓冲峰值。提前反向使激活更早释放，最大值由约 2.57 GB 降到 0.97 GB。*
 
-两种调度做相同的前后向工作，但 1F1B 在这些依赖下多用约 3% 时间；与此同时，图中所示的中间结果与缓冲区的最大显存占用从约 2.57 GB 降到 0.97 GB，减少约 62%。若为这些中间结果和缓冲区只留 1 GB，提前反向使步骤能够执行；容量充裕时，337 ms 的调度完成得更早。选择由“何时释放张量”和“何时得到下一份输入”共同决定。
+两种调度做相同的前后向工作，但 1F1B 在这些依赖下多用约 3% 时间；与此同时，图中所示的中间结果与缓冲区的最大显存占用从约 2.57 GB 降到 0.97 GB，减少约 62%。若为这些中间结果和缓冲区只留 1 GB，提前反向使这个训练步能够执行；容量充裕时，337 ms 的调度完成得更早。选择由“何时释放张量”和“何时得到下一份输入”共同决定。
 
 ### 10.3.3 反向依赖、梯度分桶与通信重叠
 
@@ -319,9 +319,9 @@
 
 ### 10.3.4 MoE 与长上下文的调度变化
 
-稠密模型（Dense）的每次前向会使用各层全部前馈权重，其数据并行中的工作划分较规则。MoE 路由会使各设备收到不同数量的 token，序列长短不一也会使相同 token 数对应不同计算量。因此，分析每步耗时，要看工作如何分到各设备上，以及哪张设备最后完成。
-
-**专家分派的不均衡。** 设两张设备上的专家分别接到 96、32 次 token 分派，每次分派把一个 token 的特征向量送给一个专家，在该专家输入矩阵中占一行。假定每次分派的计算时间近似相同。总计 128 次分派，对应 128 个有效行，平均每卡 64 行，但完成时间由 96 行的一侧决定。将分配调整为 64、64 行，专家计算时间就从处理 96 行所需的时间降到处理 64 行所需的时间，缩短三分之一。
+稠密模型（Dense）的每次前向会使用各层全部前馈权重，其数据并行中的工作划分较规则。MoE 路由会使各设备收到不同数量的 token，序列长短不一也会使相同 token 数对应不同计算量。因此，分析每步耗时，要看工作如何分到各设备上，以及哪个设备最后完成。
+
+**专家分派不均如何延长最忙设备的计算时间** 设两个设备上的专家分别接到 96、32 次 token 分派，每次分派把一个 token 的特征向量送给一个专家，在该专家输入矩阵中占一行。假定每次分派的计算时间近似相同。总计 128 次分派，对应 128 个有效行，平均每卡 64 行，但完成时间由 96 行的一侧决定。将分配调整为 64、64 行，专家计算时间就从处理 96 行所需的时间降到处理 64 行所需的时间，缩短三分之一。
 
 训练中的路由均衡机制作用于样本如何选择专家；专家副本和拓扑映射则作用于这些选择在哪里执行。
 
@@ -329,7 +329,7 @@
 
 专家负载取决于设备上的专家接到了多少次 token 分派。长上下文还有另一层差别：即使 token 总数相同，需要计算的注意力配对数也可能不同。
 
-**序列长度的不均衡。** 对长度 $s$ 的因果序列，第一个位置查看一个 token，第二个查看两个，依次相加，得到 $s(s+1)/2$ 个有效注意力配对。两条长度 4096、4096 的序列合计约 1680 万对；改为 7168、1024，总 token 数仍为 8192，配对数却增至约 2620 万，增加约 56%。注意力配对数随序列长度近似按平方增长，所以较长的序列增加了总计算量。[^length]
+**相同 token 总数下，序列长度不均如何增加注意力计算量** 对长度 $s$ 的因果序列，第一个位置查看一个 token，第二个查看两个，依次相加，得到 $s(s+1)/2$ 个有效注意力配对。两条长度 4096、4096 的序列合计约 1680 万对；改为 7168、1024，总 token 数仍为 8192，配对数却增至约 2620 万，增加约 56%。注意力配对数随序列长度近似按平方增长，所以较长的序列增加了总计算量。[^length]
 
 ![等长与不等长序列的因果注意力面积](ch10/figure-10-9-attention-area.svg)
 
@@ -339,7 +339,7 @@
 
 *图 10-24：相同 8192 个 token 改分为 7168 与 1024，总因果配对数增至 26218496。长三角形的面积增长超过短三角形的面积减少。*
 
-图 10-23 中，长三角形的额外面积来自真实的注意力关系，无法靠删除填充位置消除。打包减少填充位置（padding），按长度分组则让各设备承担的三角形面积更接近；上下文并行进一步把长序列的计算分到多个设备，并通过交换 K/V 取得其他设备保存的数据。它们分别改变无效工作、工作分配和数据到达路径。用相同总 token 数比较方案时，保留长度分布、可见性掩码（mask，规定各位置能读取哪些上下文位置）和损失权重，就能将调度收益与任务变化分开。
+图 10-24 中，长三角形的额外面积来自真实的注意力关系，无法靠删除填充位置消除。打包减少填充位置（padding），按长度分组则让各设备承担的三角形面积更接近；上下文并行进一步把长序列的计算分到多个设备，并通过交换 K/V 取得其他设备保存的数据。它们分别改变无效工作、工作分配和数据到达路径。用相同总 token 数比较方案时，保留长度分布、可见性掩码（mask，规定各位置能读取哪些上下文位置）和损失权重，就能将调度收益与任务变化分开。
 
 ## 10.4 数据输入、检查点与故障恢复
 
@@ -369,7 +369,7 @@
 
 一次训练迭代结束后，下一批梯度由反向重新产生。此时保存权重 2 bytes、主权重 4 bytes 和两份 Adam 状态 8 bytes，共每参数 14 bytes。Qwen3-8B 的这部分检查点约为 115 GB。在一次训练迭代结束时，把模型状态和已处理到的数据位置一同保存，就能确定恢复后该用哪些参数、从哪一批数据继续训练。
 
-**例：从 TP=4 恢复到 TP=8。** gate 权重形状为 $[12288,4096]$，BF16 大小 96 MiB。沿输出维四分，每片 3072 行、24 MiB；八分后，每片 1536 行、12 MiB。通信参与者的编号称为 rank；目标 rank $r$ 读取旧分片 $\lfloor r/2\rfloor$，偶数 rank 取前半，奇数 rank 取后半。全局行坐标将旧布局和新布局联系起来。
+**例：检查点如何从四路张量并行重分片为八路？** gate 权重形状为 $[12288,4096]$，BF16 大小 96 MiB。沿输出维四分，每片 3072 行、24 MiB；八分后，每片 1536 行、12 MiB。通信参与者的编号称为 rank；目标 rank $r$ 读取旧分片 $\lfloor r/2\rfloor$，偶数 rank 取前半，奇数 rank 取后半。全局行坐标将旧布局和新布局联系起来。
 
 ![从四份权重分片恢复为八份分片](ch10/figure-10-11-resharding.svg)
 
@@ -377,7 +377,7 @@
 
 沿图 10-26 的箭头，恢复程序可以找到每个新分片对应的原始行范围。FP32 主权重与 Adam 两项状态也按相同的行范围拆分，每项为 BF16 权重的两倍。这个矩阵的全部保存状态因此为 $96\times(1+2+2+2)=672$ MiB。ByteCheckpoint 一类系统用全局形状、偏移和片段长度描述这些张量，使恢复端按目标布局读取需要的范围。文件分片是存储安排，全局坐标标明每个分片属于哪个张量、位于哪些行列。[^checkpoint]
 
-权重可以凭原矩阵的行号重新拼接，训练序列也需要保留足够的信息，才能重新组成同样的输入。这又回到了图 10-25 中尚未用于训练的数据。除了批次位置，还要保留数据预处理的中间结果。
+权重可以凭原矩阵的行号重新拼接，训练序列也需要保留足够的信息，才能重新组成同样的输入。图 10-25 中已经准备好但尚未用于训练的数据，同样需要在恢复时重新定位。除了批次位置，还要保留数据预处理的中间结果。
 
 打包器若保留 2,000 个 token，下一份数据提供 6,192 个，两者组成下一条 8,192-token 序列。如果丢失这 2,000 个 token，就只能从后续数据中取出额外的 token 来补齐下一条序列，改变它的前缀、注意力关系和标签。将已用于训练的数据位置、残留与参数更新结果一起保存，恢复后才能继续构造同一批数据。[^resume]
 
@@ -391,7 +391,7 @@
 
 *图 10-27：捕获一致的训练状态，复制到独立缓冲后允许训练继续；后台写完数据并提交完整快照后，恢复程序才使用这份检查点。箭头表示先后依赖。*
 
-**例：相同暂停，为什么重做量不同？** 取每份 112 GB 的教学快照，写带宽 8 GB/s，上传需 14 s。每 10 s 产生一份时，请求速率为 11.2 GB/s，队列持续增长。改为每 20 s 一份，平均需求降到 5.6 GB/s，存储能在下一份到达前完成上一份。
+**例：检查点上传速度如何决定故障后的重做量？** 取每份 112 GB 的教学快照，写带宽 8 GB/s，上传需 14 s。每 10 s 产生一份时，请求速率为 11.2 GB/s，队列持续增长。改为每 20 s 一份，平均需求降到 5.6 GB/s，存储能在下一份到达前完成上一份。
 
 在第 20、40 s 捕获两份快照，各花 0.5 s 复制到缓冲区后上传，完成时刻为 34.5、54.5 s。若第 50 s 故障，第二份仍在上传，只能恢复到第 20 s，重做 30 s。写带宽提高到 16 GB/s 后，上传时间减半，两份分别在 27.5、47.5 s 完成；同一故障可恢复到第 40 s，只重做 10 s。
 
@@ -424,7 +424,7 @@
 \qquad \tau^*=\sqrt{\frac{2c}{\lambda}}.
 $$
 
-**例：规模怎样改变保存频率？** 先取 1024 张卡，每卡平均 365 天故障一次，任一卡故障均中断作业。独立故障率相加，作业平均约 8.6 小时中断一次。约 115 GB 检查点以 8 GB/s 保存，$c\approx14.3$ s。设恢复需 120 s，代入得到最优间隔约 940 s，即约 16 分钟。[^interval]
+**例：设备规模与故障率如何决定检查点保存间隔？** 先取 1024 张卡，每卡平均 365 天故障一次，任一卡故障均中断作业。独立故障率相加，作业平均约 8.6 小时中断一次。约 115 GB 检查点以 8 GB/s 保存，$c\approx14.3$ s。设恢复需 120 s，代入得到最优间隔约 940 s，即约 16 分钟。[^interval]
 
 ![保存间隔对保存成本与故障重做成本的相反影响](ch10/figure-10-13-save-interval.svg)
 
@@ -440,11 +440,11 @@
 
 将保存间隔从五分钟延长到十五分钟，节省的保存时间多于新增的重做时间；继续延长到三十分钟，新增的重做时间就超过了节省的保存时间。表中合计由未舍入数值计算。设备数翻倍会使故障率翻倍，最优间隔缩短为原来的 $1/\sqrt2$；保存速度翻倍也使最优间隔按同样比例缩短，因为每次保存耗时更短。
 
-本章设计案例使用的卡数较少，沿用每卡 365 天、恢复 120 s 和保存约 14.3 s，取有用训练间隔 1800 s。32 卡的保存、重做与恢复合计约增加 0.90% 时间，48 卡约增加 0.95%。48 卡更容易遇到一次设备中断，但附加比例只多约 0.05 个百分点，远小于它减少单卡计算带来的收益。到这里，容量、每步训练时间和长期附加成本已经齐备，可在 10.6 汇总完成期限。
+本章设计案例使用的卡数较少，沿用每卡 365 天、恢复 120 s 和保存约 14.3 s，取有用训练间隔 1800 s。32 卡的保存、重做与恢复合计约增加 0.90% 时间，48 卡约增加 0.95%。48 卡更容易遇到一次设备中断，但附加比例只多约 0.05 个百分点，远小于它减少单卡计算带来的收益。到这里，容量、每步训练时间和长期附加成本已经齐备，可在第 10.6 节汇总计算完成时间。
 
 ## 10.5 强化学习训练
 
-前四节假定训练数据已经给定。RL 让模型参与产生下一批数据：模型生成轨迹，系统计算奖励或验证结果，再将轨迹用于训练，更新后的权重又改变下一轮生成。系统由单向训练流水变成反馈循环，新的问题集中在各阶段的处理速度、切换时的状态重叠，以及样本究竟由哪一版策略产生。生成轨迹的推理端与计算损失的训练端还必须对同一条轨迹给出可对齐的概率，这就是本节的**训推一致性**。它直接影响参数更新是否遵循算法预期，也约束阶段加速与异步执行的收益。
+前四节假定训练数据已经给定。RL 让模型参与产生下一批数据：模型生成轨迹，系统计算奖励或验证结果，再将轨迹用于训练，更新后的权重又改变下一轮生成。系统由处理固定数据的训练流水变成反馈循环，新的问题集中在各阶段的处理速度、切换时的状态重叠，以及样本究竟由哪一版策略产生。生成轨迹的推理端与计算损失的训练端还必须对同一条轨迹给出可对齐的概率，这就是本节的**训推一致性**。它直接影响参数更新是否遵循算法预期，也约束阶段加速与异步执行的收益。
 
 ### 10.5.1 一轮回答生成、验证与参数更新
 
@@ -454,13 +454,13 @@
 
 在策略蒸馏（on-policy distillation，OPD）让当前学生模型自己生成轨迹，再由教师在这些学生实际走到的前缀上提供监督，例如下一 token 的概率分布。学生因而能在自己容易出错的位置学习，减少前缀分布偏移；教师的逐 token 反馈也提供了比单个终局奖励更细的学习信号。DeepSeek V4 用多教师 OPD 将领域专家的能力合入统一模型，第三章已将教师前向列为独立的一项计算工作。[^opd]
 
-OPD 相比固定数据 SFT 的潜在优势，主要是达到目标能力所需的学习样本或更新次数可能减少；它同时增加学生在线生成、教师前向与权重同步的成本，是否减少总 GPU 小时或完成时间仍要实测。SFT 描述监督目标，offline 描述数据是否预先固定；也可以在线收集数据后使用监督损失。online 表示数据随训练过程持续产生，on-policy 则进一步要求采样分布与所优化的策略匹配。异步 online RL 可能使用滞后策略，OPD 也会遇到生成与训练引擎的计算差异。因此，在线采样缓解数据分布问题，并不自动保证训推一致性。
+OPD 相比固定数据 SFT 的潜在优势，主要是达到目标能力所需的学习样本或更新次数可能减少；它同时增加学生在线生成、教师前向与权重同步的成本，是否减少总 GPU 小时或完成时间仍要实测。SFT 描述监督目标，offline 描述数据是否预先固定；也可以在线收集数据后使用监督损失。online 表示数据随训练过程持续产生，on-policy 则进一步要求采样分布与所优化的策略匹配。异步 online RL 可能使用滞后策略，OPD 也会遇到生成与训练引擎的计算差异。因此，在线采样用于缓解数据分布偏移，训推一致性还需要对齐生成与训练两端的计算过程。
 
 第 10.3.1 节的八条回答例子来自一次小模型实验的回答长度：有效回答共 20 token，包含表示序列结束的 EOS token；输入计算的有效位置共 366。提示词和回答共同参加前向，损失则按所选回答位置归一化。因此，生成长度影响计算成本，损失掩码（mask）为各位置标明是否参与损失计算，它决定哪些位置参与梯度计算，两者有不同作用。[^verl]
 
 同一提示词下的相对奖励还决定优势。例如两条回答奖励均为 1，减去组内均值后都为零；奖励变为 1、0，中心化后为 0.5、−0.5，策略才获得区分两条回答的方向。系统即使完成了前向、反向和优化器调用，也可能遇到第一种全零优势批次。统计训练吞吐量时，应计算每秒实际用于训练的轨迹或 token 数；模型能力是否提高，则用固定的评估任务检验。
 
-> **案例：参数变化为何不等于学会。** 固定小模型实验使用强化学习训练框架 verl 与推理引擎 vLLM 协作，算术精确奖励使同组回答取得相同奖励，优势和策略梯度为零，参数变化来自 AdamW 权重衰减。AdamW 在 Adam 的梯度更新之外，单独按比例缩小权重；即使当前策略梯度为零，这一步也能改变参数。改用固定符号奖励后，两步出现非零梯度与权重更新，但算术验证只有 2/4 正确。奖励构造决定学习方向，系统执行负责把这个方向一致地应用到参数。[^verl]
+> **案例：参数发生变化，模型能力为何没有提高。** 固定小模型实验使用强化学习训练框架 verl 与推理引擎 vLLM 协作，算术精确奖励使同组回答取得相同奖励，优势和策略梯度为零，参数变化来自 AdamW 权重衰减。AdamW 在 Adam 的梯度更新之外，单独按比例缩小权重；即使当前策略梯度为零，这一步也能改变参数。改用固定符号奖励后，两步出现非零梯度与权重更新，但算术验证只有 2/4 正确。奖励构造决定学习方向，系统执行负责把这个方向一致地应用到参数。[^verl]
 
 ### 10.5.2 各阶段的资源分配与权重同步
 
@@ -470,7 +470,7 @@
 r_{\mathrm{effective}}=\min\left(r_l,q\min(r_g,r_v)\right).
 $$
 
-**例：增加哪一阶段的设备？** 生成每秒提供 12 条等长轨迹，验证处理六条，学习阶段处理八条；验证后四分之一因版本过旧被丢弃。每秒进入学习的只有 $6\times0.75=4.5$ 条。生成翻倍到 24 条/s，仍被验证挡在 4.5 条/s；验证翻倍到 12 条/s，每秒可用于训练的轨迹达到九条，此时训练端以八条/s 限制全流水。因此，应把新增资源分配给当前最慢的阶段；这个阶段加速后，再看瓶颈转移到了哪里。
+**例：RL 生成、验证与学习阶段应向哪里增加资源？** 生成每秒提供 12 条等长轨迹，验证处理六条，学习阶段处理八条；验证后四分之一因版本过旧被丢弃。每秒进入学习的只有 $6\times0.75=4.5$ 条。生成翻倍到 24 条/s，验证瓶颈仍将进入学习的样本限制在 4.5 条/s；验证翻倍到 12 条/s，每秒可用于训练的轨迹达到九条，此时训练端以八条/s 限制全流水。因此，应把新增资源分配给当前最慢的阶段；这个阶段加速后，再看瓶颈转移到了哪里。
 
 ![生成验证学习的处理瓶颈与权重反馈](ch10/figure-10-14-rl-flow.svg)
 
@@ -498,13 +498,11 @@
 
 这里再次出现本章的两个模型：共享设备时要分析张量的保存时间，独立部署时要分析数据传输和各阶段的处理时间。阶段独立以后，还会产生策略版本差异，下一小节将分析这种版本差异如何影响可用于训练的样本数量。
 
-固定权重设备还会遇到训练特有的边界。推理中的 KV 必须可写，训练中的策略权重也必须随更新改变；只读权重通路无法直接承接频繁发布的新策略。固定版本的教师、参考模型或奖励模型可能适合这样的设备，但必须分别核对算法中哪些模型保持不变、哪些模型需要更新，以及所需算子是否支持。
-
-若希望用固定基座配可训练适配器，需要额外的可写参数存储和对应的计算、组合路径。仅仅保留可写 KV 并不能自动支持 LoRA；若在设备上训练，还要有反向计算及梯度更新能力。第一轮可以把固定参考模型换成专用设备；第二轮再调整生成、验证和训练资源的配比，同时遵守策略版本与样本有效性的要求。
-
-![图 10-34 固定版本服务与策略训练的权重生命周期。上方 ROM 重复提供同一版本；下方训练产生新版本并发布给生成端。可写 KV 不能替代权重更新通路。](ch10/figure-10-weight-update.svg)
-
-*图 10-34　固定版本服务与策略训练的权重生命周期。上方 ROM 重复提供同一版本；下方训练产生新版本并发布给生成端。可写 KV 不能替代权重更新通路。*
+固定权重设备可以承接 RL 流程中长期不变的模型角色。例如，算法采用固定参考模型时，其权重能够留在专用存储中反复读取；持续更新的策略模型则使用可写权重存储，并将新版本发布给生成端。图 10-34 按这两种生命周期画出权重路径。
+
+![图 10-34 固定版本服务与策略训练的权重生命周期。上方 ROM 重复提供同一版本；下方训练产生新版本并发布给生成端。KV 写入与权重更新使用不同的数据通路。](ch10/figure-10-weight-update.svg)
+
+*图 10-34　固定版本服务与策略训练的权重生命周期。上方 ROM 重复提供同一版本；下方训练产生新版本并发布给生成端。KV 写入与权重更新使用不同的数据通路。*
 
 ### 10.5.3 异步训练、策略版本与长轨迹恢复
 
@@ -516,7 +514,7 @@
 
 *图 10-35：μ 产生训练样本，πold 标识本轮优化的起点，πθ 随本轮更新变化。虚线表示版本演进顺序。三个概率必须针对同一前缀与同一 token 计算。*
 
-令实际行为分布为 $\mu$，本轮优化开始时的参考策略为 $\pi_{\mathrm{old}}$，当前优化策略为 $\pi_\theta$。对同一 token 和前缀，三个概率比满足
+令实际行为分布为 $\mu$，本轮优化的起点策略为 $\pi_{\mathrm{old}}$，当前优化策略为 $\pi_\theta$。起点策略随优化轮次更新，KL 正则使用的参考模型则保持固定。对同一 token 和前缀，在三个概率有定义且分母非零的位置，概率比满足
 
 $$
 \frac{\pi_\theta}{\mu}
@@ -524,13 +522,15 @@
 \frac{\pi_{\mathrm{old}}}{\mu}.
 $$
 
-右边第一项描述本轮参数优化带来的概率变化，第二项描述实际采样分布与本轮起点策略之间的差异，其中既可能有策略版本滞后，也可能有采样配置或跨引擎计算差异。这里的起点策略不同于用于 KL 正则的固定参考模型。若行为策略和当前策略对这个 token 给出的对数概率（logprob）都为 $-3$，总比值为 1；若误用重新计算得到的对数概率代替行为策略原先记录的值，将它写成 $-3.25$，比值就变为 $\exp(0.25)\approx1.28$。参数没有变化，样本权重却增加约 28%。保留行为概率使训练端能够区分策略变化与计算差异。[^identity]
+右边第一项描述本轮参数优化带来的概率变化，第二项描述实际采样分布与本轮起点策略之间的差异，其中既可能有策略版本滞后，也可能有采样配置或跨引擎计算差异。若行为策略和当前策略对这个 token 给出的对数概率（logprob）都为 $-3$，总比值为 1；若误用重新计算得到的对数概率代替行为策略原先记录的值，将它写成 $-3.25$，比值就变为 $\exp(0.25)\approx1.28$。参数没有变化，样本权重却增加约 28%。保留行为概率使训练端能够区分策略变化与计算差异。[^identity]
 
 即使每轮都先同步权重，第二项也未必等于 1。生成端通常逐 token 解码并读取 KV 缓存，训练端则把整段轨迹并行输入，重算各位置的概率后再反向传播。不同的注意力实现、归约次序、批次形状、权重或 KV 精度，都可能让相同权重、相同前缀下的 logprob 不同。这里首先不一致的是生成前向与训练前向；反向会把训练前向构造出的损失差异传入梯度。MoE 的离散路由还会放大这种差异，下一小节将展开。
 
-概率的定义也必须一致。温度缩放、top-k 或 top-p 截断会改变实际采样分布；记录模型原始 softmax 的 logprob，不能直接把它当作这些变换之后的行为概率。分词、对话模板、位置编号、注意力掩码以及 EOS 和截断位置，也要保持对应。上述概率比恒等式只用于概率有定义且分母非零的位置；截断采样排除的 token 无法靠已有样本的概率比补回。保存采样配置与明确区分各类 logprob，是使用重要性修正的前提。
-
-在 PPO 一类更新中，概率比决定优势对梯度的权重，也参与裁剪判断。若实现差异被误认为参数更新，样本就可能被错误放大、压低或触发裁剪；较长轨迹有更多受影响的位置，若使用序列概率比，各位置的 logprob 差还会相加。这解释了为什么看似很小的数值差异也可能引起更新波动。重要性加权可处理一定的分布偏移，但差异过大时会增加方差，裁剪又会引入偏差；不能依靠一个修正系数包办一致性。前述乘法恒等式也不意味着分别裁剪两个比值与裁剪总比值等价，实际损失须遵循所选算法。[^identity]
+行为概率按实际采样规则计算。温度缩放、top-k 或 top-p 截断会改变模型原始 Softmax 分布，因此应保存变换后的概率及采样配置。训练端用对应的分词、对话模板、位置编号、注意力掩码和 EOS／截断位置重建输入，才能对同一事件计算概率比。
+
+在 PPO 一类更新中，概率比决定优势对梯度的权重，也参与裁剪判断。若实现差异被误认为参数更新，样本就可能被错误放大、压低或触发裁剪；若使用序列概率比，各位置的 logprob 差还会相加。因此，较长轨迹会积累更多概率偏差，使更新更容易波动。[^identity]
+
+需要注意的是，重要性加权依赖采样分布对目标事件的覆盖。截断采样排除的 token 无法通过已有样本的概率比补回；分布差异过大时，权重方差也会增大。裁剪可以限制极端权重，但会引入偏差，而且分别裁剪两个因子与裁剪总比值会产生不同的损失。异步执行会增加策略版本的时间差，下面将可用样本比例与执行周期一起计入收益。
 
 **例：异步重叠能抵消多少样本损失？** 每批生成 40 s、学习 16 s、阻塞两侧的权重同步 4 s，同步周期为 60 s。设生成和学习使用独立资源，权重同步仍独占 4 s，则异步稳态周期为 $\max(40,16)+4=44$ s。
 
@@ -542,7 +542,7 @@
 
 *图 10-37：稳态中生成下一批与学习上一批在独立资源上重叠；两者完成后同步权重，周期为 44 s。橙色同步阶段阻塞两侧。*
 
-图 10-36 中消失的是学习端与生成端轮流等待的时间。但生成得更早也意味着样本可能使用较旧的权重，因此还要扣除被丢弃的样本。
+从图 10-36 的同步执行改为图 10-37 的异步执行后，减少的是学习端与生成端轮流等待的时间。但生成得更早也意味着样本可能使用较旧的权重，因此还要扣除被丢弃的样本。
 
 若每批原有 $B$ 条等效轨迹，异步后比例 $q$ 保留下来用于训练，则同步和异步执行的有效样本吞吐量分别为 $B/60$、$qB/44$。异步更快要求
 
@@ -556,15 +556,13 @@
 
 若总是丢弃中断轨迹并重新采样，长轨迹因执行时间更长而更容易被丢弃。设生成期间的中断率为 $\lambda$，持续 $t$ 的轨迹在不中断的情况下生成完毕的概率为 $e^{-\lambda t}$；随着 $t$ 增长，进入学习的数据比例下降。按 token 保存进展和策略版本，可以在恢复后继续生成同一条轨迹，减少这种由系统中断引入的长度偏移。跨角色资源恢复在下一章展开。
 
-V4.1 的结构限制还进入了训练过程。报告说明稀疏注意力从头在 64K 序列上训练，层级候选限制在后训练中加入，训练和推理使用相同的候选搜索域；FP4 主 KV 也通过后训练中的量化感知训练适应。模型因此学习在受约束的表示与访问方式下完成任务。训练能适应限制，不等于所有被舍弃的信息都可恢复。[^v41-case]
-
-报告还明确在后训练中模拟解码器 SWA 有界重放，让训练适应部署中的近似局部状态。编码器缺失状态的恢复与解码器每次 Prefill 的重放仍应分开验证，不能把一个机制的训练证据推广到所有恢复路径。不能仅凭候选限制在训练中出现，就认为任何缓存恢复近似都等价；也不能把一个稀疏模型推理时临时改成全注意力，视为公平的稠密基线。要隔离结构造成的质量变化，需要控制规模、数据和训练预算的消融。对现有发布模型，先使用厂方明确的机制证据和独立任务测试，不把整体成绩变化归因于单项 KV 设计。
+V4.1 将部署时的稀疏访问与恢复方式纳入训练。稀疏注意力从头在 64K 序列上训练，后训练加入层级候选限制，使训练和推理使用相同搜索域；量化感知训练适应 FP4 主 KV，解码器 SWA 的有界重放也在后训练中模拟。模型由此学习使用部署路径实际提供的表示与局部状态。[^v41-case]
 
 ### 10.5.4 专家路由重放与训推一致性
 
 上一节区分了策略版本、采样定义与数值执行造成的概率差异。MoE 将数值差异进一步变成离散路径差异：即使权重相同，生成端与训练端的数值计算差异也可能改变专家选择。由于专家选择是离散的，小幅数值变化就可能让 token 进入另一条计算路径。以选择分数最高的两个专家（top-2）为例，前三个专家分数为 0.500、0.301、0.300 时，选择专家 1、2；第三个分数因计算次序变为 0.302，便改为专家 1、3。很小的分数变化就可能让计算改用另一个专家的权重矩阵，影响可以沿后续层继续传播。
 
-**Routing Replay** 保存生成时的逻辑专家 ID，训练时按照这些 ID 选择专家，再用当前权重计算路由分数、专家输出和梯度。重放确定使用哪些专家，训练端再计算这些专家在当前权重下的输出和梯度。样本、token 位置和层号共同定位这一份记录。重放约束了当前训练所用的离散路径，不能把旧专家输出直接拿来参与反向，也不能消除其他算子的浮点误差或策略版本滞后。
+**Routing Replay** 保存生成时的逻辑专家 ID，训练时按照这些 ID 选择专家，再用当前权重计算路由分数、专家输出和梯度。样本、token 位置和层号共同定位这一份记录。这样，生成与训练经过相同的离散专家路径，而数值计算继续反映当前参数。
 
 ![重放离散选择与重算当前数值](ch10/figure-10-17-replay.svg)
 
@@ -578,19 +576,17 @@
 
 使用每个整数占 4 字节的 int32 格式则为 12 MiB。容量计算之后，执行问题是记录如何随 token 一起移动：序列打包改变 token 的位置，上下文切分改变设备归属，重计算再次读取同一层记录。让 ID 与 token 的原始位置共同经过这些变换，训练端才能选中原先那组专家。[^replay]
 
-> **案例：一致性与训练收益。** 作者公布的四组路由重放（R3）开关对照各训练 100 步，开启后日志中记录的对数概率误差中位数均下降，每步总耗时中位数略长，训练奖励没有一致提高。重放减少了路由差异，但保存记录和执行重放也增加了耗时。比较两种方案时，分别观察概率误差、训练时间和固定评估任务上的表现。[^replay]
+> **案例：路由重放对概率误差、训练耗时与奖励的影响** 作者公布的四组路由重放（R3）开关对照各训练 100 步，开启后日志中记录的对数概率误差中位数均下降，每步总耗时中位数略长，训练奖励没有一致提高。重放减少了路由差异，但保存记录和执行重放也增加了耗时。比较两种方案时，分别观察概率误差、训练时间和固定评估任务上的表现。[^replay]
 
 因此，定位 RL 不稳定性时，应先冻结同一份权重与同一批已生成 token，逐位置比较生成端记录和训练端重算的 logprob，排除模型更新与随机采样带来的干扰。先核对输入、mask 和采样定义，再比较精度、批次形状与算子路径；MoE 额外检查专家 ID。除了平均误差，还要观察误差尾部、概率比偏离 1 的程度，以及这些偏差是否在尚未更新时就触发裁剪。同版本的计算差异受控后，再引入异步执行，按策略滞后程度观察概率比与样本保留情况，才能区分数值误差和旧样本问题。
 
-训推一致性的目标是让概率差异有清楚的来源，使更新符合算法所用的分布假设；逐位相同是更强的要求，是否需要及其代价取决于具体实现。它是排查 RL 不稳定性的重要环节，但奖励噪声、全零优势、学习率和数据变化也会影响训练。前面的全零优势案例即使完全对齐两端计算，也不会凭空产生学习信号。因而应把一致性误差与奖励、梯度和固定任务评估一起观察，不能把所有训练波动都归因于训推不一致。
-
-RL 把生成、环境验证和参数更新连接成同一个反馈应用。生成端提供采样概率与专家选择，训练端据此组织更新，资源调度再安排各阶段重叠。贯通这些信息，可以同时优化数据交接、执行一致性与资源利用。前面的异步周期和路由重放例子分别量化了省下的等待与增加的记录成本，最终用有效样本和训练进展评价整个反馈循环。
+RL 把生成、环境验证和参数更新组织成一个完整的反馈过程。生成端提供采样概率与专家选择，训练端据此组织更新，资源调度再安排各阶段重叠。贯通这些信息，可以同时优化数据交接、执行一致性与资源利用。前面的异步周期和路由重放例子分别量化了省下的等待与增加的记录成本，最终用有效样本和训练进展评价整个反馈循环。
 
 ## 10.6 从系统方案到完成期限与硬件选择
 
 ### 10.6.1 汇总容量、每步耗时、停顿与恢复
 
-现在回到章首任务。两套方案都处理 100B token，每步 384 条 8,192-token 序列，共 31,790 次训练迭代。设每步训练时间 $t_s$ 已包含单卡计算、通信等待与输入等待，保存恢复附加比例为 $L$，计划性停顿为 $T_0$，则本章的一阶完成时间模型为
+下面综合计算章首训练任务的完成时间。两套方案都处理 100B token，每步 384 条 8,192-token 序列，共 31,790 次训练迭代。设每步训练时间 $t_s$ 已包含单卡计算、通信等待与输入等待，保存恢复附加比例为 $L$，计划性停顿为 $T_0$，则本章的一阶完成时间模型为
 
 $$
 T_{\mathrm{finish}}=T_0+\left\lceil\frac{D}{b}\right\rceil t_s(1+L).
@@ -628,13 +624,13 @@
 t_s\le\frac{25\times86400}{31\,790\times(1+0.00952)}\approx67.3\ \mathrm{s}.
 $$
 
-本方案为约 56.7 s，余量约 10.6 s/步。固定单卡计算时间约 52.2 s、输入等待 0.5 s，则总通信等待最多约 14.6 s；当前给定值为 4 s。这一差额可直接用于评估参数预取不及时、跨机通信争用等变化会不会推翻期限判断。
+本方案为约 56.7 s，余量约 10.6 s/步。固定单卡计算时间约 52.2 s、输入等待 0.5 s，则总通信等待最多约 14.6 s；当前给定值为 4 s。这一差额可直接用于评估参数预取不及时、跨机通信争用等变化是否会使任务超过完成期限。
 
 32 卡则需要提高单卡计算效率。扣除恢复、4 s 通信和 0.5 s 输入后，可留给单卡计算约 62.8 s，而 40% 效率下需要约 78.3 s。相同工作量要求单卡计算效率提高到约 $40\%\times78.3/62.8\approx50\%$。于是，两条改进路线可以直接比较：增加两台主机，或者在原有设备上把单卡计算效率从 40% 提高到约 50%。
 
 通信预算必须落实到物理接口。若某主机的八个 rank 共享同一网卡，每个 rank 的发送量都要经过该接口，主机总流量会形成共同排队。以普通 DP 的完整 BF16 梯度环形 AllReduce 为独立例子，32 个参与者每 rank 发送约 $2(31/32)\times16.4\approx31.7$ GB。一个独占 25 GB/s 接口的发送过程约需 1.3 s；多个 rank 共享时，应把它们穿过该接口的字节相加，再按就绪时刻排列。ZeRO-3 则按各模块的参数收集与梯度归约并分片建立同样的接口时间轴。
 
-这样，性能目标就对应到了具体的执行过程：每步最多允许 67.3 s，其中计算约需 52.2 s，其余时间留给通信和输入等待。调优时首先缩短超出预算的那段关键路径，已有足够显存保存的张量则无需为省字节而额外增加工作。
+这样，性能目标就对应到了具体的执行过程：每步最多允许 67.3 s，其中计算约需 52.2 s，其余时间留给通信和输入等待。调优时首先缩短超出预算的那段关键路径，已有足够显存保存的张量则无需为减少存储量而额外增加工作。
 
 ### 10.6.3 替换硬件后的瓶颈与选择
 
@@ -680,7 +676,7 @@
 
 **“分片数翻倍，训练峰值就减半。”** 分片只直接缩小分给各设备的长期保存的训练状态。小模型例中长期保存的训练状态从 18 MiB 减到 9 MiB，临时存活量仍约 21 MiB，峰值从 39 MiB 减到 30 MiB。决定容量的量是同一时刻的总显存占用。
 
-**“通信处理时间更短，训练步就更短。”** 三个小桶的总处理时间约 4.7 ms，比整桶 4.5 ms 更长，却可以分别进入三个计算空隙。通信的完成位置决定它是否延长步骤。
+**“通信处理时间更短，训练步就更短。”** 三个小桶的总处理时间约 4.7 ms，比整桶 4.5 ms 更长，却可以分别进入三个计算空隙。通信完成的时刻决定它是否延长训练步。
 
 **“异步保存返回了，已有进展就安全了。”** 50 s 故障时，可用恢复点取决于哪份快照已经提交。相同的 1 s 前台暂停可以对应 30 s 或 10 s 重做，区别来自后台写入的完成时刻。
 
@@ -690,45 +686,45 @@
 
 以下十题按基础计算、机制分析和综合设计递进。计算题所需条件均在正文中；实验部分使用配套记录或自行测量的数据完成。
 
-> **习题 10-1 · 基础：状态与峰值**
+> **习题 10-1 · 基础：训练状态分片与每卡峰值内存**
 >
 > 将本章梯度改为 FP32，计算 Qwen3-8B 长期保存的训练状态。再求八卡 ZeRO-1、ZeRO-2、ZeRO-3 的每卡容量。设执行中有 6 GiB 激活和两份各 3 GiB 的模块缓冲，分别计算两份缓冲同时保留与顺序复用时的峰值。
 
 > **习题 10-2 · 基础：从期限反推效率**
 >
-> 使用本章任务和 384 序列全局批次，计算 30 天和 25 天对应的每步预算。给定 32 卡、每步 4 s 通信等待和 0.5 s 输入等待，先忽略恢复，求满足 25 天的最低单卡计算效率；再加入本章恢复成本。
+> 采用本章的训练任务，将全局批次大小设为 384 条序列，计算 30 天和 25 天对应的每步预算。给定 32 卡、每步 4 s 通信等待和 0.5 s 输入等待，先忽略恢复，求在 25 天内完成训练所需的最低单卡计算效率；再加入本章恢复成本。
 
 > **习题 10-3 · 综合设计：32 卡与 48 卡方案的选择**
 >
-> 比较 32 卡与 48 卡的完成时间和训练期间 GPU·小时。将每步附加存储上限从 10 GiB 提高到 20 GiB，重新判断容量。若只能租用四台八卡主机，分别求减少任务量、延长期限和提高效率三条路线的目标值。实验部分选择一条路线进行测量，替换教学参数。
+> 比较 32 卡与 48 卡的完成时间和训练期间 GPU·小时。将每步附加存储上限从 10 GiB 提高到 20 GiB，重新判断容量。若只能租用四台八卡主机，分别计算需要将任务量减少到多少、将期限延长到多久，或将计算效率提高到多少，才能完成训练。实验部分选择一条路线进行测量，替换教学参数。
 
 > **习题 10-4 · 分析：流水气泡与重计算**
 >
-> 在四阶段、前向 10 ms、反向 20 ms 的模型中，求 1、4、8、16 个微批次的理想利用率。沿图 10-17 解释 93—95 ms 的等待来自哪项依赖。将每层 10 MiB 的可重建乘积扩展到九层、四个待反向微批次，计算长期保存的数据减少多少，以及串行重建需要多大的工作区；再讨论两层同时执行反向传播时工作区怎样变化。
+> 在四阶段、前向 10 ms、反向 20 ms 的模型中，求 1、4、8、16 个微批次的理想利用率。沿图 10-19 解释 93—95 ms 的等待来自哪项依赖。假设每层每个微批次的可重建乘积占 10 MiB，需要为九层、四个尚未执行反向传播的微批次保留这些乘积，计算长期保存的数据减少多少，以及串行重建需要多大的工作区；再讨论两层同时执行反向传播时工作区怎样变化。
 
 > **习题 10-5 · 分析：数据准备与有限队列**
 >
 > 48 卡每步处理 384 条序列，每步训练时间约 56.7 s，四个数据准备进程各准备两条/s。若一个数据准备进程暂停 60 s，求维持原处理速度至少需要的预取条数，以及恢复四个数据准备进程后补满这些条数所需时间。再将检查点与输入放在同一条 8 GB/s 通路，分析保存期间的数据输入速度。
 
-> **习题 10-6 · 分析：保存周期**
->
-> 推导一阶最优周期，复算 1024 卡的 300、900、1800 s 三个保存间隔。加入每天一次、会同时影响全作业的共同中断，重新计算故障率和最优周期。实验部分在提交前后分别注入故障，比较恢复位置与需要重做的工作。
-
-> **习题 10-7 · 综合设计：RL 阶段配置**
->
-> 从生成 12、验证 6、学习 8 条/s、样本保留比例 75% 出发，比较只将一个阶段能力翻倍的收益。再用生成 40 s、学习 16 s、权重同步 4 s 的周期模型，求样本保留比例 80% 和 70% 时的有效吞吐。实验部分追踪一批样本的标识、奖励、有效损失位置和接收端权重。
+> **习题 10-6 · 分析：检查点保存频率与故障重做成本**
+>
+> 根据本章的一阶近似模型推导最优保存周期，复算 1024 卡的 300、900、1800 s 三个保存间隔。加入每天一次、会同时影响全作业的共同中断，重新计算故障率和最优周期。实验部分在提交前后分别注入故障，比较恢复位置与需要重做的工作。
+
+> **习题 10-7 · 综合设计：RL 各阶段配比与有效样本吞吐**
+>
+> 设生成、验证与学习阶段的处理率分别为 12、6、8 条/s，验证后样本的保留比例为 75%，比较只将一个阶段能力翻倍的收益。再用生成 40 s、学习 16 s、权重同步 4 s 的周期模型，求样本保留比例 80% 和 70% 时的有效吞吐。实验部分追踪一批样本的标识、奖励、有效损失位置和接收端权重。
 
 > **习题 10-8 · 分析：路由记录与样本对应关系**
 >
-> 计算 8K token、48 层、top-8 在 uint16 和 int32 下的记录容量。将两条序列打包后，再沿上下文切成四份，描述专家 ID 如何随 token 重排。构造一个 token 位置错位的例子，说明训练端将如何使用错误专家，并设计定位这一错误的检查。
-
-> **习题 10-9 · 分析：硬件敏感性**
->
-> 在本章的 48 卡设计案例中，分别将通信时间翻倍、单卡计算效率降至 30%、输入等待增至 5 s，重算完成期限。给定额外资源只能改善其中一项，比较每项可减少的关键路径时间。再选一份硬件规格或执行记录，将其中一个教学输入替换为有来源的数值。
-
-> **习题 10-10 · 综合设计：规模与完成成本**
->
-> 用 $6ND$ 计算 1T、5T、10T Dense 模型在固定 20T token 和 $D=20N$ 下的工作，分别求 90 天与 180 天的设备边界。自行给定并说明设备日成本、准备时间和恢复参数，比较两个满足期限的方案。实验部分从一份带恢复起点的公开日志计算本次新增 token 的长期速率。
+> 设序列含 8K token，模型有 48 层，每层为每个 token 选择 8 个专家。分别计算用 uint16 和 int32 保存专家 ID 所需的记录容量。将两条序列打包后，再沿上下文切成四份，描述专家 ID 如何随 token 重排。构造一个 token 位置错位的例子，说明训练端将如何使用错误专家，并设计定位这一错误的检查。
+
+> **习题 10-9 · 分析：通信、计算效率与输入等待如何影响训练期限**
+>
+> 在本章的 48 卡设计案例中，分别将通信时间翻倍、单卡计算效率降至 30%、输入等待增至 5 s，重新计算训练完成时间，并判断能否满足期限。给定额外资源只能改善其中一项，比较每项可减少的关键路径时间。再选一份硬件规格或执行记录，将其中一个教学输入替换为有来源的数值。
+
+> **习题 10-10 · 综合设计：按训练期限选择设备规模与完成成本**
+>
+> 对于参数量分别为 1T、5T、10T 的稠密模型，用 $6ND$ 分别计算两种数据规模下的训练运算量：训练数据固定为 20T token；训练 token 数满足 $D=20N$。再求各情形下在 90 天和 180 天内完成训练所需的最少设备数。自行给定并说明设备日成本、准备时间和恢复参数，比较两个满足期限的方案。实验部分从一份带恢复起点的公开日志计算本次新增 token 的长期速率。
 
 [^state]: [官方参数与 Adam／ZeRO 状态复算](../calculations/results/training-state-book.md)；[FP32 梯度变体](../calculations/results/training-state-fp32-gradient.md)。本章使用规定的五项状态表示，其他优化器与低精度状态需另算。
 
@@ -780,6 +776,6 @@
 
 训练容量由状态表示、所有权和生命周期共同决定。ZeRO 减少状态复制，重计算减少前向到反向之间的保存，卸载将暂时不用的训练状态移到主机内存；三者都把容量收益换成新的工作或传输。执行时，数据就绪与资源空闲共同决定开始时刻，沿依赖关系累积到参数更新结束的延迟决定每步耗时。
 
-长期完成时间在每步耗时之外计入输入数据准备、保存与故障重做。检查点既保存优化器状态，也保存已完成训练的数据位置；保存周期平衡正常运行中的写入成本与故障后的重做成本。RL 进一步要求跟踪样本标识、策略版本和行为概率，并对齐生成与训练的数值路径。在线采样使数据跟随策略变化，OPD 让教师监督覆盖学生实际生成的前缀，但两者都不能替代跨引擎的训推一致性。只有同时控制分布偏移与执行误差，阶段重叠产生的速度收益才有机会转化为有效学习进展。
+长期完成时间在每步耗时之外计入输入数据准备、保存与故障重做。检查点既保存优化器状态，也保存已完成训练的数据位置；保存周期平衡正常运行中的写入成本与故障后的重做成本。RL 进一步要求跟踪样本标识、策略版本和行为概率，并对齐生成与训练的数值路径。在线采样使数据跟随策略变化，OPD 让教师监督覆盖学生实际生成的前缀，跨引擎执行还需要保证训推一致性。控制数据分布偏移与执行误差后，便可以根据有效样本吞吐和任务评估，判断阶段重叠如何加快学习。
 
 本章的设计案例中，32 卡与 48 卡均能容纳训练状态，但给定相同训练目标和执行效率后，完成时间分别约为 35.8 天和 26.1 天。48 卡满足 30 天期限，每步训练时间上限约为 67.3 s。这个结论来自容量、执行与恢复的连续推导；相同模型也给出了结论随效率、通信和故障变化的阈值。
--- before/manuscripts/11-资源调度与运行环境.md
+++ manuscripts/11-资源调度与运行环境.md
@@ -6,7 +6,7 @@
 
 前两章讨论了推理和训练服务的构建。本章研究怎样把模型服务、工具环境和共享资源组织成一个任务运行系统。我们从一个平台设计题开始，逐步讨论何时创建和释放环境、如何安排任务、如何选择模型，以及如何从失败中恢复。
 
-**贯穿设计题：一个代码修复平台。** 本章将围绕以下条件展开讨论。任务均匀到达，每 0.1 秒一项；每项执行三轮，每轮先调用模型，再运行工具。工具使用一个 CPU 核，执行 1 秒，包含读取文件、执行测试、保存结果和清理工作状态。工作目录与工具结果存放在持久存储中，工具进程可以在轮间重建。
+**贯穿设计题：代码修复平台的资源容量、完成期限与任务成本** 本章将围绕以下条件展开讨论。任务均匀到达，每 0.1 秒一项；每项执行三轮，每轮先调用模型，再运行工具。工具使用一个 CPU 核，执行 1 秒，包含读取文件、执行测试、保存结果和清理工作状态。工作目录与工具结果存放在持久存储中，工具进程可以在轮间重建。
 
 | 设计输入 | 给定值 |
 | --- | ---: |
@@ -21,7 +21,7 @@
 
 先考察一种简单的方案：任务开始时，从预先准备好的环境池中分配一个环境，直到任务结束才释放；清理工作状态后，这个环境可以交给下一项任务。接下来依次考虑环境创建开销、请求突发、模型选择和失败恢复，在同一组任务条件下比较各方案的资源需求。[^design]
 
-## 11.1 从任务轨迹到资源需求
+## 11.1 根据任务执行过程计算资源需求
 
 ### 11.1.1 一项代码任务的执行与等待
 
@@ -62,7 +62,7 @@
 
 每项任务累计使用 3 秒 CPU 时间，所以每秒到达的任务合计需要 30 秒 CPU 时间，平均有 30 个 CPU 核在执行工具；每项需要 60 GiB·秒，所以平台平均保留 600 GiB 环境内存；每项有三次 9 秒模型调用，所以平均有 $10\times3\times9=270$ 次模型调用正在处理。
 
-**例 11-1：平台首先紧张的是哪种资源？** 将需求除以章首给定的容量，CPU 占用为 $30/40=75\%$，模型调用的并发容量使用率为 $270/300=90\%$，内存占用为 $600/640\approx94\%$。内存只剩 40 GiB，可以再容纳 20 个环境；CPU 还剩 10 核。此时增加 CPU 核数，仍然无法解决环境内存不足的问题。
+**例 11-1：代码修复平台为何先受环境内存容量限制？** 将需求除以章首给定的容量，CPU 占用为 $30/40=75\%$，模型调用的并发容量使用率为 $270/300=90\%$，内存占用为 $600/640\approx94\%$。内存只剩 40 GiB，可以再容纳 20 个环境；CPU 还剩 10 核。此时增加 CPU 核数，仍然无法解决环境内存不足的问题。
 
 也可以先求环境数量。每项任务占用环境 30 秒，每秒有 10 项任务开始使用环境，因此系统稳定运行时，平均有 300 个环境正在使用。这就是 Little 定律在本题中的形式：
 
@@ -89,7 +89,7 @@
 
 ### 11.1.3 任务完成条件与必须保留的状态
 
-减少等待期间的内存占用，意味着要释放环境中的一部分状态。释放之前，必须先弄清丢失这些状态会让哪些工作重做。回到图 11-1 的三轮任务，前两轮结束时，已经花了 20 秒，其中工具累计使用的 CPU 时间只有 2 秒。如果丢失工作目录后必须从头执行，就要重新花费这 20 秒，而不仅是重做 2 秒工具计算。能保留哪些状态，决定了故障后哪些阶段需要重来。
+减少等待期间的内存占用，意味着要释放环境中的一部分状态。释放之前，必须先弄清丢失这些状态会让哪些工作重做。在图 11-1 的三轮任务中，前两轮结束时，已经花了 20 秒，其中工具累计使用的 CPU 时间只有 2 秒。如果丢失工作目录后必须从头执行，就要重新花费这 20 秒，而不仅是重做 2 秒工具计算。能保留哪些状态，决定了故障后哪些阶段需要重来。
 
 | 状态 | 需要记录的信息 | 丢失后的恢复方式 |
 | --- | --- | --- |
@@ -105,7 +105,7 @@
 
 一次提交可以触发多次尝试。平台为每次尝试支付模型、工具和环境成本，用户得到的是最后完成的任务。第 11.5 节将沿这些分支累积支出，再除以通过测试的任务数。
 
-## 11.2 运行环境的生命周期与取舍
+## 11.2 运行环境的创建、保留与释放
 
 ### 11.2.1 工具执行路径与隔离边界
 
@@ -133,7 +133,7 @@
 
 E2B 是面向工具执行的环境平台，Firecracker 是亚马逊云服务（Amazon Web Services，AWS）为无服务器计算开发、于 2018 年开源的轻量虚拟机监控器；其 2020 年 NSDI 论文讨论了如何在大量短时任务之间兼顾硬件虚拟化隔离、启动速度和内存开销。[^firecracker-history]E2B 使用 Firecracker microVM 运行工具环境，模板可由容器镜像构建。镜像提供文件与依赖，microVM 提供运行时的客体内核和设备边界。创建时，平台从模板建立环境；运行时，工具通过客体内核读写文件和启动进程。容器镜像决定环境中有哪些文件和依赖，microVM 则决定这些程序运行时如何与宿主系统隔离。[^e2b]
 
-模型根据上下文选择动作，执行器在已经配置好权限与资源的环境中运行工具。两条路径在同一个任务控制器处汇合：它知道模型调用何时开始、工具何时使用环境，以及哪些文件需要跨轮保存。这些信息让平台能够分别安排模型状态与工具状态的生命周期。模型计算期间可以准备下一轮环境，工具完成后可以保存文件并释放进程；后面的容量与时间预算将据此选择创建、驻留和恢复方式。
+模型根据上下文选择动作，执行器在已经配置好权限与资源的环境中运行工具。两条路径在同一个任务控制器处汇合：它知道模型调用何时开始、工具何时使用环境，以及哪些文件需要跨轮保存。这些信息让平台能够分别安排模型状态与工具状态的生命周期。模型计算期间可以准备下一轮环境，工具完成后可以保存文件并释放进程；后文将据此计算容量与时间开销，选择环境的创建、保留和恢复方式。
 
 ### 11.2.2 资源分配与环境创建
 
@@ -143,9 +143,9 @@
 
 ![模板保存共享的文件与依赖，私有内容保存环境修改；程序运行时还会占用进程内存和缓冲区](ch11/figure-11-template-runtime.svg)
 
-*图 11-9：模板保存共享的文件与依赖，私有内容保存环境修改；程序运行时还会占用进程内存和缓冲区。模板安装量与活跃环境内存分别核算。*
-
-**例 11-2：复制方式改变容量。** 为分析创建开销，先取章首 300 个环境中的 100 个，考察其模板安装。每份模板为 2 GiB，工具访问其中 512 MiB，修改 128 MiB，另有 4 MiB 私有管理开销。逐份完整复制时，每份本地安装量为 2,052 MiB，100 份约为 200 GiB；只安装访问到的内容时，每份为 516 MiB，总量约为 50 GiB。这样，同样一组工具任务所需安装的数据减少了约 150 GiB。[^lifecycle]
+*图 11-9：模板保存共享的文件与依赖，私有内容保存环境修改；程序运行时还会占用进程内存和缓冲区。模板安装所需空间与活跃环境的内存占用分别计算。*
+
+**例 11-2：按需加载与只读共享能节省多少环境内存？** 为分析创建开销，先取章首 300 个环境中的 100 个，考察其模板安装。每份模板为 2 GiB，工具访问其中 512 MiB，修改 128 MiB，另有 4 MiB 私有管理开销。逐份完整复制时，每份本地安装量为 2,052 MiB，100 份约为 200 GiB；只安装访问到的内容时，每份为 516 MiB，总量约为 50 GiB。这样，同样一组工具任务所需安装的数据减少了约 150 GiB。[^lifecycle]
 
 还可以让环境共享只读内容，仅保留 128 MiB 脏页（相对于模板已经被写入修改的内存页）和 4 MiB 管理开销。此时每份为 132 MiB，100 份约为 13 GiB。未装入本地的只读页在访问时取得。若每个环境再保留 256 MiB 热点只读页，本地总量回升到约 38 GiB，换来较少的远端访问。
 
@@ -157,15 +157,15 @@
 
 *图 11-10：四种方案采用相同的横轴尺度，比较每个环境本地保存的数据。另有一份共享的 2 GiB 模板，不计入各环境的柱长。数值来自例 11-2；管理开销为每环境 4 MiB。*
 
-设取回与安装都经过同一条有效速率为 1 GiB/s 的串行通路。模板尚未缓存在本机时，需要取回 2 GiB，再安装 0.5 GiB，数据服务时间至少为 2.5 秒；模板已在缓存中时，只需安装 0.5 GiB，降至 0.5 秒。两秒差额来自省去的模板传输。一般地，这条通路的服务时间下界为 $(V_{\mathrm{fetch}}+V_{\mathrm{install}})/B$。
+设取回与安装都经过同一条有效速率为 1 GiB/s 的串行通路。模板尚未缓存在本机时，需要取回 2 GiB，再安装 0.5 GiB，数据传输与安装时间至少为 2.5 秒；模板已在缓存中时，只需安装 0.5 GiB，降至 0.5 秒。两秒差额来自省去的模板传输。一般地，这条通路的传输与安装时间下界为 $(V_{\mathrm{fetch}}+V_{\mathrm{install}})/B$。
 
 如果工具首次执行时又访问 1 GiB 尚未装入的页，这条通路便再工作 1 秒。按需加载使创建接口更早返回，将尚未访问的数据留到真正需要时取得。E2B 的页与文件读取采用了这样的组织：启动过程先提供可运行的环境，随后的访问逐步补齐内容。因此，要判断按需加载能否缩短任务时间，需要从环境启动一直计时到首次工具执行完成，将启动等待和首次访问数据的等待一并计入。[^e2b]
 
 ### 11.2.3 暂停、恢复与环境重建
 
-上一节减少了每次创建时复制的数据。另一种节省内存的办法，是在两次工具执行之间暂时释放整个环境。回到三轮任务，第一轮工具在第 10 秒完成，第二轮工具要到第 19 秒才开始，中间有 9 秒模型调用。始终驻留需要 $2\times9=18$ GiB·秒。设暂停和恢复各用 1 秒，这两个阶段仍占 2 GiB：第 10–11 秒保存状态，第 18–19 秒恢复，其余 7 秒释放内存。该间隔占用降至 4 GiB·秒，节省 14 GiB·秒，下一次工具仍在第 19 秒开始。
-
-图 11-11 中被移除的是等待中间的一段内存占用。要实现这段空闲，必须先保存下一轮仍需使用的状态；因此，暂停收益与保存什么、怎样恢复密切相关。
+上一节减少了每次创建时复制的数据。另一种节省内存的办法，是在两次工具执行之间暂时释放整个环境。在章首的三轮任务中，第一轮工具在第 10 秒完成，第二轮工具要到第 19 秒才开始，中间有 9 秒模型调用。始终驻留需要 $2\times9=18$ GiB·秒。设暂停和恢复各用 1 秒，这两个阶段仍占 2 GiB：第 10–11 秒保存状态，第 18–19 秒恢复，其余 7 秒释放内存。该间隔占用降至 4 GiB·秒，节省 14 GiB·秒，下一次工具仍在第 19 秒开始。
+
+从图 11-11 改为图 11-12 的安排后，省去的是等待期间的一段内存占用。要实现这段空闲，必须先保存下一轮仍需使用的状态；因此，暂停收益与保存什么、怎样恢复密切相关。
 
 ![同一段模型等待中的内存保留与暂停恢复](ch11/figure-11-pause.svg)
 
@@ -179,9 +179,9 @@
 
 Agent 从运行快照恢复，会继承先前修改过的文件和进程。RL 验证常常要求每条样本从同一份初始目录出发，因此从干净模板派生。一个快照里的文件若包含上一次测试输出，恢复也会把这份输出带回来。初始模板用于重复试验，运行快照用于继续进度，二者保存的是不同时间点的状态。
 
-在这个 9 秒间隔里，保存和恢复状态所需的两秒都安排在模型调用期间，真正释放内存的时间为七秒。若一个间隔只有 1 秒，就无法在这段时间内完成保存和恢复，恢复会推迟下一次工具执行。等待间隔越长，释放容量的收益越大，转换工作的相对代价越小。
-
-可以通过比较成本来决定是否暂停。设释放的内存为 $M$ GiB，释放后保持空闲的时间为 $G$ 秒，每 GiB·秒价格为 $p_M$，状态保存、存储和恢复的新增成本为 $C_{\mathrm{transition}}$，暂停节省成本的条件是 $p_MMG>C_{\mathrm{transition}}$。左边随释放时间增长，右边是每次转换支付的成本；令两者相等，就能求出暂停开始节省成本时所需的空闲时间。
+在这个 9 秒间隔里，保存和恢复状态所需的两秒都安排在模型调用期间，真正释放内存的时间为七秒。若一个间隔只有 1 秒，就无法在这段时间内完成保存和恢复，恢复会推迟下一次工具执行。等待间隔越长，释放容量的收益越大，保存与恢复的相对开销越小。
+
+可以通过比较成本来决定是否暂停。设释放的内存为 $M$ GiB，释放后保持空闲的时间为 $G$ 秒，每 GiB·秒价格为 $p_M$，状态保存、存储和恢复的新增成本为 $C_{\mathrm{transition}}$，暂停节省成本的条件是 $p_MMG>C_{\mathrm{transition}}$。左边随释放时间增长，右边是每次保存和恢复需要支付的成本；令两者相等，就能求出暂停开始节省成本时所需的空闲时间。
 
 不同状态需要采用不同的恢复方式。快照保存文件和内存，连接由客户端重新建立，外部存储则继续保留已经提交的写入。控制器恢复后先取回操作结果，再接续下一轮，避免把已经完成的修改重新执行一次。[^e2b]
 
@@ -191,9 +191,9 @@
 
 已知第一轮模型将在第 9 秒返回，平台可以在第 7 秒开始准备环境，第 9 秒执行工具，第 10 秒保存结果并销毁环境。后两轮分别在第 17 秒和第 27 秒开始准备。三次准备都在模型调用期间完成，任务仍在第 30 秒完成，环境只需在每轮准备和执行的 3 秒内保留。
 
-每项内存占用由 60 降到 $3\times(2+1)\times2=18$ GiB·秒，平台平均内存由 600 降到 180 GiB。代价是每秒创建 30 个环境，准备工作平均额外占用 $30\times0.1=3$ 个 CPU 核，CPU 需求从 30 增到 33 核。平台以少量创建工作换回 420 GiB 内存，释放了原先等待模型时保留的状态。
-
-图 11-13 将这项收益放回整条任务时间线。工具的三个执行时刻没有改变，消失的是长段模型等待期间的内存占用。这里能准确安排准备时刻，是因为下一轮使用什么工具、模型何时返回都已给定；下面再考虑需要预测工具类型的情况。
+每项内存占用由 60 降到 $3\times(2+1)\times2=18$ GiB·秒，平台平均内存由 600 降到 180 GiB。代价是每秒创建 30 个环境，准备工作平均额外占用 $30\times0.1=3$ 个 CPU 核，CPU 需求从 30 增到 33 核。平台增加少量环境创建工作，便可节省 420 GiB 内存，释放了原先等待模型时保留的状态。
+
+图 11-13 和图 11-14 将这项收益放回整条任务时间线。工具的三个执行时刻没有改变，消失的是长段模型等待期间的内存占用。这里能准确安排准备时刻，是因为下一轮使用什么工具、模型何时返回都已给定；下面再考虑需要预测工具类型的情况。
 
 ![全程保留环境与每轮重建的内存占用时间](ch11/figure-11-residency.svg)
 
@@ -236,9 +236,9 @@
 
 *图 11-19：预测错误：第 2—4 s 准备了不需要的环境（灰色）；第 4 s 取消后，再花 2 s 准备实际所需环境。此图采用立即取消、无资源争用的题设。*
 
-由此可以比较三种减少等待的办法。缓存模板可以省去重复获取数据的开销；保留已经启动的环境，可以在调用到达后立即执行，但需要持续占用内存；每轮重新建立环境，则可以利用模型调用的时间完成准备。章首任务已经知道下一轮所需的运行时，因此在模型调用期间准备下一轮环境；多种工具交替出现时，预测命中率进入同一计算。
-
-本地工具重放展示了过早准备的后果：提前量从 50 ms 扩展到整段模型等待后，累计工具调用时间仍约为 0.26–0.28 秒，采样内存积分却从约 0.019 增到 0.47 GiB·秒，约增至 24 倍。增加的时间主要落在环境已经就绪、调用尚未到达的区间。只要能赶在工具调用到达前准备好环境，就没有必要更早创建。环境提前就绪后多等待的时间，都会增加内存占用。[^prewarm]
+由此可以比较三种减少等待的办法。缓存模板可以省去重复获取数据的开销；保留已经启动的环境，可以在调用到达后立即执行，但需要持续占用内存；每轮重新建立环境，则可以利用模型调用的时间完成准备。章首任务已经知道下一轮所需的运行时，因此在模型调用期间准备下一轮环境；多种工具交替出现时，还需在计算中计入预测命中率。
+
+本地工具重放展示了过早准备的后果：提前量从 50 ms 扩展到整段模型等待后，累计工具调用时间仍约为 0.26–0.28 秒，按采样结果计算的累计内存占用却从约 0.019 增到 0.47 GiB·秒，约增至 24 倍。增加的时间主要落在环境已经就绪、调用尚未到达的区间。只要能赶在工具调用到达前准备好环境，就没有必要更早创建。环境提前就绪后多等待的时间，都会增加内存占用。[^prewarm]
 
 ## 11.3 共享资源池的分配与调度
 
@@ -248,7 +248,7 @@
 
 调度器选定的节点必须同时满足 GPU 型号、数量、CPU 和内存需求，作业中的所有进程才能一起启动。章首 CPU 工具每次独立使用一个核，多卡训练则必须让一组设备共同推进。因此，多卡作业能否启动，不仅取决于空闲资源的总量，还取决于这些资源分布在哪些节点上。
 
-**例 11-3：四张空闲卡为什么仍不够。** 假设一个作业需要四张 A 型 GPU 和 16 个 CPU 核，并要求这些资源位于同一节点。节点 1 有四张 A 型空闲 GPU，却仅有 8 个可用 CPU 核；节点 2 有 32 个 CPU 核和四张 B 型 GPU。整个资源池既有四张 A 型卡，也有足够多 CPU，但没有满足全部条件的节点。
+**例 11-3：GPU、CPU 与节点约束如何造成资源碎片？** 假设一个作业需要四张 A 型 GPU 和 16 个 CPU 核，并要求这些资源位于同一节点。节点 1 有四张 A 型空闲 GPU，却仅有 8 个可用 CPU 核；节点 2 有 32 个 CPU 核和四张 B 型 GPU。整个资源池既有四张 A 型卡，也有足够多 CPU，但没有满足全部条件的节点。
 
 ![资源碎片与迁移整理前后的节点配置](ch11/figure-11-4-placement.svg)
 
@@ -276,7 +276,7 @@
 
 先只看一个执行进程（worker），它执行每项工具工作都需要 1 秒。十项工作在第 0、1、……、9 秒分别到达时，每项到达即可执行，排队时间都是零。若十项都在第 0 秒到达，执行仍需 10 秒，但它们的排队时间依次为 0、1、……、9 秒，平均为 4.5 秒。总工作量决定执行进程要工作多久，到达顺序决定每个任务先等多久。
 
-图 11-22 让等待表现为执行块前方的灰色长度。同样十秒的执行工作，既可以没有排队，也可以产生大量等待。因此，从平均资源需求推到实际响应时间，还必须知道请求是否集中到达。
+图 11-23 用执行块前方的灰色条形表示等待时间。同样十秒的执行工作，既可以没有排队，也可以产生大量等待。因此，从平均资源需求推到实际响应时间，还必须知道请求是否集中到达。
 
 ![均匀到达与同时到达时的排队过程](ch11/figure-11-queue.svg)
 
@@ -316,7 +316,7 @@
 
 验证与生成重叠的办法将在下一小节讨论。这里先看增加临时生成设备的办法：RLBoost 保持训练组不变，让临时实例在完成权重准备后加入生成池。[^rollout]
 
-**例 11-4：新实例接收权重至少需要多久？** 一份完整权重为 30 GB，要向六个 rollout 实例分别发送。六路传输共用发送端的 200 Gbit/s 出口，每个接收端的带宽为 50 Gbit/s。这里 GB 为十进制。向一个接收端传完权重至少需要
+**例 11-4：共享发送端出口如何限制多实例权重分发速度？** 一份完整权重为 30 GB，要向六个 rollout 实例分别发送。六路传输共用发送端的 200 Gbit/s 出口，每个接收端的带宽为 50 Gbit/s。这里 GB 为十进制。向一个接收端传完权重至少需要
 
 $$
 30/(50/8)=4.8\ \mathrm{s},
@@ -342,7 +342,7 @@
 
 在一次 Qwen3-8B 对照中，保留前缀省去了 256 个重复采样 token，完整时间却约为 12.8 秒，而从头重做约为 11.6 秒。节省的 token 对应生成阶段，完整路径还包含前缀重建与重新调度。评估恢复收益时，把重建前缀并继续生成的总时间，与从头生成到相同停止条件的总时间相比较，才能知道节省的生成工作是否转化为更早完成。[^recovery]
 
-还可以从成本判断增加临时实例是否划算。按 RLBoost 使用的历史价格，训练实例每小时约 84 美元，六个额外实例各约 5.3 美元，总费率约为 116 美元。全部实例全程计费时，处理同一批训练任务的吞吐量需达到原来的约 $116/84\approx1.38$ 倍，单位工作成本才下降。吞吐量只达到原来的 1.2 倍，完成时间缩短，但成本约增至原来的 1.15 倍；达到原来的 1.6 倍，成本则约降至 0.86 倍。[^rollout]
+还可以从成本判断增加临时实例是否划算。按 RLBoost 使用的历史价格，训练实例每小时约 84 美元，六个额外实例各约 5.3 美元，总费率约为每小时 116 美元。全部实例全程计费时，处理同一批训练任务的吞吐量需达到原来的约 $116/84\approx1.38$ 倍，单位工作成本才下降。吞吐量只达到原来的 1.2 倍，完成时间缩短，但成本约增至原来的 1.15 倍；达到原来的 1.6 倍，成本则约降至 0.86 倍。[^rollout]
 
 ### 11.3.4 验证批次、长尾与资源释放
 
@@ -382,7 +382,7 @@
 
 每轮重建环境已经把章首平台内存需求从 600 GiB 降到 180 GiB，任务完成时间仍为 30 秒，超过 24 秒目标。接下来要改变关键路径上的模型调用。如果改用一个同样满足任务通过率要求的快速服务，每次完整调用由 9 秒降到 6 秒，三轮便从 30 秒降到 21 秒。若环境始终驻留，每项占用也从 60 降到 42 GiB·秒，整个平台的内存需求从 600 降到 420 GiB；如果每轮重建环境，仍只需在各轮准备和执行期间保留环境，累计占用保持在 18 GiB·秒。模型服务的选择因此同时改变时间和环境容量。[^design]
 
-一次模型调用的时间和成本，包括处理输入、生成思考内容和生成可见输出三部分。思考 token 是模型用于中间推理而非直接展示给用户的生成内容。思考预算决定允许生成多长的内部推理，实际用量决定这次调用支付多少成本。先在输出都能通过测试的条件下，比较两种思考长度设置的单次调用成本，再回到三轮任务比较服务。
+一次模型调用的时间和成本，包括处理输入、生成思考内容和生成可见输出三部分。思考 token 是模型用于中间推理而非直接展示给用户的生成内容。思考预算决定允许生成多长的内部推理，实际用量决定这次调用支付多少成本。先在输出都能通过测试的条件下，比较两种思考长度设置的单次调用成本，再比较两种服务完成三轮任务所需的成本。
 
 **例 11-5：思考长度缩短到十分之一，总成本减少多少？** 设两种思考长度设置都能通过相同测试，每次输入 10,000 token，可见输出 200 token，实际思考分别为 1,000 和 100 token。输入每百万 2 个成本单位，生成每百万 10 个成本单位，思考包含在生成项中。那么单次成本分别是
 
@@ -410,7 +410,7 @@
 
 ![模型服务入口、后端选择及计费位置](ch11/figure-11-6-service.svg)
 
-*图 11-30：同一任务的模型服务与成本路径。外部应用程序接口（API）按调用用量收费，自建副本按设备预留及运行支出计价；两条路径都将成本归到任务及尝试。输入拆分为普通处理、缓存创建和缓存读取，生成单独计量。*
+*图 11-30：同一任务使用模型服务的两种方式及其计费方法。外部应用程序接口（API）按调用用量收费，自建副本按设备预留及运行支出计价；两条路径都将成本归到任务及尝试。输入拆分为普通处理、缓存创建和缓存读取，生成单独计量。*
 
 将一次调用的输入拆为互不重叠的普通输入 $I_u$、缓存创建输入 $I_w$ 和缓存读取输入 $I_h$，计费的生成 token 数为 $O$，对应的每百万 token 价格为 $p_u,p_w,p_h,p_o$，则
 
@@ -420,7 +420,7 @@
 
 同一个输入 token 在一次调用中只属于普通输入、缓存创建或缓存读取中的一类。将服务返回的用量记录（usage）按这三类整理后，分别乘以对应单价再相加，就得到输入成本。生成 token 数 $O$ 则是思考 token 数与可见输出 token 数之和。
 
-**例 11-6：十轮公共前缀。** 每轮有 8,000 token 公共前缀和 2,000 token 不复用的新输入。采用教学价格：普通输入 2、首次缓存创建 2.5、读取 0.2，均以每百万 token 为单位。第一次创建，后九次在有效期内全部命中。无缓存时十轮输入成本为 0.20；有缓存时为
+**例 11-6：跨轮缓存公共前缀能节省多少输入费用？** 每轮有 8,000 token 公共前缀和 2,000 token 不复用的新输入。采用教学价格：普通输入 2、首次缓存创建 2.5、读取 0.2，均以每百万 token 为单位。第一次创建，后九次在有效期内全部命中。无缓存时十轮输入成本为 0.20；有缓存时为
 
 $$
 \frac{8000\times2.5+2000\times2}{10^6}
@@ -437,7 +437,7 @@
 
 章首每秒 30 次模型调用还受到并发容量的约束。每次调用需 9 秒时，平均有 270 次调用正在处理；缩短到 6 秒后，降为 180 次。服务最多同时处理 300 次调用，因此可用于处理其他请求的并发容量增加了 90 次。若每次调用的输入或输出增多，token 额度可能先于请求数额度用完。选择模型服务时，需要分别比较请求排队时间、并发调用数和 token 处理能力，找出限制任务完成速度的因素。
 
-**例 11-7：单价更高的服务何时更省？** 向两个假想服务各提交 1,000 项单次调用任务。每次输入 20,000 token，其中公共前缀 19,000、新输入 1,000。没有额外缓存创建、存储、工具和预热成本，质量成功概率与缓存命中独立。价格与成功率均为教学输入。[^routing]
+**例 11-7：缓存命中与成功率何时能抵消更高的服务单价？** 向两个假想服务各提交 1,000 项单次调用任务。每次输入 20,000 token，其中公共前缀 19,000、新输入 1,000。没有额外缓存创建、存储、工具和预热成本，质量成功概率与缓存命中独立。价格与成功率均为教学输入。[^routing]
 
 | 参数 | A | B |
 | --- | ---: | ---: |
@@ -468,7 +468,7 @@
 
 本例每次前缀都是 19,000 token，所以请求命中率 $h$ 也等于前缀内部的 token 命中比例；相对于完整 20,000-token 输入，缓存读取占比为 $0.95h$。成本交点约为 84.9%，业务门槛约为 91.8%。从低命中率向右移动，B 先变得便宜，随后才满足按时完成目标。把服务目标放到成本曲线上，就能直接读出最终可选区域。
 
-### 11.4.4 自建容量、按量 API 与订阅的成本比较
+### 11.4.4 自建服务、按量 API 与订阅的成本比较
 
 路由比较确定了哪些模型服务同时满足质量、成本和期限要求。选定服务以后，还要决定是自建部署、按调用付费，还是购买订阅。模型选择确定每次任务所需的服务，采购方式决定如何为这些服务付费。自建容量先支付设备与预留成本，再由任务分摊；按量 API 随调用支付；订阅则按月或其他周期支付成本，取得相应产品的使用权限。三种方式的共同问题是：满足相同任务量时，总支出随使用量怎样变化？
 
@@ -482,7 +482,7 @@
 
 取固定支出 1,000 成本单位、自建每项新增 0.002、API 每项 0.012，并给定两种服务均能在期限内处理 200,000 项。交点为 100,000 项提交。50,000 项时自建成本 1,100，API 为 600；200,000 项时分别为 1,400 和 2,400。固定投入在低使用量下难以摊薄，高使用量下则被较低的新增成本抵消。
 
-图 11-33 中，自建曲线的高度起点来自固定支出，较缓的斜率来自较低的单项成本。优化执行效率会进一步降低这条线的斜率，使自建服务在更少的任务量下就能收回固定投入。
+图 11-33 中，自建曲线较高的起点来自固定支出，较缓的斜率来自较低的单项成本。优化执行效率会进一步降低这条线的斜率，使自建服务在更少的任务量下就能收回固定投入。
 
 ![固定支出与每项任务成本决定采购交点](ch11/figure-11-purchase.svg)
 
@@ -492,15 +492,15 @@
 
 原先每个输出 token 所需时间中，约 63% 用于 decode，其余 37% 保持不变，因而两倍 decode 加速只能将总时间缩短约 31%。即使把 decode 时间降到零，其余处理仍需要 9.4 ms。进一步降低成本应转向输入处理和上下文重建；同一组设备在单位时间内生成更多 token，固定租价才能由更多产出分摊。
 
-回到章首平台，每秒 10 项任务、每项三次调用，因此每秒调用模型 30 次。每次价格增加 0.002，平台每秒便多支出 0.06；如果这次换服务让任务从 30 秒降到 21 秒，这笔额外成本就使任务能够在 24 秒期限内完成。章末将把这笔新增模型费与节省的环境占用放在同一张方案表里。
-
-## 11.5 完整任务的恢复、核算与扩容
+章首的代码修复平台每秒接收 10 项任务，每项调用模型三次，因此每秒调用模型 30 次。每次价格增加 0.002，平台每秒便多支出 0.06；如果这次换服务让任务从 30 秒降到 21 秒，这笔额外成本就使任务能够在 24 秒期限内完成。章末将把这笔新增模型费与节省的环境占用放在同一张方案表里。
+
+## 11.5 完整任务的恢复、成本计算与扩容
 
 ### 11.5.1 工具失败后的恢复与模型选择
 
 前面选择模型时，主要考虑了调用正常完成的情况。实际运行中，还需要处理调用失败、结果丢失和工具异常。如果控制器没有收到测试完成的确认消息，就无法区分两种情况：测试尚未完成，或者测试已经完成但确认消息丢失。
 
-恢复时，控制器先查询已持久化的结果，核对操作身份和输入文件版本。如果结果存在，就可以继续；如果不存在，再判断是否允许重试。幂等操作指以同一操作身份重复调用时，最终业务效果与调用一次相同。对只读查询或具有幂等语义的操作，重复执行通常容易处理；对外部付款、创建资源或发送消息，重复调用可能产生第二次效果，需要目标系统支持操作标识、事务或补偿。
+恢复时，控制器先查询已持久化的结果，核对操作身份和输入文件版本。如果结果存在，就可以继续；如果不存在，再判断是否允许重试。幂等操作指以同一操作身份重复调用时，最终业务效果与调用一次相同。对只读查询或具有幂等语义的操作，重复执行通常容易处理；对外部付款、创建资源或发送消息，重复调用可能使同一操作实际发生两次，需要目标系统支持操作标识、事务或补偿。
 
 ![外部系统已执行并提交操作 K，确认却丢失](ch11/figure-11-commit-ack.svg)
 
@@ -523,7 +523,7 @@
 
 有限重试树可以在部署前用于初步估算。对路径 $\pi$，令路径概率为 $p_\pi$，各步骤成本之和为 $c_\pi$，则 $E[C]=\sum_\pi p_\pi c_\pi$。成功概率是所有最终成功的执行路径概率之和。每个节点的概率都应根据此前经历的执行结果计算；如果两类失败的后续成功率不同，应拆成不同节点。
 
-**例 11-8：局部修复提高成功率，也增加超时。** 另取一个首次尝试需 10 秒的小例子，比较以下有限恢复策略。首次尝试花费 0.010、耗时 10 秒；80% 直接成功，12% 进入局部修复，8% 直接升级。局部修复新增 0.006 和 4 秒，条件成功率 60%，其余升级。升级新增 0.030 和 8 秒，条件成功率 98%。20 秒期限只用于评价，不主动终止任务。[^retry]
+**例 11-8：局部修复对成功率与超时比例的影响。** 另取一个首次尝试需 10 秒的小例子，比较以下有限恢复策略。首次尝试花费 0.010、耗时 10 秒；80% 直接成功，12% 进入局部修复，8% 直接升级。局部修复新增 0.006 和 4 秒，条件成功率 60%，其余升级。升级新增 0.030 和 8 秒，条件成功率 98%。20 秒期限只用于评价，不主动终止任务。[^retry]
 
 先看图 11-35。局部修复成功时，任务在第 14 秒结束；修复失败后再升级，就比直接升级多用 4 秒。这一额外分支解释了为什么总成功率提高后，仍有一部分成功结果赶不上期限。
 
@@ -556,23 +556,19 @@
 
 恢复把成功比例从 80% 提高到约 99.7%，但其中约 4.7% 的提交在第 22 秒才完成。业务期限为 20 秒时，这部分工作占用资源，却没有增加按时完成数。收紧期限改变的是哪些终点被计入分母；限制重试次数则会改变执行树本身。前者改变成功任务的统计范围，后者改变实际执行的工作。[^records]
 
-模型变快以后，环境准备可能从可隐藏的后台工作变成关键路径。假设下一次工具调用必然发生，模型还需 10 秒思考，环境创建需 6 秒，从模型开始时提前创建即可隐藏这 6 秒。若模型阶段缩短至 1 秒，同样时机开始创建仍要再等 5 秒。[^core-calculation]第一轮只更换模型设备，保持准备策略；第二轮比较更早准备、保留热环境和按需等待，核算提前占用资源及最终未调用工具的浪费。
-
-这里的优化目标是成功任务的总成本与完成时间。模型更快不必然让每项任务需要更多 CPU：固定轨迹下工具工作量未变；只有接纳更多任务或选择更复杂的轨迹时，总工具需求才会增加。这两种负载应分别估算。
-
-![图 11-38 模型加速使原本可隐藏的环境创建暴露出来。两种情况都在零时刻开始创建环境；工具须同时等待模型决策与环境就绪。横轴为秒。](ch11/figure-11-environment-overlap.svg)
-
-*图 11-38　模型加速使原本可隐藏的环境创建暴露出来。两种情况都在零时刻开始创建环境；工具须同时等待模型决策与环境就绪。横轴为秒。*
-
-沿 V4.1 会话回到完整任务，工具运行和模型状态是两种不同资源。工具等待期间，进程、文件与环境由操作系统管理，全局 KV 和局部 SWA 由模型服务管理；暂停环境不必然释放 KV，保留 KV 也不能恢复丢失的文件与工具副作用。应分别规定两种状态的保存、过期和恢复条件。[^v41-case]
-
-如果服务减少了 Prefill 或缓存传输耗时，固定轨迹中工具所需的 CPU 工作不会自动减少。更快的模型也可能更早发起下一次工具调用，使环境创建暴露在关键路径上。第一轮保持任务数量与调用轨迹，测完整完成时间；第二轮再比较更高并发、更多分支或更早准备环境，以成功任务成本判断新增容量是否值得。
+模型变快以后，环境准备可能无法再与模型调用完全重叠，从而开始影响任务完成时间。假设下一次工具调用必然发生，模型还需 10 秒思考，环境创建需 6 秒，从模型开始时提前创建即可隐藏这 6 秒。若模型阶段缩短至 1 秒，同样时机开始创建仍要再等 5 秒。[^core-calculation]先只更换模型设备，保持准备策略不变；再比较更早准备、保留已就绪环境和按需等待的成本，并计入提前占用的资源，以及最终未使用环境时的准备开销。
+
+![图 11-38 模型加速后，环境创建成为工具开始执行前的等待来源。两种情况都在零时刻开始创建环境；工具须同时等待模型决策与环境就绪。横轴为秒。](ch11/figure-11-environment-overlap.svg)
+
+*图 11-38　模型加速后，环境创建成为工具开始执行前的等待来源。两种情况都在零时刻开始创建环境；工具须同时等待模型决策与环境就绪。横轴为秒。*
+
+V4.1 会话在等待工具时，同时保留两组状态：操作系统管理进程、文件与环境，模型服务管理全局 KV 和局部 SWA。恢复任务需要分别准备这两组状态，再在工具结果进入下一轮模型调用时汇合；已执行的外部操作通过查询目标系统确认结果。[^v41-case]环境准备与 KV 取回能够并行时，两者中较晚完成的一项决定恢复等待。
 
 ### 11.5.3 CPU、GPU、内存与服务额度的扩容选择
 
-现在回到章首的 24 秒目标，完成平台选择。此前已经得到三项结果：普通服务每次调用 9 秒，三轮需要 30 秒；快速服务每次 6 秒，三轮需要 21 秒；每轮重建环境把每项环境占用压到 18 GiB·秒，准备工作额外使用 0.3 秒 CPU 时间。
-
-**例 11-9：完成平台设计。** 沿用章首条件：每 0.1 秒到达一项任务，每项执行三轮；工具平台提供 40 核和 640 GiB 内存，模型服务最多同时处理 300 次调用。两种模型处理同一任务集时，最终通过测试的概率均为 95%，且任务能否通过测试与到达时刻无关。普通服务每次 0.010 成本单位，快速服务每次 0.012，均已包含输入、生成与缓存成本。CPU 按每 CPU·秒 0.0001 计费，环境内存按每 GiB·秒 0.0001 计费，均按实际使用量计算。文件持久化成本已包含在每轮工具执行中；平台共有的固定支出在四方案中相同。[^design]
+下面根据章首提出的 24 秒完成期限，选择模型服务与环境管理方案。此前已经得到三项结果：普通服务每次调用 9 秒，三轮需要 30 秒；快速服务每次 6 秒，三轮需要 21 秒；每轮重建环境把每项环境占用压到 18 GiB·秒，准备工作额外使用 0.3 秒 CPU 时间。
+
+**例 11-9：如何组合模型服务与环境生命周期以满足期限、降低成本？** 沿用章首条件：每 0.1 秒到达一项任务，每项执行三轮；工具平台提供 40 核和 640 GiB 内存，模型服务最多同时处理 300 次调用。两种模型处理同一任务集时，最终通过测试的概率均为 95%，且任务能否通过测试与到达时刻无关。普通服务每次 0.010 成本单位，快速服务每次 0.012，均已包含输入、生成与缓存成本。CPU 按每 CPU·秒 0.0001 计费，环境内存按每 GiB·秒 0.0001 计费，均按实际使用量计算。文件持久化成本已包含在每轮工具执行中；平台共有的固定支出在四方案中相同。[^design]
 
 首先排除“只增加 CPU”。三轮工具共 3 秒，即使将它们加速两倍，普通服务仍需 $27+1.5=28.5$ 秒；把工具时间降到零，仍有 27 秒模型调用。CPU 扩容无法使这条串行路径达到 24 秒。
 
@@ -584,7 +580,7 @@
 
 *图 11-39：两种方案均在模型调用期间准备环境，工具执行均为每轮 1 秒。模型调用从每轮 9 秒降至 6 秒，三轮结束时刻从第 30 秒移到第 21 秒，越过 24 秒期限线。*
 
-| 方案 | 完成时间 / s | 累计 CPU 时间 / CPU·秒 | 每项内存 / GiB·秒 | 平均内存 / GiB | 每项提交成本 | 按时通过测试的比例 |
+| 方案 | 完成时间 / s | 累计 CPU 时间 / CPU·秒 | 每项累计内存占用 / GiB·秒 | 平均内存 / GiB | 每项提交成本 | 按时通过测试的比例 |
 | --- | ---: | ---: | ---: | ---: | ---: | ---: |
 | 普通服务＋始终驻留 | 30 | 3 | 60 | 600 | 0.0363 | 0 |
 | 普通服务＋每轮重建环境 | 30 | 3.3 | 18 | 180 | 0.0321 | 0 |
@@ -615,16 +611,16 @@
 
 以下练习沿用 11-1 至 11-10 的资料编号。核心题 11-1、11-2 和 11-10 构成从需求到设计的完整练习；实验原始记录与选做运行入口见[本章配套](ch11/README.md)。
 
-1. **实验 11-1〔核心〕：区分任务与资源对象。** 平台每秒进入 8 项任务，其中一半执行两轮，另一半执行四轮，每轮模型调用 6 秒、工具工作 1 CPU·秒。求模型调用率、平均并发模型调用数与平均使用的 CPU 核数。环境全程占 2 GiB 时，求平均内存；若只在每轮 2 秒准备和 1 秒执行期间存在，再求内存和创建率。
-2. **实验 11-2〔核心〕：从状态选择恢复方式。** 一个环境中有可重新下载的依赖、未保存的编辑缓冲、已持久化的补丁和已提交的外部写入。逐项说明恢复来源。设完整重建需 5 秒、快照恢复需 2 秒，保存快照需 1 秒且可在 9 秒模型调用内完成；画出两种方案到下一次工具完成的路径，再计算它们的环境驻留。
-3. **实验 11-3：预热交点。** 准备需 2 秒，预测命中率为 0.6，错误预测在调用到达时取消，所有准备从开始就占 2 GiB。比较提前 1、2、4 秒的期望等待以及错误准备的内存占用量与时间的乘积。若正确率提高到 0.9，哪些收益变大，哪些浪费减少？
-4. **实验 11-4：迁移的阈值与共享代价。** 等待资源释放需 12 秒，本地执行需 20 秒；迁移时间为未知量 $m$，被迁移任务额外推迟 $m+2$ 秒。分别以新作业最早完成、两个作业新增等待之和最小为目标，求迁移优于等待的条件。说明为何两个目标可以作出不同选择。
-5. **实验 11-5：临时资源的有效窗口。** 每个新实例传输与加载共需 8 秒，之后以每秒 100 个有效 token 的速率工作。三个实例分别存活 10、20、40 秒，求总产出及有效工作时间占比。若准备时间降到 4 秒，比较三个实例各自的产出改善，并解释短存活实例为何更敏感。
-6. **实验 11-6：队列与批次。** 三条样本在 0、6、12 秒到达，验证分别需 10、2、8 秒。先求只有一个执行进程时，按到达顺序处理各条样本的开始和完成时刻；再求使用两个执行进程时，整批最早何时完成。把最后一条样本的验证时间改为 80 秒，解释增加执行进程所能节省的时间为什么会变化。
-7. **实验 11-7：缓存与质量的相关性。** 沿用例 11-7 的 B 服务成本，改为命中时成功率 99%、未命中时 90%。推导关于命中率的成功任务成本，以及在 6 秒内通过测试的任务比例。求至少让 90% 的任务按时通过测试所需的最低命中率，并与原独立概率模型比较。
-8. **实验 11-8：采购方式。** 自建固定支出 1,000、每项新增 0.002、成功率 90%；API 每项 0.012、成功率 98%。两者均有足够能力满足期限。求成功任务平均成本的交点。再将自建处理上限设为每月 80,000 项，讨论交点怎样影响购买决定。
-9. **实验 11-9：期限与执行树。** 沿用例 11-8，将期限改为 14、18、22 秒，分别求按时通过测试的比例。再设计一种在剩余时间不足以完成下一节点时停止的策略，重新计算期望成本，并解释它与仅改变评价期限的差别。
-10. **实验 11-10〔核心〕：迁移平台设计。** 沿用例 11-9，将到达率增加到每秒 15 项、快速服务调用时间降到 2 秒，每项仍三轮、工具仍需 1 秒。CPU、内存、模型调用的并发容量和单价保持原值。比较始终驻留与每轮重建环境，决定应增加哪项资源、至少增加多少，并说明环境策略是否需要改变。最后把到达方式改为每秒同时进入 15 项，画出工具执行的峰值安排。
+1. **实验 11-1〔核心〕：从多轮任务推算模型并发、CPU 与环境内存** 平台每秒接收 8 项任务，其中一半执行两轮，另一半执行四轮。每轮模型调用耗时 6 秒，工具执行消耗 1 CPU·秒。求模型调用率、平均并发模型调用数与平均使用的 CPU 核数。若每个环境在任务全程占用 2 GiB，求平台的平均环境内存占用。再假设环境只在每轮 2 秒的准备阶段和 1 秒的工具执行阶段保留，重新计算平均内存占用，以及每秒需要创建的环境数量。
+2. **实验 11-2〔核心〕：环境重建与快照恢复的耗时和驻留比较** 一个环境中有可重新下载的依赖、未保存的编辑缓冲、已持久化的补丁和已提交的外部写入。逐项说明发生故障后能从哪里恢复，以及哪些内容无法直接恢复。设完整重建环境需要 5 秒，从快照恢复需要 2 秒。保存快照需要 1 秒，可以与耗时 9 秒的模型调用重叠；画出两种方案从开始恢复到下一次工具执行完成的时间线，再计算各自需要保留环境的时长。
+3. **实验 11-3：预热提前量与预测准确率的等待收益和内存代价** 创建环境需要 2 秒，对下一次调用所需环境的预测准确率为 0.6。调用到达后取消错误预测所对应的环境；每个提前准备的环境从准备开始就占用 2 GiB。比较提前 1、2、4 秒的期望等待以及错误准备的内存占用量与时间的乘积。若正确率提高到 0.9，哪些收益变大，哪些浪费减少？
+4. **实验 11-4：不同作业完成目标下，迁移何时优于等待** 等待资源释放需 12 秒，本地执行需 20 秒；迁移时间为未知量 $m$，被迁移任务额外推迟 $m+2$ 秒。分别以新作业最早完成、两个作业新增等待之和最小为目标，求迁移优于等待的条件。说明为何两个目标可以作出不同选择。
+5. **实验 11-5：启动耗时如何影响短期实例的有效产出** 每个新实例传输与加载共需 8 秒，之后以每秒 100 个有效 token 的速率工作。三个实例分别存活 10、20、40 秒，求总产出及有效工作时间占比。若准备时间降到 4 秒，比较三个实例各自的产出改善，并解释短存活实例为何更敏感。
+6. **实验 11-6：验证进程数量与长尾样本如何决定整批完成时间** 三条样本在 0、6、12 秒到达，验证分别需 10、2、8 秒。先求只有一个执行进程时，按到达顺序处理各条样本的开始和完成时刻；再求使用两个执行进程时，整批最早何时完成。把最后一条样本的验证时间改为 80 秒，解释增加执行进程所能节省的时间为什么会变化。
+7. **实验 11-7：缓存命中与成功率相关时的任务成本和按时成功率** 沿用例 11-7 的 B 服务成本，改为命中时成功率 99%、未命中时 90%。推导平均每个成功任务的成本随命中率变化的关系，以及在 6 秒内通过测试的任务比例。求至少让 90% 的任务按时通过测试所需的最低命中率，并与原独立概率模型比较。
+8. **实验 11-8：自建与 API 服务的成功任务成本交点** 自建服务的固定支出为 1,000 个成本单位，每处理一项任务另需 0.002，任务成功率为 90%；API 服务每项任务收费 0.012，成功率为 98%。两者均有足够能力满足期限。求两种服务的平均成功任务成本相等时，各自需要处理的任务量。再将自建处理上限设为每月 80,000 项，讨论交点怎样影响购买决定。
+9. **实验 11-9：任务期限与提前停止如何改变重试成本和成功率** 沿用例 11-8，将期限改为 14、18、22 秒，分别求按时通过测试的比例。再设计一种在剩余时间不足以完成下一节点时停止的策略，重新计算期望成本，并解释它与仅改变评价期限的差别。
+10. **实验 11-10〔核心〕：到达率提高后如何扩容并选择环境驻留策略** 沿用例 11-9，将到达率增加到每秒 15 项、快速服务调用时间降到 2 秒，每项任务仍执行三轮，每轮工具执行仍需 1 秒。CPU、内存、模型调用的并发容量和单价保持原值。比较始终驻留与每轮重建环境，决定应增加哪项资源、至少增加多少，并说明环境策略是否需要改变。最后将到达方式改为每秒有 15 项任务同时到达，画出工具执行的时间线，并标出 CPU 并发占用的峰值。
 
 [^environment]: 本书 [environment-resources 固定计算](../calculations/results/environment-resources-book.md)及[工具子进程记录](../experiments/ch11/11-01/process-resources/README.md)。九组 36 个进程，采样 RSS 积分与观察窗口边界由现有程序复核；控制器与工具子进程属于不同实验，不跨运行相加。
 
@@ -670,7 +666,7 @@
 
 章首平台平均使用 30 个 CPU 核，却保留 600 GiB 环境内存。原因是三轮任务中有 27 秒在等待模型，而工作状态始终存在。在模型调用期间准备环境，把每项任务的累计内存占用从 60 降到 18 GiB·秒，准备工作多用 0.3 秒 CPU 时间，就能释放大量内存。
 
-这项改进利用了完整任务的执行关系。环境管理掌握模型等待区间后，可以把创建提前，把进程寿命缩短到工具真正使用它的阶段；持久文件继续承接跨轮状态。模型服务和环境平台共同安排准备时机，使原来贯穿任务全程的资源占用变成按阶段分配。
+这项改进利用了完整任务的执行关系。环境管理掌握模型等待区间后，可以把创建提前，把进程寿命缩短到工具真正使用它的阶段；持久化文件保存各轮之间需要保留的状态。模型服务和环境平台共同安排准备时机，使原来贯穿任务全程的资源占用变成按阶段分配。
 
 释放内存以后，任务仍需 30 秒，模型调用成为满足 24 秒期限的关键。将调用从 9 秒降到 6 秒，把三轮任务从 30 秒降到 21 秒，也将平均并发模型调用数从 270 次降到 180 次。结合每轮重建环境，现有 40 核、640 GiB 内存和最多 300 次并发模型调用的容量即可支持给定任务流。成本比较随后在满足质量和期限的方案之间完成。
 
--- before/manuscripts/12-端边云协同.md
+++ manuscripts/12-端边云协同.md
@@ -6,7 +6,7 @@
 
 我们先用图片精修建立时间模型，再用语音解释流水线中的先后依赖，最后为一个截图 Agent（根据截图判断下一步并调用工具执行操作的程序）选择部署方案。图片精修需要计算完整成片何时返回，语音需要分析音频能否连续播放，Agent 则要累计每轮通信与执行的开销。三类任务共用一套分析方法：先计算处理和传输各需多久，再确定哪些步骤必须等待，最后加入连接建立、排队和故障恢复等实际开销。
 
-## 12.1 从模型耗时到完整交互
+## 12.1 从模型运行时间到完整交互时间
 
 ### 12.1.1 图片精修
 
@@ -28,7 +28,7 @@
 
 上传的最后一个字节离开发送端后，还要传播到服务器；成片返回也要传播一次。两次单向传播合计为 $R$。传播时间由路径决定，发送时间由文件大小与速率决定，因此缩小文件只改变发送项。这里的 $B_u,B_d$ 表示有效载荷的传输速率，连接启动和反馈等待将在 12.3 中单独加入。
 
-**例：更快的模型能缩短多少等待？** 上行 20 Mbit/s，下行 100 Mbit/s，$R=0.1$ s，$T_c=0.3$ s。本章 MB 与 Mbit 使用十进制；MiB 为 $2^{20}$ bytes。代入得
+**例：图片精修的上传时间如何限制模型加速收益？** 上行 20 Mbit/s，下行 100 Mbit/s，$R=0.1$ s，$T_c=0.3$ s。本章 MB 与 Mbit 使用十进制；MiB 为 $2^{20}$ bytes。代入得
 
 $$
 T_{\mathrm{serial}}=\frac{30\times8}{20}+0.1+0.3+\frac{5\times8}{100}
@@ -53,7 +53,7 @@
 
 接下来考虑各步骤能否同时进行。假设原图分成三块后可以逐块独立处理，每块上传需 4 秒、处理需 0.1 秒；三块输出分别为 1、2、2 MB，回传依次需 0.08、0.16、0.16 秒。第一块上传后，其处理和回传可以与第二块上传重叠；第二块同理。最后一块仍要等到第 12 秒才发完，之后再经过处理、回传和传播，完整成片约在 12.4 秒返回用户。与原来的 12.8 秒相比，前两块的处理与回传在后续上传期间就已完成，不再额外增加等待；总计 12 秒的上传时间仍然需要。[^stream]
 
-图 12-3 把省下的时间标在同一时间轴上：看前两块时，处理与回传都发生在后续上传期间；看最后一块时，上传结束后的等待仍然保留。
+图 12-3 和图 12-4 用相同的时间轴比较两种安排：看前两块时，处理与回传都发生在后续上传期间；看最后一块时，上传结束后的等待仍然保留。
 
 ![分块计算怎样与后续上传重叠](ch12/figure-12-2-overlap.svg)
 
@@ -79,7 +79,7 @@
 
 其中，$c_i$、$u_i$、$r_i$ 分别表示处理完成、发送完成和到达的时刻。第一个最大值表示模型既要等输入，也要等上一块释放处理器；第二个最大值表示发送既要等计算，也要等出口空闲。这两个等待来源分别是数据依赖和资源竞争。
 
-**例：平均传输速率足够，为什么还会停顿？** 脉冲编码调制（PCM）逐次保存音频幅值采样；24 kHz 表示每秒 24000 次采样，单声道表示一条采样通道，16-bit 表示每个样本占 2 字节。使用 24 kHz、单声道、16-bit PCM，每块 20 ms、960 bytes。处理一块需 12 ms，发送需 1 ms，通常传播 5 ms。第一块在 20 ms 采集完，于 38 ms 到达；缓冲 40 ms 后在 78 ms 播放。第二块在 58 ms 到达，于 98 ms 接着播放。每块的处理时间比采集间隔短 8 ms，发送也只需 1 ms，因此处理和发送都能跟上采集速度。
+**例：网络延迟抖动为何会耗尽音频播放缓冲？** 脉冲编码调制（PCM）逐次保存音频幅值采样；24 kHz 表示每秒 24000 次采样，单声道表示一条采样通道，16-bit 表示每个样本占 2 字节。使用 24 kHz、单声道、16-bit PCM，每块 20 ms、960 bytes。处理一块需 12 ms，发送需 1 ms，通常传播 5 ms。第一块在 20 ms 采集完，于 38 ms 到达；缓冲 40 ms 后在 78 ms 播放。第二块在 58 ms 到达，于 98 ms 接着播放。每块的处理时间比采集间隔短 8 ms，发送也只需 1 ms，因此处理和发送都能跟上采集速度。
 
 ![第一块音频从采集开始计时：20 ms 就绪，32 ms 处理完，33 ms 发完，38 ms 到达；初始缓冲 40 ms 后于 78 ms 播放](ch12/figure-12-audio-clocks.svg)
 
@@ -109,7 +109,7 @@
 
 增大缓冲只会按比例延后耗尽；提高传输速率或降低音频码率，才能让收到的数据足够播放。短时抖动可以靠缓冲吸收，长期传输速率不足则需要增加带宽或减少数据量。
 
-### 12.1.3 Computer Use
+### 12.1.3 Computer Use：通过界面操作完成任务
 
 Computer Use（计算机界面操作）让程序像用户一样通过截图、点击和输入完成任务。它在观察界面、模型判断、执行操作和再次观察之间循环。它与音频流水的关键区别是：只有上一轮操作完成、界面更新后，才能取得下一张有效截图。模型还没决定点击哪里时，就无法取得点击后的界面。因此，一轮中的等待会在任务中重复累积。
 
@@ -133,7 +133,7 @@
 
 截图版本也直接影响轮数。假设模型读到版本 $v$，随后界面变化，按旧截图作出的操作就可能出错，需要恢复界面并重新截图。把截图版本与动作编号绑定，让执行端在界面变化时重新截图，可以避免这类无效工作。[^author]
 
-12.6 将回到这个任务：前十轮已完成，剩余二十轮需要选择执行位置。届时保留截图大小、终端工作和原路径参数，引入更快的模型服务，比较切换服务后能缩短多少时间。
+第 12.6 节将继续分析这个截图任务：前十轮已完成，剩余二十轮需要选择执行位置。届时保留截图大小、终端工作和原路径参数，引入更快的模型服务，比较切换服务后能缩短多少时间。
 
 ### 12.1.4 本地、附近设备与云端的初步比较
 
@@ -153,7 +153,7 @@
 
 ## 12.2 跨设备分工
 
-### 12.2.1 整模型调用与分阶段执行
+### 12.2.1 完整模型调用与分阶段执行
 
 前面按图片大小估算上传时间，默认传送的是原始输入。将部分模型移到端侧后，这个默认条件就变了。把整个模型放到远端，需要上传输入并下载结果。把编码器（将图片或音频转换为模型使用的数值特征的子网络）留在端侧，则需要上传编码器的输出。这样，即使执行同一个任务，上传的也可能是图片或特征；两者的数据量取决于各自的编码方式。
 
@@ -167,13 +167,13 @@
 
 ![远端编码：先发送压缩图片，在服务器执行视觉编码，再执行语言模型](ch12/figure-12-encoder-remote.svg)
 
-*图 12-9：远端编码：先发送压缩图片，在服务器执行视觉编码，再执行语言模型。*
+*图 12-9：远端编码的执行路径。端侧上传 0.8 MB 压缩截图，远端依次完成解码与预处理、视觉编码和特征接入；最终投影与三组 DeepStack 特征均留在服务器内。6.4 Mbit/s 上行的图片发送时间为 1 秒，编码和排队时间另计。*
 
 ![端侧编码：先执行视觉编码，再发送完整数值特征到语言模型](ch12/figure-12-encoder-local.svg)
 
-*图 12-10：端侧编码：先执行视觉编码，再发送完整数值特征到语言模型。发送对象随计算的放置位置改变。*
-
-**例：编码后上传为何更慢？** 沿用 0.8 MB 截图与 6.4 Mbit/s 上行。视觉编码采用 Qwen3-VL-4B 的固定配置：预处理后 $640\times640$，图像块合并后得到 400 个视觉位置，即用来表示图片内容的 400 个特征向量；这里的“位置”不是文本中的词元，也不是原图的单个像素。最终投影的每个特征向量有 2560 个分量，另有三组同宽 DeepStack 特征。DeepStack 将视觉编码器不同深度的特征送入语言模型不同层，因而要一起传送这些分支输出。BF16 是每个数占 2 字节的浮点格式。每个位置共需保存 $4\times2560=10240$ 个 BF16 数，完整输出为
+*图 12-10：端侧编码的执行路径。预处理和视觉编码移到终端，跨网络的对象变为最终投影与三组 DeepStack 的完整 BF16 特征，共 8.192 MB；远端接收后仍按相同方式接入语言模型。相同上行的特征发送时间为 10.24 秒，本地编码、序列化和转换时间另计。图中数值采用下文的 Qwen3-VL-4B 固定配置。*
+
+**例：上传视觉特征为何可能比上传原图更慢？** 沿用 0.8 MB 截图与 6.4 Mbit/s 上行。视觉编码采用 Qwen3-VL-4B 的固定配置：预处理后 $640\times640$，图像块合并后得到 400 个视觉位置，即用来表示图片内容的 400 个特征向量；这里的“位置”不是文本中的词元，也不是原图的单个像素。最终投影的每个特征向量有 2560 个分量，另有三组同宽 DeepStack 特征。DeepStack 将视觉编码器不同深度的特征送入语言模型不同层，因而要一起传送这些分支输出。BF16 是每个数占 2 字节的浮点格式。每个位置共需保存 $4\times2560=10240$ 个 BF16 数，完整输出为
 
 $$
 S_E=400\times10240\times2=8\,192\,000\ \mathrm{bytes}
@@ -216,7 +216,7 @@
 
 $G(N)>0$ 表示迁移获益。每增加一轮，收益增加 $\Delta t$；准备 $M$ 则把整条收益曲线向下移动。这解释了为什么同一迁移对短任务不划算，对长会话却值得。
 
-**例：迁移怎样收回准备成本？** 需搬运 64 MiB 状态，链路 80 Mbit/s，恢复 1 秒。准备为
+**例：剩余交互多少轮时，迁移状态才能缩短总时间？** 需搬运 64 MiB 状态，链路 80 Mbit/s，恢复 1 秒。准备为
 
 $$
 M=\frac{64\times2^{20}\times8}{80\times10^6}+1\approx7.7\ \mathrm{s}.
@@ -232,7 +232,7 @@
 
 例如，恢复再多花 1 秒，20 轮的净收益立即变成约负 0.7 秒，回本轮数增至 22。若预计后续还有 40 轮，新增这 1 秒之后仍可省约 7.3 秒。部署系统因此需要预测剩余会话长度，并为额外的传输和恢复开销留出时间。
 
-曲线中的“恢复时间”包括让新设备能够继续工作。除了装入缓存，它还要知道任务已经执行到哪一步。语言 KV 绑定模型、前缀和位置；截图 Agent 的进度由已经执行的操作决定。一次点击已经改变界面，即使其应答丢失，也应恢复到点击后的状态。操作编号和提交记录使目的端可以查到既有结果，从下一轮继续。保存这些记录可以避免重复执行操作，就像保存缓存可以避免重复计算。
+曲线中的“恢复时间”包括新设备继续执行前所需的准备工作。除了装入缓存，它还要知道任务已经执行到哪一步。语言 KV 绑定模型、前缀和位置；截图 Agent 的进度由已经执行的操作决定。一次点击已经改变界面，即使其应答丢失，也应恢复到点击后的状态。操作编号和提交记录使目的端可以查到既有结果，从下一轮继续。保存这些记录可以避免重复执行操作，就像保存缓存可以避免重复计算。
 
 连接预热、图编译和模型加载也可用 $N\Delta t-M$ 分析：将准备成本放入 $M$，将后续每次减少的等待放入 $\Delta t$。[^survey] 这些准备是否值得，取决于后续调用省下的时间能否抵消一次性的准备时间。
 
@@ -240,7 +240,7 @@
 
 会话迁移只在更换设备时传送一次状态。另一种分工方式是让多台设备一直共同执行模型，这时通信会在运行中反复发生。按阶段切分模型时，每个输入通常只需在相邻阶段之间传输一次；采用张量并行时，每层计算都要通信。链路从卡间互联换成局域网后，频繁同步会把微小的单次延迟累积成主要成本。
 
-以 36 层 Qwen3-8B 的 张量并行 TP=2（每层矩阵分给两台设备）为例，考察每层注意力输出和 前馈网络（FFN）输出的两次归约，归约将两台设备的局部结果相加，使后续计算得到完整输出。采用两阶段环算法。每次归约有两次启动，每个阶段启动为 $\alpha$，所以这些归约的单 token 启动时间为
+以 36 层 Qwen3-8B 的张量并行 TP=2（每层矩阵分给两台设备）为例，考察每层注意力输出和前馈网络（FFN）输出的两次归约，归约将两台设备的局部结果相加，使后续计算得到完整输出。采用两阶段环算法。每次归约有两次启动，每个阶段启动为 $\alpha$，所以这些归约的单 token 启动时间为
 
 $$
 T_{\mathrm{start}}=36\times2\times2\alpha=144\alpha.
@@ -262,7 +262,7 @@
 
 确定模型分工后，需要传什么、传多少次，以及哪些计算要等待传输，都已经明确。接下来考察实际发送过程：数据准备好之后，还要看连接是否建立、窗口是否允许发送，以及确认是否及时返回。
 
-## 12.3 实际传输怎样改变时间预算
+## 12.3 实际传输中的额外开销与等待
 
 ### 12.3.1 连接建立与复用
 
@@ -276,7 +276,7 @@
 
 ### 12.3.2 窗口、反馈与有效吞吐
 
-复用连接省下了建链等待，但还没有保证数据能够连续发送。连接建立以后，发送方也必须限制在途数据。拥塞窗口限制已经发送但尚未确认的数据量；接收流量控制根据接收端的缓冲空间，限制发送方还能发送多少数据。确认消息（ACK）告知发送方哪些数据已收到。ACK 到达后，发送方可以继续发送；接收端从缓冲区读取数据后，也会通知发送方新增的可用空间。达到发送上限时，即使链路空闲，发送方仍要等待这些通知。
+复用连接省去了建链等待，连续发送数据还需要足够的发送窗口。连接建立以后，发送方也必须限制在途数据。拥塞窗口限制已经发送但尚未确认的数据量；接收流量控制根据接收端的缓冲空间，限制发送方还能发送多少数据。确认消息（ACK）告知发送方哪些数据已收到。ACK 到达后，发送方可以继续发送；接收端从缓冲区读取数据后，也会通知发送方新增的可用空间。达到发送上限时，即使链路空闲，发送方仍要等待这些通知。
 
 在途量是已经发出、尚未得到确认的数据字节数。若允许在途量为 $W$，往返时间为 $R$，每个往返最多周转约一窗口的数据，因此
 
@@ -284,23 +284,23 @@
 B_{\mathrm{useful}}\le\min\left(B_{\mathrm{path}},\frac{W}{R}\right).
 $$
 
-式中 $B_{\mathrm{path}}$ 是路径可提供的载荷速率，$B_{\mathrm{useful}}$ 是实际有效吞吐。回到 20 Mbit/s、100 ms 的图片路径。要持续填满链路，需要约 250 KB 在途数据。若只有 64 KB 额度，速率上限为 $64000/0.1=640000$ bytes/s，约 5.1 Mbit/s，30 MB 仅发送就至少需约 47 秒。链路每秒本可以传输更多数据，发送方却要等下一批确认到来才能继续发送；扩大可用窗口才会提高这时的实际传输速率。
-
-下面单独采用一个整批确认模型：接收端收齐 64 KB 后才确认，从这批发完到确认返回固定为 100 ms。因此完整周期包含 25.6 ms 发送和 100 ms 反馈，共 125.6 ms；这个具体调度满足前面的 W/R 上界。图 12-16 将这段等待画出来。蓝色时段确实在发送，浅色时段链路虽然空闲，发送方却因为窗口已满而不能继续。物理带宽提高，只能缩短蓝色部分。
+式中 $B_{\mathrm{path}}$ 是路径可提供的载荷速率，$B_{\mathrm{useful}}$ 是实际有效吞吐。对于前述带宽为 20 Mbit/s、RTT 为 100 ms 的图片传输路径，要持续填满链路，需要约 250 KB 在途数据。若只有 64 KB 额度，速率上限为 $64000/0.1=640000$ bytes/s，约 5.1 Mbit/s，30 MB 仅发送就至少需约 47 秒。链路每秒本可以传输更多数据，发送方却要等下一批确认到来才能继续发送；扩大可用窗口才会提高这时的实际传输速率。
+
+下面单独采用一个整批确认模型：接收端收齐 64 KB 后才确认，从这批发完到确认返回固定为 100 ms。因此完整周期包含 25.6 ms 发送和 100 ms 反馈，共 125.6 ms。图 12-16 将这段等待画出来。蓝色时段确实在发送，浅色时段链路虽然空闲，发送方却因为窗口已满而不能继续。物理带宽提高，只能缩短蓝色部分。
 
 ![窗口用完后链路空闲等待确认](ch12/figure-12-9-window.svg)
 
-*图 12-16：固定窗口 64 KB，链路 20 Mbit/s，一批数据发送需 25.6 ms。为单独显示停等，图设接收端收齐一批后统一确认，从这批数据全部发出到收到整批确认再需 100 ms；每批完整周期为 125.6 ms。正文 W/R 是吞吐上界，图中的整批确认还增加了发送时间，因此实际周期更长。*
+*图 12-16：固定窗口 64 KB，链路 20 Mbit/s，一批数据发送需 25.6 ms。为单独显示停等，图设接收端收齐一批后统一确认，从这批数据全部发出到收到整批确认再需 100 ms；每批完整周期为 125.6 ms。*
 
 上图把窗口固定在 64 KB，以便看清停等过程。新连接通常从更小的窗口开始，随着确认到达逐步增加；短请求可能在窗口长大以前就已结束。设初始窗口为十个 1460-byte 段，每轮反馈后翻倍。前四轮累计可发 $14600(1+2+4+8)=219000$ bytes，第五轮累计可达 452,600 bytes。约 355 KB 的语音请求因此要进入第五轮发送。对于 200 ms RTT，几轮反馈就是数百毫秒；短请求的大部分时间也可能花在等待确认上。12.5 将用这一机制解释一次语音性能观察。
 
 窗口解释了发送为什么会暂停。完整时间还取决于究竟需要发送多少字节：网络上传送的除了图片，还有包头和确认。
 
-**例：为什么 12.8 秒还不是图片的实际完成时间？** 在原 30 MB 上传、5 MB 下载上，取每包最多 1168 bytes 载荷，协议与网络头 60 bytes，每个数据包触发一个 92-byte ACK。合计约三万个数据包，头部与确认增加约 $30000\times(60+92)/10^6\approx4.6$ MB，加上这些开销，实际传输量从 35 MB 增至约 39.6 MB。逐包计算发送、确认和后续发送的时刻，完整请求约需 14.4 秒。[^closed]
+**例：协议头与 ACK 如何增加图片传输时间？** 在原 30 MB 上传、5 MB 下载上，取每包最多 1168 bytes 载荷，协议与网络头 60 bytes，每个数据包触发一个 92-byte ACK。合计约三万个数据包，头部与确认增加约 $30000\times(60+92)/10^6\approx4.6$ MB，加上这些开销，实际传输量从 35 MB 增至约 39.6 MB。逐包计算发送、确认和后续发送的时刻，完整请求约需 14.4 秒。[^closed]
 
 与 12.8 秒相比，多出来的约 1.6 秒来自模型中刚加入的工作与等待。单看实际传输的字节也仍不够：ACK 一面占用链路，一面释放后续发送额度，减少它会同时改变两项。
 
-NewReno 是根据丢包与确认调整拥塞窗口的算法，CUBIC 在拥塞避免阶段按时间的立方函数调节窗口；慢启动是连接初期随确认快速增加窗口的阶段。在同一填充与 NewReno 配置下，将立即 ACK 改为每两包确认或最多等待 10 ms，实际传输的数据从约 39.6 MB 降至 38.2 MB，完整请求却从约 14.52 秒增至 14.56 秒，晚约 43 ms。[^ack] 这组对照揭示了反馈的双重作用：少发确认减少传输时间，确认晚到延长窗口等待。最终能否更快完成，要比较省下的传输时间与增加的等待时间。
+NewReno 是根据丢包与确认调整拥塞窗口的算法，CUBIC 在拥塞避免阶段按时间的立方函数调节窗口；慢启动是连接初期随确认快速增加窗口的阶段。在相同的数据填充方式与 NewReno 配置下，将立即 ACK 改为每两包确认或最多等待 10 ms，实际传输的数据从约 39.6 MB 降至 38.2 MB，完整请求却从约 14.52 秒增至 14.56 秒，晚约 43 ms。[^ack] 这组对照揭示了反馈的双重作用：少发确认减少传输时间，确认晚到延长窗口等待。最终能否更快完成，要比较省下的传输时间与增加的等待时间。
 
 ACK 改变窗口继续发送的时刻，拥塞控制则决定窗口本身如何变化。比较拥塞控制算法时，应先找出请求经历了哪个阶段。相同路径的大图比较中，NewReno 与 CUBIC 完整响应均约为 14.5 秒；窗口轨迹显示 CUBIC 一直在慢启动。[^ack] 本次请求在进入立方拥塞避免阶段之前就已结束，因此观察到的是启动阶段的行为。要研究长流阶段，就延长负载，使窗口经历拥塞事件并进入相应控制过程。
 
@@ -320,7 +320,7 @@
 
 *图 12-18：逐流有序交付：音频在第 3 s 收齐后立即交付，图片仍于第 7 s 交付。两图具有相同的发送、到达与恢复时刻；改变的是跨流等待依赖。*
 
-HTTP/2 将多个流放入一个有序 TCP 字节流，前面的缺口会阻挡后续字节。QUIC 以流为单位保持有序，其他流已经连续到达的字节可独立交付。[^quic] 这与音频播放递推具有相同结构：将一个覆盖全部数据的等待条件，分解成各流自己的等待条件，已经满足条件的流就能向应用交付数据。
+HTTP/2 将多个流放入一个有序 TCP 字节流，前面的缺口会阻挡后续字节。QUIC 以流为单位保持有序，其他流已经连续到达的字节可独立交付。[^quic] 这与音频播放的递推关系采用相同的分析方法：将一个覆盖全部数据的等待条件，分解成各流自己的等待条件，已经满足条件的流就能向应用交付数据。
 
 这两种机制分别回答“何时发送”和“何时交给应用”。业务还要回答第三个问题：“数据交给应用时是否仍有价值？”音频的预定播放时段和截图版本给出了答案，下一节把这些期限与无线传输结合起来。[^http]
 
@@ -332,7 +332,7 @@
 
 端到端 ACK 告知远端发送方哪些传输已收到；介质访问控制（MAC）层组织单条无线链路的发送，MAC ACK 确认本跳无线帧。承载端到端 ACK 的无线帧，也会进行自己的 MAC 确认。因此，即使端到端 ACK 很短，也需要等待信道空闲，并完成本跳确认。
 
-**例：短帧为何仍占不少时间？** 正交频分复用（OFDM）把数据分到多个相互正交的子载波上传送；PPDU 是物理层协议数据单元，包含物理层前导、头部和承载的数据，代表一次无线发送的完整物理层内容；SIFS 是连续交换之间的短帧间间隔。取无聚合的 OFDM 参考交换，数据速率 54 Mbit/s，MAC ACK 速率 6 Mbit/s。数据包交换包含 34 μs 接入等待、208 μs 数据帧、16 μs SIFS 和 44 μs MAC ACK，共 302 μs。承载端到端 ACK 时，数据帧缩至 40 μs，其余三项保持不变，共 134 μs。[^air]
+**例：短 ACK 帧的无线接入与确认开销有多大？** 正交频分复用（OFDM）把数据分到多个相互正交的子载波上传送；PPDU 是物理层协议数据单元，包含物理层前导、头部和承载的数据，代表一次无线发送的完整物理层内容；SIFS 是连续交换之间的短帧间间隔。取无聚合的 OFDM 参考交换，数据速率 54 Mbit/s，MAC ACK 速率 6 Mbit/s。数据包交换包含 34 μs 接入等待、208 μs 数据帧、16 μs SIFS 和 44 μs MAC ACK，共 302 μs。承载端到端 ACK 时，数据帧缩至 40 μs，其余三项保持不变，共 134 μs。[^air]
 
 ![短数据帧也需要相同的无线交换固定开销](ch12/figure-12-11-wireless.svg)
 
@@ -348,7 +348,7 @@
 
 保持数据与恢复轨迹不变，将端到端 ACK 数减半，能释放约 $15400\times134\ \mathrm{\mu s}\approx2.1$ 秒空口。上一节已经说明，反馈延迟又会增加窗口停等。因此，需要重新计算各包的发送与确认时刻：减少 ACK 腾出了空口时间，但等待确认又可能推迟后续发送。两者共同决定完整请求何时结束。
 
-TACK 是减少无线确认开销的一种反馈机制。它将日常累计确认与需要即时反馈的事件分开：前者通过合并来减少交换次数，后者及时返回，使发送方尽快继续发送或重传丢失的数据。[^wireless-survey] 这种分工直接对应两种成本，而非单纯追求最少 ACK。
+TACK 是减少无线确认开销的一种反馈机制。它将日常累计确认与需要即时反馈的事件分开：前者通过合并来减少交换次数，后者及时返回，使发送方尽快继续发送或重传丢失的数据。[^wireless-survey] 这种分工同时减少确认报文的发送开销和等待反馈的时间。
 
 ### 12.4.2 业务时限、过期数据与取消
 
@@ -366,9 +366,9 @@
 
 ### 12.4.3 双路径选择、分流与故障切换
 
-Wi-Fi 与蜂窝提供两条接入路径。分流把不同字节交给不同路径，目标是同时使用资源；复制把同一字节交给两条路径，目标是更早取得一份有效结果。这两种方法的数据量和完成条件不同，需要分别计算。
-
-**例：如何分流 30 MB 图片？** 两条独立恒速路径分别为 20 和 10 Mbit/s，将比例 $x$ 分给快路，其余分给慢路。两部分齐备才完成，因此
+Wi-Fi 与蜂窝网络提供两条接入路径。分流把不同字节交给不同路径，目标是同时使用资源；复制把同一字节交给两条路径，目标是更早取得一份有效结果。这两种方法的数据量和完成条件不同，需要分别计算。
+
+**例：双路径如何分配图片数据以缩短传输时间？** 两条独立恒速路径分别为 20 和 10 Mbit/s，将比例 $x$ 分给快路，其余分给慢路。两部分齐备才完成，因此
 
 $$
 T_{\mathrm{split}}=\max\left(\frac{xS}{B_1},\frac{(1-x)S}{B_2}\right).
@@ -404,7 +404,7 @@
 
 ### 12.5.1 初始测量与计时范围
 
-Queqiao 是本书采用作者归档实现进行分析的传输系统。本节计时对象是一个固定音频请求从发出到收齐结果的路径。现在用 Queqiao 的语音传输案例检验前面的模型。固定音频文件约 355 KB，链路 333 Mbit/s，往返约 200 ms，模型处理约需 30 ms；计时从发起请求开始，到收到结果结束。理想发送时间约为 8.5 ms，加上传播与处理约为 240 ms。最初的直接调用却超过一秒，而Queqiao 约需三百毫秒。
+本节使用作者归档的 Queqiao 传输系统实现开展分析。本节计时对象是一个固定音频请求从发出到收齐结果的路径。现在用 Queqiao 的语音传输案例检验前面的模型。固定音频文件约 355 KB，链路 333 Mbit/s，往返约 200 ms，模型处理约需 30 ms；计时从发起请求开始，到收到结果结束。理想发送时间约为 8.5 ms，加上传播与处理约为 240 ms。最初的直接调用却超过一秒，而 Queqiao 约需三百毫秒。
 
 这个差距有两种可能的解释。一种把优势归于传输机制本身；另一种认为直接调用的大部分时间花在了连接准备、窗口增长和等待发送上。两者给出不同预测：若连接准备与发送等待是主要原因，充分调优直接路径后，请求时间应当接近前面按数据量、计算时间和必要往返算出的最短时间；若协议机制本身仍带来很大的优势，差距会继续存在。
 
@@ -427,9 +427,9 @@
 
 ![保持连接并调优后，同一固定音频的直接路径与 Queqiao 中位数分别为 240.9、236.5 ms](ch12/figure-12-queqiao-tuned.svg)
 
-*图 12-24：保持连接并调优后，同一固定音频的直接路径与 Queqiao 中位数分别为 240.9、236.5 ms。与前图采用相同纵轴，两条路径均接近数据发送、必要往返和处理的总预算。*
-
-**后续实验设计。** 上面的归档记录给出了整体配置对照。要继续区分各项设置的贡献，可以按下面的步骤设计单因素实验。从同一个客户端开始，只改变连接是否复用，测握手结束和首字节发送；再保持连接状态，只改变窗口设置，测在途量与 ACK 到达后的发送；最后固定这些设置，改变发送节奏，测链路空闲区间。每一步既记录总时间，也记录握手、发送和确认的时刻，就能说明时间究竟省在了哪里。
+*图 12-24：保持连接并调优后，同一固定音频的直接路径与 Queqiao 中位数分别为 240.9、236.5 ms。与前图采用相同纵轴，两条路径均接近数据发送、必要往返和处理所需的总时间。*
+
+**用单因素实验区分连接复用、多流与确认策略的收益** 上面的归档记录给出了整体配置对照。要继续区分各项设置的贡献，可以按下面的步骤设计单因素实验。从同一个客户端开始，只改变连接是否复用，测握手结束和首字节发送；再保持连接状态，只改变窗口设置，测在途量与 ACK 到达后的发送；最后固定这些设置，改变发送节奏，测链路空闲区间。每一步既记录总时间，也记录握手、发送和确认的时刻，就能说明时间究竟省在了哪里。
 
 ![后续单因素实验的设计：保持相同文件、路径和其他设置，每次只改变一个因素，配对记录中间事件与总完成时间](ch12/figure-12-experiment-design.svg)
 
@@ -453,7 +453,7 @@
 
 ### 12.6.1 地域、数据与服务位置
 
-回到 12.1 的 30 轮截图任务。前十轮已完成，剩余二十轮要选择执行位置；从现在起，任务期限为 45 秒。无论选择哪种方案，此前已经花掉的时间都相同，比较的是剩余工作。原服务每轮 3.5 秒，继续执行需要 70 秒，因此需要换一种执行方案。
+继续分析第 12.1 节的 30 轮截图任务。前十轮已完成，剩余二十轮要选择执行位置；从现在起，任务期限为 45 秒。无论选择哪种方案，此前已经花掉的时间都相同，比较的是剩余工作。原服务每轮 3.5 秒，继续执行需要 70 秒，因此需要换一种执行方案。
 
 仍使用每轮 0.8 MB 截图、0.3 秒终端工作，原云路径上行 6.4 Mbit/s、RTT 0.2 秒。现在引入三个达到相同任务质量的模型服务：端侧每轮 2.9 秒，附近工作站 1.5 秒，云地域 0.8 秒。各服务的初始化和上下文准备合计列在“一次准备”中。先采用无排队、无故障、控制回复发送时间为零的教学条件。
 
@@ -469,7 +469,7 @@
 
 ### 12.6.2 时限、成本与恢复的联合比较
 
-**例：最快的模型是否给出最佳部署？** 剩余二十轮的时间为
+**例：状态迁移与剩余交互轮数如何改变端边云部署选择？** 剩余二十轮的时间为
 
 $$
 \begin{aligned}
@@ -481,7 +481,7 @@
 
 云端每轮比附近工作站少算 0.7 秒，二十轮省 14 秒，准备再省 2 秒；但上传每轮多 0.92 秒，传播多 0.18 秒，二十轮多等 22 秒。合计下来，云端反而慢 6 秒。把时间差拆开，便能看到更快的模型怎样因每轮通信而失去优势。
 
-图 12-26 将各项差额放回完整任务。云端的绿色计算段更短，蓝色上传段却更长；应比较整条横条的终点，而不是只看模型计算那一段。
+图 12-26 至图 12-28 将各项差额放回完整任务。云端的绿色计算段更短，蓝色上传段却更长；应比较整条横条的终点，而不是只看模型计算那一段。
 
 ![不同执行设备的时间组成](ch12/figure-12-14-budgets.svg)
 
@@ -521,9 +521,9 @@
 
 同一会话的矩阵计算量从约 $3.0\times10^{14}$ 降至 $6.0\times10^{13}$ FLOPs。按每秒 $10^{14}$ FLOPs 的计算速率、每 GPU 小时 1.2 成本单位计费，可以省下约 0.00079 成本单位。保存约 465 MB 状态，按每 GB 小时 0.05 计费，每秒成本约 $0.465\times0.05/3600\approx6.5\times10^{-6}$。两者相除，保存约 120 秒就花完本次省下的计算成本。这种盈亏平衡的计算方法与前面的迁移时间比较相同：复用收益必须覆盖保存或搬运状态的成本。
 
-保存状态既能减少正常执行中的重复计算，也能减少故障后的重做。下面回到附近工作站上的截图任务，比较同一次断连如何因进度保存方式不同而产生不同开销。附近工作站完成一轮任务需要 1.9 秒。设第十轮已执行完，但确认尚未返回时连接中断，恢复连接需要 1 秒。若前九轮进度已经提交，只重做未确认的第十轮，额外花 2.9 秒，任务由 41 秒增至 43.9 秒；若十轮都要重做，额外花 20 秒，任务变为 61 秒。仍然使用同一台工作站，能否保留进度却决定了任务能否按时完成。
-
-图 12-30 标出了两种恢复方式真正不同的地方：相同的断连事件之后，一种只重复最后一轮，另一种重复此前十轮。恢复是否便宜，首先取决于哪些结果已经可靠保存。
+保存状态既能减少正常执行中的重复计算，也能减少故障后的重做。下面以在附近工作站执行的截图任务为例，比较同一次断连如何因进度保存方式不同而产生不同开销。附近工作站完成一轮任务需要 1.9 秒。设第十轮已执行完，但确认尚未返回时连接中断，恢复连接需要 1 秒。若前九轮进度已经提交，只重做未确认的第十轮，额外花 2.9 秒，任务由 41 秒增至 43.9 秒；若十轮都要重做，额外花 20 秒，任务变为 61 秒。仍然使用同一台工作站，能否保留进度却决定了任务能否按时完成。
+
+图 12-30 标出了两种恢复方式真正不同的地方：相同的断连事件之后，一种只重复最后一轮，另一种重复此前十轮。恢复开销有多大，首先取决于哪些结果已经可靠保存。
 
 ![保留提交记录可以少做哪些工作](ch12/figure-12-16-recovery.svg)
 
@@ -553,19 +553,15 @@
 
 第 1 章通过容量、运算和读取时间判断设备能否承载模型；本章进一步分析通信、执行顺序和恢复过程，判断它能否按时完成任务。选择部署方案时，需要依次回答：任务包含哪些工作，哪些步骤必须等待，实际执行还会增加哪些开销，以及条件变化后哪种方案更合适。
 
-### 12.6.4 约束改变后，重新选择完整系统
-
-把全书的思想实验放回完整任务。设一次固定轨迹中，模型执行合计 8 秒，工具与网络等不可重叠部分合计 2 秒。即使模型整体加速 10 倍，任务也只能从 10 秒降至 2.8 秒，约快 3.57 倍；[^core-calculation]模型耗时趋近于零时，下界仍是 2 秒。这里假定的是模型阶段整体加速，并非第 4 章的 HBM 流量比；实际设备还必须先验证 KV、计算和通信约束。
-
-第二轮可以改变系统组织，但每个新选择都要列出代价。把执行移到本地可能减少网络往返，却需要满足容量、功耗和模型质量；保留环境能够减少创建等待，却占用驻留资源；压缩上下文能够缩短后续生成，却增加总结工作并可能损失信息。选择更长推理或更多分支，则应作为新的应用策略，以任务成功率和总成本衡量，不能与固定轨迹的加速混为一个结论。
-
-对于固定权重硬件，还要分别检查两条相互独立的条件：可写状态的容量和带宽能否承接目标负载，以及固定版本的使用寿命能否覆盖部署投入。权重供给足够快，不保证长上下文足够快；上下文可以自由编排，也不保证权重版本可以随时替换。
-
-V4／V4.1 与固定权重实验可以放在同一条会话中检验。若权重供给几乎免费，而可写 KV 仍昂贵，跨层共享、低精度状态和缓存恢复仍有价值；但 CED 省去的是一部分输入计算，其收益应按改变后的计算与访存比例重算。若进一步令 KV 容量和带宽都很便宜，第二轮可以考虑更多独立全局 KV、更高精度或更大的候选池，却仍须支付注意力计算、Prefill、网络启动和工具执行的代价，不能预言模型一定恢复全注意力。[^v41-case]
-
-这一案例的完整判断应回到同一质量目标。先记录输入和输出长度、状态命中与恢复条件，再沿设备、运行时、网络和服务核算时间；允许更长上下文或更多分支时，另列新的任务成功率与成本。V4.1 限制部分逐层表示和选择自由度，又保留更细历史粒度与独立查询，说明系统设计可以在多个维度重新分配资源。总体能力改善和局部近似可能有损，并不互相排斥。
-
-从片上存储到端边云，设计选择始终依赖对完整执行过程的了解。注意力的归约关系使中间矩阵可以按块处理，重复的模型结构使执行计划可以复用，前缀与分支关系使上下文可以共享，工具等待则为状态换出和环境准备提供了时间。每一项改进都把模型或任务的信息用于其他层次，重新选择数据保存、执行边界或交接方式。固定任务计算直接收益，再根据新约束调整模型与部署，就能说明性能提升来自哪里，以及下一步应改变什么。
+### 12.6.4 资源条件变化后的系统调整
+
+将前文的硬件与执行方式比较用于完整任务。设一次固定轨迹中，模型执行合计 8 秒，工具与网络等不可重叠部分合计 2 秒。即使模型整体加速 10 倍，任务也只能从 10 秒降至 2.8 秒，约快 3.57 倍；[^core-calculation]模型耗时趋近于零时，下界仍是 2 秒。
+
+要继续缩短剩余两秒，可以改变系统组织。把执行移到本地可减少网络往返，保留环境可减少创建等待；相应增加的本地设备与驻留空间，则进入新的成本预算。前面的部署算例已经给出了比较方法：先求满足质量与期限的方案，再比较每个完成任务的总成本。
+
+V4 到 V4.1 展示了模型侧的同类调整。跨层共享减少全局 KV 的重复保存，使模型可以用更细的历史粒度；CED 减少输入阶段反复经过解码器的工作，缓存恢复则缩短多轮会话重新开始的路径。[^v41-case]这些改变分别作用于容量、输入计算和恢复等待，可以沿同一任务逐项计入预算。
+
+从片上存储到端边云，设计选择始终依赖对完整执行过程的了解。注意力的归约关系使中间矩阵可以按块处理，重复的模型结构使执行计划可以复用，前缀与分支关系使上下文可以共享，工具等待则为状态换出和环境准备提供了时间。每一项改进都把模型或任务的信息用于其他层次，重新安排数据保存位置、计算划分和交接方式。先保持任务不变，计算直接收益，再根据新约束调整模型与部署，就能说明性能提升来自哪里，以及下一步应改变什么。
 
 ![图 12-32 固定任务轨迹中的加速上限。模型从 8 秒缩短至 0.8 秒，其他串行阶段保持 2 秒；第三行表示模型时间趋近于零的理想下界。](ch12/figure-12-task-counterfactual.svg)
 
@@ -577,29 +573,29 @@
 
 **12-1　图片精修的传输与计算预算〔延伸，★〕。**
 
-(a) 分别取上行 10、20、100 Mbit/s，计算原方案、处理十倍加速和输入减半方案的完成时间。压缩新增编解码仍为 0.15 秒。
-
-(b) 求压缩恰好回本的上行速率。若只有 0.3 秒处理中的 0.2 秒可以加速，十倍加速后的完整时间是多少？
+(a) 分别取上行 10、20、100 Mbit/s，分别计算三种方案的完成时间：保持原方案、将图像处理速度提高到原来的十倍，以及将输入大小减半。输入压缩所增加的编解码耗时仍为 0.15 秒。
+
+(b) 求压缩节省的传输时间恰好等于新增编解码时间时的上行速率。若只有 0.3 秒处理中的 0.2 秒可以加速，十倍加速后的完整时间是多少？
 
 (c) 把图像分为三个可独立处理的块，画出上传、处理与回传的重叠。标出使整项完成时间无法继续下降的工作。
 
-**12-2　远端执行的延迟收支〔核心，★〕。**
-
-(a) 复算 0.8 MB 降至 0.2 MB、新增编码 30 ms 时，30 轮截图任务的节省。
+**12-2　远端执行节省与增加的时间〔核心，★〕。**
+
+(a) 将每轮截图从 0.8 MB 压缩到 0.2 MB，每轮新增 30 ms 编码时间。复算完成 30 轮截图任务总共能节省多少时间。
 
 (b) 压缩后需要 38 轮时，任务是否更快？求仍有收益的最大整数轮数。
 
-(c) 将每轮 RTT 增加 100 ms，分别计算原图 30 轮和压缩图 38 轮的总时间。解释为什么同一个 RTT 变化会产生不同的任务增量。
+(c) 将每轮 RTT 增加 100 ms，分别计算原图 30 轮和压缩图 38 轮的总时间。解释为什么同一个 RTT 变化会产生不同的任务耗时增量。
 
 (d) 为截图版本与操作编号设计记录字段，说明怎样识别压缩导致的纠错轮次与旧状态导致的无效轮次。
 
 **12-3　AI 助手的跨设备分工〔核心，★〕。**
 
-(a) 用 $[400,10240]$ BF16、0.8 MB 图片和 6.4 Mbit/s 上行，求本地编码 0.12 秒与远端编码 0.04 秒两种方案的时间差。
-
-(b) 改为 25 GB/s，求使端侧编码有利所需的计算与排队净节省。
-
-(c) 搬运 64 MiB，速率 80 Mbit/s，恢复 1 秒，每轮快 0.4 秒，求 10、20、40 轮的净收益。恢复再增加 1 秒，最少回本轮数如何改变？
+(a) 一张图片占 0.8 MB，编码后得到形状为 $[400,10240]$ 的 BF16 特征张量，上行带宽为 6.4 Mbit/s。本地编码耗时 0.12 秒，远端编码耗时 0.04 秒。比较本地编码后上传特征与上传原图后在远端编码的总耗时。
+
+(b) 将上行带宽改为 25 GB/s，求端侧编码至少需要节省多少计算与排队时间，才能抵消额外的传输时间。
+
+(c) 迁移需要以 80 Mbit/s 传输 64 MiB 状态，再用 1 秒恢复；迁移后每轮执行时间缩短 0.4 秒。分别求剩余 10、20、40 轮时净节省的时间。若恢复时间再增加 1 秒，至少还需执行多少轮，才能收回迁移耗时？
 
 (d) 分别说明保存图片、视觉特征和语言 KV 后，从何处开始重算；说明已提交的操作结果怎样进入恢复过程。
 
@@ -609,13 +605,13 @@
 
 (b) 复算图片传输中包头与 ACK 的总字节数，解释为什么实际传输的字节更少，请求完成时间反而可能更长。
 
-(c) 比较发送优先级与图 12-17 的逐流交付：分别指出资源使用时段和等待依赖发生了什么变化。
-
-(d) 设计仅改变连接复用的匹配实验，列出起止事件及用于解释时间变化的中间事件。
+(c) 比较发送优先级与图 12-18 的逐流交付：分别指出资源使用时段和等待依赖发生了什么变化。
+
+(d) 设计一组只改变连接复用方式、其他条件相同的对照实验，列出起止事件及用于解释时间变化的中间事件。
 
 **12-5　ACK 频率与空口效率〔延伸，★〕。**
 
-(a) 数据交换 302 μs、反馈交换 134 μs，数据交换 30,800 次，求 ACK 减半释放的空口时间。
+(a) 一次数据帧交换耗时 302 μs，一次确认帧交换耗时 134 μs。共发生 30,800 次数据帧交换，原先每次都单独确认。将 ACK 数量减半后，能释放多少空口时间？
 
 (b) 画出发送方因等待聚合 ACK 而暂停发送的时间线，解释释放空口与提前交付之间的关系。
 
@@ -627,17 +623,17 @@
 
 (b) 加入 24 Mbit/s 共同出口，求新的资源下界，并指出增加哪一段容量能够降低它。
 
-(c) 两条接入路径失败概率为 0.1、0.05，共同端点为 0.02，求复制后失败概率；两条接入路径完全可靠后，该概率是多少？
-
-(d) 八块 640-byte 音频全量复制，求新增字节，并说明接收端怎样避免重复播放。
+(c) 两条接入路径的失败概率分别为 0.1、0.05，共同端点的失败概率为 0.02。按正文的独立故障假设，求向两路各发送一份完整副本后，任务仍然失败的概率；两条接入路径完全可靠后，该概率是多少？
+
+(d) 将八块音频各复制一份，每块为 640 bytes，求新增的传输字节数，并说明接收端怎样避免重复播放。
 
 **12-7　Queqiao 的单因素比较〔核心，★★〕。**
 
-(a) 计算新建与调优保持连接两组中位数之比，说明“主要差距来自连接准备与发送等待”这一解释给出了什么预测。
-
-(b) 用约 355 KB、333 Mbit/s、200 ms RTT、30 ms 处理求理想时间，指出最初一秒级结果中待解释的大致有多少等待时间。
-
-(c) 设计窗口大小的单因素实验，说明如何交替运行，以及何种在途量、ACK 和发送轨迹支持窗口解释。
+(a) 分别计算每次新建连接和调优后保持连接这两组实验中，直接路径与 Queqiao 路径耗时中位数的比值，说明“主要差距来自连接准备与发送等待”这一解释给出了什么预测。
+
+(b) 设数据量约为 355 KB，传输速率为 333 Mbit/s，RTT 为 200 ms，处理耗时为 30 ms，求理想完成时间。与最初约一秒的实测耗时比较，估算还有多少等待时间需要解释。
+
+(c) 设计窗口大小的单因素实验，说明如何交替运行不同窗口配置，以及应观察到怎样的在途数据量、ACK 与发送时序，才能支持“窗口限制了传输速度”的解释。
 
 (d) 使用配套音频记录，画出一个块从生成到播放的各个时刻，说明如何区分生成不及时、传输延迟与本地调度等待。
 
@@ -647,9 +643,9 @@
 
 (b) 改为剩余十轮、期限 23 秒，选择按时且成本最低的方案；再把云上传单价加倍，重新选择。
 
-(c) 二十轮中十轮上行 6 Mbit/s、十轮 10 Mbit/s，求云端的总时间，与恒定 8 Mbit/s 比较并解释差额。
-
-(d) 对十轮任务，设第八轮已执行完时断连，恢复 2 秒。分别计算保留前七轮、重做第八轮，以及丢失全部八轮两种情形的剩余与总时间。这次断连发生在远程服务路径，端侧继续正常执行；恢复成本包含在基础费率中。恢复期间连接与服务状态保留，初始化只做一次；成本按实际执行轮次及发送字节计收，按原上传单价重新判断最低成本的可行方案。
+(c) 二十轮交互中，十轮的上行速率为 6 Mbit/s，另外十轮为 10 Mbit/s，求云端的总时间，与恒定 8 Mbit/s 比较并解释差额。
+
+(d) 对十轮任务，设第八轮已执行完时断连，恢复 2 秒。分别考虑两种恢复情况：已保留前七轮的进度，只需重做第八轮；前八轮的进度全部丢失，需要全部重做。计算两种情况下完成剩余工作所需的时间，以及整项任务的总耗时。这次断连发生在远程服务路径，端侧继续正常执行；恢复成本包含在基础费率中。恢复期间连接与服务状态保留，初始化只做一次；成本按实际执行轮次及发送字节计收，按原上传单价重新判断最低成本的可行方案。
 
 [^raw]: [RAW 图片精修案例与条件预算](../case-studies/raw-retouching.md)。RAW／DNG 与信息保留由归档的华为专业模式及 Adobe 材料支持；本章 30 MB／5 MB／0.3 秒为教学输入，不是历史服务规格。
 
@@ -701,7 +697,7 @@
 
 ## 本章小结
 
-完整交互应从三个层次分析。先根据数据量、计算量和传播距离求出各步骤所需时间，再以执行依赖确定哪些时间相加、哪些可以重叠，最后加入连接、窗口、竞争和恢复来解释实际等待。图片的 12.8 秒预算、语音的逐块递推和 Agent 的逐轮累积，分别展示了这三个层次怎样配合。
+完整交互应从三个层次分析。先根据数据量、计算量和传播距离求出各步骤所需时间，再以执行依赖确定哪些时间相加、哪些可以重叠，最后加入连接、窗口、竞争和恢复来解释实际等待。图片的 12.8 秒完成时间、语音的逐块递推和 Agent 的逐轮累积，分别展示了这三个层次怎样配合。
 
 模型分工改变了跨设备传输的数据形式和次数。把计算移到端侧，可能把紧凑图片换成更大的特征；把模型拆到多个设备，可能把一次传输换成逐层同步。比较时先扣除两种方案中相同的耗时，再比较省下的计算时间与增加的通信时间，便能找到收益来自哪里。
 
@@ -711,4 +707,4 @@
 
 最终选择先满足质量和期限，再比较成本。即使平均计算速度和传输速率足够，任务仍可能因慢速轮次或丢失进度超期；按时完成的概率与恢复后的重做量，因而是部署模型的组成部分。
 
-再看第一章提出的模型应用，开发者通过上下文表达任务，执行系统把它展开为计算、状态和数据传输。现在，保留一段历史、增加一个候选分支、提前准备工具环境或迁移一次会话，都有了可以追踪的资源代价。理解这些代价，也就能够识别哪些通用执行开销可以利用应用信息消除，并用具体的质量、时间和成本选择实现。这是学习 AI Infra 带来的系统设计能力。
+再看第一章提出的模型应用，开发者通过上下文表达任务，执行系统据此安排计算、保存状态并传输数据。现在，保留一段历史、增加一个候选分支、提前准备工具环境或迁移一次会话，都有了可以追踪的资源代价。理解这些代价，也就能够识别哪些通用执行开销可以利用应用信息消除，并根据任务质量、完成时间和总成本选择实现方式。这是学习 AI Infra 带来的系统设计能力。
--- before/outlines/editorial-notes.md
+++ outlines/editorial-notes.md
@@ -1,11 +1,28 @@
 # 编辑笔记
 
 这些备注供作者扩写和核对资料使用。读者在正文里跟着问题、计算和设计选择走；版本核对、缺失参数和制作要求集中放在这里。各章结论仍需在采用处给出处，不能用这份笔记替代正文证据。
+## 例题标题、粗体提示与题干表达
+
+作者要求（2026-09-10）：全书系统检查例题名称、粗体文字及练习题干，使标题具有描述性、中文表达流畅。
+
+例题和练习标题要说明分析对象及要解决的问题、比较的方案或关键机制。设备数量、数据规模等题设参数放在题干中；除非参数变化本身就是讨论对象，不用“固定八个 worker 的配比”“十轮公共前缀”等参数清单充当标题。粗体段首要说明该段内容，不用“问题”“推导”“结果解释”“改变条件”等通用标签反复分割叙述。术语首次定义时的加粗可以保留；普通“解”“解答”用正文表达即可。
+
+题干按条件、执行顺序、待求量和比较问题展开。动作写明执行者与先后依赖，数值写明对象和单位；两组独立情景分段说明。引用已有例子时写明例题号、节号或具体任务及必要条件，不用“回到……”代替指向。避免“产生的 GiB·s”“求日历时间”“恢复 128 个目标”等压缩表达，明确是在求占用量与保留时间的乘积、实际完成天数，还是改变目标数量。“快四倍”等歧义表述应写为“速度提高到原来的四倍”。改写时保持数值、计算含义、编号和资料引用一致。
+
 ## 术语必须在使用处可理解
 
 作者要求（2026-09-10）：名词一定要有定义；读者不容易找到此前定义时，宁可在后续使用处简短重复一遍。
 
 专业名词、缩写、框架实现名称和测量指标首次出现时，要说明它是什么、在当前过程里做什么；仅给英文全称不算完成定义。跨章、跨节或独立试读片段再次使用时，若前文定义不易定位，就地重述一句，不让读者依赖脚注、外部链接或术语表才能读懂正文。图、表、实验结论中的名称也适用这条要求。比如“数学后端”“FlashAttention 后端”须先说明是同一注意力接口内部的两种执行实现，再解释保存完整中间矩阵与分块在线计算的差别。
+
+## 限定说明与正文衔接
+
+作者要求（2026-09-10）：系统清理全书的 hedging 与适用范围插话，审查单位包括整段，不能只替换“可能”“不等于”等词。
+
+- 不推进当前问题、只是重复提醒“不能据此推断”“还要分别测量”的句子或段落，直接删除。
+- 推导所必需的模型、精度、输入、计数位置与假设，在定义或题设中先交代，再连续展开计算与解释。
+- 确实重要的实现差异或适用限制，以“需要注意的是，……”另起一段，放在相关推导完整结束处，并写明它怎样引出下一步分析。不能插入段中，也不能任意追加到段末或图注。
+- 段末应解释本段结果或引出后文；整段删除后，回读前后段及图表引导语，保证论述仍然连续。不要用增加免责声明替代技术准确性，也不要为删除限定而把假设或估算写成实测事实。
 
 ## 叙述与取证
 
