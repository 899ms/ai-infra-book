# 第 2 章《模型架构》翻译腔清扫对照表（2026-09-11）

章节文件：`manuscripts/02-模型架构.md`。共 22 处改动（其中图 2-7 一处同步改了 alt 文本与图注两行）。只动句式，不改任何技术内容、数字、公式、术语；表格、数学式、代码与链接均未触碰。

| 修改前 | 修改后 |
|---|---|
| …共有 $m=BP$ 行。从缓存读取键和值的已有上下文 token，不计入这里的 $m$。 | …共有 $m=BP$ 行。已有上下文 token 的键和值从缓存读取，不计入这里的 $m$。 |
| …末端词表头计算输出分数。访问哪些权重，因而由计算图中的具体操作决定。 | …末端词表头计算输出分数。因此，访问哪些权重由计算图中的具体操作决定。 |
| …再追加一个橙色新 token；本图初始上下文为四，四步后保留八个 token。（alt 文本与图注两处同步） | …再追加一个橙色新 token；本图初始上下文为 4 个 token，四步后共保留 8 个。 |
| …则 $n_d=1024$，累计读取上下文 token 的 KV 共 8,912,384 次。 | …则 $n_d=1024$，全过程累计读取上下文 token 的 KV 达 8,912,384 次。 |
| …64 维额外分支；NoPE 表示该处不使用位置编码的设置，它保持该分支的计算维度不变，省去旋转位置编码。 | …64 维额外分支；NoPE 表示这一分支不做位置编码，保持计算维度不变，从而省去旋转位置编码。 |
| 将 43 层相加，8192 个 token 的上下文下的窗口、压缩上下文和索引约为 59.125 MiB；… | 将 43 层相加，在 8192 个 token 的上下文下，窗口、压缩上下文和索引共约 59.125 MiB；… |
| …压缩器是否完成一块，需要分别计算。第 128 个 token 可以同时触发两种压缩块的完成。 | …压缩器是否完成一块，需要分别计算。处理到第 128 个 token 时，两种压缩块可能同时完成。 |
| …紧凑 BF16 MLA、FP32 递推状态及给定 BF16 短卷积槽位下，三项分别为 216 MiB… | …紧凑 BF16 MLA、FP32 递推状态及 BF16 短卷积槽位下，三项分别为 216 MiB… |
| 其 30 个线性注意力层加 10 个完整注意力层，使每请求每个上下文 token 的全局 KV 为 20 KiB，另有 60 MiB 的固定 FP32 递推状态，以及保存短卷积所需近期输入的 BF16 缓冲区 1.875 MiB。 | 其 30 个线性注意力层加 10 个完整注意力层，每请求每个上下文 token 的全局 KV 为 20 KiB，另有 60 MiB 的固定 FP32 递推状态，以及一个 1.875 MiB 的 BF16 缓冲区，用于保存短卷积所需的近期输入。 |
| 接下来的混合专家（Mixture of Experts，MoE）分支准备多个前馈子网络，由路由器为每个 token 选择其中一部分执行… | 接下来的混合专家（Mixture of Experts，MoE）分支提供多个前馈子网络，由路由器为每个 token 选择其中一部分来执行… |
| 解码器需要的全局 KV 由编码器末层表示经过投影生成，而不必先运行每个输入 token 的全部解码器层。 | 解码器需要的全局 KV 由编码器末层表示投影生成，不必让每个输入 token 都先经过全部解码器层。 |
| …20 层 128:1 压缩。按照官方 FlashMLA 的主记录 584 B、索引记录 68 B 格式，其全局 KV 的平均存储增量为… | …20 层 128:1 压缩。按官方 FlashMLA 的格式（主记录 584 B、索引记录 68 B），其全局 KV 的平均存储增量为… |
| 第一式按分派的总行数累计，第二式按被读入的不同权重累计。 | 第一式按分派的总行数累计，第二式按实际读入的不同权重累计。 |
| 与此同时，原来要执行许多输入为两行的矩阵乘法，现在只需执行少数输入为 64 行的矩阵乘法；… | 与此同时，原来要执行许多输入只有两行的矩阵乘法，现在只需执行少数输入为 64 行的矩阵乘法；… |
| Qwen3.6 在已有 8192 个 token 的上下文的 $B=1$ decode 中约需 7.33 GFLOPs… | Qwen3.6 在已有 8192 个 token 上下文、$B=1$ 的 decode 中约需 7.33 GFLOPs… |
| 专家数减半、单专家宽度加倍时，相同数量的权重被分成更少的大矩阵；… | 专家数减半、单专家宽度加倍时，同样多的权重分成更少的大矩阵；… |
| 后面的层还要使用这些块表示，所以它们需要在本次前向中继续保存；前向结束后即可释放。 | 后面的层还要使用这些块表示，所以本次前向中要继续保存它们；前向结束后即可释放。 |
| 保留多少历史由此成为一个同时影响应用和系统的决定。 | 保留多少历史，由此成了一个既影响应用、也影响系统的决定。 |
| …其参数量约为 705.5 亿。其 BF16 权重为 141.107 GB，每条 8192 个 token 的请求的 BF16 KV 为 2.5 GiB。 | …其参数量约为 705.5 亿。BF16 权重为 141.107 GB；对一条含 8192 个 token 的请求，BF16 KV 为 2.5 GiB。 |
| …以及 scale 与打包开销，比将全部参数一律按半字节计算更大。 | …以及 scale 与打包开销，比把所有参数一律按半字节折算的结果更大。 |
| 先确定服务所需的状态大小，计算留给权重的空间。 | 先确定服务所需的状态大小，再计算留给权重的空间。 |
| …推理使用两台、共 16 张 H100，其所需容量超过一台服务器所能提供的显存。 | …推理使用两台、共 16 张 H100，其所需容量超出了单台服务器能提供的显存。 |

## 看过但决定不改的句子

以下句子略有嫌疑，但尚在可接受范围内或改动会引入新的歧义，本次未动，供复核：

- "……为理解模型架构如何改变系统需求提供了新的案例"（第 9 行）："为……提供了案例"属常见书面语，改成"是……的新案例"收益不大。
- "共享 $K$、$V$ 改变了被查询的数据"（2.3.1 节）：此处的"被查询"是必要的限定关系，不宜去"被"。
- "训练时，这些额外目标促使当前表示包含对更远后续内容有用的信息"（2.5.3 节）：多层定语但层次清楚，拆开反而啰嗦。
- "在本例采用的分组量化方案中，让嵌入、输出头和归一化等部分继续使用较高精度，并计入……后，8-bit 方案约为……"（2.6.2 节）：长句但节奏可接受，主语隐含"方案"，改写需动结构，风险大于收益。
