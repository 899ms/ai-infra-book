# TTS receive configuration

Preserve the exact Fish WAV/PCM and294-release source client-read availability schedule from ../tts-network. Begin that schedule after complete request upload; no new GPU inference or native model chunk timing is claimed. Compare defaults,64KiB,4MiB TCP receive buffers and QUIC initial connection/stream credits on both endpoints. Leave other transport settings fixed. Use the previously verified window runner's TCP readbacks/TCP_INFO sampling and QUIC parameters/MAX frames, retaining the original TTS release and client-arrival records.

Three trials use position-balanced mode order default/64k/4m,64k/4m/default,4m/default/64k. Each subrun warms one request per protocol then transfers two sequential formal requests over one fresh reused connection per protocol. Nine fresh Docker containers give54 exchanges (36 formal18 warmups),network-none,NET_ADMIN,2CPU/2GiB,netem80ms RTT/20Mbit/s/0.1% loss. Common120s body deadline; handshake20s. Reuse pairs mix fresh and reused requests; retain both.

Verify all WAV bytes and complete PCM SHA,mono44100Hz PCM16 format, every release offset/length/no-early timestamp, client arrival byte conservation and first complete20ms/three-frame thresholds (44+1764=1808 and44+5292=5336 bytes). These are client PCM availability times, not speaker onset or acoustic playback. Preserve every failure/slow sample. Container resources and drained queues/cleanup must verify before completion. The recorded audio schedule is held fixed so this measures receive configuration under that trace, not changed synthesis quality or runtime.
