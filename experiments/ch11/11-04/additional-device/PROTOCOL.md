# Additional physical device: matched two-task batch

Three repetitions, fixed Mac-only versus Mac+RTX, within-repetition order shuffled seed1105. Fixed: original sequence and extraction tasks sequentially through existing verified MLX worker/HTTP SQLite manager. Additional: same Mac sequence concurrent with extraction on RTX BF16, from existing resident disk weight cache but fresh model process. Whole batch requires both outputs. This is heterogeneous device addition, not equal GPU scaling. Same greedy input token IDs and strict JSON acceptance; compare every output token to original formal baseline and record any disagreement.

RTX worker captures each generated token, writes a terminal artifact with fsync, and exits; the Mac retrieves the artifacts and commits output positions to a new SQLite receipt transaction. Count RTX tokens received only after that transaction; no streaming receipt or prefix recovery is implied by this route. The earlier formal experiment supplies actual restart/preserve behavior, and this run supplies actual physical additional capacity. Fresh producer preparation and return transfer count in batch time. Weight files already cached; do not call this measured weight distribution. Distinct deployment assumptions stay explicit.

Record controller-monotonic batch windows, per-worker intervals, GPU cleanup and Mac process cleanup. Teaching costs may price Mac and RTX allocated intervals separately; no vendor billing claim. These batches are generation-only; completed-training tokens for them remain unknown until explicitly consumed. Existing checkpoints for the original logical samples cannot be relabeled as this run's training.
