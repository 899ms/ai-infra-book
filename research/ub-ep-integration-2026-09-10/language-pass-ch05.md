# 第 5 章语言通读：逐句修改对照

本文件记录 2026-09-11 全书通读在本章留下的净改动。

## 全书通读（2026-09-11，按最终文本与通读前快照逐行对照）

共 35 行有改动；英文术语按“首次出现保留中文注释、之后用英文”的原则已回退，下表是回退之后的净改动。省略号表示只截取了改动附近的文字。

| 修改前 | 修改后 |
|---|---|
| 第四章介绍了加速器的计算能力、存储容量和数据传输带宽。本章讨论程序如何利用这些资源。… | 第 4 章介绍了加速器的计算能力、存储容量和数据传输带宽。本章讨论程序如何利用这些资源。… |
| …地址和参数，再通过运行时与驱动向 GPU 提交任务。向加速器发起一次内核执行称为 **kernel launch（内核启动）**。运行时负责提交任务、管理内存和同步执行；编译器则事先或在首次调用时生成加… | …地址和参数，再通过运行时与驱动向 GPU 提交任务。向加速器发起一次内核执行称为**内核启动（kernel launch）**。运行时负责提交任务、管理内存和同步执行；编译器则事先或在首次调用时生成加… |
| …好处是第一份数据可以更早到达：GPU 开始计算前半批时，复制引擎继续传送后半批。5.3 节将进一步计算传输与计算重叠后的总时间。[^buffer] | …好处是第一份数据可以更早到达：GPU 开始计算前半批时，复制引擎继续传送后半批。第 5.3 节将进一步计算传输与计算重叠后的总时间。[^buffer] |
| …等“批 0 已用完”事件。虚线箭头表示等待事件，不搬运数据。两个槽的交替使用在 5.3.2 节展开。* | …等“批 0 已用完”事件。虚线箭头表示等待事件，不搬运数据。两个槽的交替使用在 第 5.3.2 节展开。* |
| …。CPU 需要 D2H 结果时，可以等待对应事件；其他无关的加速器任务继续运行。5.3 节的双缓冲正是把两套这样的使用顺序交错起来。 | …。CPU 需要 D2H 结果时，可以等待对应事件；其他无关的加速器任务继续运行。第 5.3 节的双缓冲正是把两套这样的使用顺序交错起来。 |
| …s。调用次数越多，平均分担的编译时间越少，而每次执行所需的 1 ms 保持不变。在 5.5 节，我们将用同一方法判断分桶和特化需要重复多少次才值得。 | …s。调用次数越多，平均分担的编译时间越少，而每次执行所需的 1 ms 保持不变。第 5.5 节将用同一方法判断分桶和特化需要重复多少次才值得。 |
| …成。不过，切成什么形状，要同时看计算单元能处理什么，以及它附近能放下多少数据。第四章已经说明，硬件矩阵指令支持有限的形状；整张加速器上的多个计算单元可以同时处理许… | …成。不过，切成什么形状，要同时看计算单元能处理什么，以及它附近能放下多少数据。第 4 章已经说明，硬件矩阵指令支持有限的形状；整张加速器上的多个计算单元可以同时处理许… |
| 分块还要让搬来的数据多用几次。沿用第四章的工作台比喻，当前输入和尚未算完的部分和要放在计算单元附近，用完才能腾出空间。… | 分块还要让搬来的数据多用几次。沿用第 4 章的工作台比喻，当前输入和尚未算完的部分和要放在计算单元附近，用完才能腾出空间。… |
| …%。可以先按容量排除放不下的方案，再按访问量缩小比较范围，最后测量实际执行效率。5.4 节将把这一过程交给编译器。 | …%。可以先按容量排除放不下的方案，再按访问量缩小比较范围，最后测量实际执行效率。第 5.4 节将把这一过程交给编译器。 |
| 这种在行末增加填充元素的做法称为 padding（填充）。它让已有的并行请求分散到不同 bank，有效数据和读取次数都没有变化。 | 这种在行末增加填充元素的做法称为**填充（padding）**。它没有改变计算本身，只是让已有的并行请求分散到不同 bank。 |
| …Norm（均方根归一化）先根据一行元素的均方值计算共同的缩放因子，再缩放这一行。将多个元素合并为一个统计量的操作称为归约，它正好说明这种情况：一行的缩放因子来自整行平方和，随后每个元素乘以这个因子和对应的可学习参数 ga… | …Norm（均方根归一化）先根据一行元素的均方值计算共同的缩放因子，再缩放这一行。把多个元素合并成一个统计量的操作称为归约，RMSNorm 正是这样的例子：一行的缩放因子来自整行平方和，随后每个元素乘以这个因子和对应的可学习参数 ga… |
| …；第三种切法省下了输入的复制，却像 RMSNorm 的局部和一样，多出一次汇合。5.2.2 节的输出块正是同时沿 $M$ 和 $N$ 切分，再沿 $K$ 逐块累… | …；第三种切法省下了输入的复制，却像 RMSNorm 的局部和一样，多出一次汇合。第 5.2.2 节的输出块正是同时沿 $M$ 和 $N$ 切分，再沿 $K$ 逐块累… |
| 5.1 节的主机输入也可以按此组织。这里的流（stream）是按提交顺序执行操作的队列，事件（event）用于标记进度，让另一条流等待指定操作完成。复制流传入下一批，计算流处理当前批；计算流等待“复制完成”事件后读取新输入，复… | 第 5.1 节的主机输入也可以按此组织。复制流传入下一批，计算流处理当前批；计算流等待“复制完成”事件后读取新输入，复… |
| 读到 FlashAttention 时，我想起了自己在 2019 年做 AKG 的一段经历。当时为了融合 softmax 算子，我四处寻找合适的在线算法，后来找到 NVIDIA 在 2018 年发表的研究，才得以结合 AKG 的分块与融合能力，把前面的矩阵乘法和后面的 softmax 接在一起计算。我那时甚至还不了解 Attention，后来才意识到，自己偶然摸到了 FlashAttention 的一条核心思路。从… | 读到 FlashAttention 时，笔者想起了自己在 2019 年做 AKG 的一段经历。当时为了融合 softmax 算子，笔者四处寻找合适的在线算法，后来找到 NVIDIA 在 2018 年发表的研究，才得以结合 AKG 的分块与融合能力，把前面的矩阵乘法和后面的 softmax 接在一起计算。笔者那时甚至还不了解注意力机制，后来才意识到，自己偶然摸到了 FlashAttention 的一条核心思路。从… |
| 其中 Q 是查询向量，K 是供查询匹配的键向量，V 是最终按权重汇总的值向量。Soft… | 注意力计算中，Q 是查询向量，K 是供查询匹配的键向量，V 是最终按权重汇总的值向量。Soft… |
| …BF16，一次处理一个序列、一个注意力头，头维度为 128。序列长为 8192 token，每个 token 对应序列中的一个输入 token；采用因果注意力，即每个位置只使用自身及此前的位置。预热后，使用 C… | …BF16，一次处理一个序列、一个注意力头，头维度为 128。序列长为 8192 个 token；采用因果注意力，即每个位置只使用自身及此前的位置。预热后，使用 C… |
| 第一项变换是 **split（拆分）**。把 i 拆成块号 $i_o$ 与块内坐标 $i_i$，令 $i=64i_o+i_i$。原来的 1024 行就成为 16 个块，每块 64 行。对 j 同样处理，得到 192 个列块。同时拆分两个输出维度，就形成 64×64 的 **tile（分块）**。 | 第一项变换是**拆分（split）**。把 i 拆成块号 $i_o$ 与块内坐标 $i_i$，令 $i=64i_o+i_i$。原来的 1024 行就成为 16 个块，每块 64 行。对 j 同样处理，得到 192 个列块。同时拆分两个输出维度，就形成 64×64 的**分块（tile）**。 |
| 拆分首先改变了索引的表示方式。要连续完成一个 tile，还需要 **reorder（重排）**：在外层循环中遍历输出块，在内层循环中遍历块内元素。再把 K 按 32 拆… | 拆分首先改变了索引的表示方式。要连续完成一个 tile，还需要**重排（reorder）**：在外层循环中遍历输出块，在内层循环中遍历块内元素。再把 K 按 32 拆… |
| Halide 是将运算定义与执行调度分离的编程系统，把“计算什么”和“如何调度”分开，程序员用 split、tile、reorder 等动作改变执行方式。这里把生成… | Halide 是把“计算什么”和“如何调度”分开表达的编程系统，程序员用 split、tile、reorder 等动作改变执行方式。这里把生成… |
| …号，再分配给加速器工作组。这改变了遍历输出块和分配任务的方式；将 SiLU 紧接到矩阵乘 tile 后面，则省去了中间矩阵的写回和重读。两者可以在同一份调度中组合使用。 | …号，再分配给加速器工作组。这改变了遍历输出块和分配任务的方式；将 SiLU 紧接在矩阵乘的 tile 后面，则省去了中间矩阵的写回和重读。两者可以在同一份调度中组合使用。 |
| AKG 是面向张量算子的自动代码生成与优化系统，其核心技术是 **Polyhedral Compilation（多面体编译）**。它把规则循环表示为三类信息：有哪些迭代要执行，每次迭代读写哪些数组元素，… | AKG 是面向张量算子的自动代码生成与优化系统，其核心技术是**多面体编译（Polyhedral Compilation）**。它把规则循环表示为三类信息：有哪些迭代要执行，每次迭代读写哪些数组元素，… |
| …28 列扩大为 1536 列，需要十二倍空间。因此，选择是否融合量化时，仍要比较 5.2 节讨论过的局部存储占用与数据复用。[^numerics] | …28 列扩大为 1536 列，需要十二倍空间。因此，选择是否融合量化时，仍要比较第 5.2 节讨论过的局部存储占用与数据复用。[^numerics] |
| …值规则，再排除局部存储不足以容纳的实现，最后估计执行开销，决定优先测试哪些实现。5.2 节已给出一种估计：用访问量和有效带宽比较两个 tile。编译器还可以加入… | …值规则，再排除局部存储不足以容纳的实现，最后估计执行开销，决定优先测试哪些实现。第 5.2 节已给出一种估计：用访问量和有效带宽比较两个 tile。编译器还可以加入… |
| \| 列出候选 \| tile、循环序、缓冲数、融合范围 \| 编译器与硬件是否支持 \| | \| 列出候选 \| tile、循环次序、缓冲数、融合范围 \| 编译器与硬件是否支持 \| |
| …到候选表改变块形状或缓冲数。第 6 章采用同样过程，只把 tile 的放置扩展到加速卡，并把跨块依赖换算成集合通信。 | …到候选表改变块形状或缓冲数。第 6 章采用同样过程，只把 tile 的放置扩展到多张卡，并把跨块依赖换算成集合通信。 |
| …使使用同一份内核代码，改变提交方式或复用已有的准备结果，也会改变总时间。本节接着 5.1 节的时间线，分析这些主机工作。 | …使使用同一份内核代码，改变提交方式或复用已有的准备结果，也会改变总时间。本节接着第 5.1 节的时间线，分析这些主机工作。 |
| 5.1 节区分了 CPU 提交时间与加速器执行时间，5.3 节又说明两类资源可以重叠工作。把这两点用于模型的一次前向计算：CPU 为… | 第 5.1 节区分了 CPU 提交时间与加速器执行时间，第 5.3 节又说明两类资源可以重叠工作。把这两点用于模型的一次前向计算：CPU 为… |
| …录成图，再重复提交。使用 CUDA Graph 时，主机每次只需发起一次图重放，无需重新逐个准备和提交 kernel。加速器仍然执行图中的各个节点，减少的是主机的重… | …录成图，再重复提交。使用 CUDA Graph 时，主机每次只需发起一次图重放，不必重新逐个准备和提交 kernel。加速器仍然执行图中的各个节点，减少的是主机的重… |
| …ch，加速器仍然执行图中记录的六个 kernel。要减少 kernel 的数量，需要的是融合，不是图重放。* | …ch，加速器仍然执行图中记录的六个 kernel。要减少 kernel 的数量，靠的是融合，不是图重放。* |
| …的程序；**特化（specialization）**则利用已知的输入条件生成专门实现；这里针对每个常见形状分别编译程序。越充分地利用已知形状，越有机会简化加速器工作，但也需要… | …的程序；**特化（specialization）**则利用已知的输入条件生成专门的实现，这里指为每个常见形状分别编译程序。越充分地利用已知形状，越有机会简化加速器工作，但也需要… |
| 第八章将结合请求到达情况和动态批处理，进一步讨论如何选择和缓存程序。如果某种形状在短… | 第 8 章将结合请求到达情况和动态批处理，进一步讨论如何选择和缓存程序。如果某种形状在短… |
| **Persistent Kernel（常驻内核）**在加速器上持续运行，从队列中取出任务，通过事件确认所需数据是否准备好。这样… | **常驻内核（persistent kernel）**在加速器上持续运行，从队列中取出任务，通过事件确认所需数据是否准备好。这样… |
| 第二章的 V4.1 会话说明，保存同一份数据所需的容量，与执行期间对它的累计访问量可… | 第 2 章的 V4.1 会话说明，保存同一份数据所需的容量，与执行期间对它的累计访问量可… |
| > 在 5.4.2 的伪代码中，分别将 SiLU 移到 ko 循环内、删除中间 BF16 舍入、将量… | > 在第 5.4.2 节的伪代码中，分别将 SiLU 移到 ko 循环内、删除中间 BF16 舍入、将量… |
| FPGA 是可配置逻辑器件，高层次综合（HLS）将较高层程序转换为硬件数据通路。我早期的 FPGA／HLS 工作，也经历过从接口封装转向代码变换的过程：为了形成所… | FPGA 是可配置逻辑器件，高层次综合（HLS）将较高层程序转换为硬件数据通路。笔者早期的 FPGA／HLS 工作，也经历过从接口封装转向代码变换的过程：为了形成所… |

## 翻译腔审查（2026-09-11）

作者确认术语原则并统一“笔者”“N 个 token”“Queqiao”之后，再做一遍只针对翻译腔的审查（含图注里汉字数字改阿拉伯数字）。共 31 行有改动，按最终文本与审查前快照逐行对照。

| 修改前 | 修改后 |
|---|---|
| …接着又要用于旁边的输出：如果它还留在计算单元附近，下一次就可以直接使用；如果已经被换走，就要重新搬来。矩阵乘法的程序需要安排这些使用次序，并给输入和部分和留下合适… | …接着又要用于旁边的输出：如果它还留在计算单元附近，下一次就可以直接使用；如果已经换走，就要重新搬来。矩阵乘法的程序需要安排这些使用次序，并给输入和部分和留下合适… |
| … MiB，一种分块实现的读写量却达到约 3 GiB。扩大输出块可以让输入多用几次、使读写量减半，但每个块的局部存储需求也从 24 KiB 增至 80 KiB。这些数字将帮助我们判断，减少搬运带来的收益，能否抵消同时执行的块数减少所带来的性能损失。 | … MiB，一种分块实现的读写量却达到约 3 GiB。扩大输出块可以让输入多用几次，把读写量减半，但每个块的局部存储需求也从 24 KiB 增至 80 KiB。有了这些数字，就能判断减少搬运省下的时间，能否抵消同时执行的块数减少造成的损失。 |
| \| 投影 C \| $[1024,12288]$ \| 24 MiB \| 随后进行激活等处理 \| | \| 投影 C \| $[1024,12288]$ \| 24 MiB \| 随后做激活等处理 \| |
| …执行的程序；**launch** 是 CPU 发起这次执行的动作。一个矩阵乘可以通过多个 kernel 完成，几个逐元素算子也可以由同一个 kernel 完成。因此… | …执行的程序；**launch** 是 CPU 发起这次执行的动作。一个矩阵乘可以由多个 kernel 完成，几个逐元素算子也可以由同一个 kernel 完成。因此… |
| …提交，GPU 则按照任务队列和依赖关系执行。CPU 可以立即准备下一项任务。只有在 CPU 需要加速器结果，或准备覆盖仍被加速器使用的数据时，CPU 才需要等待相应的加速器操作完成。[^execution] | …提交，GPU 则按照任务队列和依赖关系执行。CPU 可以立即准备下一项任务。只有当 CPU 要用加速器的结果，或者要覆盖加速器还在使用的数据时，CPU 才需要等相应的加速器操作完成。[^execution] |
| …工作。Host 指主机，Device 指设备，在本节中具体指 GPU 加速器。对具有独立显存的 GPU，常见复制方向有三种： | …工作。Host 指主机，Device 指设备，在本节中具体指 GPU 加速器。对有独立显存的 GPU，常见的复制方向有三种： |
| …U 提交、H2D 输入复制、加速器内核与 D2H 结果返回依次发生。上方内核执行二十微秒，结果在第三十五微秒可用；下方内核执行五微秒，结果在第二十微秒可用。* | …U 提交、H2D 输入复制、加速器内核与 D2H 结果返回依次发生。上方内核执行 20 μs，结果在第 35 μs 可用；下方内核执行 5 μs，结果在第 20 μs 可用。* |
| …标出起止位置，测得加速器执行这段任务所用的时间。性能分析工具（profiler）将主机调用、加速器计算和复制画在同一时间线上，使提交之后的排队、依赖等待和执行可以逐段辨认。 | …标出起止位置，测得加速器执行这段任务所用的时间。性能分析工具（profiler）把主机调用、加速器计算和复制画在同一条时间线上，提交之后的排队、依赖等待和执行就能逐段看清。 |
| 作为比较基准，三份 BF16 矩阵各经过接口一次的读写量为： | 比较的基准是三份 BF16 矩阵各经过接口一次的读写量： |
| …错发起访存：一些块等待数据时，另一些块继续工作。同时驻留的工作集从四份减为一份，可能减少在等待访存期间执行其他计算的机会。因此，大块虽然少读数据，实际获得的带宽却可能下降。 | …错发起访存：一些块等待数据时，另一些块继续工作。同时驻留的工作集从四份减为一份，等待访存期间能执行的其他计算也就少了。因此，大块虽然少读数据，实际获得的带宽却可能下降。 |
| … 2 KiB。原来因寄存器不足而写入较远存储的临时数据，可以因此保留在寄存器中。并行任务数增加与每组临时数据减少，是拆分归约获得收益的两个具体来源；增加的输入读取和阶段间数据传递则构成额外开销。 | … 2 KiB。原来因寄存器不足而写入较远存储的临时数据，可以因此保留在寄存器中。拆分归约的收益来自两处：并行任务变多，每组的临时数据变少；代价则是多读一遍输入，以及阶段之间要传递数据。 |
| RMSNorm 的拆分沿的是归约维：一行的平方和被分成八个局部和，必须再合并一次。矩阵乘也有同样的选择。对 $C_{M\times… | RMSNorm 的拆分沿的是归约维：一行的平方和分成八个局部和，必须再合并一次。矩阵乘也有同样的选择。对 $C_{M\times… |
| …。例如沿 $K$ 切分后，是等全部输入到齐再算，还是边接收边累加，会改变存储占用与暴露的等待。模型中的样本、序列、特征、层和专家提供更多分工方向；其中层是计算图上的顺序阶段… | …。例如沿 $K$ 切分后，是等全部输入到齐再算，还是边接收边累加，会改变存储占用和实际等待的时间。模型中的样本、序列、特征、层和专家提供更多分工方向；其中层是计算图上的顺序阶段… |
| …位置之间没有归约。因此，读入同一位置的两个元素后，就能直接算出该位置的最终结果，而无需先生成全部 SiLU 结果。 | …位置之间没有归约。因此，读入同一位置的两个元素后，就能直接算出该位置的最终结果，不必先算出全部 SiLU 结果。 |
| 先比较中间结果 T 的去向。分开执行时，完整 T 要写到下一级存储，再被乘法读回；融合后，一小块 SiLU 结果可以直接交给同一 GPU 内核程序（ke… | 先比较中间结果 T 的去向。分开执行时，完整 T 要写到下一级存储，再由乘法读回；融合后，一小块 SiLU 结果可以直接交给同一 GPU 内核程序（ke… |
| 中间张量的容量与访问量在这里具有不同含义。设每步先分配输出，完成后释放已用完的输入。独立执行 SiLU 时，G、U、T 同时占用内存，峰值为 72 MiB；三步融合时，G、U 与最终输出同时占用内存，峰值为 60 MiB。读写减少 96 MiB，峰值却只减少 12 MiB，因为同一块存储空间可以被反复读写。[^fusion] | 中间张量的容量与访问量在这里是两回事。设每步先分配输出，完成后释放已用完的输入。独立执行 SiLU 时，G、U、T 同时占用内存，峰值为 72 MiB；三步融合时，G、U 与最终输出同时占用内存，峰值为 60 MiB。读写减少 96 MiB，峰值却只减少 12 MiB，因为同一块存储空间可以反复读写。[^fusion] |
| …还决定两个阶段能否重叠执行。设搬运器把块 0 放入槽 A，计算单元随后读取 A。在 A 被读取期间，搬运器将块 1 放入槽 B；计算转向 B 后，便可将块 2 加载到 A。这样，两个槽交替使用，使计算单元读取当前块时，搬运器可以写入下一块。 | …还决定两个阶段能否重叠执行。设搬运器把块 0 放入槽 A，计算单元随后读取 A。计算单元读取 A 期间，搬运器把块 1 放入槽 B；计算转向 B 后，就可以把块 2 加载到 A。这样两个槽交替使用，计算单元读当前块时，搬运器就可以写下一块。 |
| 当两阶段访问顺序不同，缓冲需要保存更多尚未使用的数据。三个排队的 16 KiB 块占 48 KiB，再加 3… | 两个阶段的访问顺序不同时，缓冲要保存更多尚未使用的数据。三个排队的 16 KiB 块占 48 KiB，再加 3… |
| …，减少中间数据交换和非矩阵运算；FlashAttention-3 利用异步执行，使搬运、矩阵乘和 Softmax 重叠进行。省去巨大的中间矩阵之后，这些改进进一步缩短了剩余的计算和搬运时间。[^fa] | …，减少中间数据交换和非矩阵运算；FlashAttention-3 利用异步执行，让搬运、矩阵乘和 Softmax 重叠执行。省去巨大的中间矩阵之后，这些改进进一步缩短了剩余的计算和搬运时间。[^fa] |
| 实验在 RTX PRO 6000 Blackwell 上进行，输入输出为 BF16，一次处理一个序列、一个注意力头，头维度为 128。序列长… | 实验用 RTX PRO 6000 Blackwell，输入输出为 BF16，一次处理一个序列、一个注意力头，头维度为 128。序列长… |
| 读到后一块中的 10 后，即使调整后续计算所用的尺度，也无法使第一项重新经历 44.8→44 的舍入。两种做法的结果相差 $1/56$，约 1.8%。因此，按整行最大值确定尺度时，应先读完一行并求出最大值，再进行量化。[^numerics] | 读到后一块中的 10 后，即使调整后续计算所用的尺度，第一项也不可能重新经历 44.8→44 的舍入。两种做法的结果相差 $1/56$，约 1.8%。因此，按整行最大值确定尺度时，应先读完一行求出最大值，再量化。[^numerics] |
| 这些例子说明了哪些变换可以进行，哪些运算顺序需要保留。输出之间可以交换执行顺序，输入可以分块缓存，算好的结果可以直接供后续运算使… | 这些例子说明了哪些变换可以做，哪些运算顺序必须保留。输出之间可以交换执行顺序，输入可以分块缓存，算好的结果可以直接供后续运算使… |
| …会增加访存。前面的逐元素链通过融合少读了中间值；若中间结果采用位数更少的表示，又需要被后续计算反复读取，保留它反而可以减少读取。 | …会增加访存。前面的逐元素链通过融合少读了中间值；若中间结果采用位数更少的表示，又要供后续计算反复读取，保留它反而可以减少读取。 |
| *图 5-30：橙色为准备，蓝色为额外输入复制，绿色为加速器计算。普通方式四十微秒；图的两 MiB 输入约二十七微秒，十六 MiB 输入约四十二微秒。* | *图 5-30：橙色为准备，蓝色为额外输入复制，绿色为加速器计算。普通方式 40 μs；图的两 MiB 输入约 27 μs，十六 MiB 输入约 42 μs。* |
| …中取出任务，通过事件确认所需数据是否准备好。这样既减少主机反复启动内核的工作，也使相邻算子能够按块重叠执行。Mirage Persistent Kernel／MPK 是采用这… | …中取出任务，通过事件确认所需数据是否准备好。这样既减少主机反复启动内核的工作，也让相邻算子能按块重叠执行。Mirage Persistent Kernel／MPK 是采用这… |
| …启动八块激活。每块投影约 32.2 μs、激活约 39.3 μs，两次主机启动各五微秒；竖线为激活开始。* | …启动八块激活。每块投影约 32.2 μs、激活约 39.3 μs，两次主机启动各 5 μs；竖线为激活开始。* |
| …充足。每个任务另计 0.7 μs 调度与通知，首块投影完成后即可激活，八块流水约三百五十八微秒结束。* | …充足。每个任务另计 0.7 μs 调度与通知，首块投影完成后即可激活，八块流水约 358 μs 结束。* |
| 沿着图 5-34 的激活时间线看，首块开始后就连续执行到最后一块。这与双缓冲具有同样的规律：第一块先完成前一阶段，之后每隔较慢阶段所需的时间完成一块。任务调度增加了开… | 沿着图 5-34 的激活时间线看，首块开始后就连续执行到最后一块。这与双缓冲是同一个规律：第一块先完成前一阶段，之后每隔较慢阶段所需的时间完成一块。任务调度增加了开… |
| …0 μs，请求就变成 $10+\max(15,90)+10=110$ μs。B 增加的等待时间超过了 A 节省的计算时间，导致整个请求反而变慢。[^dag] | …0 μs，请求就变成 $10+\max(15,90)+10=110$ μs。B 多等的时间超过了 A 省下的计算时间，整个请求反而变慢。[^dag] |
| …局部窗口与索引的逐层逻辑读取为 12.556 与 11.375 MiB。共享状态无需为每层保存一份副本，各使用层仍需要访问它。[^v41-case] | …局部窗口与索引的逐层逻辑读取为 12.556 与 11.375 MiB。共享状态不必每层各存一份副本，但用到它的每一层仍要访问它。[^v41-case] |
| 除了数据从哪里取得，程序是否真正省去了计算也会改变开销。候选池通过缩小后续索引的搜索范围来节省工作。公开参考实现先对完整索引缓存执行点积，再屏蔽候选池之外的位置；论文中的生产实现… | 除了数据从哪里取得，程序是否真正省去了计算也会改变开销。候选池把后续索引的搜索范围缩小，工作量随之减少。公开参考实现先对完整索引缓存执行点积，再屏蔽候选池之外的位置；论文中的生产实现… |
