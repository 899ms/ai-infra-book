# Historical11-5 integrated comparison

Original scope: calculation/short scripts using weight size, network throughput, generation records and preemption times. Combine verified measurements without relabeling their execution environments.

Use the same two accepted semantic tasks for four reported arrangements: fresh fixed Mac sequential batch; actual additional-device Mac+RTX batch; original Mac restart batch under K32/K96 commit-before-ACK interruption; original Mac preserved-prefix batch under the same interruption. Restart is a control for the third requested arrangement. Generation paths were not captured in a single randomized four-arm live trial; summed older per-task windows and later batch windows are separate record-based comparisons. MacMLX4bit and RTXBF16 outputs have different token formatting but equal accepted task contents. Never report paired identical training streams across those backends.

Join each logical sample only to its own verified optimizer checkpoint. New physical-device batches use the separately measured canonical-training pass: parsed answers are normalized to the original baseline serialization, so every arrangement consumes identical two prompt/output streams (500 canonical positions). Raw-stream training remains a separate alternative and is not added to canonical pipeline cost. For an explicit cold-consumer scenario add one measured consumer preparation plus both measured per-sample optimizer/checkpoint durations. This is sequential composition of recorded stages, not observed end-to-end pipeline latency; base resource reserved generation only, RTX consumer priced for its composed training window. Prices are teaching units per hour (Mac1, RTX1); no real billing.

Lifetime diagnostics use each actual RTX worker's monotonic token timeline. At cutoff L before terminal generation, record generated tokens but zero received usable sample for this nonstreaming receiver. These are replay cutoffs, not new physical kills. Full completed artifacts may be transferred after generation; transfer/receipt time remains a separate observed controller interval. Existing formal8 real SIGKILLs establish restart/preserve behavior on Mac only.

For sustained optional-resource price sensitivity, use one complete extraction per measured RTX generation duration and one per fixed Mac extraction worker interval as throughput proxies. Optional price ratios0.1/0.3/1.0. Preparation D is measured RTX model preparation, optionally plus full cached-model byte size/declared1GB/s cold-transfer scenario; no measured weight transfer is claimed. Under the declared fluid repeated-task approximation optional resource is strictly cheaper iff r*g < m and L>D/(1-r*g/m), where g=RTX seconds/task and m=Mac seconds/task. Also require one full task window D+g. Finite tasks have floor((L-D)/g) completions; report their exact discrete price checks rather than presenting the fluid boundary as sufficient. Reusing measured one-task times at scale is a teaching assumption.
