# 第 11 章语言通读：逐句修改对照

本文件记录 2026-09-11 全书通读在本章留下的净改动。

## 全书通读（2026-09-11，按最终文本与通读前快照逐行对照）

共 36 行有改动；英文术语按“首次出现保留中文注释、之后用英文”的原则已回退，下表是回退之后的净改动。省略号表示只截取了改动附近的文字。

| 修改前 | 修改后 |
|---|---|
| …用 CPU，环境中的进程和缓存占用内存。沿着每次调用、返回和等待的过程，可以分别计算这些资源在什么时候被使用、使用多久。 | …用 CPU，环境中的进程和缓存占用内存。沿着每次调用、返回和等待的过程，可以分别算出这些资源什么时候被占用、占用多久。 |
| 模型采用流式输出时，可以更早返回部分文本，但工具仍需等待完整的调用参数：在收到目标文件、修改内容和操作类型之后，执行器才能进行这次修改。模板下载和运行时初始化只依赖预先确定的环境类型，可以提前放到模型生成… | 模型采用流式输出时，可以更早返回部分文本，但工具仍需等待完整的调用参数：收到目标文件、修改内容和操作类型之后，执行器才能执行这次修改。模板下载和运行时初始化只依赖预先确定的环境类型，可以提前放到模型生成… |
| 将这个例子推广到 $R$ 轮。在各阶段依次执行的情况下，任务总时间为 | 把这个例子推广到 $R$ 轮。各阶段依次执行时，任务总时间为 |
| …与错开启动的累计 CPU 时间分别约为 0.29 秒和 0.27 秒，工作量接近；驻留集大小（RSS）是进程当前驻留在物理内存中的页所占的容量。采样得到的 RSS 峰值从约 320 MiB 降到 80 MiB，整组完成时间从… | …与错开启动的累计 CPU 时间分别约为 0.29 秒和 0.27 秒，工作量接近。**驻留集大小**（resident set size，RSS）是进程当前驻留在物理内存中的页所占的容量；采样得到的 RSS 峰值从约 320 MiB 降到 80 MiB，整组完成时间从… |
| 任务、工具操作和每一次执行尝试需要各自的标识。一次测试结果可以被重复传送；同一项测试也可能实际执行两次。前一种情况需要消息去重，后一种情况已经发生了两次资源消耗。文件版本不相同时，两次同名测试甚至不是同一个工具操作。 | 任务、工具操作和每一次执行尝试需要各自的标识。同一份测试结果可能被传送两次；同一项测试也可能实际执行两次。前一种情况只需对消息去重，后一种情况已经消耗了两次资源。文件版本不相同时，两次同名测试甚至不是同一个工具操作。 |
| …，则需要恢复执行。两类失败对应不同的后续工作，也会产生不同的完成概率。强化学习（RL）根据样本的验证结果更新模型。RL 验证也需要区分这两种情况：答案是否正确用于计算学习反馈，执行故障则先由运行系统处理。 | …，则需要恢复执行。两类失败对应不同的后续工作，也会产生不同的完成概率。强化学习（reinforcement learning，RL）根据样本的验证结果更新模型。强化学习的验证也要区分这两种情况：答案是否正确用来计算学习反馈，执行故障则先由运行系统处理。 |
| 进程通常共享同一操作系统内核，通过地址空间和权限进行基本隔离。容器增加命名空间（让进程看到各自的进程编号、网络等资源视图）、文件系统… | 进程通常共享同一个操作系统内核，靠地址空间和权限做基本隔离。容器增加命名空间（让进程看到各自的进程编号、网络等资源视图）、文件系统… |
| …S）为无服务器计算开发、于 2018 年开源的轻量虚拟机监控器；其 2020 年 NSDI 论文讨论了如何在大量短时任务之间兼顾硬件虚拟化隔离、启动速度和内存开销。[^fi… | …S）为无服务器计算开发、于 2018 年开源的轻量虚拟机监控器；其 2020 年发表于 NSDI 会议的论文讨论了如何在大量短时任务之间兼顾硬件虚拟化隔离、启动速度和内存开销。[^fi… |
| …并准备首次工作所需的数据。最简单的方式是完整复制模板；另一种方式是共享只读内容，仅复制需要写入的页面；还可以按访问需求加载数据。创建环境时少加载一些数据，可以缩短启动时间，但程序首… | …并准备首次工作所需的数据。最简单的方式是完整复制模板；另一种方式是共享只读内容，只复制需要写入的页；还可以按访问需求加载数据。创建环境时少加载一些数据，可以缩短启动时间，但程序首… |
| …iB：第 10–11 秒保存状态，第 18–19 秒恢复，其余 7 秒释放内存。该间隔占用降至 4 GiB·秒，节省 14 GiB·秒，下一次工具仍在第 19 秒开始。 | …iB：第 10–11 秒保存状态，第 18–19 秒恢复，其余 7 秒释放内存。这段间隔的占用降到 4 GiB·秒，节省 14 GiB·秒，下一次工具仍在第 19 秒开始。 |
| 从图 11-11 改为图 11-12 的安排后，省去的是等待期间的一段内存占用。要在这段时间内释放内存，必须先保存下一轮仍需… | 把图 11-11 的安排改成图 11-12 之后，省去的是等待期间的一段内存占用。要在这段时间内释放内存，必须先保存下一轮仍需… |
| 在这个 9 秒间隔里，保存和恢复状态所需的两秒都安排在模型调用期间，真正释放内存的时间为七秒。若一个间隔只有 1 秒，就无法在这段时间内完成保存和恢复，恢复会推迟下一次工… | 在这个 9 秒间隔里，保存和恢复状态所需的 2 秒都安排在模型调用期间，真正释放内存的时间为 7 秒。若一个间隔只有 1 秒，就无法在这段时间内完成保存和恢复，恢复会推迟下一次工… |
| 这项安排依赖准备能在工具执行前完成。若下一个工具的类型要从流式输出中预测，那么调用发出后还要… | 这项安排的前提是准备能在工具执行前完成。若下一个工具的类型要从流式输出中预测，那么调用发出后还要… |
| … 个 CPU 核和四张 B 型 GPU。整个资源池既有四张 A 型卡，也有足够多 CPU，但没有满足全部条件的节点。 | … 个 CPU 核和四张 B 型 GPU。整个资源池既有四张 A 型卡，也有足够多的 CPU 核，但没有一个节点满足全部条件。 |
| …被抢占的低优先级作业后，研究中 GPU 分配比例从 68% 提高到 93%。这些工作利用高优先级作业暂时用不到的加速器；当需要成组启动的作业到来时，调度器收回这些加速器… | …被抢占的低优先级作业后，研究中 GPU 分配比例从 68% 提高到 93%。这些低优先级作业用的是高优先级作业暂时用不到的加速器；当需要成组启动的作业到来时，调度器收回这些加速器… |
| 图 11-23 用执行块前方的灰色条形表示等待时间。同样十秒的执行工作，既可以没有排队，也可以产生大量等待。因此，从平均资源需求推到实际响… | 图 11-23 用执行块前方的灰色条形表示等待时间。同样 10 秒的执行工作，既可以没有排队，也可以产生大量等待。因此，从平均资源需求推到实际响… |
| …用同时在第 0 s 到达；灰色为等待、蓝色为执行，各行的等待依次为 0—9 s。处理器仍只工作 10 s，平均排队为 4.5 s。* | …用同时在第 0 s 到达；灰色为等待、蓝色为执行，各行的等待依次为 0—9 s。执行进程仍只工作 10 s，平均排队为 4.5 s。* |
| …如果一种卡执行同样工作所需的时间是另一种卡的两倍，完成进度就不同。主导资源公平（DRF）把每个任务在各类资源中占比最高的一类称为主导资源，并依据主导资源份额公平… | …如果一种卡执行同样工作所需的时间是另一种卡的两倍，完成进度就不同。主导资源公平（dominant resource fairness，DRF）把每个任务在各类资源中占比最高的一类称为主导资源，并依据主导资源份额公平… |
| …会随阶段切换。rollout 指用当前策略生成供训练使用的交互轨迹，包括模型输出及工具或环境反馈。下一节计算增加这类轨迹生成加速器实际能缩短多少训练时间。 | …会随阶段切换。rollout 指用当前策略生成供训练使用的交互轨迹，包括模型输出以及工具或环境的反馈。下一节计算增加这类轨迹生成加速器实际能缩短多少训练时间。 |
| 一个同步 RL 迭代需要生成样本、完成验证并执行更新。如果每轮所需有效生成量为 $Q$，… | 一轮同步的 RL 迭代需要生成样本、完成验证并执行更新。如果每轮所需有效生成量为 $Q$，… |
| **例 11-4：共享发送端出口如何限制多实例权重分发速度？** 一份完整权重为 30 GB，要向六个 rollout 实例分别发送。六路传输共用发送端的 200 Gbit/s 出口，每个接收端的带宽为 50 Gbit/s。这里 GB 为十进制。向一个接收端传完权重至少需要 | **例 11-4：共享的发送出口如何限制多实例的权重分发速度？** 一份完整权重为 30 GB，要向六个 rollout 实例分别发送。六路传输共用发送方的 200 Gbit/s 出口，每个接收方的带宽为 50 Gbit/s。这里 GB 为十进制。向一个接收方传完权重至少需要 |
| …到权重，至少需要 7.2 秒；第一个实例可以更早收到自己的 30 GB。随后接收端把权重加载到 GPU 显存并确认版本，调度器再向它分配请求。PolyRL 是利用临时资源扩展 RL 生成池的系统。在这里，它先将完整权重接收到 CPU 缓冲区，再交给采用张量并行（TP，将层内矩阵分给多张卡）的 GPU 组。因此，TP=2 将实例内部的计算分配到两张卡上，每个实例从外部接收的权重仍为 30 GB。 | …到权重，至少需要 7.2 秒；第一个实例可以更早收到自己的 30 GB。随后接收方把权重加载到 GPU 显存并确认版本，调度器再向它分配请求。PolyRL 是利用临时资源扩展 RL 生成池的系统。它先把完整权重接收到 CPU 缓冲区，再交给采用张量并行（tensor parallelism，TP，见第 6.2.2 节）的 GPU 组。因此，TP=2 只是把实例内部的计算分到两张卡上，每个实例从外部接收的权重仍是 30 GB。 |
| …25 标出六份权重共同经过的出口。接收实例增加时，需要发送的总字节数也增加，发送端带宽却没有随之增加。权重传完后还要加载到 GPU，这些准备时间都要从临时实例可用… | …25 标出六份权重共同经过的出口。接收实例增加时，需要发送的总字节数也增加，发送方的带宽却没有随之增加。权重传完后还要加载到 GPU，这些准备时间都要从临时实例可用… |
| …完整的 30 GB 权重，六路传输共用 200 Gbit/s 发送出口。每个接收端为 50 Gbit/s，单份传输至少 4.8 秒，而发送端累计发送 180 GB 至少需要 7.2 秒；图中六个接收框各代表一个独立实例。… | …完整的 30 GB 权重，六路传输共用 200 Gbit/s 发送出口。每个接收方为 50 Gbit/s，单份传输至少 4.8 秒，而发送方累计发送 180 GB 至少需要 7.2 秒；图中六个接收框各代表一个独立实例。… |
| …共复用 2,000 token，较长那条的 3,000 token 要重新生成。将前缀长度对齐后，可以统一安排后续生成，但较长回答超出共同前缀的部分就需要重新生成。 | …共复用 2,000 token，较长那条的 3,000 token 要重新生成。对齐前缀长度后可以统一安排后续生成，代价是较长的回答超出共同前缀的部分要重新生成。 |
| …1-27 中的两种安排都需要 30 秒的验证处理时间，但完成时刻相差 20 秒；改变的是执行顺序。 | …1-27 中的两种安排都需要 30 秒的验证处理时间，但完成时刻相差 20 秒；差别只在提交时机。 |
| 上一节通过减少生成量降低成本。对于输入侧，主要的节省机会来自重复前缀：同一任务的多轮调用通常会重复发送公共提示和此前的对话记录。一项模型请求从控制器进入统一服务入口，再交给选定的模型后端。这里的后端指实际执行模型推理的服务实例或服务提供方，负责输入处理和输出生成。入口处理鉴权、路由和限流，后端处理输入与生成。章首每秒 30 次调用在这条路径上形成持续需求；每次调用携带的公共提示与任… | 上一节通过减少生成量降低成本。输入这一侧的主要节省机会来自重复前缀：同一任务的多轮调用通常会重复发送公共提示和此前的对话记录。一项模型请求从控制器进入统一服务入口，再交给选定的模型后端。这里的后端指实际执行模型推理的服务实例或服务提供方。入口负责鉴权、路由和限流，后端负责输入处理与输出生成。章首每秒 30 次调用在这条路径上形成持续需求；每次调用携带的公共提示与任… |
| …en，所以请求命中率 $h$ 也等于前缀内部的 token 命中比例；相对于完整 20,000-token 输入，缓存读取占比为 $0.95h$。成本交点约为 84.9%，业务… | …en，所以请求命中率 $h$ 也等于前缀内部的 token 命中比例；相对于完整的 20,000 token 输入，缓存读取占比为 $0.95h$。成本交点约为 84.9%，业务… |
| …执行并提交操作 K，确认却丢失。恢复环境不会撤回外部操作的结果；控制器按同一操作 ID 查询结果后接续任务。实线表示请求与执行，虚线表示确认及恢复查询。* | …执行并提交操作 K，确认却丢失。恢复环境不会撤回外部操作的结果；控制器按同一操作标识查询结果后接续任务。实线表示请求与执行，虚线表示确认及恢复查询。* |
| 假设执行一次工具需要 3 CPU·秒，结果被发送两次。如果接收端按操作标识去重，累计 CPU 时间仍为 3 秒；如果控制器因确认丢失而真的再执行… | 假设执行一次工具需要 3 CPU·秒，结果被发送两次。如果接收方按操作标识去重，累计 CPU 时间仍为 3 秒；如果控制器因确认丢失而真的再执行… |
| ….010、耗时 10 秒；80% 直接成功，12% 进入局部修复，8% 直接升级。局部修复新增 0.006 和 4 秒，条件成功率 60%，其余升级。升级新增 … | ….010、耗时 10 秒；80% 直接成功，12% 进入局部修复，8% 直接升级（换用能力更强的模型重试）。局部修复新增 0.006 和 4 秒，条件成功率 60%，其余升级。升级新增 … |
| …，同样时机开始创建仍要再等 5 秒。[^core-calculation] 先只更换模型加速器，保持准备策略不变；再比较更早准备、保留已就绪环境和按需等待的成本，并计入提前占用的资源，以及提前准备的环境最终未被使用时所产生的开销。 | …，同样时机开始创建仍要再等 5 秒。[^core-calculation] 先只把模型调用加快，保持准备策略不变；再比较更早准备、保留已就绪环境和按需等待三种做法的成本，并计入提前占用的资源，以及提前准备的环境最终没有用上时的开销。 |
| ![图 11-38 模型加速后，环境创建成为工具开始执行前的等待来源。两种情况都在零时刻开始创建环境… | ![模型加速后，环境创建成为工具开始执行前的等待来源。两种情况都在零时刻开始创建环境… |
| *图 11-38　模型加速后，环境创建成为工具开始执行前的等待来源。两种情况都在零时刻开始创建环境… | *图 11-38：模型加速后，环境创建成为工具开始执行前的等待来源。两种情况都在零时刻开始创建环境… |
| V4.1 会话在等待工具时，同时保留两组状态：操作系统管理进程、文件与环境，模型服务管理全局 KV 和局部 SWA。恢复任务需要分别准备这两组状态，再在工具结果进入下一轮模型调用时汇合；已执行的… | DeepSeek V4.1 的会话在等待工具时，同时保留两组状态：操作系统管理进程、文件与环境，模型服务管理全局 KV 和局部滑动窗口注意力（SWA）状态。恢复任务需要分别准备这两组状态，再在工具结果进入下一轮模型调用时汇合；已执行的… |
| 同样的方法也解释了 RL 的资源组织：成组作业先取得配套资源，临时 rollout 先完成权重传输和加载，样本生成后立即提交验证，可以让验证与后续生成同时进行；失败… | 同样的方法也解释了 RL 的资源组织：成组作业先取得配套资源，临时的 rollout 实例先完成权重传输和加载，样本生成后立即提交验证，可以让验证与后续生成同时进行；失败… |

## 翻译腔审查（2026-09-11）

作者确认术语原则并统一“笔者”“N 个 token”“Queqiao”之后，再做一遍只针对翻译腔的审查（含图注里汉字数字改阿拉伯数字）。共 30 行有改动，按最终文本与审查前快照逐行对照。

| 修改前 | 修改后 |
|---|---|
| …境的进程与内存；工作目录和工具结果已经在持久存储中，是否值得销毁环境、下轮再建，将在后文比较。 | …境的进程与内存；工作目录和工具结果已经在持久存储中，是否值得销毁环境、下轮再建，第 11.2 节再比较。 |
| 所有这些需求都可由同一个关系得到： | 这些需求都来自同一个关系： |
| …vices，AWS）为无服务器计算开发、于 2018 年开源的轻量虚拟机监控器；其 2020 年发表于 NSDI 会议的论文讨论了如何在大量短时任务之间兼顾硬件虚拟化隔离、启动速度和内存开销。[^firecrack… | …vices，AWS）为无服务器计算开发、于 2018 年开源的轻量虚拟机监控器；Firecracker 团队 2020 年在 NSDI 会议上发表的论文讨论了怎样在大量短时任务之间兼顾硬件虚拟化隔离、启动速度和内存开销。[^firecrack… |
| …制器处汇合：它知道模型调用何时开始、工具何时使用环境，以及哪些文件需要跨轮保存。这些信息让平台能够分别安排模型状态与工具状态的保留和释放。模型计算期间可以准备下一轮环境，工具完成后可以保存文件并释放进程；后文将据此计算容量与时间开销，选择环境的创建、保留和恢复方式。 | …制器处汇合：它知道模型调用何时开始、工具何时使用环境，以及哪些文件需要跨轮保存。有了这些信息，平台就能分别安排模型状态与工具状态的保留和释放。模型计算期间可以准备下一轮环境，工具完成后可以保存文件并释放进程；下面几节据此计算容量与时间开销，选择环境的创建、保留和恢复方式。 |
| 确定隔离方式以后，平台还需为环境分配资源并加载程序所需的数据。工具平台通过 CPU 配额、内存上限以及文件和设备访问权限来分配资源。给一个等待模型的环境增加 CPU 配额，并不会改变其文件和内存状态；销… | 确定隔离方式以后，平台还需为环境分配资源并加载程序所需的数据。工具平台用 CPU 配额、内存上限以及文件和设备访问权限分配资源。给一个等待模型的环境增加 CPU 配额，并不会改变其文件和内存状态；销… |
| …只读内容可以减少重复存储，将常用数据保留在本地则可以减少远端读取。具体保留多少，应根据工具首次访问哪些页，以及以后多久会再次访问这些页来决定。 | …只读内容可以减少重复存储，将常用数据保留在本地则可以减少远端读取。具体保留多少，要看工具首次访问哪些页，以及以后多久会再访问它们。 |
| …载使创建接口更早返回，将尚未访问的数据留到真正需要时取得。E2B 的页与文件读取采用了这样的组织：启动过程先提供可运行的环境，随后的访问逐步补齐内容。首次工具执行的完成时间包含… | …载使创建接口更早返回，将尚未访问的数据留到真正需要时取得。E2B 的页与文件读取就是这样组织的：启动过程先提供可运行的环境，随后的访问逐步补齐内容。首次工具执行的完成时间包含… |
| …一段内存占用。要在这段时间内释放内存，必须先保存下一轮仍需使用的状态；因此，暂停收益与保存什么、怎样恢复密切相关。 | …一段内存占用。要在这段时间内释放内存，必须先保存下一轮仍需使用的状态；因此，暂停能省多少，取决于要保存什么、怎样恢复。 |
| 可以通过比较成本来决定是否暂停。设释放的内存为 $M$ GiB，释放后保持空闲的时间为 $G$ 秒，每 GiB… | 是否暂停，可以比较成本来决定。设释放的内存为 $M$ GiB，释放后保持空闲的时间为 $G$ 秒，每 GiB… |
| 不同状态需要采用不同的恢复方式。快照保存文件和内存，连接由客户端重新建立，外部存储则继续保留已经提交的写入。控… | 不同的状态要用不同的方式恢复。快照保存文件和内存，连接由客户端重新建立，外部存储则继续保留已经提交的写入。控… |
| 图 11-13 和图 11-14 对照了两种环境管理方式下的完整任务时间线，展示了内存占用如何变化。工具的三个执行时刻没有改变，消失的是长段模型等待期间的内存占用。这里能准确… | 图 11-13 和图 11-14 对照两种环境管理方式下的完整任务时间线，看内存占用怎样变化。工具的三个执行时刻没有改变，消失的是长段模型等待期间的内存占用。这里能准确… |
| …U 集群。研究表明，配套 CPU 不足、作业需要成组启动以及网络位置限制，都可能使空闲 GPU 无法得到利用。加入可以被抢占的低优先级作业后，研究中 GPU 分配比例从 68% 提高到 93%。这些低… | …U 集群。研究表明，配套 CPU 不足、作业需要成组启动以及网络位置限制，都可能让空闲 GPU 用不上。加入可抢占的低优先级作业后，研究中 GPU 分配比例从 68% 提高到 93%。这些低… |
| …180 GiB，为突发请求预留了更大的空间。减少等待期间保留的环境，可以腾出内存供新调用使用，使空闲 CPU 能及时开始执行。 | …180 GiB，为突发请求预留了更大的空间。减少等待期间保留的环境，可以腾出内存给新调用，空闲的 CPU 也就能及时开始执行。 |
| 图 11-24 中，增加生成加速器只缩短蓝色部分。下一步可以采取两种办法：改变执行顺序，使验证与生成重叠；或者继续增加生成加速器，但先算清新加速… | 图 11-24 中，增加生成加速器只缩短蓝色部分。下一步有两种办法：改变执行顺序，使验证与生成重叠；或者继续增加生成加速器，但先算清新加速… |
| …输数据也需要时间，这些开销都会进一步推迟整批完成的时刻。尽早提交已经生成的样本，能够将验证与后续生成重叠。图 11-26 和图 11-27 中的两种安排都需要 30 … | …输数据也需要时间，这些开销都会进一步推迟整批完成的时刻。尽早提交已经生成的样本，就能让验证与后续生成重叠。图 11-26 和图 11-27 中的两种安排都需要 30 … |
| …后，约 0.8 ms 后观察到进程退出。如果超时一到就把同一资源交给下一项任务，仅报告超时而未终止后台线程的做法，就会导致两项任务同时使用资源。对于 CUDA 性能验证，这种竞争还可能改变测得的执行时间和奖励。… | …后，约 0.8 ms 后观察到进程退出。如果超时一到就把同一资源交给下一项任务，只报告超时而不终止后台线程，两项任务就会同时使用资源。对于 CUDA 性能验证，这种竞争还可能改变测得的执行时间和奖励。… |
| …和成本，包括处理输入、生成思考内容和生成可见输出三部分。思考 token 是模型用于中间推理而非直接展示给用户的生成内容。思考预算决定允许生成多长的内部推理，实际用量决定每次调用需要支付多少费用。先在… | …和成本，包括处理输入、生成思考内容和生成可见输出三部分。思考 token 是模型生成的中间推理内容，不直接展示给用户。思考预算决定允许生成多长的内部推理，实际用量决定每次调用需要支付多少费用。先在… |
| 上一节通过减少生成量降低成本。输入这一侧的主要节省机会来自重复前缀：同一任务的多轮调用通常会重复发送公共提示和此前的对话记录。一项模型请求… | 上一节靠减少生成量降低成本。输入这一侧的主要节省机会是重复前缀：同一任务的多轮调用通常会重复发送公共提示和此前的对话记录。一项模型请求… |
| …，使用缓存更省的条件是 $np_u>p_w+(n-1)p_h$。缓存有效期越长，就越有机会通过重复读取收回首次创建的额外成本。[^platform] | …，使用缓存更省的条件是 $np_u>p_w+(n-1)p_h$。缓存有效期越长，重复读取就越有机会收回首次创建的额外成本。[^platform] |
| …读取占比变成 90%。按 token 收费时，后者节省的输入处理费用远多于前者。输入长度给每次命中赋予了不同权重。 | …读取占比变成 90%。按 token 收费时，后者节省的输入处理费用远多于前者。每次命中的权重，由输入长度决定。 |
| 执行效率提高后，自建服务开始具有成本优势时所需的任务量也会变化。以一组教学估算为例：用 4 张 B200 运行 DeepS… | 执行效率提高后，自建服务开始占成本优势所需的任务量也会变化。以一组教学估算为例：用 4 张 B200 运行 DeepS… |
| …断是否允许重试。幂等操作指以同一操作标识重复调用时，最终业务效果与调用一次相同。对只读查询或具有幂等语义的操作，重复执行通常容易处理；对外部付款、创建资源或发送消息，重复调用可能使同一操作实际发生两次，需要目标系统支持操作标识、事务或补偿。 | …断是否允许重试。幂等操作指以同一操作标识重复调用时，最终业务效果与调用一次相同。只读查询或幂等操作重复执行通常不成问题；外部付款、创建资源或发送消息重复调用，却可能让同一操作实际发生两次，需要目标系统支持操作标识、事务或补偿。 |
| … 6 秒。两种情况最后都可能只有一条结果，却消耗不同资源。消息去重解决前一种重复，持久结果和目标系统的幂等协议才有助于避免后一种重复。环境外已经提交的操作在快照恢复后仍然存在，恢复流程要先查询其提交状态。[^re… | … 6 秒。两种情况最后都可能只有一条结果，却消耗不同资源。消息去重解决前一种重复；后一种重复要靠持久化的结果和目标系统的幂等协议来避免。环境外已经提交的操作在快照恢复后仍然存在，恢复流程要先查询其提交状态。[^re… |
| …计五项成功尝试花费的 0.5 个成本单位，就会得到 0.1，恰好漏掉另一半资源。总支出除以完成数，使成功与失败共同决定单位产出成本。 | …计五项成功尝试花费的 0.5 个成本单位，就会得到 0.1，恰好漏掉另一半资源。用总支出除以完成数，成功与失败就共同决定了单位产出成本。 |
| … 20 秒时，这部分工作占用资源，却没有增加按时完成数。收紧期限改变的是哪些终点被计入分母；限制重试次数则会改变执行树本身。前者改变成功任务的统计范围，后者改变实… | … 20 秒时，这部分工作占用资源，却没有增加按时完成数。收紧期限改变的是哪些终点计入分母；限制重试次数则会改变执行树本身。前者改变成功任务的统计范围，后者改变实… |
| 模型变快以后，环境准备可能无法再与模型调用完全重叠，从而开始影响任务完成时间。假设下一次工具调用必然发生，模型还需 10 秒思考，环境创… | 模型变快以后，环境准备可能不再能与模型调用完全重叠，开始影响任务完成时间。假设下一次工具调用必然发生，模型还需 10 秒思考，环境创… |
| 表中的平均需求满足容量要求，接下来还要确认各阶段能否按时安排。均匀到达使我们可以直接写出一个满足容量限制的时间表。快速服务下，以每项任务的到达时刻为起点，分… | 表中的平均需求满足容量要求，接下来还要确认各阶段能否按时安排。任务均匀到达，可以直接写出一个满足容量限制的时间表。快速服务下，以每项任务的到达时刻为起点，分… |
| …内存的空闲时段也会缩短，原来的环境策略就需要重新比较。保留环境还是重新建立环境，可以根据两种方案成本相等时的等待长度来判断。设两次工具执行之间需要等待模型 $G$ 秒。保持 2 GiB 环境，驻留成… | …内存的空闲时段也会缩短，原来的环境策略就需要重新比较。保留环境还是重新建立环境，看两种方案成本相等时的等待长度就能判断。设两次工具执行之间需要等待模型 $G$ 秒。保持 2 GiB 环境，驻留成… |
| …为 $1/(0.1+0.9/2)\approx1.82$。同样加速两倍，模型调用带来的任务时间收益更大，因为它占用了更高比例的执行时间。 | …为 $1/(0.1+0.9/2)\approx1.82$。同样加速两倍，模型调用省下的任务时间更多，因为它占的执行时间比例更高。 |
| …核、640 GiB 内存和最多 300 次并发模型调用的容量即可支持给定任务流。成本比较随后在满足质量和期限的方案之间完成。 | …核、640 GiB 内存和最多 300 次并发模型调用的容量即可支持给定任务流。最后在满足质量和期限的方案之间比较成本。 |
