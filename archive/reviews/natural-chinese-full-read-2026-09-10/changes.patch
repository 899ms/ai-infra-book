--- a/manuscripts/00-前言.md
+++ b/manuscripts/00-前言.md
@@ -10,9 +10,9 @@
 
 这个目标很容易用一句话表达，真正设计时却要回答许多具体问题。应用需要传递一条消息，还是访问一块数据？数据传完以后，谁负责通知使用它的程序？哪些操作必须保持先后顺序，哪些可以独立推进？这些功能应该由硬件、操作系统、运行时还是应用来完成？每一种划分都能简化某些工作，也会把责任和代价转移到别处。设计接口时如果不了解应用，就可能保留许多应用并不需要的工作；只看应用的便利而忽略底层实现，又可能付出过高的容量、通信或同步代价。
 
-这段经历使我更加重视系统边界两侧的信息。一个接口隐藏了什么，实现它需要付出什么，应用是否真的需要它提供的全部能力，这些问题往往决定了优化的空间。后来回顾这段工作，我写下了《Unified Bus 背后的思考》。其中记录的是我参与早期设计时的认识；今天产品化的 UB 已经经历了后续团队的大量工作，具体实现也可能与早期方案不同。本书会在相关章节使用这些经历，说明当时的问题、备选和取舍，再结合公开规范与具体计算讨论它们的适用条件。
+这段经历使我更加重视系统边界两侧的信息。一个接口隐藏了什么，实现它需要付出什么，应用是否真的需要它提供的全部能力，这些问题往往决定了优化的空间。后来回顾这段工作，我写下了《Unified Bus 背后的思考》。其中记录的是我参与早期设计时的认识；今天产品化的 UB 已经经历了后续团队的大量工作，具体实现也可能与早期方案不同。本书会在相关章节使用这些经历，说明当时的问题、备选方案和选择理由，再结合公开规范与具体计算讨论它们的适用条件。
 
-模型应用的发展，让这些问题有了新的展开。开发者既用代码规定流程，也通过选择模型、组织上下文和提供工具来表达任务。同一个模型可以承担多种应用行为，底层则围绕共同的模型计算组织执行。应用的多样性与执行过程的共同性由此结合起来，为系统设计提供了新的机会。
+模型应用的发展，使这些问题有了新的研究对象。开发者既用代码规定流程，也通过选择模型、组织上下文和提供工具来表达任务。同一个模型可以承担多种应用行为，底层则围绕共同的模型计算组织执行。应用的多样性与执行过程的共同性由此结合起来，为系统设计提供了新的机会。
 
 模型结构给出了计算之间的依赖，运行时掌握数据的产生与使用时机，芯片和网络承担相应的运算与传输。利用这些信息，可以让相邻算子直接复用中间结果，重复使用已经准备好的执行计划，提前安排设备间的数据交接，也可以在多轮调用之间保留值得复用的状态。模型结构、编译运行时与硬件之间的选择因此相互影响。理解模型训练和推理的完整执行过程，才能判断各层哪些设计值得重新考虑。
 
@@ -26,7 +26,7 @@
 
 全书沿三个阶段推进。首先理解模型与负载，弄清需要完成什么工作；随后进入芯片与系统，研究这些工作如何在设备上执行、如何在设备之间协作；最后组织推理与训练系统，并把模型服务放回完整的任务环境与部署条件中。第一章给出了十二章的结构图、逐章导读和阅读所需的基础。随着阅读推进，同一个问题会不断加入新的约束：最初只考虑权重容量，后来还要计算上下文状态；最初比较单次执行，后来还要面对并发、交接和故障恢复。每补上一项重要条件，我们就有机会修正此前的判断。
 
-我希望这本书能帮助应用开发者理解自己的选择会带来什么资源需求，也帮助系统、网络和芯片工程师理解，上层任务为什么需要这些能力。读者可以从熟悉的问题进入，再沿着计算与数据的关系走向相邻层次。书中保留具体模型、设备和实现，是为了让推导能够落到可以检查的数字上。配套的计算脚本、实验记录和扩展资料，则方便读者更换输入，继续追问。
+我希望这本书能帮助应用开发者理解自己的选择会带来什么资源需求，也帮助系统、网络和芯片工程师理解，上层任务为什么需要这些能力。读者可以从熟悉的问题进入，再沿着计算与数据的关系走向相邻层次。书中采用具体的模型、设备和实现作为例子，是为了让推导能够落到可以检查的数字上。配套的计算脚本、实验记录和扩展资料，则方便读者更换输入，继续追问。
 
 我希望读者带走提出问题、建立估算和检验证据的习惯。遇到新的系统时，先问它要完成什么，再追踪计算、数据和等待，最后解释为什么选择这一种实现。
 
--- a/manuscripts/01-初识 AI Infra.md
+++ b/manuscripts/01-初识 AI Infra.md
@@ -10,7 +10,7 @@
 
 *图 1-1　全书结构与阅读顺序。前一部分为后一部分提供分析依据；进入服务与部署之后，还要根据任务质量、完成时间和成本，重新检查模型与资源选择。*
 
-**逐章导读。** 下表列出每章要回答的主要问题，便于读者把具体技术放回全书的分析过程。
+**逐章导读。** 下表列出每章要回答的主要问题，便于读者理解具体技术与全书各部分的联系。
 
 | 章节 | 主要问题 |
 | --- | --- |
@@ -65,7 +65,7 @@
 
 最上面是**应用与任务**。对话、代码生成、语音交互，以及能够调用工具推进任务的智能体（Agent），都在这一层提出具体要求：要完成什么任务，多快返回结果，成本上限是多少。训练也有自己的任务目标，例如在给定时间内完成一次模型更新或一轮预训练。
 
-向下一层是**模型与负载**。模型结构规定要进行哪些计算、保留哪些数据；实际负载则决定这些工作何时到来、持续多久。一个简短的问题和一篇很长的文档，即使交给同一个模型，也会形成不同的计算与存储需求。模型结构在第 2 章展开，训练与推理负载在第 3 章展开。
+下一层是**模型与负载**。模型结构规定要进行哪些计算、保留哪些数据；实际负载则决定这些工作何时到来、持续多久。一个简短的问题和一篇很长的文档，即使交给同一个模型，也会形成不同的计算与存储需求。模型结构在第 2 章展开，训练与推理负载在第 3 章展开。
 
 **训练与推理系统**负责把这些需求组织成可执行的工作。它接收请求或作业，安排批次，管理运行状态，并决定怎样使用一台或多台设备。在推理中，它要协调正在生成的请求和新进入的请求；在训练中，它还要组织参数更新、通信和恢复。第 8 至第 10 章讨论这一层。
 
@@ -77,11 +77,11 @@
 
 六个层次说明了主要分工，但有些能力贯穿多个层次。资源调度、运行环境、观测和计费需要结合多个层次的信息来组织；工艺、封装、供电和散热则决定设备能够怎样制造和部署。网络也并非只在最下方发挥作用：需要跨设备协作的模型、算子和服务，都要经过具体互联。
 
-沿这六层向下看，任务逐步变成具体的计算与数据访问；向上看，设备容量、算力与带宽决定系统能运行多大的模型、同时处理多少请求，以及多快作出响应。海思首席科学家廖恒博士提出的“十八层宝塔”，用来描述从应用、模型与软件系统，到芯片、制造工艺和底层物理的多个层次，强调这些层次之间的联系。本书据此组织全栈视角，并按教学需要将讨论内容归为图 1-3 的六层。[^panorama]
+沿这六层向下看，任务逐步变成具体的计算与数据访问；向上看，设备容量、算力与带宽决定系统能运行多大的模型、同时处理多少请求，以及多快作出响应。海思首席科学家廖恒博士提出的“十八层宝塔”，用来描述从应用、模型与软件系统，到芯片、制造工艺和底层物理的多个层次，强调这些层次之间的联系。本书借鉴这一思路，从各层之间的联系出发，并按教学需要将讨论内容归为图 1-3 的六层。[^panorama]
 
 这些层次之间的接口，也是开展联合优化的切入点。模型改变状态表示，会改变存储和通信需求；编译器将相邻算子融合，会改变中间结果的保存位置；运行时让设备直接交接，会改变主机参与的次数。后续章节将从这些具体变化出发，计算省下的读写、提交和等待，再比较完整请求的耗时，判断哪一种组织方式更合适。
 
-AI Infra 建立在云数据中心已有的服务器、网络、存储和资源管理技术之上，但模型执行的需求改变了资源配置的重点。同一个模型可以支持问答、代码生成等多种应用；一次模型调用又可能分布在许多加速器上，使网络通信直接进入计算依赖链。基础设施既要服务独立请求，也要组织设备之间的紧密协作。
+AI Infra 建立在云数据中心已有的服务器、网络、存储和资源管理技术之上，但模型执行的需求改变了资源配置的重点。同一个模型可以支持问答、代码生成等多种应用；一次模型调用又可能分布在许多加速器上，使后续计算必须等待网络传输完成。基础设施既要服务独立请求，也要组织设备之间的紧密协作。
 
 多个独立用户共用资源的服务称为多租户服务。它既要共享模型计算能力，也要隔离不同用户的数据和权限；第 11 章将讨论相应的运行环境。下面沿一次生成请求，把六层分工连接起来。
 
@@ -99,7 +99,7 @@
 
 模型先处理输入，这一阶段称为预填充（prefill）；随后逐步生成新 token，这一阶段称为解码（decode）。后续步骤会复用此前保存的状态，其中的 KV 缓存（KV cache）保存上下文位置产生的键和值两类向量，供后续位置计算注意力时复用。键和值是注意力机制使用的数据表示，第 2 章会从具体运算说明它们怎样产生。生成循环由实例内部的调度器持续推进，前一步输出成为下一步输入。产生的 token 经输出处理转换为文本，沿连接流式返回；如果输出要求调用工具，应用运行工具，把结果加入上下文，再发起后续调用。
 
-模型权重是训练得到、用于把输入变换成输出的数值参数。它们在实例启动或模型切换时从存储加载，随后常驻显存（称为驻留），供多次请求使用。生成时，GPU 计算单元从显存读取当前层的权重和上下文状态，完成运算，再将结果交给下一层。于是，同一份数据经历了两种不同频率的搬运：加载把模型送到设备，执行则反复把所需数据送到计算单元。在多卡实例中，一张卡算出的中间结果还要传给其他卡，后续计算才能继续。中间张量是运算产生并交给后续运算的多维数值数组，矩阵就是二维张量。**外部请求可能只有一小段文字，内部搬运的数据却包括大得多的权重、状态和中间张量。**
+模型权重是训练得到、用于把输入变换成输出的数值参数。它们在实例启动或模型切换时从存储加载，随后持续存放在显存中（称为驻留），供多次请求使用。生成时，GPU 计算单元从显存读取当前层的权重和上下文状态，完成运算，再将结果交给下一层。于是，同一份数据经历了两种不同频率的搬运：加载把模型送到设备，执行则反复把所需数据送到计算单元。在多卡实例中，一张卡算出的中间结果还要传给其他卡，后续计算才能继续。中间张量是运算产生并交给后续运算的多维数值数组，矩阵就是二维张量。**外部请求可能只有一小段文字，内部搬运的数据却包括大得多的权重、状态和中间张量。**
 
 ![权重加载一次，生成时逐步读取。蓝框表示显存中持续保存的同一份权重，三个绿框表示先后发生的计算；箭头表示读取或结果依赖，步骤间距不代表耗时。](ch01/figure-1-weight-lifetime.svg)
 
@@ -257,7 +257,7 @@
 
 为便于手算，将该模型的参数量近似取为 $N=70\times10^9$，先按每参数一字节估算主要权重数据，读入后转换为 BF16 参加矩阵运算。一个请求每步处理一个新 token，每步从显存读取一遍这些权重。
 
-**估算条件：权重读取与矩阵计算充分重叠。** 容量采用上一节计入量化附加数据的 73.73 GB 结果；本节先用 70 GB 近似主要权重的读取量。时间模型只计主要权重矩阵的乘加和一次完整权重读取，采用资源表中的 BF16 矩阵峰值与 HBM 带宽；读取和计算按充分重叠估算。上下文状态访问、格式转换和程序启动在后续模型中逐项加入。这一题设用来识别当前最紧的资源约束。
+**估算条件：权重读取与矩阵计算充分重叠。** 容量采用上一节计入量化附加数据的 73.73 GB 结果；本节先用 70 GB 近似主要权重的读取量。时间模型只计主要权重矩阵的乘加和一次完整权重读取，采用资源表中的 BF16 矩阵峰值与 HBM 带宽；读取和计算按充分重叠估算。上下文状态访问、格式转换和程序启动在后续模型中逐项加入。这一题设用来识别当前最先限制性能的资源。
 
 上一节已经检查过单请求的容量预算。现在看生成速度：权重加载到显存之后，每一步仍需将参与计算的权重送到计算单元。按上述近似，每步读取约 70 GB，下面先计算读取这些数据需要多久。
 
@@ -333,7 +333,7 @@
 
 *图 1-12　批内请求增加时，矩阵运算量按 $2BN$ 增长，权重读取保持每批 70 GB。约 148 个请求处两项下界相等，随后计算耗时决定整体速度。*
 
-把每批输出数 $B$ 除以上图中的时间下界，便得到这组题设对应的吞吐上界。转折前，共享读取被更多输出分摊；转折后，计算时间与输出数一起增长，曲线逐渐进入平台。
+把每批输出数 $B$ 除以上图中的时间下界，便得到这组题设对应的吞吐上界。转折前，共享读取被更多输出分摊；转折后，计算时间与输出数一起增长，曲线逐渐趋于平坦。
 
 ![同一批量模型的理想输出吞吐。每批输出数除以时间下界得到曲线；竖虚线与前图对应同一个约 148 请求的转折点。](ch01/figure-1-batch-throughput.svg)
 
@@ -347,19 +347,19 @@
 
 ### 1.3.3 用实测检验估算
 
-前面的模型预测了两个趋势：batch 较小时，吞吐随 batch 增加；整批时间则受到一次权重读取的限制。将模型用于真实程序时，还要检查各请求新增的上下文访问和计算。图 1-14、1-14 给出 Qwen3-8B 在 RTX PRO 6000 Blackwell 上的测量：使用 BF16 权重。vLLM 是加州大学伯克利分校等机构的研究者于 2023 年提出的开源推理服务系统，最初重点解决 KV 缓存浪费显存、限制批量的问题；它组织模型请求并执行推理。本次采用 0.23 版本的 eager 模式，即按程序运行过程逐项提交设备工作。每请求输入 2048 个 token、生成 256 个 token，每档 batch 测量三次。[^measurement] 吞吐表示单位时间内产生的输出数；TPOT（time per output token）在这里表示客户端观察到的平均输出间隔。下面先看整批产出，再看单个请求的生成节奏。
+前面的模型预测了两个趋势：batch 较小时，吞吐随 batch 增加；整批时间则受到一次权重读取的限制。将模型用于真实程序时，还要检查各请求新增的上下文访问和计算。图 1-14、1-15 给出 Qwen3-8B 在 RTX PRO 6000 Blackwell 上的测量：使用 BF16 权重。vLLM 是加州大学伯克利分校等机构的研究者于 2023 年提出的开源推理服务系统，最初重点解决 KV 缓存浪费显存、限制批量的问题；它组织模型请求并执行推理。本次采用 0.23 版本的 eager 模式，即按程序运行过程逐项提交设备工作。每请求输入 2048 个 token、生成 256 个 token，每档 batch 测量三次。[^measurement] 吞吐表示单位时间内产生的输出数；TPOT（time per output token）在这里表示客户端观察到的平均输出间隔。下面先看整批输出速度，再看单个请求的生成节奏。
 
 ![Qwen3-8B 实测的整批输出吞吐。四档请求数等距排列，纵轴从零开始；模型、精度、输入输出长度和计时范围保持一致。](ch01/figure-1-measured-throughput.svg)
 
 *图 1-14　Qwen3-8B 实测的整批输出吞吐。四档请求数等距排列，纵轴从零开始；模型、精度、输入输出长度和计时范围保持一致。*
 
-![同一组实测中的每请求输出间隔。吞吐增加的同时，单个请求的平均间隔也在增大；两张图分别回答设备产出和用户等待的问题。](ch01/figure-1-measured-tpot.svg)
-
-*图 1-15　同一组实测中的每请求输出间隔。吞吐增加的同时，单个请求的平均间隔也在增大；两张图分别回答设备产出和用户等待的问题。*
+![同一组实测中的每请求输出间隔。吞吐增加的同时，单个请求的平均间隔也在增大；两张图分别说明设备的输出速度和用户的等待时间。](ch01/figure-1-measured-tpot.svg)
+
+*图 1-15　同一组实测中的每请求输出间隔。吞吐增加的同时，单个请求的平均间隔也在增大；两张图分别说明设备的输出速度和用户的等待时间。*
 
 从 1 个请求增到 64 个请求，整批吞吐约提高 31 倍，单请求输出间隔则从约 26.5 ms 增至 40.5 ms。两幅图合起来描述批量组织的取舍：设备在同一时间内完成更多请求的工作，每个请求完成一步生成所需的时间也更长。
 
-图 1-14 的吞吐按整批输出数除以处理这批请求的总时间计算，其中包括输入处理时间。TPOT 则先对每个请求计算首末输出事件之间的时间，再除以剩余的 255 个输出间隔，最后求所有请求的平均值。前者描述整个实例的产出，后者描述单个用户经历的生成节奏。第三章将进一步分析请求到达、排队和少量慢请求对这些指标的影响。
+图 1-14 的吞吐按整批输出数除以处理这批请求的总时间计算，其中包括输入处理时间。TPOT 则先对每个请求计算首末输出事件之间的时间，再除以剩余的 255 个输出间隔，最后求所有请求的平均值。前者描述整个实例的输出速度，后者描述单个用户经历的生成节奏。第三章将进一步分析请求到达、排队和少量慢请求对这些指标的影响。
 
 这一趋势可以从权重与上下文状态的区别来解释。多个请求共同使用模型权重，但各自保存上下文状态。并发增加时，一次矩阵运算会处理更多请求当前输入的 token 表示，权重得到更多复用；与此同时，注意力计算和上下文访问也随请求数增加。
 
@@ -373,7 +373,7 @@
 
 > **练习 1-4〔核心〕：用测量检验预测**
 >
-> 根据图 1-14、1-14 标出的四档并发请求测量值，计算相对单请求的吞吐加速比和 TPOT 增长比例。如果只有共享权重读取、没有新增计算和上下文访问，吞吐应怎样变化？对照实际结果，提出两个可区分的原因，并设计一次只改变上下文长度的测量来判断它们。指出测量前需要保持不变的模型、输出长度与计时起止点。
+> 根据图 1-14、1-15 标出的四档并发请求测量值，计算相对单请求的吞吐加速比和 TPOT 增长比例。如果只有共享权重读取、没有新增计算和上下文访问，吞吐应怎样变化？对照实际结果，提出两个可区分的原因，并设计一次只改变上下文长度的测量来判断它们。指出测量前需要保持不变的模型、输出长度与计时起止点。
 
 回看这次估算，我们实际上一直在回答五个问题：**搬什么、搬多少、搬几次、经过哪里、谁在等待。** 模型结构决定需要哪些权重，位宽和参数量决定存储大小，批量复用改变了读取次数，显存接口提供了带宽，执行依赖决定了等待。这五个问题把模型结构、数据路径和执行顺序连成一份可以逐项计算的预算。
 
@@ -431,9 +431,9 @@
 
 *图 1-18　统一互联连接不同设备的计算与存储资源。模型分工确定要交换什么，互联负责把数据送到后续使用它的设备。*
 
-从语音推理到云网络，再到大模型的多设备执行，设计目标依次落在计算效率、主机 CPU 占用和跨设备访问上。需求改变了需要优先解决的瓶颈，也改变了硬件、软件与互联之间的分工。
-
-再看开篇的六层图，三个案例分别从不同问题出发，却都会改变计算与数据移动的组织。应用需求推动资源专用化，处理位置改变主机与网卡之间的工作分配，更大的模型则要求重新安排设备协作。后面的章节会沿这些具体关系展开本书的主线：**数据搬运塑造了 AI Infra 的架构。**
+从语音推理到云网络，再到大模型的多设备执行，这些设计分别着重提高计算效率、减少主机 CPU 占用和改善跨设备访问。需求改变了需要优先解决的瓶颈，也改变了硬件、软件与互联之间的分工。
+
+再看开篇的六层图，三个案例分别从不同问题出发，却都会改变计算与数据移动的组织。应用需求推动专用硬件的设计，处理位置改变主机与网卡之间的工作分配，更大的模型则要求重新安排设备协作。后面的章节会沿这些具体关系展开本书的主线：**数据搬运塑造了 AI Infra 的架构。**
 
 以本章的两卡模型部署为例：权重均分可以满足逐卡容量约束，但每个阶段还要等待输入数据和前序结果。增加设备改变了 $M_{\mathrm{cap}}$ 和可用算力，也引入新的通信量与依赖。因此，第一步先检查每台设备能否放下所需数据，第二步计算各设备的运算量与读写量，第三步根据执行依赖计算总时间。模型再大、设备再多，这个分析顺序仍然成立。
 
@@ -463,7 +463,7 @@
 
 这组数字解释了两阶段的差别：prefill 在一次调用中处理大量输入，矩阵工作量较大；单请求 decode 每次只处理一个位置，却仍要读取大量权重和上下文。提高计算吞吐、提高带宽和增加批内复用，分别作用于不同的时间项。
 
-实际响应表现可参见图 1-14、1-14：那里使用 RTX PRO 6000 Blackwell、vLLM 0.23 和明确的请求长度，显示吞吐与客户端输出间隔。GPU 规格表提供硬件性能数据，模型表用于估算工作量，实测图则展示系统实际运行的表现。
+实际响应表现可参见图 1-14、1-15：那里使用 RTX PRO 6000 Blackwell、vLLM 0.23 和明确的请求长度，显示吞吐与客户端输出间隔。GPU 规格表提供硬件性能数据，模型表用于估算工作量，实测图则展示系统实际运行的表现。
 
 [^panorama]: “十八层宝塔”的提出者为海思首席科学家廖恒博士。全栈讨论的背景资料见[《Unified Bus 背后的思考》本地原件](../references/files/documents/ub-reflection.md)及[系统抽象边界取材记录](../research/system-abstraction-boundary/report.md)。六层图为本书自绘的教学分组。
 
@@ -501,7 +501,7 @@
 
 ## 本章小结
 
-应用通过模型与上下文表达行为，使许多任务共享相近的底层计算。掌握模型的计算关系与状态寿命，就能统筹考虑原来分属不同层次的设计选择：改变数据表示、合并执行步骤、复用准备工作，或缩短设备交接。后续各章将量化这些改动的收益，说明哪些设计取舍因此改变，再根据新的约束选择模型与系统方案。
+应用通过模型与上下文表达行为，使许多任务共享相近的底层计算。掌握模型的计算关系与状态寿命，就能统筹考虑原来分属不同层次的设计选择：改变数据表示、合并执行步骤、复用准备工作，或缩短设备间数据传递和同步的时间。后续各章将量化这些改动的收益，说明哪些设计取舍因此改变，再根据新的约束选择模型与系统方案。
 
 分析系统时，先检查数据能否放下，再用运算量和读写量估算耗时，最后根据执行顺序分析重叠与等待。在 70B 算例中，权重读取比矩阵计算慢约 148 倍，因此增加带宽、减少读取和提高复用更有助于缩短时间。批内共享可以提高吞吐，而每个请求的响应时间还要包括整批执行和排队。
 
--- a/manuscripts/02-模型架构.md
+++ b/manuscripts/02-模型架构.md
@@ -93,8 +93,8 @@
 | $m=BP$ | 本次参与投影的 token 总数，也就是输入矩阵行数；普通 decode 时 $P=1$，故 $m=B$ |
 | $d,f$ | 主干隐藏宽度、前馈网络中间宽度 |
 | $n_Q,n_{\mathrm{KV}},d_h$ | 查询头数、KV 头数、每头维度 |
-| $N_{\mathrm{pair}}$ | 每层跨全部请求的有效因果位置对数，不含头数 |
-| $t_e$ | 分派给专家 $e$ 的 token 数，每个 token 表示占该专家输入矩阵的一行；无丢弃路由时 $\sum_e t_e=mk_{\mathrm{top}}$ |
+| $N_{\mathrm{pair}}$ | 每层所有请求合计的有效因果位置对数，不含头数 |
+| $t_e$ | 分派给专家 $e$ 的 token 数，每个 token 表示占该专家输入矩阵的一行；路由过程中不丢弃分派给专家的 token 时 $\sum_e t_e=mk_{\mathrm{top}}$ |
 | $m_{\mathrm{out}}$ | 实际进行词表投影的 token 位置数；只用每条请求的末位置预测下一个 token 时为 $B$，它不等于本次生成的全部输出长度 |
 
 计算整模型时，先按表格中每项操作的矩阵尺寸求一次调用的运算量，再乘这一类层的数量。词表头另外按实际输出位置数 $m_{\mathrm{out}}$ 计算；生成下一 token 时，每条请求只需最后一个位置的分数。归一化、激活与查表操作各有自己的运算方式，下面沿数据流说明它们的作用。
@@ -109,7 +109,7 @@
 
 采用数学右乘记号，输入 $X$ 分别乘三个权重矩阵，得到 $Q$、$K$、$V$。$Q$ 的总宽度为 $32\times 128=4096$，$K$ 和 $V$ 的总宽度各为 $8\times 128=1024$。因此，$W_q$ 为 $[4096,4096]$，$W_k$ 和 $W_v$ 各为 $[4096,1024]$。代码中的线性层常以 $(d_{\mathrm{out}},d_{\mathrm{in}})$ 保存权重；换成数学右乘记号后，输入宽度在前、输出宽度在后，便可逐项对照下面的表格。
 
-上述投影产生了 $Q$、$K$、$V$，但尚未让当前位置与上下文交换信息。接下来将 $Q$ 拆成 32 个头，把 $K$、$V$ 拆成 8 个头。每组四个 $Q$ 头共用一个 KV 头。对一个 $Q$ 头，查询向量与允许访问位置的 $K$ 做点积，将点积除以头宽的平方根，使分数尺度随头宽保持稳定，然后屏蔽未来位置。Softmax 把允许位置的分数 $s_j$ 变成正数并归一化，系数为 $a_j=e^{s_j}/\sum_i e^{s_i}$；这些系数之和等于一，决定各上下文位置贡献多少信息。这里的“注意力权重”是本次输入算出的系数，区别于训练后保存的模型参数。随后用这些权重对 $V$ 加权求和。各 $Q$ 头的结果拼接后，经 $[4096,4096]$ 的输出投影 $W_o$ 返回隐藏空间。
+上述投影产生了 $Q$、$K$、$V$，但尚未让当前位置与上下文交换信息。接下来将 $Q$ 拆成 32 个头，把 $K$、$V$ 拆成 8 个头。每组四个 $Q$ 头共用一个 KV 头。对一个 $Q$ 头，查询向量与允许访问位置的 $K$ 做点积，将点积除以头宽的平方根，使分数尺度随头宽保持稳定，然后屏蔽未来位置。Softmax 把允许位置的分数 $s_j$ 变成正数并归一化，系数为 $a_j=e^{s_j}/\sum_i e^{s_i}$；这些系数之和等于一，决定各上下文位置贡献多少信息。这里的“注意力权重”是本次输入算出的系数，区别于训练后保存的模型参数。随后用这些权重对 $V$ 加权求和。各 $Q$ 头的结果拼接后，经 $[4096,4096]$ 的输出投影 $W_o$ 转回隐藏向量。
 
 用矩阵记号写出一个头的计算：
 
@@ -163,7 +163,7 @@
 
 **表 2-1　Qwen3-8B 的单层模块与输出接口**
 
-注意力和前馈各行按 36 个主干层累计；词嵌入是词表查行，不再按一次词表矩阵乘法（GEMM，通用矩阵乘法）计算。注意力的 $K$、$V$ 只各有 1024 维，而查询仍有 4096 维。
+注意力和前馈各行按 36 个主干层累计；词嵌入根据 token 编号读取词表中对应的行，不再按一次词表矩阵乘法（GEMM，通用矩阵乘法）计算。注意力的 $K$、$V$ 只各有 1024 维，而查询仍有 4096 维。
 
 **模型入口**
 
@@ -237,7 +237,7 @@
 | --- | --- | --- |
 | 权重 | 通常在多次调用间保持不变 | 常驻字节、被访问的权重集合、批内复用 |
 | 上下文状态 | 随请求保留，持续追加或更新 | 实际占用的容量、每步读写量及更新所需的计算 |
-| 临时数据 | 在相应算子或阶段中产生，使用后释放 | 张量尺寸、接口数据量、需要同时保存的部分 |
+| 临时数据 | 在相应算子或阶段中产生，使用后释放 | 张量尺寸、经过相应存储接口的数据量、需要同时保存的部分 |
 
 Qwen3-8B 全部 BF16 权重约占 16.38 GB，即 15.26 GiB。模型加载后，这份数据供多个请求反复使用。一次前向中，嵌入按 token ID 查行，层内投影与 FFN 使用对应矩阵，末端词表头计算输出分数。访问哪些权重，因而由计算图中的具体操作决定。
 
@@ -257,7 +257,7 @@
 
 以 $R_W=15{,}136{,}811{,}008$ 字节、$c_{\mathrm{KV}}=147456$ 字节代入，$H=8192$ 时最小整数 batch 为 13；$H=2048$ 时为 51。上下文缩短到四分之一，使上下文读取量超过权重读取量所需的请求数约增至四倍。上下文越长，批内权重复用的收益就越早受到限制，因为每个新增请求都会增加一份独立上下文的读取。[^source-12]
 
-沿第一章的数据路径看，加载把权重从存储送到 GPU 显存，执行再把所需权重送到计算单元。KV 和层间激活也可在设备内部保存与使用。相同的逻辑访问因此可以留在片上、经过 HBM，或跨设备传输；第四章将给这些路径分别配置容量与带宽。
+沿第一章的数据路径看，加载把权重从存储送到 GPU 显存，执行再把所需权重送到计算单元。KV 和层间激活也可在设备内部保存与使用。相同的逻辑访问因此可以留在片上、经过 HBM，或跨设备传输；第四章将分别分析这些路径的存储容量与传输带宽。
 
 ### 2.2.3 生成完整回答的计算量与状态读写量
 
@@ -533,7 +533,7 @@
 
 解： 即使激活参数相同，batch 内的权重读取也可能不同。假设同时为 $B=64$ 条请求各执行一步 decode，每条请求本次输入一个 token，因此 $m=B=64$。每个 token 选择 8 个专家，总共有 512 次 token–expert 分派。若均匀分到 256 个专家，每个专家平均接收 2 个 token，当前批次会访问全部路由专家；若所有 token 都集中到同 8 个专家，则每个专家接收 64 个 token，当前批次只访问 8 份专家权重。
 
-设本批实际访问的不同专家数为 $U$。未丢弃任何分派的路由满足 $\sum_e t_e=Bk_{\mathrm{top}}$，因此
+设本批实际访问的不同专家数为 $U$。若路由过程中不丢弃任何分派给专家的 token，则有 $\sum_e t_e=Bk_{\mathrm{top}}$，因此
 
 $$
 F_{\mathrm{expert}}=2N_e\sum_e t_e=2N_eBk_{\mathrm{top}},\qquad R_{\mathrm{expert}}=b_WN_eU.
@@ -554,13 +554,13 @@
 
 Qwen3.6 在已有 8192-token 上下文的 $B=1$ decode 中约需 7.33 GFLOPs，小于 Qwen3-8B 的约 20.0 GFLOPs。整个模型的计算量差异来自几项结构上的变化：2048 维主干缩小投影，30 个线性层使用递推状态，MoE 每次只执行选中的专家及共享分支。专家数量增加使总参数量增大，而每次调用的工作量取决于实际执行了哪些运算。
 
-稀疏激活把总参数容量与每 token 的激活计算分开，使模型可以用较少计算访问较大的参数集合。服务时，路由结果决定每份专家权重供多少 token 复用：少量 token 分散到很多专家，会产生许多小矩阵，每次读入的权重也很少得到复用；集中到同一组专家，则能组成更大的矩阵。例题 2-4 展示的正是这两种访问方式的差别。
+稀疏激活让总参数量与每个 token 实际使用的参数量可以分别调整，使模型可以用较少计算访问较大的参数集合。服务时，路由结果决定每份专家权重供多少 token 复用：少量 token 分散到很多专家，会产生许多小矩阵，每次读入的权重也很少得到复用；集中到同一组专家，则能组成更大的矩阵。例题 2-4 展示的正是这两种访问方式的差别。
 
 ### 2.4.3 共享专家与潜空间计算
 
 上述差距由专家接收的输入行数决定。接下来还要考虑专家本身的尺寸：共享专家处理全部输入，潜空间专家则先缩小输入维度。两者都会改变一个专家需要多少权重、执行多少运算。
 
-DeepSeek V4-Flash 每层有 256 个路由专家，每 token 选 6 个，另有 1 个共享专家；路由专家中间维度为 2048。单专家的 gate/up 为 $[t_e,4096]\times [4096,2048]$，down 为反向尺寸，参数为 25,165,824。前三层采用哈希路由，后续采用评分路由：哈希路由按 token 确定专家，评分路由根据当前表示选择专家。
+DeepSeek V4-Flash 每层有 256 个路由专家，每 token 选 6 个，另有 1 个共享专家；路由专家中间维度为 2048。单专家的 gate/up 为 $[t_e,4096]\times [4096,2048]$，down 的输入与输出维度对调，参数为 25,165,824。前三层采用哈希路由，后续采用评分路由：哈希路由按 token 确定专家，评分路由根据当前表示选择专家。
 
 共享专家处理本次全部 $m$ 个 token 的特征向量，路由专家 $e$ 只处理分派给它的 $t_e$ 个 token。若共享专家有 $N_s$ 个参数，一层的专家矩阵运算量为 $2mN_s+2\sum_e t_eN_e$。普通单步 decode 中，每条请求输入一个新 token，此时 $m=B$；prefill 则要累计各请求本次处理的全部 token。DeepSeek V4-Flash 每 token 选择六个路由专家并执行一个共享专家，正是这两个部分相加；容量则需要保存全部 256 个路由专家和共享分支。
 
@@ -582,7 +582,7 @@
 
 专家结构也可以作类似调整，同时保持参数量和运算量不变。单层路由专家的总参数为 $3Edf$，单 token 专家运算量为 $6k_{\mathrm{top}}df$。将专家数 $E$ 减半、中间维度 $f$ 加倍、选中数 $k_{\mathrm{top}}$ 减半，这两个量都不变。Qwen3-235B-A22B 的 128 个宽度 1536 的专家、每 token 选 8 个，可以与 64 个宽度 3072 的专家、每 token 选 4 个作这一对照。路由输出维度与每专家接收行数仍会改变，从而改变分组矩阵乘法的形状。
 
-在 16-token 算例中，三种专家粒度的专家矩阵运算量保持相同，基线与 64 专家变体均约为 454 GFLOPs。专家数减半、单专家宽度加倍时，相同数量的权重被分成更少的大矩阵；专家数加倍、宽度减半时，则成为更多的小矩阵。路由器要为每个专家计算路由分数，因此其参数也随专家数变化。[专家粒度计算](../calculations/results/qwen235-granularity-baseline.md)将路由与专家矩阵分别累计，呈现同一专家参数总量如何形成不同的执行形状。
+在 16-token 算例中，三种专家粒度的专家矩阵运算量保持相同，基线与 64 专家变体均约为 454 GFLOPs。专家数减半、单专家宽度加倍时，相同数量的权重被分成更少的大矩阵；专家数加倍、宽度减半时，则成为更多的小矩阵。路由器要为每个专家计算路由分数，因此其参数也随专家数变化。[专家粒度计算](../calculations/results/qwen235-granularity-baseline.md)将路由与专家矩阵分别累计，说明专家参数总量相同时，矩阵形状为何仍会不同。
 
 形状也决定多卡分工的最小单位。128 个专家可分成八组，每组 16 个；四个 KV 头按完整头分工时只能形成四份，八卡执行就需要复制部分 KV 或调整分组。第五章从矩阵分块分析执行效率，第六章将进一步把这些矩阵和状态分配到设备。
 
@@ -592,9 +592,9 @@
 
 普通残差把不同深度的信息不断相加到同一条通路中。为了让层间信息有更多组合方式，同时保持深层网络中的信号传播稳定，DeepSeek V4-Flash 使用 **mHC（Manifold-Constrained Hyper-Connections，流形约束超连接）**。它把一条残差通路扩展成多条，并约束通路之间的混合系数；这里的“流形约束”具体表现为将残差混合矩阵的元素限制为非负、各行与各列的和接近 1。[^architecture-motivation]
 
-该模型保存 4 路残差状态，每一路都是当前 token 的一份 4096 维中间表示。保留四路表示，是为了让后续层学习如何组合不同通路的信息。每个子层先将四路表示混合为 4096 维子层输入，执行注意力或 FFN，再把结果写回残差通路。因此，主体矩阵保持 4096 维输入，新增工作集中在进入和离开子层的混合与汇合。四路状态扩大了跨层表示，混合操作负责连接它们与原有子层。
-
-处理 8192 个输入 token 时，mHC 的混合投影约需 555 GFLOPs，普通算术约需 149 GFLOPs。残差混合矩阵先经 **Sinkhorn 迭代**归一化，即交替将每行、每列除以各自的和，使行和与列和逐渐接近 1；这使多层混合中的数值尺度更稳定。归一化后的系数再用于组合各条残差通路；末端汇合这些通路，形成送入词表头的表示。因此，多路残差不仅增加要保存的表示，还增加每层的混合与归一化计算。[^source-4]
+该模型保存 4 路残差状态，每一路都是当前 token 的一份 4096 维中间表示。保留四路表示，是为了让后续层学习如何组合不同通路的信息。每个子层先将四路表示混合为 4096 维子层输入，执行注意力或 FFN，再把结果写回残差通路。因此，主体矩阵保持 4096 维输入，新增工作集中在进入和离开子层的混合与汇合。四路状态让层间可以传递更多组表示，混合操作负责连接它们与原有子层。
+
+处理 8192 个输入 token 时，mHC 的混合投影约需 555 GFLOPs，非矩阵运算约需 149 GFLOPs。残差混合矩阵先经 **Sinkhorn 迭代**归一化，即交替将每行、每列除以各自的和，使行和与列和逐渐接近 1；这使多层混合中的数值尺度更稳定。归一化后的系数再用于组合各条残差通路；末端汇合这些通路，形成送入词表头的表示。因此，多路残差不仅增加要保存的表示，还增加每层的混合与归一化计算。[^source-4]
 
 Kimi K3 希望当前层能够按需要选取不同深度的信息，而不只是接收逐层相加后的一个混合结果。它使用 **Attention Residuals（AttnRes，注意力残差连接）**，对较浅层的表示计算权重，再加权求和，形成当前层的输入。这种注意力沿网络深度选择信息，前面的序列注意力则在不同 token 位置之间选择信息。为减少保存和读取所有层输出的开销，块式实现把若干层组织成一块，保留供后续块选择的表示。[^architecture-motivation]
 
@@ -675,7 +675,7 @@
 
 阅读时可以按数据流走一遍：输入进入哪类注意力层，随后经过哪类前馈网络，最终如何得到词表分数。每张表按注意力、前馈与连接、输出接口分组；“层／调用数”给出整模型累计倍数，“矩阵 FLOPs”仍是该行对应操作执行一次的浮点运算量。先确定这一层采用的注意力、前馈网络与连接结构，再累加该层的注意力、前馈与连接工作，最后加入嵌入查行、末端归一化和词表头。
 
-将同样的累加规则用于其他模型，可以逐项列出它们的计算分支。对 Qwen3-8B，表中包含嵌入、36 层注意力与 FFN、残差和归一化、最终输出头；对 Qwen3.6，还需分别计算线性注意力层与完整注意力层，并列路由与共享专家。对 DeepSeek V4-Flash，还要加入窗口、压缩、索引和 mHC；对 Kimi K3，则包括 KDA、MLA、潜空间投影、共享专家与 AttnRes。MTP 与视觉模块按本次执行范围单独加入。
+将同样的累加规则用于其他模型，可以逐项列出它们的计算分支。对 Qwen3-8B，表中包含嵌入、36 层注意力与 FFN、残差和归一化、最终输出头；对 Qwen3.6，还需分别计算线性注意力层与完整注意力层，分别列出路由专家与共享专家。对 DeepSeek V4-Flash，还要加入窗口、压缩、索引和 mHC；对 Kimi K3，则包括 KDA、MLA、潜空间投影、共享专家与 AttnRes。MTP 与视觉模块按本次执行范围单独加入。
 
 | 模型分支 | 上下文状态 | 权重与附加计算 | 合计时最容易遗漏的部分 |
 | --- | --- | --- | --- |
@@ -814,9 +814,9 @@
 N_{\max}=\frac{C-M_0-BK}{b_w}.
 $$
 
-先固定服务状态，计算留给权重的空间。取 24 GB 容量、2 GiB 工作区、4 条各 8192 位置的请求，每请求 KV 沿用 Qwen3-8B 的 1.125 GiB。BF16 每参数 2 字节，剩余空间最多容纳约 85.1 亿参数。[^core-calculation]图中先扣除服务状态，再划出权重预算。
-
-改变模型结构时，权重预算与状态预算会一起变化。增加层数或 KV 头数会增大每请求的 $K$，留给权重的空间随之减少；跨设备部署则扩大总容量，同时增加通信。Llama 3 报告中的 405B BF16 推理使用两台、共 16 张 H100，便跨越了单机容量边界。[^codesign]模型规模、状态结构与设备数量因此需要共同选择。
+先确定服务所需的状态大小，计算留给权重的空间。取 24 GB 容量、2 GiB 工作区、4 条各 8192 位置的请求，每请求 KV 沿用 Qwen3-8B 的 1.125 GiB。BF16 每参数 2 字节，剩余空间最多容纳约 85.1 亿参数。[^core-calculation]图中先扣除服务状态，再划出权重预算。
+
+改变模型结构时，权重预算与状态预算会一起变化。增加层数或 KV 头数会增大每请求的 $K$，留给权重的空间随之减少；跨设备部署则扩大总容量，同时增加通信。Llama 3 报告中的 405B BF16 推理使用两台、共 16 张 H100，其所需容量超过一台服务器所能提供的显存。[^codesign]模型规模、状态结构与设备数量因此需要共同选择。
 
 ![图 2-34 从设备反推权重预算。24 GB 中先为 4 条 8K 请求的 KV 和 2 GiB 工作区预留空间，余量给出 BF16 参数上界；图中容量均为十进制 GB。](ch02/figure-2-reverse-budget.svg)
 
@@ -1042,7 +1042,7 @@
 
 **表 2-5　Kimi K3：KDA、紧凑 MLA 与潜空间专家**
 
-93 层包含 69 个 KDA 层、24 个 MLA 层；首层使用稠密 FFN，其余层从 896 个路由专家中选 16 个，并执行合计宽度 $2\times3072=6144$ 的共享分支。表内 MLA 采用紧凑代数路径，96 路小投影分别使用每头参数。共享专家保持 7168 维主干空间，路由专家才进入 3584 维潜空间。
+93 层包含 69 个 KDA 层、24 个 MLA 层；首层使用稠密 FFN，其余层从 896 个路由专家中选 16 个，并执行合计宽度 $2\times3072=6144$ 的共享分支。表内 MLA 采用直接对潜变量计算注意力的紧凑路径，96 路小投影分别使用每头参数。共享专家直接处理 7168 维主干向量，路由专家才进入 3584 维潜空间。
 
 **模型入口**
 
@@ -1069,7 +1069,7 @@
 | MLA：查询下投影       | $m\times7168\to m\times1536$  | $7168\times1536$      | $2\times m\times7168\times1536$             |    24 |
 | MLA：查询上投影       | $m\times1536\to m\times18432$ | $1536\times18432$     | $2\times m\times1536\times18432$            |    24 |
 | MLA：潜变量及额外分支    | $m\times7168\to m\times576$   | $7168\times576$       | $2\times m\times7168\times576$              |    24 |
-| MLA：将键上投影吸收到查询侧 | $m\times128\to m\times512$    | $128\times512$，共 96 个 | $96\times 2\times m\times128\times512$      |    24 |
+| MLA：将键的上投影与查询变换合并 | $m\times128\to m\times512$    | $128\times512$，共 96 个 | $96\times 2\times m\times128\times512$      |    24 |
 | 紧凑 MLA：潜空间评分与汇总 | $96$ 头；评分宽 $576$，汇总宽 $512$    | 使用紧凑的上下文表示                | $2\times96\times(576+512)N_{\mathrm{pair}}$ |    24 |
 | MLA：汇总后恢复值表示    | $m\times512\to m\times128$    | $512\times128$，共 96 个 | $96\times 2\times m\times512\times128$      |    24 |
 | MLA：输出门控        | $m\times7168\to m\times12288$ | $7168\times12288$     | $2\times m\times7168\times12288$            |    24 |
--- a/manuscripts/03-推理与训练负载.md
+++ b/manuscripts/03-推理与训练负载.md
@@ -16,7 +16,7 @@
 
 *图 3-1　恢复 6144 个上下文位置，处理 2048 个新输入并生成四个输出。首输出来自 prefill，后续三次 decode 各追加一个位置；纵向表示调用次序，间距用于示意。*
 
-两个阶段使用同一套权重，对资源的需求却不同。以一组权重矩阵为例，prefill 的输入包含 $BP$ 行，单步 decode 只有 $B$ 行。输入位置较多时，一次读入的权重可以参与更多乘加运算；低并发 decode 则可能为很少几个 token 读取整组权重。这是“prefill 常偏计算密集、decode 常偏带宽密集”的来源。
+两个阶段使用同一套权重，对资源的需求却不同。以一组权重矩阵为例，prefill 的输入包含 $BP$ 行，单步 decode 只有 $B$ 行。输入位置较多时，一次读入的权重可以参与更多乘加运算；低并发 decode 则可能为很少几个 token 读取整组权重。这是“prefill 通常更依赖算力、decode 通常更依赖内存带宽”的来源。
 
 不同请求之间可以形成新的复用机会。多个 decode 同时执行时，共同使用权重，各自读取上下文；一条长输入到来时，prefill 可以同时处理较多已知 token，它们的特征向量组成行数更大的输入矩阵。服务系统需要在这些不同形状的工作之间分配时间。仅把总 token 数相加，会丢失这些 token 分别属于哪个阶段、何时能够执行的信息。
 
@@ -158,7 +158,7 @@
 
 修改上下文会改变可复用的前缀长度。末尾追加新内容时，原有前缀保持不变；摘要替换一段旧上下文时，替换位置之后的表示要从新的前缀重新计算；工具定义或模板变化则可能更早改变输入。这些操作决定下一轮有多少位置命中、多少位置重新进入 prefill。
 
-上下文策略还会改变未来负载。把新内容持续追加在上下文末尾，有利于保住旧前缀，却会让陈旧内容继续占据上下文；替换前面的内容可以缩短上下文，但其后的缓存可能需要重建。第八章将根据重建计算量、状态大小与复用间隔，比较保留、淘汰和重算的代价。[^context]
+上下文策略还会改变未来负载。把新内容持续追加在上下文末尾，有利于复用原有前缀，却会让陈旧内容继续占据上下文；替换前面的内容可以缩短上下文，但其后的缓存可能需要重建。第八章将根据重建计算量、状态大小与复用间隔，比较保留、淘汰和重算的代价。[^context]
 
 用这一模型比较两种编辑方式：在末尾追加 100 个 token，只增加 100 行新输入；若在已有 2000 个 token 的第 501 个位置改写一个 token，变化会影响其后的表示，最长可直接复用的前缀只剩 500 个位置。缓存收益不仅取决于改了多少字，还取决于改动发生在哪里。
 
@@ -237,7 +237,7 @@
 
 *图 3-13　两个生成分支指向同一份公共前缀，并各自保存新增尾部。箭头表示引用关系，共享前缀只计一份容量。*
 
-若分支还要分别生成文本，公共前缀可以共享，分支尾部独立增长。设共享前缀长 $H_0$，第 $i$ 个分支新增 $h_i$ 个位置，每位置状态为 $c_{\mathrm{KV}}$，则总状态为 $c_{\mathrm{KV}}(H_0+\sum_i h_i)$。并行一方面缩短关键路径，另一方面让更多分支同时占用状态，需要同时检查时间和容量。
+若分支还要分别生成文本，公共前缀可以共享，分支尾部独立增长。设共享前缀长 $H_0$，第 $i$ 个分支新增 $h_i$ 个位置，每位置状态为 $c_{\mathrm{KV}}$，则总状态为 $c_{\mathrm{KV}}(H_0+\sum_i h_i)$。并行一方面缩短关键路径，另一方面让更多分支的状态同时占用内存，需要同时检查时间和容量。
 
 这条轨迹中的工具调用很短，大部分时间都花在模型计算上。若工具操作改为耗时 10 秒的编译，模型暂停的同时，下一轮所需上下文仍占据容量；若多个任务同时等待，驻留量按任务累加。工具执行时间越长，状态保留得越久，系统能同时处理的请求数也会受到影响，第八、九章将在这条时间线上安排缓存与交接。
 
@@ -247,15 +247,15 @@
 >
 > 本节的代码任务实测记录显示，任务总耗时为 76.510 秒，其中首轮模型计算耗时 36.375 秒，所有工具执行合计耗时 0.078 秒。保持其余阶段不变，分别计算两种优化能节省多少任务时间：将首轮模型计算速度提高到原来的四倍；将所有工具的执行速度提高到原来的两倍。根据计算结果，解释为什么局部加速倍数不能单独决定一项优化对完整任务的收益。
 
-把同一条会话换成 V4.1，可以看到负载需求如何推动模型结构的变化。设 Agent 已有一段公共上下文，工具随后返回新材料；如果前缀未命中，服务要重新处理较多输入，而本轮输出可能很短。为这样的输入密集场景降低 Prefill 成本，比只优化单步生成更有价值。V4.1 因而使用 20 层因果编码器和 20 层解码器：大部分输入只执行编码器主体，解码器全局 KV 从编码器末层表示投影得到；解码器局部 SWA 则通过提示末尾最多 128 个 token 的编码器输出进行有界重放，近似构建。输出 token 继续经过全部 40 层。[^v41-case]
+把同一条会话换成 V4.1，可以看到负载需求如何推动模型结构的变化。设 Agent 已有一段公共上下文，工具随后返回新材料；如果前缀未命中，服务要重新处理较多输入，而本轮输出可能很短。为这样的输入密集场景降低 Prefill 成本，比只优化单步生成更有价值。V4.1 因而使用 20 层因果编码器和 20 层解码器：大部分输入只执行编码器主体，解码器全局 KV 从编码器末层表示投影得到；为构建解码器局部 SWA，将提示末尾最多 128 个 token 的编码器输出重新送入解码器，以近似恢复这部分局部状态。输出 token 继续经过全部 40 层。[^v41-case]
 
 先单独计算专家矩阵运算量，用各 token 经过的专家层数之和表示工作量。设一次从空状态开始的输入为 $P$ 个 token，则普通 40 层路径为 $40P$，论文 CED 调度为 $20P+20\min(P,128)$。当 $P=8192$ 时，两者为 327,680 与 166,400，后者约为前者的 50.8%；当 $P\leq128$ 时，这个子项没有减少。
 
 已有前缀时，输入工作由追加长度与恢复路径共同决定。若编码器 SWA 命中缓存，编码器主体便可以继续处理追加的输入；编码器 SWA 缺失，则先重放前缀末尾窗口。两条路径随后都要构建本轮的解码器 SWA。第八章将用 8K 与 128K 上下文的教学算例，比较保存状态和重新执行的成本。
 
-![同一批 8K 输入的专家矩阵 token 层数。普通全层路径执行 40 层；CED 路径执行 20 层编码器，并为最近 128 个 token 重放 20 层解码器。灰色说明项仍需另外计算；生成阶段执行完整主干。](ch03/figure-3-v41-ced.svg)
-
-*图 3-14　同一批 8K 输入的专家矩阵 token 层数。普通全层路径执行 40 层；CED 路径执行 20 层编码器，并为最近 128 个 token 重放 20 层解码器。灰色说明项仍需另外计算；生成阶段执行完整主干。*
+![同一批 8K 输入中，各 token 经过的专家层数之和。普通全层路径执行 40 层；CED 路径执行 20 层编码器，并为最近 128 个 token 重放 20 层解码器。灰色说明项仍需另外计算；生成阶段执行完整主干。](ch03/figure-3-v41-ced.svg)
+
+*图 3-14　同一批 8K 输入中，各 token 经过的专家层数之和。普通全层路径执行 40 层；CED 路径执行 20 层编码器，并为最近 128 个 token 重放 20 层解码器。灰色说明项仍需另外计算；生成阶段执行完整主干。*
 
 ## 3.3 多模态与实时交互
 
@@ -301,7 +301,7 @@
 
 视觉问答仍通过语言模型逐 token 生成文本。若输出本身是声音或图像，一次迭代处理的数据和迭代次数也会改变。下面分别看声音与图像的生成过程。
 
-音频输出还有帧内循环。以 Fish Audio 的语音生成模型 S2 Pro 为例，slow（慢速）路径每次推进一个音频帧，fast（快速）路径在帧内补齐多个码本的编号。码本是离散声音表示的候选向量集合，codec（编解码器）将这些编号还原为波形。每帧先用一次 fast 前向建立状态，再用九次预测补齐码本，因此生成一帧需要十次 fast 前向计算。因此，语言 token/s、声学帧/s 与每秒生成的音频时长，衡量的是不同的生成速度。[^source-1][^media]
+音频输出还有帧内循环。以 Fish Audio 的语音生成模型 S2 Pro 为例，slow（慢速）路径每次推进一个音频帧，fast（快速）路径在帧内补齐多个码本的编号。码本是离散声音表示的候选向量集合，codec（编解码器）将这些编号还原为波形。每帧先用一次 fast 前向建立状态，再用九次预测补齐码本，因此生成一帧需要十次 fast 前向计算。由此可见，语言 token/s、声学帧/s 与每秒生成的音频时长，衡量的是不同的生成速度。[^source-1][^media]
 
 图像生成又换了一种循环。latent 是压缩后的连续图像表示；去噪逐步把含噪表示变为目标图像。扩散 Transformer（DiT）执行每步变换，无分类器引导（CFG）组合带条件与无条件两路预测；变分自编码器（VAE）负责像素与压缩表示之间的转换。以 Qwen-Image-2512 为例，$1024\times1024$ 图像形成 4,096 个 latent 位置，执行 50 个去噪步，true CFG 路径每步有两支 DiT 前向，总共执行 100 次 DiT 前向计算。去噪矩阵运算量约 $7.830\times10^{15}$ FLOPs；文本编码器与 VAE 还有各自的工作。latent 的位置集合在去噪期间保持固定，各步持续更新其内容，累计工作随去噪步数增加。[^image]
 
@@ -311,7 +311,7 @@
 
 ### 3.3.3 连续感知、首段响应与打断
 
-工作总量决定平均处理需求，实时播放还要求每块数据按时到达。令 $A(t)$ 表示截至时刻 $t$ 已到达的可播放音频时长，$P(t)$ 表示已消费的音频时长，缓冲中剩余音频为
+工作总量决定平均处理需求，实时播放还要求每块数据按时到达。令 $A(t)$ 表示截至时刻 $t$ 已到达的可播放音频时长，$P(t)$ 表示已播放的音频时长，缓冲中剩余音频为
 
 $$
 B_{\mathrm{audio}}(t)=A(t)-P(t).
@@ -323,7 +323,7 @@
 
 语音的时间约束更直接。用户停止说话后，系统要采集或确认输入，经过识别／编码、推理、语音生成和播放。首块在客户端到达，只说明数据已到；扬声器开始出声还取决于解码、缓冲和设备时钟。开始播放后，后续音频还必须及时到达，才能避免中断。
 
-音频块到达应用之后，还要经过解码、排队和播放。两次语音交互测量中，用户停止说话到首块音频到达的时间分别约为 400 ms、370 ms，后续音频块的到达间隔中位数均约为 94 ms。[^audio-real] 到达时间说明数据何时可用，播放时钟则决定数据何时被消费。下面用一条时间线说明两者之间的缓冲如何避免声音中断。
+音频块到达应用之后，还要经过解码、排队和播放。两次语音交互测量中，用户停止说话到首块音频到达的时间分别约为 400 ms、370 ms，后续音频块的到达间隔中位数均约为 94 ms。[^audio-real] 到达时间说明数据何时可用，播放时钟则决定音频何时被播放。下面用一条时间线说明两者之间的缓冲如何避免声音中断。
 
 为了看清缓冲的作用，采用下面这组教学参数安排时间线。输入每 20 ms 形成一块，24 kHz、单声道、每样本 2 bytes，每块数据量为：
 
@@ -333,7 +333,7 @@
 
 每块采集结束后，模型处理 12 ms、发送 1 ms，通常再传播 5 ms。第一块在 38 ms 到达，等待 40 ms 抖动缓冲后，于从采集起点算起的 78 ms 开始播放。第三块的传播延迟被设为 50 ms，直到 123 ms 才到，错过 118 ms 的播放计划，造成 5 ms 新增停顿。后续块虽已到达，也要按声音顺序等待。
 
-将缓冲改为 60 ms，这次停顿消失，首播却推迟到 98 ms。增加缓冲可以吸收本次抖动，代价是更晚开始；若音频的平均到达速度长期低于播放速度，有限缓冲终究会耗尽。[^audio]
+将缓冲改为 60 ms，这次停顿消失，首播却推迟到 98 ms。增加缓冲可以避免这次到达时间波动造成播放中断，代价是更晚开始；若音频的平均到达速度长期低于播放速度，有限缓冲终究会耗尽。[^audio]
 
 ![可组合的多模态阶段。编码形成模型输入，语言模型生成回复，声学模块将回复转成音频，接收端缓冲与播放设备决定何时真正发声。](ch03/figure-3-4-realtime.svg)
 
@@ -347,7 +347,7 @@
 
 *图 3-19　同一教学场景中的本地打断。123 ms 发出操作，130 ms 设备静音；远端计算是否停止属于另一条控制路径。*
 
-打断沿控制链依次生效：应用发出取消，播放设备静音，后端停止生成，缓冲与状态释放。教学时序在 123 ms 发出打断，控制延迟 5 ms，设备每 10 ms 生效一次，于是 130 ms 静音，用户感受到的打断延迟为 7 ms。后端停止生成决定后续计算何时结束，缓冲释放决定容量何时可供其他任务使用，三者分别对应停止播放、停止计算和释放存储的时刻。[^audio-interrupt]
+打断沿控制链依次生效：应用发出取消，播放设备静音，后端停止生成，缓冲与状态释放。教学时序在 123 ms 发出打断，控制延迟 5 ms，设备每 10 ms 检查并执行一次控制指令，于是 130 ms 静音，用户感受到的打断延迟为 7 ms。后端停止生成决定后续计算何时结束，缓冲释放决定容量何时可供其他任务使用，三者分别对应停止播放、停止计算和释放存储的时刻。[^audio-interrupt]
 
 RAW 是保留相机传感器原始采样信息的图像表示。RAW 图片精修关注的是何时能得到完整成片，无需像语音那样连续输出。精修还需要保留原始图像信息，视觉理解使用的 EC 无法替代原图。由此可见，任务不同，时间要求也不同：有的要尽快开始，有的要持续流畅，有的要尽早全部完成。
 
@@ -397,11 +397,11 @@
 
 *图 3-22　Qwen3-8B 全参数训练的参数相关状态。每参数包括两字节计算权重和四组四字节状态，共十八字节；激活和工作区的容量需求按各自的保存时间单独计算。*
 
-训练过程中还要不断准备下一批输入。CPU 读取、解码、分词并整理批次，再把张量送到设备。双缓冲可以让当前批计算时准备下一批：一个缓冲供计算读取，另一个接收新数据，交替使用。计算耗尽已准备数据时会等待输入，取回设备结果时又会等待相应计算完成。第五章将结合前向、反向和参数更新的执行顺序，分析数据准备与计算如何重叠。
+训练过程中还要不断准备下一批输入。CPU 读取、解码、分词并整理批次，再把张量送到设备。双缓冲可以让当前批计算时准备下一批：一个缓冲供计算读取，另一个接收新数据，交替使用。计算用完已准备的数据后，就必须等待新输入，取回设备结果时又会等待相应计算完成。第五章将结合前向、反向和参数更新的执行顺序，分析数据准备与计算如何重叠。
 
 ### 3.4.2 预训练、中期训练与 SFT
 
-把一次参数更新所需的完整批次拆成较小批次、逐次执行并累积梯度，这些较小批次称为微批次。训练阶段的名称不同，系统仍要回答同样的问题：输入有多少位置，哪些位置参与损失，多少个微批次后更新一次参数。预训练通常对连续文本做下一 token 预测；中期训练调整数据组成或上下文长度；监督微调（SFT）使用给定输入与目标回答继续训练模型，常把指令与回答放在一起输入，只对回答计算损失。它们的差异通过输入形状、监督位置和更新频率进入资源模型。
+把一次参数更新所需的完整批次拆成较小批次、逐次执行并累积梯度，这些较小批次称为微批次。训练阶段的名称不同，系统仍要回答同样的问题：输入有多少位置，哪些位置参与损失，多少个微批次后更新一次参数。预训练通常对连续文本做下一 token 预测；中期训练调整数据组成或上下文长度；监督微调（SFT）使用给定输入与目标回答继续训练模型，常把指令与回答放在一起输入，只对回答计算损失。估算资源时，需要把这些差异体现为输入形状、监督位置和更新频率。
 
 Qwen3 的训练过程体现了这些变化：通用阶段在 4096 长度上训练超过 30 万亿 token，随后增加科学、技术、工程与数学（STEM）、代码、推理与合成数据，再训练约 5 万亿 token；长上下文阶段将长度扩至 32768。[^qwen] 数据组成改变学习内容，序列长度还直接改变每个位置可以访问多少前文，因此需要分别观察。
 
@@ -409,7 +409,7 @@
 
 SFT 中，模型读取的位置与参与损失计算的位置往往不同。一条样本可以包含系统提示、用户问题、工具结果和回答，但训练只对回答部分计算损失。其余位置仍要经过主干网络，为回答提供上下文。实现还可以只对受监督位置计算词表投影；如果只是屏蔽这些位置的损失，之前的矩阵乘法仍会照常执行。
 
-部分参数训练减少需要保存梯度与优化器状态的参数集合。冻结模块的权重保持不变，但梯度仍需穿过它们传到较早的可训练模块，因此相应输入梯度继续执行。沿反向图逐层判断是否求输入梯度、是否求权重梯度，就能把训练状态与计算分别算清。
+部分参数训练减少需要保存梯度与优化器状态的参数集合。冻结模块的权重保持不变，但梯度仍需穿过它们传到较早的可训练模块，因此仍须计算相应的输入梯度。沿反向图逐层判断是否求输入梯度、是否求权重梯度，就能把训练状态与计算分别算清。
 
 微批次与更新数则控制工作的重复次数。设每微批次有 $B_\mu$ 条长度为 $P$ 的序列，累计 $a$ 个微批次后更新一次，则每次更新读取 $aB_\mu P$ 个输入 token。若每条只有 $P_{\mathrm{label}}$ 个监督位置，损失归一化使用的有效 token 数为 $aB_\mu P_{\mathrm{label}}$。增大累计次数可以扩大更新批次，同时让一次更新分多次前反向执行；参数状态一直保留，每个微批次的激活可以在反向完成后释放。
 
@@ -453,9 +453,9 @@
 
 DeepSeek-R1 提供了从推理任务和可验证奖励出发的训练路线。对代码、数学等容易检查的任务，反馈可以来自规则或测试；对需要实际操作的任务，则要运行工具与环境。奖励是用来评价一次回答或行动效果的数值。奖励若由另一个模型生成，还会产生额外推理。一次“训练迭代”因此可能包含多个模型、CPU 测试、沙箱等待和梯度更新。[^r1]
 
-DeepSeek V4-Flash 先通过 SFT 和 RL 训练领域专家，再以多教师的在策略蒸馏（on-policy distillation，OPD）将能力合入统一模型。蒸馏让学生模型学习教师模型给出的输出信息；在策略蒸馏由当前学生自己生成轨迹，教师在这些位置提供候选输出的概率分布，学生据此学习。教师前向因此成为生成与更新之间的一项模型工作；规则验证则以测试或程序执行提供反馈，需要另一组计算资源。[^v4]
-
-沿这个循环分析资源时，先找数据的生产者和消费者：策略生成 token，奖励函数或教师读取回答，学习器读取训练样本并产生新权重，生成副本再使用新权重。某一角色慢下来，会让后继角色等待；增加教师则新增一段前向计算和模型容量。后续的并行安排都必须建立在这张依赖图上。
+DeepSeek V4-Flash 先通过 SFT 和 RL 训练领域专家，再以多教师的同策略蒸馏（on-policy distillation，OPD）将多个专家的能力整合到同一个模型中。蒸馏让学生模型学习教师模型给出的输出信息；同策略蒸馏由当前学生自己生成轨迹，教师在这些位置提供候选输出的概率分布，学生据此学习。教师前向因此成为生成与更新之间的一项模型工作；规则验证则以测试或程序执行提供反馈，需要另一组计算资源。[^v4]
+
+沿这个循环分析资源时，先找数据的生产者和消费者：策略生成 token，奖励函数或教师读取回答，学习器读取训练样本并产生新权重，生成副本再使用新权重。某一角色慢下来，会让后继角色等待；增加教师模型则需要额外的前向计算和权重存储空间。后续的并行安排都必须建立在这张依赖图上。
 
 ### 3.5.2 有效样本、策略更新与权重同步
 
@@ -510,9 +510,9 @@
 
 *图 3-24　强化学习中的角色和数据流。生成端产生回答，反馈环节评价结果，筛选后送给学习器更新；新权重再用于下一批生成。*
 
-![保持十六条有效样本目标，生成数由三十二增到六十四。生成输入、后续生成和参考评分随全部回答增加，策略更新处理的样本数保持相同。](ch03/figure-3-rl-stage-work.svg)
-
-*图 3-25　保持十六条有效样本目标，生成数由三十二增到六十四。生成输入、后续生成和参考评分随全部回答增加，策略更新处理的样本数保持相同。*
+![保持十六条有效样本目标，生成数由三十二增到六十四。生成时的输入处理、后续解码和参考模型评分随回答总数增加，策略更新处理的样本数保持相同。](ch03/figure-3-rl-stage-work.svg)
+
+*图 3-25　保持十六条有效样本目标，生成数由三十二增到六十四。生成时的输入处理、后续解码和参考模型评分随回答总数增加，策略更新处理的样本数保持相同。*
 
 更新完成后，生成副本需要获得新权重。设有 $r$ 个副本，逐个独立发送大小为 $M_W$ 的完整权重，则传输量为 $rM_W$。Qwen3-8B BF16 的一份权重约为 16.381 GB，四个副本共需约 65.526 GB。优化器状态留在学习器，用于下一次更新；生成副本只需要执行前向所需的权重。广播或分层分发则通过复用中间传输减少重复发送。
 
@@ -644,7 +644,7 @@
 
 *图 3-33　DeepSeek-V3 采用 H800 的三个训练阶段。分别累计预训练、上下文扩展与后训练用量，沿用各阶段的公开统计范围。*
 
-Qwen3 表 21 将特定 8B 模型的 RL 与 on-policy distillation 两条后训练路线并列，投入分别为 17,920 与 1,800 GPU 小时。两者是替代路线，各自包含各自的生成、反馈与学习工作。把 GPU 小时对应到具体阶段和设备，再结合各阶段的执行过程，就能解释训练投入主要花在哪里。
+Qwen3 表 21 将特定 8B 模型的 RL 与 on-policy distillation 两条后训练路线并列，投入分别为 17,920 与 1,800 GPU 小时。两者是可供选择的两条路线，各自包含生成、反馈与学习工作。把 GPU 小时对应到具体阶段和设备，再结合各阶段的执行过程，就能解释训练投入主要花在哪里。
 
 ### 3.6.3 满足质量要求的全生命周期成本
 
@@ -754,7 +754,7 @@
 
 [^v4]: [DeepSeek V4 报告](../references/files/papers/deepseek-v4.pdf)，§5.1 的领域专家与多教师 OPD、§5.2 的教师调度、rollout 与沙箱。
 
-[^verl]: [实验 10-8：固定 verl 最小训练流程](../experiments/ch10/10-08/README.md)。本章引用其质量与更新区别，完整系统组织归第十章。
+[^verl]: [实验 10-8：固定 verl 最小训练流程](../experiments/ch10/10-08/README.md)。本章用这一实验区分任务质量与参数更新，完整系统的组织方式见第十章。
 
 [^verl-loss]: [固定 verl 配方的损失归一与更新路径](../research/2026-infra-survey/qa/verl-recipe-loss-closure.md)，绑定原配置、源码与导出张量。
 
--- a/manuscripts/04-加速器架构.md
+++ b/manuscripts/04-加速器架构.md
@@ -98,7 +98,7 @@
 
 早期 TPU 的面积分配展示了计算与存储之间的实际取舍。TPU 是 Google 设计的张量处理器。第一代 TPU v1 具有 $256\times256$ 个 8-bit 乘加单元、24 MiB 的统一数据缓冲（Unified Buffer） 和 4 MiB 累加存储。论文给出的芯片面积分布中，数据缓冲约占 37%，计算单元约占 30%，控制电路约占 2%。缓冲占用的面积甚至超过计算单元，因为阵列需要反复使用输入、权重和部分和。如果每次都从片外读取，乘加器就会花大量时间等待数据。[^tpu]
 
-假设为了增加乘加器，设计者缩小了缓冲容量。原来能保留的输入块被提前逐出，下次使用时要再次加载；重复读取增加的时间，可能抵消新增乘加器带来的收益。反过来，为很少重复使用的数据配置过大的缓冲，也会占用本可用于计算的面积。比较设计方案时，要计算同一任务在改动前后的总时间，把计算加快与重复读取增加的影响一起考虑。
+假设为了增加乘加器，设计者缩小了缓冲容量。原来能留在缓冲中的输入块被提前移除，下次使用时要再次加载；重复读取增加的时间，可能抵消新增乘加器带来的收益。反过来，为很少重复使用的数据配置过大的缓冲，也会占用本可用于计算的面积。比较设计方案时，要计算同一任务在改动前后的总时间，把计算加快与重复读取增加的影响一起考虑。
 
 > **思考：矩阵加速与其余工作加速的收益比较。** 在上述 60 μs 加 40 μs 的任务中，把矩阵速度从两倍提高到四倍，与把其余工作从 40 μs 降至 25 μs，哪种改动节省的时间更多？如果两者面积代价不同，应怎样选择？
 
@@ -348,7 +348,7 @@
 
 *图 4-11　在每事务 128 bytes、返回延迟 500 ns 的题设下，增加在途请求数提高带宽上界，直到碰到接口本身的速率上限。*
 
-两条曲线在并发较小时重合，是因为限制来自在途字节数。越过各自拐点以后，接口带宽成为新的瓶颈。实际系统中，增加并发访问还会使队列变长，延迟也随负载变化；内存性能测量工具 Mess 用延迟探针与可调背景流量同时观察这两项，得到带宽—延迟曲线。[^mess] 它把“同时发起独立请求可以减少空等”与“请求过多会增加排队时间”放进同一张图中。
+两条曲线在并发较小时重合，是因为限制来自在途字节数。越过各自拐点以后，接口带宽成为新的瓶颈。实际系统中，增加并发访问还会使队列变长，延迟也随负载变化；内存性能测量工具 Mess 用延迟探针与可调背景流量同时观察这两项，得到带宽—延迟曲线。[^mess] 它把“同时发起独立请求可以减少等待期间的空闲”与“请求过多会增加排队时间”放进同一张图中。
 
 分析存储性能时，先根据容量判断数据能否驻留，再根据复用情况计算各级接口的流量，最后结合并发量和延迟判断接口能否持续传输。下一节将把这些条件放入一条具体的加载—计算时间线。
 
@@ -487,7 +487,7 @@
 
 一个封装可以包含多颗裸片（die），由它们共同提供计算单元和内存接口。Blackwell 数据中心产品与昇腾 910C 都采用双裸片设计：各裸片提供本地计算能力和存储接口，封装内链路交换跨 die 数据。[^package] 扩大的容量使更多模型能够驻留，计算放在哪一侧则决定多少访问要经过链路。
 
-设两个 die 各有 64 GiB 本地存储，一份 96 GiB 权重在两侧各放 48 GiB。某阶段全部在 die 0 上执行，需要读取 32 GiB 本地权重与 32 GiB 远程权重；两段依次进行，本地有效带宽为 4 TiB/s，跨 die 带宽为 1 TiB/s。远程读取需要 $32/1024$ s，约 31 ms；本地读取约 7.8 ms，合计约 39 ms。远程数据只占一半字节，却占约五分之四的读取时间。
+设两个 die 各有 64 GiB 本地存储，一组共 96 GiB 的权重在两侧各放 48 GiB。某阶段全部在 die 0 上执行，需要读取 32 GiB 本地权重与 32 GiB 远程权重；两段依次进行，本地有效带宽为 4 TiB/s，跨 die 带宽为 1 TiB/s。远程读取需要 $32/1024$ s，约 31 ms；本地读取约 7.8 ms，合计约 39 ms。远程数据只占一半字节，却占约五分之四的读取时间。
 
 可以把处理远程权重的计算移到 die 1，让两侧权重都在本地使用。这样会增加输入或结果交换，但传输的内容从体积较大的权重变成体积较小的激活张量。若迁移计算所需的输入与结果共 64 MiB，同一 1 TiB/s 链路只需约 61 μs，远小于原来的 31 ms。让计算单元在本地反复使用大块权重，只在芯片之间传递较小的输入和结果，便能减少跨 die 通信。[^host]
 
@@ -503,7 +503,7 @@
 
 这种放置还创造了并行机会：两侧本地读取和计算可以分别推进，最终再交接结果。是否需要等两侧都完成，由模型的计算图决定；如果下一步要合并部分和，就在合并点形成同步。第六章会从具体并行切分推导这些交接。
 
-容量增加与局部性改善是两个连续步骤。第 4.3 节约 137.9 GB 的工作集，需要足够容量才能驻留；驻留后，将大块权重放在使用它的计算单元附近，才能避免把封装内链路变成新的瓶颈。分析封装扩展时，先确认容量足以保存所需数据，再安排本地复用，最后计算跨芯片传输的数据量。
+先增加容量，再让计算靠近数据，是这里改善性能的两个步骤。第 4.3 节约 137.9 GB 的工作集，需要足够容量才能驻留；驻留后，将大块权重放在使用它的计算单元附近，才能避免把封装内链路变成新的瓶颈。分析封装扩展时，先确认容量足以保存所需数据，再安排本地复用，最后计算跨芯片传输的数据量。
 
 ### 4.5.2 CPU 与加速器之间的数据访问
 
@@ -592,13 +592,13 @@
 
 ### 4.6.3 固定数据流与固定权重
 
-专用化利用执行中已经确定的条件重新分配硬件资源。固定算子种类使指令和控制简化，固定数据流使通信提前安排，固定数值格式使存储器和运算单元可以按所需位宽设计，固定权重则让不可变参数进入专门的存储组织。原来用于处理多种情况的控制和数据通路，可以相应简化。条件确定得越早，可省去的通用工作越多；模型变化时需要更新哪些部分，也由这些选择决定。
+专用化利用执行中已经确定的条件重新分配硬件资源。固定算子种类使指令和控制简化，固定数据流使通信提前安排，固定数值格式使存储器和运算单元可以按所需位宽设计，固定权重则可以为不再变化的参数设计专门的存储结构。原来用于处理多种情况的控制和数据通路，可以相应简化。条件确定得越早，可省去的通用工作越多；模型变化时需要更新哪些部分，也由这些选择决定。
 
 设计中哪些部分允许重新配置，决定了模型改变后需要更新什么。SambaNova 的数据流组织与现场可编程门阵列（FPGA）的可重配置通路，允许通过重新配置改变部分连接和执行顺序；固定权重设计则围绕特定参数组织存储与计算。[^special] 选择哪一种，取决于哪些部分长期稳定，以及省掉的工作原来占据多少时间。
 
 改变输入，就能让同一个模型执行不同的上层任务。同一组模型权重既可以处理文档问答，也可以根据代码和测试结果提出修改；每次调用使用自己的上下文状态。因而，硬件可以针对共同的模型执行设计权重通路，同时用可写存储保存各条请求的上下文。下面把权重与上下文分别计量，计算这种分工能够释放多少容量和带宽。
 
-作者的 OpenTallas 项目研究将不可变权重放入掩模 ROM 的架构。下面以项目中的 Qwen3-8B 为例，通过解析估算说明“从独立只读存储器（ROM）读取权重、从 HBM 读写 KV”的设计怎样改变各接口的传输量。[^opentallas] 每批只读取一次本步计算所需的权重，约 $W=15.1$ GB；每条请求完整读取 8,192 位置 KV，约 $K=1.21$ GB。完整驻留权重约 16.4 GB，每步读取其中参与计算的部分。
+作者的 OpenTallas 项目研究将不可变权重放入掩模 ROM 的架构。下面以项目中的 Qwen3-8B 为例，通过公式估算说明“从独立只读存储器（ROM）读取权重、从 HBM 读写 KV”的设计怎样改变各接口的传输量。[^opentallas] 每批只读取一次本步计算所需的权重，约 $W=15.1$ GB；每条请求完整读取 8,192 位置 KV，约 $K=1.21$ GB。完整驻留权重约 16.4 GB，每步读取其中参与计算的部分。
 
 先看同一接口原来承担多少读取。权重与 KV 共用 HBM 时，每步读取为 $W+BK$。把权重移到独立 ROM 后，HBM 只承担 $BK$。两种 HBM 读取量之比为
 
@@ -622,7 +622,7 @@
 n_{\mathrm{lane}}\ge\left\lceil\frac{15\,134\,641\,792}{2\times10^9\times100\times10^{-6}}\right\rceil=75\,674.
 $$
 
-256 条运算通道完成这些操作需约 29.6 ms。若分给矩阵计算的时间只有 40 μs，且有效利用率为 60%，需要 315,306 条运算通道，约为最初所需通道数的 4.2 倍。时间预算越紧，留给等待的余量越小，必须配置的计算与存储吞吐就越大。
+256 条运算通道完成这些操作需约 29.6 ms。若分给矩阵计算的时间只有 40 μs，且有效利用率为 60%，需要 315,306 条运算通道，约为最初所需通道数的 4.2 倍。时间预算越紧，留给等待的余量越小，必须配置的算力与存储带宽就越高。
 
 因此，评价专用架构需要回答两个问题：它省去了哪些操作，省去之后又由什么限制速度；模型被替换之前，它能完成多少任务，节省的成本能否收回投入。第 4.7 节将结合性能与能耗计算这些收益。
 
@@ -664,9 +664,9 @@
 
 输入流水给出了同样的例子：四块共需 256 tick 输入传输、512 tick 计算，而首块在 192 才就绪，所以最早完成时间为 704 tick。三个槽可以按前面的时间表完成计算，两个槽则因第三块晚到又增加 64 tick。计算量和传输量决定各步骤耗时，数据依赖与缓冲容量决定各步骤何时开始。
 
-矩阵单元的运算速率还与输入和累加精度有关。RTX 4090 的 Tensor Core 执行稠密矩阵运算时的峰值，FP16（每元素 2 字节的半精度浮点格式）输入与 FP16 累加约为 330 TFLOP/s，FP32 累加约为 165 TFLOP/s；本章 BF16／FP32 投影采用 165.2 TFLOP/s。[^hardware] 对 8.59 GFLOPs，两个速率分别对应约 26 和 52 μs。后面算出的读取时间约为 37 μs，速率选择会直接改变计算与读取的大小关系。因此，速率应从执行路径确定：先知道矩阵单元实际处理的格式，再用对应的操作吞吐计算时间。
-
-由此可以把建模分成三步：先确定实际执行的操作，再把占用同一硬件单元的耗时相加，最后按数据依赖安排执行顺序。接下来先对最初的 Q 投影应用前两步，观察 batch 怎样改变资源配比。
+矩阵单元的运算速率还与输入和累加精度有关。RTX 4090 的 Tensor Core 执行稠密矩阵运算时的峰值，FP16（每元素 2 字节的半精度浮点格式）输入与 FP16 累加约为 330 TFLOP/s，FP32 累加约为 165 TFLOP/s；本章 BF16／FP32 投影采用 165.2 TFLOP/s。[^hardware] 对 8.59 GFLOPs，两个速率分别对应约 26 和 52 μs。后面算出的读取时间约为 37 μs，速率选择会直接改变计算与读取的大小关系。因此，速率应从执行路径确定：先确定矩阵单元实际处理的数据格式，再用该格式对应的运算速率计算时间。
+
+由此可以把建模分成三步：先确定实际执行的操作，再把占用同一硬件单元的耗时相加，最后按数据依赖安排执行顺序。接下来先对最初的 Q 投影应用前两步，观察 batch 怎样改变对算力和带宽的需求比例。
 
 ### 4.7.2 Roofline 与负载变化
 
@@ -870,7 +870,7 @@
 
 ## 本章小结
 
-模型的长期运行需求推动硬件专用化，新硬件又改变不同模型方案的运行成本。固定权重实验把这一关系具体化：ROM 减轻权重通路与 HBM 容量压力，可写状态、计算和互联仍会限制性能，更新权重时还需要相应调整部署方式。
+模型的长期运行需求推动硬件专用化，新硬件又改变不同模型方案的运行成本。固定权重的算例把这一关系具体化：ROM 分担权重读取，并腾出原来存放权重的 HBM 空间，可写状态、计算和互联仍会限制性能，更新权重时还需要相应调整部署方式。
 
 本章从一次 Q 投影出发，分析了计算量、数据读写量和硬件性能怎样共同决定执行时间。同一份权重被更多输入行使用，可以减少每行分摊的读取量；矩阵尺寸与指令要求不一致时，补零又会增加无用计算。数据保存在片外、缓存还是局部缓冲中，决定了读取代价；缓冲容量和数据依赖则决定各步骤何时能够开始。
 
@@ -878,4 +878,4 @@
 
 不同优化改变的是不同的代价：局部复用减少重复读取，异步搬运让传输与计算重叠，合理分配多芯片任务减少远距离访问，专用化则省去部分通用处理。某一项改进取得足够收益后，瓶颈会转移：矩阵计算加快以后，读取开始占主导；缓冲已经能让所有数据按时到达，继续增加容量便不再缩短执行时间；权重读取减少以后，KV 成为主要流量。设计取舍要围绕这些变化，比较下一项投入还能省下多少时间。
 
-实际测量让我们看到运行库怎样使用硬件。缓存计数显示数据从哪里取得，内核记录显示一个算子被分成哪些步骤，硬件单元的工作时间则帮助区分计算效率低和等待过多。第五章将进一步研究布局、融合和调度如何改善这些具体步骤。
+实际测量让我们看到运行库怎样使用硬件。缓存计数显示数据从哪一级存储读取，内核记录显示一个算子被分成哪些步骤，硬件单元的工作时间则帮助区分计算效率低和等待过多。第五章将进一步研究布局、融合和调度如何改善这些具体步骤。
--- a/manuscripts/05-算子与运行时.md
+++ b/manuscripts/05-算子与运行时.md
@@ -105,9 +105,9 @@
 
 *图 5-2：先算一个输出，再算相邻输出。蓝色表示所用的 A 行，橙色表示所用的 W 列，绿色表示本次得到的输出元素。图中矩阵缩小为示意尺寸，实际算例的 K 为 4096。*
 
-如果这行 A 在两次使用之间仍留在局部存储或缓存中，就能直接复用；若已被替换，就需要再次向下一层请求。转到下一行输出时，W 中的元素也会再次使用。同一份数据在数学运算中需要反复使用。程序怎样安排这些运算，决定了这份数据要经过存储接口多少次。
-
-本节以工作缓冲和下一层存储之间的接口为计数位置，累计程序每次读入与写出的字节数。后面约 3 GiB 的访问量，也按这一接口计算。[^tiles]
+如果这行 A 在两次使用之间仍留在局部存储或缓存中，就能直接复用；若已被替换，就需要再次向下一级存储请求数据。转到下一行输出时，W 中的元素也会再次使用。同一份数据在数学运算中需要反复使用。程序怎样安排这些运算，决定了这份数据要经过存储接口多少次。
+
+本节以工作缓冲和下一级存储之间的接口为计数位置，累计程序每次读入与写出的字节数。后面约 3 GiB 的访问量，也按这一接口计算。[^tiles]
 
 把图中的逐元素计算写成循环，就是：
 
@@ -136,7 +136,7 @@
 
 循环顺序还决定每次访问的地址。W 按行连续存储时，上面的内层循环依次读取 W[k,j]、W[k+1,j]，地址相差 N 个元素。若改成 i、k、j 的顺序，内层便依次读取 W[k,j]、W[k,j+1]，同时将 A[i,k] 用于多个输出。这样一次连续传输取得的数据能立即参与计算。
 
-不过，新顺序也要同时保存一段输出的累加结果。若这些结果放不进局部存储，每轮更新都可能增加向下一层的读写。接下来用分块限制同时保留的数据量，让输入复用与输出保存相互配合。
+不过，新顺序也要同时保存一段输出的累加结果。若这些结果放不进局部存储，每轮更新都可能增加对下一级存储的读写。接下来用分块限制同时保留的数据量，让输入复用与输出保存相互配合。
 
 ### 5.2.2 容量约束下的分块复用
 
@@ -172,7 +172,7 @@
 
 完整数值如下，算术强度用同一矩阵乘的 FLOPs 除以上述接口字节数计算：
 
-| 输出块 | 局部存储需求 | 工作缓冲与下一层之间的访问 | 算术强度 |
+| 输出块 | 局部存储需求 | 工作缓冲与下一级存储之间的访问 | 算术强度 |
 | --- | ---: | ---: | ---: |
 | 32×32 | 8 KiB | 6168 MiB | 15.9 FLOP/byte |
 | 64×64 | 24 KiB | 3096 MiB | 31.7 FLOP/byte |
@@ -184,7 +184,7 @@
 
 *图 5-5：条形总长均表示 96 KiB。24 KiB 的工作集能放四份，80 KiB 的只能放一份。*
 
-同时驻留的线程块可以交错发起访存：一些块等待数据时，另一些块继续工作。工作集从四份减为一份，可能降低利用其他计算掩盖访存延迟的能力。因此，大块虽然少读数据，实际获得的带宽却可能下降。
+同时驻留的线程块可以交错发起访存：一些块等待数据时，另一些块继续工作。工作集从四份减为一份，可能减少在等待访存期间执行其他计算的机会。因此，大块虽然少读数据，实际获得的带宽却可能下降。
 
 假设两种实现都受这一层的带宽限制，有效带宽分别为 $B_{64}$ 和 $B_{128}$，则：
 
@@ -228,13 +228,13 @@
 
 这里 SiLU 是激活函数，$\operatorname{SiLU}(x)=x/(1+e^{-x})$；符号 $\odot$ 表示对应位置的元素相乘。SiLU 和乘法都是逐元素运算。Z[i,j] 只依赖 G[i,j] 与 U[i,j]，不同位置之间没有归约。因此，读入同一位置的两个元素后，就能直接算出该位置的最终结果，而无需先生成全部 SiLU 结果。
 
-先比较中间结果 T 的去向。分开执行时，完整 T 要写到下一层存储，再被乘法读回；融合后，一小块 SiLU 结果可以直接交给同一 GPU 内核程序（kernel）中的乘法，用完就释放局部空间。
+先比较中间结果 T 的去向。分开执行时，完整 T 要写到下一级存储，再被乘法读回；融合后，一小块 SiLU 结果可以直接交给同一 GPU 内核程序（kernel）中的乘法，用完就释放局部空间。
 
 ![图 5-8 融合前后中间结果经过的路径](ch05/figure-5-fusion-path.svg)
 
-*图 5-8：上半图的完整 T 经过一次写出与一次读回；下半图中局部片段 t 直接传给乘法。两种方式仍读取 G、U 并写出 Z，图中省略这些共同的输入输出边。融合保持原来的中间舍入语义。*
-
-**例 5-4：激活算子融合如何减少中间张量读写？** 分开执行时，先计算 $T=\operatorname{SiLU}(G)$，读取 G、写出 T，共 48 MiB；再计算 $Z=T\odot U$，读取 T、U，写出 Z，共 72 MiB。总访问为 120 MiB。将两步融合，每次读取 G、U 的一片，在局部完成 SiLU 和乘法，写出 Z，只需 72 MiB。
+*图 5-8：上半图的完整 T 经过一次写出与一次读回；下半图中局部片段 t 直接传给乘法。两种方式仍读取 G、U 并写出 Z，图中省略这些共同的输入输出边。融合保持原来的中间结果的舍入规则。*
+
+**例 5-4：激活算子融合如何减少中间张量读写？** 分开执行时，先计算 $T=\operatorname{SiLU}(G)$，读取 G、写出 T，共 48 MiB；再计算 $Z=T\odot U$，读取 T、U，写出 Z，共 72 MiB。总读写量为 120 MiB。将两步融合，每次读取 G、U 的一片，在局部完成 SiLU 和乘法，写出 Z，只需 72 MiB。
 
 省下的 48 MiB 正是 T 的一次写回和一次读取。沿用上一节的带宽模型，取两种程序的有效带宽相同，时间比为 $72/120=0.6$，加速约 1.7 倍。实际激活链对照从约 14.2 μs 降到 7.7 μs，约快 1.8 倍。理论计算与实测结果都说明了同一个收益来源：省去完整 T 的写回和重读。[^exp2]
 
@@ -246,7 +246,7 @@
 
 中间张量的容量与访问量在这里具有不同含义。设每步先分配输出，完成后释放已用完的输入。独立执行 SiLU 时，G、U、T 同时存活，峰值为 72 MiB；三步融合时，G、U 与最终输出同时存活，峰值为 60 MiB。读写减少 96 MiB，峰值却只减少 12 MiB，因为同一块存储空间可以被反复读写。[^fusion]
 
-逐元素运算的每个输出只依赖对应位置的输入，因此可以在读入后立即计算，减少中间结果的保存。要让这种复用顺利发生，还得让前后两步使用相容的数据布局，并保证缓冲在最后一次读取结束前不会被覆盖。下面沿着一个数据块的使用过程，分析这两个条件。
+逐元素运算的每个输出只依赖对应位置的输入，因此可以在读入后立即计算，减少中间结果的保存。要让这种复用顺利发生，还得让前后两步使用彼此兼容的数据布局，并保证缓冲在最后一次读取结束前不会被覆盖。下面沿着一个数据块的使用过程，分析这两个条件。
 
 ### 5.3.2 布局转换、缓冲复用与双缓冲
 
@@ -330,7 +330,7 @@
 
 将 b 缩为 1，能放下 a=166，扫描次数降为 50，访问降至 $50\times4+4=204$ MiB。但每次扫描从 128 个 K/V 块变成 8192 个块，在线状态必须更频繁地更新。
 
-| K/V 块行数 b | 可容纳的 Q 行数 a | K/V 完整扫描次数 | 缓冲与下一层之间的访问 | Q 块与 K/V 块之间的状态更新次数 |
+| K/V 块行数 b | 可容纳的 Q 行数 a | K/V 完整扫描次数 | 缓冲与下一级存储之间的访问 | Q 块与 K/V 块之间的状态更新次数 |
 | --- | ---: | ---: | ---: | ---: |
 | 1 | 166 | 50 | 204 MiB | 409600 |
 | 64 | 110 | 75 | 304 MiB | 9600 |
@@ -338,7 +338,7 @@
 
 ![图 5-14 注意力块大小在访问量与更新次数之间的取舍](ch05/figure-5-8-attention-tradeoff.svg)
 
-*图 5-14：快速缓冲为 128 KiB，序列长 8192、头维度 128，无掩码；每个点对应正文表格的一种 K/V 块大小。横轴为缓冲与下一层之间的访问量，纵轴为在线更新次数，采用对数刻度。从 b=64 的点移到 b=1 的点，读取减少，更新次数却约增至 43 倍。*
+*图 5-14：快速缓冲为 128 KiB，序列长 8192、头维度 128，无掩码；每个点对应正文表格的一种 K/V 块大小。横轴为缓冲与下一级存储之间的访问量，纵轴为在线更新次数，采用对数刻度。从 b=64 的点移到 b=1 的点，读取减少，更新次数却约增至 43 倍。*
 
 图 5-14 把两项代价放在同一平面上，越靠左表示少读数据，越靠下表示少做更新。从 64 行块缩到 1 行块，少读 100 MiB，却把状态更新从 9600 次增至 409600 次，约为原来的 43 倍。每次更新都要执行循环控制、最大值比较、指数与输出缩放；每次只处理一行 K/V，也使矩阵乘的一个维度过小，难以充分利用矩阵计算单元。较小的 K/V 块能减少读取，却会增加更新次数；较大的块则有利于矩阵计算，并减少循环开销。因此，选择块大小时，需要同时比较这两类开销。
 
@@ -360,7 +360,7 @@
 
 ### 5.4.1 为什么要比较多种实现
 
-回顾逐元素链：SiLU 对每个数执行非线性变换，再与另一输入对应相乘，最后量化成低位宽表示。仍看这条 SiLU—乘法—量化链。两处相邻边界各有“保留”和“融合”两种选择，共有四种划分：三步独立、融合前两步、融合后两步、三步融合。如果一条链包含十个算子，就有 $2^9=512$ 种相邻边界划分。每一种划分还可以选择块大小、局部布局和流水深度。
+回顾逐元素链：SiLU 对每个数执行非线性变换，再与另一输入对应相乘，最后量化成低位宽表示。仍看这条 SiLU—乘法—量化链。两处相邻算子之间，都可以选择分开执行或融合执行，共有四种划分：三步独立、融合前两步、融合后两步、三步融合。如果一条链包含十个算子，就有 $2^9=512$ 种相邻边界划分。每一种划分还可以选择块大小、局部布局和流水深度。
 
 这些选择会相互影响。合并两步可以消除中间写回，也会增加同一时刻需要保存的值；临时数据增多，又会改变能够采用的块大小。因而，编译器要同时表达计算内容、执行顺序和数据保存位置。只记录一个算子名，无法推导这些代价。
 
@@ -403,7 +403,7 @@
         store_tile(Y, io, jo, silu(c))
 ```
 
-这里 `load_tile` 和 `matmul_accumulate` 分别表示成块读取和矩阵累加；尾块通过有效坐标掩码处理。伪代码的缩进清楚地标出了这些先后关系：输入块在 ko 循环内更新，累加器在整个 ko 循环期间持续保存，激活在 ko 循环结束后发生。
+这里 `load_tile` 和 `matmul_accumulate` 分别表示成块读取和矩阵累加；最后一个不足整块的数据块用掩码标出有效位置。伪代码的缩进清楚地标出了这些先后关系：输入块在 ko 循环内更新，累加器在整个 ko 循环期间持续保存，激活计算在 ko 循环结束后执行。
 
 ![图 5-15 循环层级决定临时数据的保存时间](ch05/figure-5-9-polyhedral.svg)
 
@@ -417,11 +417,11 @@
 
 ### 5.4.3 依赖与舍入怎样限制变换
 
-刚才把激活移入输出 tile，但仍安排在 ko 循环结束后执行，原因可以用两个数说明。设一个输出的两个部分和为 1 与 −1，完整和为 0，SiLU(0)=0。若在每个部分和之后先做 SiLU，再相加，结果为 $\operatorname{SiLU}(1)+\operatorname{SiLU}(-1)\approx0.4621$。移动激活越过归约边界，已经改变了运算。
+刚才把激活移入输出 tile，但仍安排在 ko 循环结束后执行，原因可以用两个数说明。设一个输出的两个部分和为 1 与 −1，完整和为 0，SiLU(0)=0。若在每个部分和之后先做 SiLU，再相加，结果为 $\operatorname{SiLU}(1)+\operatorname{SiLU}(-1)\approx0.4621$。把激活计算移到完整求和之前，已经改变了运算。
 
 ![图 5-16 激活与归约的顺序](ch05/figure-5-activation-order.svg)
 
-*图 5-16：同样两个部分和，先相加得到零，再做 SiLU 仍为零；先对各部分做 SiLU 再相加，得到约 0.4621。两个数说明变换越过归约后改变了函数。*
+*图 5-16：同样两个部分和，先相加得到零，再做 SiLU 仍为零；先对各部分做 SiLU 再相加，得到约 0.4621。两个数说明把激活计算移到求和之前会改变结果。*
 
 编译器需要遵守的依赖关系因此有两个层次：同一输出的部分和要按归约规则合并，后续算子要等所需的结果算好。不同输出可以并行计算；同一输出的归约和后续运算则必须按依赖顺序执行。
 
@@ -647,7 +647,7 @@
 
 上下文增长后，仍需执行的索引扫描会产生更大的开销。128K 时，两代模型的全局历史状态与索引的逻辑读取量分别约为 65.085 与 33.422 MiB。V4.1 后续解码索引器只访问候选池，初始索引仍需遍历全局。因此，即使后续层索引的访问范围固定，整个 decode 的开销仍会随上下文长度增长。
 
-除了数据从哪里取得，程序是否真正省去了计算也会改变开销。候选池通过缩小后续索引的搜索范围来节省工作。公开参考实现先对完整索引缓存执行点积，再屏蔽候选池之外的位置；论文生产路径直接减少后续索引扫描的条目。前者已经完成了池外点积，后者在发起计算时就省去这部分工作。同一选择规则由此对应两种执行成本。
+除了数据从哪里取得，程序是否真正省去了计算也会改变开销。候选池通过缩小后续索引的搜索范围来节省工作。公开参考实现先对完整索引缓存执行点积，再屏蔽候选池之外的位置；论文中的生产实现直接减少后续索引扫描的条目。前者已经完成了池外点积，后者在发起计算时就省去这部分工作。同一选择规则由此对应两种执行成本。
 
 ![图 5-30 全局驻留与逐层逻辑读取分开比较。上方仅计全局 KV 容量，下方读取包含全局条目、局部窗口和索引；两栏各自采用相同横轴尺度。数值按生产布局计算。](ch05/figure-5-v41-traffic.svg)
 
--- a/manuscripts/06-超节点.md
+++ b/manuscripts/06-超节点.md
@@ -184,7 +184,7 @@
 
 TP 将一层拆开，**流水线并行**（pipeline parallelism，PP）则将层按顺序分给不同设备。前一阶段完成若干层，传递激活，后一阶段继续计算。各阶段保存所负责模型层的权重，阶段之间只传递中间结果。
 
-**微批次（microbatch）**是送入流水线的一小组输入；一个批次可以拆成多个微批次，使不同阶段同时处理不同组输入。设模型分成 $q$ 个阶段，每阶段包含激活传输的执行时间为 $t$，输入由 $b$ 个已经就绪的独立微批次组成。先看四阶段、每阶段 1 ms 的情形。微批次 0 在 0 ms 进入阶段 0，经过四阶段，在 4 ms 完成。阶段 0 在 1 ms 已能接收微批次 1，后者在 5 ms 完成。其余微批次依次跟进，完成间隔为 1 ms。
+**微批次（microbatch）**是送入流水线的一小组输入；一个批次可以拆成多个微批次，使不同阶段同时处理不同组输入。设模型分成 $q$ 个阶段，每阶段的执行时间（含激活传输）为 $t$，输入由 $b$ 个已经就绪的独立微批次组成。先看四阶段、每阶段 1 ms 的情形。微批次 0 在 0 ms 进入阶段 0，经过四阶段，在 4 ms 完成。阶段 0 在 1 ms 已能接收微批次 1，后者在 5 ms 完成。其余微批次依次跟进，完成间隔为 1 ms。
 
 ![四阶段流水的填充与排空](ch06/figure-6-4-pipeline.svg)
 
@@ -347,7 +347,7 @@
 
 操作名称规定了计算方式和结果在各卡上的分布，算法则规定消息怎样分轮传递。一个 AllReduce 可以先 ReduceScatter，让各卡各自获得一块完整归约结果，再 AllGather，交换这些已归约的数据块。
 
-用四元素向量说明两个阶段各自的作用。各卡开始时都有四个元素的贡献；第一阶段求和后，卡 0 留元素 0，卡 1 留元素 1，依次类推。第二阶段交换这四个已求和的元素。第一阶段中，每卡的输入都是完整形状的部分和，第二阶段的输入是已经归约好的四分之一结果。这正对应“先对每块求和，再分发求和结果”。
+用四元素向量说明两个阶段各自的作用。各卡开始时都有四个元素的贡献；第一阶段求和后，卡 0 留元素 0，卡 1 留元素 1，依次类推。第二阶段交换这四个已求和的元素。第一阶段中，每卡的输入都是形状与完整输出相同的部分和，第二阶段的输入是已经归约好的四分之一结果。这正对应“先对每块求和，再分发求和结果”。
 
 各卡开始通信的时刻也会影响等待时间。设四卡分别在 0、0、0、2 ms 发起一次集合通信，所有卡都发起通信后，再用 0.4 ms 完成交换。前三卡等待 2.4 ms，最后一卡等待 0.4 ms；把交换加快一倍只省 0.2 ms，消除最后一卡的计算延误却省 2 ms。前三张卡的调用时间较长，是因为它们还要等待最后一张卡完成前面的计算。[^collective-path]
 
@@ -514,7 +514,7 @@
 
 *图 6-23：把所有发送者的消息累加到各条有向链路上，取每轮最大值。递归前三轮均为 4 MiB，Swing 为 4、2、2 MiB。*
 
-主机中转是同一问题的另一种形式。非统一内存访问（NUMA）结构把主机内存分到不同 CPU 附近，跨 CPU 读取要经过处理器间接口。G0、G1 接 NUMA 节点 A，G2、G3 接 B，四卡 ring 共发送 48 MiB。把全部中转缓冲放在 A，CPU 间每个方向承担 24 MiB；把缓冲移到发送卡附近，并让同 NUMA 的卡在环中相邻，每方向降到 12 MiB。若再将环改成交替跨 NUMA，载荷又回到 24 MiB。若 CPU 间链路的单向带宽为 8 GB/s，三种方案的传输时间至少分别为约 3.15、1.57、3.15 ms。缓冲位置和卡的通信次序共同决定搬运路径。[^numa]
+主机中转是同一问题的另一种形式。非统一内存访问（NUMA）结构把主机内存分到不同 CPU 附近，跨 CPU 读取要经过处理器间接口。G0、G1 接 NUMA 节点 A，G2、G3 接 B，四卡 ring 共发送 48 MiB。把全部中转缓冲放在 A，CPU 间每个方向承担 24 MiB；把缓冲移到发送卡附近，并让同 NUMA 的卡在环中相邻，每方向降到 12 MiB。若再将环改成交替跨 NUMA，每个方向的传输量又回到 24 MiB。若 CPU 间链路的单向带宽为 8 GB/s，三种方案的传输时间至少分别为约 3.15、1.57、3.15 ms。缓冲位置和卡的通信次序共同决定搬运路径。[^numa]
 
 **设计案例：从单跳延迟估算全局同步时间。** 作者参与 OpenTallas 设计时，需要把局部数据交换扩展为所有设备参与的数据交换。用 60 层、每层两次同步、100 μs token 目标的教学例说明：若通信占 30%，120 次同步每次只有 0.25 μs。相邻设备间传输一跳需要 100 ns，看起来足够；通信关键路径若经过 15 跳，仅传播就需 1.5 μs，120 次累计 180 μs。从单跳传输扩大到所有设备间的同步后，传播时间的累积就可能超过单个 token 的时间预算。减少通信跳数、降低同步次数或调整模型切分，都可以缩短这部分等待。[^opentallas]
 
@@ -558,7 +558,7 @@
 
 端口、距离和功率共同限制了系统规模。NVIDIA 的多代系统展示了这些条件下，互联如何从机箱内直连发展到机柜级交换网络。DGX-1 用八张 GPU 组成 NVLink 混合立方体网格，部分 GPU 直接相连，其他 GPU 之间的通信需要中转。此时将频繁通信的计算任务分配到相邻 GPU，就能减少中转与共享链路压力。
 
-NVLink 是 NVIDIA 的设备互联，NVSwitch 是组织 NVLink 交换网络的交换芯片。DGX-2 使用 16 张 V100 和 NVSwitch。交换结构把“某对 GPU 是否具有直连”转化为“交换层能同时服务多少流量”，扩大了可用协作范围。DGX A100 又以八卡服务器为基本单元，配备 NVLink／NVSwitch。这些系统采用不同的 GPU 数量与互联结构，以适应芯片接口、机箱空间和应用需求。[^nvidia]
+NVLink 是 NVIDIA 的设备互联，NVSwitch 是组织 NVLink 交换网络的交换芯片。DGX-2 使用 16 张 V100 和 NVSwitch。交换结构把“某对 GPU 是否具有直连”转化为“交换层能同时转发多少数据”，扩大了可用协作范围。DGX A100 又以八卡服务器为基本单元，配备 NVLink／NVSwitch。这些系统采用不同的 GPU 数量与互联结构，以适应芯片接口、机箱空间和应用需求。[^nvidia]
 
 GB200 NVL72 将 72 张 GPU 和 36 个 Grace CPU 放进机柜级 NVLink 系统。机柜内的更多 GPU 因而能够通过 NVLink 频繁交换数据。以第 6.5.1 节的八卡任务为例：在两台服务器之间，每轮启动耗时增加，使单步从约 4.28 ms 增到 6.32 ms；若所需八卡能放在同一个满足前述低延迟条件的互联域内，便可省去这部分跨服务器通信开销。更大的互联域也可以容纳多个两卡或四卡实例，各自完成自己的请求。[^nvl]
 
@@ -683,7 +683,7 @@
 
 ### 6.7.1 更大的协作组与更多推理实例
 
-至此，已经知道数据能否存下、每步如何执行，以及连接和内存位置怎样改变执行时间。下面利用这些结果，为章首给定的八张卡安排多个会话。四个会话都已完成 prefill，各自保存了 8192 个上下文位置的 KV。每个会话还有一个已经生成、尚未写入 KV 的 token，用作下一步输入。现在，四个会话同时请求再生成八个 token。一个实例一次服务一个会话，完成这八步后再接下一个会话；请求按实例轮转分配。采用 6.4 的低延迟八卡连接，全部设备持续占用到四个会话都完成。
+至此，已经知道数据能否存下、每步如何执行，以及连接和内存位置怎样改变执行时间。下面利用这些结果，为章首给定的八张卡安排多个会话。四个会话都已完成 prefill，各自保存了 8192 个上下文位置的 KV。每个会话还有一个已经生成、尚未写入 KV 的 token，用作下一步输入。现在，四个会话同时请求再生成八个 token。一个实例一次服务一个会话，完成这八步后再接下一个会话；请求按实例轮转分配。采用 6.4 的低延迟八卡连接，全部设备一直分配给这组任务，直到四个会话都完成。
 
 每步时间来自式（6-5）。续写时，式（6-4）的上下文长度依次为 8192 到 8199，KV 访问量逐步增加。将八步相加，就得到会话的服务时间。等待中的会话只保存各自的 KV，同一实例中的会话共享模型权重；四种部署方式的每卡内存需求均不超过 24 GB。
 
@@ -712,7 +712,7 @@
 
 八卡协作使单个会话的续写时间从约 132 ms 缩短到 34 ms，但四个会话排成一队，所有会话约 137 ms 才完成。采用四个两卡实例时，单个会话的续写时间较长，却能同时推进四个会话，约 69 ms 全部完成。八个单卡实例中，只有四个分配到了会话，多出来的空闲实例无法加速正在执行的请求。
 
-这里同时使用了模型内部与请求之间的并行关系。层内矩阵可以切分给多卡，独立会话可以分配给多个实例；两种并行共同竞争这八张卡。掌握这两层关系，才能根据请求的完成期限选择协作组大小。服务调度给出同时推进的会话数，模型切分给出每步通信量，互联拓扑给出交接时间，三者共同决定表中的完成时刻。
+这里同时使用了模型内部与请求之间的并行关系。层内矩阵可以切分给多卡，独立会话可以分配给多个实例；两种并行共同竞争这八张卡。掌握这两层关系，才能根据请求的完成期限选择协作组大小。服务调度给出同时推进的会话数，模型切分给出每步通信量，互联拓扑决定数据交换与同步的耗时，三者共同决定表中的完成时刻。
 
 图中色块的横向长度表示单个会话的执行时间，纵向轨道数表示多少会话可以同时推进。完成期限决定了这两者怎样取舍。只有一个会话、要求 40 ms 完成，八卡实例满足目标；四个会话、要求 90 ms 全部完成，两卡和四卡部署方式都满足，两卡更早完成。较短的单会话完成期限需要更大协作组，足够多的独立会话则可以利用更多实例。
 
@@ -788,7 +788,7 @@
 
 [^variants]: [MeshSlice 的矩阵形状与流水推算](../case-studies/mesh-shape-and-slicing.md)、[Shift Parallelism 的状态与额外驻留](../case-studies/parallel-switching-and-state.md)。MeshSlice 的部分重叠为预测，Arctic 的 SP 定义及动态切换边界按固定实现说明。
 
-[^pipeline]: [有限槽位与背压计算](../calculations/results/pipeline-finite-1-slots.md)、[基础 TP／PP 通信路径](../calculations/results/dense-comm-qwen8-tp8-pp1-t1.md)。声明的时序、缓冲和线格式决定所计范围。
+[^pipeline]: [有限槽位与背压计算](../calculations/results/pipeline-finite-1-slots.md)、[基础 TP／PP 通信路径](../calculations/results/dense-comm-qwen8-tp8-pp1-t1.md)。声明的执行时序、缓冲安排和线上传输格式决定所计范围。
 
 [^ownership]: [Qwen235 所有权连接练习](../research/2026-infra-survey/parallel-moe-ownership/NOTES.md)及[独立核对结果](../research/2026-infra-survey/parallel-moe-ownership/results.json)。本例采用给定的 TP／EP 教学布局。
 
--- a/manuscripts/07-数据中心网络.md
+++ b/manuscripts/07-数据中心网络.md
@@ -95,7 +95,7 @@
 
 八个参与者各发送 336 MiB，共发送 2688 MiB，执行十四轮。算法决定总发送量，参与者在服务器上的分布则决定其中多少需要跨服务器传输。
 
-**例题 7.2：哪种归约算法能在 8 ms 内完成传输？** 使用第 7.1 节的两台服务器，比较连续平坦环、交错平坦环和分层归约。
+**例题 7.2：哪种归约算法能在 8 ms 内完成传输？** 使用第 7.1 节的两台服务器，比较不分层的连续环、交错环和分层归约。
 
 **解答：先求流量。** 连续环按 0、1、2、3、4、5、6、7 排列。八条有向边只有 3→4 和 7→0 跨服务器。每轮每边发送 24 MiB，十四轮每方向各发送 336 MiB，两方向合计 672 MiB。若改成 0、4、1、5、2、6、3、7，八条边全部跨服务器，每方向每轮有四份 24 MiB，跨服务器总量增至 2688 MiB。
 
@@ -232,11 +232,11 @@
 
 分层归约的跨服务器阶段中，卡 0 已经得到本地归约后的分片，需要交给卡 4。一次数据交接包含两项工作：把分片写到卡 4 可以读取的位置，以及让卡 4 知道数据已经就绪。前一项改变数据所在的位置，后一项告诉接收方何时可以开始计算。
 
-**单边读写**让发起者访问预先授权的远端内存。例如卡 0 可以向卡 4 的接收区域写入分片，由对端 NIC 完成数据移动。**双边消息**需要发送方与接收方配合。接收方预先提交接收请求并提供缓冲区，通信系统将到达的消息与相应请求匹配。已知目标位置的大块分片适合直接读写；来自多个发送者的通知适合用消息队列区分各条通知，并将其分发给相应的消费者。
+**单边读写**让发起者访问预先授权的远端内存。例如卡 0 可以向卡 4 的接收区域写入分片，由对端 NIC 完成数据移动。**双边消息**需要发送方与接收方配合。接收方预先提交接收请求并提供缓冲区，通信系统将到达的消息与相应请求匹配。已知目标位置的大块分片适合直接读写；来自多个发送者的通知适合用消息队列区分各条通知，并将其分发给相应的数据使用方（消费者）。
 
 大块数据与通知由此可以分工：先写分片，再发一个短消息说明分片可以使用。把写入和通知合并为一次提交，还能减少重复发起。消费者观察通知之后开始归约，两项操作的正确先后关系将在第 7.4 节展开。
 
-写入和通知完成了数据交接。如果还希望接收方按指定方式处理数据，就可以使用**远程过程调用**（remote procedure call，RPC）。它增加“请对端执行一个函数”的语义。一次调用包括参数传送、远端执行和结果返回。参数的表示会直接改变这条路径：JSON 是文本形式的数据交换格式；base64 把每 3 个二进制字节编码为 4 个文本字符。将 1 MiB 二进制数据放进 JSON 的 base64 字段，编码载荷约增加三分之一。改用二进制请求，就同时减少发送字节和编码工作。
+写入和通知完成了数据交接。如果还希望接收方按指定方式处理数据，就可以使用**远程过程调用**（remote procedure call，RPC）。它要求对端执行指定函数。一次调用包括参数传送、远端执行和结果返回。参数的表示会直接改变这条路径：JSON 是文本形式的数据交换格式；base64 把每 3 个二进制字节编码为 4 个文本字符。将 1 MiB 二进制数据放进 JSON 的 base64 字段，编码载荷约增加三分之一。改用二进制请求，就同时减少发送字节和编码工作。
 
 本书一组跨主机 RPC 测量中，请求大小从约 1.33 MiB 降至 1.00 MiB，客户端 CPU 处理时间中位数从约 9.8 ms 降至 1.1 ms。20 对正式调用中有 11 对整体变快，网络传输和转发等待的耗时波动更大。[^rpc] 这些测量区分了两类耗时：减少编码可以节省本地 CPU 时间；要缩短整个调用，还要减少网络、转发和远端执行中的等待。
 
@@ -431,7 +431,7 @@
 
 恢复时间若增至 200 μs，串行安排中的 C 要到 232 μs 才完成，独立安排中的 C 仍在 10 μs 完成。删除额外依赖，把恢复造成的停顿限制在需要这份数据的链上。对于等待 C 的下游任务，这种隔离比全组只提前 10 μs 更重要。[^ordering]
 
-全局顺序让上层容易描述操作的先后关系，也把独立工作放进了同一条等待链。计算图已经明确 A→B 的发布依赖后，接口和运行时可以围绕这条依赖提供顺序保证，让 C 独立推进。应用提供更准确的依赖信息，网络据此只为相关操作保证顺序，便能在保持发布语义的同时缩短等待。
+全局顺序让上层容易描述操作的先后关系，也把独立工作放进了同一条等待链。计算图已经明确 A→B 的发布依赖后，接口和运行时可以围绕这条依赖提供顺序保证，让 C 独立推进。应用提供更准确的依赖信息，网络据此只为相关操作保证顺序，便能在保证接收方收到通知后能够读到已写入的数据的同时缩短等待。
 
 再假设所有操作共用一个处理单元：从 A 开始写入到 B 完成通知，这个单元一直被占用，C 只能随后执行。此时资源使用本身增加了发布链到 C 的顺序边，两种安排都到 112 μs 完成。程序依赖和资源依赖因此要画在同一张图里：减少程序中的顺序约束后，资源约束仍会限制并行程度。
 
@@ -463,7 +463,7 @@
 
 *图 7-29：增加槽位后，后续请求更早提交。完成通知仍每 20 μs 批量处理四项，最后一个槽位仍在 80 μs 释放。两图时间轴相同。*
 
-对比图 7-28 和图 7-29，增加槽位让蓝色传输条带提前了，右侧长长的橙色等待却仍然存在。决定持续吞吐的，是这些槽位最终释放得有多快。用并发与吞吐模型看，处理完成通知的线程每秒只处理 $4/(20\times10^{-6})=200000$ 项；每项 8 KiB，对应约 1.64 GB/s 的持续处理能力。采用这样的操作大小，要达到 40 GB/s，平均每秒需处理约 488 万项，约每 0.205 μs 一项。增加槽位只能吸收短时间突发，持续负载需要提高完成通知的处理速率或扩大每项操作的载荷。
+对比图 7-28 和图 7-29，增加槽位让蓝色传输条带提前了，右侧长长的橙色等待却仍然存在。决定持续吞吐的，是这些槽位最终释放得有多快。用并发与吞吐模型看，处理完成通知的线程每秒只处理 $4/(20\times10^{-6})=200000$ 项；每项 8 KiB，对应约 1.64 GB/s 的持续处理能力。采用这样的操作大小，要达到 40 GB/s，平均每秒需处理约 488 万项，约每 0.205 μs 一项。增加槽位只能容纳短时间内突然增多的请求，持续负载需要提高完成通知的处理速率或扩大每项操作的载荷。
 
 下游处理不及、通过限制上游提交速度来防止请求继续积压的机制，称为**背压（backpressure）**。这里，请求槽位用满后，端点暂停或减慢提交，直到有槽位释放。它通过限制未完成请求数，控制上游提交速度，防止未完成工作无限增长。若缓冲区的释放需要等待远端程序用完数据，背压就会跨设备传播；若许多工作共享传输状态，传播范围还会进一步扩大。
 
@@ -503,7 +503,7 @@
 
 *图 7-31：将到达与服务速率的差额随时间累积，得到 600、150、0 MB 三种峰值。此算例使用无限缓冲计算需求积压。*
 
-**讨论：发送时刻偏移多少会使共享出口产生积压？** 重叠时间为 $h$ 时，新增积压为 $(80-50)h$。重叠 5 ms 就积压 150 MB。若只给这次突发留出 512 KiB 缓冲，从空队列出发，允许的重叠时间约为 $512\ \mathrm{KiB}/30\ \mathrm{GB/s}=17.5$ μs。毫秒级的高峰漂移，远大于这份缓冲能够吸收的时间。
+**讨论：发送时刻偏移多少会使共享出口产生积压？** 重叠时间为 $h$ 时，新增积压为 $(80-50)h$。重叠 5 ms 就积压 150 MB。若只给这次突发留出 512 KiB 缓冲，从空队列出发，允许的重叠时间约为 $512\ \mathrm{KiB}/30\ \mathrm{GB/s}=17.5$ μs。高峰时刻发生毫秒级偏移，远远超过这份缓冲所能容忍的重叠时间。
 
 CASSINI 利用训练的周期通信，通过安排作业的部署位置和通信时刻，让通信高峰错开，并监测漂移、重新调整。[^cassini] 本书两个 CPU 分布式数据并行（DDP）训练作业的一次性错峰对照也显示，初始延迟 50 ms 后，相位在运行中继续漂移，三轮测量中，整组作业的完成时间反而增加约 2%–3%。[^phase] 周期调度需要持续维护相位；拥塞控制则根据实时反馈，调整偏离预期的发送速率。
 
@@ -519,7 +519,7 @@
 
 **链路流控**让相邻接收端在缓冲不足时暂停上游，防止接收端缓冲区溢出。**端到端拥塞控制**把瓶颈信息送回发送者，降低发送速率。前者迅速阻止局部溢出，后者让源端需求适应整条路径的传输能力。
 
-沿用 80 GB/s 到达与 50 GB/s 出口，设缓冲总量为 512 KiB，已经占用 256 KiB。剩余空间每微秒被 30 KB 的超额到达消耗，只能支撑约 8.7 μs。反馈若到 20 μs 才使发送减速，会新产生 600 KB 超额数据，扣除剩余空间，约 338 KB 被丢弃。[^feedback]
+沿用 80 GB/s 到达与 50 GB/s 出口，设缓冲总量为 512 KiB，已经占用 256 KiB。每微秒多到达的 30 KB 数据会占用剩余空间，只能支撑约 8.7 μs。反馈若到 20 μs 才使发送减速，会新产生 600 KB 超额数据，扣除剩余空间，约 338 KB 被丢弃。[^feedback]
 
 将剩余缓冲记为 $Q_{\mathrm{free}}$，从发现拥塞到降速的时延为 $T_f$，避免这次溢出需要
 
@@ -565,7 +565,7 @@
 
 *图 7-36：序号 0 丢失后按算例等待时间重传，其他七个报文保留在接收端。只补发缺失报文后，约 23 μs 全部可交付。*
 
-图 7-34 中的水平线说明，快路径发完并不等于整条消息可以交付。两条路径节省了发送时间，却可能增加等待缺口的时间。可以直接求出双路径传输更快的条件。单路径需要发送八个报文，双路径每条发送四个。每报文串行发送时间为 1.024 μs，双路径节省约 4.1 μs。第二条路径比第一条多出的传播时延若超过这 4.1 μs，平均分配便失去时间优势。路径选择或不均匀分配应减少慢路径承担的报文，使两条路径的最后一个报文尽量同时到达。
+图 7-34 中的水平线说明，快路径发完并不等于整条消息可以交给应用使用。两条路径节省了发送时间，却可能增加等待缺口的时间。可以直接求出双路径传输更快的条件。单路径需要发送八个报文，双路径每条发送四个。每报文串行发送时间为 1.024 μs，双路径节省约 4.1 μs。第二条路径比第一条多出的传播时延若超过这 4.1 μs，平均分配便失去时间优势。路径选择或不均匀分配应减少慢路径承担的报文，使两条路径的最后一个报文尽量同时到达。
 
 再让序号 0 丢失，在其原发送结束 20 μs 后重传，整条消息约在 23.0 μs 才能交付。此时其他七个报文均已到达，接收端缓存了 7 KiB 载荷，只需重传缺失的 1 KiB；若从缺口开始重发整个后缀，则再搬 8 KiB。**选择性重传**用保存接收进度的状态，换取更少的重复传输。
 
@@ -685,7 +685,7 @@
 
 *图 7-48：原配置每轮启动 5 μs、带宽 25 GB/s。绿色将带宽增至 75 GB/s，橙色将每轮启动减至 2 μs；小消息主要受启动影响，大消息主要受传输影响。*
 
-沿图 7-48 的横轴从左向右看，小消息区的总时间接近水平，大消息区则随载荷增大而上升。提高带宽主要降低右侧的斜坡，缩短启动主要降低左侧的平台。
+沿图 7-48 的横轴从左向右看，小消息区的总时间接近水平，大消息区则随载荷增大而上升。提高带宽主要降低右侧的斜坡，缩短启动时间主要使左侧平坦区的曲线下移。
 
 对 8 MiB 输入，带宽由 25 增至 75 GB/s，一次归约从约 657 μs 降至 266 μs；对 8 KiB 输入，则只从约 70.6 μs 降至 70.2 μs。假设执行一个 36 层模型时，每层注意力与前馈网络（FFN）各有一次这样的归约，72 次串行归约在两种带宽下均约为 5.1 ms，差值约 28 μs。把每轮启动从 5 μs 降到 2 μs，72 次归约却能减少约 3.0 ms。[^planning]
 
--- a/manuscripts/08-单实例推理.md
+++ b/manuscripts/08-单实例推理.md
@@ -57,7 +57,7 @@
 M_w+M_{\mathrm{KV}}+M_a+M_u\le C.
 $$
 
-在这几项内存占用中，权重在请求到达前就已装入，KV 则随着请求到达和生成不断增长。要算出实例能同时处理多少请求，先求每个 token 要保存多少 KV。Qwen3-8B 有 36 层，每层有 8 个 KV 头，每个头的维度为 128。每个 token 在每层都需要保存 K 和 V，BF16 每个元素占 2 bytes，因此每个 token 所需的 KV 容量为
+在这几项内存占用中，权重在请求到达前就已加载到内存，KV 则随着请求到达和生成不断增长。要算出实例能同时处理多少请求，先求每个 token 要保存多少 KV。Qwen3-8B 有 36 层，每层有 8 个 KV 头，每个头的维度为 128。每个 token 在每层都需要保存 K 和 V，BF16 每个元素占 2 bytes，因此每个 token 所需的 KV 容量为
 
 $$
 k=2\times36\times8\times128\times2
@@ -120,7 +120,7 @@
 
 图 8-3 展示了这一变化。横轴上相邻的刻度表示 batch 加倍，因此权重项每次减半；KV 项保持不变，两项之和逐渐接近 KV 对应的水平线。对于 2K 上下文，batch 从 1 增至 64 时，每个输出 token 的读取量从约 14.38 GiB 降至 0.50 GiB，约为原来的 1/29。上下文长度增至 8K 后，KV 容量增至原来的四倍，合批后的每个输出 token 仍需读取更多数据。
 
-![批量增加时每个输出 token分摊的权重与上下文读取量](ch08/figure-8-2-batch.svg)
+![批量增加时每个输出 token 分摊的权重与上下文读取量](ch08/figure-8-2-batch.svg)
 
 *图 8-3：权重由批内输出共同分摊，上下文 KV 的读取量决定了曲线最终接近的下限。Qwen3-8B BF16，每份矩阵权重批内读取一次，各请求的上下文彼此独立；实线为权重与 KV 两项之和，虚线为对应上下文的 KV 项，灰线为共享权重项。横轴按倍数取值。*
 
@@ -199,7 +199,7 @@
 
 *图 8-9：八个旧位置形成 4 × 8 = 32 个蓝色格，块内仍为 10 个绿色格。块长相同，总配对从 10 增至 42。*
 
-模型还要执行投影和前馈网络（FFN，对各位置分别进行特征变换），这些计算每块都处理 512 个新位置，工作量基本相同。在已有逐块记录中，末块的模型主干矩阵运算量比首块多约 32%，执行时间中位数从约 25.3 ms 增至 35.1 ms，增加约 39%。整块执行时间因此由两部分共同决定：一部分主要随新位置数变化，另一部分还随已有上下文长度增长。[^chunk]
+模型还要执行投影和前馈网络（FFN，对各位置分别进行特征变换），这些计算每块都处理 512 个新位置，工作量基本相同。在已有逐块记录中，末块的模型主干矩阵运算量比首块多约 32%，执行时间中位数从约 25.3 ms 增至 35.1 ms，增加约 39%。整块执行时间因此由两部分共同决定：一部分主要随新增位置数变化，另一部分还随已有上下文长度增长。[^chunk]
 
 上下文增长解释了同样大小的块为何越来越慢；缩小块长则会带来另一种开销。将一个 256-token 块拆成两个 128-token 块，调度机会增加一次，矩阵行数减半，每个小块都要使用同一份权重。以一层 FFN 的 288 MiB 权重为例，设两次执行分别从高带宽内存（HBM）读取它们，这部分读取就从 288 增至 576 MiB。小块增加了响应机会，也增加了执行次数；调度器应选择能满足输出间隔的较大块，使新增启动与读取尽量少。[^nanoflow]
 
@@ -227,7 +227,7 @@
 
 *图 8-10：逻辑块 0、1、2 按顺序组成序列，块表分别指向物理块 2、0、3。物理块 1 为空闲，注意力按块表恢复逻辑顺序。*
 
-设每块有 $p$ 个位置，当前长度为 $L$，需要 $\lceil L/p\rceil$ 个块。只有最后一块存在未使用的尾槽，浪费为
+设每块有 $p$ 个位置，当前长度为 $L$，需要 $\lceil L/p\rceil$ 个块。只有最后一块存在尾部未使用的位置，浪费为
 
 $$
 p\left\lceil\frac{L}{p}\right\rceil-L,
@@ -235,7 +235,7 @@
 
 最多 $p-1$ 个位置。块越小，这一上限越低，块表项和分配次数也越多。2023 年，伯克利等机构的研究者在 vLLM 中提出 PagedAttention，将操作系统分页管理的思路用于 KV 缓存，减少预留空间与内存碎片对服务并发的限制。PagedAttention 将这种映射带入注意力执行；FlashAttention 则在算子内部组织分块计算与中间结果，两者分别作用于状态分配和计算访存。
 
-**算例：KV 分页分配能减少多少预留空间？** 设 A、B、C、D 当前长度分别为 9、13、5、15，每条最大允许 16 个位置。整段预留共占 64 个位置；取 $p=4$，分页分别分配 12、16、8、16，共 52 个位置。其中有效位置仍是 42 个，未用尾槽从 22 减到 10 个。
+**算例：KV 分页分配能减少多少预留空间？** 设 A、B、C、D 当前长度分别为 9、13、5、15，每条最大允许 16 个位置。整段预留共占 64 个位置；取 $p=4$，分页分别分配 12、16、8、16，共 52 个位置。其中有效位置仍是 42 个，尾部未用位置从 22 减到 10 个。
 
 ![整段预留、分页与前缀共享的物理位置数](ch08/figure-8-5-pages.svg)
 
@@ -243,7 +243,7 @@
 
 ![改用四位置分页](ch08/figure-8-pages-paged.svg)
 
-*图 8-12：各请求分别分配 12、16、8、16 个位置，共 52 个。灰色尾槽从 22 减到 10。*
+*图 8-12：各请求分别分配 12、16、8、16 个位置，共 52 个。灰色的尾部未用位置从 22 减到 10。*
 
 若 A、B 的前八个位置相同，还可以让它们共用两个完整块，使总分配量进一步降至 44 个位置。分页减少了尚未使用的预留空间，共享消除了重复保存的上下文，两者节省的是不同部分的内存。
 
@@ -385,15 +385,15 @@
 
 **Q2_K** 是张量计算库 GGML 系列实现中的一种分组量化格式，主编码使用 2 bit，并为组内缩放保存额外信息。以它为例，每组有 256 个值，2-bit 编码占 $256\times2/8=64$ bytes，缩放系数和最小值等元数据另占 20 bytes。因此，一组共需 84 bytes，平均每值为 $84\times8/256=2.625$ bit。位宽降低后，元数据在总容量中的比重相应提高，在这个格式中约占 24%。[^gguf]
 
-**扩展算例：量化权重装入内存后，还能容纳多长的上下文？** Qwen3-235B-A22B 的一个 Q2_K 文件变体采用以 2 比特为主的分组量化，并混合使用多个位宽，量化编码与未量化的浮点数据约 64.43 GiB、量化元数据约 15.37 GiB，加上文件头与填充，共约 79.81 GiB。另一个 `UD-Q2_K_XL` 变体约 81.97 GiB。取 96 GB 十进制内存、给系统与工作区预留 8 GiB，剩余约 81.41 GiB。[^files]
-
-装入前一个文件后，还剩 1.60 GiB；后一个文件则已经超出可用空间约 0.56 GiB。该模型一条 8K 上下文的 BF16 KV 约占 1.47 GiB，因此前一种方案还能处理一条请求，并剩余约 0.13 GiB。若上下文长度增至 32K，KV 就需要约 5.88 GiB，必须增加内存或改变权重的存放方式。两个文件只相差 2.16 GiB，已经足以决定能否容纳请求。
+**扩展算例：量化权重加载到内存后，还能容纳多长的上下文？** Qwen3-235B-A22B 的一个 Q2_K 文件变体采用以 2 比特为主的分组量化，并混合使用多个位宽，量化编码与未量化的浮点数据约 64.43 GiB、量化元数据约 15.37 GiB，加上文件头与填充，共约 79.81 GiB。另一个 `UD-Q2_K_XL` 变体约 81.97 GiB。取 96 GB 十进制内存、给系统与工作区预留 8 GiB，剩余约 81.41 GiB。[^files]
+
+将前一个文件加载到内存后，还剩 1.60 GiB；后一个文件则已经超出可用空间约 0.56 GiB。该模型一条 8K 上下文的 BF16 KV 约占 1.47 GiB，因此前一种方案还能处理一条请求，并剩余约 0.13 GiB。若上下文长度增至 32K，KV 就需要约 5.88 GiB，必须增加内存或改变权重的存放方式。两个文件只相差 2.16 GiB，已经足以决定能否容纳请求。
 
 在统一内存系统中，CPU 和 GPU 共用物理内存。将权重文件映射到地址空间后，访问相应数据时，文件页面才需要驻留在物理内存中。工作集超过内存容量时，执行就会伴随页面换入。因此，解决容量不足可以减少数据本身的字节数，也可以只保留当前需要的数据，其余数据在使用前搬入。下面先计算 KV 压缩的收益，再分析权重搬入所需的时间。
 
 ### 8.4.2 KV 压缩与读取成本
 
-权重量化主要减少模型装入后占据的空间。接下来把同样的分组方法用于 KV，看看省下的空间能多容纳多少请求。权重由许多请求共享，KV 却持续随输入与输出增长。对本章模型，一条 8K 上下文的 BF16 KV 为 1152 MiB。每 32 个 BF16 值占 64 bytes，**q8_0** 是每 32 个值共用一个缩放系数的 8 bit 量化格式。改成 q8_0 时保存 32 bytes 码值和 2 bytes scale，变为原来的 $34/64$；**q4_0** 采用同样的分组方式、将码值降至 4 bit，保存 16 bytes 码值和 2 bytes scale，变为 $18/64$。
+权重量化主要减少模型加载到内存后占用的空间。接下来把同样的分组方法用于 KV，看看省下的空间能多容纳多少请求。权重由许多请求共享，KV 却持续随输入与输出增长。对本章模型，一条 8K 上下文的 BF16 KV 为 1152 MiB。每 32 个 BF16 值占 64 bytes，**q8_0** 是每 32 个值共用一个缩放系数的 8 bit 量化格式。改成 q8_0 时保存 32 bytes 码值和 2 bytes scale，变为原来的 $34/64$；**q4_0** 采用同样的分组方式、将码值降至 4 bit，保存 16 bytes 码值和 2 bytes scale，变为 $18/64$。
 
 因此，同一条上下文所需容量为
 
@@ -461,7 +461,7 @@
 
 ### 8.4.4 质量约束下的容量与速度选择
 
-前面分别算出了压缩节省的空间和增加的转换时间。还有一个问题尚未回答：改变数值格式后，得到的答案是否相同？这里的执行后端指实际完成注意力计算的内核实现。压缩 KV 改变保存的数值，执行后端还可能一起改变 Q 的计算格式、转换过程和归约精度。逐项比较张量的存储格式与计算精度，能够定位输出差异来自哪里。
+前面分别算出了压缩节省的空间和增加的转换时间。还有一个问题尚未回答：改变数值格式后，得到的答案是否相同？这里的执行后端指实际完成注意力计算的内核实现。压缩 KV 改变保存的数值，执行后端还可能一起改变 Q 的计算格式、转换过程和归约运算的数值精度。逐项比较张量的存储格式与计算精度，能够定位输出差异来自哪里。
 
 下面三幅图按相同顺序排列八道固定任务，标为任务 1—8；每道任务在两种并发数下各运行两次，用相同任务逐一比较不同数值格式的影响。
 
@@ -676,13 +676,13 @@
 
 ## 历史与延伸阅读
 
-迭代级接纳、分页注意力与前缀树缓存分别推动了请求调度、状态分配和计算复用。Orca、PagedAttention 与 SGLang 的相关设计可沿这些机制阅读；Sarathi-Serve 和 NanoFlow 进一步研究分块、流水及不同资源之间的重叠。[^schedule-survey][^nanoflow]
+每轮迭代接纳新请求、分页注意力与前缀树缓存分别推动了请求调度、状态分配和计算复用。Orca、PagedAttention 与 SGLang 的相关设计可沿这些机制阅读；Sarathi-Serve 和 NanoFlow 进一步研究分块、流水及不同资源之间的重叠。[^schedule-survey][^nanoflow]
 
 低秩适配（LoRA）用两个较小矩阵的乘积表示权重调整量，基础模型权重保持不变。多 LoRA 服务让不同适配器（adapter）共用一个基础模型。对 Qwen3-8B 的 Q、V 投影使用低秩维度为 16 的 BF16 适配器时，每套约占 14.6 MiB，100 套约占 1.43 GiB；100 条独立 8K 上下文的 KV 却需要 112.5 GiB。Punica、S-LoRA 分别管理 adapter 与请求状态，在共用基础模型的同时执行各 adapter 的低秩计算。[^lora]
 
 混合注意力与递推模型把缓存索引扩展为状态恢复问题，Marconi 等工作讨论恢复机会和缓存价值。权重卸载、张量编码以及统一内存执行，则将设备容量与链路成本联系起来；配套 M2 Max 记录展示了小模型的真实 KV 分配与输入处理。[^hybrid][^codec][^mac]
 
-推测采样的接受与修正规则奠定了保持目标分布的基础，后续上下文查询、特征草稿和并行草稿主要改变每轮成本及接受长度。完整 Agent 任务还包含工具和外部阶段，公开速度与任务记录可结合第 8.6 节的阶段分解阅读。[^spec][^task]
+推测采样的接受与修正规则奠定了保持目标分布的基础，后续从上下文查找草稿、依据中间特征预测草稿以及并行生成草稿的方法，主要改变每轮开销与被接受的草稿长度。完整 Agent 任务还包含工具和外部阶段，公开速度与任务记录可结合第 8.6 节的阶段分解阅读。[^spec][^task]
 
 [^batch]: 固定模型的[批处理计算](../calculations/results/batch-reuse-h100-2k.md)及[8K 对照](../calculations/results/batch-reuse-h100-8k.md)。完整 BF16 权重为 16,381,470,720 bytes，一步理想共享矩阵权重读取为 15,136,811,008 bytes；embedding 按请求查行，驻留仍包含完整 embedding。读取表先用精确字节计算，再舍入各项。
 
@@ -690,7 +690,7 @@
 
 [^schedule-survey]: [Orca 原文](../references/files/papers/orca.pdf)与[分块计算和版本说明](../case-studies/chunking-and-state-transfer.md)。
 
-[^schedule]: [固定批次](../calculations/results/iteration-batching-fixed.md)、[连续批处理](../calculations/results/iteration-batching-continuous.md)、[分块策略](../calculations/results/iteration-batching-chunked.md)，显式教学成本，最终输出的 pending 状态与容量口径见各文件。
+[^schedule]: [固定批次](../calculations/results/iteration-batching-fixed.md)、[连续批处理](../calculations/results/iteration-batching-continuous.md)、[分块策略](../calculations/results/iteration-batching-chunked.md)，显式教学成本，最终输出已生成但尚未写入 KV 的状态及容量计算范围见各文件。
 
 [^chunk]: [逐块计算和封存测量](../calculations/results/chunk-history-book.md)。11 次请求、每次 16 块，首末块时间中位数 25.30、35.07 ms；CUDA event 区间包括完整模型路径和可能的主机间隙，不含外部 logits／采样。各块按原始顺序执行，输入位置与内容逐块变化。
 
--- a/manuscripts/09-分布式推理.md
+++ b/manuscripts/09-分布式推理.md
@@ -62,7 +62,7 @@
 | --- | --- | --- |
 | 增加完整副本 | 处理更多独立请求，分散排队 | 模型复制、缓存分散、启动及保持实例运行 |
 | PD 分离 | 分别配置两个阶段的能力 | KV 交接、两端同时占用内存、两个队列 |
-| AF 分离 | 让算子使用适合的计算与存储资源 | 逐层激活往返、同步、流水平衡 |
+| AF 分离 | 让算子使用适合的计算与存储资源 | 逐层激活往返、同步、流水线各阶段的负载均衡 |
 | 共享 KV | 跨实例复用上下文，减少重复 prefill | 缓存标识、读取、就绪通知、失效与空间释放 |
 
 部署选择从当前负载的资源需求开始。如果两个阶段的资源需求相近，完整副本已经能够均衡服务，分离可能只会增加交接开销。如果许多请求共享长前缀，跨实例缓存可能比进一步拆分计算更有价值。分别计算资源占用和交接时间，就能判断当前应增加副本、分离阶段还是共享状态。
@@ -77,7 +77,7 @@
 
 以本章的 Qwen3-8B 为例，一份包含 8192 个位置的 BF16 上下文 KV 占 1.125 GiB。工具执行 10 秒时，GPU 可以处理其他请求，但这份上下文 KV 仍需占用 1.125 GiB 内存，容量与占用时长的乘积为 11.25 GiB·s；十个这样的会话同时等待，就占 11.25 GiB。计算暂停后，状态仍需保留，这正是共享缓存和换出策略要解决的问题。
 
-这条请求若随后返回 129 个 token，P 先产生第一个输出，128 次 decode 再处理前 128 个生成位置。此时连续 KV 覆盖 $8192+128=8320$ 个位置，最后一个返回 token 尚未送回模型。下一轮在这 8320 个位置之后继续，先处理这个尾 token 和新增输入。因此，已返回的输出序列比 KV 所覆盖的序列多一个 token；恢复生成时，需要从这个位置继续计算。
+这条请求若随后返回 129 个 token，P 先产生第一个输出，128 次 decode 再处理前 128 个生成位置。此时连续 KV 覆盖 $8192+128=8320$ 个位置，最后一个返回 token 尚未送回模型。下一轮在这 8320 个位置之后继续，先处理这个尚未写入 KV 的末尾 token 和新增输入。因此，已返回的输出序列比 KV 所覆盖的序列多一个 token；恢复生成时，需要从这个位置继续计算。
 
 ## 9.2 Prefill–Decode 分离与资源配比
 
@@ -205,7 +205,7 @@
 
 对 Qwen3-235B-A22B，一个专家包含三个矩阵，参数数量为 $3\times4096\times1536=18874368$。BF16 权重为 36 MiB。
 
-**例 9.3　专家权重复用如何改变 CPU 与 GPU 的执行选择？** 设有效 CPU 算力为 2 TFLOP/s、DRAM 带宽为 200 GB/s，GPU 算力为 100 TFLOP/s、HBM 带宽为 1000 GB/s，交接带宽为 25 GB/s，每次启动为 5 μs，BF16 激活宽度为 4096。八个专家依次执行，总时间为各专家耗时之和，权重每批读取一次。每个专家只收到一个 token 的特征向量时，CPU 可以直接读取本地主存中的权重，避免了较慢链路上的 36 MiB 搬运；随着每个专家的输入行数增加，一次权重传输的开销由更多行计算分摊，而 CPU 更早进入计算受限区。
+**例 9.3　专家权重复用如何改变 CPU 与 GPU 的执行选择？** 设有效 CPU 算力为 2 TFLOP/s、DRAM 带宽为 200 GB/s，GPU 算力为 100 TFLOP/s、HBM 带宽为 1000 GB/s，交接带宽为 25 GB/s，每次启动为 5 μs，BF16 激活宽度为 4096。八个专家依次执行，总时间为各专家耗时之和，权重每批读取一次。每个专家只收到一个 token 的特征向量时，CPU 可以直接读取本地主存中的权重，避免了较慢链路上的 36 MiB 搬运；随着每个专家的输入行数增加，一次权重传输的开销由更多行计算分摊，而 CPU 更早受到算力限制。
 
 先看八个热点专家各处理一个 token：权重合计 288 MiB。在 200 GB/s 主存上读取约 1.5 ms，加激活交接后 CPU 路径约 1.6 ms；搬到 GPU 的路径仅传权重就约 12.1 ms，连同启动与 GPU 计算约 12.4 ms。低复用时，避免大块权重搬运比提高矩阵算力更有价值。
 
@@ -342,7 +342,7 @@
 
 ### 9.5.1 缓存标识、共享范围与可用状态
 
-前两节通过改变执行位置和任务分配，减少了当前请求的耗时。另一种节省计算的方法，是让后续请求直接使用前一次计算留下的 KV。第 8.3 节已经说明分页、前缀匹配、共享和淘汰的机制。本节接着考虑状态分布在不同执行位置时，怎样确认匹配、找到数据并完成传输。前缀缓存的价值来自避免重复执行，但“相同文本”不足以唯一确定所有可复用状态。模型与适配器（adapter，附加在基础模型上的参数模块）版本、token 序列、位置、状态格式以及必要的编码配置都可能参与缓存标识的计算。例如，同样的 token 使用不同 adapter，会在投影后得到不同的 K、V；同样的后缀接在不同前文后，attention 的上下文也发生变化。缓存键需要沿前缀链标识生成这份状态的计算。窗口或循环状态则还要携带所在位置与更新时刻，才能从同一个状态继续。
+前两节通过改变执行位置和任务分配，减少了当前请求的耗时。另一种节省计算的方法，是让后续请求直接使用前一次计算留下的 KV。第 8.3 节已经说明分页、前缀匹配、共享和淘汰的机制。本节接着考虑状态分布在不同执行位置时，怎样确认匹配、找到数据并完成传输。前缀缓存的价值来自避免重复执行，但“相同文本”不足以唯一确定所有可复用状态。模型与适配器（adapter，附加在基础模型上的参数模块）版本、token 序列、位置、状态格式以及必要的编码配置都可能参与缓存标识的计算。例如，同样的 token 使用不同 adapter，会在投影后得到不同的 K、V；同样的后缀接在不同前文后，attention 的上下文也发生变化。缓存键需要沿前缀链标识生成这份状态的计算。滑动窗口状态或递推状态则还要携带所在位置与更新时刻，才能从同一个状态继续。
 
 确认两份状态可以相互替代之后，还要确定它们存在哪里、其他实例怎样读取。容量相加并不会自动形成共享缓存。两个实例各有 4 GiB 私有主机内存（host memory）缓存时，一份 1.125 GiB 前缀可能在两端各占一份，共占 2.25 GiB；另一个实例却仍无法直接复用它。若二者连接到同一共享存储后端（接收和提供缓存对象的存储服务），一份对象可以服务两端，但每端在生成前可能还要取回本地。SGLang 的分层缓存 HiCache、vLLM 的多级缓存路径和 KV 缓存管理系统 LMCache 提供了不同的组织方式，取回是否经 CPU 决定下一节的路径时间。[^cache]
 
@@ -372,7 +372,7 @@
 
 ### 9.5.3 持久化、检查点与部分重算
 
-上一节假定保存下来的状态能完整取回。实例重启后，目录记录、磁盘数据和可连续复用的前缀可能并不一致，恢复过程需要重新确认哪些计算已经保存。持久化让状态在实例重启或任务暂停后仍可使用。完整保存减少恢复时的重算，周期检查点减少写入但增加恢复工作，部分重算则利用保存前缀补齐缺失部分。检查点要保存继续计算所需的全部状态：完整 GQA 保存上下文 K、V；DeepSeek V4 的状态还涉及压缩结果、窗口以及未完成的压缩块。恢复时从保存的更新边界继续推进。[^v4]
+上一节假定保存下来的状态能完整取回。实例重启后，目录记录、磁盘数据和可连续复用的前缀可能并不一致，恢复过程需要重新确认哪些计算已经保存。持久化让状态在实例重启或任务暂停后仍可使用。完整保存减少恢复时的重算，周期检查点减少写入但增加恢复工作，部分重算则利用保存前缀补齐缺失部分。检查点要保存继续计算所需的全部状态：完整 GQA 保存上下文 K、V；DeepSeek V4 的状态还涉及压缩结果、窗口以及未完成的压缩块。恢复时从检查点所记录的最后一次完整更新继续计算。[^v4]
 
 先考虑完整 GQA 页。Qwen3-8B 每个 16-token 逻辑页为 2.25 MiB。若各层 K、V 分片分别搬运，可能形成许多小连续段；若按页汇集，则消息数和布局转换不同。一个逻辑页分成 36 层的 K、V 后有 72 个 32 KiB 连续段；按页汇集后则成为一个 2.25 MiB 对象。前者更容易受每秒操作次数限制，后者把更多时间用于连续载荷传输。
 
@@ -439,7 +439,7 @@
 
 ## 9.6 服务启动、扩缩容与故障恢复
 
-将第 8 章的状态恢复过程计入路由决策后，需要同时比较实例的空闲时间与状态准备时间。设实例 A 保留全局 KV 与编码器 SWA 但有排队，实例 B 可立即执行却需要取回全局 KV 并恢复编码器 SWA。A 的优势是能够复用状态，B 的优势是设备可以立即开始工作；路由器需要比较它们的预计完成时间，而不是只看单个缓存命中标志。
+将第 8 章的状态恢复过程计入路由决策后，需要同时比较实例何时空闲与状态何时准备好。设实例 A 保留全局 KV 与编码器 SWA 但有排队，实例 B 可立即执行却需要取回全局 KV 并恢复编码器 SWA。A 的优势是能够复用状态，B 的优势是设备可以立即开始工作；路由器需要比较它们的预计完成时间，而不是只看单个缓存命中标志。
 
 设两端后续的新增输入处理、解码器窗口重放与生成耗时相同，准备工作串行执行。B 的全局传输按第七章为 4.666 ms，编码器 SWA 恢复假设为 8 ms，共需 12.666 ms。A 保留了这两份状态，准备时间就是排队时间：排队 10 ms 时，A 更早开始处理新增输入；排队增至 20 ms 时，B 更早。12.666 ms 由此成为本例切换路由选择的排队阈值。
 
@@ -527,7 +527,7 @@
 
 **例 9.7　哪种推理部署能持续服务并按期清空启动积压？** 采用章首的资源与负载：四个 A 类 worker、四个 B 类 worker，每秒到达四个独立请求，每个请求输入 8192 个位置、输出 129 个 token。有效能力沿用例 9.2，两阶段在同一 worker 上串行占用资源，表中有效能力对应下述驻留限制。链路为共享的 25 GB/s 载荷通道，各次搬运占用同一份链路带宽。比较窗口内模型与生成设置相同，请求需要的前缀互不复用。服务从停止状态启动，三种部署方案都在 10 秒后就绪；就绪后的队列按连续流量模型处理。设计目标是持续处理新到达的请求，并在开始启动后的 25 秒内消除启动积压。
 
-容量条件也一并给定：各 worker 装入模型后，留出 12 GiB 专用于本例的 KV 与交接缓冲，调度器最多允许八份活动请求状态同时驻留；使用共享池的方案另有 64 GiB 状态容量。每个请求经过 128 次 decode 后，其 KV 覆盖 8320 个位置，占约 1.14 GiB。八份活动状态共约 9.14 GiB，再预留一份 1.125 GiB 接收缓冲，共约 10.3 GiB，能放入每个 worker 的 12 GiB 可用空间。尚未执行的请求只保留输入，分配到缓存空间后再开始计算。
+容量条件也一并给定：各 worker 将模型加载到内存后，留出 12 GiB 专用于本例的 KV 与交接缓冲，调度器最多允许八份活动请求状态同时驻留；使用共享池的方案另有 64 GiB 状态容量。每个请求经过 128 次 decode 后，其 KV 覆盖 8320 个位置，占约 1.14 GiB。八份活动状态共约 9.14 GiB，再预留一份 1.125 GiB 接收缓冲，共约 10.3 GiB，能放入每个 worker 的 12 GiB 可用空间。尚未执行的请求只保留输入，分配到缓存空间后再开始计算。
 
 现在比较完整副本、直接交接的 PD、经共享池交接的 PD。共享池按整份写入后读取，因此每请求在公共通道上搬两次；其读写端均能达到该通道的带宽。
 
@@ -592,7 +592,7 @@
 
 **9-5　异构推理的权重容量与 CPU 处理能力约束。** 从已保存的 DeepSeek V4-Flash 或 Kimi K2 配置还原权重存放、GPU 缓冲和主存需求。增加请求并发后，分别预测被访问的专家数量、各专家的任务数，以及负载最重的 NUMA 节点。再构造一组总权重可以容纳、但到达率超过 CPU 计算能力的请求条件，推导积压增长率。
 
-**9-6　专家副本的容量约束与复制成本回收。** 设八个专家分别接收 $[32,16,8,4,2,1,1,0]$ 行输入，比较只计算有效行、只对非空专家补齐，以及对全部专家补齐时的计算量。随后复算例 9.5 的严格回本批数，把每卡额外容量改为 32 MiB，解释为什么应先拒绝不可行副本方案。对动态热点给出观察窗口与迁移成本的比较方法。
+**9-6　专家副本的容量约束与复制成本回收。** 设八个专家分别接收 $[32,16,8,4,2,1,1,0]$ 行输入，比较只计算有效行、只对非空专家补齐，以及对全部专家补齐时的计算量。随后复算例 9.5 的累计节省时间首次超过复制时间的批次数，把每卡额外容量改为 32 MiB，解释为什么应先拒绝不可行副本方案。对动态热点给出观察窗口与迁移成本的比较方法。
 
 **9-7　KV 保存、取回与重算的收益条件〔核心〕。** 给定一份占 1.125 GiB 的前缀 KV，比较重算、一次取回并驻留、100 次逐步远程读取。进一步计入写入耗时，以及两次请求之间保留缓存的时间，推导何时保存 KV 比重算更合算；设计一个改变 HBM 可用容量后需要将状态从 HBM 换出的例子。结合已有双引擎记录，解释共享 CPU 池从 4 GiB 增至 8 GiB 时，哪些复用机会得以保留。
 
--- a/manuscripts/10-训练系统.md
+++ b/manuscripts/10-训练系统.md
@@ -190,7 +190,7 @@
 
 相应算子需要等待这段时间。传输和计算能重叠多少，取决于数据何时可发、链路何时空闲、相应算子何时需要这些数据。直接内存访问（DMA）由设备发起数据搬运，传输期间源数据必须保留。源缓冲在 DMA 结束前保持有效，目标缓冲从接收开始占用容量；卸载同时改变两端缓冲区的使用时间。
 
-**例：在 CPU 还是 GPU 上转换梯度，能更快完成卸载？** 门控投影（gate）决定前馈子网络各位置的通过程度。一个 gate 梯度形状为 $[12288,4096]$，BF16 大小 $G=96$ MiB，FP32 为 192 MiB。CPUAdam 是在 CPU 上执行 Adam 更新的实现，它使用 FP32 梯度，可以先传 BF16 再在 CPU 转换，也可以先在 GPU 转换再传 FP32。转换都要读 $G$、写 $2G$，共访问 $3G$ bytes。设 CPU、GPU 转换吞吐分别为 100、1500 GB/s，串行路径时间为
+**例：在 CPU 还是 GPU 上转换梯度，能更快完成卸载？** 门控投影（gate）的输出经过激活函数后，用于调节前馈子网络中另一支投影的对应元素。一个 gate 梯度形状为 $[12288,4096]$，BF16 大小 $G=96$ MiB，FP32 为 192 MiB。CPUAdam 是在 CPU 上执行 Adam 更新的实现，它使用 FP32 梯度，可以先传 BF16 再在 CPU 转换，也可以先在 GPU 转换再传 FP32。转换都要读 $G$、写 $2G$，共访问 $3G$ bytes。设 CPU、GPU 转换吞吐分别为 100、1500 GB/s，串行路径时间为
 
 $$
 T_c=G/B+3G/C_c,\qquad T_g=3G/C_g+2G/B.
@@ -267,7 +267,7 @@
 
 ### 10.3.2 流水线调度、气泡与激活的保存时间
 
-微批次的划分保持了训练目标，却改变了计算的先后顺序。对流水线并行而言，顺序还决定每个阶段什么时候可以开始计算，以及多少份激活需要等待反向。流水线并行把模型分成多个阶段，每个微批次依次前向，再沿反方向传播梯度。**填满排空**先完成所有微批次前向，再执行反向。**1F1B（one forward, one backward）**在流水线预热后交替执行一次前向和一次反向，让较早微批次更快释放激活。
+微批次的划分保持了训练目标，却改变了计算的先后顺序。对流水线并行而言，顺序还决定每个阶段什么时候可以开始计算，以及多少份激活需要等待反向。流水线并行把模型分成多个阶段，每个微批次依次前向，再沿反方向传播梯度。**填满排空（fill–drain）调度**先完成所有微批次的前向传播，再集中执行反向传播。**1F1B（one forward, one backward）**在流水线预热后交替执行一次前向和一次反向，让较早微批次更快释放激活。
 
 先设 $p$ 个阶段完全平衡，阶段前向为 $t_f$、反向为 $t_b$，传输和参数更新耗时取零。$m$ 个微批次填满排空需要
 
@@ -279,7 +279,7 @@
 
 **例：1F1B 如何改变激活驻留与流水完成时间？** 将 Qwen3-8B 的 36 层分为四个九层阶段，八个微批次各含一条 128-token 序列。每阶段前向 10 ms、反向 20 ms，边界传输每次 1 ms，正反方向链路独立，参数更新耗时 1 ms。先看填满排空：无传输时为 $(8+4-1)\times30=330$ ms；前向填充经过三条边界增加 3 ms，反向排空再增加 3 ms，参数更新增加 1 ms，总计 337 ms。
 
-1F1B 更早开始反向，也使后续前向等待反向释放计算资源。在阶段 0，前四个微批次于 40 ms 完成前向，第一份传回的梯度于 106 ms 到达。随后反向执行到 126 ms，第五个微批次才开始前向。这种交错逐级影响下游到达时间。阶段 3 在 93 ms 完成第二个微批次反向，但第三个前向输入到 95 ms 才到，产生 2 ms 空隙；后续相似的等待继续进入关键路径。按这些依赖关系排定执行顺序后，所有梯度在 346 ms 时计算完毕，加上参数更新共为 347 ms。[^pipeline]
+1F1B 更早开始反向，也使后续前向等待反向释放计算资源。在阶段 0，前四个微批次于 40 ms 完成前向，第一份传回的梯度于 106 ms 到达。随后反向执行到 126 ms，第五个微批次才开始前向。这种交错逐级影响下游到达时间。阶段 3 在 93 ms 完成第二个微批次反向，但第三个前向输入到 95 ms 才到，产生 2 ms 空隙；后续相似的等待继续延长关键路径。按这些依赖关系排定执行顺序后，所有梯度在 346 ms 时计算完毕，加上参数更新共为 347 ms。[^pipeline]
 
 ![两种流水的依赖等待与激活保存](ch10/figure-10-7-pipeline.svg)
 
@@ -329,7 +329,7 @@
 
 训练中的路由均衡机制作用于样本如何选择专家；专家副本和拓扑映射则作用于这些选择在哪里执行。
 
-均衡专家的计算量以后，数据传输仍可能让设备等待。MoE 的前向 token 交换、反向输入梯度传输和参数梯度归约会争用同一条链路，因此还要利用上一节的通信时间轴分析。取一个 72 MiB 的 FP32 专家梯度，有效传输速率 16 GiB/s，每次通信启动 0.1 ms。整桶耗时约 $72/16384\times1000+0.1=4.5$ ms，放不进一个 2 ms 空隙。若三个 24 MiB 子桶分别提前就绪，每桶约 1.6 ms，可进入三个这样的空隙；总通信处理时间却增加到约 4.7 ms。虽然多启动了两次通信，但每个小桶都能在计算结束前传完，因此减少了计算之后的等待。[^moe]
+均衡专家的计算量以后，数据传输仍可能让设备等待。MoE 的前向 token 交换、反向输入梯度传输和参数梯度归约会争用同一条链路，因此还要利用上一节的通信时间轴分析。取一个 72 MiB 的 FP32 专家梯度，有效传输速率 16 GiB/s，每次通信启动 0.1 ms。整桶耗时约 $72/16384\times1000+0.1=4.5$ ms，无法在一个 2 ms 空闲时段内完成。若三个 24 MiB 子桶分别提前就绪，每桶约 1.6 ms，可分别在三个这样的空闲时段内完成；总通信处理时间却增加到约 4.7 ms。虽然多启动了两次通信，但每个小桶都能在计算结束前传完，因此减少了计算之后的等待。[^moe]
 
 专家负载取决于设备上的专家接到了多少次 token 分派。长上下文还有另一层差别：即使 token 总数相同，需要计算的注意力配对数也可能不同。
 
@@ -357,7 +357,7 @@
 
 本章的设计案例为输入安排四个这样的数据准备进程，合计八条/s。48 卡输入就绪时每步约 56.2 s，每步处理 384 条，需求约为 6.8 条/s，低于八条/s。数据准备速度高于训练消耗速度，可以在短暂变慢之后重新补满预取队列。设计题再给每步 0.5 s 的未被预取消除的平均输入等待时间，32 卡和 48 卡的每步训练时间由此成为约 82.8 s 和 56.7 s。[^design]
 
-预取队列让数据准备与 GPU 计算以不同进度运行。正常训练时，这种进度差可以吸收波动；保存检查点时，则必须分清哪些数据已经用于训练。假设数据加载器已经为前 108 个批次分配了准备任务，而训练只完成第 100 个，第 101—108 个仍在处理或排队。若恢复时直接从第 109 个批次开始，就会跳过八个批次。已分配给数据准备进程的批次位置反映预取进度，已用于训练的批次位置反映训练进度；两者之间的缓冲保存了尚未训练的数据。
+预取队列让数据准备与 GPU 计算以不同进度运行。正常训练时，提前准备的数据可以缓解输入速度波动造成的等待；保存检查点时，则必须分清哪些数据已经用于训练。假设数据加载器已经为前 108 个批次分配了准备任务，而训练只完成第 100 个，第 101—108 个仍在处理或排队。若恢复时直接从第 109 个批次开始，就会跳过八个批次。已分配给数据准备进程的批次位置反映预取进度，已用于训练的批次位置反映训练进度；两者之间的缓冲保存了尚未训练的数据。
 
 ![预取进度与已经完成的训练进度](ch10/figure-10-10-input-queue.svg)
 
@@ -369,7 +369,7 @@
 
 ### 10.4.2 可恢复状态与布局变换
 
-上一节已经说明，恢复训练要从正确的数据位置继续。模型一侧也有同样要求：下一次参数更新既取决于当前权重，也取决于优化器保留的历史。因此，检查点需要权重、Adam 的一阶矩与二阶矩、步数、学习率状态、随机状态和下一批数据的组成。学习率控制更新步长，随机状态决定后续随机采样的序列；保存它们可以从中断处继续同一训练过程。权重相同而优化器的矩状态不同，下一次参数更新的方向便可能不同；数据读取位置相同而打包残留不同，下一条训练序列也会改变。
+上一节已经说明，恢复训练要从正确的数据位置继续。模型一侧也有同样要求：下一次参数更新既取决于当前权重，也取决于优化器保留的历史。因此，检查点需要权重、Adam 的一阶矩与二阶矩、步数、学习率状态、随机状态和下一批数据的组成。学习率控制更新步长，随机状态决定后续随机采样的序列；保存它们可以从中断处继续同一训练过程。权重相同而优化器的矩状态不同，下一次参数更新的方向便可能不同；数据读取位置相同而打包时剩余的 token不同，下一条训练序列也会改变。
 
 一次训练迭代结束后，下一批梯度由反向重新产生。此时保存权重 2 bytes、主权重 4 bytes 和两份 Adam 状态 8 bytes，共每参数 14 bytes。Qwen3-8B 的这部分检查点约为 115 GB。在一次训练迭代结束时，把模型状态和已处理到的数据位置一同保存，就能确定恢复后该用哪些参数、从哪一批数据继续训练。
 
@@ -383,7 +383,7 @@
 
 权重可以凭原矩阵的行号重新拼接，训练序列也需要保留足够的信息，才能重新组成同样的输入。图 10-25 中已经准备好但尚未用于训练的数据，同样需要在恢复时重新定位。除了批次位置，还要保留数据预处理的中间结果。
 
-打包器若保留 2,000 个 token，下一份数据提供 6,192 个，两者组成下一条 8,192-token 序列。如果丢失这 2,000 个 token，就只能从后续数据中取出额外的 token 来补齐下一条序列，改变它的前缀、注意力关系和标签。将已用于训练的数据位置、残留与参数更新结果一起保存，恢复后才能继续构造同一批数据。[^resume]
+打包器若保留 2,000 个 token，下一份数据提供 6,192 个，两者组成下一条 8,192-token 序列。如果丢失这 2,000 个 token，就只能从后续数据中取出额外的 token 来补齐下一条序列，改变它的前缀、注意力关系和标签。将已用于训练的数据位置、尚未组成完整序列的 token 与参数更新结果一起保存，恢复后才能继续构造同一批数据。[^resume]
 
 ### 10.4.3 同步、异步保存与带宽竞争
 
@@ -456,7 +456,7 @@
 
 这里的数据来源与通常采用固定数据集的监督微调（SFT）不同。SFT 在给定答案的真实前缀上预测下一个 token，即教师强制（teacher forcing）；部署时模型则根据自己生成的前缀继续回答。一旦前面生成错误，后续就可能进入训练数据较少覆盖的上下文，误差随生成累积。这种训练与使用时的前缀分布偏移，是固定数据 SFT 的一个局限。梯度仍可以正确对应当前损失，但训练覆盖的情境与模型实际遇到的情境不同。
 
-在策略蒸馏（on-policy distillation，OPD）让当前学生模型自己生成轨迹，再由教师针对学生实际生成的前缀提供监督，例如下一 token 的概率分布。学生因而能在自己容易出错的位置学习，减少前缀分布偏移；教师的逐 token 反馈也提供了比单个终局奖励更细的学习信号。DeepSeek V4 用多教师 OPD 将领域专家的能力合入统一模型，第三章已将教师前向列为独立的一项计算工作。[^opd]
+同策略蒸馏（on-policy distillation，OPD）让当前学生模型自己生成轨迹，再由教师针对学生实际生成的前缀提供监督，例如下一 token 的概率分布。学生因而能在自己容易出错的位置学习，减少前缀分布偏移；教师的逐 token 反馈也提供了比单个终局奖励更细的学习信号。DeepSeek V4 用多教师 OPD 将领域专家的能力合入统一模型，第三章已将教师前向列为独立的一项计算工作。[^opd]
 
 OPD 相比固定数据 SFT 的潜在优势，主要是达到目标能力所需的学习样本或更新次数可能减少；它同时增加学生在线生成、教师前向与权重同步的成本，是否减少总 GPU 小时或完成时间仍要实测。SFT 描述监督目标，offline 描述数据是否预先固定；也可以在线收集数据后使用监督损失。online 表示数据随训练过程持续产生，on-policy 则进一步要求采样分布与所优化的策略匹配。异步 online RL 可能使用滞后策略，OPD 也会遇到生成与训练引擎的计算差异。因此，在线采样用于缓解数据分布偏移，训推一致性还需要对齐生成与训练两端的计算过程。
 
@@ -560,7 +560,7 @@
 
 若总是丢弃中断轨迹并重新采样，长轨迹因执行时间更长而更容易被丢弃。设生成期间的中断率为 $\lambda$，持续 $t$ 的轨迹在不中断的情况下生成完毕的概率为 $e^{-\lambda t}$；随着 $t$ 增长，进入学习的数据比例下降。按 token 保存进展和策略版本，可以在恢复后继续生成同一条轨迹，减少这种由系统中断引入的长度偏移。跨角色资源恢复在下一章展开。
 
-V4.1 将部署时的稀疏访问与恢复方式纳入训练。稀疏注意力从训练之初就使用 64K 序列，后训练加入层级候选限制，使训练和推理使用相同搜索域；量化感知训练适应 FP4 主 KV，解码器 SWA 的有界重放也在后训练中模拟。模型由此学习使用部署路径实际提供的表示与局部状态。[^v41-case]
+V4.1 将部署时的稀疏访问与恢复方式纳入训练。稀疏注意力从训练之初就使用 64K 序列，后训练加入层级候选限制，使训练和推理使用相同搜索域；量化感知训练适应 FP4 主 KV，只重放有限长度输入来重建解码器 SWA 的过程也在后训练中模拟。模型由此学习使用部署路径实际提供的表示与局部状态。[^v41-case]
 
 ### 10.5.4 专家路由重放与训推一致性
 
@@ -582,7 +582,7 @@
 
 > **案例：路由重放对概率误差、训练耗时与奖励的影响。** 作者公布的四组路由重放（R3）开关对照各训练 100 步，开启后日志中记录的对数概率误差中位数均下降，每步总耗时中位数略长，训练奖励没有一致提高。重放减少了路由差异，但保存记录和执行重放也增加了耗时。比较两种方案时，分别观察概率误差、训练时间和固定评估任务上的表现。[^replay]
 
-同一份权重、同一批 token 下，生成端与训练端的 logprob 差异反映计算路径和采样定义的影响；MoE 的专家 ID 则标明离散路由是否一致。这些差异会传入概率比，使部分样本在参数尚未更新时就触发裁剪。异步训练又引入策略滞后，进一步改变概率比与样本保留比例。数值一致性和策略新鲜度由此共同影响有效训练样本数。
+同一份权重、同一批 token 下，生成端与训练端的 logprob 差异反映计算路径和采样定义的影响；MoE 的专家 ID 则标明离散路由是否一致。这些差异会传入概率比，使部分样本在参数尚未更新时就触发裁剪。异步训练又引入策略滞后，进一步改变概率比与样本保留比例。数值一致性和采样策略相对于当前策略的滞后程度由此共同影响有效训练样本数。
 
 RL 把生成、环境验证和参数更新组织成一个完整的反馈过程。生成端提供采样概率与专家选择，训练端据此组织更新，资源调度再安排各阶段重叠。贯通这些信息，可以同时优化数据交接、执行一致性与资源利用。前面的异步周期和路由重放例子分别量化了省下的等待与增加的记录成本，最终用有效样本和训练进展评价整个反馈循环。
 
@@ -680,7 +680,7 @@
 
 **误区：分片数翻倍，训练显存峰值就减半。** 分片只直接缩小分给各设备的长期保存的训练状态。小模型例中长期保存的训练状态从 18 MiB 减到 9 MiB，执行期间的其他张量和缓冲区仍占约 21 MiB，峰值从 39 MiB 减到 30 MiB。决定容量的量是同一时刻的总显存占用。
 
-**误区：通信处理时间更短，训练步耗时就更短。** 三个小桶的总处理时间约 4.7 ms，比整桶 4.5 ms 更长，却可以分别进入三个计算空隙。通信完成的时刻决定它是否延长训练步。
+**误区：通信处理时间更短，训练步耗时就更短。** 三个小桶的总处理时间约 4.7 ms，比整桶 4.5 ms 更长，却可以分别利用三个计算空闲时段完成传输。通信完成的时刻决定它是否延长训练步。
 
 **误区：异步保存调用返回，就能恢复全部已有进度。** 50 s 故障时，可用恢复点取决于哪份快照已经提交。相同的 1 s 前台暂停可以对应 30 s 或 10 s 重做，区别来自后台写入的完成时刻。
 
@@ -750,7 +750,7 @@
 
 [^checkpoint]: [检查点布局、ByteCheckpoint 与后台保存阅读](../case-studies/checkpoint-layout-and-loading.md)、[逻辑重分片计算](../calculations/results/checkpoint-reshard-book.md)、[112 GB 时间线](../calculations/results/checkpoint-async-rounded.md)。
 
-[^resume]: [DCP 恢复记录](../experiments/ch10/10-06/README.md)、[真实文本管线恢复](../experiments/ch10/10-06/input-state-recovery/README.md)。 2,000＋6,192 是解释打包残留的教学例。DCP 小模型将两份行分片恢复为三份列分片与完整状态，参数、Adam、随机状态和下一次参数更新结果与不中断训练一致。
+[^resume]: [DCP 恢复记录](../experiments/ch10/10-06/README.md)、[真实文本管线恢复](../experiments/ch10/10-06/input-state-recovery/README.md)。 2,000＋6,192 是解释打包时剩余的 token的教学例。DCP 小模型将两份行分片恢复为三份列分片与完整状态，参数、Adam、随机状态和下一次参数更新结果与不中断训练一致。
 
 [^fault]: [异步保存与提交前故障实验](../experiments/ch10/10-07/README.md)及[无保存／同步／异步对照](../experiments/ch10/10-07/save-baseline/README.md)。 原正常路径 API 为约 7.25 ms、元数据提交约 48.30 ms；故障实验在提交前设置屏障并终止进程。
 
@@ -760,7 +760,7 @@
 
 [^handoff]: [Qwen3-8B 共享设备峰值](../calculations/results/weight-handoff-qwen8.md)、[Qwen3-235B-A22B 分片权重同步](../calculations/results/weight-handoff-book.md)。 40 GiB 为分片／卸载后显存占用的教学输入；两个切换峰值精确约为 83.256／59.256 GiB。
 
-[^opd]: [DeepSeek V4 技术报告](../references/files/papers/deepseek-v4.pdf)，§5.1 的领域专家与多教师在策略蒸馏；阶段工作划分参见[第三章](03-推理与训练负载.md)。
+[^opd]: [DeepSeek V4 技术报告](../references/files/papers/deepseek-v4.pdf)，§5.1 的领域专家与多教师同策略蒸馏；阶段工作划分参见[第三章](03-推理与训练负载.md)。
 
 [^identity]: [RL 状态、概率比与版本](../case-studies/rl-state-and-reproducibility.md)、[DeepSeek V4 技术报告](../references/files/papers/deepseek-v4.pdf)。
 
--- a/manuscripts/11-资源调度与运行环境.md
+++ b/manuscripts/11-资源调度与运行环境.md
@@ -117,7 +117,7 @@
 
 *图 11-5：代码任务经过模型服务与工具环境两条路径。控制器从模型取得参数，再向环境平台发起工具操作；执行结果成为下一轮输入。虚线框标出工具环境所在的执行节点。*
 
-进程通常共享同一操作系统内核，通过地址空间和权限进行基本隔离。容器增加命名空间（让进程看到各自的进程编号、网络等资源视图）、文件系统视图和资源控制，但通常仍共享宿主内核。完整虚拟机具有客体内核（在虚拟机内部运行的操作系统内核）和虚拟硬件边界；微型虚拟机（microVM）保留虚拟机隔离模型，通过精简虚拟设备和管理功能来降低开销。沙箱是对执行约束的描述，不专指某一种实现。
+进程通常共享同一操作系统内核，通过地址空间和权限进行基本隔离。容器增加命名空间（让进程看到各自的进程编号、网络等资源视图）、文件系统视图和资源控制，但通常仍共享宿主内核。完整虚拟机具有虚拟机内核（在虚拟机内部运行的操作系统内核）和虚拟硬件边界；微型虚拟机（microVM）保留虚拟机隔离模型，通过精简虚拟设备和管理功能来降低开销。沙箱是对执行约束的描述，不专指某一种实现。
 
 ![进程拥有独立地址空间，通过权限限制访问；同一节点上的进程共享宿主内核](ch11/figure-11-isolation-0.svg)
 
@@ -127,29 +127,29 @@
 
 *图 11-7：容器为任务建立各自的资源视图和配额，底层仍使用同一宿主内核。*
 
-![microVM 让每个任务环境运行独立客体内核，通过虚拟硬件访问宿主资源](ch11/figure-11-isolation-2.svg)
-
-*图 11-8：microVM 让每个任务环境运行独立客体内核，通过虚拟硬件访问宿主资源。*
-
-E2B 是面向工具执行的环境平台，Firecracker 是亚马逊云服务（Amazon Web Services，AWS）为无服务器计算开发、于 2018 年开源的轻量虚拟机监控器；其 2020 年 NSDI 论文讨论了如何在大量短时任务之间兼顾硬件虚拟化隔离、启动速度和内存开销。[^firecracker-history]E2B 使用 Firecracker microVM 运行工具环境，模板可由容器镜像构建。镜像提供文件与依赖，microVM 提供运行时的客体内核和设备边界。创建时，平台从模板建立环境；运行时，工具通过客体内核读写文件和启动进程。容器镜像决定环境中有哪些文件和依赖，microVM 则决定这些程序运行时如何与宿主系统隔离。[^e2b]
+![microVM 让每个任务环境运行独立虚拟机内核，通过虚拟硬件访问宿主资源](ch11/figure-11-isolation-2.svg)
+
+*图 11-8：microVM 让每个任务环境运行独立虚拟机内核，通过虚拟硬件访问宿主资源。*
+
+E2B 是面向工具执行的环境平台，Firecracker 是亚马逊云服务（Amazon Web Services，AWS）为无服务器计算开发、于 2018 年开源的轻量虚拟机监控器；其 2020 年 NSDI 论文讨论了如何在大量短时任务之间兼顾硬件虚拟化隔离、启动速度和内存开销。[^firecracker-history]E2B 使用 Firecracker microVM 运行工具环境，模板可由容器镜像构建。镜像提供文件与依赖，microVM 提供运行时的虚拟机内核和设备边界。创建时，平台从模板建立环境；运行时，工具通过虚拟机内核读写文件和启动进程。容器镜像决定环境中有哪些文件和依赖，microVM 则决定这些程序运行时如何与宿主系统隔离。[^e2b]
 
 模型根据上下文选择动作，执行器在已经配置好权限与资源的环境中运行工具。两条路径在同一个任务控制器处汇合：它知道模型调用何时开始、工具何时使用环境，以及哪些文件需要跨轮保存。这些信息让平台能够分别安排模型状态与工具状态的生命周期。模型计算期间可以准备下一轮环境，工具完成后可以保存文件并释放进程；后文将据此计算容量与时间开销，选择环境的创建、保留和恢复方式。
 
 ### 11.2.2 资源分配与环境创建
 
-确定隔离方式以后，平台还需为环境分配资源并装入程序所需的数据。工具平台通过 CPU 配额、内存上限以及文件和设备访问权限来分配资源。给一个等待模型的环境增加 CPU 配额，其文件和内存内容保持原样；销毁这个环境，则会释放内存，同时要求下一次执行前重建这些内容。CUDA 是 NVIDIA GPU 的编程与运行平台；验证 CUDA 程序还需向设备池申请 GPU。设备直通让客体直接操作分配给它的物理设备，可由虚拟机程序 QEMU 配合 Linux 的 VFIO 设备访问接口完成。[^ub]
-
-创建环境需要取得模板、建立私有状态并准备首次工作所需的数据。最简单的方式是完整复制模板；另一种方式是共享只读内容，仅复制需要写入的页面；还可以按访问需求装入数据。创建时少加载一些数据，可以缩短启动时间，但程序首次访问这些数据时仍需等待加载。
+确定隔离方式以后，平台还需为环境分配资源并加载程序所需的数据。工具平台通过 CPU 配额、内存上限以及文件和设备访问权限来分配资源。给一个等待模型的环境增加 CPU 配额，其文件和内存内容保持原样；销毁这个环境，则会释放内存，同时要求下一次执行前重建这些内容。CUDA 是 NVIDIA GPU 的编程与运行平台；验证 CUDA 程序还需向设备池申请 GPU。设备直通让虚拟机直接操作分配给它的物理设备，可由虚拟机程序 QEMU 配合 Linux 的 VFIO 设备访问接口完成。[^ub]
+
+创建环境需要取得模板、建立私有状态并准备首次工作所需的数据。最简单的方式是完整复制模板；另一种方式是共享只读内容，仅复制需要写入的页面；还可以按访问需求加载数据。创建时少加载一些数据，可以缩短启动时间，但程序首次访问这些数据时仍需等待加载。
 
 ![模板保存共享的文件与依赖，私有内容保存环境修改；程序运行时还会占用进程内存和缓冲区](ch11/figure-11-template-runtime.svg)
 
-*图 11-9：模板保存共享的文件与依赖，私有内容保存环境修改；程序运行时还会占用进程内存和缓冲区。模板安装所需空间与活跃环境的内存占用分别计算。*
-
-**例 11-2：按需加载与只读共享能节省多少环境内存？** 为分析创建开销，先取章首 300 个环境中的 100 个，比较将模板内容装入这 100 个环境的内存需求。每份模板为 2 GiB，工具访问其中 512 MiB，修改 128 MiB，另有 4 MiB 私有管理开销。逐份完整复制时，每个环境需占用 2,052 MiB 本地内存，100 份约为 200 GiB；只安装访问到的内容时，每份为 516 MiB，总量约为 50 GiB。这样，同样一组工具任务所需安装的数据减少了约 150 GiB。[^lifecycle]
-
-还可以让环境共享只读内容，仅保留 128 MiB 脏页（相对于模板已经被写入修改的内存页）和 4 MiB 管理开销。此时每份为 132 MiB，100 份约为 13 GiB。未装入本地的只读页在访问时取得。若每个环境再保留 256 MiB 热点只读页，本地总量回升到约 38 GiB，换来较少的远端访问。
-
-四种安装方式依次占用约 200、50、13 和 38 GiB，并另保留一份共享的 2 GiB 模板。这组计算比较了不同数据加载方式的容量开销。章首为每个活跃环境分配的 2 GiB 内存，还包括进程、缓冲区等工具运行所需的空间。共享只读内容可以减少重复存储，将常用数据保留在本地则可以减少远端读取。具体保留多少，应根据工具首次访问哪些页，以及以后多久会再次访问这些页来决定。
+*图 11-9：模板保存共享的文件与依赖，私有内容保存环境修改；程序运行时还会占用进程内存和缓冲区。加载模板所需的空间与活跃环境的内存占用分别计算。*
+
+**例 11-2：按需加载与只读共享能节省多少环境内存？** 为分析创建开销，先取章首 300 个环境中的 100 个，比较将模板内容加载到这 100 个环境中所需的内存。每份模板为 2 GiB，工具访问其中 512 MiB，修改 128 MiB，另有 4 MiB 私有管理开销。逐份完整复制时，每个环境需占用 2,052 MiB 本地内存，100 份约为 200 GiB；只加载访问到的内容时，每份为 516 MiB，总量约为 50 GiB。这样，同样一组工具任务需要加载到内存的数据减少了约 150 GiB。[^lifecycle]
+
+还可以让环境共享只读内容，仅保留 128 MiB 脏页（相对于模板已经被写入修改的内存页）和 4 MiB 管理开销。此时每份为 132 MiB，100 份约为 13 GiB。尚未加载到本地内存的只读页在访问时取得。若每个环境再保留 256 MiB 热点只读页，本地总量回升到约 38 GiB，换来较少的远端访问。
+
+四种加载方式依次占用约 200、50、13 和 38 GiB，并另保留一份共享的 2 GiB 模板。这组计算比较了不同数据加载方式的容量开销。章首为每个活跃环境分配的 2 GiB 内存，还包括进程、缓冲区等工具运行所需的空间。共享只读内容可以减少重复存储，将常用数据保留在本地则可以减少远端读取。具体保留多少，应根据工具首次访问哪些页，以及以后多久会再次访问这些页来决定。
 
 图 11-10 把每个环境本地保存的内容展开。共享方案的主要收益来自省去重复的只读内容；把常用页放回本地，则是在容量与读取延迟之间折中。接下来还要计算：少保存这些数据，是否会让工具在首次读取时等待更久。
 
@@ -157,9 +157,9 @@
 
 *图 11-10：四种方案采用相同的横轴尺度，比较每个环境本地保存的数据。另有一份共享的 2 GiB 模板，不计入各环境的柱长。数值来自例 11-2；管理开销为每环境 4 MiB。*
 
-设取回与安装都经过同一条有效速率为 1 GiB/s 的串行通路。模板尚未缓存在本机时，需要取回 2 GiB，再安装 0.5 GiB，数据传输与安装时间至少为 2.5 秒；模板已在缓存中时，只需安装 0.5 GiB，降至 0.5 秒。两秒差额来自省去的模板传输。一般地，这条通路的传输与安装时间下界为 $(V_{\mathrm{fetch}}+V_{\mathrm{install}})/B$。
-
-如果工具首次执行时又访问 1 GiB 尚未装入的页，这条通路便再工作 1 秒。按需加载使创建接口更早返回，将尚未访问的数据留到真正需要时取得。E2B 的页与文件读取采用了这样的组织：启动过程先提供可运行的环境，随后的访问逐步补齐内容。首次工具执行的完成时间包含环境启动，以及首次访问缺失页时的等待。按需加载将一部分准备工作推迟到了工具执行期间。[^e2b]
+设取回与加载到内存都经过同一条有效速率为 1 GiB/s 的串行通路。模板尚未缓存在本机时，需要取回 2 GiB，再将 0.5 GiB 数据加载到内存，数据传输与加载到内存的时间至少为 2.5 秒；模板已在缓存中时，只需将 0.5 GiB 数据加载到内存，降至 0.5 秒。两秒差额来自省去的模板传输。一般地，这条通路的传输与加载到内存的时间下界为 $(V_{\mathrm{fetch}}+V_{\mathrm{install}})/B$。
+
+如果工具首次执行时又访问 1 GiB 尚未加载到内存的页，这条通路便再工作 1 秒。按需加载使创建接口更早返回，将尚未访问的数据留到真正需要时取得。E2B 的页与文件读取采用了这样的组织：启动过程先提供可运行的环境，随后的访问逐步补齐内容。首次工具执行的完成时间包含环境启动，以及首次访问缺失页时的等待。按需加载将一部分准备工作推迟到了工具执行期间。[^e2b]
 
 ### 11.2.3 暂停、恢复与环境重建
 
@@ -228,9 +228,9 @@
 
 *图 11-17：预测正确且提前 2 s：准备恰好在第 4 s 调用到达时完成。*
 
-![预测正确且提前 3 s：准备在第 3 s 完成，橙色部分为等待调用的 1 s 就绪空闲](ch11/figure-11-prewarm-3.svg)
-
-*图 11-18：预测正确且提前 3 s：准备在第 3 s 完成，橙色部分为等待调用的 1 s 就绪空闲。*
+![预测正确且提前 3 s：准备在第 3 s 完成，橙色部分为等待调用的 1 s 已就绪但尚未使用的时间](ch11/figure-11-prewarm-3.svg)
+
+*图 11-18：预测正确且提前 3 s：准备在第 3 s 完成，橙色部分为等待调用的 1 s 已就绪但尚未使用的时间。*
 
 ![预测错误：第 2—4 s 准备了不需要的环境（灰色）；第 4 s 取消后，再花 2 s 准备实际所需环境](ch11/figure-11-prewarm-wrong.svg)
 
@@ -328,7 +328,7 @@
 T_{\mathrm{all}}\geq\max\left(4.8,\frac{6\times30}{200/8}\right)=7.2\ \mathrm{s}.
 $$
 
-六个实例全部收到权重，至少需要 7.2 秒；第一个实例可以更早收到自己的 30 GB。随后接收端把权重装入 GPU 并确认版本，调度器再向它分配请求。PolyRL 是利用临时资源扩展 RL 生成池的系统。在这里，它先将完整权重接收到 CPU 缓冲区，再交给采用张量并行（TP，将层内矩阵分给多张卡）的 GPU 组。因此，TP=2 将实例内部的计算分配到两张卡上，每个实例从外部接收的权重仍为 30 GB。
+六个实例全部收到权重，至少需要 7.2 秒；第一个实例可以更早收到自己的 30 GB。随后接收端把权重加载到 GPU 显存 并确认版本，调度器再向它分配请求。PolyRL 是利用临时资源扩展 RL 生成池的系统。在这里，它先将完整权重接收到 CPU 缓冲区，再交给采用张量并行（TP，将层内矩阵分给多张卡）的 GPU 组。因此，TP=2 将实例内部的计算分配到两张卡上，每个实例从外部接收的权重仍为 30 GB。
 
 图 11-25 标出六份权重共同经过的出口。接收实例增加时，需要发送的总字节数也增加，发送端带宽却没有随之增加。权重传完后还要加载到 GPU，这些准备时间都要从临时实例可用的时间中扣除。
 
@@ -488,7 +488,7 @@
 
 *图 11-33：自建总成本为 1,000＋0.002N，API 为 0.012N，其中 N 为提交任务数。两者成功率相同，并有足够能力满足期限。自建曲线起点较高、斜率较小，在 100,000 项时与 API 相交。*
 
-执行效率提高后，自建服务开始更省的任务量也会变化。以一组教学估算为例：用 4 张 B200 运行 DeepSeek V4-Flash，同时处理 32 项任务，每项保留 200K token 的上下文。设并发任务数和设备租价保持不变。每个输出 token 约需 25.3 ms，其中逐 token 解码生成（decode）15.9 ms，其余输入处理和上下文重建 9.4 ms。净 decode 加速两倍后，每个输出 token 所需时间降为 $15.9/2+9.4\approx17.4$ ms，总加速约 1.46 倍，每百万输出成本由约 6.0 降到 4.1。[^purchase]
+执行效率提高后，自建服务开始更省的任务量也会变化。以一组教学估算为例：用 4 张 B200 运行 DeepSeek V4-Flash，同时处理 32 项任务，每项保留 200K token 的上下文。设并发任务数和设备租价保持不变。每个输出 token 约需 25.3 ms，其中逐 token 解码生成（decode）15.9 ms，其余输入处理和上下文重建 9.4 ms。decode 阶段的实际速度提高到两倍后，每个输出 token 所需时间降为 $15.9/2+9.4\approx17.4$ ms，总加速约 1.46 倍，每百万输出成本由约 6.0 降到 4.1。[^purchase]
 
 原先每个输出 token 所需时间中，约 63% 用于 decode，其余 37% 保持不变，因而两倍 decode 加速只能将总时间缩短约 31%。即使把 decode 时间降到零，其余处理仍需要 9.4 ms。进一步降低成本应转向输入处理和上下文重建；同一组设备在单位时间内生成更多 token，固定租价才能由更多产出分摊。
 
@@ -504,7 +504,7 @@
 
 ![外部系统已执行并提交操作 K，确认却丢失](ch11/figure-11-commit-ack.svg)
 
-*图 11-34：外部系统已执行并提交操作 K，确认却丢失。恢复环境不会撤回外部效果；控制器按同一操作 ID 查询结果后接续任务。实线表示请求与执行，虚线表示确认及恢复查询。*
+*图 11-34：外部系统已执行并提交操作 K，确认却丢失。恢复环境不会撤回外部操作的结果；控制器按同一操作 ID 查询结果后接续任务。实线表示请求与执行，虚线表示确认及恢复查询。*
 
 假设执行一次工具需要 3 CPU·秒，结果被发送两次。如果接收端按操作标识去重，累计 CPU 时间仍为 3 秒；如果控制器因确认丢失而真的再执行一次，累计 CPU 时间就增至 6 秒。两种情况最后都可能只有一条结果，却消耗不同资源。消息去重解决前一种重复，持久结果和目标系统的幂等协议才有助于避免后一种重复。环境外已经提交的操作在快照恢复后仍然存在，恢复流程要先查询其提交状态。[^records]
 
@@ -624,11 +624,11 @@
 
 [^environment]: 本书 [environment-resources 固定计算](../calculations/results/environment-resources-book.md)及[工具子进程记录](../experiments/ch11/11-01/process-resources/README.md)。工具实验包含九组、36 个进程，累计内存占用由采样 RSS 对时间积分得到。
 
-[^lifecycle]: [environment-lifecycle 固定预算](../calculations/results/environment-lifecycle-default.md)区分共享／私有容量、串行字节下界和局部预热实测。本章使用固定容量预算比较四种内容安装方式。
+[^lifecycle]: [environment-lifecycle 固定预算](../calculations/results/environment-lifecycle-default.md)区分共享／私有容量、串行字节下界和局部预热实测。本章使用固定容量预算比较四种内容加载方式。
 
 [^platform]: [调度、模型路由与云端环境研究](../case-studies/platform-routing.md)，包含 ASI、RLBoost、DistRS、SpecBox。ASI 研究覆盖六个月、155,410 张 GPU，其分配比例度量设备归属。计费公式采用题设价格。
 
-[^e2b]: [E2B 固定架构](../references/outline-checks/2026-09-07/platform-routing/e2b-architecture.md)、[暂停与快照契约核对](../experiments/ch11/11-02/README.md)。实现采用 Firecracker；保存文件／内存与重建连接分别说明。
+[^e2b]: [E2B 固定架构](../references/outline-checks/2026-09-07/platform-routing/e2b-architecture.md)、[暂停与快照接口行为核对](../experiments/ch11/11-02/README.md)。实现采用 Firecracker；保存文件／内存与重建连接分别说明。
 
 [^ub]: [UB 操作系统参考设计](../references/files/specs/UB-Software-Reference-Design-for-OS-2.0-zh.pdf)中的设备虚拟化与交付路径，作为 QEMU／VFIO 的实现例证；这里描述支持设备直通的虚拟机配置。
 
@@ -654,7 +654,7 @@
 
 [^retry]: 精确结果：成功概率 0.99744，通过测试且按时完成概率 0.9504，每项期望成本 0.01456，累计 CPU 时间 3.376 秒，驻留 23.008 GiB·秒。计算结果分别列出资源用量和成本；各节点的给定成本已经是总价，无需再按资源用量重复计费。见[retry-paths 有限条件图](../calculations/results/retry-paths-book.md)。各节点概率与成本按题设给定，重试沿有限执行树展开；超过评价期限的任务仍继续执行到终点。
 
-[^design]: 贯穿平台与例 11-9 是确定性教学设计，输入、逐阶段时间表、成本、成本相等的条件与复算见[贯穿设计数据](ch11/platform-design.json)及[验证程序](ch11/verify.py)。40 核、640 GiB 内存、最多 300 次并发模型调用、两种服务时间／价格和 95% 质量概率为题设。完整环境准备含文件取得与初始化共 2 秒、0.1 CPU·秒，结果保存与工作状态清理包含在工具的 1 秒内；原模板容量算例单独说明内容安装。
+[^design]: 贯穿平台与例 11-9 是确定性教学设计，输入、逐阶段时间表、成本、成本相等的条件与复算见[贯穿设计数据](ch11/platform-design.json)及[验证程序](ch11/verify.py)。40 核、640 GiB 内存、最多 300 次并发模型调用、两种服务时间／价格和 95% 质量概率为题设。完整环境准备含文件取得与初始化共 2 秒、0.1 CPU·秒，结果保存与工作状态清理包含在工具的 1 秒内；原模板容量算例单独说明内容加载。
 
 [^firecracker-history]: Agache 等，AWS，[Firecracker: Lightweight Virtualization for Serverless Applications](../references/text/firecracker.txt)，NSDI 2020。
 
--- a/manuscripts/12-端边云协同.md
+++ b/manuscripts/12-端边云协同.md
@@ -97,7 +97,7 @@
 p_i=\max(r_i,p_{i-1}+\tau).
 $$
 
-这里取最大值，是因为播放器既要等待“本块到达”，也要等待“上一块播完”。当第一项更大时，差额就是新增停顿。把初始缓冲从 40 ms 增至 45 ms，第三块的计划播放时刻也推迟到 123 ms，恰好吸收晚到的 5 ms；代价是首段同样晚开始 5 ms。增加初始缓冲会延长首次播放前的等待，但也能吸收更大的到达时间波动，使后续播放更连续。
+这里取最大值，是因为播放器既要等待“本块到达”，也要等待“上一块播完”。当第一项更大时，差额就是新增停顿。把初始缓冲从 40 ms 增至 45 ms，第三块的计划播放时刻也推迟到 123 ms，恰好避免这 5 ms 的晚到造成播放中断；代价是首段同样晚开始 5 ms。增加初始缓冲会延长首次播放前的等待，但也能容忍更大的到达时间波动，使后续播放更连续。
 
 长期传输速率不足会产生另一种停顿。为便于计算，取 16 kHz、单声道、16-bit PCM，播放所需的数据速率为 256 kbit/s，20 ms 对应 640 bytes。若网络长期只能以 130 kbit/s 传输音频，缓冲每秒净减少 126 kbit。初始缓冲保存的 60 ms 音频包含 15,360 bits，约 $15360/(256000-130000)\approx0.12$ 秒便会耗尽。
 
@@ -107,7 +107,7 @@
 
 *图 12-7：缓冲量等于初始数据量加累计接收量，再减累计播放量。PCM 播放需要 256 kbit/s，接收只有 130 kbit/s，两条直线的下降速率均为 126 kbit/s。初始缓冲分别包含 60 ms 和 120 ms 音频，约在 0.12 秒和 0.24 秒耗尽；图画到各自第一次耗尽为止。*
 
-增大缓冲只会按比例延后耗尽；提高传输速率或降低音频码率，才能让收到的数据足够播放。短时抖动可以靠缓冲吸收，长期传输速率不足则需要增加带宽或减少数据量。
+增大缓冲只会按比例延后耗尽；提高传输速率或降低音频码率，才能让收到的数据足够播放。缓冲可以减轻短时延迟波动对播放的影响，长期传输速率不足则需要增加带宽或减少数据量。
 
 ### 12.1.3 Computer Use：通过界面操作完成任务
 
@@ -173,7 +173,7 @@
 
 *图 12-10：端侧编码的执行路径。预处理和视觉编码移到终端，跨网络的对象变为最终投影与三组 DeepStack 的完整 BF16 特征，共 8.192 MB；远端接收后仍按相同方式接入语言模型。相同上行的特征发送时间为 10.24 秒，本地编码、序列化和转换时间另计。图中数值采用下文的 Qwen3-VL-4B 固定配置。*
 
-**例：上传视觉特征为何可能比上传原图更慢？** 沿用 0.8 MB 截图与 6.4 Mbit/s 上行。视觉编码采用 Qwen3-VL-4B 的固定配置：预处理后 $640\times640$，图像块合并后得到 400 个视觉位置，即用来表示图片内容的 400 个特征向量；这里的“位置”不是文本中的词元，也不是原图的单个像素。最终投影的每个特征向量有 2560 个分量，另有三组同宽 DeepStack 特征。DeepStack 将视觉编码器不同深度的特征送入语言模型不同层，因而要一起传送这些分支输出。BF16 是每个数占 2 字节的浮点格式。每个位置共需保存 $4\times2560=10240$ 个 BF16 数，完整输出为
+**例：上传视觉特征为何可能比上传原图更慢？** 沿用 0.8 MB 截图与 6.4 Mbit/s 上行。视觉编码采用 Qwen3-VL-4B 的固定配置：预处理后 $640\times640$，图像块合并后得到 400 个视觉位置，即用来表示图片内容的 400 个特征向量；这里的“位置”不是文本中的 token，也不是原图的单个像素。最终投影的每个特征向量有 2560 个分量，另有三组同宽 DeepStack 特征。DeepStack 将视觉编码器不同深度的特征送入语言模型不同层，因而要一起传送这些分支输出。BF16 是每个数占 2 字节的浮点格式。每个位置共需保存 $4\times2560=10240$ 个 BF16 数，完整输出为
 
 $$
 S_E=400\times10240\times2=8\,192\,000\ \mathrm{bytes}
@@ -202,7 +202,7 @@
 
 ![三种保存对象对应三个恢复计算的起点](ch12/figure-12-cache-restart.svg)
 
-*图 12-12：三种保存对象对应三个恢复计算的起点。EC 省去视觉编码；匹配模型权重、前缀与位置的 KV 进一步省去已处理前缀的语言计算。容量对应正文固定视觉配置。*
+*图 12-12：三种保存对象对应三个恢复计算的起点。EC 省去视觉编码；匹配模型权重、前缀与位置的 KV 进一步省去已处理前缀的语言模型计算。容量对应正文固定视觉配置。*
 
 ### 12.2.3 会话迁移的准备、复用与恢复
 
@@ -232,7 +232,7 @@
 
 例如，恢复再多花 1 秒，20 轮的净收益立即变成约负 0.7 秒，回本轮数增至 22。若预计后续还有 40 轮，新增这 1 秒之后仍可省约 7.3 秒。部署系统因此需要预测剩余会话长度，并为额外的传输和恢复开销留出时间。
 
-曲线中的“恢复时间”包括新设备继续执行前所需的准备工作。除了装入缓存，它还要知道任务已经执行到哪一步。语言 KV 绑定模型、前缀和位置；截图 Agent 的进度由已经执行的操作决定。一次点击已经改变界面，即使其应答丢失，也应恢复到点击后的状态。操作编号和提交记录使目的端可以查到既有结果，从下一轮继续。保存这些记录可以避免重复执行操作，就像保存缓存可以避免重复计算。
+曲线中的“恢复时间”包括新设备继续执行前所需的准备工作。除了将缓存数据加载到内存，它还要知道任务已经执行到哪一步。语言 KV 绑定模型、前缀和位置；截图 Agent 的进度由已经执行的操作决定。一次点击已经改变界面，即使其应答丢失，也应恢复到点击后的状态。操作编号和提交记录使目的端可以查到既有结果，从下一轮继续。保存这些记录可以避免重复执行操作，就像保存缓存可以避免重复计算。
 
 连接预热、图编译和模型加载也可用 $N\Delta t-M$ 分析：将准备成本放入 $M$，将后续每次减少的等待放入 $\Delta t$。[^survey] 这些准备是否值得，取决于后续调用省下的时间能否抵消一次性的准备时间。
 
@@ -276,7 +276,7 @@
 
 ### 12.3.2 窗口、反馈与有效吞吐
 
-复用连接省去了建链等待，连续发送数据还需要足够的发送窗口。连接建立以后，发送方也必须限制在途数据。拥塞窗口限制已经发送但尚未确认的数据量；接收流量控制根据接收端的缓冲空间，限制发送方还能发送多少数据。确认消息（ACK）告知发送方哪些数据已收到。ACK 到达后，发送方可以继续发送；接收端从缓冲区读取数据后，也会通知发送方新增的可用空间。达到发送上限时，即使链路空闲，发送方仍要等待这些通知。
+复用连接省去了建链等待，连续发送数据还需要足够的发送窗口。连接建立以后，发送方也必须限制在途数据。拥塞窗口限制已经发送但尚未确认的数据量；接收端流量控制根据接收端的缓冲空间，限制发送方还能发送多少数据。确认消息（ACK）告知发送方哪些数据已收到。ACK 到达后，发送方可以继续发送；接收端从缓冲区读取数据后，也会通知发送方新增的可用空间。达到发送上限时，即使链路空闲，发送方仍要等待这些通知。
 
 在途量是已经发出、尚未得到确认的数据字节数。若允许在途量为 $W$，往返时间为 $R$，每个往返周期最多传送约一个窗口的数据，因此
 
@@ -401,7 +401,7 @@
 
 保持数据与恢复轨迹不变，将端到端 ACK 数减半，能释放约 $15400\times134\ \mathrm{\mu s}\approx2.1$ 秒空口。上一节已经说明，反馈延迟又会增加窗口停等。因此，需要重新计算各包的发送与确认时刻：减少 ACK 腾出了空口时间，但等待确认又可能推迟后续发送。两者共同决定完整请求何时结束。
 
-TACK 是减少无线确认开销的一种反馈机制。它将日常累计确认与需要即时反馈的事件分开：前者通过合并来减少交换次数，后者及时返回，使发送方尽快继续发送或重传丢失的数据。[^wireless-survey] 这种分工同时减少确认报文的发送开销和等待反馈的时间。
+TACK 是减少无线确认开销的一种反馈机制。它将常规的累积确认与需要即时反馈的事件分开：前者通过合并来减少交换次数，后者及时返回，使发送方尽快继续发送或重传丢失的数据。[^wireless-survey] 这种分工同时减少确认报文的发送开销和等待反馈的时间。
 
 ### 12.4.2 业务时限、过期数据与取消
 
