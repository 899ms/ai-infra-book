--- before/02-模型架构.md
+++ manuscripts/02-模型架构.md
@@ -4,17 +4,17 @@
 
 本章从一个 token 的数值表示开始，逐步分析它经过网络各层后，如何得到下一个 token 的分数，再计算这一过程所需的存储容量 $M$、运算量 $F$ 和读写量 $R$。计算图用节点表示运算，用边表示运算之间传递的数据。先认识图上的对象，才能知道资源预算中的每个数字来自哪里。
 
-这张计算图也提供了优化实现所需的信息。哪些权重在不同请求间保持不变，哪些状态会被后续生成反复使用，哪些结果只交给紧接着的运算，都可以从模型结构中找到。后面的存储、编译和调度设计将利用这些区别，决定哪些数据可以共享、哪些需要长期保存、哪些只需临时缓冲。
-
-我们使用 Qwen3-8B 作为贯穿例子。这是一个约含 81.9 亿参数的语言模型，通过 36 层网络处理文本。先完整理解这一种模型，再改变上下文的保存方式、每个 token 使用的参数集合和层间连接，比较 Qwen3.6-35B-A3B（下文简称 Qwen3.6）、DeepSeek V4-Flash 与 Kimi K3 的资源需求。
-
-计算运算量时，一次乘法加一次加法计为 2 FLOPs。存储量采用 GB 和 GiB：$1\ \mathrm{GB}=10^9\ \mathrm{bytes}$，$1\ \mathrm{GiB}=2^{30}\ \mathrm{bytes}$。BF16 是每个数占 16 位的浮点表示，每个元素占 2 字节，因此将张量的元素数乘以 2，就得到它的存储大小。
+这张计算图也提供了优化实现所需的信息。哪些权重在不同请求间保持不变，哪些状态会被后续生成反复使用，哪些结果只供紧接着的运算使用，都可以从模型结构中找到。后面的存储、编译和调度设计将利用这些数据在用途上的区别，决定哪些数据可以共享、哪些需要长期保存、哪些只需暂存。
+
+我们使用 Qwen3-8B 作为贯穿例子。这是一个约含 81.9 亿参数的语言模型，通过 36 层网络处理文本。先完整地理解这个模型，再改变上下文的保存方式、每个 token 使用的参数集合和层间连接，比较 Qwen3.6-35B-A3B（下文简称 Qwen3.6）、DeepSeek V4-Flash 与 Kimi K3 的资源需求。
+
+统计运算量时，一次乘法加一次加法计为 2 FLOPs。存储量采用 GB 和 GiB：$1\ \mathrm{GB}=10^9\ \mathrm{bytes}$，$1\ \mathrm{GiB}=2^{30}\ \mathrm{bytes}$。BF16 是每个数占 16 位的浮点表示，每个元素占 2 字节，因此将张量的元素数乘以 2，就得到它的存储大小。
 
 ## 2.1 计算图与前向执行
 
 张量是按一个或多个维度排列的数值数组：向量只有一个维度，矩阵有两个维度。模型用向量表示每个 token 的特征，把多个位置的向量按行排成矩阵。理解模型资源需求，首先要把计算节点和数据依赖分开。一个节点执行矩阵乘法或其他变换，一条边表示后续节点需要前面的结果。节点的运算量决定计算需求，边上的张量决定传递的数据量，边的方向则表明哪些运算必须先后执行。理解模型的执行过程，需要同时考虑这三个方面。
 
-本章的计算图先描述数学运算和数据依赖。实际执行时，框架会把这些运算交给设备程序；一个运算可能拆成多个程序，多个运算也可能合在一起执行。图上的一条张量边只说明后续需要这份数据，不表示它一定要在 CPU 与 GPU 之间复制。第四章说明数据放在哪里，第五章再解释程序怎样提交、数据怎样传入传出。
+本章的计算图先描述数学运算和数据依赖。实际执行时，框架会把这些运算交给设备程序；一个运算可能拆成多个程序，多个运算也可能合在一起执行。计算图中表示张量的一条边，只说明后续运算需要这份数据，不表示它一定要在 CPU 与 GPU 之间复制。第四章说明数据放在哪里，第五章再解释程序怎样提交、数据怎样传入传出。
 
 ### 2.1.1 序列依赖与因果计算图
 
@@ -34,13 +34,13 @@
 
 编码器把已知输入转换为表示，解码器根据允许访问的信息逐步产生输出。现代因果语言模型常用只含解码器的结构（decoder-only），本章采用这一结构。处理已知输入时，同层位置可以并行；生成时，第一个输出确定后才能成为下一次输入。因此，输入处理沿网络深度推进，生成还增加了跨调用的串行依赖。[^source-10]
 
-> **练习 2-1〔延伸〕：从依赖图判断并行**
+> **练习 2-1〔延伸〕：RNN 与 Transformer 的依赖如何限制并行计算**
 >
 > 对于含四个已知输入位置的序列，分别画出三层 RNN 和三层因果 Transformer 的计算依赖图。再增加一个待生成的输出位置，指出计算该位置之前必须等待哪个节点完成。设每层每位置的计算耗时为一个单位、并行资源充足，分别求完成已知输入的最长依赖链长度；解释增加计算单元可以缩短哪些等待、不能缩短哪些等待。
 
 ### 2.1.2 模型配置与张量结构
 
-先把一个计算节点展开。设本次为 $m$ 个输入 token 计算投影，每个 token 用一个包含 $k$ 个特征的向量表示。把这些向量逐行排成输入矩阵 $X$，它就有 $m$ 行、$k$ 列，输出宽度为 $n$：
+先把一个计算节点展开。设本次对 $m$ 个输入 token 执行投影，每个 token 用一个包含 $k$ 个特征的向量表示。把这些向量逐行排成输入矩阵 $X$，它就有 $m$ 行、$k$ 列，输出宽度为 $n$：
 
 $$
 X\in\mathbb R^{m\times k},\qquad W\in\mathbb R^{k\times n},\qquad Y=XW\in\mathbb R^{m\times n}.
@@ -52,15 +52,15 @@
 N_W=kn,\qquad F=2mkn,\qquad M_W=b_Wkn.
 $$
 
-$N_W$ 是权重参数的数量，$b_W$ 是每个参数占用的字节数。行数 $m$ 翻倍，运算量翻倍，权重大小保持不变；输出宽度 $n$ 翻倍，权重与运算都翻倍。这两个变化分别对应“同一模型处理更多输入”与“模型的一层变得更宽”。下面的模型表都可以用这条规则逐行计算。
-
-先只看 Qwen3-8B 的一条数据路径。一次**前向执行**是使用当前权重从输入算到输出的过程；隐藏向量是网络内部传递的特征表示，其维度给出每个位置保存多少个数。**前馈网络**（feed-forward network，FFN）对每个位置分别做特征变换；**注意力**则在位置之间选择并汇总信息。这两类运算交替组成网络的一层。
+$N_W$ 是权重参数的数量，$b_W$ 是每个参数占用的字节数。行数 $m$ 翻倍，运算量翻倍，权重大小保持不变；输出宽度 $n$ 翻倍，权重数量与运算量都翻倍。这两个变化分别对应“同一模型处理更多输入”与“模型的一层变得更宽”。下面的模型表都可以用这条规则逐行计算。
+
+先只看 Qwen3-8B 的一条数据路径。一次**前向执行**是使用当前权重从输入算到输出的过程；隐藏向量是网络内部传递的特征表示，其维度表示每个位置包含多少个数。**前馈网络**（feed-forward network，FFN）对每个位置分别做特征变换；**注意力**则在位置之间选择并汇总信息。这两类运算交替组成网络的一层。
 
 Qwen3-8B 包含 $L=36$ 层，隐藏维度为 $d=4096$，FFN 中间维度为 $f=12288$；注意力把特征分成多组并分别建立位置间的关系，每一组称为一个头。这里有 32 个查询头和 8 组供查询使用的键值头（KV 头），每头 128 维；2.1.3 将沿查询、键和值的路径解释它们。词表包含 151936 个 token，词嵌入是一张把 token 编号映射为向量的查找表，输出头则把末层向量变成每个候选 token 的分数。两者各自保存一份权重。各层参数与这两份词表矩阵相加，模型共有约 81.9 亿个参数。[^source-1]
 
-一次前向执行从 token ID（词元在词表中的整数编号）开始。词嵌入为每个 ID 查出一个 4096 维向量，向量依次经过 36 层注意力与 FFN，最后经过归一化和词表投影产生 logits，即尚未转换为概率的候选 token 分数。归一化按向量的统计量调整各特征的尺度，保持逐层计算的数值范围。词嵌入按 ID 选择行，一个 token 取出一条向量，整个词表则作为常驻数据供不同 token 查询。词表输出头则将隐藏向量与词表矩阵相乘；只生成下一个 token 时，通常只需要最后一个输入位置的 logits。
-
-设同时处理 $B$ 条等长请求，每条本次输入 $P$ 个已知 token。投影和 FFN 可以把它们的输入表示为 $X\in\mathbb{R}^{BP\times4096}$，每一行是一个 token 在当前层的表示，共有 $m=BP$ 行。已有上下文中从缓存读取的键和值不计入这里的 $m$。每条请求只补入一个 token 时，$m=B$。第一种情形容易形成行数较大的矩阵，第二种在小 batch 下接近矩阵与向量的乘法。权重相同，输入形状却明显不同。
+一次前向执行从 token ID（词元在词表中的整数编号）开始。词嵌入为每个 ID 查出一个 4096 维向量，向量依次经过 36 层注意力与 FFN，最后经过归一化和词表投影产生 logits，即尚未转换为概率的候选 token 分数。归一化按向量的统计量调整各特征的尺度，使逐层计算的数值保持在适当范围内。词嵌入按 ID 选择行，一个 token 取出一条向量，整个词表则作为常驻数据供不同 token 查询。词表输出头则将隐藏向量与词表矩阵相乘；只生成下一个 token 时，通常只需要最后一个输入位置的 logits。
+
+设同时处理 $B$ 条等长请求，每条本次输入 $P$ 个已知 token。投影和 FFN 可以把它们的输入表示为 $X\in\mathbb{R}^{BP\times4096}$，每一行是一个 token 在当前层的表示，共有 $m=BP$ 行。已有上下文中从缓存读取的键和值所对应的位置，不计入这里的 $m$。每条请求只补入一个 token 时，$m=B$。第一种情形容易形成行数较大的矩阵，第二种在小 batch 下接近矩阵与向量的乘法。权重相同，输入形状却明显不同。
 
 下面比较两种输入形状：一条请求一次处理 8192 个输入位置，以及一条已有 8192 个上下文位置的请求再处理一个新位置。用 $B$ 表示请求数，$S$ 表示调用前已有的上下文长度，$P$ 表示本次输入长度，$d$ 表示隐藏维度。前者取 $B=1,S=0,P=8192$，后者取 $B=1,S=8192,P=1$。
 
@@ -93,11 +93,11 @@
 | $m=BP$ | 本次参与投影的 token 总数，也就是输入矩阵行数；普通 decode 时 $P=1$，故 $m=B$ |
 | $d,f$ | 主干隐藏宽度、前馈网络中间宽度 |
 | $n_Q,n_{\mathrm{KV}},d_h$ | 查询头数、KV 头数、每头维度 |
-| $N_{\mathrm{pair}}$ | 每层跨全部请求的有效因果位置对数，不含头数 |
-| $t_e$ | 分派给专家 $e$ 的 token 数，每个 token 表示占该专家输入矩阵的一行；无丢弃路由时 $\sum_e t_e=mk_{\mathrm{top}}$ |
+| $N_{\mathrm{pair}}$ | 每层所有请求合计的有效因果位置对数，不含头数 |
+| $t_e$ | 分派给专家 $e$ 的 token 数，每个 token 表示占该专家输入矩阵的一行；路由过程中不丢弃分派给专家的 token 时 $\sum_e t_e=mk_{\mathrm{top}}$ |
 | $m_{\mathrm{out}}$ | 实际进行词表投影的 token 位置数；只用每条请求的末位置预测下一个 token 时为 $B$，它不等于本次生成的全部输出长度 |
 
-计算整模型时，先按表格中每项操作的矩阵尺寸求一次调用的运算量，再乘这一类层的数量。词表头另外按实际输出位置数 $m_{\mathrm{out}}$ 计算；生成下一 token 时，每条请求只需最后一个位置的分数。归一化、激活与查表操作各有自己的运算方式，下面沿数据流说明它们的作用。
+计算整个模型的资源需求时，先按表格中每项操作的矩阵尺寸求一次调用的运算量，再乘这一类层的数量。词表头另外按实际输出位置数 $m_{\mathrm{out}}$ 计算；生成下一 token 时，每条请求只需最后一个位置的分数。归一化、激活与查表操作各有自己的运算方式，下面沿数据流说明它们的作用。
 
 ### 2.1.3 注意力计算与位置表示
 
@@ -109,7 +109,7 @@
 
 采用数学右乘记号，输入 $X$ 分别乘三个权重矩阵，得到 $Q$、$K$、$V$。$Q$ 的总宽度为 $32\times 128=4096$，$K$ 和 $V$ 的总宽度各为 $8\times 128=1024$。因此，$W_q$ 为 $[4096,4096]$，$W_k$ 和 $W_v$ 各为 $[4096,1024]$。代码中的线性层常以 $(d_{\mathrm{out}},d_{\mathrm{in}})$ 保存权重；换成数学右乘记号后，输入宽度在前、输出宽度在后，便可逐项对照下面的表格。
 
-上述投影产生了 $Q$、$K$、$V$，但尚未让当前位置与上下文交换信息。接下来将 $Q$ 拆成 32 个头，把 $K$、$V$ 拆成 8 个头。每组四个 $Q$ 头共用一个 KV 头。对一个 $Q$ 头，查询向量与允许访问位置的 $K$ 做点积，将点积除以头宽的平方根，使分数尺度随头宽保持稳定，然后屏蔽未来位置。Softmax 把允许位置的分数 $s_j$ 变成正数并归一化，系数为 $a_j=e^{s_j}/\sum_i e^{s_i}$；这些系数之和等于一，决定各上下文位置贡献多少信息。这里的“注意力权重”是本次输入算出的系数，区别于训练后保存的模型参数。随后用这些权重对 $V$ 加权求和。各 $Q$ 头的结果拼接后，经 $[4096,4096]$ 的输出投影 $W_o$ 返回隐藏空间。
+上述投影产生了 $Q$、$K$、$V$，但尚未让当前位置与上下文交换信息。接下来将 $Q$ 拆成 32 个头，把 $K$、$V$ 拆成 8 个头。每组四个 $Q$ 头共用一个 KV 头。对一个 $Q$ 头，查询向量与允许访问位置的 $K$ 做点积，将点积除以头宽的平方根，避免分数的数值尺度随头宽增大而过度增长，然后屏蔽未来位置。Softmax 把允许位置的分数 $s_j$ 变成正数并归一化，系数为 $a_j=e^{s_j}/\sum_i e^{s_i}$；这些系数之和等于一，决定各上下文位置贡献多少信息。这里的“注意力权重”是本次输入算出的系数，区别于训练后保存的模型参数。随后用这些权重对 $V$ 加权求和。各 $Q$ 头的结果拼接后，经 $[4096,4096]$ 的输出投影 $W_o$ 转回隐藏向量。
 
 用矩阵记号写出一个头的计算：
 
@@ -123,21 +123,21 @@
 
 *图 2-4　两段上下文形成长方形加三角形。蓝色为三个新位置分别读取两个旧位置，绿色为新输入内部的因果访问，空白为被屏蔽的未来位置。*
 
-现在数清有多少个点积。输入 $P$ 个新位置、已有 $S$ 个上下文位置时，第 $i$ 个新位置可访问 $S+i$ 个位置，其中 $i=1,\ldots,P$。把每个查询允许访问的上下文逐行相加，就得到一个长方形加一个三角形：
+现在数清有多少个点积。输入 $P$ 个新位置、已有 $S$ 个上下文位置时，第 $i$ 个新位置可访问 $S+i$ 个位置，其中 $i=1,\ldots,P$。将各查询可以访问的上下文位置数相加，就得到一个长方形加一个三角形：
 
 $$
 N_{\mathrm{pair}}=B\left(PS+\frac{P(P+1)}{2}\right)
 $$
 
-每个位置对上，一个 $Q$ 头的 $QK^{\mathsf T}$ 点积约需 $2d_h$ FLOPs，随后 $AV$ 的贡献又约需 $2d_h$ FLOPs。合计所有 $Q$ 头，$QK^{\mathsf T}$ 与 $AV$ 的有效矩阵运算量为：
+对于每一对查询与上下文位置，一个 $Q$ 头的 $QK^{\mathsf T}$ 点积约需 $2d_h$ FLOPs，随后 $AV$ 的贡献又约需 $2d_h$ FLOPs。合计所有 $Q$ 头，$QK^{\mathsf T}$ 与 $AV$ 的有效矩阵运算量为：
 
 $$
 F_{\mathrm{attn}}=4n_Qd_hN_{\mathrm{pair}}=16384N_{\mathrm{pair}}
 $$
 
-在没有已有上下文的 prefill 中，序列长度加倍时，投影的输入行数变为两倍，全注意力的位置对数则约为原来的四倍，因为新加入的位置也要与此前的上下文交互。计算这些位置对有两种常见路径：完整矩形乘法先产生所有分数再屏蔽上三角，因果分块则跳过上三角。分块还可以把评分、归一化和 $V$ 汇总接在一起，让分数在片上被使用后释放。第五章将沿这条数据流解释如何减少中间结果的显存读写。
-
-因此，保持 $BP$ 不变只能保持投影行数不变，不能保持上下文交互不变。例如，把一条长输入拆成两条互不相见的短输入，原来跨越分界的那些位置对就消失了。相反，仅在同一请求前面增加上下文，会增加每个新位置的点积数，输入投影的行数却保持不变。
+在此前没有缓存上下文的 prefill 中，序列长度加倍时，投影的输入行数变为两倍，全注意力的位置对数则约为原来的四倍，因为新加入的位置也要与此前的上下文交互。计算这些位置对有两种常见路径：完整矩形乘法先产生所有分数再屏蔽上三角，因果分块则跳过上三角。分块还可以把评分、归一化和 $V$ 汇总接在一起，让分数在片上被使用后释放。第五章将沿这条数据流解释如何减少中间结果的显存读写。
+
+因此，保持 $BP$ 不变只能保持投影行数不变，不能保持上下文交互不变。例如，把一条长输入拆成两条彼此独立的短输入，原来跨越分界的那些位置对就消失了。相反，仅在同一请求前面增加上下文，会增加每个新位置的点积数，输入投影的行数却保持不变。
 
 ### 2.1.4 前馈网络与前向计算量
 
@@ -147,13 +147,13 @@
 \operatorname{FFN}(X)=\left[\operatorname{SiLU}(XW_{\mathrm{gate}})\odot(XW_{\mathrm{up}})\right]W_{\mathrm{down}}
 $$
 
-式中 $\odot$ 表示对应元素相乘；gate 是门控分支，up 是升维分支，down 是降维投影。它们产生的中间向量称为激活。gate 与 up 将每行从 $d$ 维升到 $f$ 维，down 再降回 $d$ 维。三个矩阵因此合计 $3df$ 个参数，处理 $m$ 个 token 的特征向量需 $6mdf$ FLOPs。Qwen3-8B 取 $d=4096,f=12288$，FFN 约有 1.51 亿个参数，BF16 权重占 288 MiB。升维后较宽的激活也需要临时空间，但只在相应计算期间存活。
+式中 $\odot$ 表示对应元素相乘；gate 是门控分支，up 是升维分支，down 是降维投影。它们产生的中间向量称为激活。gate 与 up 将每行从 $d$ 维升到 $f$ 维，down 再降回 $d$ 维。三个矩阵因此合计 $3df$ 个参数，处理 $m$ 个 token 的特征向量需 $6mdf$ FLOPs。Qwen3-8B 取 $d=4096,f=12288$，FFN 约有 1.51 亿个参数，BF16 权重占 288 MiB。升维后较宽的激活也需要临时空间，但只需在相应计算期间保留。
 
 ![SwiGLU 的两条升维分支。gate 经 SiLU 后调节 up 的对应元素，乘积再由 down 投影降回主干维度。](ch02/figure-2-ffn-gates.svg)
 
 *图 2-5　SwiGLU 的两条升维分支。gate 经 SiLU 后调节 up 的对应元素，乘积再由 down 投影降回主干维度。*
 
-注意力的 QKV 与输出投影合计约 4194 万个参数。将它与 FFN 的约 1.51 亿相加，FFN 占这些主要投影参数的约 78%，因此短上下文的计算中，三个 FFN 矩阵占据很大份额。随着输入变长，FFN 随位置数线性增长，全注意力则随 $P(P+1)/2$ 增长。两者增长速度不同，因此各部分计算量的占比会随上下文长度变化。
+注意力的 QKV 与输出投影合计约有 4194 万个参数。加上 FFN 的约 1.51 亿个参数后，FFN 占这些主要投影参数的约 78%，因此短上下文的计算中，三个 FFN 矩阵占据很大份额。随着输入变长，FFN 的运算量随位置数线性增长，全注意力的运算量则随 $P(P+1)/2$ 增长。两者增长速度不同，因此各部分计算量的占比会随上下文长度变化。
 
 残差连接把一个子层的输入保留下来，在该子层完成变换后，与输出逐元素相加。这样，一条路径执行新变换，另一条路径直接传递已有表示。下面把注意力、前馈和两次残差连接连成完整的一层。
 
@@ -163,7 +163,7 @@
 
 **表 2-1　Qwen3-8B 的单层模块与输出接口**
 
-注意力和前馈各行按 36 个主干层累计；词嵌入是词表查行，不再按一次词表矩阵乘法（GEMM，通用矩阵乘法）计算。注意力的 $K$、$V$ 只各有 1024 维，而查询仍有 4096 维。
+注意力和前馈各行按 36 个主干层累计；词嵌入根据 token 编号读取词表中对应的行，不再按一次词表矩阵乘法（GEMM，通用矩阵乘法）计算。注意力的 $K$、$V$ 只各有 1024 维，而查询仍有 4096 维。
 
 **模型入口**
 
@@ -201,11 +201,11 @@
 F_{\mathrm{matrix}}=36\left(385{,}875{,}968m+16{,}384N_{\mathrm{pair}}\right)+2m_{\mathrm{out}}\times4096\times151936.
 $$
 
-式中第一项随输入行数增长，第二项随查询与上下文的位置对数增长，最后一项随实际执行词表投影的位置数增长。对 8192-token 输入，按有效因果位置对计算注意力，词表头只处理最后一个位置，共需 133.6 TFLOPs；已有 8192 个上下文位置、再补入一个 token 时，共需 20.0 GFLOPs。[^source-11]
+式中第一项随输入行数增长，第二项随查询与上下文的位置对数增长，最后一项随实际执行词表投影的位置数增长。对包含 8192 个 token 的输入，按有效因果位置对计算注意力，词表头只处理最后一个位置，共需 133.6 TFLOPs；已有 8192 个上下文位置、再补入一个 token 时，共需 20.0 GFLOPs。[^source-11]
 
 prefill 同时处理 8192 个输入位置，每个位置都要经过投影和 FFN。decode 只对新位置执行这些运算，旧位置的 $K$、$V$ 则从缓存读取。因此，缓存省去了旧位置的投影和 FFN 重算，当前查询与上下文的注意力计算仍要执行。下一节将计算这部分缓存和访问。
 
-> **练习 2-2〔核心〕：输入行数相同，注意力为何不同**
+> **练习 2-2〔核心〕：输入行数相同，注意力计算量为何不同**
 >
 > 使用表 2-1，取 $B=4,S=4096,P=1024$，分别计算投影与 FFN、有效因果注意力，以及最后一个位置的词表输出层所需的 FLOPs。保持 $BP=4096$，改为 $B=1,S=4096,P=4096$，先预测哪些项相同，再计算差异。最后比较两种输入处理方式：完整处理 8K 输入，或复用前 6144 个位置的 KV、只处理新增的 2048 个位置。求后一种方式节省的矩阵运算比例。
 
@@ -236,8 +236,8 @@
 | 数据类别 | 保存与复用 | 本章计算的主要项目 |
 | --- | --- | --- |
 | 权重 | 通常在多次调用间保持不变 | 常驻字节、被访问的权重集合、批内复用 |
-| 上下文状态 | 随请求保留，持续追加或更新 | 有效容量、每步读取、写入及更新计算 |
-| 临时数据 | 在相应算子或阶段中产生，使用后释放 | 张量尺寸、接口数据量、需要同时保存的部分 |
+| 上下文状态 | 随请求保留，持续追加或更新 | 实际占用的容量、每步读写量及更新所需的计算 |
+| 临时数据 | 在相应算子或阶段中产生，使用后释放 | 张量尺寸、经过相应存储接口的数据量、需要同时保存的部分 |
 
 Qwen3-8B 全部 BF16 权重约占 16.38 GB，即 15.26 GiB。模型加载后，这份数据供多个请求反复使用。一次前向中，嵌入按 token ID 查行，层内投影与 FFN 使用对应矩阵，末端词表头计算输出分数。访问哪些权重，因而由计算图中的具体操作决定。
 
@@ -255,13 +255,13 @@
 B_* = \frac{R_W}{c_{\mathrm{KV}}H}.
 $$
 
-以 $R_W=15{,}136{,}811{,}008$ 字节、$c_{\mathrm{KV}}=147456$ 字节代入，$H=8192$ 时最小整数 batch 为 13；$H=2048$ 时为 51。上下文缩短到四分之一，使上下文读取量超过权重读取量所需的请求数约增至四倍。上下文越长，批内权重复用的收益就越早受到限制，因为每个新增请求都会增加一份独立上下文的读取。[^source-12]
-
-沿第一章的数据路径看，加载把权重从存储送到 GPU 显存，执行再把所需权重送到计算单元。KV 和层间激活也可在设备内部保存与使用。相同的逻辑访问因此可以留在片上、经过 HBM，或跨设备传输；第四章将给这些路径分别配置容量与带宽。
+以 $R_W=15{,}136{,}811{,}008$ 字节、$c_{\mathrm{KV}}=147456$ 字节代入，$H=8192$ 时最小整数 batch 为 13；$H=2048$ 时为 51。上下文长度缩短到原来的四分之一，使上下文读取量超过权重读取量所需的请求数约增至四倍。上下文越长，批内权重复用的收益就越早受到限制，因为每个新增请求都会增加一份独立上下文的读取。[^source-12]
+
+沿第一章的数据路径看，加载把权重从存储送到 GPU 显存，执行再把所需权重送到计算单元。KV 和层间激活也可在设备内部保存与使用。相同的逻辑访问因此可以留在片上、经过 HBM，或跨设备传输；第四章将分别分析这些路径的存储容量与传输带宽。
 
 ### 2.2.3 生成完整回答的计算量与状态读写量
 
-上一节比较了一步执行中的读取量。一个完整回答会重复执行许多步，同一份上下文也随之被读取许多次。现在固定一个普通串行生成请求：$B=1$，已恢复前缀为 $S$，本次输入 $P\ge1$ 个 token，最终返回 $G\ge1$ 个 token。令 $H=S+P$。prefill 处理新输入并产生首输出，随后执行 $n_d=G-1$ 次 decode。最后返回的 token 尚未重新送入模型，所以请求结束时并不自动为它保存 KV。
+上一节比较了一步执行中的读取量。一个完整回答会重复执行许多步，同一份上下文也随之被读取许多次。现在固定一个普通串行生成请求：$B=1$，已恢复前缀为 $S$，本次输入 $P\ge1$ 个 token，最终返回 $G\ge1$ 个 token。令 $H=S+P$。prefill 处理新输入并产生首个输出，随后执行 $n_d=G-1$ 次 decode。最后返回的 token 尚未重新送入模型，所以请求结束时并不自动为它保存 KV。
 
 ![四步生成的上下文读写。每一步读取蓝色的已有位置，再追加一个橙色新位置；本图初始上下文为四，四步后保留八个位置。](ch02/figure-2-history.svg)
 
@@ -279,7 +279,7 @@
 
 decode 新增的状态大小为 $c_{\mathrm{KV}}n_d$；若保留整个上下文，最终有效 KV 为 $c_{\mathrm{KV}}(H+n_d)$。本次请求的全部新增 KV 还应包含 prefill 写入的 $c_{\mathrm{KV}}P$。已有前缀的恢复与传输单独核算。求和中的每一项都对应一次 decode 开始时已有的上下文：早出现的位置参与更多后续查询，晚出现的位置参与较少查询。
 
-**例题 2-3：逐步生成如何累积出千 GiB 级的 KV 读取量？** 求请求结束时保存的 KV 大小，以及生成期间读取已有 KV 的总量。
+**例题 2-3：逐步生成为何使 KV 累计读取量远超存储量？** 求请求结束时保存的 KV 大小，以及生成期间读取已有 KV 的总量。
 
 解： 取 $S=0$、$P=8192$、$G=1025$，则 $n_d=1024$，累计访问 8,912,384 个上下文位置。每个位置占 144 KiB，因此累计读取约 1224 GiB。生成期间只新增 144 MiB KV，结束时共保存约 1.27 GiB。累计读取量是最终保存量的近千倍，因为较早生成的上下文被后续查询反复使用。
 
@@ -289,15 +289,17 @@
 F_{\mathrm{decode,total}}=n_dF_0+a\left[n_dH+\frac{n_d(n_d-1)}2\right].
 $$
 
-输出变长时，固定工作按 $n_d$ 增长，生成中新出现的上下文又形成二次增长项。返回一个 token 时 $G=1$、$n_d=0$，只有 prefill；这一特殊情形也说明为什么完整回答包含 $G-1$ 次后续 decode。
-
-图 2-7 沿生成步数累加读取，图 2-8 沿请求数累加读取。多条独立请求的状态容量与上下文访问分别相加，批内权重则共同使用。长度不同时，逐请求代入各自的 $H$ 与 $n_d$：长输出让已有上下文被更多次读取，输入越长，第一次 decode 需要访问的上下文就越多。对实际对话，可先从每轮输入、缓存命中与返回数重建这条调用链，[已保存的 Chat 长度核算](../calculations/results/sealed-chat-known-prefill.md)给出了具体代入过程。
+输出变长时，固定工作按 $n_d$ 增长，生成过程中新增的上下文还会使一部分累计运算量按生成步数的平方增长。返回一个 token 时 $G=1$、$n_d=0$，只有 prefill；这一特殊情形也说明为什么完整回答包含 $G-1$ 次后续 decode。
+
+图 2-7 沿生成步数累加读取，图 2-8 沿请求数累加读取。多条独立请求的状态容量与上下文访问分别相加，批内权重则共同使用。长度不同时，逐请求代入各自的 $H$ 与 $n_d$：长输出让已有上下文被更多次读取，输入越长，第一次 decode 需要访问的上下文就越多。对实际对话，可先根据每轮输入长度、缓存命中情况和返回的 token 数，还原这条调用链，[已保存的 Chat 长度核算](../calculations/results/sealed-chat-known-prefill.md)给出了具体代入过程。
 
 ## 2.3 上下文状态表示与访问机制
 
-这里的上下文状态，是模型处理已有上下文后保存、供后续计算复用的表示，例如 KV 缓存或递推状态。上下文长度按 token 位置计数，状态容量按实际保存的字节数计量。
-
-完整上下文 KV 的容量随序列长度增长，累计读取又随生成次数增加。改变模型结构，可以从几个方面降低这些开销：减少每个上下文位置的表示宽度，压缩或选择上下文条目，或者把上下文信息汇总到固定大小的状态中。这些选择影响的对象不同，也会产生不同的新增工作。比较时需要同时计算状态占用的空间，以及选择、读取和更新状态所需的工作。
+模型继续生成时，需要利用前面已经读过的内容。先想象两种记录办法：一种逐页留下记录，要查哪一页就取哪一页；另一种只保留一张定长摘要，每读到新内容，就改写这张摘要。前一种记录随历史增长，后一种大小固定，但不能保证找回每个位置的全部细节。
+
+对应到模型，保存逐位置 KV 的机制留下各位置的键和值，有限状态递推则把历史贡献汇入固定大小的状态。这里的“记录”和“摘要”实际都是供计算使用的数值向量或矩阵，并非自然语言笔记。模型处理已有上下文后保存、供后续计算复用的这些表示，称为上下文状态。上下文长度按 token 位置计数，状态容量按实际保存的字节数计量。
+
+比较这些机制时，先看历史留下了什么，再看新一步怎样取得所需信息：每个位置可以保存得更窄，也可以只直接访问近期位置、将多个位置汇总为较少条目，或先筛选再读取。另一类机制持续更新固定大小的状态，查询从中取得结果。这些安排分别改变表示宽度、条目数量和访问方式；省下的存储与读取，要同新增的选择、压缩和更新工作一起计算。
 
 ### 2.3.1 多头注意力与 KV 共享
 
@@ -313,7 +315,7 @@
 
 共享 $K$、$V$ 改变了被查询的数据，32 个 $Q$ 头仍分别计算分数和加权输出。因此，KV 投影和保存量按组数减少，$QK^{\mathsf T}$ 与 $AV$ 交互仍按查询头数累计。一次读入的 $K$、$V$ 还可服务同组多个查询头，从而在实际计算中复用同一份数据。
 
-可以直接由组数预测资源变化。将 $n_{\mathrm{KV}}$ 减半，键值投影参数和上下文状态容量都减半；查询头数 $n_Q$ 不变，查询与上下文交互的运算量仍按原来的头数累计。减少存储与减少计算是两个不同结果，只有沿各组件分别计算才能判断。
+可以直接由组数预测资源变化。将 $n_{\mathrm{KV}}$ 减半，键值投影参数和上下文状态容量都减半；查询头数 $n_Q$ 不变，查询与上下文交互的运算量仍按原来的头数累计。
 
 ![固定 Qwen 层数、头宽、8192 个上下文位置与 BF16，只改变 KV 组数。实际模型采用 GQA；其余两柱是用于分析共享比例的结构变体。](ch02/figure-2-4-cache.svg)
 
@@ -345,15 +347,15 @@
 
 *图 2-12　Kimi K3 的 24 层 MLA 在相同 8K 上下文、BF16 下的两种保存量。紧凑路径保存上投影前的表示，展开路径保存各头的键和值。*
 
-这个变换减小了每个上下文位置的保存宽度，但仍需为每个位置保存一个潜变量向量。因此，存储量和点积运算量仍随上下文长度增长。若上下文很长，紧凑表示减少的读取更加显著；若上下文很短，查询变换本身的开销占比更高。接下来先改变保存和选择哪些上下文条目，再研究把上下文汇总到固定大小矩阵中的方法。
-
-> **练习 2-3〔延伸〕：状态大小与累计读取**
+这个变换减小了每个上下文位置的保存宽度，但仍需为每个位置保存一个潜变量向量。因此，存储量和点积运算量仍随上下文长度增长。若上下文很长，紧凑表示节省的读取量更加可观；若上下文很短，查询变换本身的开销占比更高。接下来先讨论如何改变上下文条目的保存和选择方式，再研究把上下文汇总到固定大小矩阵中的方法。
+
+> **练习 2-3〔延伸〕：输出变长后，KV 容量与累计读取量如何增长**
 >
 > 取 Qwen3-8B、$B=1,S=0,P=8192$，分别返回 $G=513$ 和 $G=1025$ 个 token。求后续 decode 次数、最终 KV 大小和累计旧上下文读取量。解释输出数量接近翻倍时，新增状态、总状态与累计读取为什么有不同的比例。再将 KV 位宽减半，指出这三项中哪些随之减半。
 
 ### 2.3.3 局部窗口、上下文压缩与稀疏索引
 
-减少上下文表示宽度之后，还可以改变保留哪些上下文、每次访问哪些上下文。局部窗口只关注最近的一段位置；压缩机制把若干位置合成较少的条目；稀疏选择先确定相关条目，再把它们送入主注意力。窗口决定保留的近期范围，压缩决定长期条目的数量，索引决定当前查询读取哪些条目。索引器先扫描用于选择的表示，再把选中的主状态交给注意力，索引扫描和主状态读取都需要单独计算开销。
+假设模型已经读过一段很长的文本，现在要处理一个新位置。它可以直接读取最近一段位置，也可以读取由多个旧位置汇成的条目，还可以先找出与当前查询相关的条目，再读取它们的主状态。这三种动作分别引出局部窗口、上下文压缩与稀疏选择。它们可以组合使用，但要分别计量：窗口限制近期范围，压缩减少长期条目，索引选择当前要读哪些条目。筛选本身也要读取索引表示并计算分数，不能只统计最后选中的主状态。
 
 先把这里的三个对象分开。**局部滑动窗口注意力（Sliding-Window Attention，SWA）**直接读取最近的一段位置；窗口长 128 时，较早位置退出这一局部窗口。**全局主 KV**是供主注意力读取的长期表示，可能已将多个位置合并；“全局”说明它覆盖较长历史，不表示每次都读取全部条目。**索引 K**是用于检索的另一份较小表示：索引器先用它给条目打分，再让主注意力读取选中的主 KV。索引分数用于选位置，主注意力另用自己的查询计算最终汇总权重。
 
@@ -367,7 +369,7 @@
 
 一次 CSA 查询的主注意力最多读取窗口加 512 个压缩条目，但索引打分仍扫描已保存的压缩索引条目。在 $S=8192$ 时，一个 CSA 层保留 2048 个压缩条目：主注意力状态约 2 MiB、索引约 0.5 MiB，窗口约 0.125 MiB；主注意力选择的压缩条目最多约 0.5 MiB，索引扫描却仍需处理那 2048 个索引条目。HCA 则有 64 个已生成的条目，主注意力状态约 0.0625 MiB，并访问窗口状态与这组粗粒度压缩条目。
 
-将 43 层相加，8192-token 上下文下的窗口、压缩上下文和索引约为 59.125 MiB；另有约 11.641 MiB 的 FP32（每元素 4 字节浮点格式）压缩器缓冲，合计约 70.8 MiB。最后查询的主注意力与索引数据量约 27.6 MiB。[状态分解](../calculations/results/state-deepseek-v4-flash-n8192-b1-native.md)分别列出了这些数据，因此能看出选择条目数、保存量和每步读取量并不相等。
+将 43 层相加，8192-token 上下文下的窗口、压缩上下文和索引约为 59.125 MiB；另有约 11.641 MiB 的 FP32（每元素 4 字节浮点格式）压缩器缓冲，合计约 70.8 MiB。最后查询的主注意力与索引数据量约 27.6 MiB。[状态分解](../calculations/results/state-deepseek-v4-flash-n8192-b1-native.md)分别列出了这些数据，因此能看出选中的条目数、保存的状态大小和每步读取量分别代表什么。
 
 ![上下文压缩与索引选择的先后关系。上方只示意八个位置产生两条记录的数量关系，省略 V4 CSA 的重叠投影与加权汇总，下方展示一次查询先扫描索引再读取主状态的过程。](ch02/figure-2-5-sparse.svg)
 
@@ -383,13 +385,13 @@
 
 *图 2-15　压缩比为四时的一个压缩块的生成过程。前几次输入更新同一块内缓冲，第四个位置到来后形成可供后续查询使用的压缩条目。*
 
-> **练习 2-4〔延伸〕：共享、压缩与实际访问**
+> **练习 2-4〔延伸〕：KV 头共享与上下文压缩如何改变存储和访问量**
 >
 > 先把 Qwen3-8B 的 KV 头数从 8 改为 4，保持 32 个查询头不变，分别计算 KV 容量、KV 投影参数量，以及查询与上下文之间的运算量如何变化。再对 DeepSeek V4-Flash 的 CSA 层，把上下文从 8192 增至 16384，按压缩比 4、每次最多选择 512 个压缩条目，计算保存条目数、索引扫描条目数和选中条目上限。解释三者为何不按同一比例增长。
 
 ### 2.3.4 线性注意力与有限状态递推
 
-线性注意力通过重新组织查询、键和值的计算，避免显式计算所有位置对的注意力分数。在特征维度固定时，下面这种递推形式处理整段序列的运算量随序列长度线性增长，状态矩阵的大小保持不变。
+前面几种机制仍能区分保存下来的各个上下文条目。有限状态递推采用另一种过程：每来一个新位置，就把它的贡献合入已有状态，当前查询直接使用更新后的状态；下一步继续改写这份状态。这对应 2.3 节开头不断改写定长摘要的图景。下面这类线性注意力通过重新组织查询、键和值的计算，避免显式计算所有位置对的注意力分数。在特征维度固定时，状态矩阵大小不变，处理整段序列的运算量随序列长度线性增长。
 
 外积把一个键向量与一个值向量的每对元素相乘，组成二维的关联表。考虑一个由键值外积累加得到的矩阵 $\mathcal S_t$。每个新位置到来时，先读取旧状态，再写入本位置的贡献，当前查询直接从状态矩阵读出结果：
 
@@ -403,11 +405,11 @@
 
 *图 2-16　固定矩阵的递推更新。新键和值的外积加入旧状态，矩阵形状保持不变；查询使用更新后的状态计算输出。本图采用简单累加递推。*
 
-**Kimi Delta Attention（KDA）**是 Kimi 使用的一种门控递推注意力，用固定大小的关联矩阵保存上下文信息。它使用门控与 delta 更新；delta 在这里指依据当前预测误差写入修正量。状态更新不仅写入新信息，还根据当前键对既有状态的预测形成修正，并通过衰减控制旧信息的保留程度。直观上，它更新的是一张固定大小的关联矩阵，而不是不断追加一张逐 token 列表。旧信息以汇总后的关联形式保存，当前键控制修正的位置，衰减门决定保留多少旧信息。
-
-Kimi K3 每个 KDA 层有 96 个头，每头递推矩阵为 $128\times128$。按 FP32 保存，单层为 6 MiB，69 层为 414 MiB。一次 decode 读取旧矩阵、融合新信息并写回，理想一读一写就是 828 MiB。这个量不随上下文长度增长：短上下文时它是一笔显著的固定开销，长上下文时则避免了扫描量随位置数持续增加。
-
-prefill 与 decode 的实现方式也不同。decode 使用单步递推；一段已知输入可以通过块式算法并行计算，再在块之间传递状态。块内中间量和块间状态会占用临时空间，因此最终递推状态的大小不足以预测 prefill 峰值。第五章按缓冲寿命分析这一问题。[^source-15]
+**Kimi Delta Attention（KDA）**是 Kimi 使用的一种门控递推注意力，用固定大小的关联矩阵保存上下文信息。它使用门控与 delta 更新；delta 在这里指依据当前预测误差写入修正量。状态更新不仅写入新信息，还根据当前键对既有状态的预测形成修正，并通过衰减控制旧信息的保留程度。直观上，它更新的是一张固定大小的关联矩阵，而不是不断向逐 token 保存的列表追加条目。旧信息以汇总后的关联形式保存，当前键控制修正的位置，衰减门决定保留多少旧信息。
+
+Kimi K3 每个 KDA 层有 96 个头，每头递推矩阵为 $128\times128$。按 FP32 保存，单层为 6 MiB，69 层为 414 MiB。一次 decode 读取旧矩阵、融合新信息并写回，理想情况下，各读写一次的总量就是 828 MiB。这个量不随上下文长度增长：短上下文时它是一笔显著的固定开销，长上下文时则避免了扫描量随位置数持续增加。
+
+prefill 与 decode 的实现方式也不同。decode 使用单步递推；一段已知输入可以通过块式算法并行计算，再在块之间传递状态。块内中间量和块间状态会占用临时空间，因此仅凭最终递推状态的大小，还无法预测 prefill 期间的内存占用峰值。第五章按缓冲寿命分析这一问题。[^source-15]
 
 ### 2.3.5 混合注意力的状态构成
 
@@ -437,11 +439,11 @@
 M_{\mathrm{state}}(H)=M_{\mathrm{fixed}}+cH,\qquad H_* = \frac{M_{\mathrm{fixed}}}{c}.
 $$
 
-$H_*$ 表示两部分占用相等的上下文长度。Qwen3.6 的固定部分为 $61.875$ MiB，每位置增加 20 KiB，因此 $H_*=3168$；Kimi K3 的紧凑表示每位置增加 27 KiB，固定部分约为 433.4 MiB，交点约在 $16.4$$K$。低于交点时，缩短上下文只能节省较小部分；远高于交点时，减少每位置宽度对容量更有效。DeepSeek V4-Flash 则按完成的压缩块增加条目，曲线呈阶梯增长。
+$H_*$ 表示两部分占用相等的上下文长度。Qwen3.6 的固定部分为 $61.875$ MiB，每位置增加 20 KiB，因此 $H_*=3168$；Kimi K3 的紧凑表示每位置增加 27 KiB，固定部分约为 433.4 MiB，交点约在 16.4K 个上下文位置。低于交点时，缩短上下文只能节省较小部分；远高于交点时，减少每位置宽度对容量更有效。DeepSeek V4-Flash 则按完成的压缩块增加条目，曲线呈阶梯增长。
 
 这些状态支持不同的信息访问方式：全局 KV 允许查询直接选择旧位置，递推矩阵通过更新保留上下文关系，压缩索引则在较少条目中寻找相关内容。2.6.3 将结合具体检索任务比较资源需求。[^source-17]
 
-> **练习 2-5〔核心〕：固定状态与增长状态的交点**
+> **练习 2-5〔核心〕：上下文多长时，KV 容量会超过固定状态？**
 >
 > 对 Qwen3.6，取固定状态 61.875 MiB、每个上下文位置 20 KiB；对 Kimi K3 的紧凑表示，递推状态占 414 MiB，卷积状态占 20,348,928 字节，每个上下文位置另占 27 KiB。分别求固定项与增长项相等的上下文长度，以及 32K、128K 上下文的总状态。若只有 256 MiB 状态预算，两个模型各自最多能保存多少个上下文位置？说明容量预算小于固定状态大小时，为什么缩短上下文仍无法容纳模型状态。
 
@@ -477,11 +479,11 @@
 
 长上下文的成本还取决于同一段信息在多少层中重复保存。V4.1 的技术报告直接把 HBM、SSD 与主机内存容量，以及 KV 迁移带宽列为设计约束。它没有继续提高 V4 的序列压缩比：V4 使用 4:1 的 CSA 和 128:1 的 HCA，V4.1 取消 HCA，编码器的全局条目按 2:1 压缩，解码器按 1:1 保留。更小的缓存主要来自跨层共享与更低精度，同时保留了更细的历史位置粒度。[^v41-case]
 
-共享也改变了模型能够独立选择什么。复用层不再生成自己的全局 K/V，但每层仍保留独立的查询 Q 和局部 SWA，因而可以对共享条目计算不同的注意力权重，形成新的输出。缓存共享减少了一类逐层独立表示，却没有把所有层的计算变成相同操作。
-
-![V4 与 V4.1 的全局 KV 保存方式。左侧以三个代表层示意逐层保存；右侧显示 V4.1 四个全局 KV 拥有层及共享关系。实线表示数据读取；各使用层仍有自己的 Q 与局部 SWA，图中省略其他计算。](ch02/figure-2-v41-sharing.svg)
-
-*图 2-21　V4 与 V4.1 的全局 KV 保存方式。左侧以三个代表层示意逐层保存；右侧显示 V4.1 四个全局 KV 拥有层及共享关系。实线表示数据读取；各使用层仍有自己的 Q 与局部 SWA，图中省略其他计算。*
+共享也改变了各层能够独立选择的信息。复用层不再生成自己的全局 K/V，但每层仍保留独立的查询 Q 和局部 SWA，因而可以对共享条目计算不同的注意力权重，形成新的输出。缓存共享减少了一类逐层独立表示，却没有把所有层的计算变成相同操作。
+
+![V4 与 V4.1 的全局 KV 保存方式。左侧以三个代表层示意逐层保存；右侧显示 V4.1 四个独立保存全局 KV 的层及共享关系。实线表示数据读取；各使用层仍有自己的 Q 与局部 SWA，图中省略其他计算。](ch02/figure-2-v41-sharing.svg)
+
+*图 2-21　V4 与 V4.1 的全局 KV 保存方式。左侧以三个代表层示意逐层保存；右侧显示 V4.1 四个独立保存全局 KV 的层及共享关系。实线表示数据读取；各使用层仍有自己的 Q 与局部 SWA，图中省略其他计算。*
 
 把上述共享关系代入容量计算。2026 年 9 月发布的 DeepSeek V4.1 Flash 有 40 层文本主干，但只有 4 层拥有独立的全局 KV：从零编号的第 2、8、14 层每两个 token 生成一条压缩记录，第 20 层每个 token 生成一条记录，其余全局注意力层共享这些缓存。主记录的 512 个值采用 FP4，每 16 个值另有一个字节的 scale，因此每条记录占 $512/2+512/16=288$ 字节；索引记录占 $128/2+128/32=68$ 字节。全局 KV 的平均存储增量为
 
@@ -497,9 +499,9 @@
 
 两者的全局 KV 平均增量相差约 3.95 倍，对应官方图中取整后的 3,514 与 890 B/token。全局历史之外，V4.1 的局部窗口每层最多保存 128 条记录，每条 FP8+scale 记录占 528 B，40 层合计 2.578 MiB。全局历史随上下文增长，局部窗口则在填满后保持固定容量。
 
-沿生成过程逐步观察，还能看出全局缓存的增长节奏。对 V4.1，全局缓存从偶数长度增长到下一个奇数长度只增加 356 B；到下一个偶数长度时，三个 2:1 压缩器同时完成，增加 1,424 B。窗口满后继续覆盖旧槽，容量不再增长，但仍然产生写入：40 层 SWA 每步合计写入 $40\times528=21{,}120$ B，压缩器更新另计。
-
-共享同一份全局缓存的各层仍需读取数据、部分层还会重新索引；8K 场景下，两代 Flash 的注意力 KV（含局部 SWA）与索引逻辑读取量约为 12.56 和 11.38 MiB，跨层共享主要减少了重复保存的空间。
+沿生成过程逐步观察，还能看出全局缓存的增长节奏。对 V4.1，全局缓存从偶数长度增长到下一个奇数长度只增加 356 B；到下一个偶数长度时，三个 2:1 压缩器各完成一条记录，增加 1,424 B。窗口填满后，后续写入会覆盖旧槽位，容量不再增长，但仍然产生写入：40 层 SWA 每步合计写入 $40\times528=21{,}120$ B，压缩器更新另计。
+
+共享同一份全局缓存的各层仍需读取数据，部分层还会重新索引；8K 场景下，两代 Flash 的注意力 KV（含局部 SWA）与索引逻辑读取量约为 12.56 和 11.38 MiB，跨层共享主要减少了重复保存的空间。
 
 缓存复用与位置选择还可以分别安排。**压缩稀疏注意力第二版（Compressed Sparse Attention 2，CSA2）**把层分成三种模式。**Full** 层完整执行“生成全局 KV、索引、选取和注意力”的流程。**Reindex** 层共享 KV，用自己的索引查询重新选取 Top-K；**Reuse** 层连同所选位置一起复用。V4.1 的 38 个全局注意力层中，有 4 个 Full、4 个 Reindex 和 30 个 Reuse；另外两个层仅使用 SWA。解码器的首个 Full 层扫描全局，并选出最多 $2048\times8=16{,}384$ 个候选位置；技术报告第 2.3.2 节将这个集合称为**候选池（candidate pool）**，其中的候选是可供注意力检索的位置。后续 Reindex 层只在池内各自选取最多 512 个条目。[^v41-candidate]后续层的搜索量因此有了上限，初始全局扫描仍随上下文增长。
 
@@ -507,15 +509,15 @@
 
 候选池让后续层的搜索量有了上限，也使首轮筛选影响后续选择：某个位置一旦落在候选池之外，本次查询的后续 Reindex 层便无法再选入它。模型训练需要适应这一搜索范围，第十章将说明 V4.1 怎样把候选限制纳入后训练。
 
-![V4.1 解码器的层级位置选择。首个 Full 层先扫描全局，构造最多 16,384 个候选；后续 Reindex 在候选内重新选 512 个，Reuse 使用已有选择。示意位置数量不按比例；各层独立 Q 与局部 SWA 未画出。](ch02/figure-2-v41-selection.svg)
-
-*图 2-22　V4.1 解码器的层级位置选择。首个 Full 层先扫描全局，构造最多 16,384 个候选；后续 Reindex 在候选内重新选 512 个，Reuse 使用已有选择。示意位置数量不按比例；各层独立 Q 与局部 SWA 未画出。*
+![V4.1 解码器的层级位置选择。首个 Full 层先扫描全局，选出最多 16,384 个候选位置；后续 Reindex 在候选内重新选 512 个，Reuse 使用已有选择。示意位置数量不按比例；各层独立 Q 与局部 SWA 未画出。](ch02/figure-2-v41-selection.svg)
+
+*图 2-22　V4.1 解码器的层级位置选择。首个 Full 层先扫描全局，选出最多 16,384 个候选位置；后续 Reindex 在候选内重新选 512 个，Reuse 使用已有选择。示意位置数量不按比例；各层独立 Q 与局部 SWA 未画出。*
 
 ## 2.4 条件计算与专家权重复用
 
 前面几节通过共享、压缩和递推减少上下文状态。对另一个主要数据来源——模型权重——也可以问同样的问题：每个 token 是否必须使用模型中的全部参数？下面从 FFN 出发分析这一问题。
 
-稠密 FFN 对每个 token 使用同一组矩阵；MoE 把参数分配给多个专家，由路由器为每个 token 选择少数专家。它使“模型保存多少参数”与“一个 token 激活多少参数”分离，也让 batch 内不同 token 的选择影响实际权重访问集合。
+稠密 FFN 对每个 token 使用同一组矩阵；MoE 把参数分配给多个专家，由路由器为每个 token 选择少数专家。这样，“模型保存多少参数”与“一个 token 激活多少参数”就可以分别调整，也让 batch 内不同 token 的选择影响实际权重访问集合。
 
 ### 2.4.1 稠密前馈网络与专家路由
 
@@ -533,7 +535,7 @@
 
 解： 即使激活参数相同，batch 内的权重读取也可能不同。假设同时为 $B=64$ 条请求各执行一步 decode，每条请求本次输入一个 token，因此 $m=B=64$。每个 token 选择 8 个专家，总共有 512 次 token–expert 分派。若均匀分到 256 个专家，每个专家平均接收 2 个 token，当前批次会访问全部路由专家；若所有 token 都集中到同 8 个专家，则每个专家接收 64 个 token，当前批次只访问 8 份专家权重。
 
-设本批实际访问的不同专家数为 $U$。没有丢弃的路由满足 $\sum_e t_e=Bk_{\mathrm{top}}$，因此
+设本批实际访问的不同专家数为 $U$。若路由过程中不丢弃任何分派给专家的 token，则有 $\sum_e t_e=Bk_{\mathrm{top}}$，因此
 
 $$
 F_{\mathrm{expert}}=2N_e\sum_e t_e=2N_eBk_{\mathrm{top}},\qquad R_{\mathrm{expert}}=b_WN_eU.
@@ -550,27 +552,27 @@
 
 *图 2-23　64 个 token 各选八个专家，总计 512 次分派。分散时可覆盖 256 个专家，集中时只访问八个；每专家处理的行数随之改变。*
 
-读取差额来自每份权重服务的输入行数：均匀分派时每个专家处理两个 token，集中分派时每个专家处理 64 个 token。同一份权重支持的计算增加 32 倍，理想权重访问量便减少 32 倍。与此同时，原来要执行许多输入为两行的矩阵乘法，现在只需执行少数输入为 64 行的矩阵乘法；权重复用和计算效率因此都会受到影响。[^source-18]
+读取差额来自每份权重服务的输入行数：均匀分派时每个专家处理两个 token，集中分派时每个专家处理 64 个 token。同一份权重参与的计算量增至原来的 32 倍，理想权重访问量便降至原来的三十二分之一。与此同时，原来要执行许多输入为两行的矩阵乘法，现在只需执行少数输入为 64 行的矩阵乘法；权重复用和计算效率因此都会受到影响。[^source-18]
 
 Qwen3.6 在已有 8192-token 上下文的 $B=1$ decode 中约需 7.33 GFLOPs，小于 Qwen3-8B 的约 20.0 GFLOPs。整个模型的计算量差异来自几项结构上的变化：2048 维主干缩小投影，30 个线性层使用递推状态，MoE 每次只执行选中的专家及共享分支。专家数量增加使总参数量增大，而每次调用的工作量取决于实际执行了哪些运算。
 
-稀疏激活把总参数容量与每 token 的激活计算分开，使模型可以用较少计算访问较大的参数集合。服务时，路由结果决定每份专家权重供多少 token 复用：少量 token 分散到很多专家，会产生许多小矩阵和低复用读取；集中到同一组专家，则能组成更大的矩阵。下面用一个批次比较这两种访问方式。
+稀疏激活让总参数量与每个 token 实际使用的参数量可以分别调整，使模型可以用较少计算访问较大的参数集合。服务时，路由结果决定每份专家权重供多少 token 复用：少量 token 分散到很多专家，会产生许多小矩阵，每次读入的权重也很少得到复用；集中到同一组专家，则能组成更大的矩阵。例题 2-4 展示的正是这两种访问方式的差别。
 
 ### 2.4.3 共享专家与潜空间计算
 
 上述差距由专家接收的输入行数决定。接下来还要考虑专家本身的尺寸：共享专家处理全部输入，潜空间专家则先缩小输入维度。两者都会改变一个专家需要多少权重、执行多少运算。
 
-DeepSeek V4-Flash 每层有 256 个路由专家，每 token 选 6 个，另有 1 个共享专家；路由专家中间维度为 2048。单专家的 gate/up 为 $[t_e,4096]\times [4096,2048]$，down 为反向尺寸，参数为 25,165,824。前三层采用哈希路由，后续采用评分路由：哈希路由按 token 确定专家，评分路由根据当前表示选择专家。
+DeepSeek V4-Flash 每层有 256 个路由专家，每 token 选 6 个，另有 1 个共享专家；路由专家中间维度为 2048。单专家的 gate/up 为 $[t_e,4096]\times [4096,2048]$，down 将宽度从 2048 变回 4096；三个投影合计包含 25,165,824 个参数。前三层采用哈希路由，后续采用评分路由：哈希路由按 token 确定专家，评分路由根据当前表示选择专家。
 
 共享专家处理本次全部 $m$ 个 token 的特征向量，路由专家 $e$ 只处理分派给它的 $t_e$ 个 token。若共享专家有 $N_s$ 个参数，一层的专家矩阵运算量为 $2mN_s+2\sum_e t_eN_e$。普通单步 decode 中，每条请求输入一个新 token，此时 $m=B$；prefill 则要累计各请求本次处理的全部 token。DeepSeek V4-Flash 每 token 选择六个路由专家并执行一个共享专家，正是这两个部分相加；容量则需要保存全部 256 个路由专家和共享分支。
 
 Kimi K3 每个 MoE 层有 896 个路由专家，每个 token 选择其中 16 个。输入先从 7168 维主干空间投影到 3584 维潜空间，再经过中间维度为 3072 的专家变换，合并后投影回主干空间。共享专家直接处理主干表示。降维减少了路由专家的矩阵尺寸，同时增加了进入和离开潜空间的两次投影。[^source-3]
 
-如果专家分布在不同设备上，就要把输入 token 发送到对应设备，专家完成计算后再合并结果。通信量因此取决于专家放在哪里、输入由哪张设备持有。第六章讨论多卡划分，第九章介绍 CPU／GPU 放置，以及将注意力（attention）与前馈网络（FFN）交给不同资源的 AF 分离。本节先确定每个专家处理的 token 数、矩阵尺寸和权重读取量。
-
-> **练习 2-6〔延伸〕：专家总量、激活与复用**
+如果专家分布在不同设备上，就要把输入 token 发送到对应设备，专家完成计算后再合并结果。通信量因此取决于专家放在哪里、输入保存在何处。第六章讨论多卡划分，第九章介绍 CPU／GPU 放置，以及将注意力（attention）与前馈网络（FFN）交给不同资源的 AF 分离。本节先确定每个专家处理的 token 数、矩阵尺寸和权重读取量。
+
+> **练习 2-6〔延伸〕：专家数量与分派方式如何影响计算和权重读取**
 >
-> 对 Qwen3.6 的一层，取 $B=64$、256 个路由专家、每 token 选 8 个。分别考虑均匀分派和集中分派到同一组 8 个专家的情况，写出各专家接收的 token 数 $t_e$，再计算 FLOPs、被访问的专家数量与 BF16 权重读取量。再将专家数减半、专家中间宽度加倍、每 token 选中数减半，判断总专家参数与激活矩阵运算是否变化。最后说明共享专家与潜空间进出投影应加在哪一项。
+> 对 Qwen3.6 的一层，取 $B=64$、256 个路由专家、每 token 选 8 个。分别考虑均匀分派和集中分派到同一组 8 个专家的情况，写出各专家接收的 token 数 $t_e$，再计算 FLOPs、被访问的专家数量与 BF16 权重读取量。再将专家总数减半、中间维度加倍、每个 token 选中的专家数减半，判断专家总参数量与实际执行的专家矩阵运算量是否变化。最后说明如何将共享专家，以及进入和离开潜空间的投影，计入参数量与运算量。
 
 ## 2.5 网络形状与计算组织
 
@@ -578,11 +580,11 @@
 
 ### 2.5.1 深度、宽度与专家粒度
 
-先忽略词表和小向量参数。若 FFN 宽度与隐藏维度保持固定比例，稠密主干的参数量大致正比于 $Ld^2$。把隐藏维度减半、层数增至四倍，可以保持这部分参数量相近，却把逐层依赖加长了四倍。每个矩阵更小，但必须经过更多层；每层还要保存相应的上下文状态。层数与宽度由此在相近参数预算下改变了计算粒度和依赖深度。
-
-专家也有类似的在参数量和运算量不变时调整结构的方法。单层路由专家的总参数为 $3Edf$，单 token 专家运算量为 $6k_{\mathrm{top}}df$。将专家数 $E$ 减半、中间维度 $f$ 加倍、选中数 $k_{\mathrm{top}}$ 减半，这两个量都不变。Qwen3-235B-A22B 的 128 个宽度 1536 的专家、每 token 选 8 个，可以与 64 个宽度 3072 的专家、每 token 选 4 个作这一对照。路由输出维度与每专家接收行数仍会改变，从而改变分组矩阵乘法的形状。
-
-在 16-token 算例中，三种专家粒度的专家矩阵运算量保持相同，基线与 64 专家变体均约为 454 GFLOPs。专家数减半、单专家宽度加倍时，相同数量的权重被分成更少的大矩阵；专家数加倍、宽度减半时，则成为更多的小矩阵。路由器要为每个专家计算路由分数，因此其参数也随专家数变化。[专家粒度计算](../calculations/results/qwen235-granularity-baseline.md)将路由与专家矩阵分别累计，呈现同一专家参数总量如何形成不同的执行形状。
+先忽略词表和小向量参数。若 FFN 宽度与隐藏维度保持固定比例，稠密主干的参数量大致正比于 $Ld^2$。把隐藏维度减半、层数增至四倍，可以保持这部分参数量相近，但逐层依赖链也增至原来的四倍长。每个矩阵更小，但必须经过更多层；每层还要保存相应的上下文状态。层数与宽度由此在相近参数预算下改变了计算粒度和依赖深度。
+
+专家结构也可以作类似调整，同时保持参数量和运算量不变。单层路由专家的总参数为 $3Edf$，单 token 专家运算量为 $6k_{\mathrm{top}}df$。将专家数 $E$ 减半、中间维度 $f$ 加倍、选中数 $k_{\mathrm{top}}$ 减半，这两个量都不变。Qwen3-235B-A22B 的 128 个宽度 1536 的专家、每 token 选 8 个，可以与 64 个宽度 3072 的专家、每 token 选 4 个作这一对照。路由输出维度与每专家接收行数仍会改变，从而改变分组矩阵乘法的形状。
+
+在 16-token 算例中，三种专家粒度的专家矩阵运算量保持相同，基线与 64 专家变体均约为 454 GFLOPs。专家数减半、单专家宽度加倍时，相同数量的权重被分成更少的大矩阵；专家数加倍、宽度减半时，则成为更多的小矩阵。路由器要为每个专家计算路由分数，因此其参数也随专家数变化。[专家粒度计算](../calculations/results/qwen235-granularity-baseline.md)将路由与专家矩阵分别累计，说明专家参数总量相同时，矩阵形状为何仍会不同。
 
 形状也决定多卡分工的最小单位。128 个专家可分成八组，每组 16 个；四个 KV 头按完整头分工时只能形成四份，八卡执行就需要复制部分 KV 或调整分组。第五章从矩阵分块分析执行效率，第六章将进一步把这些矩阵和状态分配到设备。
 
@@ -592,9 +594,9 @@
 
 普通残差把不同深度的信息不断相加到同一条通路中。为了让层间信息有更多组合方式，同时保持深层网络中的信号传播稳定，DeepSeek V4-Flash 使用 **mHC（Manifold-Constrained Hyper-Connections，流形约束超连接）**。它把一条残差通路扩展成多条，并约束通路之间的混合系数；这里的“流形约束”具体表现为将残差混合矩阵的元素限制为非负、各行与各列的和接近 1。[^architecture-motivation]
 
-该模型保存 4 路残差状态，每一路都是当前 token 的一份 4096 维中间表示。保留四路表示，是为了让后续层学习如何组合不同通路的信息。每个子层先将四路表示混合为 4096 维子层输入，执行注意力或 FFN，再把结果写回残差通路。因此，主体矩阵保持 4096 维输入，新增工作集中在进入和离开子层的混合与汇合。四路状态扩大了跨层表示，混合操作负责连接它们与原有子层。
-
-处理 8192 个输入 token 时，mHC 的混合投影约需 555 GFLOPs，普通算术约需 149 GFLOPs。残差混合矩阵先经 **Sinkhorn 迭代**归一化，即交替将每行、每列除以各自的和，使行和与列和逐渐接近 1；这使多层混合中的数值尺度更稳定。归一化后的系数再用于组合各条残差通路；末端汇合这些通路，形成送入词表头的表示。因此，多路残差不仅增加要保存的表示，还增加每层的混合与归一化计算。[^source-4]
+该模型保存 4 路残差状态，每一路都是当前 token 的一份 4096 维中间表示。保留四路表示，是为了让后续层学习如何组合不同通路的信息。每个子层先将四路表示混合为 4096 维子层输入，执行注意力或 FFN，再把结果写回残差通路。因此，主体矩阵保持 4096 维输入，新增工作集中在进入和离开子层的混合与汇合。四路状态让层间可以传递更多组表示，混合操作负责连接它们与原有子层。
+
+处理 8192 个输入 token 时，mHC 的混合投影约需 555 GFLOPs，非矩阵运算约需 149 GFLOPs。残差混合矩阵先经 **Sinkhorn 迭代**归一化，即交替将每行、每列除以各自的和，使行和与列和逐渐接近 1；这使多层混合中的数值尺度更稳定。归一化后的系数再用于组合各条残差通路；末端汇合这些通路，形成送入词表头的表示。因此，多路残差不仅增加要保存的表示，还增加每层的混合与归一化计算。[^source-4]
 
 Kimi K3 希望当前层能够按需要选取不同深度的信息，而不只是接收逐层相加后的一个混合结果。它使用 **Attention Residuals（AttnRes，注意力残差连接）**，对较浅层的表示计算权重，再加权求和，形成当前层的输入。这种注意力沿网络深度选择信息，前面的序列注意力则在不同 token 位置之间选择信息。为减少保存和读取所有层输出的开销，块式实现把若干层组织成一块，保留供后续块选择的表示。[^architecture-motivation]
 
@@ -606,13 +608,13 @@
 
 ### 2.5.3 多 token 预测与辅助计算
 
-普通自回归主干根据当前上下文预测下一个 token。**多 token 预测（Multi-Token Prediction，MTP）**在此基础上增加后续多个位置的预测目标或辅助预测模块。训练时，这些额外目标促使当前表示包含对更远后续内容有用的信息；推理时，辅助模块可以先提出多个候选 token，再由主模型验证，以减少生成同样数量 token 所需的串行轮数。分析时必须区分三件事：训练时优化了哪些预测目标，发布的模型实际包含哪些模块，服务后端又启用了哪些模块。
+普通自回归主干根据当前上下文预测下一个 token。**多 token 预测（Multi-Token Prediction，MTP）**在此基础上增加后续多个位置的预测目标或辅助预测模块。训练时，这些额外目标促使当前表示包含对更远后续内容有用的信息；推理时，辅助模块可以先提出多个候选 token，再由主模型验证，以减少生成同样数量 token 所需的串行轮数。
 
 **推测解码（speculative decoding）**先用较低开销生成候选序列，再让目标模型验证这些候选。用于推测解码时，MTP 根据当前主干特征预先生成后续 token，再交给目标模型验证。如果多个 token 通过验证，一轮就能产生多个有效输出；否则从最后一个通过验证的位置继续生成。因此，加速效果取决于草稿生成、目标模型验证各需多久，以及每轮有多少 token 被接受。第八章将计算这三者的关系。
 
 DeepSeek V4-Flash 的 MTP 辅助块接收主干特征和 token ID，预测后续位置。对单个 token，即 $B=1,T=1$，一次辅助计算约需 1.80 GFLOPs，其中共享词表头约需 1.06 GFLOPs。共享词表头占这次计算的一半以上：虽然它不增加一份独立权重，每次调用仍要执行词表投影。[^source-5]
 
-辅助预测提醒我们，一次前向执行、一次采样和一个有效输出可能是不同的单位。第三章将把编码、生成、迭代及实时输出组织成完整负载；第八章再计算生成草稿与验证所需的时间，以及由此节省的生成时间。
+辅助预测使一次前向执行能够提供多个候选，经验证和采样后形成有效输出。第三章将把编码、生成、迭代及实时输出组织成完整负载；第八章再计算生成草稿与验证所需的时间，以及由此节省的生成时间。
 
 设一次普通 decode 耗时为 $T_d$，一轮推测解码的草稿和验证合计耗时为 $T_s+T_v$，平均得到 $a$ 个有效输出。每输出分摊时间为 $(T_s+T_v)/a$，有收益的条件是
 
@@ -675,7 +677,7 @@
 
 阅读时可以按数据流走一遍：输入进入哪类注意力层，随后经过哪类前馈网络，最终如何得到词表分数。每张表按注意力、前馈与连接、输出接口分组；“层／调用数”给出整模型累计倍数，“矩阵 FLOPs”仍是该行对应操作执行一次的浮点运算量。先确定这一层采用的注意力、前馈网络与连接结构，再累加该层的注意力、前馈与连接工作，最后加入嵌入查行、末端归一化和词表头。
 
-将同样的累加规则用于其他模型，可以逐项列出它们的计算分支。对 Qwen3-8B，表中包含嵌入、36 层注意力与 FFN、残差和归一化、最终输出头；对 Qwen3.6，还需分别计算线性注意力层与完整注意力层，并列路由与共享专家。对 DeepSeek V4-Flash，还要加入窗口、压缩、索引和 mHC；对 Kimi K3，则包括 KDA、MLA、潜空间投影、共享专家与 AttnRes。MTP 与视觉模块按本次执行范围单独加入。
+将同样的累加规则用于其他模型，可以逐项列出它们的计算分支。对 Qwen3-8B，表中包含嵌入、36 层注意力与 FFN、残差和归一化、最终输出头；对 Qwen3.6，还需分别计算线性注意力层与完整注意力层，分别列出路由专家与共享专家。对 DeepSeek V4-Flash，还要加入窗口、压缩、索引和 mHC；对 Kimi K3，则包括 KDA、MLA、潜空间投影、共享专家与 AttnRes。MTP 与视觉模块按本次执行范围单独加入。
 
 | 模型分支 | 上下文状态 | 权重与附加计算 | 合计时最容易遗漏的部分 |
 | --- | --- | --- | --- |
@@ -685,7 +687,7 @@
 | DeepSeek V4-Flash | 窗口＋压缩＋索引＋缓冲 | 专家、压缩器、mHC | 索引扫描、块边界更新、共享分支 |
 | Kimi K3 | MLA＋KDA＋卷积 | 潜空间专家、AttnRes | 展开路径、跨层临时状态、首层差异 |
 
-先连续比较完整权重、单步计算和上下文状态，再通过各模型的分项表解释差异。Qwen3.6 的权重比 Qwen3-8B 多，单步计算和上下文状态却更少；DeepSeek V4-Flash 的专家权重继续增加，上下文压缩又使 8K 状态进一步减小。后面的表将说明这些差异分别来自哪些矩阵和状态。
+先依次比较完整权重、单步计算量和上下文状态大小，再通过各模型的分项表解释差异。Qwen3.6 的权重比 Qwen3-8B 多，单步计算和上下文状态却更少；DeepSeek V4-Flash 的专家权重继续增加，上下文压缩又使 8K 状态进一步减小。后面的表将说明这些差异分别来自哪些矩阵和状态。
 
 ![四模型完整权重统一用 BF16 表示时的容量，使用同一线性尺度。这里累计全部参数，包括每次仅选中部分的路由专家。](ch02/figure-2-resources.svg)
 
@@ -703,9 +705,9 @@
 
 将矩阵尺寸相乘并按层数累计，DeepSeek V4-Flash 主干约有 2843 亿个逻辑参数，统一 BF16 表示约为 568.7 GB。发布的模型检查点（保存参数的文件集合）则采用混合存储格式，主干数据量约为 156.0 GB。前者回答结构包含多少参数，后者回答这些参数在文件中占多少字节；格式转换把二者连接起来。
 
-Kimi K3 文本主干约有 2.78 万亿个参数，绝大多数位于 92 个 MoE 层的路由专家中。每个 token 在每层只选择 16 个专家，因此完整模型的存储容量与单 token 的计算量相差很大：前者由全部专家决定，后者由选中的专家及共同执行的注意力、共享专家和投影决定。[^source-7]
-
-下面将各模型归并到互不重叠的五类参数：嵌入与输出、注意力投影、稠密或共享 FFN、路由专家、其余组件。将各类参数相加，就得到模型总参数量。这种分解能直观地展示参数分布，也为解释下一张计算与状态表提供依据。
+Kimi K3 文本主干约有 2.78 万亿个参数，绝大多数位于 92 个 MoE 层的路由专家中。每个 token 在每层只选择 16 个专家，因此完整模型的存储容量与单 token 的计算量取决于不同的因素：前者由全部专家决定，后者由选中的专家及共同执行的注意力、共享专家和投影决定。[^source-7]
+
+下面将各模型的参数划分为互不重叠的五类：嵌入与输出、注意力投影、稠密或共享 FFN、路由专家、其余组件。将各类参数相加，就得到模型总参数量。这种分解能直观地展示参数分布，也为解释下一张计算与状态表提供依据。
 
 <!-- MODEL-COMPARISON:START -->
 
@@ -776,7 +778,7 @@
 B_{\max}=\left\lfloor\frac{M_{\mathrm{device}}-M_{\mathrm{weight}}-M_{\mathrm{workspace}}}{M_{\mathrm{state/request}}}\right\rfloor
 $$
 
-分子是扣除固定驻留之后可用于请求的空间，分母是每条请求的状态。每增加一条请求，都要为它分配相应的状态空间；剩余空间放不下完整的一份状态时，就无法再增加请求，因此结果要向下取整。递推与卷积状态同样属于每请求数据，与 KV 一起计入分母。
+分子是扣除固定驻留数据后可用于请求的空间，分母是每条请求的状态大小。每增加一条请求，都要为它分配相应的状态空间；剩余空间放不下完整的一份状态时，就无法再增加请求，因此结果要向下取整。递推与卷积状态同样属于每请求数据，与 KV 一起计入分母。
 
 以 24 GB 显存运行 Qwen3-8B BF16 为例，权重占 $16{,}381{,}470{,}720$ 字节，工作区预留 2 GiB，每条请求的 8K 上下文占 1.125 GiB。可容纳请求数为
 
@@ -784,23 +786,23 @@
 B_{\max}=\left\lfloor\frac{24\times10^9-16{,}381{,}470{,}720-2\times2^{30}}{1.125\times2^{30}}\right\rfloor=4.
 $$
 
-上下文变为 16K 时，每条请求占 2.25 GiB，同一空间只能放两条；变为 4K 时，则能放九条。整数跳变来自剩余空间必须足以容纳完整的一条状态，而不是只够存放其中一部分。
+上下文变为 16K 时，每条请求占 2.25 GiB，同一空间只能放两条；变为 4K 时，则能放九条。整数跳变来自剩余空间必须足以容纳一条请求的完整状态，而不是只够存放其中一部分。
 
 保留多少历史由此成为一个同时影响应用和系统的决定。应用多保留一段材料，模型就能继续参考它；服务需要为每条请求分配更多状态空间。前面的 GQA、MLA 和递推表示进一步改变了保存这些信息的方式。模型结构、上下文组织与并发容量可以放在同一份预算里比较，任务检查则确定哪些方案能够提供所需的回答质量。
 
 再看 DeepSeek-R1-Distill-Llama-70B，其参数量约为 705.5 亿。其 BF16 权重为 141.107 GB，每条 8192-token BF16 KV 为 2.5 GiB。在本例采用的分组量化方案中，让嵌入、输出头和归一化等部分继续使用较高精度，并计入 scale 与打包尾部后，8-bit 方案约为 73.726 GB，4-bit 方案约为 39.500 GB。
 
-4-bit 方案的 39.500 GB 来自低位宽矩阵、仍采用高精度的参数、scale 与打包的共同贡献，比将全部参数一律按半字节计算更大。显存容量为 80 GB（十进制）、固定预留 2 GiB 时，8-bit 方案可放 1 条 8K 请求，4-bit 方案可放 14 条。上下文从 8K 增至 32K 时，每请求 KV 增至四倍，4-bit 方案可同时容纳的请求数降到 3 条。压缩权重腾出的空间可以保存更多请求的上下文，但每条上下文越长，能同时容纳的请求就越少。[^source-23]
+4-bit 方案的 39.500 GB 包括低位宽矩阵、仍采用高精度的参数，以及 scale 与打包开销，比将全部参数一律按半字节计算更大。显存容量为 80 GB（十进制）、固定预留 2 GiB 时，8-bit 方案可放 1 条 8K 请求，4-bit 方案可放 14 条。上下文从 8K 增至 32K 时，每请求 KV 增至四倍，4-bit 方案可同时容纳的请求数降到 3 条。压缩权重腾出的空间可以保存更多请求的上下文，但每条上下文越长，能同时容纳的请求就越少。[^source-23]
 
 ![权重、固定预留和一条请求 KV 的逐项容量。短竖线标出各设备容量；KV 采用 BF16、上下文长度 8192，固定预留为 2 GiB。](ch02/figure-2-9-capacity.svg)
 
 *图 2-32　权重、固定预留和一条请求 KV 的逐项容量。短竖线标出各设备容量；KV 采用 BF16、上下文长度 8192，固定预留为 2 GiB。*
 
-![70B 的 4-bit 方案在同一张 80 GB 设备上，上下文长度从 8K 增至 32K 时可同时容纳的独立请求数。每请求状态增加，使剩余空间容纳的请求数减少。](ch02/figure-2-history-capacity.svg)
-
-*图 2-33　70B 的 4-bit 方案在同一张 80 GB 设备上，上下文长度从 8K 增至 32K 时可同时容纳的独立请求数。每请求状态增加，使剩余空间容纳的请求数减少。*
-
-同样方法也能分析更大的部署。235B 的 BF16 权重约 470 GB，超过八张 24 GB 卡合计的 192 GB，而八张 80 GB 卡合计为 640 GB。后者为状态、工作区与复制留下约 170 GB 总余量；第六章将按具体分工把这笔总量分配到每张设备，得到逐卡预算。
+![70B 的 4-bit 方案在同一张 80 GB 加速卡上，上下文长度从 8K 增至 32K 时可同时容纳的独立请求数。每请求状态增加，使剩余空间容纳的请求数减少。](ch02/figure-2-history-capacity.svg)
+
+*图 2-33　70B 的 4-bit 方案在同一张 80 GB 加速卡上，上下文长度从 8K 增至 32K 时可同时容纳的独立请求数。每请求状态增加，使剩余空间容纳的请求数减少。*
+
+同样方法也能分析更大的部署。235B 的 BF16 权重约 470 GB，超过八张 24 GB 卡合计的 192 GB，而八张 80 GB 卡合计为 640 GB。后者为状态、工作区与复制留下约 170 GB 总余量；第六章将按具体分工把这笔总量分配到每张卡，得到逐卡预算。
 
 容量决定一个实例至少需要多少设备，请求长度与并发数则决定实例需要处理多少工作。更低位宽释放空间，较短上下文允许更多请求同时驻留，批内复用则改变这些请求的执行成本。因此，选择部署方案时需要依次考虑容量、访问量与运算量，第三章将进一步加入请求的到达与时限。
 
@@ -814,9 +816,9 @@
 N_{\max}=\frac{C-M_0-BK}{b_w}.
 $$
 
-先固定服务状态，计算留给权重的空间。取 24 GB 容量、2 GiB 工作区、4 条各 8192 位置的请求，每请求 KV 沿用 Qwen3-8B 的 1.125 GiB。BF16 每参数 2 字节，剩余空间最多容纳约 85.1 亿参数。[^core-calculation]图中先扣除服务状态，再划出权重预算。
-
-改变模型结构时，权重预算与状态预算会一起变化。增加层数或 KV 头数会增大每请求的 $K$，留给权重的空间随之减少；跨设备部署则扩大总容量，同时增加通信。Llama 3 报告中的 405B BF16 推理使用两台、共 16 张 H100，便跨越了单机容量边界。[^codesign]模型规模、状态结构与设备数量因此需要共同选择。
+先确定服务所需的状态大小，计算留给权重的空间。取 24 GB 容量、2 GiB 工作区、4 条各 8192 位置的请求，每请求 KV 沿用 Qwen3-8B 的 1.125 GiB。BF16 每参数 2 字节，剩余空间最多容纳约 85.1 亿参数。[^core-calculation]图中先扣除服务状态，再划出权重预算。
+
+改变模型结构时，权重预算与状态预算会一起变化。增加层数或 KV 头数会增大每请求的 $K$，留给权重的空间随之减少；跨设备部署则扩大总容量，同时增加通信。Llama 3 报告中的 405B BF16 推理使用两台、共 16 张 H100，其所需容量超过一台服务器所能提供的显存。[^codesign]模型规模、状态结构与设备数量因此需要共同选择。
 
 ![图 2-34 从设备反推权重预算。24 GB 中先为 4 条 8K 请求的 KV 和 2 GiB 工作区预留空间，余量给出 BF16 参数上界；图中容量均为十进制 GB。](ch02/figure-2-reverse-budget.svg)
 
@@ -832,16 +834,16 @@
 
 | 模型 | 完整请求矩阵运算量（TFLOPs） | 本次计算采用的实现方式 |
 | --- | ---: | --- |
-| Qwen3-8B | 1.83 | 有效因果矩阵运算量，最后位置输出头 |
+| Qwen3-8B | 1.83 | 有效因果矩阵运算量，只在最后一个位置执行输出头 |
 | DeepSeek V4-Flash | 3.40 | 主干有效注意力／专家工作，参考索引计算 |
 | DeepSeek V4-Pro | 12.72 | 独立 Pro 配置，按该配置中各类注意力层的数量和条目选择上限计算 |
 | Kimi K3 | 27.13 | 展开 MLA 与块式 KDA 算法 |
 
 这张表中的 Kimi K3 使用展开 MLA：上下文先恢复为各头的 $K$、$V$，再执行交互。前面的紧凑 MLA 则把上投影移到查询和汇总一侧。完整请求沿同一条选定路径累计 prefill 与三次 decode，上下文从 128 增到 131；这一路径的矩阵形状与状态表示共同决定各项运算量。
 
-从一次调用扩展到完整请求之后，输入和输出的作用更加清楚。增加 $P$，主要增加一次输入处理的工作量，以及后续解码开始时的上下文长度；增加 $G$，则反复执行投影、专家与输出头，并多次读取上下文。长输入、短输出与短输入、长输出由此形成不同的资源组成，第三章将分析这两类请求持续到达时的系统负载。
-
-> **练习 2-8〔延伸〕：从多轮输入重建调用**
+从一次调用扩展到完整请求之后，输入和输出的作用更加清楚。增加 $P$，主要增加一次输入处理的工作量，以及后续解码开始时的上下文长度；增加 $G$，则反复执行投影、专家与输出头，并多次读取上下文。长输入、短输出与短输入、长输出由此对各类资源提出不同的需求，第三章将分析这两类请求持续到达时的系统负载。
+
+> **练习 2-8〔延伸〕：前缀复用与输入改写如何改变模型调用量**
 >
 > 一轮输入总长 1443 个 token，其中前 1392 个命中缓存，返回 128 个 token。求 $S,P,G,n_d$ 的值、请求结束时的上下文长度，以及本轮新增 KV 的大小。比较完全重算与复用缓存时，投影行数、有效因果位置对数分别减少多少。若在第 501 个输入位置修改了内容，重新求最长可复用前缀与新输入长度。
 
@@ -853,9 +855,9 @@
 
 *图 2-35　生成四个输出的调用顺序。prefill 处理 128 个输入并产生首输出，随后三次 decode 各将前一输出送回模型；最终保留 131 个位置。*
 
-![同一 128 输入、四输出请求的矩阵运算量。模型执行范围与缓存路径见本节题设；这是逐调用累计的计算量。](ch02/figure-2-request-compute.svg)
-
-*图 2-36　同一 128 输入、四输出请求的矩阵运算量。模型执行范围与缓存路径见本节题设；这是逐调用累计的计算量。*
+![同一个输入 128 个 token、输出四个 token 的请求的矩阵运算量。模型执行范围与缓存路径见本节题设；这是逐调用累计的计算量。](ch02/figure-2-request-compute.svg)
+
+*图 2-36　同一个输入 128 个 token、输出四个 token 的请求的矩阵运算量。模型执行范围与缓存路径见本节题设；这是逐调用累计的计算量。*
 
 > **练习 2-9〔延伸〕：根据容量、计算需求与任务质量选择模型**
 >
@@ -865,13 +867,13 @@
 
 ## 谬误与陷阱
 
-**参数更多，每 token 计算就一定更多。** 总参数决定完整模型的容量；MoE 的选中专家、注意力机制的类型和调用形状决定当前工作。对照表 2-A 与资源表，分别解释这两种大小。
-
-**KV 缩小四倍，注意力计算就缩小四倍。** GQA 共享上下文表示，查询头仍分别评分；MLA 改变上下文宽度，也改变上投影的执行位置。先找被改变的矩阵与状态，再计算其余工作。
-
-**状态不随上下文增长，状态访问就可以忽略。** 固定递推矩阵仍需更新；prefill 还可能产生临时块状态。容量、累计访问与峰值工作区分别回答不同问题。
-
-**同 token 数就是公平的模型能力比较。** 输入和输出的 token 数适合比较资源结构；同文本任务还需固定评分、质量与执行条件，并允许不同分词器产生不同长度。
+**误区：参数更多，每个 token 的计算量就一定更大。** 总参数决定完整模型的容量；MoE 的选中专家、注意力机制的类型和调用形状决定当前工作。对照表 2-A 与资源表，分别解释参数量和计算量由哪些因素决定。
+
+**误区：KV 容量降至四分之一，注意力计算量也会同比下降。** GQA 共享上下文表示，查询头仍分别评分；MLA 改变上下文宽度，也改变上投影的执行位置。先找被改变的矩阵与状态，再计算其余工作。
+
+**误区：状态大小固定，访问开销就可以忽略。** 固定递推矩阵仍需更新；prefill 还可能产生临时块状态。容量、累计访问与峰值工作区分别回答不同问题。
+
+**误区：输入 token 数相同，就能公平比较模型能力。** 输入和输出的 token 数适合比较资源结构；同文本任务还需固定评分、质量与执行条件，并允许不同分词器产生不同长度。
 
 ## 本章练习与复现
 
@@ -888,7 +890,7 @@
 | 已知轨迹字段代入 | `python3 calculations/calc.py trace-resource-bridge --format md` |
 | 四模型请求比较 | `python3 calculations/calc.py request-model-comparison --format md` |
 
-本章配图、计算数据与重建方法见[配图目录](ch02/README.md)。下列资料介绍各模型的结构与设计。
+本章配图、计算数据与重建方法见[配图目录](ch02/README.md)。章末资料介绍了各模型的结构与设计。
 
 <a id="model-matrix-tables"></a>
 
@@ -986,7 +988,7 @@
 
 **表 2-4　DeepSeek V4-Flash：选择性上下文访问与专家计算**
 
-43 层包含 2 个纯窗口层、21 个 CSA 层和 20 个 HCA 层；每 token 选 6 个路由专家，另有一个共享专家。$A$ 是该层跨全部请求的主注意力有效位置对数，$I$ 是索引实际扫描的位置对数，两者均不含头数。分组输出投影的八组处理八份不同切片。CSA 的压缩投影先产生包含重叠分支的 1024／256 维表示，再形成 512／128 维的主注意力使用的上下文表示与索引条目。
+43 层包含 2 个纯窗口层、21 个 CSA 层和 20 个 HCA 层；每 token 选 6 个路由专家，另有一个共享专家。$A$ 是该层跨全部请求的主注意力有效位置对数，$I$ 是索引实际扫描的位置对数，两者均不含头数。分组输出投影的八组处理八份不同切片。CSA 的压缩投影先产生包含重叠分支的 1024／256 维表示，再分别形成 512 维的主注意力上下文表示和 128 维的索引条目。
 
 **模型入口**
 
@@ -1038,11 +1040,11 @@
 
 累计时，用各行公式乘最后一列的层数；路由专家再对实际接收行数 $t_e$ 求和。末端归一化执行一次，层内归一化、激活、位置处理和状态更新沿相应分支执行。
 
-这张表解释了为什么“每步只选少量上下文”不等于总工作固定：主注意力使用 $A$，索引扫描使用 $I$，压缩投影则对新输入持续执行。三者是不同的增长项。[^source-21]
+这张表解释了为什么“每步只选少量上下文”不等于总工作固定：主注意力使用 $A$，索引扫描使用 $I$，压缩投影则对新输入持续执行。这三类工作量的增长规律不同。[^source-21]
 
 **表 2-5　Kimi K3：KDA、紧凑 MLA 与潜空间专家**
 
-93 层包含 69 个 KDA 层、24 个 MLA 层；首层使用稠密 FFN，其余层从 896 个路由专家中选 16 个，并执行合计宽度 $2\times3072=6144$ 的共享分支。表内 MLA 采用紧凑代数路径，96 路小投影分别使用每头参数。共享专家保持 7168 维主干空间，路由专家才进入 3584 维潜空间。
+93 层包含 69 个 KDA 层、24 个 MLA 层；首层使用稠密 FFN，其余层从 896 个路由专家中选 16 个，并执行合计宽度 $2\times3072=6144$ 的共享分支。表内 MLA 采用直接对潜变量计算注意力的紧凑路径，96 路小投影分别使用每头参数。共享专家直接处理 7168 维主干向量，路由专家才进入 3584 维潜空间。
 
 **模型入口**
 
@@ -1069,7 +1071,7 @@
 | MLA：查询下投影       | $m\times7168\to m\times1536$  | $7168\times1536$      | $2\times m\times7168\times1536$             |    24 |
 | MLA：查询上投影       | $m\times1536\to m\times18432$ | $1536\times18432$     | $2\times m\times1536\times18432$            |    24 |
 | MLA：潜变量及额外分支    | $m\times7168\to m\times576$   | $7168\times576$       | $2\times m\times7168\times576$              |    24 |
-| MLA：将键上投影吸收到查询侧 | $m\times128\to m\times512$    | $128\times512$，共 96 个 | $96\times 2\times m\times128\times512$      |    24 |
+| MLA：将键的上投影与查询变换合并 | $m\times128\to m\times512$    | $128\times512$，共 96 个 | $96\times 2\times m\times128\times512$      |    24 |
 | 紧凑 MLA：潜空间评分与汇总 | $96$ 头；评分宽 $576$，汇总宽 $512$    | 使用紧凑的上下文表示                | $2\times96\times(576+512)N_{\mathrm{pair}}$ |    24 |
 | MLA：汇总后恢复值表示    | $m\times512\to m\times128$    | $512\times128$，共 96 个 | $96\times 2\times m\times512\times128$      |    24 |
 | MLA：输出门控        | $m\times7168\to m\times12288$ | $7168\times12288$     | $2\times m\times7168\times12288$            |    24 |
@@ -1100,7 +1102,7 @@
 
 累计时，用各行公式乘最后一列的层数；路由专家再对实际接收行数 $t_e$ 求和。末端归一化执行一次，层内归一化、激活、位置处理和状态更新沿相应分支执行。
 
-阅读 Kimi K3 的表时，需要分别看注意力和前馈网络的层数：69 层采用 KDA，24 层采用 MLA；第一层采用稠密 FFN，后 92 层采用潜空间 MoE。共享专家保持 7168 维主干输入，路由专家先经过降维投影，以 3584 维输入工作。表中的层数、宽度与 $t_e$ 因而分别控制重复次数、单次尺寸与实际调用量。[^source-22]
+阅读 Kimi K3 的表时，需要分别看注意力和前馈网络的层数：69 层采用 KDA，24 层采用 MLA；第一层采用稠密 FFN，后 92 层采用潜空间 MoE。共享专家保持 7168 维主干输入，路由专家先经过降维投影，处理 3584 维输入。表中的层数、宽度与 $t_e$ 因而分别控制重复次数、单次尺寸与实际调用量。[^source-22]
 
 [^comparison-data]: [四模型比较数据](ch02/model-comparison.json)；[生成程序](ch02/compare_models.py)；[Qwen3.6 末位置输出头计算](../calculations/results/qwen36-prefill-last-head.md)。
 
@@ -1154,18 +1156,18 @@
 
 [^architecture-motivation]: [DeepSeek V4 技术报告](../references/text/deepseek-v4.txt)第 2.2 节说明 mHC 的多路表示与稳定传播，第 2.3 节说明 CSA／HCA；[Kimi K3 技术报告](../references/text/kimi-k3.txt)说明 Attention Residuals 沿深度选择信息的动机与块式实现。
 
-[^codesign]: [MQA 原文](../references/text/mqa.txt)摘要；[GQA 原文](../references/text/gqa.txt)摘要；[Llama 3 报告](../references/text/llama3.txt)第 6.1 节。容量示例是本书按固定服务状态推导的教学预算，不是这些模型的历史设计记录。
-
-[^core-calculation]: 本章新增条件比较的[逐项复算](../calculations/results/core-principles.json)与[计算程序](../calculations/core_principles.py)。
-
-[^v41-case]: [DeepSeek V4.1 官方技术报告](../calculations/sources/deepseek-v4.1-flash/DeepSeek_V41_Tech_Report.pdf)，第 1、2、3 节与第 6 节；[跨章会话的固定条件与复算](../calculations/results/v41-throughline.json)。架构、格式和部署机制按报告区分，教学时间假设不作实测。
+[^codesign]: [MQA 原文](../references/text/mqa.txt)摘要；[GQA 原文](../references/text/gqa.txt)摘要；[Llama 3 报告](../references/text/llama3.txt)第 6.1 节。容量示例按题设的服务状态推导。
+
+[^core-calculation]: 本章条件比较的[逐项复算](../calculations/results/core-principles.json)与[计算程序](../calculations/core_principles.py)。
+
+[^v41-case]: [DeepSeek V4.1 官方技术报告](../calculations/sources/deepseek-v4.1-flash/DeepSeek_V41_Tech_Report.pdf)，第 1、2、3 节与第 6 节；[跨章会话的固定条件与复算](../calculations/results/v41-throughline.json)。
 
 [^v41-candidate]: [DeepSeek V4.1 技术报告](../calculations/sources/deepseek-v4.1-flash/DeepSeek_V41_Tech_Report.pdf)，第 2.3.2 节 Hierarchical Sparse Indexer 使用 candidate pool 和 candidate positions；Figure 5 展示共享候选池与后续 Top-K 的区别。
 
 ## 本章小结
 
-设计模型时，也可以先从设备容量中扣除服务所需的状态和工作区，再比较剩余空间能够容纳的模型规模与结构。MQA、GQA 与稀疏激活说明，提高系统效率也是选择模型结构的重要依据；容量上界仍需经过质量、时延与成本检验，才能成为设计决定。
-
-本章把第一章的三个资源量展开为可计算的对象。矩阵尺寸给出权重容量与线性运算，查询—上下文位置对给出注意力交互，缓存的保存时间与模型调用次数决定状态的存储量和读取量，专家分派决定每批使用多少份权重。面对新条件，先指出它改变了哪一个对象，再重新代入相应公式：增加 batch、延长上下文、扩大专家集合和增加选中专家数，会作用于不同的项。
-
-读完本章，应能从新配置列出一次前向、一次 decode 和完整生成的需求，并解释长度或 batch 改变时哪些项增长。第三章将继续加入到达与工具依赖，使这些需求成为随时间变化的负载。
+设计模型时，也可以先从设备容量中扣除服务所需的状态和工作区，再比较剩余空间能够容纳的模型规模与结构。MQA、GQA 与稀疏激活说明，提高系统效率也是选择模型结构的重要依据；在容量允许的范围内，还要比较质量、时延与成本，才能确定设计方案。
+
+本章把第一章的三个资源量展开为可计算的对象。矩阵尺寸决定权重容量与线性运算量，查询与上下文的位置对数决定注意力交互的运算量，缓存的保存时间与模型调用次数决定状态的存储量和读取量，专家分派决定每批使用多少份权重。面对新条件，先指出它改变了哪一个对象，再重新代入相应公式：增加 batch、延长上下文、扩大专家集合和增加选中专家数，会作用于不同的项。
+
+读完本章，应能根据新的模型配置，列出一次前向、一次 decode 和完整生成各自的资源需求，并解释长度或 batch 改变时哪些项增长。第三章将进一步考虑请求的到达过程与工具调用的依赖关系，使这些需求成为随时间变化的负载。
