# 第 5 章《算子与运行时》翻译腔清扫（2026-09-11）

章节文件：`manuscripts/05-算子与运行时.md`，共 942 行，全文逐行通读。

净改动共 30 行（每行一处改动），其中句式类（翻译腔）16 处，体例类（汉字数字与上下文阿拉伯数字不一致、多余空格）14 处。数字体例的改法沿用第 4 章通读的既有做法（如"六十四 tick"→"64 tick"），所有数值、单位、量级均未改变。省略号表示只截取改动附近的文字。

## 句式类：翻译腔修改（16 处）

| 修改前 | 修改后 |
|---|---|
| …就能判断减少搬运省下的时间，能否抵消同时执行的块数减少造成的损失。 | …就能判断减少搬运省下的时间，能否抵消同时执行的块数变少带来的损失。 |
| …输出块太小，同一份输入可能为不同输出块反复搬运；块太大，又会占用更多局部存储… | …输出块太小，同一份输入可能要为不同的输出块反复搬运；块太大，又会占用更多局部存储… |
| …考虑一个 32×32 的 FP32 暂存块，分成 32 个可独立服务读取请求的存储体，称为 bank，每个 bank 每轮提供一个 32-bit 字，bank 号为字地址对 32 取模。 | …考虑一个 32×32 的 FP32 暂存块，它的存储分成 32 个存储体，称为 bank；每个 bank 可独立服务读取请求，每轮提供一个 32-bit 字，bank 号就是字地址对 32 取模的结果。 |
| …可以独立安排 1024 组；对单请求每次生成一个 token 的解码步骤（decode），同一种分配只产生一组，加速器的大量执行资源会空闲。 | …可以独立安排 1024 组；而在解码（decode）阶段，单请求每次只生成一个 token，同样的分法只产生一组，加速器的大量执行资源就会空闲。 |
| …单卡中，这些块由线程块、共享内存和局部归约协作；分到多卡后… | …单卡中，这些块的计算由线程块、共享内存和局部归约协作完成；分到多卡后… |
| …激活链计算 $Z=\operatorname{SiLU}(G)\odot U$，再将 Z 交给降回隐藏维度的 down 投影。 | …激活链计算 $Z=\operatorname{SiLU}(G)\odot U$，再把 Z 交给 down 投影，把结果降回隐藏宽度。 |
| …在相同注意力范围内，该算的查询与键的匹配、值的加权仍然要做；改变的是计算顺序… | …在相同注意力范围内，查询与键的匹配、值的加权仍然都要算；改变的是计算顺序… |
| …配套实验比较了 PyTorch 中同一个注意力接口的两种执行实现。 | …配套实验比较了 PyTorch 中同一个注意力接口的两种实现。 |
| …使用 CUDA Graph 将一组 GPU 操作预先记录并重复执行，以减少逐次由主机提交的干扰。 | …使用 CUDA Graph 将一组 GPU 操作预先记录并重复执行，以免每次由主机提交造成干扰。 |
| *图 5-28：…数据来自与前图相同实验的单独标记的采集区间。* | *图 5-28：…数据来自与前图相同的实验中单独标记的采集区间。* |
| 如果专用加速器把模型执行从 100 μs 缩短到 10 μs，而主机每轮准备仍需 20 μs，在可重叠、无其他依赖的稳定流水中，步间隔的下界就… | 如果专用加速器把模型执行从 100 μs 缩短到 10 μs，而主机每轮准备仍需 20 μs，那么在可重叠、无其他依赖的稳定流水中，步间隔的下界就… |
| Amdahl 关系按串行阶段相加计算时间。 | 在 Amdahl 关系中，时间按串行阶段相加。 |
| …保存同一份数据所需的容量，与执行期间对它的累计访问量可以明显不同。 | …保存同一份数据所需的容量，与执行期间对它的累计访问量可能相差很大。 |
| 除了数据从哪里取得，程序是否真正省去了计算也会改变开销。 | 改变开销的不只是数据从哪里取得，程序是否真正省去了计算也有影响。 |
| …这里的 eager 表示由主机沿程序过程逐项提交加速器工作。 | …这里的 eager 表示由主机按程序顺序逐项提交加速器工作。 |
| 算子编排优化系统 Korch 提供了另一个有启发的例子：在一个历史案例中，子图原本由三个 kernel 执行… | 算子编排优化系统 Korch 提供了另一个有启发性的例子：其中一个历史案例里，子图原本由三个 kernel 执行… |

## 体例类：数字用法与上下文统一、多余空格（14 处）

| 修改前 | 修改后 |
|---|---|
| *图 5-2：…两个槽的交替使用在 第 5.3.2 节展开。* | *图 5-2：…两个槽的交替使用在第 5.3.2 节展开。* |
| *图 5-20：…外层选输出块，创建十六 KiB 累加器…* | *图 5-20：…外层选输出块，创建 16 KiB 累加器…* |
| *图 5-22：一行分成两个块，后一块中的十决定整行尺度…* | *图 5-22：一行分成两个块，后一块中的 10 决定整行尺度…* |
| *图 5-23：…保存 FP8 结果后十二列块合计重读 192 MiB…* | *图 5-23：…保存 FP8 结果后 12 个列块合计重读 192 MiB…* |
| …相应输出累加器也从 128 列扩大为 1536 列，需要十二倍空间。 | …相应输出累加器也从 128 列扩大为 1536 列，需要 12 倍空间。 |
| 图 5-27 取三次 FFN 执行作对照。 | 图 5-27 取 3 次 FFN 执行作对照。 |
| *图 5-27：普通提交的三次 FFN 实测。…十八次内核启动对应十八个加速器 kernel。* | *图 5-27：普通提交的 3 次 FFN 实测。…18 次内核启动对应 18 个加速器 kernel。* |
| *图 5-28：三次 FFN 融合激活链后，主机启动内核十五次，加速器相应执行十五个内核。…* | *图 5-28：3 次 FFN 融合激活链后，主机启动内核 15 次，加速器相应执行 15 个内核。…* |
| *图 5-29：三次 graph launch 对应十八个加速器 kernel。…* | *图 5-29：3 次 graph launch 对应 18 个加速器 kernel。…* |
| *图 5-30：先融合再重放，主机发起三次图执行，加速器执行十五个 kernel。…* | *图 5-30：先融合再重放，主机发起 3 次图执行，加速器执行 15 个 kernel。…* |
| *图 5-32：…普通方式 40 μs；图的两 MiB 输入约 27 μs，十六 MiB 输入约 44 μs。…* | *图 5-32：…普通方式 40 μs；图的 2 MiB 输入约 27 μs，16 MiB 输入约 44 μs。…* |
| **例 5-12：…** 取十次 FFN 为一组：8 次为 256 行… | **例 5-12：…** 取 10 次 FFN 为一组：8 次为 256 行… |
| *图 5-33：…十次调用合计实际 5632 行，执行 8192 行。* | *图 5-33：…10 次调用合计实际 5632 行，执行 8192 行。* |
| …需要 $600/(5\times10^{-6})=1.2\times10^8$ 次调用。每秒一万次约需 3.3 小时，每秒一百次约需 14 天。 | …需要 $600/(5\times10^{-6})=1.2\times10^8$ 次调用。每秒 10,000 次约需 3.3 小时，每秒 100 次约需 14 天。 |

## 注意到但未改动的句子

- 5.6.2 节"V4.1 后续解码索引器只访问候选池，初始索引仍需遍历全局"："后续解码索引器"断句不清（是"后续解码"过程中索引器的行为，还是某个组件名）。可改为"V4.1 在后续解码中，索引器只访问候选池"，但因不确定技术含义是否指特定组件，未改动，提请作者确认。
- 5.2.2 节"输入复用增多，输出累加器也增长得更快"："增长得更快"的比较对象隐含（复用线性增长、存储平方增长），通顺但不够显豁；为避免改动技术表述，未动。
- 5.3.3 节连续两段分别以"要在处理完一个块后丢弃其中的分数，就需要保留几个统计量"和"要丢弃已经处理过的分数，需要保留三个量"开头，语义重复；疑为行文安排（先讲必要性、再给具体清单），未合并。

## 存疑句复核结论（2026-09-11，内容核对）

- 5.6.2 节存疑句已改："V4.1 后续解码索引器只访问候选池，初始索引仍需遍历全局" → "V4.1 后续层的索引只访问候选池，初始索引仍需遍历全局"。依据：第 2 章（`manuscripts/02-模型架构.md:512`）对同一机制的权威描述——解码器首个 Full 层扫描全局形成候选池，后续 Reindex 层只在池内选取；技术含义不变，仅消除断句歧义。
