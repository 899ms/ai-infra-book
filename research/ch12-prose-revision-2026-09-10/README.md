# 第十二章数字论证与插图修订

2026-09-10。范围为第十二章正文和配图。修订前文件保存于 `before.tar.gz`，未改其他章节正文及原计算、survey、实验数据。

## 正文

保留六节、20 小节和八项练习，按“同一条件下比较—算出收益—改变条件—提出下一项问题”重写。主要推导包括上传与模型加速、截图压缩的最大有利轮数、完整视觉特征传输、状态迁移摊销、窗口限制、ACK 反馈、固定空口开销、共同故障、调优基线及地域部署。

- 截图：105 秒降至约 83 秒；增加至 38 轮则约 106 秒，最大有利整数轮数为 37。
- 迁移：约 7.7 秒准备，每轮节省 0.4 秒，20 轮起才能回本。
- Queqiao：新连接约 1.19／0.30 秒；调优保持连接均约 0.24 秒。未舍入值和统计量区别进入注释。
- 地域：云上行约 7.1 Mbit/s 达到期限，约 9.1 Mbit/s 才比附近更快；8 与 10 Mbit/s 分别需 43 与 39.8 秒。
- 状态驻留：从节省的 FLOPs 推导费用，再除以每秒驻留费，得约 120 秒边界。

配置形状、包数和整数边界保持精确；性能与费用精度跟随判断用途。正文脚注及配套证据保留精确统计、版本与验收条件。

## 插图

| 图 | 旧组成 | 本次主图 |
|---|---|---|
| 12-1 | 阶段预算与敏感性两面板 | 上行与三种方案总时间 |
| 12-2 | 三类任务流程及音频小图 | 晚到块与后续播放 |
| 12-3 | 三类交接流程及图片／EC／KV | 图片与完整 EC 发送时间 |
| 12-4 | 连接、发送调度、交付 | 同一轨迹下的交付依赖 |
| 12-5 | 空口、双路径、故障结果 | 短帧也需支付的固定交换开销 |
| 12-6 | 不同纵轴请求柱图及设备实验 | 共用纵轴的两组基线比较 |
| 12-7 | 带宽曲线与费用柱图 | 达到期限与更快的两个边界 |

全部主图均为单坐标关系，图内无图号。未绘制的数据仍可在 figure-data 和配套证据中复查，明确注明实际绘制范围。

## 来源与检查

模型和科学数据来源锁保持原值。第六章生成器仅为历史排版参考，旧哈希迁入历史字段，实际使用的 KaTeX 文件及字体进入来源锁。本次发现大纲已有新增实验摘要，复核六节与 20 小节标题一致后更新其锁，原哈希保留在 `outline_review`。新增摘要采用不同条件，未将其结果混入正文教学对照。

重建 SVG／PNG／PDF 和离线 HTML；检查结构、算式、链接、来源与产物哈希、公式及图内标签，并通过桌面／手机浏览器检查。最新结果见 `manuscripts/ch12/validation.json`、`math-validation.json`、`browser-validation.json`。七图总览经过目视检查。
