# 全书中文表达修订

逐章阅读十二章正文，修改标题、段落、图注、表头和习题中的生硬表达。保留原有章节结构、技术含义和量化分析方法，使叙述具体、连贯，并保持教材的肯定语气。

## 主要修改

- 第二章“自回归生成与资源核算”改为“自回归生成的资源消耗计算”；将 KV 缓存的作用写为“通过保存已经算出的结果，减少后续生成中的重复计算”。
- 将“资源时间”“模块核算”“延迟收支”等含义不够明确的表达，按上下文改为计算时间、读取时间、资源消耗计算，以及节省和增加的时间。
- 例题和习题标题直接说明所分析的问题；正文具体说明对象、执行动作、条件和结果，减少抽象名词堆叠与翻译腔。
- 保留必要的计算假设、质量条件及实测与估算的区别，将重复的否定和资料范围声明融入相应机制的说明。
- 保留第一章的主线：模型训练和推理提供完整的计算、状态与依赖信息，支持跨层共同设计；后续章节通过复用、分块、状态恢复、调度和任务成本的计算展开这一主线。
- 同步修正发现的图号引用，并同步大纲中的相关标题。

## 修改记录与检查

`ch01-edits.json` 至 `ch12-edits.json` 保存逐项修改前后的文字与替换次数。记录包括本轮修订过程中的再次润色，不能把记录条数视为最终差异行数。工作区中同时进行的模型概念复核与文字修改已保留。

`validation.json` 记录各章修改数量、行内公式与链接目标检查、最终正文哈希和 PDF 验证结果。公式和链接检查针对这些编辑记录，独立于工作区中其他技术修订。

PDF 使用独立临时目录编译，避免与其他构建互相覆盖。检查完整章节、字体嵌入、页面边界、缺字、溢出及引用警告，并渲染检查目录、第二章修改段落、第四章分页和后部章节的长标题。

## 最终排版结果

本轮记录 564 项修改、581 次文字替换。全书 PDF 为 407 页、409 幅图，第二章样张为 48 页、36 幅图。字体嵌入、章节完整性、页面边界、缺字、溢出和引用检查全部通过；第四章图 4-3 后的面积与功耗算例另起一页，避免脚注与公式造成纵向溢出。

PDF 正文对应提交 `21b17188` 的十二章内容，逐章哈希已核对一致。之后其他会话继续修改的工作区正文保留原样，未作为此次已验证 PDF 的来源。
