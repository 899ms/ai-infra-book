# Independent host-capacity sensitivity

Repeat the exact12 Agent targets and per-turn3584-token pressure sequences from agent-host-capacity, holding HBM max_total_tokens4096 and varying only hicache_ratio1.01/1.25. The prior4096/ratio2 run remains the separately recorded comparison point; do not rerun or relabel it. Native host implementation rounds to16-token pages and requires host pool larger than device pool; source sizing formula is recorded. No storage backend. All target/pressure/target calls force one token, fixed seed908. Fresh engine per ratio, preserved output and native cache-level counters. Compare output IDs and actual host-vs-recomputation behavior; do not assert a desired hit pattern before measurement. One pass per setting, not representative p95 or Agent quality.
