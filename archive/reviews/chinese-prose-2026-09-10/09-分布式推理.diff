--- before/09-分布式推理.md
+++ manuscripts/09-分布式推理.md
@@ -2,7 +2,7 @@
 
 第八章讨论了怎样通过批处理、请求调度、缓存和推测解码，提高给定设备组合的推理效率。这些方法既适用于单卡，也适用于多卡协同执行的实例。本章进一步研究计算与状态放在哪里：不同设备分别承担哪些阶段和算子，权重与 KV 怎样分布，执行位置之间怎样传输数据、分配工作并处理故障。
 
-本章用 **P** 表示预填充（prefill），即处理已知输入并生成首个输出；用 **D** 表示解码（decode），即将新生成的 token 送回模型，继续产生输出。**PD 分离**把这两个阶段交给不同资源。**AF 分离**把每层的注意力（attention）与前馈网络（FFN，包括专家网络）交给不同资源。**KV** 是上下文位置的键和值，保存了继续注意力计算所需的状态。
+本章用 **P** 表示预填充（prefill），即处理已知输入并生成首个输出；用 **D** 表示解码（decode），即将新生成的 token 送回模型，继续产生输出。**PD 分离**把这两个阶段交给不同资源。**AF 分离**把每层的注意力（attention）与前馈网络（FFN，包括专家网络）交给不同资源。**KV** 是上下文位置的键和值，保存了继续进行注意力计算所需的状态。
 
 增加完整副本，可以让不同资源组分别处理请求。将 prefill 与 decode 分开，可以为两个阶段分别配置设备；将 attention 与专家计算分开，可以利用不同设备的计算和存储能力；共享 KV 则让请求使用其他执行位置留下的状态。这些组织方式可能发生在一个推理实例内部，也可能连接多个独立调度的服务池。下面直接分析执行位置、调度关系和数据流向。
 
@@ -18,7 +18,7 @@
 
 下面逐一画出四种组织方式。前两种都执行完整模型，但一个八卡协作组共同处理同一批请求，多个完整副本则各自接收请求。后两种进一步改变分工：PD 按生成阶段划分，AF 按层内算子划分。各组织方式由相应的调度器管理其负责的计算。
 
-![多卡协作完整副本和计算分工](ch09/figure-9-1-organization.svg)
+![多卡协作、完整副本和计算分工](ch09/figure-9-1-organization.svg)
 
 *图 9-1：八张卡共同执行一个完整模型实例，接收同一批请求。卡号表示协作组成员。*
 
@@ -42,7 +42,7 @@
 \lambda d_r < n_r,
 $$
 
-其中 $\lambda$ 是到达率。若多个操作使用同一资源，应先将它们的资源占用时间相加；若操作分别使用相互独立的资源，则为每种资源单独列出约束。资源占用时间按“资源数 × 占用秒数”计算，单位为资源秒。例如，每秒到达四个请求，每个占用 0.5 个资源秒，就需要两个持续工作的资源单元。若恰好只有两个单元，额外到达的请求就会一直积压，因为两个单元都在忙于处理后续请求；增加可用资源，才能处理突发请求。
+其中 $\lambda$ 是到达率。若多个操作使用同一资源，应先将它们的资源占用时间相加；若操作分别使用相互独立的资源，则为每种资源单独列出约束。资源占用量按“资源数 × 占用秒数”计算，单位为资源秒。例如，每秒到达四个请求，每个占用 0.5 个资源秒，就需要两个持续工作的资源单元。若恰好只有两个单元，额外到达的请求就会一直积压，因为两个单元都在忙于处理后续请求；增加可用资源，才能处理突发请求。
 
 设每个请求在某类副本上的 prefill 和 decode 分别占用 $d_P$、$d_D$ 秒，两个阶段在该副本上串行执行，那么 $N$ 个相同共置副本的请求率上界为
 
@@ -60,12 +60,12 @@
 
 | 部署方式 | 希望获得的收益 | 新增的主要约束 |
 | --- | --- | --- |
-| 增加完整副本 | 承接更多独立请求，分散排队 | 模型复制、缓存分散、启动及保持实例运行 |
+| 增加完整副本 | 处理更多独立请求，分散排队 | 模型复制、缓存分散、启动及保持实例运行 |
 | PD 分离 | 分别配置两个阶段的能力 | KV 交接、两端同时占用内存、两个队列 |
-| AF 分离 | 让算子使用适合的计算与存储资源 | 逐层激活往返、同步、流水平衡 |
+| AF 分离 | 让算子使用适合的计算与存储资源 | 逐层激活往返、同步、流水线各阶段的负载均衡 |
 | 共享 KV | 跨实例复用上下文，减少重复 prefill | 缓存标识、读取、就绪通知、失效与空间释放 |
 
-部署选择从当前负载的资源需求开始。如果两个阶段的资源需求相近，完整副本已经能够均衡服务，分离可能只增加交接。如果许多请求共享长前缀，跨实例缓存可能比进一步拆分计算更有价值。分别计算资源占用和交接时间，就能判断当前应增加副本、分离阶段还是共享状态。
+部署选择从当前负载的资源需求开始。如果两个阶段的资源需求相近，完整副本已经能够均衡服务，分离可能只会增加交接开销。如果许多请求共享长前缀，跨实例缓存可能比进一步拆分计算更有价值。分别计算资源占用和交接时间，就能判断当前应增加副本、分离阶段还是共享状态。
 
 ### 9.1.3 同一请求的执行路径与状态保存时间
 
@@ -77,17 +77,17 @@
 
 以本章的 Qwen3-8B 为例，一份包含 8192 个位置的 BF16 上下文 KV 占 1.125 GiB。工具执行 10 秒时，GPU 可以处理其他请求，但这份上下文 KV 仍需占用 1.125 GiB 内存，容量与占用时长的乘积为 11.25 GiB·s；十个这样的会话同时等待，就占 11.25 GiB。计算暂停后，状态仍需保留，这正是共享缓存和换出策略要解决的问题。
 
-这条请求若随后返回 129 个 token，P 先产生第一个输出，128 次 decode 再处理前 128 个生成位置。此时连续 KV 覆盖 $8192+128=8320$ 个位置，最后一个返回 token 尚未送回模型。下一轮在这 8320 个位置之后继续，先处理这个尾 token 和新增输入。因此，已返回的输出序列比 KV 所覆盖的序列多一个 token；恢复生成时，需要从这个位置继续计算。
+这条请求若随后返回 129 个 token，P 先产生第一个输出，128 次 decode 再处理前 128 个生成位置。此时连续 KV 覆盖 $8192+128=8320$ 个位置，最后一个返回 token 尚未送回模型。下一轮在这 8320 个位置之后继续，先处理这个尚未写入 KV 的末尾 token 和新增输入。因此，已返回的输出序列比 KV 所覆盖的序列多一个 token；恢复生成时，需要从这个位置继续计算。
 
 ## 9.2 Prefill–Decode 分离与资源配比
 
 ### 9.2.1 Prefill 与 Decode 为什么需要分离
 
-Prefill 可以在一次调用中处理许多新位置，较大的矩阵往往有较高的权重复用。在 decode 阶段，每个请求每步通常只处理一个新位置，需要反复读取权重与上下文状态。两者混合执行时，一个较长的 prefill 可能推迟已有请求的下一个输出；过度优先 decode 又会延长新请求的等待。
-
-第 8.2 节已经用分块 prefill 缩短了长输入对 decode 的阻塞。本节保持相同模型和请求，进一步改变执行位置。PD 分离进一步把两个阶段放入不同的服务池，使它们拥有独立的队列、批处理和资源配比。DistServe 与 Splitwise 是研究 P、D 分离部署的推理系统。同构设备可通过独立队列减少阶段间的调度干扰，异构设备还可以利用各自更擅长的阶段。[^pd]
-
-PD 将 prefill 和 decode 分配给两个服务池，但两个池都要执行完整的前向计算。Qwen3-8B 的 P 处理 8192 个输入位置，D 随后调用 128 次；每次都经过 attention 和 FFN。于是 P、D 都需要相应权重，而上下文 KV 还要从 P 交给 D。独立批处理带来的收益，首先要补偿这次新增交接。
+Prefill 可以在一次调用中处理许多新位置，较大的矩阵计算往往能更充分地复用权重。在 decode 阶段，每个请求每步通常只处理一个新位置，需要反复读取权重与上下文状态。两者混合执行时，一个较长的 prefill 可能推迟已有请求的下一个输出；过度优先 decode 又会延长新请求的等待。
+
+第 8.2 节已经用分块 prefill 缩短了长输入对 decode 的阻塞。本节保持相同模型和请求，进一步改变执行位置。PD 分离把两个阶段放入不同的服务池，使它们拥有独立的队列、批处理和资源配比。DistServe 与 Splitwise 是研究 P、D 分离部署的推理系统。同构设备可通过独立队列减少阶段间的调度干扰，异构设备还可以利用各自更擅长的阶段。[^pd]
+
+这种分工像是先由一个人读完材料，再交给另一个人继续处理：接手者需要已有的工作记录，才能接着往下做。对本例模型，这份需要交接的记录就是上下文 KV。P 和 D 分别处理同一请求的不同阶段，每个阶段都要执行完整的前向计算。Qwen3-8B 的 P 处理 8192 个输入位置，D 随后调用 128 次；每次都经过 attention 和 FFN。因此，两个池都需要相应权重，KV 则从 P 交给 D。独立批处理带来的收益，首先要补偿这次新增交接。
 
 应用通过一次调用请求完整的模型推理，服务内部则可以按阶段安排不同资源。模型的数据依赖确定了阶段之间的切分位置，KV 给出了两阶段之间需要交接的状态，负载比例决定各池需要多少资源。利用这些信息，服务可以在统一调用接口之下，为输入处理和逐步生成分别选择批次与设备。接下来用传输量和处理能力计算这种分工的收益。
 
@@ -132,7 +132,7 @@
 
 ### 9.2.3 阶段处理能力与实例数量配比
 
-比较各阶段的处理速度前，应先算出完成一个请求分别需要多少工作。设一个请求有 $L_{new}$ 个需要 P 新处理的位置、$G-1$ 次 decode 调用。某类 worker 的有效能力分别为 $r_P$ 个新位置/s 与 $r_D$ 次 decode/s，则它的每请求资源需求为
+比较各阶段的处理速度前，应先算出一个请求在各阶段分别需要多少工作。设一个请求有 $L_{new}$ 个需要 P 新处理的位置、$G-1$ 次 decode 调用。某类 worker 的有效能力分别为 $r_P$ 个新位置/s 与 $r_D$ 次 decode/s，则它的每请求资源需求为
 
 $$
 d_P=\frac{L_{new}}{r_P},\qquad d_D=\frac{G-1}{r_D}.
@@ -157,7 +157,7 @@
 
 *图 9-9：每个 A 在 P 阶段提供 2 请求/s，每个 B 在 D 阶段也提供 2 请求/s。两池均为 8 请求/s，低于给定链路约 20.7 请求/s 的交接能力。*
 
-分离后，两类 worker 都避开了各自较慢的阶段，请求率上界从 3.2 增至 8 请求/s，提高了 2.5 倍。模型仍处理同样的 8192 个输入位置与 128 次 decode，差别在于由谁执行。
+分离后，两类 worker 都避开了各自较慢的阶段，请求率上界从 3.2 增至 8 请求/s，增至原来的 2.5 倍。模型仍处理同样的 8192 个输入位置与 128 次 decode，差别在于由谁执行。
 
 再作一个对照：假设八个 worker 完全相同，每请求 P、D 各占 1 秒。共置上界为 $8/(1+1)=4$ 请求/s；四个做 P、四个做 D，两池也都只能提供 4 请求/s。一个请求在分离前后都消耗两个资源秒，所以仅改变执行位置无法突破四请求/s。[^pool]
 
@@ -173,7 +173,7 @@
 
 把两个 A 调到 D，留下的两个 A 提供 16 请求/s 的 P 能力；D 则由两个 A 与四个 B 提供 $2\times0.5+4\times2=9$ 请求/s。总体吞吐率上限提高到 9 请求/s。再调走一个 A，P 只剩 8 请求/s，反而更慢。假定 D 没有缓存这段前缀，因此两种配比仍须交接完整的 1.125 GiB；P 的计算量减少了四分之三，但传给 D 的 KV 大小没有变化。
 
-再恢复原始输入，把输出从 129 增加到 1025 token。D 调用数从 128 增至 1024，扩大八倍；A、B 的 D 能力分别降为 $1/16$ 和 $1/4$ 请求/s。一个 A 做 P 已能提供 2 请求/s，其余三个 A 与四个 B 做 D，合计为 $3/16+4/4=19/16$，约 1.19 请求/s。两个 A 做 P 则只剩 1.125 请求/s 的 D 能力，更不划算。与原来四 P、四 D 相比，长输出将资源需求推向 D；若仍以 4 请求/s 到达，队列就会持续增长。
+再恢复原始输入，把输出从 129 增加到 1025 token。D 调用数从 128 增至 1024，增至原来的八倍；A、B 的 D 能力分别降为 $1/16$ 和 $1/4$ 请求/s。一个 A 做 P 已能提供 2 请求/s，其余三个 A 与四个 B 做 D，合计为 $3/16+4/4=19/16$，约 1.19 请求/s。两个 A 做 P 则只剩 1.125 请求/s 的 D 能力，更不划算。与原来四 P、四 D 相比，长输出将资源需求推向 D；若仍以 4 请求/s 到达，队列就会持续增长。
 
 图 9-11 将三次调整画在同一组设备上。每个方块都是原来的一台 worker，变化的只是它承担的阶段。前缀命中后，P 的工作减少，可以让出两台 A；输出变长后，D 的工作增加，则需要再给 D 一台 A。
 
@@ -193,9 +193,11 @@
 
 ### 9.3.2 CPU／GPU 协作
 
-第 8.4.3 节将权重暂存主存，使用时搬到 GPU，计算位置保持不变。这里比较另一种选择：权重留在主存，由 CPU 执行专家计算。KTransformers 展示了这类 CPU／GPU 协作的实际路径。GPU 可以处理 attention 和部分常驻专家，CPU 处理分配给自己的专家；提交 CPU 工作后，GPU 执行可并行的分支，最后同步并合并结果。热点专家、共享专家以及 prefill 阶段可以采用不同的设备分工。分支汇合时，下一层必须等两侧都完成后才能开始。[^kt]
-
-![专家权重搬运与CPU就地计算](ch09/figure-9-5-local.svg)
+一份专家权重已经放在主存里，GPU 上的输入需要用它计算。可以把整份权重搬到 GPU，也可以把输入送到 CPU，让 CPU 在权重所在的位置完成计算，再把结果传回。两条路径交付同一份输出，跨链路的对象却分别是权重与输入、输出。先沿图中的箭头看清传什么，再比较字节数和计算时间。
+
+第 8.4.3 节采用第一条路径，将权重暂存主存，使用时搬到 GPU。KTransformers 展示了第二类 CPU／GPU 协作的实际路径：GPU 可以处理 attention 和部分常驻专家，CPU 处理分配给自己的专家；提交 CPU 工作后，GPU 执行可并行的分支，最后同步并合并结果。热点专家、共享专家以及 prefill 阶段可以采用不同的设备分工。分支汇合时，下一层必须等两侧都完成后才能开始。[^kt]
+
+![专家权重搬运与 CPU 就地计算](ch09/figure-9-5-local.svg)
 
 *图 9-12：把一份 36 MiB 专家权重从主存送到 GPU，再由 GPU 读取本地输入完成计算。*
 
@@ -205,11 +207,11 @@
 
 对 Qwen3-235B-A22B，一个专家包含三个矩阵，参数数量为 $3\times4096\times1536=18874368$。BF16 权重为 36 MiB。
 
-**例 9.3　专家权重复用如何改变 CPU 与 GPU 的执行选择？** 设有效 CPU 算力为 2 TFLOP/s、DRAM 带宽为 200 GB/s，GPU 算力为 100 TFLOP/s、HBM 带宽为 1000 GB/s，交接带宽为 25 GB/s，每次启动为 5 μs，BF16 激活宽度为 4096。八个专家依次执行，总时间为各专家耗时之和，权重每批读取一次。每个专家只收到一个 token 的特征向量时，CPU 可以直接读取本地主存中的权重，避免了较慢链路上的 36 MiB 搬运；随着每个专家的输入行数增加，一次权重传输的开销由更多行计算分摊，而 CPU 更早进入计算受限区。
+**例 9.3　专家权重复用如何改变 CPU 与 GPU 的执行选择？** 设有效 CPU 算力为 2 TFLOP/s、DRAM 带宽为 200 GB/s，GPU 算力为 100 TFLOP/s、HBM 带宽为 1000 GB/s，交接带宽为 25 GB/s，每次启动为 5 μs，BF16 激活宽度为 4096。八个专家依次执行，总时间为各专家耗时之和，权重每批读取一次。每个专家只收到一个 token 的特征向量时，CPU 可以直接读取本地主存中的权重，避免了较慢链路上的 36 MiB 搬运；随着每个专家的输入行数增加，一次权重传输的开销由更多行计算分摊，而 CPU 更早受到算力限制。
 
 先看八个热点专家各处理一个 token：权重合计 288 MiB。在 200 GB/s 主存上读取约 1.5 ms，加激活交接后 CPU 路径约 1.6 ms；搬到 GPU 的路径仅传权重就约 12.1 ms，连同启动与 GPU 计算约 12.4 ms。低复用时，避免大块权重搬运比提高矩阵算力更有价值。
 
-若这八个专家各处理 128 个 token，权重仍是 288 MiB，计算量却增加 128 倍。CPU 路径增至约 20.1 ms，GPU 路径约 12.5 ms，此时在 GPU 上计算更快。图 9-14 展示了中间过程：按题设参数计算，当每个专家的输入从 78 个 token 增至 79 个时，GPU 开始比 CPU 更快；交点附近两条路径均约 12.4 ms。每个专家多处理一个 token 的特征向量时，CPU 完成新增矩阵计算所需的时间远长于 GPU；权重搬运保持不变，因而 GPU 从交点之后取得优势。[^locality]
+若这八个专家各处理 128 个 token，权重仍是 288 MiB，计算量却增至原来的 128 倍。CPU 路径增至约 20.1 ms，GPU 路径约 12.5 ms，此时在 GPU 上计算更快。图 9-14 展示了中间过程：按题设参数计算，当每个专家的输入从 78 个 token 增至 79 个时，GPU 开始比 CPU 更快；交点附近两条路径均约 12.4 ms。每个专家多处理一个 token 的特征向量时，CPU 完成新增矩阵计算所需的时间远长于 GPU；权重搬运保持不变，因而 GPU 从交点之后取得优势。[^locality]
 
 将上述数字写成一般形式。记专家参数数量为 $P_e$、权重字节数为 $W_e$，输入宽度为 $h$、每元素字节数为 $b$。若同一个专家本批收到 $m$ 个 token，其矩阵计算量为 $F_e(m)=2mP_e$ FLOPs，权重至少需要被访问一次。设有效 CPU 算力、DRAM 带宽分别为 $C_C,B_D$，GPU 算力、HBM 带宽为 $C_G,B_H$，交接带宽为 $B_L$。假定权重与激活已经转换为执行所需的格式，交接与专家计算串行发生，算子时间取计算与权重读取两项中的较大值，则单个专家在两种执行方式下的耗时分别为
 
@@ -225,7 +227,7 @@
 
 前式将输入传输、CPU 上的专家计算和输出传输三段时间相加；后式先搬一份权重，再执行 GPU 计算。公式中的 $\alpha$ 是每次交接的固定代价。输入激活先到 CPU，输出激活在计算后返回，式中的两次启动和往返载荷正对应这两条依赖边。
 
-硬件变化可以用同一组数估计。若有效主存带宽从 200 降到 100 GB/s，八份权重的读取时间从约 1.5 增至 3.0 ms；单 token 情形明显变慢，而每专家 128 个 token 时，CPU 仍主要受约 19.3 ms 的矩阵计算时间限制。就地计算是否有利，由当前的主要瓶颈决定：读取受限时提高主存带宽有效，计算受限时提高矩阵吞吐有效。非统一内存访问（NUMA）把内存分到不同 CPU 附近，跨节点读取要经过处理器间互联。跨 NUMA 读取降低前者，矩阵内核与量化格式改变后者。[^kt-audit]
+硬件变化可以用同一组数估计。若有效主存带宽从 200 降到 100 GB/s，八份权重的读取时间从约 1.5 增至 3.0 ms；单 token 情形明显变慢，而每专家 128 个 token 时，CPU 仍主要受约 19.3 ms 的矩阵计算时间限制。就地计算是否有利，由当前的主要瓶颈决定：读取受限时提高主存带宽有效，计算受限时提高矩阵吞吐有效。非统一内存访问（NUMA）把内存分到不同 CPU 附近，跨节点读取要经过处理器间互联。跨 NUMA 读取会降低有效主存带宽，矩阵内核与量化格式则会改变有效矩阵吞吐率。[^kt-audit]
 
 ![专家复用与执行位置边界](ch09/figure-9-6-reuse.svg)
 
@@ -237,9 +239,9 @@
 
 一个 token 选择八个专家，并不意味着一个批次也只访问八个专家。设一批有 64 个 token，每个选择八个专家，共有 512 次分派。如果它们均匀覆盖全部 128 个专家，每专家处理四行；如果所有 token 都选择同样的八个专家，每专家处理 64 行。两种情况下专家矩阵的有效计算量都是 $512\times2\times18874368$，约 19.3 GFLOPs。
 
-假定批内每份专家权重只读取一次，均匀分派读取 $128\times36=4608$ MiB，即 4.5 GiB；集中分派读取 $8\times36=288$ MiB，相差 16 倍。在同一条 200 GB/s 数据读取通道上，单看权重读取约为 24.2 ms 与 1.5 ms。计算量相同，访问的不同权重却相差一个数量级。
-
-下面两图把这两个批次画成面积相同的矩形。宽度表示访问了多少个专家，高度表示每个专家处理多少行；面积对应分派总数。矩形变窄、变高时，计算量不变，需要读取的不同权重却减少了。
+假定批内每份专家权重只读取一次，均匀分派读取 $128\times36=4608$ MiB，即 4.5 GiB；集中分派读取 $8\times36=288$ MiB，相差 16 倍。在同一条 200 GB/s 数据读取通道上，单看权重读取约为 24.2 ms 与 1.5 ms。计算量相同，需要读取的权重总量却相差一个数量级。
+
+下面两图把这两个批次画成面积相同的矩形。宽度表示访问了多少个专家，高度表示每个专家处理多少行；面积对应分派总数。矩形变窄、变高时，计算量不变，需要读取的权重总量却减少了。
 
 ![同样分派次数下的专家数与输入行数](ch09/figure-9-7-footprint.svg)
 
@@ -249,9 +251,9 @@
 
 *图 9-16：八个专家各处理 64 行，总面积仍为 512。两图横纵轴相同，权重读取却从 4608 MiB 降至 288 MiB。*
 
-于是，总专家数决定需要安排多少权重，分派次数决定这批的有效矩阵计算，批内专家集合决定需要访问哪些权重。图 9-14 已说明，输入行数决定 CPU 与 GPU 哪一侧更快。这里还要补上另一个量：一批请求究竟会访问多少个专家。知道这个数量，才能将单专家的结果扩展到整个批次。
-
-除完全均匀和完全集中这两种情况外，还可以分析随机路由平均会访问多少个专家。设每个 token 独立、均匀地从 $E$ 个专家中选择 $K$ 个不同专家，一个指定专家被一个 token 跳过的概率为 $1-K/E$，被全部 $M$ 个 token 跳过的概率为 $(1-K/E)^M$。用 1 减去这一概率，再对 $E$ 个专家求和，得到批内活跃专家数期望
+于是，总专家数决定需要安排多少权重，分派次数决定这一批的有效矩阵计算量，批内专家集合决定需要访问哪些权重。图 9-14 已说明，输入行数决定 CPU 与 GPU 哪一侧更快。这里还要补上另一个量：一批请求究竟会访问多少个专家。知道这个数量，才能将单专家的结果扩展到整个批次。
+
+除完全均匀和完全集中这两种情况外，还可以分析随机路由平均会访问多少个专家。设每个 token 独立、均匀地从 $E$ 个专家中选择 $K$ 个不同专家，一个指定专家被一个 token 跳过的概率为 $1-K/E$，被全部 $M$ 个 token 跳过的概率为 $(1-K/E)^M$。用 1 减去这一概率，再对 $E$ 个专家求和，得到批内活跃专家数的期望值
 
 $$
 \mathbb{E}[U]=E\left[1-\left(1-\frac{K}{E}\right)^M\right].
@@ -267,7 +269,7 @@
 
 例如，attention 与专家阶段每微批次分别需要 2 ms、3 ms。四个微批次串行执行共需 20 ms；理想双阶段流水只需 $2+3+3\times3=14$ ms，省下 6 ms。若拆小微批次后，每阶段的实际服务时间变长，或无法与计算重叠的往返通信超过 6 ms，这项收益就会消失。因此，通信和计算效率下降引入的额外耗时合计必须小于 6 ms，流水执行才会更快。[^af]
 
-**例 9.4　逐层注意力与前馈分离如何累积通信开销？** 在 Qwen3-8B 的示例部署中，把 36 层稠密前馈网络放在另一侧，每层发送一个完整 BF16 隐状态向量，并返回同样大小的结果。单向传输一次的数据量为 $4096\times2=8192$ bytes；一步 decode 合计 72 次、576 KiB。25 GB/s 下纯载荷约 23.6 μs，72 次 5 μs 启动合计 360 μs，串行交接约 0.384 ms。
+**例 9.4　注意力与前馈网络逐层交接会增加多少通信开销？** 在 Qwen3-8B 的示例部署中，把 36 层稠密前馈网络放在另一侧，每层发送一个完整 BF16 隐状态向量，并返回同样大小的结果。单向传输一次的数据量为 $4096\times2=8192$ bytes；一步 decode 合计 72 次、576 KiB。25 GB/s 下纯载荷约 23.6 μs，72 次 5 μs 启动合计 360 μs，串行交接约 0.384 ms。
 
 这段时间短于例 9.1 中的一次 PD 交接，但两者对应不同工作范围。若生成 129 个输出，后续 128 次 decode 的 AF 串行交接累计约 49.1 ms；8192 个输入位置的 prefill 若也按整批 dense AF 交接，激活载荷为 4.5 GiB，约需 193 ms 纯传输，再加启动。一次 PD 搬运与逐层 AF 往返的区别在此显现：前者随上下文长度增长，后者还随生成步数反复发生。
 
@@ -275,7 +277,7 @@
 
 *图 9-17：相同总载荷下，消息数放大启动开销。两条曲线总载荷均为 1.125 GiB、有效带宽均为 25 GB/s，分别串行发送 1 次和 72 次；差值为 $71\alpha$，纵轴从 48.2 ms 起以显示差异。这样保持总字节数不变，只比较消息数量的影响，实际一步 AF 仅传 576 KiB。*
 
-保持总字节数相同，更容易看清传输次数的影响：把同一份 1.125 GiB 载荷分成 72 次串行交接，载荷时间不变，启动开销比一次交接多 $71\alpha$，在本例中约为 0.36 ms。PD 往往先受大块 KV 搬运影响，AF 则更容易对小消息延迟和依赖敏感。下一节考察 MoE 中的激活传输：一个输入可能发往多个执行位置，完成时还要等待它所选择的专家汇合。
+保持总字节数相同，更容易看清传输次数的影响：把同一份 1.125 GiB 载荷分成 72 次串行交接，载荷时间不变，启动开销比一次交接多 $71\alpha$，在本例中约为 0.36 ms。PD 往往先受大块 KV 搬运影响，AF 则更容易受到小消息延迟和操作间依赖的影响。下一节考察 MoE 中的激活传输：一个输入可能发往多个执行位置，完成时还要等待它所选择的专家汇合。
 
 ## 9.4 专家放置、复制与负载均衡
 
@@ -283,7 +285,7 @@
 
 上一节中，集中复用减少了权重读取。但这些热点专家如果都放在同一张卡上，其他卡就帮不上忙。本节继续跟踪同样的 512 次分派，考察各张卡何时完成计算。即使全系统的总计算量不变，最忙的设备也可能使整层推迟完成。对设备 $r$，记矩阵计算量为 $F_r$、需要读取的权重字节数为 $W_r$，有效算力和带宽分别为 $C_r$、$B_r$。该设备计算与权重读取所需时间至少为 $\max(F_r/C_r,W_r/B_r)$；合并各设备的结果时，必须等耗时最长的设备完成。
 
-把上一节的 512 次分派放到八张卡上，可以看到集中路由的另一面。若各卡恰好分到 64 次任务，每卡处理约 2.42 GFLOPs；若八个热点专家都位于一张卡，该卡处理全部约 19.3 GFLOPs。假设每卡有效算力都为 100 TFLOP/s，只看计算，最忙卡的服务时间从约 24 μs 增到 193 μs，扩大八倍。
+把上一节的 512 次分派放到八张卡上，可以看到集中路由的另一面。若各卡恰好分到 64 次任务，每卡处理约 2.42 GFLOPs；若八个热点专家都位于一张卡，该卡处理全部约 19.3 GFLOPs。假设每卡有效算力都为 100 TFLOP/s，只看计算，最忙卡的服务时间从约 24 μs 增到 193 μs，增至原来的八倍。
 
 图 9-18 中，横条的长度表示各卡完成计算所需的时间。所有结果合并后才能进入下一层，因此决定完成时刻的是最长的一条，而不是八条的平均长度。
 
@@ -295,13 +297,13 @@
 
 *图 9-19：512 次全部落在卡 0，其他卡空闲。相同总计算量，需要等待卡 0 完成；两图使用相同时间尺度。*
 
-集中路由将需要读取的权重从 4.5 GiB 减少到 288 MiB，却可能把最忙卡的计算扩大八倍。前者有利于读取受限的执行，后者不利于计算受限的执行。均衡策略因此要先确定瓶颈所在，再决定是追求复用还是分散工作。
+集中路由将需要读取的权重从 4.5 GiB 减少到 288 MiB，却可能把最忙卡的计算量增至原来的八倍。前者有利于读取受限的执行，后者不利于计算受限的执行。均衡策略因此要先确定瓶颈所在，再决定是追求复用还是分散工作。
 
 通信也依赖 token 的初始归属。在第 9.1 节的 TP2×EP4 布局中，各 EP 组处理同一批请求，每个 EP 成员已拥有相同的隐状态输入，跨 EP 的输入分发（dispatch）可以为零，结果通过部分和归约合并。另一种布局让各设备持有各自不同的输入 token，则可能需要将激活分派给远端专家。输入已复制时，通信集中在结果合并；输入分属不同设备时，执行前还多出远端分派。通信量由 token 的起点、专家的位置与结果的归属共同确定。
 
 ### 9.4.2 专家副本、放置与动态调整
 
-图 9-19 中集中在少数卡上的工作提示了一个改进方向：把热点专家的副本放到空闲卡上，让这些卡也能处理热点任务。专家复制让同一逻辑专家拥有多个物理副本，调度器可以把任务分给这些副本。只要权重相同、路由权重与汇合正确，复制不必改变每个 token 选择分数最高的 k 个专家这一规则（top-k）；修改专家选择规则则会改变模型行为，两者应分别评价。vLLM、SGLang 的专家负载均衡机制（EPLB）根据路由统计，调整各逻辑专家的副本位置和任务分配。
+图 9-19 中集中在少数卡上的工作提示了一个改进方向：把热点专家的副本放到空闲卡上，让这些卡也能处理热点任务。专家复制让同一个逻辑专家拥有多个物理副本，调度器可以把任务分给这些副本。只要副本权重相同、路由权重使用正确且结果合并无误，复制不必改变每个 token 选择分数最高的 k 个专家这一规则（top-k）；修改专家选择规则则会改变模型行为，两者应分别评价。vLLM、SGLang 的专家负载均衡机制（EPLB）根据路由统计，调整各逻辑专家的副本位置和任务分配。
 
 专家副本占用显存，也会挤占 KV 和缓冲。每卡每层增加一个 36 MiB 专家，94 层共需 3384 MiB，约 3.30 GiB；若只为八个最拥挤的层各增加一个副本，则只需 288 MiB。两者相差近 12 倍。CRAFT 是研究专家副本与显存分配的服务系统。它要解决的各层显存分配问题由此产生：应把空间花在最能缩短等待的层，而不是机械地给每层同样多的副本。[^moe]
 
@@ -318,15 +320,15 @@
 
 *图 9-20：热点持续多久，专家复制才值得。一次准备约 10.6 ms，每批节省约 0.40 ms；曲线使用例 9.5 未舍入时间计算，第 27 批开始净获益。七张接收卡各需额外 36 MiB，假设容量足够且热点不变。*
 
-副本策略需要预测的是热点还会持续多久。若统计窗口结束时热点只剩 16 批，复制将多花约 4.2 ms；若还剩 64 批，则净省约 15.2 ms。每次重新复制都要再花费 10.6 ms，所以频繁追逐热点会反复推迟回本。将容量优先分给持续拥挤的层，才能让缩短的等待累计超过准备成本。
+副本策略需要预测的是热点还会持续多久。若统计窗口结束时热点只剩 16 批，复制将多花约 4.2 ms；若还剩 64 批，则净省约 15.2 ms。每次重新复制都要再花费 10.6 ms，所以频繁随热点迁移副本，会延后收回复制成本的时间。将容量优先分给持续拥挤的层，才能让缩短的等待累计超过准备成本。
 
 ### 9.4.3 输入分发、计算与结果合并的重叠
 
-分组通用矩阵乘法（Grouped GEMM）把多个专家矩阵组织成一个执行批次，内核按照分块要求补齐各矩阵的行数。例如八个本地专家收到 $[32,16,8,4,2,1,1,0]$ 个 token，合计 64 次 token 到专家的分派，对应 64 个有效输入矩阵行。若每个非空专家补齐到 32 行，需要 224 行；若八个专家都按最大值补齐，需要 256 行。实际执行工作分别为有效行数的 3.5 倍和 4 倍。这解释了为什么有效任务数相同，矩阵时间仍可能不同：硬件执行的是补齐后的矩阵，模型所需的专家计算只对应其中 64 个有效行；补齐的零行没有对应的真实 token。
-
-假设 dispatch、专家计算与 combine 分别需要 0.2、0.6、0.2 ms，完全串行为 1.0 ms。若可均分成两个微批次，各阶段时间减半，理想三阶段流水为 $0.1+0.3+0.1+0.3=0.8$ ms，只省下 0.2 ms。若共享 HBM 使计算阶段每微批次从 0.3 增至 0.4 ms，同一流水就回到 $0.1+0.4+0.1+0.4=1.0$ ms。通信确实重叠了，整批却没有变快。
-
-![专家分派计算与结果合并的流水时间线](ch09/figure-9-11-overlap.svg)
+分组通用矩阵乘法（Grouped GEMM）把多个专家矩阵组织成一个执行批次，内核按照分块要求补齐各矩阵的行数。例如八个本地专家收到 $[32,16,8,4,2,1,1,0]$ 个 token，合计 64 次 token 到专家的分派，对应输入矩阵中的 64 个有效行。若每个非空专家补齐到 32 行，需要 224 行；若八个专家都按最大值补齐，需要 256 行。实际执行的行数分别为有效行数的 3.5 倍和 4 倍。这解释了为什么有效任务数相同，矩阵时间仍可能不同：硬件执行的是补齐后的矩阵，模型所需的专家计算只对应其中 64 个有效行；补齐的零行没有对应的真实 token。
+
+假设 dispatch、专家计算与 combine 分别需要 0.2、0.6、0.2 ms，完全串行为 1.0 ms。若可均分成两个微批次，各阶段时间减半，理想三阶段流水为 $0.1+0.3+0.1+0.3=0.8$ ms，只省下 0.2 ms。若 HBM 争用使每个微批次的计算耗时从 0.3 增至 0.4 ms，同一流水就回到 $0.1+0.4+0.1+0.4=1.0$ ms。通信确实重叠了，整批却没有变快。
+
+![专家分派、计算与结果合并的流水时间线](ch09/figure-9-11-overlap.svg)
 
 *图 9-21：整批依次分派 0.2 ms、计算 0.6 ms、合并 0.2 ms，总时间 1 ms。*
 
@@ -342,9 +344,9 @@
 
 ### 9.5.1 缓存标识、共享范围与可用状态
 
-前两节通过改变执行位置和任务分配，减少了当前请求的耗时。另一种节省计算的方法，是让后续请求直接使用前一次计算留下的 KV。第 8.3 节已经说明分页、前缀匹配、共享和淘汰的机制。本节接着考虑状态分布在不同执行位置时，怎样确认匹配、找到数据并完成传输。前缀缓存的价值来自避免重复执行，但“相同文本”不足以唯一确定所有可复用状态。模型与适配器（adapter，附加在基础模型上的参数模块）版本、token 序列、位置、状态格式以及必要的编码配置都可能参与缓存标识的计算。例如，同样的 token 使用不同 adapter，会在投影后得到不同的 K、V；同样的后缀接在不同前文后，attention 的上下文也发生变化。缓存键需要沿前缀链标识生成这份状态的计算。窗口或循环状态则还要携带所在位置与更新时刻，才能从同一个状态继续。
-
-确认两份状态可以互用之后，还要确定它们存在哪里、其他实例怎样读取。容量相加并不会自动形成共享缓存。两个实例各有 4 GiB 私有主机内存（host memory）缓存时，一份 1.125 GiB 前缀可能在两端各占一份，共占 2.25 GiB；另一个实例却仍无法直接复用它。若二者连接到同一共享存储后端（接收和提供缓存对象的存储服务），一份对象可以服务两端，但每端在生成前可能还要取回本地。SGLang 的分层缓存 HiCache、vLLM 的多级缓存路径和 KV 缓存管理系统 LMCache 提供了不同的组织方式，取回是否经 CPU 决定下一节的路径时间。[^cache]
+前两节通过改变执行位置和任务分配，减少了当前请求的耗时。另一种节省计算的方法，是让后续请求直接使用前一次计算留下的 KV。第 8.3 节已经说明分页、前缀匹配、共享和淘汰的机制。本节接着考虑状态分布在不同执行位置时，怎样确认匹配、找到数据并完成传输。前缀缓存的价值来自避免重复执行，但“相同文本”不足以唯一确定所有可复用状态。模型与适配器（adapter，附加在基础模型上的参数模块）版本、token 序列、位置、状态格式以及必要的编码配置都可能参与缓存标识的计算。例如，同样的 token 使用不同 adapter，会在投影后得到不同的 K、V；同样的后缀接在不同前文后，attention 的上下文也发生变化。缓存键需要沿前缀链标识生成这份状态的计算。滑动窗口状态或递推状态则还要携带所在位置与更新时刻，才能从同一个状态继续。
+
+确认两份状态可以相互替代之后，还要确定它们存在哪里、其他实例怎样读取。容量相加并不会自动形成共享缓存。两个实例各有 4 GiB 私有主机内存（host memory）缓存时，一份 1.125 GiB 前缀可能在两端各占一份，共占 2.25 GiB；另一个实例却仍无法直接复用它。若二者连接到同一共享存储后端（接收和提供缓存对象的存储服务），一份对象可以服务两端，但每端在生成前可能还要取回本地。SGLang 的分层缓存 HiCache、vLLM 的多级缓存路径和 KV 缓存管理系统 LMCache 提供了不同的组织方式，取回是否经 CPU 决定下一节的路径时间。[^cache]
 
 状态产生后，需要先保存并发布，其他实例才能查询、取回并使用它。路由器使用的缓存事件通常是存储位置和缓存标识等元数据，不携带完整 KV。GPU 副本被淘汰时 CPU 副本可能仍在。**目录**保存缓存标识与位置的映射，**对象**保存实际 KV 字节；恢复时分别确认目录映射和对象数据。事件遗漏、延迟或缓存空间被释放，都可能使原先记录的位置不再可用。
 
@@ -354,11 +356,11 @@
 
 ### 9.5.2 多级存储
 
-第 8.3.4 节比较了本地缓存保留、换出和重算的代价。现在把同一份状态放到远端：除了对象大小，还要考虑经过哪些链路，以及每次生成是否重复读取。共享 KV 的三种典型路径是本地重新 prefill、从远端取回一次并在本地驻留、每一步都从远端读取数据。它们可能使用同样的上下文内容，却传输频率却完全不同。
+第 8.3.4 节比较了本地缓存保留、换出和重算的代价。现在把同一份状态放到远端：除了对象大小，还要考虑经过哪些链路，以及每次生成是否重复读取。共享 KV 的三种典型路径是本地重新 prefill、从远端取回一次并在本地驻留、每一步都从远端读取数据。它们可能使用同样的上下文内容，传输频率却完全不同。
 
 沿用 1.125 GiB 前缀和 25 GB/s 链路，一次载荷取回约 48.3 ms。若 100 次 decode 调用都重新远程读取这份不变前缀，累计占用链路约 4.83 s。若这 100 次读取分布在一秒内，仅这份上下文就要求约 121 GB/s，是给定链路能力的 4.8 倍。将读取与计算并行安排，也无法使 25 GB/s 链路在一秒内送完这些字节。
 
-一次取回并驻留将频次从“每步”降到“每次复用”，代价是占用本地 HBM。设一份状态占 $V$ bytes，保留 $\tau$ 秒，其内存占用量可用 $V\tau$ 个 byte-seconds 表示。保留时间相同时，大对象占用更多容量；大小相同时，等待越久，占用越大。因此可把不活跃会话放到远端，在会话恢复时取回，并在连续 decode 期间保留在本地。这样，数据传输主要发生在会话恢复和暂停时。
+一次取回并驻留将频次从“每步”降到“每次复用”，代价是占用本地 HBM。设一份状态占 $V$ bytes，保留 $\tau$ 秒，其内存占用与保留时长的乘积可用 $V\tau$ 个 byte-seconds 表示。保留时间相同时，大对象占用更多容量；大小相同时，保留越久，这一乘积越大。因此可把不活跃会话放到远端，在会话恢复时取回，并在连续 decode 期间保留在本地。这样，数据传输主要发生在会话恢复和暂停时。
 
 假定状态将在未来复用 $k$ 次，暂不考虑排队时间，也不考虑它占用空间后导致其他缓存内容被淘汰的影响。保存后再取回比重新计算更快的条件为
 
@@ -368,21 +370,21 @@
 
 例如，保存需要 50 ms，每次取回需要 60 ms，而重算需要 100 ms。复用一次时，保存加取回为 110 ms，比重算慢；复用两次时为 170 ms，比两次重算的 200 ms 少 30 ms。保存是否值得取决于未来复用次数。若链路变慢使每次取回需要 120 ms，复用越多反而损失越大；此时应重算，或设法把取回提前到关键路径之外。
 
-缓存容量还会影响一份状态能保留多久，以及后续请求能否复用它。在一组同卡双引擎实验中，12 次前缀复用机会里，4 GiB 共享 CPU 池完整命中 5 次，8 GiB 池完整命中 12 次。多出的 4 GiB 保住了七次本来会丢失的完整前缀；而已在本地驻留的再次请求，无须重复取回。于是，共享容量的收益要沿“保存多久—避免几次重算—增加几次搬运”来比较。[^shared]
+缓存容量还会影响一份状态能保留多久，以及后续请求能否复用它。在一组同卡双引擎实验中，12 次前缀复用机会里，4 GiB 共享 CPU 池完整命中 5 次，8 GiB 池完整命中 12 次。多出的 4 GiB 保住了七次本来会丢失的完整前缀；而再次请求所需的状态若已在本地驻留，就无须重复取回。于是，共享容量的收益要沿“保存多久—避免几次重算—增加几次搬运”来比较。[^shared]
 
 ### 9.5.3 持久化、检查点与部分重算
 
-上一节假定保存下来的状态能完整取回。实例重启后，目录记录、磁盘数据和可连续复用的前缀可能并不一致，恢复过程需要重新确认哪些计算已经保存。持久化让状态在实例重启或任务暂停后仍可使用。完整保存减少恢复时的重算，周期检查点减少写入但增加恢复工作，部分重算则利用保存前缀补齐缺失部分。检查点要保存继续计算所需的全部状态：完整 GQA 保存上下文 K、V；DeepSeek V4 的状态还涉及压缩结果、窗口以及未完成的压缩块。恢复时从保存的更新边界继续推进。[^v4]
+上一节假定保存下来的状态能完整取回。实例重启后，目录记录、磁盘数据和可连续复用的前缀可能并不一致，恢复过程需要重新确认哪些计算结果已经保存。持久化让状态在实例重启或任务暂停后仍可使用。完整保存减少恢复时的重算，周期检查点减少写入但增加恢复工作，部分重算则利用保存前缀补齐缺失部分。检查点要保存继续计算所需的全部状态：完整 GQA 保存上下文 K、V；DeepSeek V4 的状态还涉及压缩结果、窗口以及未完成的压缩块。恢复时从检查点所记录的最后一次完整更新继续计算。[^v4]
 
 先考虑完整 GQA 页。Qwen3-8B 每个 16-token 逻辑页为 2.25 MiB。若各层 K、V 分片分别搬运，可能形成许多小连续段；若按页汇集，则消息数和布局转换不同。一个逻辑页分成 36 层的 K、V 后有 72 个 32 KiB 连续段；按页汇集后则成为一个 2.25 MiB 对象。前者更容易受每秒操作次数限制，后者把更多时间用于连续载荷传输。
 
-一组重启后的请求读取了 64 页，覆盖 1024 个位置，但可复用的连续前缀只有 1008 个位置，即 63 页。多读的一页占 2.25 MiB，却没有消除相应的重算。对于这个请求，读取字节为 144 MiB，有效复用约 142 MiB；更有解释力的差别是“读入 64 页，使用 63 页”。这里的限制发生在读取之后：只有通过匹配、能够连续接到已有上下文上的页，才能替代计算。把磁盘读得更快会缩短读取时间，却不会改变这一请求需要处理的末页。[^restart]
+一组重启后的请求读取了 64 页，覆盖 1024 个位置，但可复用的连续前缀只有 1008 个位置，即 63 页。多读的一页占 2.25 MiB，却没有消除相应的重算。对于这个请求，读取量为 144 MiB，有效复用量约为 142 MiB；更有解释力的差别是“读入 64 页，使用 63 页”。这里的限制发生在读取之后：只有通过匹配、能够连续接到已有上下文上的页，才能替代计算。把磁盘读得更快会缩短读取时间，却不会改变这一请求需要处理的末页。[^restart]
 
 ![读取页与可复用连续前缀](ch09/figure-9-12-cache.svg)
 
 *图 9-24：读入的页不一定全部成为可复用前缀。正常重启后的这一请求读入 64 个 16-token 页，只复用前 63 页；末页仍需处理。每页 2.25 MiB，该请求的匹配边界为 1008 个位置。*
 
-页面缺失时，需要比较继续等待与重新计算所需的时间。例 9.6 中本地重算只需 180 ms；若取回一个不可用对象已等待 1 秒，仅等待这一对象所花的时间，就已超过五次重算所需的时间。实际截断页实验里，无条件等待的请求在 60 秒观察窗口内仍未完成，允许放弃等待的策略则通过重算返回。完成当前请求之后，还需要隔离或修复坏页，否则下一次请求会再次付出等待。[^fault]
+页面缺失时，需要比较继续等待与重新计算所需的时间。例 9.6 中本地重算只需 180 ms；若取回一个不可用对象已等待 1 秒，仅等待这一对象所花的时间，就已超过五次重算所需的时间。实际截断页实验里，无条件等待的请求在 60 秒观察窗口内仍未完成，采用允许放弃等待的策略时，请求则通过重算得到结果。完成当前请求之后，还需要隔离或修复坏页，否则下一次请求还会遇到同样的等待。[^fault]
 
 ### 9.5.4 缓存亲和性与请求路由
 
@@ -392,7 +394,7 @@
 T_{first}=\max(Q,R)+C.
 $$
 
-如果取回必须等到 GPU 排队结束后才开始，排队时间与取回时间就需要相加；如果逐层流水，则需要更细的执行图。例如 $Q=80$ ms、取回耗时 60 ms、剩余计算 10 ms，并行就绪时共需 90 ms；等 GPU 空闲后再启动取回则需 150 ms。相同计算和字节，通过改变依赖关系便相差 60 ms。
+如果取回必须等到 GPU 排队结束后才开始，排队时间与取回时间就需要相加；如果逐层流水，则需要更细的执行图。例如 $Q=80$ ms、取回耗时 60 ms、剩余计算 10 ms，并行就绪时共需 90 ms；等 GPU 空闲后再启动取回则需 150 ms。计算量和传输字节数相同，仅改变依赖关系，耗时就相差 60 ms。
 
 **例 9.6　缓存命中与排队等待如何共同决定请求路由？** 对同一 8K 前缀，A 有 HBM 命中，但需等 250 ms；B 等 20 ms，完整重算需 180 ms；命中后的剩余工作为 10 ms。远端读取固定开销 10 ms，整份先到 host，再通过 25 GB/s 搬到 GPU。
 
@@ -403,7 +405,7 @@
 | B：远端 5 GB/s，经 host 取回 | 约 310 ms |
 | B：远端 20 GB/s，经 host 取回 | 约 129 ms |
 
-![缓存路由中排队取回与计算的时间关系](ch09/figure-9-13-route.svg)
+![缓存路由中排队、取回与计算的时间关系](ch09/figure-9-13-route.svg)
 
 *图 9-25：A 本地缓存已命中，GPU 排队 250 ms 后再计算 10 ms，首 token 在 260 ms 返回。灰为排队，绿为计算。*
 
@@ -421,11 +423,11 @@
 
 第一张路由图中，A 的数据已经在 GPU 上，但计算要等到 250 ms 才能开始。B 在 20 ms 时空闲；直接重算会立即开始计算，远端取回则要继续等数据。提高远端带宽缩短的是橙色条，查找、主存到 GPU 的传输和最后的计算仍然存在。
 
-B 要在 200 ms 内返回，扣除命中后 10 ms 计算，状态必须在 190 ms 内就绪。再扣除查找的 10 ms 与 host→GPU 的约 48.3 ms，远端读取只剩约 131.7 ms。用 1.125 GiB 除以这个预算，得到约 9.2 GB/s 的取回与重算耗时相等时的带宽。
+B 要在 200 ms 内返回，扣除命中后 10 ms 计算，状态必须在 190 ms 内就绪。再扣除查找的 10 ms 与 host→GPU 的约 48.3 ms，远端读取只剩约 131.7 ms。用 1.125 GiB 除以这个预算，得到取回与重算耗时相等时的带宽，约为 9.2 GB/s。
 
 同一会话由此可以根据状态与资源的位置重新安排。应用保留连续的上下文，服务选择继续使用本地状态、迁移状态或重新计算；选择依据是这三条路径何时能让后续模型执行开始。第八章根据内容判断可复用的前缀，本节进一步将排队时间与链路带宽纳入路由决策。
 
-把远端提高到 20 GB/s 后，单请求取回明显占优；但每秒 16 次取回需要约 19.3 GB/s，已用去约 97% 的链路能力。突发请求会增加传输排队时间。因此，按单请求算出所需带宽后，还要计算持续到达时的总流量。[^route]
+把远端读取带宽提高到 20 GB/s 后，单请求取回明显占优；但每秒 16 次取回需要约 19.3 GB/s，已用去约 97% 的链路能力。突发请求会增加传输排队时间。因此，按单请求算出所需带宽后，还要计算持续到达时的总流量。[^route]
 
 上面的比较假定缓存位置已知且数据可用。路由器通常依据缓存事件预测位置，事件延迟或对象淘汰会使预测失准。为了看清这种误差怎样影响响应时间，现在把 A 的队列改为 80 ms，且缓存只以概率 $p$ 真正可用。命中时为 90 ms，所有层次都失效时为 260 ms。其期望为
 
@@ -435,11 +437,11 @@
 
 平均优于 B 的 200 ms 需要 $p>6/17$。取 $p=0.9$，平均仅 107 ms，但 10% 的请求仍为 260 ms，因此该两点分布的 p99 为 260 ms，达不到 220 ms 的目标。为达到 220 ms 的 p99 目标，这组两点时间模型要求命中概率至少达到 99%；提高到 90% 已经显著改善均值，却仍让十分之一请求走上 260 ms 的慢路径。[^events]
 
-同时运行两个任务的压力实验，更直观地显示了这种取舍。缓存优先时，目标请求约 1.38 秒完成，两个任务也在此时全部结束；把目标移到空闲实例后，它缩短到约 0.33 秒，两个任务却约 1.54 秒才全部完成。目标快了约 1.05 秒，全部任务完成的时刻却推迟了约 0.16 秒。路由决策之前必须确定优化的是目标请求响应，还是全部任务的完成时间。[^pressure]
+同时运行两个任务的压力实验，更直观地显示了这种取舍。缓存优先时，目标请求约 1.38 秒完成，两个任务也在此时全部结束；把目标移到空闲实例后，它缩短到约 0.33 秒，两个任务却约 1.54 秒才全部完成。目标快了约 1.05 秒，全部任务完成的时刻却推迟了约 0.16 秒。路由决策之前必须确定优化的是目标请求的响应时间，还是全部任务的完成时间。[^pressure]
 
 ## 9.6 服务启动、扩缩容与故障恢复
 
-将第 8 章的状态恢复过程计入路由决策后，需要同时比较实例的空闲时间与状态准备时间。设实例 A 保留全局 KV 与编码器 SWA 但有排队，实例 B 可立即执行却需要取回全局 KV 并恢复编码器 SWA。A 的优势是能够复用状态，B 的优势是设备可以立即开始工作；路由器需要比较它们的预计完成时间，而不是只看单个缓存命中标志。
+将第 8 章的状态恢复过程计入路由决策后，需要同时比较实例何时空闲与状态何时准备好。设实例 A 保留全局 KV 与编码器 SWA 但有排队，实例 B 可立即执行却需要取回全局 KV 并恢复编码器 SWA。A 的优势是能够复用状态，B 的优势是设备可以立即开始工作；路由器需要比较它们的预计完成时间，而不是只看单个缓存命中标志。
 
 设两端后续的新增输入处理、解码器窗口重放与生成耗时相同，准备工作串行执行。B 的全局传输按第七章为 4.666 ms，编码器 SWA 恢复假设为 8 ms，共需 12.666 ms。A 保留了这两份状态，准备时间就是排队时间：排队 10 ms 时，A 更早开始处理新增输入；排队增至 20 ms 时，B 更早。12.666 ms 由此成为本例切换路由选择的排队阈值。
 
@@ -459,13 +461,13 @@
 T_{drain}=\frac{\lambda T_s}{\mu-\lambda}.
 $$
 
-例如，启动需要 10 秒，期间每秒到达 4 个请求，就绪时已有 40 个请求积压。若就绪后每秒服务 8 个，新请求仍占去 4 个，实际每秒只能消掉 4 个旧请求，还需 10 秒排空；若服务能力只有 5 请求/s，每秒仅消掉一个旧请求，要再等 40 秒。相同的 10 秒启动，对应从开始启动算起 20 秒或 50 秒的排空时间，如图 9-30 所示。
+例如，启动需要 10 秒，期间每秒到达 4 个请求，就绪时已有 40 个请求积压。若就绪后每秒服务 8 个，新请求仍占去 4 个，实际每秒只能处理完 4 个积压请求，还需 10 秒排空；若服务能力只有 5 请求/s，每秒仅能处理完一个积压请求，要再等 40 秒。相同的 10 秒启动，对应从开始启动算起 20 秒或 50 秒的排空时间，如图 9-30 所示。
 
 ![服务能力与启动积压消退](ch09/figure-9-17-service.svg)
 
 *图 9-30：服务余量决定启动积压的消退速度。连续流量模型，每秒到达四请求，启动 10 秒后积压 40 个。就绪后服务率为八或五请求/s，净排空率分别为四或一请求/s，从开始启动算起在第 20 或 50 秒排空。例 9.7 的排空期限为第 25 秒。*
 
-并发预取还可能改变谁先开始执行。一个 1024 位置的请求在单独运行时复用 1008 个位置；八个请求同时到达时，前四个各登记 1024 位置预取，占用达到 4096，后续请求因超过容量限额而未登记。首个完成的请求恰来自后者，以零命中开始 prefill。它没有等缓存，因而更早进入执行队列；因此，即使缓存对象存在，请求也要先获得预取所需的空间，才能使用这份缓存。预取空间用尽后，后续请求会直接重算，而已经开始预取的请求仍在等待数据。[^branch]
+并发预取还可能改变谁先开始执行。一个 1024 位置的请求在单独运行时复用 1008 个位置；八个请求同时到达时，前四个请求各登记了 1024 个位置的预取，合计占用 4096 个位置，后续请求因超过容量限额而未登记。首个完成的请求恰来自后者，以零命中开始 prefill。它没有等缓存，因而更早进入执行队列；因此，即使缓存对象存在，请求也要先获得预取所需的空间，才能使用这份缓存。预取空间用尽后，后续请求会直接重算，而已经开始预取的请求仍在等待数据。[^branch]
 
 权重固定在不可修改介质上时，增加同型号设备可以扩展同版本服务；部署新权重则要配置支持新版本的设备。路由器按模型版本分派请求，容量规划为新旧版本并存和设备替换预留资源。固定版本服务持续得越久、需求越大，部署投入便能由更多请求分担。
 
@@ -473,7 +475,7 @@
 
 启动解决的是新实例何时可用；运行中的迁移还要让目标实例接着源端的进度执行。改变 TP、EP 或服务池规模，需要重新安排权重与请求状态。目标设备获得参数只是第一步，还可能需要建立通信组、准备新图、转换布局并恢复接收请求。这些准备与数据搬运形成先后依赖，共同决定何时能够把新请求交给目标。
 
-计划迁移可以利用源端仍在运行这一条件。先后台复制不再改变的上下文，源端继续生成；最终短暂停止，复制完迁移期间新增的状态并将执行权交给目标。设还有 1 GiB 待复制，复制速率为 2 GiB/s，源端每秒新增 0.5 GiB，则积压以 1.5 GiB/s 减少，约 0.67 秒后完成复制；若新增也达到 2 GiB/s，积压便保持不变。最后的交接点同时确定状态版本与执行权，防止目标漏掉源端最后的更新。
+计划迁移可以利用源端仍在运行这一条件。先后台复制不再改变的上下文，源端继续生成；最终短暂停止，复制完迁移期间新增的状态并将执行权交给目标。设还有 1 GiB 待复制，复制速率为 2 GiB/s，源端每秒新增 0.5 GiB，则积压以 1.5 GiB/s 减少，约 0.67 秒后完成复制；若状态增长速率也达到 2 GiB/s，积压便保持不变。最后的交接点同时确定状态版本与执行权，防止目标漏掉源端最后的更新。
 
 ![状态生成与后台复制的进度](ch09/figure-9-14-migration.svg)
 
@@ -481,11 +483,11 @@
 
 图 9-31 中，源端曲线也在上升，所以追赶速度是两条曲线斜率之差。复制能力恰好等于状态增长率时，两条线平行，初始积压便不会缩小。最终交接必须安排在目标已追上源端、两端状态一致的时刻。
 
-运行中并行切换将同一组设备重新组织为 TP 或 SP×TP。这里 TP 切分层内矩阵，序列并行（SP）按 token 位置分配部分计算与中间状态，SP×TP 表示组合使用两种划分。ArcticInference 与 vLLM 的相关案例展示了这种组织方式：切换改变每步参与计算的设备与 token 分布，也改变所需权重、KV 布局和执行图。预先保留两种模式的权重与图可以缩短切换等待，额外驻留则减少 KV 可用空间。Elastic EP 扩大专家组时，新设备先获得专家权重，随后才能承担分派；承接完整请求还需要相应的 attention 状态。[^switch]
+运行中并行切换将同一组设备重新组织为 TP 或 SP×TP。这里 TP 切分层内矩阵，序列并行（SP）按 token 位置分配部分计算与中间状态，SP×TP 表示组合使用两种划分。ArcticInference 与 vLLM 的相关案例展示了这种组织方式：切换改变每步参与计算的设备与 token 分布，也改变所需权重、KV 布局和执行图。预先保留两种模式的权重与图可以缩短切换等待，额外保存这些权重与图则会减少 KV 可用空间。Elastic EP 扩大专家组时，新设备先获得专家权重，随后才能承担分派；处理完整请求还需要相应的 attention 状态。[^switch]
 
 追上状态解决了数据一致性问题，目标还要完成通信组和执行图的准备。下面保持数据量不变，比较提高传输带宽究竟能缩短多少迁移时间。一个从四路张量并行（TP4）切换到八路张量并行（TP8）的迁移例子需要约 16.4 GB 网络传输，在本地构建目标张量还需要读写约 4.7 GB 数据。把同一份网络载荷放到有效 5 GB/s 和 25 GB/s 的路径，单看搬运约需 3.3 秒和 0.66 秒，相差五倍。
 
-但若另假设建立通信组、执行图准备与恢复接收请求在传输后串行占用 9 秒，总时间便约为 12.3 秒和 9.7 秒，只缩短约 21%。继续提高带宽的收益已经被这 9 秒限制。即使网络传输趋近于零，总时间也只能趋近 9 秒。若在传输开始前建立通信组并准备好执行图，就能进一步缩短迁移时间。[^migration]
+但若另假设建立通信组、执行图准备与恢复接收请求在传输后串行占用 9 秒，总时间便约为 12.3 秒和 9.7 秒，只缩短约 21%。继续提高带宽的收益已经被这 9 秒限制。即使网络传输趋近于零，总时间也只能趋近 9 秒。若能提前建立通信组并准备好执行图，就能进一步缩短迁移时的等待。[^migration]
 
 ### 9.6.3 部分故障与流式生成恢复
 
@@ -493,7 +495,7 @@
 
 假设原输入有 8192 个位置，已经向用户返回 129 个输出，但最近保存的 KV 仅覆盖原输入。恢复时，若这些输出已可靠记录，只需把前 128 个输出补入模型，重建到 8320 个 KV 位置，再处理第 129 个输出以继续生成。若已有 8320 位置的兼容 KV，则可省去这 128 个位置的重放。
 
-![输出记录与KV检查点的恢复位置](ch09/figure-9-15-recovery.svg)
+![输出记录与 KV 检查点的恢复位置](ch09/figure-9-15-recovery.svg)
 
 *图 9-32：输出记录决定继续哪条序列，KV 检查点决定从哪里补算。假定 129 个输出均已可靠记录，但 KV 只保存了原输入。下方按输入、前 128 个输出和第 129 个输出分段示意，宽度不按 token 数量比例绘制。*
 
@@ -501,7 +503,7 @@
 
 **预写日志**（write-ahead log，WAL）在对外确认之前可靠保存待恢复的操作或结果。这里的 token 级 WAL 保存已经确定的输出序列，KV 保存的是这段序列已经完成的计算。前者决定恢复后应继续哪条输出，后者决定还要重做多少工作。DeepSeek V4 的生成服务采用这类机制；恢复时还要保持权重版本、位置和解码状态一致。[^v4]
 
-故障范围由同步组决定。TP 或 EP 的一个 rank 丢失，可能使整个协作实例上的请求停住；完整副本的其他实例则可能继续服务。恢复时应依次观察检测、重建、恢复后首个有效 token 的返回时间，以及积压请求处理完毕的时间，并记录哪些输出已经返回用户。第 10 章进一步分析这些选择对 RL 样本与训练进展的影响，第 11 章再处理工具和环境的任务恢复。
+故障范围由同步组决定。TP 或 EP 的一个 rank 丢失，可能使整个协作实例上的请求停住；完整副本的其他实例则可能继续服务。故障恢复依次经历检测、实例重建与请求恢复；恢复后的处理能力决定积压请求还需多久才能完成。第 10 章进一步分析这些选择对 RL 样本与训练进展的影响，第 11 章再处理工具和环境的任务恢复。
 
 ## 9.7 部署方案的综合比较
 
@@ -519,15 +521,15 @@
 
 对本章的 1.125 GiB 上下文 KV 缓存，直接 P→D 交接搬一次；经池中转则在 P→池、池→D 两条边各搬一次，总载荷为 2.25 GiB。假设两条边都为 25 GB/s，且整份写入后才能读取，中转等待约 96.6 ms，比直接交接多约 48.3 ms。
 
-若状态只被 D 使用一次，这次中转没有获得复用收益。若后面还有另一个实例要继续处理同一会话，它便有机会用约 48.3 ms 的读取替代一次 180 ms 的重算，节省约 132 ms，超过先前多花的约 48.3 ms。因此，后续请求复用这些 KV 时省下的计算时间，可以抵消额外的搬运时间。共享池将一次会话留下的计算成果用于后续请求；AF 则改变当前请求每一层的执行位置，两者改变时间线的不同部分。
-
-同样的规则适用于视觉。E 产生的 EC 与语言 KV 使用不同的缓存标识，占用的空间也不同；图像缓存命中可能减少 E 的需求，却不自动消除后续语言 P。在 E→P→D 的执行图中，EC 命中使 E 的需求下降，语言前缀命中使 P 的需求下降，资源配比随各自被消除的工作调整。
+若状态只被 D 使用一次，这次中转没有获得复用收益。若后面还有另一个实例要继续处理同一会话，它便有机会用约 48.3 ms 的读取替代一次 180 ms 的重算，节省约 132 ms，超过先前多花的约 48.3 ms。因此，后续请求复用这些 KV 时省下的计算时间，可以抵消额外的搬运时间。共享池将一次会话留下的计算成果用于后续请求；AF 则改变当前请求每一层的执行位置，两者分别改变时间线上的不同部分。
+
+同样的规则适用于视觉。E 产生的 EC 与语言 KV 使用不同的缓存标识，占用的空间也不同；图像缓存命中可能减少 E 的工作，却不会自动消除后续语言模型的 P 阶段。在 E→P→D 的执行图中，EC 命中使 E 的需求下降，语言前缀命中使 P 的工作减少，资源配比也随各阶段工作量的变化而调整。
 
 ### 9.7.2 同质量、同资源约束下的方案比较
 
 **例 9.7　哪种推理部署能持续服务并按期清空启动积压？** 采用章首的资源与负载：四个 A 类 worker、四个 B 类 worker，每秒到达四个独立请求，每个请求输入 8192 个位置、输出 129 个 token。有效能力沿用例 9.2，两阶段在同一 worker 上串行占用资源，表中有效能力对应下述驻留限制。链路为共享的 25 GB/s 载荷通道，各次搬运占用同一份链路带宽。比较窗口内模型与生成设置相同，请求需要的前缀互不复用。服务从停止状态启动，三种部署方案都在 10 秒后就绪；就绪后的队列按连续流量模型处理。设计目标是持续处理新到达的请求，并在开始启动后的 25 秒内消除启动积压。
 
-容量条件也一并给定：各 worker 装入模型后，留出 12 GiB 专用于本例的 KV 与交接缓冲，调度器最多允许八份活动请求状态同时驻留；使用共享池的方案另有 64 GiB 状态容量。每个请求经过 128 次 decode 后，其 KV 覆盖 8320 个位置，占约 1.14 GiB。八份活动状态共约 9.14 GiB，再预留一份 1.125 GiB 接收缓冲，共约 10.3 GiB，能放入每个 worker 的 12 GiB 可用空间。尚未执行的请求只保留输入，分配到缓存空间后再开始计算。
+容量条件也一并给定：各 worker 将模型加载到内存后，留出 12 GiB 专用于本例的 KV 与交接缓冲，调度器最多允许八份活动请求状态同时驻留；使用共享池的方案另有 64 GiB 状态容量。每个请求经过 128 次 decode 后，其 KV 覆盖 8320 个位置，占约 1.14 GiB。八份活动状态共约 9.14 GiB，再预留一份 1.125 GiB 接收缓冲，共约 10.3 GiB，能放入每个 worker 的 12 GiB 可用空间。尚未执行的请求只保留输入，分配到缓存空间后再开始计算。
 
 现在比较完整副本、直接交接的 PD、经共享池交接的 PD。共享池按整份写入后读取，因此每请求在公共通道上搬两次；其读写端均能达到该通道的带宽。
 
@@ -543,7 +545,7 @@
 
 接着检查这一方案能否按期处理完启动期间积压的请求。10 秒后积压 40 个请求；直接 PD 就绪后每秒完成八个，同时每秒又来四个，净减少四个，10 秒后排空。从开始启动算起共 20 秒，满足 25 秒目标。完整副本在就绪后仍每秒增加 0.8 个请求，到第 25 秒时积压已从 40 增至 52 个。
 
-上述方案能在 20 秒内排空，但设备稍慢时是否仍能满足 25 秒期限？将问题反过来，就能求出达到这个期限所需的最低服务率。启动占去 10 秒，剩余 15 秒要消除 40 个请求，同时继续承接每秒四个新请求，因而要求
+上述方案能在 20 秒内排空，但设备稍慢时是否仍能满足 25 秒期限？将问题反过来，就能求出达到这个期限所需的最低服务率。启动占去 10 秒，剩余 15 秒要消除 40 个请求，同时继续处理每秒到达的四个新请求，因而要求
 
 $$
 \mu-4\geq\frac{40}{15},\qquad \mu\geq\frac{20}{3}\approx6.7\ \text{请求/s}.
@@ -551,9 +553,9 @@
 
 直接 PD 的八请求/s 高于这一最低要求。若有效服务能力降至五请求/s，稳态仍能处理持续到达的请求，但排空要到第 50 秒，错过目标。图 9-30 中两条下降线的斜率，正是八减四和五减四得到的净排空能力。
 
-计算服务成本时，还要考虑资源的计费方式。设八个 worker 按小时预留，总成本为八元，实际每秒完成四个请求，则每小时完成 14400 个请求，成本约为每千请求 0.56 元。若到达率仍为四，具有八请求/s 的能力不会凭空产生另外四个请求；留下的能力用于消化突发和启动积压。
-
-比较满负载时的请求处理量，3.2 与八请求/s 对应每千请求约 0.69 元与 0.28 元。若业务另设完成时限，后一方案仅有四分之一请求合格，每秒合格完成的请求便降到两个，每千个合格请求的成本升至约 1.11 元。一般写成
+计算服务成本时，还要考虑资源的计费方式。设八个 worker 按小时预留，每小时总成本为八元，实际每秒完成四个请求，则每小时完成 14400 个请求，成本约为每千请求 0.56 元。若到达率仍为四，具有八请求/s 的能力不会凭空产生另外四个请求；留下的能力用于消化突发和启动积压。
+
+比较满负载时的请求处理量，3.2 与八请求/s 对应每千请求约 0.69 元与 0.28 元。若业务另设完成时限，后一方案仅有四分之一请求合格，每秒合格的请求数便降到两个，每千个合格请求的成本升至约 1.11 元。一般写成
 
 $$
 C_{effective}=\frac{\text{统计期间的总成本}}
@@ -570,9 +572,9 @@
 
 共享池的比较改变了传输次数。下面固定 D 没有缓存，只改变 P 需要重新计算多少输入，看看资源分配是否也应调整。假定 P 已在本地命中 6144 个位置，D 仍为空。沿第 9.2 节计算，完整副本合计约 5.88 请求/s，原来四 P、四 D 为八请求/s，改成两个 A 做 P、其余六个 worker 做 D 则为九请求/s。若仍采用相同的 10 秒启动与四请求/s 到达，三者从开始启动算起分别约在第 31.3、20 和 18 秒排空。前缀复用使完整副本也能处理稳定到达的请求，但它仍达不到第 25 秒排空的目标；两 P、六 D 则进一步缩短积压持续时间。
 
-再把输出改为 1025 个 token，并恢复无命中输入，最佳一 P、七 D 也只有约 1.19 请求/s。此时改变 P、D 实例的数量比例已无法让原来的八个 worker 承接四请求/s。复制四组这样的八 worker 部署，可得到约 4.75 请求/s 的合计能力；若还要求相同的启动与排空期限，所需能力仍为至少 $20/3$ 请求/s，五组只能提供约 5.94，六组约为 7.13，因而要配置六组。保持原有设备组合按组扩展时，持续处理到达的请求需要 32 个 worker，满足恢复期限需要 48 个。额外设备的用途是加快积压消退。
-
-这也给出了把本章推导用于新系统的顺序。先把每请求工作换算成各类资源需求；再按整数资源分配找出最忙的池或设备；按执行顺序计入状态传输时间与内存占用；最后用服务率减去到达率得到的剩余处理能力求出突发或启动积压的持续时间。若限制项是 D，增加 P 不会缩短队列；若限制项是共享通道，增加计算副本只会让更多请求等候取回。
+再把输出改为 1025 个 token，并恢复无命中输入，最佳一 P、七 D 也只有约 1.19 请求/s。此时改变 P、D 实例的数量比例已无法让原来的八个 worker 处理每秒四个请求。复制四组这样的八 worker 部署，可得到约 4.75 请求/s 的合计能力；若还要求相同的启动与排空期限，所需能力仍为至少 $20/3$ 请求/s，五组只能提供约 5.94，六组约为 7.13，因而要配置六组。保持原有设备组合按组扩展时，持续处理到达的请求需要 32 个 worker，满足恢复期限需要 48 个。额外设备的用途是加快积压消退。
+
+这也给出了把本章推导用于新系统的顺序。先把每个请求的工作量换算成各类资源需求；再按整数资源分配找出最忙的池或设备；按执行顺序计入状态传输时间与内存占用；最后用服务率减去到达率得到的剩余处理能力求出突发或启动积压的持续时间。若瓶颈是 D，增加 P 不会减少积压；若瓶颈是共享通道，增加计算副本只会让更多请求等候取回。
 
 PD 的八 worker 案例和 MoE 的专家案例在这里相接。前者将不同阶段交给更擅长处理该阶段的设备，后者通过批内复用减少权重读取，再通过专家副本分散最忙设备上的工作。共享 KV 则保存已完成的计算结果，供下一次请求复用，启动与恢复决定这些设备何时能够提供服务。一次部署选择的依据，是这些部署方式共同改变了多少计算量和等待时间，以及消除积压所需的时间。
 
@@ -588,17 +590,17 @@
 
 **9-3　专家批量与权重位宽如何改变 CPU 执行瓶颈。** 用例 9.3 的同一专家形状，求 CPU 执行从读取受限转为计算受限时，每个专家接收的 token 数，再将 DRAM 带宽减半。分别比较每个专家接收 1 个和 128 个 token 时的执行时间；将权重改为每元素一字节、有效 CPU 算力保持不变，重新求解读取与计算的交点，并解释它的移动方向。
 
-**9-4　PD 与 AF 的传输量、启动次数与通信重叠〔核心〕。** 采用 Qwen3-8B，设有效交接带宽为 25 GB/s，分别取 1、5、20 μs 的启动开销，比较一次 1.125 GiB 交接与总字节数相同的 72 次交接。再采用例 9.4 中 AF 的实际载荷大小，计算输入 8192 个 token、输出 129 个 token 的完整请求所需的累计交接时间。若有四个微批次，每个微批次在两侧分别计算 2 ms 和 3 ms，求在分离方案仍能节省总时间的条件下，最多可以增加多少通信与计算耗时。
+**9-4　PD 与 AF 的传输量、启动次数与通信重叠〔核心〕。** 采用 Qwen3-8B，设有效交接带宽为 25 GB/s，分别取 1、5、20 μs 的启动开销，比较一次 1.125 GiB 交接与总字节数相同的 72 次交接。再采用例 9.4 中 AF 的实际载荷大小，计算输入 8192 个 token、输出 129 个 token 的完整请求所需的累计交接时间。若有四个微批次，每个微批次在两侧分别计算 2 ms 和 3 ms，求在分离方案仍能节省总时间的条件下，通信与计算合计最多可以增加多少耗时。
 
 **9-5　异构推理的权重容量与 CPU 处理能力约束。** 从已保存的 DeepSeek V4-Flash 或 Kimi K2 配置还原权重存放、GPU 缓冲和主存需求。增加请求并发后，分别预测被访问的专家数量、各专家的任务数，以及负载最重的 NUMA 节点。再构造一组总权重可以容纳、但到达率超过 CPU 计算能力的请求条件，推导积压增长率。
 
-**9-6　专家副本的容量约束与复制成本回收。** 设八个专家分别接收 $[32,16,8,4,2,1,1,0]$ 行输入，比较只计算有效行、只对非空专家补齐，以及对全部专家补齐时的计算量。随后复算例 9.5 的严格回本批数，把每卡额外容量改为 32 MiB，解释为什么应先拒绝不可行副本方案。对动态热点给出观察窗口与迁移成本的比较方法。
+**9-6　专家副本的容量约束与复制成本回收。** 设八个专家分别接收 $[32,16,8,4,2,1,1,0]$ 行输入，比较只计算有效行、只对非空专家补齐，以及对全部专家补齐时的计算量。随后复算例 9.5 的累计节省时间首次超过复制时间的批次数，把每卡额外容量改为 32 MiB，解释为什么应先排除不可行的副本方案。对动态热点给出观察窗口与迁移成本的比较方法。
 
 **9-7　KV 保存、取回与重算的收益条件〔核心〕。** 给定一份占 1.125 GiB 的前缀 KV，比较重算、一次取回并驻留、100 次逐步远程读取。进一步计入写入耗时，以及两次请求之间保留缓存的时间，推导何时保存 KV 比重算更合算；设计一个改变 HBM 可用容量后需要将状态从 HBM 换出的例子。结合已有双引擎记录，解释共享 CPU 池从 4 GiB 增至 8 GiB 时，哪些复用机会得以保留。
 
 **9-8　KV 缺页与版本不兼容时的恢复选择。** 解释读取 1024 个位置但只复用 1008 个位置的计量差异。对缺页、截断页、版本不兼容分别提出继续等待、部分重算与放弃缓存的判断条件。把“请求完成”“数据修复”“输出一致”列为三个分别检查的问题。
 
-**9-9　缓存路由的带宽交点与首 token 尾延迟。** 推导例 9.6 的远端取回与本地重算耗时相等时的带宽，并计算 $p=0.5,0.9,0.99$ 时，选择 A 路径的首 token 时间均值，以及按逆累积分布定义的 p99。再假设 HBM 副本不可用时，CPU 副本仍可用，且取回过程可以与 80 ms 的排队时间重叠。检查原来的二点分布模型是否仍然适用。
+**9-9　缓存路由的带宽交点与首 token 尾延迟。** 推导例 9.6 的远端取回与本地重算耗时相等时的带宽，并计算 $p=0.5,0.9,0.99$ 时，选择 A 路径的首 token 时间均值，以及按逆累积分布定义的 p99。再假设 HBM 副本不可用时，CPU 副本仍可用，且取回过程可以与 80 ms 的排队时间重叠。检查原来的两点分布模型是否仍然适用。
 
 **9-10　长短输出混合负载下的 PD 扩容与排队〔核心〕。** 沿用例 9.7，每秒四个请求中一半输出 129 个 token，另一半输出 1025 个 token，均输入 8192 个位置，无前缀命中；池内按长期平均工作量分配能力。先求每请求平均 D 调用数，再枚举将八个 worker 分配给 P、D 两阶段的所有数量组合。允许复制相同的八 worker 组合，分别求两种目标下所需的最少组数：持续处理新到达的请求；启动耗时 10 秒，并在开始启动后的第 25 秒前清空积压。最后将长输出请求集中到每分钟的前十秒到达，画出队列变化，并说明平均需求不变时，哪一段时间决定容量选择。
 
@@ -613,57 +615,57 @@
 
 这个十行边界与图 9-14 的 78／79 行边界回答不同问题。十行是 CPU 从带宽受限转为计算受限的分界；78／79 行比较包括权重搬运的完整 CPU、GPU 两条路径。CPU 在十行后已经计算受限，但仍能继续胜过需要先搬权重的 GPU 路径，直到累计计算代价超过这笔搬运成本。
 
-[^ownership]: [同一 MoE 请求的权重、状态与通信归属](../research/2026-infra-survey/parallel-moe-ownership/NOTES.md)，已有调研中在指定条件下完成的计算与验证。其 TP2×EP4、处理同一批请求和 FP32 传输格式是明确教学条件，用于具体展示通信归属。
-
-[^pd]: DistServe、Splitwise 与阶段放置的已归档材料见[本章扩写资料](../outlines/extensions/09-分布式推理.md#detail-9.2)及[阶段分工与状态交接研究](../case-studies/chunking-and-state-transfer.md)。历史实验的硬件和请求不移作本章 Qwen3 测量。
+[^ownership]: [同一 MoE 请求的权重、状态与通信归属](../research/2026-infra-survey/parallel-moe-ownership/NOTES.md)。其 TP2×EP4、处理同一批请求和 FP32 传输格式是明确教学条件，用于具体展示通信归属。
+
+[^pd]: DistServe、Splitwise 与阶段放置的已归档材料见[本章扩写资料](../outlines/extensions/09-分布式推理.md#detail-9.2)及[阶段分工与状态交接研究](../case-studies/chunking-and-state-transfer.md)。
 
 [^handoff]: [Qwen3-8B 的 PD／AF 交接计算](../calculations/results/pd-af-handoff-qwen8.md)，固定官方模型形状、字节与启动假设。生成图读取同名 JSON。 25 GB/s使用十进制单位，1.125 GiB使用二进制单位；精确载荷1207959552 bytes，纯传输48.31838208 ms，加5 μs为48.32338208 ms。
 
-[^pool]: [P、D 实例数量的整数分配](../calculations/results/pd-pool-book.md)、[同构对照](../calculations/results/pd-pool-homogeneous.md)、[前缀命中](../calculations/results/pd-pool-prefix.md)、[长输出](../calculations/results/pd-pool-long-output.md)。输入有效能力、固定分配和理想池内流量模型的限制随结果保存。
-
-[^kt]: [权重卸载与执行位置](../case-studies/weight-offload-execution.md)及[固定版本查阅范围](../references/framework-history/2026-09-08/offload-execution/README.md)。本文没有重新审查 kt-kernel 全部实现。
-
-[^locality]: [每专家 78 个 token](../calculations/results/expert-locality-boundary78.md)、[79 个 token](../calculations/results/expert-locality-boundary79.md)。本章使用 200 GB/s 有效 DRAM 能力；较早案例笔记中的 100 GiB/s 是另一组假设，不混用其时间。
-
-[^kt-audit]: [KTransformers 公开实验记录复核](../experiments/ch09/09-03/README.md)。教程与论文平台分开；公开质量变化为原表复核，没有在本书环境重测。 教程为双6454S＋4090，论文为双8452Y，不能移用平台计时；Expert Deferral公开质量变化有正有负。
-
-[^capacity]: [DeepSeek V4-Flash 与 Kimi K2 配置记录](../experiments/ch09/09-05/README.md)。配置中的并发上限不等于实际容量扫描。
-
-[^af]: [跨机执行与框架演进](../case-studies/framework-evolution.md)及[AF 扩写依据](../outlines/extensions/09-分布式推理.md#detail-9.3.4)，保存 MegaScale-Infer 与实验性 AFD 的版本条件。
-
-[^moe]: [MoE Serving Tax、CRAFT 与启动研究笔记](../case-studies/moe-and-startup.md)，分别保存论文所读范围和历史平台，正文采用资源分析而非跨平台性能排名。
-
-[^replica]: [专家副本回本计算](../calculations/results/replica-payback-book.md)。固定路由、有效能力、串行复制、逐卡额外可用空间，不包含真实在线迁移协议。 准备为10.60464608 ms，每批节省0.402784256 ms；正文显示值经舍入，27批边界按原值求解。
-
-[^expert-exp]: [专家执行与后端已有记录](../experiments/ch09/09-06/README.md)，多设备复制和通信收益仍需验证。
-
-[^cache]: [缓存层次、路径与路由研究](../case-studies/cache-tiers-and-routing.md)。本文按已查阅实现区分实例私有 host 与配置共享的存储层。
-
-[^shared]: [共享 CPU KV 池容量对照](../experiments/ch09/09-07/shared-kv/README.md)与[真实上下文保留后的 Agent 回放](../experiments/ch09/09-07/history-preserved/README.md)。受控查找任务与真实 Agent 质量结论分别报告。 真实Agent回放还出现提前结束及任务质量下降，此处容量对照只讨论受控复用机会；其耗时不能作同质量加速结论。
-
-[^v4]: [DeepSeek V4 技术报告](../references/files/papers/deepseek-v4.pdf)的状态持久化与生成服务内容，以及[本章的阅读笔记](../outlines/extensions/09-分布式推理.md#detail-9.6.3)。模型状态形状和恢复机制依固定版本解释。
-
-[^restart]: [重启页读取与实际复用核算](../calculations/results/cache-restart-book.md)，读取 144 MiB 与复用 141.75 MiB 分列，输出核验不等于 KV 逐位一致。 原始有效复用为141.75 MiB，正文约142 MiB；1024／1008位置与64／63页保留精确整数。
-
-[^fault]: [截断页的预取策略对照](../experiments/ch09/09-08/prefetch-policy/README.md)，60 秒为观察窗口，不是所有故障的通用超时保证。
+[^pool]: [P、D 实例数量的整数分配](../calculations/results/pd-pool-book.md)、[同构对照](../calculations/results/pd-pool-homogeneous.md)、[前缀命中](../calculations/results/pd-pool-prefix.md)、[长输出](../calculations/results/pd-pool-long-output.md)。
+
+[^kt]: [权重卸载与执行位置](../case-studies/weight-offload-execution.md)及[实现版本说明](../references/framework-history/2026-09-08/offload-execution/README.md)。
+
+[^locality]: [每专家 78 个 token](../calculations/results/expert-locality-boundary78.md)、[79 个 token](../calculations/results/expert-locality-boundary79.md)。本例采用 200 GB/s 的有效 DRAM 速率。
+
+[^kt-audit]: [KTransformers 公开实验记录](../experiments/ch09/09-03/README.md)。教程平台为双 6454S＋4090，论文平台为双 8452Y；Expert Deferral 的公开结果包含质量提升与下降两种情况。
+
+[^capacity]: [DeepSeek V4-Flash 与 Kimi K2 配置记录](../experiments/ch09/09-05/README.md)。表中并发上限取自部署配置。
+
+[^af]: [跨机执行与框架演进](../case-studies/framework-evolution.md)及[AF 扩写依据](../outlines/extensions/09-分布式推理.md#detail-9.3.4)。
+
+[^moe]: [MoE Serving Tax、CRAFT 与启动研究笔记](../case-studies/moe-and-startup.md)。
+
+[^replica]: [专家副本回本计算](../calculations/results/replica-payback-book.md)。算例固定路由、有效传输速率与逐卡额外可用空间，按串行复制计算准备时间。 准备为10.60464608 ms，每批节省0.402784256 ms；正文显示值经舍入，27批边界按原值求解。
+
+[^expert-exp]: [专家执行与后端实验记录](../experiments/ch09/09-06/README.md)。
+
+[^cache]: [缓存层次、路径与路由研究](../case-studies/cache-tiers-and-routing.md)。
+
+[^shared]: [共享 CPU KV 池容量对照](../experiments/ch09/09-07/shared-kv/README.md)与[真实上下文保留后的 Agent 回放](../experiments/ch09/09-07/history-preserved/README.md)。容量对照采用受控查找任务；真实 Agent 回放中出现了提前结束及任务质量下降。
+
+[^v4]: [DeepSeek V4 技术报告](../references/files/papers/deepseek-v4.pdf)的状态持久化与生成服务内容，以及[本章的阅读笔记](../outlines/extensions/09-分布式推理.md#detail-9.6.3)。
+
+[^restart]: [重启页读取与实际复用核算](../calculations/results/cache-restart-book.md)，读取量为 144 MiB，有效复用量为 141.75 MiB。读取 64 页、共 1024 个位置，其中 63 页、共 1008 个位置得到复用。
+
+[^fault]: [截断页的预取策略对照](../experiments/ch09/09-08/prefetch-policy/README.md)，实验观察窗口为 60 秒。
 
 [^route]: [缓存路由计算](../calculations/results/cache-route-book.md)、[快速远端对照](../calculations/results/cache-route-fast-remote.md)。具体公式包含整份远端→host→GPU 串行路径。
 
-[^events]: [缓存事件与路由判断](../case-studies/cache-events-and-routing.md)、[缓存失效两点分布](../calculations/results/cache-route-stale.md)。Dynamo、vLLM 与 SGLang 的事件范围按各自固定集成条件解释。
+[^events]: [缓存事件与路由判断](../case-studies/cache-events-and-routing.md)、[缓存失效两点分布](../calculations/results/cache-route-stale.md)。
 
 [^pressure]: [真实路由压力的配对核算](../calculations/results/router-pressure-book.md)，区分目标请求和完整任务对的完成时间，原始条件随结果保存。
 
-[^startup]: [Breaking the Ice 的研究整理](../case-studies/moe-and-startup.md)，采用历史 vLLM v0.10.1.1 的阶段分析方法，不沿用其时间为当前部署预测。
-
-[^branch]: [HiCache 请求分支观测](../experiments/ch09/09-10/branch-observation/README.md)，使用同请求 ID 的事件链解释限额和有效命中；额外 trace 扰动使本批不适合性能排名。 原记录预取限额为3289位置，4096为已登记占用；八个请求由客户端同时发出，GPU最大运行请求数为一。
-
-[^switch]: [动态并行与状态条件](../case-studies/parallel-switching-and-state.md)、[专家分派与扩缩容](../case-studies/expert-dispatch-and-resizing.md)，保留当前接口支持范围与未测量项。
-
-[^migration]: [串行迁移的时间计算](../calculations/results/reconfiguration-declared-serial.md)，分别记录理论最短耗时、假设的额外耗时和尚未测量的项目。
-
-[^cost-model]: 例 9.7 的容量、共享通道、启动时间、排空目标和成本，以及 SLO 通过比例、9.4.3 的重叠时间均为教材假设，用于推导条件变化；价格不是设备报价。单位成本用完整小时成本除以（3600×有效请求率）。迁移对比沿用原载荷，另给5／25 GB/s带宽与9秒串行准备，区别于原结果的100秒声明网络时间。
-
-[^v41-case]: [DeepSeek V4.1 官方技术报告](../calculations/sources/deepseek-v4.1-flash/DeepSeek_V41_Tech_Report.pdf)，第 1、2、3 节与第 6 节；[跨章会话的固定条件与复算](../calculations/results/v41-throughline.json)。架构、格式和部署机制按报告区分，教学时间假设不作实测。
+[^startup]: [Breaking the Ice 的研究整理](../case-studies/moe-and-startup.md)，研究使用 vLLM v0.10.1.1。
+
+[^branch]: [HiCache 请求分支观测](../experiments/ch09/09-10/branch-observation/README.md)，使用同请求 ID 的事件链解释限额和有效命中。 原记录预取限额为3289位置，4096为已登记占用；八个请求由客户端同时发出，GPU最大运行请求数为一。
+
+[^switch]: [动态并行与状态条件](../case-studies/parallel-switching-and-state.md)、[专家分派与扩缩容](../case-studies/expert-dispatch-and-resizing.md)。
+
+[^migration]: [串行迁移的时间计算](../calculations/results/reconfiguration-declared-serial.md)，按载荷、设定带宽和串行准备时间计算迁移耗时。
+
+[^cost-model]: 例 9.7 的容量、共享通道、启动时间、排空目标和成本，以及 SLO 通过比例、9.4.3 的重叠时间均为教材假设，用于推导条件变化。单位成本用完整小时成本除以（3600×有效请求率）。迁移算例分别采用 5／25 GB/s 带宽，串行准备时间设为 9 秒。
+
+[^v41-case]: [DeepSeek V4.1 官方技术报告](../calculations/sources/deepseek-v4.1-flash/DeepSeek_V41_Tech_Report.pdf)，第 1、2、3 节与第 6 节；[跨章会话的固定条件与复算](../calculations/results/v41-throughline.json)。
 
 ## 本章小结
 
