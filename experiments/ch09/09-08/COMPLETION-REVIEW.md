# Historical experiment 9-8 completion review

The original exercise specifies advanced request replay / storage records and explicitly asks for V4 state calculations. The evidence below completes that scope. Actual native Qwen calls, actual storage operations, reference-policy replay and V4 shape calculations remain distinct; this is not a live V4 four-tier implementation or an Agent task-quality benchmark.

| Original requirement | Evidence | Verified coverage and limits |
|---|---|---|
| Same Agent traces | agent-host-capacity and agent-dram-capacity input/source hashes; ../09-09/remote-kv/cross-mac-traces-001/results/agent-producer/requests.jsonl | The same twelve fixed input prefixes and 197 chained page identities. Native target runs return one token; replay completion measures these recorded requests, not an independently completed semantic Agent task. |
| HBM / DRAM capacities | agent-host-capacity/summary.json; agent-dram-capacity/summary.json | Five fresh engines, 180 model calls including 120 targets. GPU pools 4096/8192/16384 tokens; separate fixed-GPU4096 scan changes host ratio1.01/1.25/2. Native device/host counters, seeded pressure and output comparison verified. |
| Local SSD / remote capacities | agent-storage-io/results/operations.jsonl; ../09-09/remote-kv/cross-mac-traces-001; agent-policy-replay/results.json | Real direct-I/O on the server ext4/md filesystem and real Mac–RTX page RPCs calibrate a reference capacity replay. All four retained-page budgets vary independently over0/64/256; no physical disk repartition or quota sweep is claimed. |
| Full saving / periodic checkpoints / recomputation | agent-policy-replay/PROTOCOL.md and results.json |729 scenarios and8748 per-request records, three policies and three fault conditions. Inclusive page-LRU, synchronous barriers and sufficient separate execution working set are declared. |
| V4 compressed/window state: write, read, necessary recomputation | v4-state-accounting/results.json and PROTOCOL.md |24 scenarios,288 recovery decisions. Full backbone window/compressed/index history and FP32 main/index compressor buffers; incomplete groups and CSA overlap retained. Same workload shape, not V4 tokenizer output. Snapshot endpoints require capture before the input tail. Complete compatible snapshots restore; incomplete/history-only snapshots require full recomputation under this contract. |
| Restart recovery and invalidation | results/consumer-v6; missing-pages; storage-contract; truncated-request; prefetch-policy; both new replay results | Actual worker restart recovers1008 tokens; missing-page/corruption/native fallback evidence retained, including timeout failure. Replay clears volatile or all indexed states explicitly. Remote HTTP persistence covers a model-worker restart with the store intact; it does not establish power-loss durability. |
| Hit rates | Native summaries and agent-policy-replay/results.json | Both request and token denominators, tier counts and first missing prefix are retained. Repeated page keys do not imply semantic quality or bitwise KV equality. |
| p95 retrieval time | agent-storage-io/summary.json; remote RPC logs; agent-policy-replay/results.json |591 actual direct reads: page sample p95=1.074ms. Replay storage-read p95 is a quantile of composed measured primitives, not a production percentile. Full local256-page restart example:151.874ms; periodic4:105.642ms. No retrieval gives null, not a fabricated zero-latency hit. |
| Write volume / completion time | Storage summaries; native request records; agent-policy-replay/results.json | Exact logical writes and synchronous composed trace seconds recorded. For local256 pages, full writes464,781,312B /3.505s; periodic4 writes460,062,720B /3.243s; no-persistence recomputation1.162s. These are calibrated request-replay completion proxies, not observed full Agent task times. V4 tensor bytes/recompute counts are analytical; V4 timing remains unmeasured. |
| Figure9-7: state paths, capacities, writes and recovery | final-comparison/figure-9-7.svg, .pdf, .png; plot.py and plot-sources.json | Six panels include tier path, dense-page capacity/hits/writes/completion and V4 payload/recovery work. Rendered PNG visually inspected; source hashes bind the figure to results. Native capacity figure remains in agent-host-capacity/capacity.svg. |

Revalidation passed native capacity analyzers, direct-I/O payload/hash analysis,729-scenario reference-policy invariants and the independent V4 layer-payload formula/causal recovery checks. The conclusions are negative where the measurements or model indicate higher cost. Saving more state is not asserted to improve latency.

Historical9-8 is delivered in the original request-replay/storage-record scope. Production p95, an implemented V4 serializer, V4 model execution, full semantic task evaluation and equal local/remote power-loss durability are not established by these artifacts and must not be presented as measured results.

Figure numbering follows the frozen historical inventory: its requested figure9-7 is the supplemental figure delivered here. The current outline has renumbered figures and separately describes figure9-12; its existing plan is preserved. Storage topology was rechecked by SSH: ext4/md0p1 is backed by RAID5 across three HP SSD EX900 Plus NVMe drives, not one standalone disk; raw lsblk output is in final-comparison/storage-topology.json.
