--- before/07-数据中心网络.md
+++ manuscripts/07-数据中心网络.md
@@ -12,7 +12,7 @@
 
 ### 7.1.1 存储需求如何引出跨服务器协作
 
-模型需要的存储空间超过单机容量，是跨服务器协作的一个直接原因。一个 8B 参数模型采用混合精度 Adam 训练。**混合精度**是在训练中为不同对象选用不同数值精度；**Adam** 是利用梯度一阶矩与二阶矩调整更新步长的优化器，一阶矩记录梯度的平滑平均，二阶矩记录梯度平方的平滑平均。若参数、梯度、FP32 主权重以及优化器的一阶矩和二阶矩合计占用 16 bytes／参数，就需要 128 GB。一张 80 GB 卡放不下，两张卡合计 160 GB，扣除这些状态后剩下 32 GB 留给激活和工作区。参数增加到 284B 时，采用相同的数据表示，这些状态共需约 4.5 TB，必须分散到更多设备上存储和处理。[^capacity]
+模型需要的存储空间超过单机容量，是跨服务器协作的一个直接原因。以一个采用混合精度 Adam 训练的 8B 参数模型为例。**混合精度**是在训练中为不同对象选用不同数值精度；**Adam** 是利用梯度一阶矩与二阶矩调整更新步长的优化器，一阶矩记录梯度的平滑平均，二阶矩记录梯度平方的平滑平均。若参数、梯度、FP32 主权重以及优化器的一阶矩和二阶矩合计占用 16 bytes／参数，就需要 128 GB。一张 80 GB 卡放不下，两张卡合计 160 GB，扣除这些状态后剩下 32 GB 留给激活和工作区。参数增加到 284B 时，采用相同的数据表示，这些状态共需约 4.5 TB，必须分散到更多设备上存储和处理。[^capacity]
 
 推理虽然不需要保存梯度和优化器状态，大模型的权重仍可能需要多台服务器才能容纳。将约 1.6T 参数的 DeepSeek V4-Pro 和约 2.8T 参数的 Kimi K3 按每参数 0.5 byte 存放，权重分别约为 800 GB 和 1.4 TB。一台八卡、每卡 80 GB 的服务器合计 640 GB，仅存放权重就至少分别需要两台和三台服务器。混合专家模型（MoE）由路由器为每个 token 选择部分前馈网络，即专家。每次只激活部分专家，降低的是当次计算量；其余专家的权重也需要安排存放位置。
 
@@ -44,7 +44,7 @@
 
 这个式子给出传输时间的下界：无论怎样调度，链路每秒最多只能传送其带宽所允许的字节数。全双工链路的两个方向分别计算；若多个端口共享内部接口，再对该接口计算合计流量。同一份数据必须依次通过串联的各段链路，因此端到端带宽受最慢的一段限制，不能将各段带宽相加。
 
-**Clos 网络**由多层交换机构成，通过中间交换层为端点提供多条路径。在分层 Clos 网络中，叶交换机连接服务器，上层交换机提供叶交换机之间的多条路径。下联总带宽为 400 GB/s、上联总带宽为 200 GB/s 时，**超售比**即下联总带宽与上联总带宽的比值，此处为 2:1。200 GB/s 的上行就是需要离开本叶交换机的流量共同经过的割集。连接在同一叶交换机上的服务器可以直接交换数据；只有跨叶交换机的通信才占用上行带宽。
+**Clos 网络**由多层交换机构成，通过中间交换层为端点提供多条路径。在分层 Clos 网络中，叶交换机连接服务器，上层交换机提供叶交换机之间的多条路径。下联总带宽为 400 GB/s、上联总带宽为 200 GB/s 时，**超售比**即下联总带宽与上联总带宽的比值，此处为 2:1。这些总带宽为 200 GB/s 的上行链路，构成了离开本叶交换机的流量必须经过的割集。连接在同一叶交换机上的服务器可以直接交换数据；只有跨叶交换机的通信才占用上行带宽。
 
 **例题 7.1：跨服务器通信何时限制增加设备的加速收益？** 先采用两服务器算例中的环形归约：每台服务器向另一台发送 336 MiB。共享出口为 40 GB/s，原设备规模下的计算时间为 20 ms。增加设备时，计算均匀分摊，跨服务器传输量保持不变。求完全串行和完全重叠两种安排的完成时间。
 
@@ -75,7 +75,7 @@
 
 笔者参与 UB 早期设计时，一个核心问题就是怎样为本地和远程协作提供统一接口。应用希望表达的是“读取这份数据”“把结果交给下一阶段”“等它完成后复用空间”。统一互联为寻址、数据访问和设备间通信提供共同的接口，让 CPU、加速器和其他设备都能参与其中。[^ub]
 
-理解这种统一，需要分开两件事。接口决定程序怎样表达工作，物理路径决定工作经过哪些资源。一次远程读取可以用一条指令发起，但数据仍要经过 40 GB/s 的出口；异步写入可以提前提交，接收方则要等数据就绪后才能开始使用。因此，本章将先求必需的流量，再求充分利用带宽所需的并发请求数，最后分析应用要求的操作顺序。统一接口的收益也在这三个层次上体现：减少搬运，缩短发起路径，简化协作。
+理解这种统一，需要区分两件事。接口决定程序怎样表达工作，物理路径决定工作经过哪些资源。一次远程读取可以用一条指令发起，但数据仍要经过 40 GB/s 的出口；异步写入可以提前提交，接收方则要等数据就绪后才能开始使用。因此，本章将先求必需的流量，再求充分利用带宽所需的并发请求数，最后分析应用要求的操作顺序。统一接口的收益也在这三个层次上体现：减少搬运，缩短发起路径，简化协作。
 
 ## 7.2 跨节点流量与物理路径
 
@@ -198,11 +198,11 @@
 
 将源设备到目标设备的载荷记为 $V_{ij}$。一行的和是对应发送端的发送量，一列的和是对应接收端的接收量；将跨越某个割集的元素相加，得到该割集的传输量。把这些量分别除以相应带宽，再取最大值，就是第 7.1 节资源模型在不规则交换上的应用。
 
-专家放置因此有两项互相联系的目标：让更多分派留在本地，让远端分派分散到有余量的端点。复制热门专家用额外容量减少远端访问；在同一目标设备合并多个专家的输入，可以先传一次激活，再在目标端展开分派。这两种方法都减少了相应路径上的传输量，从而缩短通信时间。
+专家放置因此有两项互相联系的目标：让更多分派留在本地，让远端分派分散到有余量的端点。复制热门专家用额外容量减少远端访问；将发往同一目标设备上多个专家的输入合并，可以先传一次激活，再在目标端展开分派。这两种方法都减少了相应路径上的传输量，从而缩短通信时间。
 
 ### 7.2.4 多网卡
 
-专家分派说明了多入口并行的收益和共享入口的限制。同样的分析也适用于发送端：增加网卡，只有在它提供了额外可用带宽时才会缩短传输。以前述两张 25 GB/s NIC 的配置为例。如果只使用一张，连续环十四轮共需约 14.1 ms；将每次传送拆成两个不重叠区间交给两张 NIC，吞吐升至共享出口允许的 40 GB/s，时间降至约 8.8 ms。第三张 NIC 加入同一个 40 GB/s 接口，无法再缩短这段传输时间。共享接口带宽耗尽后，增加网卡便不再提高吞吐率。
+专家分派说明了多入口并行的收益和共享入口的限制。同样的分析也适用于发送端：增加网卡，只有在它提供了额外可用带宽时才会缩短传输。以前述两张 25 GB/s NIC 的配置为例。如果只使用一张，连续环十四轮共需约 14.1 ms；将每次传送的数据拆成两个不重叠的区间，分别交给两张 NIC，吞吐升至共享出口允许的 40 GB/s，时间降至约 8.8 ms。第三张 NIC 加入同一个 40 GB/s 接口，无法再缩短这段传输时间。共享接口带宽耗尽后，增加网卡便不再提高吞吐率。
 
 另一种情况是与某张 GPU 直接连接的 NIC 忙碌，相邻 GPU 的 NIC 空闲。借用相邻出口要先把数据送到相邻 GPU，因此又引入一段本地传输。FuseLink 利用这种路径，通过机内 GPU 互联和网络缓冲重映射使用空闲 NIC。[^fuselink]
 
@@ -212,7 +212,7 @@
 
 *图 7-11：借用相邻网卡的路径。直接路径最多传输 40 GB/s；两张中继网卡合计 80 GB/s，但必须先经过剩余带宽为 60 GB/s 的本地互联。两路在下游汇合，最后还受外部网络和接收端可用带宽限制。箭头表示传输方向。*
 
-沿图 7-11 的分叉计算：直接路径与中继路径可以同时传输，但中继路径内部的两段必须依次经过。因此，直接和中继路径合计可提供 $40+\min(80,60)=100$ GB/s。设外部网络与接收路径共同允许的带宽为 $B_D$，真正的发送能力为
+沿图 7-11 的分叉计算：直接路径与中继路径可以同时传输，但数据必须依次经过中继路径内部的两段。因此，直接和中继路径合计可提供 $40+\min(80,60)=100$ GB/s。设外部网络与接收路径共同允许的带宽为 $B_D$，真正的发送能力为
 
 $$
 B_{\mathrm{eff}}=\min\bigl(B_D,\ 40+\min(80,60)\bigr)\ \mathrm{GB/s},
@@ -224,7 +224,7 @@
 
 > **实验 7-3 · 核心：本地带宽与共享出口如何影响归约算法选择**
 >
-> 完整复算例题 7.2。将本地带宽从 200 GB/s 降至 50 GB/s，求连续环和分层归约的时间，解释本地带宽下降后，为什么应选择另一种归约算法。再将每服务器 NIC 数从一张增为两张、三张，保持共享出口 40 GB/s 不变，画出增加网卡不再缩短时间的位置。
+> 完整复算例题 7.2。将本地带宽从 200 GB/s 降至 50 GB/s，求连续环和分层归约的时间，解释本地带宽下降后，为什么应选择另一种归约算法。再将每服务器 NIC 数从一张增为两张、三张，保持共享出口 40 GB/s 不变，画出归约时间随网卡数量变化的曲线，指出增加到多少张网卡后，继续增加网卡已无法缩短时间。
 
 ## 7.3 远程访问的路径与并发
 
@@ -234,7 +234,7 @@
 
 **单边读写**让发起者访问预先授权的远端内存。例如卡 0 可以向卡 4 的接收区域写入分片，由对端 NIC 完成数据移动。**双边消息**需要发送方与接收方配合。接收方预先提交接收请求并提供缓冲区，通信系统将到达的消息与相应请求匹配。已知目标位置的大块分片适合直接读写；来自多个发送者的通知适合用消息队列区分各条通知，并将其分发给相应的数据使用方（消费者）。
 
-大块数据与通知由此可以分工：先写分片，再发一个短消息说明分片可以使用。把写入和通知合并为一次提交，还能减少重复发起。消费者观察通知之后开始归约，两项操作的正确先后关系将在第 7.4 节展开。
+大块数据与通知由此可以分工：先写分片，再发一个短消息说明分片可以使用。把写入和通知合并为一次提交，还能减少重复发起。消费者收到并确认通知之后开始归约，两项操作的正确先后关系将在第 7.4 节展开。
 
 写入和通知完成了数据交接。如果还希望接收方按指定方式处理数据，就可以使用**远程过程调用**（remote procedure call，RPC）。它要求对端执行指定函数。一次调用包括参数传送、远端执行和结果返回。参数的表示会直接改变这条路径：JSON 是文本形式的数据交换格式；base64 把每 3 个二进制字节编码为 4 个文本字符。将 1 MiB 二进制数据放进 JSON 的 base64 字段，编码载荷约增加三分之一。改用二进制请求，就同时减少发送字节和编码工作。
 
@@ -242,13 +242,13 @@
 
 > **实验 7-4 · 延伸：去掉编码转换能缩短多少远程调用时间**
 >
-> 从配套记录选取同一载荷的成对调用，画出客户端编码、发送、等待、解码与服务端执行的位置。解释为什么服务端的执行时间已经包含在客户端的等待时间中。计算省去 base64 的字节收益，以及编码开销占比变化如何改变总调用的收益。
+> 从配套记录选取同一载荷的成对调用，画出客户端编码、发送、等待、解码和服务端执行的时间线。解释为什么服务端的执行时间已经包含在客户端的等待时间中。计算省去 base64 编码后减少的传输字节数，并分析编码耗时占比如何影响完整调用的加速收益。
 
 ### 7.3.2 由产生数据的设备直接发起通信
 
 上一小节从接收方看到的操作出发，区分了读写、消息与 RPC。现在沿相反方向看发送端：谁最先知道数据已经就绪，谁又负责提交请求？卡 0 的分片在 GPU 上产生。若 CPU 负责提交通信，GPU 先报告就绪，CPU 构造请求并提交 NIC，完成后再将状态交还 GPU。这条控制路径包含多次交接，即使载荷从未经过 CPU 内存，交接仍要消耗时间。
 
-**远程直接内存访问**（remote direct memory access，RDMA）让网卡访问已授权的远端内存。NVIDIA 的 **GPUDirect RDMA** 让 NIC 直接访问已经注册和授权的 GPU 内存，省去载荷经主机内存中转的步骤。采用 CPU 发起通信的实现时，请求提交和完成通知处理仍由 CPU 负责。设备发起则进一步让 GPU 构造或触发请求，并读取完成状态，使 GPU 在生成数据后即可直接发起发送。NVLink 是 NVIDIA 的设备互联，UB 是 Unified Bus 统一互联；URMA 是 UB 提供的统一远程内存访问接口。GPU 经 NVLink 访问对端、设备经 UB 发起异步访问，也是在各自路径上组织这种直接协作。
+**远程直接内存访问**（remote direct memory access，RDMA）让网卡访问已授权的远端内存。NVIDIA 的 **GPUDirect RDMA** 让 NIC 直接访问已经注册和授权的 GPU 内存，省去载荷经主机内存中转的步骤。采用 CPU 发起通信的实现时，请求提交和完成通知处理仍由 CPU 负责。由设备发起通信则进一步让 GPU 构造或触发请求，并读取完成状态，使 GPU 在生成数据后即可直接发起发送。NVLink 是 NVIDIA 的设备互联，UB 是 Unified Bus 统一互联；URMA 是 UB 提供的统一远程内存访问接口。GPU 经 NVLink 访问对端、设备经 UB 发起异步访问，也是在各自路径上组织这种直接协作。
 
 ![通信发起位置与数据通路](ch07/figure-7-7-access.svg)
 
@@ -264,13 +264,13 @@
 
 ![设备发起的 URMA 访问](ch07/figure-7-access-3.svg)
 
-*图 7-15：设备通过 URMA 提交异步读写，载荷经过 UB 互联到达目标设备；请求完成后按接口定义观察结果。*
+*图 7-15：设备通过 URMA 提交异步读写，载荷经过 UB 互联到达目标设备；请求完成后按接口规定检查完成状态和结果。*
 
 这种变化可以用第 7.2 节的消息模型解释。一条 8 KiB 消息在 25 GB/s 链路上的数据传输时间约为 0.33 μs。若请求启动时间从 5 μs 缩短到 2 μs，总时间便从约 5.3 μs 降至 2.3 μs，节省超过一半。64 MiB 的载荷本身需要约 2.7 ms，同样省下的 3 μs 只占很小部分。因此，小消息应优先减少提交请求的开销，大消息应优先提高数据传输速度。
 
 由主机统一组织通信，可以为不同程序提供通用的任务提交和完成处理机制。模型执行明确了数据的生产者、消费者和就绪条件后，可以把频繁的发起与完成处理放到设备侧。系统预先建立地址映射和访问权限，随后由设备连续发起计算与通信，省去反复经主机中转的控制操作。收益直接体现在上例的启动时间中。
 
-操作系统负责建立映射、权限和资源分配；设备负责已经授权的快速操作；完成通知把操作结果交回运行时。职责这样划分后，设备上的通信线程也会限制吞吐。要分析这种限制，先要分清一次请求等待多久、等待期间能否发起其他请求，再计算单位时间内能处理多少项请求。
+操作系统负责建立映射、设置权限和分配资源；设备负责已经授权的快速操作；完成通知把操作结果交回运行时。职责这样划分后，设备上的通信线程也会限制吞吐。要分析这种限制，先要分清一次请求等待多久、等待期间能否发起其他请求，再计算单位时间内能处理多少项请求。
 
 ### 7.3.3 指令与异步请求
 
@@ -280,7 +280,7 @@
 
 考察八个独立的远端读取，硬件能同时处理八项请求，每次等待 2 μs，忽略相对于等待时间很短的提交开销。逐次“发出—等待—使用”需要约 16 μs；若八项同时发出并等待结果，结果可以在约 2 μs 后一起到达。若第一个读取返回第二个地址，第二个又返回第三个地址，就必须等上一次读取返回后才能发起下一次读取。这个例子说明，能够同时发起多少次读取，取决于有多少个地址已经确定且互不依赖的请求。
 
-并行读取利用了不同请求之间的独立性。如果多次请求访问的是同一份数据，还可以利用另一种关系：第一次搬到本地，后续读取反复使用这个副本。假设消费者将完整读取一个 144 MiB 的不变 KV 快照，本地有足够空间容纳它。远程读取路径为 40 GB/s，搬回路径为 25 GB/s，本地读取为 1 TB/s。计入启动与目的端写入，一次远程读取约为 3.78 ms，一次搬回的固定成本约为 6.20 ms，之后每次本地读取约为 0.152 ms。[^remote]
+并行读取利用了不同请求之间的独立性。如果多次请求访问的是同一份数据，还可以利用另一种关系：第一次访问时将数据搬到本地，后续读取反复使用这个副本。假设消费者将完整读取一个 144 MiB 的不变 KV 快照，本地有足够空间容纳它。远程读取路径为 40 GB/s，搬回路径为 25 GB/s，本地读取为 1 TB/s。计入启动与目的端写入，一次远程读取约为 3.78 ms，一次搬回的固定成本约为 6.20 ms，之后每次本地读取约为 0.152 ms。[^remote]
 
 令复用次数为 $r$，两条路径的时间近似为
 
@@ -299,7 +299,7 @@
 
 *图 7-17：每次读取完整 144 MiB 快照时，两种方法的累计耗时。先搬回本地需要一次固定成本，从第二次读取开始节省时间。*
 
-两条曲线的差别来自“先付一次搬运成本，还是每次都跨网络读取”。接下来改变每次读取的范围。若一次调用只访问快照的少量位置，就可以按引用传递，让远端函数按需读取数据。设每次仅访问快照的 10%，而搬回方案仍复制全部快照。此时，按需远程读取的成本变为约 $0.378r$ ms，搬回后每次本地访问约为 0.0152 ms，交点约在 17.1 次，因而从第 18 次读取开始节省总读取时间。
+两条曲线的差别来自“先付一次搬运成本，还是每次都跨网络读取”。接下来改变每次读取的范围。若一次调用只访问快照的少量位置，就可以按引用传递，让远端函数按需读取数据。设每次仅访问快照的 10%，而搬回方案仍复制全部快照。此时，按需远程读取的成本变为约 $0.378r$ ms，搬回后每次本地访问约为 0.0152 ms，交点约在 17.1 次，因而从第 18 次读取开始，先搬回本地的方案才会节省总读取时间。
 
 ![每次仅读取十分之一快照](ch07/figure-7-snapshot-partial.svg)
 
@@ -321,9 +321,9 @@
 N\ge\left\lceil\frac{BT}{m}\right\rceil.
 $$
 
-**带宽—时延积**是目标带宽与等待时间的乘积，表示在等待期间持续利用链路所需的在途数据量。再除以每事务载荷，就得到请求数量。链路越快，等待越长，每次返回的数据越少，系统就需要同时保存更多未完成工作。
-
-**例题 7.3：远程读取的在途槽位不足，为何无法用满出口带宽？** 仍取 256 B、2 μs 和 40 GB/s。求 128 个活跃槽位的吞吐；再假设请求处理单元每 100 ns 最多启动一项事务，求新的吞吐上界。
+**带宽—时延积**是目标带宽与等待时间的乘积，表示在等待期间持续利用链路所需的在途数据量。再除以每事务载荷，就得到请求数量。链路越快，等待越长，每次返回的数据越少，系统就需要同时记录更多尚未完成的请求。
+
+**例题 7.3：远程读取的在途槽位不足，为何无法用满出口带宽？** 每个事务传输 256 B，占用槽位 2 μs，路径带宽为 40 GB/s。求使用 128 个活跃槽位时的有效读取带宽；再假设请求处理单元每 100 ns 最多启动一项事务，求同时受槽位数量与启动速率限制时的有效带宽上界。
 
 解答： 128 个槽位每 2 μs 周转一次，提供的载荷为 $128\times256$ B，因此最多约为 16.4 GB/s。若只让一个事务在途，则为 0.128 GB/s。分配了多少槽位与实际同时使用多少槽位，是两个不同的数量。
 
@@ -352,7 +352,7 @@
 
 > **实验 7-5 · 延伸：用满远程读取带宽需要多少在途事务**
 >
-> 将例题 7.3 的等待从 2 μs 增为 4 μs，分别求每次传输 256 B 和 4 KiB 时，维持目标带宽所需的在途事务数。固定 100 ns 启动间隔，求达到 40 GB/s 所需的最小事务大小。最后将独立地址改成指针链，解释完成整条指针链的读取所需时间如何随链长增长。
+> 将例题 7.3 中槽位从发出请求到可以复用的时间从 2 μs 增为 4 μs，分别求每次传输 256 B 和 4 KiB 时，维持目标带宽所需的在途事务数。固定 100 ns 启动间隔，求达到 40 GB/s 所需的最小事务大小。最后将独立地址改成指针链，解释完成整条指针链的读取所需时间如何随链长增长。
 
 ## 7.4 数据交接、顺序与状态管理
 
@@ -360,7 +360,7 @@
 
 数据送到以后，接收方可能还没开始用，也可能正在使用。这像把装有材料的托盘交给下一道工序：托盘已经送到，对方仍要花时间处理其中的材料，在用完之前不能把托盘中的材料换成下一批。对应到通信，发送完成、数据对接收方可见、接收方消费完成，需要分别判断。前一节把槽位占用时间作为已知条件，这一节沿这些事件确定缓冲区何时可以释放。
 
-在分层归约中，卡 0 将本地分片交给卡 4，卡 4 把它与自己的分片相加。发送者需要知道源缓冲何时可以改写，接收者需要知道目的缓冲何时可以读取，下一次操作需要知道这块目的缓冲何时可以覆盖。三者对应不同的事件。
+在分层归约中，卡 0 将本地分片交给卡 4，卡 4 把它与自己的分片相加。发送者需要知道源缓冲何时可以改写，接收者需要知道目的缓冲何时可以读取，下一次写入操作则需要确定这块目的缓冲何时可以覆盖。三者对应不同的事件。
 
 一份分片从生成到释放，要经过以下过程：生成数据、写入远端、使数据对接收方可见、发出就绪通知、读取并处理数据，最后释放空间。源缓冲能否复用，要看所用接口的完成语义是否保证传输不再读取它；接收方开始使用数据，需要保证写入已对它可见，并按约定发布和观察就绪通知；目的缓冲能否覆盖，还要等接收方不再使用其中的数据。网络传输完成本身不能代替消费完成通知。
 
@@ -465,13 +465,13 @@
 
 对比图 7-28 和图 7-29，增加槽位让蓝色传输条带提前了，右侧长长的橙色等待却仍然存在。决定持续吞吐的，是这些槽位最终释放得有多快。用并发与吞吐模型看，处理完成通知的线程每秒只处理 $4/(20\times10^{-6})=200000$ 项；每项 8 KiB，对应约 1.64 GB/s 的持续处理能力。采用这样的操作大小，要达到 40 GB/s，平均每秒需处理约 488 万项，约每 0.205 μs 一项。增加槽位只能容纳短时间内突然增多的请求，持续负载需要提高完成通知的处理速率或扩大每项操作的载荷。
 
-下游处理不及、通过限制上游提交速度来防止请求继续积压的机制，称为**背压（backpressure）**。这里，请求槽位用满后，端点暂停或减慢提交，直到有槽位释放。它通过限制未完成请求数，控制上游提交速度，防止未完成工作无限增长。若缓冲区的释放需要等待远端程序用完数据，背压就会跨设备传播；若许多工作共享传输状态，传播范围还会进一步扩大。
+下游来不及处理时，通过限制上游提交速度来防止请求继续积压的机制，称为**背压（backpressure）**。这里，请求槽位用满后，端点暂停或减慢提交，直到有槽位释放。它通过限制未完成请求数，控制上游提交速度，防止未完成工作无限增长。若缓冲区的释放需要等待远端程序用完数据，背压就会跨设备传播；若许多工作共享传输状态，传播范围还会进一步扩大。
 
 发生故障后，也必须先结束旧请求，再释放它们占用的空间。取消请求之后，系统先停止新提交，再等待或隔离旧的在途访问，最后释放空间。否则，迟到的旧写入会覆盖新对象。给每次复用的对象分配新的版本号、撤销访问权、隔离失联端点，都是为了防止旧请求访问已经重新分配的空间。超时触发恢复后，系统要先确认旧请求已经结束，或阻止其继续访问，再复用空间。至此，端点已经能限制提交、处理完成通知并安全释放空间；当多个端点共享网络时，同样的到达与处理速度之差，会表现为交换机中的队列。
 
 > **实验 7-7 · 核心：放宽操作顺序如何减少等待并保持数据正确**
 >
-> 将例题 7.4 的独立操作改为 50 μs，求两种安排的整组操作和独立操作的完成时间。再假设消费者收到通知后需要 30 μs 处理数据，标出目的缓冲区最早可以写入新数据的时刻。对旧值反例分别画出等待后读取、提前读取后重读两条执行路径。
+> 将例题 7.4 中独立操作 C 的耗时改为 50 μs，求两种安排的整组操作和独立操作的完成时间。再假设消费者收到通知后需要 30 μs 处理数据，标出目的缓冲区最早可以写入新数据的时刻。对旧值反例分别画出等待后读取、提前读取后重读两条执行路径。
 
 ## 7.5 共享网络中的拥塞与可靠性
 
@@ -493,7 +493,7 @@
 Q_{\max}=(80-50)\ \mathrm{GB/s}\times20\ \mathrm{ms}=600\ \mathrm{MB}.
 $$
 
-高峰结束后，还需 $600\ \mathrm{MB}/50\ \mathrm{GB/s}=12$ ms 排空。两项作业在 20 ms 时停止发送，但出口还要再传输 12 ms 才能发完这些数据。把第二项作业推迟 20 ms，高峰完全错开，任意时刻只有 40 GB/s 到达，队列保持为空。两种安排发送同样多的数据，差别来自相对时刻。[^queue]
+高峰结束后，还需 $600\ \mathrm{MB}/50\ \mathrm{GB/s}=12$ ms 排空。两项作业在 20 ms 时停止发送，但出口还要再传输 12 ms 才能发完这些数据。把第二项作业推迟 20 ms，高峰完全错开，任意时刻只有 40 GB/s 到达，队列保持为空。两种安排发送同样多的数据，差别在于两项作业开始发送的相对时刻。[^queue]
 
 ![通信高峰与积压](ch07/figure-7-15-congestion.svg)
 
@@ -517,7 +517,7 @@
 
 到达速率高于发送速率，队列增长；到达速率低于发送速率，积压减少。前面计算了一批数据通过链路所需的时间，这里进一步计算流量随时间变化时的队列长度。
 
-**链路流控**让相邻接收端在缓冲不足时暂停上游，防止接收端缓冲区溢出。**端到端拥塞控制**把瓶颈信息送回发送者，降低发送速率。前者迅速阻止局部溢出，后者让源端需求适应整条路径的传输能力。
+**链路流控**让相邻接收端在缓冲不足时暂停上游，防止接收端缓冲区溢出。**端到端拥塞控制**把瓶颈信息送回发送者，降低发送速率。前者迅速阻止局部溢出，后者让源端的发送速率适应整条路径的传输能力。
 
 沿用 80 GB/s 到达与 50 GB/s 出口，设缓冲总量为 512 KiB，已经占用 256 KiB。每微秒多到达的 30 KB 数据会占用剩余空间，只能支撑约 8.7 μs。反馈若到 20 μs 才使发送减速，会新产生 600 KB 超额数据，扣除剩余空间，约 338 KB 被丢弃。[^feedback]
 
@@ -543,7 +543,7 @@
 
 > **实验 7-8 · 延伸：缓冲大小与降速幅度如何影响积压消退**
 >
-> 设到达速率为 80 GB/s，出口速率为 50 GB/s，初始积压为 256 KiB，求总缓冲为 512 KiB、1 MiB 时可以容忍的反馈时延。将降速后的到达速率分别设为 50、45、40 GB/s，求排空时间，并解释其中一个条件为何无法排空。
+> 设到达速率为 80 GB/s，出口速率为 50 GB/s，初始积压为 256 KiB，求总缓冲为 512 KiB、1 MiB 时可以容忍的反馈时延。假设反馈在缓冲刚好用满时生效，将降速后的到达速率分别设为 50、45、40 GB/s，求从反馈生效到积压清空所需的时间，并解释其中一个条件为何无法排空。
 
 ### 7.5.3 多路径与重传
 
@@ -551,7 +551,7 @@
 
 取一个 8 KiB 隐藏向量，拆成八个 1 KiB 报文，轮转到两条独立的 1 GB/s 路径。每条发送四个报文，约需 4.1 μs。两条路径的传播时延都为 1 μs 时，整条消息约在 5.1 μs 交付；同样八个报文只走一条路径则约需 9.2 μs。两条路径各承担了一半发送工作，并行完成传输。[^packets]
 
-把第二条路径的传播时延改为 9 μs，它的最后一个报文在约 13.1 μs 才到达。快路径先到的后续报文需要等待缺口，接收端最多保留 3 KiB 乱序载荷。消息的总字节数相同，增加路径却比单路径更慢，因为节省的串行发送时间小于新增的路径时延。
+把第二条路径的传播时延改为 9 μs，这条路径上的最后一个报文在约 13.1 μs 才到达。快路径上提前到达的后续报文需要等待前面的缺失报文补齐，接收端最多需要缓存 3 KiB 乱序数据。消息的总字节数相同，增加路径却比单路径更慢，因为节省的串行发送时间小于新增的路径时延。
 
 ![多路径报文到达与等待缺口](ch07/figure-7-17-packets.svg)
 
@@ -565,15 +565,15 @@
 
 *图 7-36：序号 0 丢失后按算例等待时间重传，其他七个报文保留在接收端。只补发缺失报文后，约 23 μs 全部可交付。*
 
-图 7-34 中的水平线说明，快路径发完并不等于整条消息可以交给应用使用。两条路径节省了发送时间，却可能增加等待缺口的时间。可以直接求出双路径传输更快的条件。单路径需要发送八个报文，双路径每条发送四个。每报文串行发送时间为 1.024 μs，双路径节省约 4.1 μs。第二条路径比第一条多出的传播时延若超过这 4.1 μs，平均分配便失去时间优势。路径选择或不均匀分配应减少慢路径承担的报文，使两条路径的最后一个报文尽量同时到达。
-
-再让序号 0 丢失，在其原发送结束 20 μs 后重传，整条消息约在 23.0 μs 才能交付。此时其他七个报文均已到达，接收端缓存了 7 KiB 载荷，只需重传缺失的 1 KiB；若从缺口开始重发整个后缀，则再搬 8 KiB。**选择性重传**用保存接收进度的状态，换取更少的重复传输。
-
-重传还包含一个独立问题：何时认定缺口需要恢复。等待太久，真正丢包后的停顿增长；仅因慢路径上的报文暂未到达就重发，又会增加重复流量。路径时延分布决定检测需要容忍多大乱序，接收状态决定缺口后多少工作可以保留。可靠交付与第 7.4 节的应用依赖共同决定哪些已经到达的操作可以继续执行。
+图 7-34 中的水平线说明，快路径发完并不等于整条消息可以交给应用使用。两条路径节省了发送时间，却可能增加等待缺失报文的时间。可以直接求出双路径传输更快的条件。单路径需要发送八个报文，双路径每条发送四个。每报文串行发送时间为 1.024 μs，双路径节省约 4.1 μs。第二条路径比第一条多出的传播时延若超过这 4.1 μs，平均分配便失去时间优势。路径选择或不均匀分配应减少慢路径承担的报文，使两条路径的最后一个报文尽量同时到达。
+
+再让序号 0 丢失，在其原发送结束 20 μs 后重传，整条消息约在 23.0 μs 才能交付。此时其他七个报文均已到达，接收端缓存了 7 KiB 载荷，只需重传缺失的 1 KiB；若从缺失报文开始重发它及其后的所有报文，则要再传输 8 KiB。**选择性重传**用保存接收进度的状态，换取更少的重复传输。
+
+重传还包含一个独立问题：何时认定缺口需要恢复。等待太久，真正丢包后的停顿增长；仅因慢路径上的报文暂未到达就重发，又会增加重复流量。路径时延分布决定检测需要容忍多大乱序，接收端记录的状态决定能缓存多少在缺失报文之后到达的数据。可靠交付与第 7.4 节的应用依赖共同决定哪些已经到达的操作可以继续执行。
 
 > **实验 7-9 · 延伸：路径时延差与重传如何推迟按序交付**
 >
-> 将第二条路径的传播时延从 1 μs 扫描到 9 μs，求均分双路径与单路径的交点。再令两条路径分别承担五个和三个报文，求 1／9 μs 条件下的完成时间。保持首包丢失，把恢复等待从 20 μs 增为 40 μs，计算首次能够按序交付数据的时刻，以及此前需要缓存的数据量。
+> 将第二条路径的传播时延从 1 μs 扫描到 9 μs，求均分到两条路径与仅使用第一条路径的完成时间相等时，第二条路径的传播时延。再令两条路径分别承担五个和三个报文，求第一条路径传播时延为 1 μs、第二条为 9 μs 时的完成时间。保持首包丢失，把恢复等待从 20 μs 增为 40 μs，计算首次能够按序交付数据的时刻，以及此前需要缓存的数据量。
 
 ### 7.5.4 死锁
 
@@ -601,7 +601,7 @@
 
 前面沿一份数据追踪了传输、等待、完成通知和空间释放。要判断这些改进能节省多少训练时间，还必须结合产生和使用这份数据的计算过程来分析。到这里，我们已经减少了归约需要传输的数据量，配置了足够的并发请求，并让请求记录与数据缓冲区可以再次使用。最后还要确定通信在什么时候开始。某张卡进入集合调用很早，另一张卡的梯度尚未生成，前者会在调用内部等待后者。这段时间出现在通信接口中，来源却是前面的计算或数据准备。
 
-考虑四个需要进行本地归约的参与者，分别在 0、0、0、2 ms 就绪，全部到齐后执行 0.4 ms 的交换。按第 7.4 节的依赖图，交换的开始时刻等于四个就绪时刻中的最大值，全组在 2.4 ms 完成。交换速度提高一倍，完成时间变为 2.2 ms；如果四个参与者都在起点准备好，完成时间就变为 0.4 ms。两个优化分别作用于依赖图上的不同节点。[^tail]
+考虑四个需要进行本地归约的参与者，分别在 0、0、0、2 ms 就绪，全部到齐后执行 0.4 ms 的交换。按第 7.4 节的依赖图，交换的开始时刻等于四个就绪时刻中的最大值，全组在 2.4 ms 完成。将交换耗时减半，完成时间变为 2.2 ms；如果四个参与者都在起点准备好，完成时间就变为 0.4 ms。两个优化分别作用于依赖图上的不同节点。[^tail]
 
 ![就绪与交换的关键路径](ch07/figure-7-19-progress.svg)
 
@@ -691,7 +691,7 @@
 
 因此，训练和大批量 prefill 更容易从减少字节、增加带宽和重叠中获益；低并发 decode 更需要减少层内跨服务器同步、缩短发起与完成路径。这也解释了第 7.2 节讨论的 PP 方案：把一个阶段放在本地，可以减少阶段内部的远程通信；同时处理多少个独立请求，则决定流水线利用率。
 
-偶发的长时间等待也会推迟任务完成。取 100 次通信，其中 98 次为 0.4 ms，一次额外晚就绪 2 ms，一次额外恢复 10 ms。平均约为 0.52 ms，按由小到大排序的第 99 项取 p99，得到 2.4 ms。将正常传输时间减半，p99 降至 2.2 ms；若恢复事件增加为两次，p99 就由包含故障恢复的那次通信决定，变为 10.4 ms。[^tail]
+偶发的长时间等待也会推迟任务完成。取 100 次通信，其中 98 次为 0.4 ms，一次因晚就绪而多等 2 ms，一次因恢复而多等 10 ms。平均约为 0.52 ms，按由小到大排序的第 99 项取 p99，得到 2.4 ms。将正常传输时间减半，p99 降至 2.2 ms；若恢复事件增加为两次，p99 就由包含故障恢复的那次通信决定，变为 10.4 ms。[^tail]
 
 正常传输、数据准备延迟和故障恢复，都可能成为决定任务完成时间的主要因素。频繁发送的短消息需要降低单次启动开销；耗时较长的故障恢复则需要缩小故障影响范围。训练中发生通信故障后，还需要重建通信组并恢复训练状态；第 10 章将进一步说明这一过程。
 
@@ -707,7 +707,7 @@
 
 大梯度、小消息和故障恢复分别突出了带宽、启动与依赖等待。部署方案的比较也应从这些工作负载出发。**InfiniBand** 是支持低时延消息和 RDMA 的交换互联体系；**RoCE** 将 RDMA 承载在以太网上；**UB** 是前文讨论的统一互联体系。它们分别提供具体的连接与通信机制。选择部署方案时，先用本章的三个模型比较各部署方案的通信路径。GPU 到 NIC 的连接、共享接口和交换拓扑决定可用带宽；请求提交与完成处理机制决定实际吞吐；应用的同步与恢复机制决定执行顺序。
 
-计算可用带宽时，需要先明确物理端口的连接方式。例如昇腾 950 的以太网上的 UB 通信（UBoE）与 UB Link 链路静态复用串行器／解串器（SerDes，连接芯片内部并行数据与链路串行信号的电路），一组 SerDes 的用途由端口配置确定。选择端口用途之后，才能计算该方向实际拥有的链路容量。[^ubspec] 接口统一使程序使用共同的访问方式，端口配置则决定数据经过哪些链路，以及这些链路有多少带宽。
+计算可用带宽时，需要先明确物理端口的连接方式。例如，昇腾 950 中承载于以太网的 UB 通信（UBoE）与 UB Link 链路按预先配置的方式共用串行器／解串器（SerDes，连接芯片内部并行数据与链路串行信号的电路），一组 SerDes 的用途由端口配置确定。选择端口用途之后，才能计算该方向实际拥有的链路容量。[^ubspec] 接口统一使程序使用共同的访问方式，端口配置则决定数据经过哪些链路，以及这些链路有多少带宽。
 
 公开 AllReduce 运行记录可以用来分析这些因素的共同影响。16 GiB 输入、输入与输出分别使用不同缓冲区时，两台八卡 H100 服务器约需 68.7 ms，四台约需 91.0 ms。[^allreduce] 增加服务器带来了更多参与者和更长的协作路径。
 
@@ -751,7 +751,7 @@
 
 [^states]: [活跃关系与传输状态](../calculations/results/connection-states-full.md)、[八类隔离](../calculations/results/connection-states-isolated.md)。模型分别计入端点、关系绑定和传输状态三类容量。
 
-[^reclaim]: [完成通知处理与槽位释放](../calculations/results/completion-reclaim-book.md)、[增加槽位](../calculations/results/completion-reclaim-more-slots.md)。轮询周期、每次轮询处理的完成项数量与槽位寿命为教学条件。
+[^reclaim]: [完成通知处理与槽位释放](../calculations/results/completion-reclaim-book.md)、[增加槽位](../calculations/results/completion-reclaim-more-slots.md)。轮询周期、每次轮询处理的完成项数量与槽位占用时间为教学条件。
 
 [^queue]: [周期需求与队列](../calculations/results/periodic-queue-aligned.md)、[错峰](../calculations/results/periodic-queue-staggered.md)、[相位漂移](../calculations/results/periodic-queue-drift.md)。算例的到达速率按给定周期变化。
 
@@ -761,7 +761,7 @@
 
 [^feedback]: [有限缓冲与反馈延迟](../calculations/results/feedback-queue-overflow.md)。反馈时刻设为 20 μs，反馈后的发送速率按题设调整。
 
-[^packets]: [两路径均延迟](../calculations/results/packet-reorder-balanced.md)、[路径偏斜](../calculations/results/packet-reorder-skewed.md)、[指定丢包恢复](../calculations/results/packet-reorder-loss.md)。字节数按应用载荷统计。报文采用固定载荷与轮转分配，恢复时刻由题设给定；8 KiB 后缀重发用于计算额外载荷。
+[^packets]: [两路径均延迟](../calculations/results/packet-reorder-balanced.md)、[路径偏斜](../calculations/results/packet-reorder-skewed.md)、[指定丢包恢复](../calculations/results/packet-reorder-loss.md)。字节数按应用载荷统计。报文采用固定载荷与轮转分配，恢复时刻由题设给定；从缺失报文起重发的 8 KiB 数据用于计算额外载荷。
 
 [^megascale]: Jiang 等，*MegaScale: Scaling Large Language Model Training to More Than 10,000 GPUs*，NSDI 2024；[正式论文](../references/proceedings/NSDI/2024/selected/nsdi24-jiang-ziheng.pdf)、[集合通信诊断](../case-studies/collective-paths-and-diagnosis.md)。
 
