# 第 3 章《推理与训练负载》翻译腔清扫（2026-09-11）

章节文件：`manuscripts/03-推理与训练负载.md`

改动总数：20 处（其中 7 处图注类的 alt 文本与图注行同步修改，实际涉及 28 行）。所有数字、公式、单位、图片路径、脚注均未改动；已经通顺的句子一律未动。

改动分两类：句式翻译腔修改（13 处），以及按第 4 章通读确立的"图注里汉字数字改阿拉伯数字"惯例做的图注数字统一（7 处，表中以〔图注数字〕标注）。

| 修改前 | 修改后 |
|---|---|
| 〔图注数字〕…处理 2048 个新输入并生成四个输出。首输出来自 prefill，后续三次 decode 各追加一个 token…（alt 与图注行两处） | …处理 2048 个新输入并生成 4 个输出。首输出来自 prefill，后续 3 次 decode 各追加一个 token… |
| …相同两分钟总量的三个时间窗中的请求组成。…（alt 与图注行两处） | …两分钟总量相同的三个时间窗中的请求组成。… |
| 三个时间窗中的请求组成对应的输入与后续生成需求。…（alt 与图注行两处） | 三个时间窗中，请求组成对应的输入与后续生成需求。… |
| …下面按四轮成功完成原定检查的轨迹，分开统计模型时间、工具时间和等待期间保存的状态。 | …下面按四轮内成功完成原定检查的那条轨迹，分开统计模型时间、工具时间和等待期间保存的状态。 |
| 〔图注数字〕…模型先运行两秒，工具 A 用 6 秒…（alt 与图注行两处） | …模型先运行 2 秒，工具 A 用 6 秒… |
| …视觉编码内部的视觉 token 之间的交互则还要按注意力结构计算。 | …视觉编码内部 token 之间的交互则还要按注意力结构计算。 |
| 增加缓冲可以避免这次到达时间波动造成播放中断，代价是更晚开始… | 增加缓冲可以避免这次到达时间波动造成的播放中断，代价是更晚开始… |
| 〔图注数字〕每参数包括两字节计算权重和四组四字节状态，共十八字节…（alt 与图注行两处） | 每参数包括 2 字节计算权重和 4 组 4 字节状态，共 18 字节… |
| Qwen3-8B 的 8192 个 token 的输入的全参数训练。…（alt 与图注行两处） | Qwen3-8B、8192 个 token 输入的全参数训练。… |
| 强化学习（RL）通过行动得到的反馈改进后续行动。 | 强化学习（RL）用行动得到的反馈来改进后续行动。 |
| 这条训练路线已有公开实践。DeepSeek-R1 提供了从推理任务和可验证奖励出发的训练路线。 | 这条训练路线已有公开实践：DeepSeek-R1 给出了从推理任务和可验证奖励出发的路线。 |
| …有效 token 较少的微批次中的 token 就可能获得更大权重。 | …有效 token 较少的微批次里，token 就可能获得更大权重。 |
| 〔图注数字〕保持十六条有效样本目标，生成数由三十二增到六十四。（alt 与图注行两处） | 保持 16 条有效样本的目标，生成数由 32 增到 64。（同时补上"样本"后缺失的"的"） |
| 下面采用参数与数据两项都为正、训练配置和损失评估方式固定的幂律模型： | 下面采用幂律模型，其中参数与数据两项都为正，训练配置和损失评估方式固定： |
| $L$ 是验证损失，$E$ 是模型中的渐近损失，… | $L$ 是验证损失，$E$ 是渐近损失，… |
| …Chinchilla 研究用不同实验和拟合，得到模型规模与数据量应随预算更接近等比例增长的结果。 | …Chinchilla 研究用不同的实验和拟合得出：模型规模与数据量应随预算更接近等比例增长。 |
| 〔图注数字〕…六点用于拟合，两点预先留出作检验…（alt 与图注行两处） | …6 点用于拟合，2 点预先留出作检验… |
| …把更多前期工作用于提高给定规模模型的任务能力。 | …把更多前期工作用于提高给定规模下模型的任务能力。 |
| …并假设这项工作全部使用最大规模 16,384 卡，计算为 $30.84\times10^6/(16384\times24)\approx78.43$ 天。 | …并假设这项工作全部使用最大规模 16,384 卡，实际历时约为 $30.84\times10^6/(16384\times24)\approx78.43$ 天。 |
| …价格变化、模型寿命、维护与贴现等假设也应随预算说明。 | …价格变化、模型寿命、维护与贴现等假设也应在给出预算时说明。 |

## 发现但未改的句子（把握不足，留给作者定夺）

- 第 3.2.3 节"下面按四轮成功完成原定检查的轨迹"已改；同段"本章的实测任务是修复一个区间合并函数，检查返回结果是否正确、输入是否保持不变"中两个"是否"连用稍显密，但语义清楚，未动。
- 第 3.5.2 节"异步方案允许部分阶段重叠，却要处理样本版本与滞后。"——"处理样本版本与滞后"偏电报式，但上下文可解，改动可能引入歧义，未动。
- 第 3.4.2 节"序列长度还直接改变每个 token 可以访问多少前文"——"可以访问多少前文"略口语化但通顺，未动。
- 正文保留了"两轮尝试""三个中间视觉层"等汉字数字（中文行文惯例），只按第 4 章先例统一了图注中的数字。
