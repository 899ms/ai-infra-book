# 媒体共享连接与真实反馈

全部业务完成（结果标志）：**是**；全部消息交付：是。
观察截止：14s；待发送包：上行 0、下行 0。队列为空不等于业务成功。

## 业务交付与质量

| 业务 | 完成 | 必要消息齐备 | 完成时刻 | 版本可用 | 缺少依赖数 |
|---|---|---|---|---|---:|
| file | 是 | 是 | 3.25s | 不适用 | 0 |

## 实际发送与反馈

已安排发送的线上字节：2,640B；观察期内实际序列化字节：未提供B。
唯一接收业务字节：2B；完整消息交付业务字节：2B。发送 payload 可含重传，不能当作唯一接收量。

| 方向 | 发送包 | ACK 包 | STREAM 发送payload B | DATAGRAM 发送payload B | 线上 B | 实际drop | 恢复包 | probe包 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 上行 | 2 | 0 | 2 | 0 | 2,456 | 0 | 0 | 0 |
| 下行 | 2 | 2 | 0 | 0 | 184 | 0 | 0 | 0 |

发送方声明丢失的传输身份：无。纯 ACK 的声明丢失不是数据丢包；声明 loss 与网络实际 drop 分列。
实际 sender RTT 样本 2；PTO 事件 0。

## 共享资源与等待

等待记录是受阻检查次数，不是累计等待时长；同一次检查可包含多个原因。

| 受阻原因 | 记录次数 |
|---|---:|
| 无受阻记录 | 0 |

消息与任务状态计数：delivered=2。
峰值可靠接收内存：上行 2B、下行 0B。

## 输入声明与适用范围

发送调度 fifo；计算调度 fifo；控制器 reference。
声明任务 0 个、消息 2 条。计算任务耗时是输入假设，不是实测 GPU、模型或编解码延迟。

ACK 策略：immediate_each_packet。初始 cwnd 2,400B；消费延迟：不消费／不动态释放额度。

| 声明计算任务（最多12项） | 输入耗时 |
|---|---:|

这是有限单路径 QUIC 风格网络与声明媒体 DAG 的参考计算。每方向共享发送窗口、拥塞反馈与物理链路；不代表浏览器默认、真实 TCP/HTTP3 或无线媒体实现。取消、到期、版本过时与播放失败保留为业务结果。完整 JSON 保存逐包 PN、STREAM 区间、ACK、loss、RTT 和数值误差，Markdown 不展开逐包日志。

## 单共享空口分层账

声明的共享非抢占服务模型；不是实测 Wi-Fi、完整 DCF 或 TACK。

| 已观测量 | 值 |
|---|---:|
| 截止内预约占用 s | 7/2 |
| 截止内 WAN 实际 IP B | 2640 |
| 已开始 MAC 尝试 | 4 |
| 接收端已收到的尝试 | 4 |
| 已知 MAC 反馈／其中失败 | 4／0 |
| 同 PN 重试尝试 | 0 |

完整成功 PHY 账不可用：包含教学未拆分确认、失败或截止未完成的服务；不把未来整段或教学确认时间记为已观测 RF。

PHY 前导、符号填充与空等不换算为虚构 IP 字节；无线尝试字节不是唯一业务字节。
WAN 与无线可流水重叠，累计服务不可直接加到基线完成时刻。MAC 确认不等于端到端 ACK 或业务可用；逐交换证据保留在 JSON。
