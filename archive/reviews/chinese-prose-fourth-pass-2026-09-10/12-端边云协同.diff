--- before/12-端边云协同.md
+++ manuscripts/12-端边云协同.md
@@ -308,11 +308,11 @@
 
 ### 12.3.3 多流传输与优先级
 
-图片、音频和控制消息共用出口时，存在两个可改变的顺序。发送调度决定谁先占用链路；接收端的顺序要求决定哪些已到达的字节可以交给应用。前者决定链路先为谁发送数据，后者决定接收端何时把数据交给应用。
-
-先看资源分配。在一组共享链路的计算中，先进先出（FIFO）按入队顺序发送，它让图片第 5 秒完成、音频第 6 秒完成；音频优先则让音频第 3 秒完成、图片第 6 秒完成。音频早了 3 秒，图片晚了 1 秒，原因是音频把自身发送提前到原先由图片使用的时段。[^media]
-
-再看接收依赖。固定发送和丢包恢复时刻，音频自己的数据第 3 秒已经齐备，图片直到第 7 秒才齐备。这里把接收数据交给应用称为交付。整个连接共用一个接收顺序时，前方缺口会阻塞后续数据，这称为队头阻塞（head-of-line blocking）。整体有序交付使音频也要等到第 7 秒才能交给应用；逐流交付在第 3 秒就能将音频交给应用，图片仍为第 7 秒。这里节省的 4 秒来自解除音频对图片接收进度的等待，发送的数据量和链路速率都没有改变。
+图片、音频和控制消息共用出口时，存在两个可改变的顺序。发送调度决定谁先占用链路；接收端对数据顺序的要求决定哪些已到达的字节可以交给应用。前者决定链路先为谁发送数据，后者决定接收端何时把数据交给应用。
+
+先看资源分配。在一组共享链路的计算中，先进先出（FIFO）按入队顺序发送，它让图片第 5 秒完成、音频第 6 秒完成；音频优先则让音频第 3 秒完成、图片第 6 秒完成。音频早了 3 秒，图片晚了 1 秒，原因是音频数据提前占用了原先用于发送图片的时段。[^media]
+
+再看接收依赖。保持发送和丢包恢复的时刻不变，音频数据在第 3 秒已经齐备，图片直到第 7 秒才齐备。这里把接收数据交给应用称为交付。整个连接共用一个接收顺序时，前方缺口会阻塞后续数据，这称为队头阻塞（head-of-line blocking）。整体有序交付使音频也要等到第 7 秒才能交给应用；逐流交付在第 3 秒就能将音频交给应用，图片仍为第 7 秒。这里节省的 4 秒来自解除音频对图片接收进度的等待，发送的数据量和链路速率都没有改变。
 
 ![相同发送轨迹下交付顺序改变音频完成时间](ch12/figure-12-10-transport.svg)
 
@@ -334,7 +334,7 @@
 
 项目 README 描述了这一实际需求：模型运行在美国，客户端分布在不同地方。ASR 上传几百 KB 音频，返回一句识别文本；TTS 提交一段文本，返回几百 KB 音频。README 中的 TTS 场景在模型完成后集中返回音频，属于一次突发传输；12.1 节的流式场景则在生成首块后开始传输，接收与播放随生成过程持续推进。对于这类短请求，即使链路能在几毫秒内发送完全部字节，连接建立、窗口增长和丢包恢复仍可能各自引入长距离反馈等待。
 
-鹊桥的设计目标是减少这些额外等待，使短请求尽量接近必要传播、数据发送和模型处理所需的时间，同时提高长流对可用带宽的利用。它让多条应用流共享客户端—网关路径的测量与传输状态，复用连接，并根据路径情况安排发送和恢复；前向纠错通过增加冗余数据，让接收端有机会不等下一轮重传就恢复缺包。交互流与大文件共存时，还需要通过发送调度控制交互等待。前面讨论的连接、窗口和多流机制，在这里共同服务于跨地域调用。传播距离本身仍然决定必须付出的时间，优化后的完整请求能否满足业务期限，决定了集中部署是否适合这项任务。
+鹊桥的设计目标是减少这些额外等待，使短请求尽量接近必要传播、数据发送和模型处理所需的时间，同时提高长流对可用带宽的利用。它让多条应用流共享客户端—网关路径的测量结果与传输状态，复用连接，并根据路径情况安排发送和恢复；前向纠错通过增加冗余数据，让接收端有机会不等下一轮重传就恢复缺包。交互流与大文件共存时，还需要通过发送调度控制交互等待。前面讨论的连接、窗口和多流机制，在这里共同服务于跨地域调用。传播距离本身仍然决定必须付出的时间，优化后的完整请求能否满足业务期限，决定了集中部署是否适合这项任务。
 
 **跨地域语音请求为何远慢于约 240 毫秒的理想耗时？** 下面使用作者归档的语音测试检验这些机制。实际测量路径是贵阳至美国 Irvine，并非上面的美东—美西部署示例所用路径。固定音频文件约 355 KB，链路 333 Mbit/s，往返约 200 ms，模型处理约需 30 ms；计时从发起请求开始，到收到结果结束。理想发送时间约为 8.5 ms，加上传播与处理约为 240 ms。最初的直接调用却超过一秒，而 Queqiao 约需三百毫秒。
 
@@ -361,7 +361,7 @@
 
 *图 12-20：保持连接并调优后，同一固定音频的直接路径与 Queqiao 中位数分别为 240.9、236.5 ms。与前图采用相同纵轴，两条路径均接近数据发送、必要往返和处理所需的总时间。*
 
-**单因素实验怎样区分连接、窗口与发送节奏的影响？** 连接复用改变请求开始时是否需要握手，窗口大小改变确认返回前允许在途的数据量，发送节奏改变链路的空闲区间。图 12-21 将这三类设置与对应事件配对：固定文件、路径和其余设置，每次改变一个因素，记录该因素影响的事件时刻与请求总时间。连接实验比较握手结束和首字节发送时刻，窗口实验比较在途量与 ACK 到达后的发送，节奏实验比较两次发送之间的空闲时间。
+**单因素实验怎样区分连接、窗口与发送节奏的影响？** 连接复用改变请求开始时是否需要握手，窗口大小改变确认返回前允许在途的数据量，发送节奏改变链路的空闲区间。图 12-21 将这三类设置与对应事件配对：固定文件、路径和其余设置，每次改变一个因素，记录受其影响的事件发生时刻与请求总时间。连接实验比较握手结束和首字节发送时刻，窗口实验比较在途量与 ACK 到达后的发送，节奏实验比较两次发送之间的空闲时间。
 
 ![单因素实验方法：保持相同文件、路径和其他设置，每次只改变一个因素，对照中间事件与总完成时间](ch12/figure-12-experiment-design.svg)
 
@@ -411,7 +411,7 @@
 
 例如，八块音频各长 20 ms，计划从第 0.4 秒开始连续播放，共 0.16 秒。让这些音频与大图共用链路时，FIFO 可使这些块最终全部到达，却全部错过各自预定的播放时刻；媒体优先则使八块都能按时到达。[^mixed] 两种情况下最终收到的字节数相同，按时可用的声音却分别为零与 0.16 秒。调度时统计按时播放的内容，才能反映用户实际听到了多少声音。
 
-取消会让尚未播放的数据立即失去价值。播放器若排入三块 20 ms 音频，即使远端停止生成，本地仍可继续播出 60 ms。若控制消息需要 100 ms 才能到达服务端，那么在它到达前，远端还会产生新块。取消过程因此分成两条并行路径：本地清空待播缓冲，远端停止生成和发送后续音频。前者决定用户何时不再听到声音，后者决定何时停止消耗资源。
+取消会让尚未播放的数据立即失去价值。播放器的待播队列中若已有三块 20 ms 音频，即使远端停止生成，本地仍可继续播出 60 ms。若控制消息需要 100 ms 才能到达服务端，那么在它到达前，远端还会产生新块。取消过程因此分成两条并行路径：本地清空待播缓冲，远端停止生成和发送后续音频。前者决定用户何时不再听到声音，后者决定何时停止消耗资源。
 
 ![一次取消同时触发两条路径：本地清空待播音频，远端在控制消息到达后停止生成和发送](ch12/figure-12-cancel-paths.svg)
 
@@ -485,7 +485,7 @@
 \end{aligned}
 $$
 
-云端每轮比附近工作站少算 0.7 秒，二十轮省 14 秒，准备再省 2 秒；但上传每轮多 0.92 秒，传播多 0.18 秒，二十轮多等 22 秒。合计下来，云端反而慢 6 秒。把时间差拆开，便能看到更快的模型怎样因每轮通信而失去优势。
+云端每轮的模型计算比附近工作站少用 0.7 秒，二十轮省 14 秒，准备再省 2 秒；但上传每轮多 0.92 秒，传播多 0.18 秒，二十轮多等 22 秒。合计下来，云端反而慢 6 秒。把时间差拆开，便能看到更快的模型怎样因每轮通信而失去优势。
 
 图 12-26 至图 12-28 汇总了三种部署方式下完整任务的各阶段耗时。云端的绿色计算段更短，蓝色上传段却更长；两段与其余工作依次累加，决定横条终点所示的任务完成时间。
 
@@ -509,7 +509,7 @@
 T_{\mathrm{cloud}}(b)=27+\frac{128}{b}\ \mathrm{s}.
 $$
 
-27 秒来自一次准备和二十轮非上传工作，128 Mbit 是二十张截图的总上传量。满足期限需要 $b\ge128/18\approx7.1$ Mbit/s；比附近的 41 秒更快需要 $b>128/14\approx9.1$ Mbit/s。上行 8 Mbit/s 时，云端任务需 43 秒，已经按时且更便宜；10 Mbit/s 时约 39.8 秒，才同时更快。
+27 秒来自一次准备和二十轮非上传工作，128 Mbit 是二十张截图的总上传量。满足期限需要 $b\ge128/18\approx7.1$ Mbit/s；比附近的 41 秒更快需要 $b>128/14\approx9.1$ Mbit/s。上行 8 Mbit/s 时，云端任务需 43 秒，已经能按时完成，成本也更低；10 Mbit/s 时约 39.8 秒，才同时更快。
 
 ![云路径上行决定部署方案何时满足期限和何时更快](ch12/figure-12-15-deployment.svg)
 
@@ -545,7 +545,7 @@
 
 本例应把剩余二十轮放到附近工作站：41 秒完成，成本 0.080。与继续使用原服务的 70 秒相比，省 29 秒。终端继续采集并执行操作，工作站负责模型判断，每轮只传输截图和控制结果。
 
-这个决定来自对三种部署方案的逐项比较。端侧先因 64 秒超期被排除；云端虽然模型更快、成本更低，但原上行使它需要 47 秒；附近工作站虽然执行模型较慢，但通信时间较短，整个任务反而更快完成。前面的压缩、特征与状态分析则给出了进一步改进的方法：压缩截图可以减少上传时间，上传完整特征会增加传输时间，保留已提交的进度则可以减少恢复后的重复工作。
+这个决定来自对三种部署方案的逐项比较。端侧先因 64 秒超期被排除；云端虽然模型更快、成本更低，但受原有上行速率限制，仍需 47 秒；附近工作站虽然执行模型较慢，但通信时间较短，整个任务反而更快完成。前面的压缩、特征与状态分析则给出了进一步改进的方法：压缩截图可以减少上传时间，上传完整特征会增加传输时间，保留已提交的进度则可以减少恢复后的重复工作。
 
 | 条件变化 | 结果 | 决策 |
 |---|---|---|
@@ -567,7 +567,7 @@
 
 V4 到 V4.1 展示了模型侧的同类调整。跨层共享减少全局 KV 的重复保存，使模型可以用更细的粒度保存历史信息；CED 减少输入阶段反复经过解码器的工作，缓存恢复则缩短多轮会话重新开始的路径。[^v41-case] 这些改变分别作用于容量、输入计算和恢复等待，可以沿同一任务逐项计入预算。
 
-从片上存储到端边云，设计选择始终依赖对完整执行过程的了解。注意力的归约关系使中间矩阵可以按块处理，重复的模型结构使执行计划可以复用，前缀与分支关系使上下文可以共享，工具等待则为状态换出和环境准备提供了时间。每一项改进都把模型或任务的信息用于其他层次，重新安排数据保存位置、计算划分和交接方式。
+从片上存储到端边云，设计选择始终依赖对完整执行过程的了解。注意力的归约关系使中间矩阵可以按块处理，重复的模型结构使执行计划可以复用，前缀与分支关系使上下文可以共享，工具等待则为状态换出和环境准备提供了时间。每一项改进都利用模型或任务的信息，指导其他层次的设计，重新安排数据保存位置、计算划分和交接方式。
 
 ![图 12-32 固定任务轨迹中的加速上限。模型从 8 秒缩短至 0.8 秒，其他串行阶段保持 2 秒；第三行表示模型时间趋近于零的理想下界。](ch12/figure-12-task-counterfactual.svg)
 
@@ -581,7 +581,7 @@
 
 (a) 上行速率分别取 10、20、100 Mbit/s，计算三种方案的完成时间：保持原方案、将图像处理速度提高到原来的十倍，以及将输入大小减半。输入压缩所增加的编解码耗时仍为 0.15 秒。
 
-(b) 求压缩节省的传输时间恰好等于新增编解码时间时的上行速率。另保持原图大小和 20 Mbit/s 上行速率不变。若 0.3 秒处理时间中只有 0.2 秒可以加速，将这部分的执行速度提高到原来的十倍后，任务总耗时是多少？
+(b) 求压缩节省的传输时间恰好等于新增编解码时间时的上行速率。再以原图大小和 20 Mbit/s 上行速率为条件：若原有的 0.3 秒处理时间中只有 0.2 秒可以加速，将这部分的执行速度提高到原来的十倍后，任务总耗时是多少？
 
 (c) 把图像分为三个可独立处理的块，画出上传、处理与回传的重叠。标出限制整个任务进一步缩短耗时的步骤。
 
@@ -595,7 +595,7 @@
 
 (d) 为截图版本与操作编号设计记录字段，说明怎样识别压缩导致的纠错轮次与旧状态导致的无效轮次。
 
-**12-3　AI 助手的跨设备分工〔核心，★〕。**
+**12-3　视觉编码位置与状态迁移如何影响交互耗时〔核心，★〕。**
 
 (a) 一张图片占 0.8 MB，编码后得到形状为 $[400,10240]$ 的 BF16 特征张量，上行带宽为 6.4 Mbit/s。本地编码耗时 0.12 秒，远端编码耗时 0.04 秒。比较本地编码后上传特征与上传原图后在远端编码的总耗时。
 
@@ -643,7 +643,7 @@
 
 (d) 使用配套音频记录，画出一个块从生成到播放的各个时刻，说明如何区分生成不及时、传输延迟与本地调度等待。
 
-**12-8　跨地域计算与数据成本〔延伸，★★〕。**
+**12-8　上行带宽与故障恢复如何改变跨地域部署的时间和成本〔延伸，★★〕。**
 
 (a) 复算剩余二十轮任务在三种部署方案下的时间与成本，再求云端按期完成、以及比附近工作站更快所需的上行带宽。
 
@@ -651,7 +651,9 @@
 
 (c) 二十轮交互中，十轮的上行速率为 6 Mbit/s，另外十轮为 10 Mbit/s，求云端的总时间，与恒定 8 Mbit/s 比较并解释差额。
 
-(d) 对十轮任务，设第八轮已执行完时断连，恢复 2 秒。分别考虑两种恢复情况：已保留前七轮的进度，只需重做第八轮；前八轮的进度全部丢失，需要全部重做。计算两种情况下完成剩余工作所需的时间，以及整项任务的总耗时。这次断连发生在远程服务路径，端侧继续正常执行；恢复成本包含在基础费率中。连接中断期间保留服务状态，连接恢复后继续使用，初始化只做一次；成本按实际执行轮次及发送字节计收，按原上传单价重新判断最低成本的可行方案。
+(d) 对十轮任务，设第八轮刚执行完就发生连接中断，恢复连接需要 2 秒。分别考虑两种恢复情况：已保留前七轮的进度，只需重做第八轮；前八轮的进度全部丢失，需要全部重做。计算两种情况下完成剩余工作所需的时间，以及整项任务的总耗时。
+
+本小题仅假设远程服务发生断连，端侧方案仍正常执行。远程服务在断连期间保留运行状态，连接恢复后继续使用，因此无需重新初始化；任务进度则按上述两种情况分别恢复。恢复成本已包含在基础费率中，其余成本按实际执行轮次和发送字节数计算。采用原上传单价，重新选择满足期限且成本最低的方案。
 
 [^raw]: [RAW 图片精修案例与条件预算](../case-studies/raw-retouching.md)。算例设原图 30 MB、成片 5 MB、图像处理耗时 0.3 秒。
 
