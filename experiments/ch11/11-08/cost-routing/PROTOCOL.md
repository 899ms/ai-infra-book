# Declared-price quality-constrained routing replay

Use cache-queue32 target records and model-choice-moe8 target records for the same task/checker. Prices are hypothetical teaching units per million tokens: Qwen3-8B input0.1/cached0.025/output0.3;30BMoE input0.4/cached0.1/output1.2. These are not provider prices or current market recommendations. Reasoning-before-close is a subset of total generated tokens, billed once under output. Charge failed attempts. Report target-only, warmup and background costs separately; the experiment warms each case independently, so warmup is not silently amortized.

Candidate recipes in fixed tie order:8B no-thinking1000,8B thinking100,8B thinking1000,8B thinking4096,MoE no-thinking1000. Fixed route=8B thinking1000. Lowest output-token unit-price route chooses minimum price, ties by that declared order. Quality-constrained selection uses repetition0 only, separately for each cache/queue state; admit recipes that pass all six checks, then choose minimum estimated target+warmup cost from repetition0, tie by declared order. Evaluate on repetition1 and charge the actual chosen attempt. If no recipe passes calibration, report no feasible route, not an invented successful cost. This is offline recipe selection on one known task, not production routing or a general quality estimate.

For100 versus1000 reasoning tokens, record the hypothetical marginal900*declared_output_price/1e6 if answer tokens and everything else are fixed, explicitly separate from actual total-generation caps. For warm-but-queued, compare native observed queue and full completion against idle cold for the same recipe/rep; no assumption cache hit implies faster completion. Real provider bill fields stay null; requested success cost is reported only in declared teaching units when successes exist.

Declared token prices cover model usage only; validation compute, model startup, taxes and other fees are not priced. Reported target completion starts at target submission; warmup duration is separate. No full-service invoice or initialization-inclusive latency is implied.

Additional preregistered recipe: MoE_feedback allows one retry from actual failed checks using the same model prices. Append it last in tie order. Charge both target calls plus initial warmup, retain initial failure, and evaluate eight logical tasks independently from one-shot MoE. It is selected on repetition0 and evaluated on repetition1 like the other recipes. Background load occurs on the initial attempt only.

Report calibration dataset acquisition cost separately from evaluated selections. Abstention may issue no evaluation request but is not described as a free experiment; all calibration attempts and all dataset background load remain priced separately.
