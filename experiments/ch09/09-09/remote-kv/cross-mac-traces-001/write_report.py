import json,hashlib
from pathlib import Path
R=Path(__file__).resolve().parent
for directory,title in [(R,'Default-threshold multi-turn remote cache'),(R.parent/'cross-mac-chat-threshold16-001','Short-Chat remote prefetch at 16 tokens')]:
 if not (directory/'summary.json').exists():continue
 s=json.loads((directory/'summary.json').read_text())
 text=f'# {title}\n\nActual Qwen3-8B BF16 SGLang producer and fresh consumer engines on RTX PRO, with native KV pages stored on the Mac over SSH. Fixed recorded inputs, one generated token per turn. All paired outputs equal: {s["all_paired_outputs_equal"]}. Published pages: {s["published_pages"]}; GET page operations: {s["get_operations"]}; conflicting duplicate writes: {s["conflicting_duplicate_writes"]}.\n\n'
 text+='| Phase | Requests | Request hit rate | Token-weighted hit rate | Device / host / storage hit tokens | Sum of request seconds | Requests with GET | Sample p95 GET sum per fetching request |\n|---|---:|---:|---:|---|---:|---:|---:|\n'
 for name,p in s['phases'].items():
  l=p['levels'];v=p['per_request_get_sum_p95_s'];value=f'{v:.6f} s' if v is not None else 'N/A'
  text+=f'| {name} | {p["requests"]} | {p["request_hit_fraction"]:.2%} | {p["token_hit_fraction"]:.2%} | {l["device"]} / {l["host"]} / {l["storage"]} | {p["wall_s"]:.6f} | {p["requests_with_remote_get"]} | {value} |\n'
 text+='\nAll fetched page hashes match the Mac files. Native chained page hashes are independently recalculated from the exact input token IDs; each publication barrier confirms the required keys. Every client GET is assigned to a request interval on the same remote monotonic clock. GET time excludes existence checks, queueing and model computation; no clocks are subtracted across hosts. Per-turn records include the replay interval since the previous response, which includes publication instrumentation and is not original user/tool think time.\n\n'
 if directory==R:text+='The installed backend defaults to a 256-token prefetch threshold (source hash and matching lines in installed-threshold-source.json). All Chat inputs are shorter than 256, so the default consumer performs no remote GET. The Agent consumer fetches 110 pages in five turns; higher token hit rate accompanies much longer measured request time. The producer already reuses device prefixes across turns and is not an independent cold control. The separate threshold16 Chat variant and cache-disabled control address these distinctions.\n\n'
 else:text+='This separate run changes prefetch_threshold to16 and starts from a new empty Mac store. Compare with the preserved default-threshold Chat run. It tests admission of short prefixes; the default is not silently changed in the original records.\n\n'
 text+='All engines and SSH commands exited zero, final GPU compute-process listings were empty, and each Mac store was terminated and waited. Raw responses, per-page client traces, Mac ledger, publication barriers and source hashes are retained.\n\nScope: one replay per trace/configuration; quantiles use nearest rank and are descriptive small-sample values. One-token outputs cannot establish task quality; source Agent task failure and Chat arithmetic errors remain documented. These results add actual remote retrieval to the existing routing evidence, but the matched remote-aware three-policy comparison remains pending.\n\nRecheck from repository root with `python3 '+str(directory.relative_to(Path.cwd()) if directory.is_relative_to(Path.cwd()) else directory)+'/analyze.py`.\n'
 (directory/'README.md').write_text(text)
 files=[p for p in directory.rglob('*') if p.is_file() and p.name!='manifest.json' and '__pycache__' not in p.parts]
 (directory/'manifest.json').write_text(json.dumps({str(p.relative_to(directory)):hashlib.sha256(p.read_bytes()).hexdigest() for p in files},indent=2)+'\n')
